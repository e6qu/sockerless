# Known runtime spec-shape violations — the burn-down list.
#
# Each line is a violation key (op<TAB>kind<TAB>field, array indices collapsed
# to []) recorded by SOCKERLESS_SPEC_VALIDATE and accepted by
# scripts/check-spec-violations.sh. Every entry carries a BUG ID; this list only
# shrinks — fixing the simulator (or bumping the pinned spec snapshot) removes
# lines, never the other way around.

# BUG-2090: the ECS Express `taskDefinitionArn` field IS real — it is a member of
# ExpressGatewayServiceConfiguration in aws-sdk-go-v2/service/ecs@v1.85.0 ("present
# for all Express services …") and the AWS API reference — so the simulator emits
# it faithfully. The pinned ecs.smithy snapshot (specs/cloud-api/aws/SOURCES.md,
# SHA 0016334f, 2026-06-11) predates the field's addition to that shape, so the
# ratchet flags it. Remove these lines when the ecs spec snapshot is next bumped.
AmazonEC2ContainerServiceV20141113.CreateExpressGatewayService	unknown-field	$.service.activeConfigurations[].taskDefinitionArn
AmazonEC2ContainerServiceV20141113.DescribeExpressGatewayService	unknown-field	$.service.activeConfigurations[].taskDefinitionArn
AmazonEC2ContainerServiceV20141113.DeleteExpressGatewayService	unknown-field	$.service.activeConfigurations[].taskDefinitionArn
AmazonEC2ContainerServiceV20141113.UpdateExpressGatewayService	unknown-field	$.service.targetConfiguration.taskDefinitionArn
