# E2E-004: GitLab runner pipelines (12)

**Status:** DONE
**Phase:** 13 — E2E Runner Tests
**Effort:** S

## What Was Built

12 GitLab CI pipeline YAML files in `tests/e2e-live-tests/gitlab-runner-docker/pipelines/`:

1. basic — single echo job
2. multi-step — multiple script lines
3. env-vars — global/job variables, CI_* variables
4. exit-codes — exit 1 with allow_failure
5. before-after — before_script + script + after_script
6. multi-stage — build/test/deploy stages
7. artifacts — artifacts:paths between jobs (SKIP for FaaS)
8. services — job with service container (SKIP for FaaS)
9. large-output — 1000-line stdout
10. parallel-jobs — 3 jobs in same stage
11. custom-image — python:3-alpine
12. timeout — timeout: 30s
