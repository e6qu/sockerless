# P66-001: Tracer Initialization + go.mod Updates

**Status:** Done
**Phase:** 66 — OpenTelemetry Tracing

## What
Created 4 identical `otel.go` files (backends/core, frontends/docker, bleephub, gitlabhub) with `InitTracer(serviceName)` function. Checks `OTEL_EXPORTER_OTLP_ENDPOINT` env var — if set, creates OTLP HTTP exporter + TracerProvider with batching and sets global provider; if unset, returns no-op shutdown func.

Updated 4 `cmd/main.go` files to call `InitTracer` and defer shutdown. Added OTel dependencies to 4 go.mod files. Ran `go work sync`.

## Tests
All modules build clean (`go build ./...`).
