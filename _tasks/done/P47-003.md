# P47-003: Wire LoadContextEnv Into Backends

**Status:** Done

Added `core.LoadContextEnv(logger)` call to all 8 backend/frontend main.go files:
- 6 cloud backends (ECS, Lambda, Cloud Run, GCF, ACA, AZF): before `ConfigFromEnv()`
- Memory backend: before `NewServer()`
- Docker frontend: before `NewServer()`, added `backend-core` + `agent` to go.mod/replace

All 8 binaries compile successfully.
