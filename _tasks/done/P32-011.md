# P32-011: Full verification and state update

**Phase:** 32C (Verification)
**Depends on:** P32-005, P32-006, P32-007, P32-008, P32-009, P32-010
**Estimated effort:** M

---

## Description

Run the full test matrix to verify zero regressions from all Phase 32 changes. Update all project state files.

## Verification Steps

### 1. Unit tests and lint
```bash
make test
make lint
```

### 2. Sandbox tests
```bash
cd sandbox && go test -v ./...
```
Expect: 46 PASS

### 3. Sim-test-all
```bash
make sim-test-all
```
Expect: 129 PASS / 0 FAIL

### 4. Driver bypass audit
```bash
grep -rn 'ProcessFactory' backends/core/handle_*.go
grep -rn 'Store\.Processes' backends/core/handle_*.go
```
Both must return 0 results.

### 5. File size audit
```bash
wc -l backends/core/handle_*.go backends/core/drivers*.go sandbox/*.go frontends/docker/*.go | sort -rn | head -20
```
No file should exceed 400 lines.

### 6. Upstream act (if Docker available)

Build and run upstream act memory tests to verify 91/24 is maintained:
```bash
make upstream-test-act
```

## State Updates

### Update these files:

1. **`PLAN.md`** — Mark Phase 32 as complete, add completion summary
2. **`STATUS.md`** — Update phase count, add note about driver interface completion
3. **`WHAT_WE_DID.md`** — Add Phase 32 section describing the refactoring
4. **`DO_NEXT.md`** — Update immediate priority to Phase 33 (Service Containers)
5. **`_tasks/done/`** — Move all P32-*.md to done/ with status updates
6. **`MEMORY.md`** — Update with any new learnings from this phase

### Checklist for each state file:
- [ ] `PLAN.md` updated
- [ ] `STATUS.md` updated
- [ ] `WHAT_WE_DID.md` updated
- [ ] `DO_NEXT.md` updated
- [ ] All P32-*.md moved to `_tasks/done/`
- [ ] `MEMORY.md` updated

## Acceptance Criteria

1. All tests pass with zero regressions
2. No handler file references `ProcessFactory` or `Store.Processes`
3. No source file exceeds 400 lines
4. All 6 state files updated
5. Upstream act memory: 91 PASS / 24 FAIL (no regressions)

## After Completing

This is the final task of Phase 32. All state files should be updated as part of this task.
