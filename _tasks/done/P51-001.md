# P51-001: Parse services from Workflow YAML

**Status:** Done
**Phase:** 51 — CI Service Container Integration

Added `ServiceDef` struct and `Services` field to `JobDef`/`rawJobDef`. Parse `services:` in `normalizeJob()` — each value can be a plain string (just image name) or a full object (image, env, ports, volumes, options, credentials). 4 new unit tests: object form, string shorthand, full spec with env/ports/volumes, backward compat (no services).
