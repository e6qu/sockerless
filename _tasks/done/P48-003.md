# P48-003: Frontend Management API

**Status:** Done
**Phase:** 48 — Private API Skeleton + Health + Status

Created `frontends/docker/mgmt.go` with `MgmtServer` — separate HTTP listener on `:9080` (configurable via `--mgmt-addr`). Endpoints: `GET /healthz`, `GET /status`. Added `--mgmt-addr` flag to frontend `cmd/main.go`. MgmtServer started as goroutine alongside Docker API.
