# P22-002: Harden matrix.yml with exact value verification

**Component:** E2E Tests
**Phase:** 22
**Estimated effort:** S
**Status:** DONE

---

## Description
Replaced weak `test -n "$GREETING"` assertion with exact value verification (`test "$GREETING" = "hello" || test "$GREETING" = "world"`) and file round-trip check.
