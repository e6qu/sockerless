# P58-002: Comprehensive Test Workflow

Created `.github/workflows/test-comprehensive.yml` — triggers on push to `main` and `workflow_dispatch` with suite selector.

Jobs: simulator-tests (3 clouds), bleephub-integration (integration + gh-cli), e2e-github (memory), e2e-gitlab (memory), terraform-tests (manual only), upstream-act (manual, continue-on-error).
