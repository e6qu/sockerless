# P46-001 — StartedIDs + PodDeferredStart helper

**Status:** Done
**Date:** 2026-02-22

## What
Added `StartedIDs` field to `PodContext`, `MarkStarted()` method to `PodRegistry`, and `PodDeferredStart()` helper on `BaseServer` in new file `backends/core/pod_materialize.go`.
