# P22-003: Remove dead test files

**Component:** E2E Tests
**Phase:** 22
**Estimated effort:** S
**Status:** DONE

---

## Description
Deleted 5 never-executed test files (services.yml, custom-image.yml, container-action.yml for GitHub; services.yml, custom-image.yml for GitLab). Removed "services" and "custom-image" from GitLab ALL_PIPELINES. All backends route to WASM variants via get_test_variant().
