# P26-006: Expand gitlab-ci-local to multi-backend

**Status:** DONE
**Phase:** 26 — GitLab Real Execution + Upstream Multi-Backend Coverage

## Changes
- `tests/upstream/gitlab-ci-local/Dockerfile`: Full multi-backend build (all 3 simulators, all 7 backends, frontend, agent). Copies `lib.sh` and `start-backend.sh`.
- `tests/upstream/gitlab-ci-local/run.sh`: Added `--backend` parameter (default: memory). Non-memory backends source `start-backend.sh`.
