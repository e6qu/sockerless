# P58-007: Release Workflow

Created `.github/workflows/release.yml` — triggers on `v*` tags.

Pipeline: ci (reusable ci.yml) → test-gate (bleephub unit + sim integration) → release (goreleaser with `fetch-depth: 0`).
Docker images publish via `docker.yml` (also triggered by tags).
