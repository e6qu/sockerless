# P53-003 — Enable Registry Config Fetch by Default

**Status:** Done
**Phase:** 53 — Production Docker API: Code-First

Removed `FetchImageConfigEnabled()`. Registry config fetch is now always attempted by default. Opt-out via `SOCKERLESS_SKIP_IMAGE_CONFIG=true`. E2E tests set this env var to preserve synthetic behavior and avoid network timeouts. Updated existing registry tests to use new env var.

**Files modified:** `backends/core/registry.go`, `backends/core/registry_test.go`, `tests/e2e-live-tests/lib.sh`
