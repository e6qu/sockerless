# P59-002 — Secrets Injection & Masking
**Status:** Done
**Summary:** Injected secrets into the runner context as `secrets.*` variables and added a mask array to job messages so the runner redacts secret values from log output.
