# P50-002: HealthChecker + /check Endpoint

**Status:** Done
**Phase:** 50 — Resource Management + Permissions + Live Reload

Added `HealthChecker` interface (`RunChecks(ctx) []CheckResult`) to `backends/core/handle_mgmt.go`. `GET /internal/v1/check` runs store check, registry check, and optional backend-specific checks. `HealthChecker` field on `BaseServer` — backends can wire their own checker. 2 new tests (with mock checker, without checker).
