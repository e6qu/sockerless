# P25-001: Validate Phase 24 fixes — rebuild + run upstream act tests

**Status:** DONE
**Phase:** 25

## Summary

Rebuilt Docker image and ran `make upstream-test-act` to validate Phase 24 sandbox builtins against upstream act test suite.

## Results

- **TestRunEvent Docker mode:** 56 PASS / 31 FAIL (was 53/34, **+3**)
- **TestRunEventHostEnvironment:** 35 PASS / 8 FAIL
- **Total across all test functions:** 93 PASS / 41 FAIL

### Newly passing tests (+3)
1. `shells/defaults` — $BASH/$SHELL env vars fix
2. `shells/bash` — $BASH/$SHELL env vars fix
3. `container-hostname` — hostname builtin + HOSTNAME env injection

### Expected fixes that did NOT work (5)
- `env-and-path` — busybox builtins override PATH scripts (not a mktemp issue)
- `environment-files` — same as env-and-path
- `defaults-run` — `/tmp` dir doesn't exist in WASM virtual FS (not just bash env)
- `workdir` — `/root`, `/tmp` missing in WASM FS + bash `[[ ]]` syntax
- `docker-action-custom-path` — docker action image uses `bash` entrypoint (not mv issue)

## Files Modified
- `tests/upstream/act/RESULTS.md` — Updated with 56/31 results, recategorized failures
