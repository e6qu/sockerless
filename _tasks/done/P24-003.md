# P24-003: Extend uname builtin with individual flags

**Status:** DONE
**Phase:** 24

Extended the `uname` builtin to parse individual flags: `-s` (Linux), `-n` (hostname from `HOSTNAME` env or "sandbox"), `-r` (5.15.0), `-m` (architecture). `-a` now uses hostname. Prerequisite for container-hostname test.

**Files changed:**
- `sandbox/shell.go` — Rewrote `uname` case with per-flag parsing
- `sandbox/sandbox_test.go` — Added `TestUnameFlags`
