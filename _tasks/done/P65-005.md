# P65-005: Tests

**Status:** Done
**Phase:** 65 — GitHub Apps

## What
Created `bleephub/gh_apps_test.go` with 23 tests total: 11 unit tests (store CRUD, JWT sign/verify/expired/long-lifetime/wrong-ID/bad-sig, token generation/expiry, manifest code one-time-use, delete installation) + 12 integration tests (HTTP endpoints for app creation, manifest flow, JWT auth, installation CRUD, installation token creation/auth, cross-app rejection, repo installation, PAT backward compat).

## Verification
`go test -race -count=1 ./...` → 293 PASS (270 prior + 23 new), 0 FAIL
