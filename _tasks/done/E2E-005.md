# E2E-005: GitLab runner Docker Compose + Dockerfiles

**Status:** DONE
**Phase:** 13 — E2E Runner Tests
**Effort:** M

## What Was Built

- `tests/e2e-live-tests/gitlab-runner-docker/docker-compose.yml` — Services: gitlab (CE), sockerless-backend (all-in-one with start-backend.sh), orchestrator. BACKEND/MODE/PIPELINE env vars for runtime selection.
- `tests/e2e-live-tests/gitlab-runner-docker/Dockerfile.backend` — Multi-stage: builds all 7 backends + 3 simulators + frontend. Runtime stage is debian:bookworm-slim with curl.
- `tests/e2e-live-tests/gitlab-runner-docker/Dockerfile.orchestrator` — gitlab-runner image + orchestrate.sh + all pipeline YAML files.
