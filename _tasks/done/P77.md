# Phase 77 — gitlabhub Dashboard (GitLab CI)

**Completed**: 2026-03-02

## Tasks

| Task | Description |
|---|---|
| P77-001 | TypeScript types (`ui/packages/gitlabhub/src/types.ts`) — Pipeline, Job, Runner, Project, Metrics, Status, Health |
| P77-002 | Go management endpoints (`gitlabhub/handle_mgmt.go`) — 5 endpoints: pipelines list/detail/logs, runners, projects |
| P77-003 | API client (`ui/packages/gitlabhub/src/api.ts`) — 8 typed fetch functions |
| P77-004 | SPA scaffolding — package.json, vite config, App.tsx with BrowserRouter + AppShell |
| P77-005 | Pipeline pages — PipelinesPage (DataTable, 3s refetch) + PipelineDetailPage (stage columns, LogViewer) |
| P77-006 | Job log viewer — reused LogViewer from core, TraceData split server-side |
| P77-007 | Overview, Runners, Projects, Metrics pages |
| P77-008 | Go embed (`ui_embed.go`, `ui_noembed.go`), Makefile (MODULES→MODULES_UI), CI, .gitignore, Dockerfile.release |
| P77-009 | Tests: 7 Go mgmt + 16 Vitest + 5 Playwright E2E |
| P77-010 | State save |

## Files Created

- `gitlabhub/handle_mgmt.go` — management API endpoints
- `gitlabhub/handle_mgmt_test.go` — 7 tests
- `gitlabhub/ui_embed.go` — SPA handler with `//go:embed all:dist`
- `gitlabhub/ui_noembed.go` — no-op registerUI
- `ui/packages/gitlabhub/` — 26 files (SPA, pages, tests, E2E, config)

## Files Modified

- `gitlabhub/server.go` — added registerMgmtRoutes, registerUI, GET /{$} redirect
- `gitlabhub/Dockerfile.release` — added `-tags noui`
- `Makefile` — MODULES→MODULES_UI, dist copy, build/e2e targets, test with noui
- `.gitignore` — gitlabhub/dist/, playwright artifacts
- `.github/workflows/ci.yml` — build→noui for gitlabhub, -tags noui in test

## Test Results

- Go: 136 PASS (129 existing + 7 new mgmt tests)
- Vitest: 16 PASS (4 Overview + 3 Pipelines + 3 PipelineDetail + 3 Runners + 3 Metrics)
- Playwright E2E: 5 tests (redirect, title, nav links, metrics cards, navigation)
- UI build: 16 packages build (15 existing + gitlabhub)
- Lint: 0 issues
