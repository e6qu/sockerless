# P72-001: ECS Arithmetic Integration Tests

**Phase**: 72 (Full-Stack E2E Tests)
**Status**: ✅ Complete

## What
Added 6 arithmetic evaluator integration tests to `backends/ecs/` that exercise real execution through the full Docker API → ECS backend → AWS simulator stack.

## Tests Added
- `TestECSArithmeticSuccess` — `3 + 4 * 2` → exit 0, logs "11"
- `TestECSArithmeticParentheses` — `(3 + 4) * 2` → exit 0, logs "14"
- `TestECSArithmeticInvalid` — `3 +` → exit 1, logs "ERROR"
- `TestECSArithmeticDivision` — `10 / 3` → exit 0, logs "3.333"
- `TestECSArithmeticWithLabels` — `100 - 42` → exit 0, logs "58", label filter
- `TestECSArithmeticEnvVar` — `(3 + 4) * 2` with EXPR env → exit 0, logs "14"

## Files
- `backends/ecs/arithmetic_integration_test.go` — new (6 tests)
- `backends/ecs/integration_test.go` — modified (eval binary build in TestMain)
