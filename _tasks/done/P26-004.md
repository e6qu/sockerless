# P26-004: Add sandbox stdin unit tests

**Status:** DONE
**Phase:** 26 — GitLab Real Execution + Upstream Multi-Backend Coverage

## Changes
- `sandbox/sandbox_test.go`: Added 3 new tests:
  - `TestProcessStdinPipe` — Creates `sh` process, writes script to stdin, verifies output
  - `TestProcessStdinEOF` — Writes partial input, closes writer, verifies shell exits
  - `TestProcessStdinWithExec` — stdin-attached process + concurrent RunExec both work
- Result: 29 PASS total (26 existing + 3 new)
