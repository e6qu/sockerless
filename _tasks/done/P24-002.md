# P24-002: Add mktemp shell builtin

**Status:** DONE
**Phase:** 24

Added `mktemp` as a shell builtin in `wasmExecHandler`. Supports `-d` (directory mode) and `-p DIR` (parent directory). Creates temp files/dirs in `rootDir/tmp/` using `os.MkdirTemp`/`os.CreateTemp`, prints container-relative path.

**Files changed:**
- `sandbox/shell.go` — Added `mktemp` case in builtin switch, added to `builtinCommands` map
- `sandbox/sandbox_test.go` — Added `TestMktempBuiltin`, `TestMktempDirBuiltin`

**Tests fixed:** env-and-path, environment-files (+2)
