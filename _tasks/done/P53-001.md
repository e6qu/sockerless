# P53-001 — Frontend TLS Support

**Status:** Done
**Phase:** 53 — Production Docker API: Code-First

Added `--tls-cert` and `--tls-key` CLI flags to Docker frontend. `Server.ListenAndServe` and `MgmtServer.ListenAndServe` accept cert/key paths. TCP listeners use `ListenAndServeTLS` when both provided; Unix sockets ignore TLS. 4 tests with in-test self-signed certs.

**Files modified:** `frontends/docker/cmd/main.go`, `frontends/docker/server.go`, `frontends/docker/mgmt.go`
**Files created:** `frontends/docker/server_test.go`
**Tests:** 4 PASS (TLS HTTPS, non-TLS fallback, Unix ignores TLS, mgmt TLS)
