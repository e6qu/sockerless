# E2E-019: Add assertions to GitHub workflow YAMLs

**Status:** DONE
**Phase:** 19 — Meaningful E2E Tests

Added `test` assertions to 6 GitHub workflow YAML files that previously only checked exit codes:
- `basic.yml` — assert echo output matches exact string
- `multi-step.yml` — assert step ordering via marker file line count
- `exit-codes.yml` — assert post-failure step runs via marker file
- `multi-job.yml` — assert each job produces marker file
- `container-action.yml` — assert action execution via marker file
- `large-output.yml` — assert `wc -l` = 1000; verify first/last lines
