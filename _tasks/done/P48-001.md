# P48-001: Context Config Extension

**Status:** Done
**Phase:** 48 — Private API Skeleton + Health + Status

Added `frontend_addr` and `backend_addr` fields to contextConfig struct in both CLI (`cmd/sockerless/context.go`) and core (`backends/core/context.go`). Added `--frontend-addr` and `--backend-addr` flags to `sockerless context create`. Updated `contextShow` to display new fields.
