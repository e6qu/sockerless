# P43-002: Tag ECS Resources

**Status:** Done
**Phase:** 43 — Cloud Resource Tracking

## What
Added Sockerless tags to ECS task definitions and tasks:
- `RegisterTaskDefinitionInput`: tags via `mapToECSTags(TagSet.AsMap())`
- `RunTaskInput`: tags via `mapToECSTags(TagSet.AsMap())`
- Registry register after RunTask, MarkCleanedUp on remove

## Files
- `backends/ecs/taskdef.go` (MODIFIED — TagSet + tags on RegisterTaskDefinition)
- `backends/ecs/containers.go` (MODIFIED — tags on RunTask, mapToECSTags helper, registry calls)
