# P86-008: Tests for P86-003/004/005

**Status:** Done
**PR:** (pending — same branch)
**Branch:** `phase86-complete-runner-support`

## What

Tests covering the no-AWS-track code changes in P86-003 through P86-005.

### Unit tests

- `backends/ecs/service_discovery_cloud_test.go` — 4 tests for `searchDomainsForContainer` (nil container, pre-defined networks skipped, missing namespace skipped, empty NetworkID skipped).
- `backends/lambda/image_inject_test.go` — 3 tests for `RenderOverlayDockerfile` (required-field validation across 3 fields, basic Dockerfile structure assertions, entrypoint/cmd env-var encoding).

### Integration tests

- `backends/lambda/integration_test.go#TestLambdaContainerStopUnblocksWait` — replaces the old `TestLambdaContainerStopNoOp`. Starts a `sleep 30` container, calls stop, asserts `docker wait` returns within 5s.
- `backends/lambda/integration_test.go#TestLambdaContainerLogsFollowLazyStream` — starts a container that prints 3 lines over 0.6s, opens follow-mode logs *immediately*, asserts all 3 lines arrive within 60s. Regression test for the "log stream resolved once upfront → empty stream forever" bug fixed in P86-004b.

### Harness fix

- `backends/lambda/integration_test.go#TestMain` previously `os.Exit(0)`-ed when `SOCKERLESS_INTEGRATION != 1`, which killed unit tests in the package before they ran. Now `os.Exit(m.Run())` and each integration test carries its own `skipIfNoIntegration(t)` guard.

## Files touched

| File | Change |
|------|--------|
| `backends/ecs/service_discovery_cloud_test.go` | New (4 tests) — landed in P86-003 commit |
| `backends/lambda/image_inject_test.go` | New (3 tests) — landed in P86-005 commit |
| `backends/lambda/integration_test.go` | 2 new tests + TestMain + skipIfNoIntegration — landed in P86-004a/b and P86-005 commits |
| `backends/lambda/arithmetic_integration_test.go` | skipIfNoIntegration on all 6 tests — landed in P86-005 commit |

Tests are bundled with the feature commits rather than in a separate "tests" commit to keep diffs self-contained per change.

## Results

- `go build ./... && go vet ./...` clean in `agent/`, `backends/ecs/`, `backends/lambda/`.
- Unit tests (no integration env): 7 new tests pass. Pre-existing unit tests still green.
- Integration tests: skip cleanly when `SOCKERLESS_INTEGRATION != 1`. Live AWS validation belongs to the AWS track.
