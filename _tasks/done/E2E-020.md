# E2E-020: Add assertions to GitLab pipeline YAMLs

**Status:** DONE
**Phase:** 19 — Meaningful E2E Tests

Added `test` assertions to 8 GitLab pipeline YAML files:
- `basic.yml`, `multi-step.yml`, `exit-codes.yml`, `before-after.yml`
- `multi-stage.yml`, `large-output.yml`, `parallel-jobs.yml`, `timeout.yml`
