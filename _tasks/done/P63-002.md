# P63-002: Dockerfile SHELL Directive

**Status:** Done
**Phase:** 63

## Summary
Implemented parsing and propagation of SHELL directive from Dockerfile, supporting both JSON array and default shell forms.

## Changes
- `backends/core/build.go`: Added `parseShellInstruction` to parse `SHELL ["/bin/bash", "-c"]` format
- `backends/core/build.go`: Extended `ContainerConfig.Shell []string` field to store parsed shell
- `backends/core/build.go`: Integrated shell parsing into `buildMergeConfig` to propagate across layers and multi-stage builds
