# E2E-001: Shared test library (lib.sh + start-backend.sh)

**Status:** DONE
**Phase:** 13 — E2E Runner Tests
**Effort:** M

## What Was Built

- `tests/e2e-live-tests/lib.sh` — Shared bash functions: logging (log_info/error/pass/fail/skip), wait_for_url, start_simulator, bootstrap_simulator, get_backend_env (all 7 backends), get_backend_binary, get_backend_cloud, is_faas_backend, should_skip_for_faas, PID file management
- `tests/e2e-live-tests/start-backend.sh` — Universal backend starter script. Parses --backend/--mode/--backend-addr/--frontend-addr/--pidfile flags. Starts simulator (if needed), bootstraps cloud resources, exports env vars, starts backend + frontend, writes PIDs for cleanup

## Files Created

- `tests/e2e-live-tests/lib.sh`
- `tests/e2e-live-tests/start-backend.sh`
