# P67-005: Linux-Aware NetworkDriver

**Status:** Done
**Phase:** 67 — Network Isolation (Linux)

Created `drivers_network_linux.go` (LinuxNetworkDriver wrapping SyntheticNetworkDriver with best-effort netns ops) and `drivers_network_other.go` (nil factory for macOS). Wired into InitDrivers() with platform detection.
