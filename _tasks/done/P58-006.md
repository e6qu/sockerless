# P58-006: goreleaser Configuration

Created `.goreleaser.yml` — builds 8 binaries × 4 platforms (linux/darwin × amd64/arm64):
bleephub, sockerless, frontend-docker, backend-memory, simulator-aws, simulator-gcp, simulator-azure, agent.

All `CGO_ENABLED=0`, `GOWORK=off`. Version/commit injected via `-ldflags` where applicable.
Archives as `tar.gz`. Changelog from git log (exclude docs/test/ci/merge commits). Checksums file.
