# P48-002: Backend Management Handlers + Tests

**Status:** Done
**Phase:** 48 — Private API Skeleton + Health + Status

Created `backends/core/handle_mgmt.go` with 3 endpoints:
- `GET /internal/v1/healthz` — health check with uptime
- `GET /internal/v1/status` — backend type, instance ID, container/resource counts
- `GET /internal/v1/containers/summary` — cloud-agnostic container table

Added `StartedAt` field to `BaseServer`. 4 unit tests in `handle_mgmt_test.go`.
