# P66-002: Backend Core otelhttp Middleware

**Status:** Done
**Phase:** 66 — OpenTelemetry Tracing

## What
Wrapped backend core HTTP handler with `otelhttp.NewHandler(MetricsMiddleware(s.Metrics, s.Mux), "sockerless-backend")` in `backends/core/server.go`. Automatic per-request spans with method, path, and status code attributes.

## Tests
241 core tests PASS.
