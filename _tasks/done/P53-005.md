# P53-005 — Tmpfs Mount Support

**Status:** Done
**Phase:** 53 — Production Docker API: Code-First

Process `HostConfig.Tmpfs` field during container start. `resolveTmpfsMounts` creates `os.MkdirTemp` for each tmpfs entry and returns containerPath → hostDir map. Merged into binds map before process lifecycle start.

**Files modified:** `backends/core/handle_containers.go`, `backends/core/handle_containers_archive.go`
**Files created:** `backends/core/tmpfs_test.go`
**Tests:** 3 PASS (creates-temp-dirs, empty-map, merges-with-binds)
