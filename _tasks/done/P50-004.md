# P50-004: Live Reload

**Status:** Done
**Phase:** 50 — Resource Management + Permissions + Live Reload

Backend `POST /internal/v1/reload` re-reads active context config and applies changed env vars (force-sets, unlike initial load). Frontend `POST /reload` stub. `sockerless context reload` POSTs to both servers. 1 new test verifying reload applies env var changes.
