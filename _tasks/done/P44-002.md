# P44-002 — Add Metadata + Status Fields to ResourceEntry
**Status:** Done
**Phase:** 44 — Crash-Only Software

Added Metadata map[string]string and Status string fields to ResourceEntry. Added IsActive() method (backward-compatible: empty status treated as active). Added MarkActive() to set status to "active" with auto-save. Updated ListActive/ListOrphaned to use IsActive(). 3 new tests: TestRegistryStatusTransitions, TestRegistryBackwardCompatibility, TestRegistryMetadataPersistence.
