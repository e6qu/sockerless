# P24-001: Inject bash environment variables ($BASH, $SHELL)

**Status:** DONE
**Phase:** 24

When `RunSimpleCommand` is invoked with `bash` or `/bin/bash`, inject `BASH=/bin/bash` and `SHELL=/bin/bash` into the env list before calling `runShellInDir`. Does not inject when invoked as `sh`.

**Files changed:**
- `sandbox/shell.go` — Added `isBashBinary()`, `injectBashEnv()`, call in `RunSimpleCommand`
- `sandbox/sandbox_test.go` — Added `TestBashEnvVars`

**Tests fixed:** shells/defaults, shells/bash, defaults-run, possibly workdir (+3-4)
