# E2E-025: E2E Dockerfiles build agent binary

**Status:** DONE
**Phase:** 19 — Meaningful E2E Tests

Updated both E2E Dockerfiles to build the `sockerless-agent` binary and copy it
to `/usr/local/bin/` so simulators can find it via PATH.

Files modified:
- `tests/e2e-live-tests/github-runner/Dockerfile`
- `tests/e2e-live-tests/gitlab-runner-docker/Dockerfile.backend`
