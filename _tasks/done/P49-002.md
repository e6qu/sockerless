# P49-002: Frontend Metrics

**Status:** Done
**Phase:** 49 — Server Lifecycle + Metrics

Extended `MgmtServer` with atomic request counter, `GET /metrics` endpoint (returns docker_requests, goroutines, heap_alloc_mb, uptime). Linked `MgmtServer` to frontend `Server` via `Mgmt` field — request logger middleware increments counter on each Docker API request.
