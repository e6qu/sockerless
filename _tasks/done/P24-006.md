# P24-006: Add hostname builtin + mv cross-device fallback

**Status:** DONE
**Phase:** 24

Two improvements:
1. **hostname builtin** — Returns `HOSTNAME` env var or `"localhost"`
2. **mv cross-device fallback** — When WASM busybox `mv` fails (errno 52 EXDEV across mount boundaries), falls back to Go-native copy+remove with `resolveContainerPath()` for mount-aware path resolution

**Files changed:**
- `sandbox/shell.go` — Added `hostname` case, `mvFallback()`, `resolveContainerPath()`, added to `builtinCommands`
- `sandbox/sandbox_test.go` — Added `TestHostnameBuiltin`, `TestMvCrossMount`
