# P49-001: Backend Metrics + Middleware

**Status:** Done
**Phase:** 49 — Server Lifecycle + Metrics

Created `backends/core/metrics.go` with `Metrics` struct: per-route request counting, latency ring buffer (max 1000 samples), percentile computation (p50/p95/p99). `MetricsMiddleware` wraps the backend mux. `MetricsSnapshot` includes goroutines, heap alloc, uptime, container/resource counts. Added `Metrics` field to `BaseServer`. 4 unit tests in `metrics_test.go`.
