# P24-005: Thread hostname and user into sandbox env

**Status:** DONE
**Phase:** 24

In `handleContainerStart`, before spawning the process, inject env vars from container config:
- If `Config.Hostname != ""` → inject `HOSTNAME=<value>`
- If `Config.User != ""` → parse `uid:gid` format, inject `SOCKERLESS_UID=<uid>` and `SOCKERLESS_GID=<gid>`

No interface changes — pure env injection.

**Files changed:**
- `backends/core/handle_containers.go` — Added env injection in `handleContainerStart`

**Tests fixed:** container-hostname (+1)
