# P43-006: Normalize Cloud Run + ACA Tags

**Status:** Done
**Phase:** 43 — Cloud Resource Tracking

## What
Replaced hardcoded tags in Cloud Run and ACA with the shared TagSet builder:
- Cloud Run `jobspec.go`: replaced `map[string]string{"sockerless-container-id": ..., "managed-by": ...}` with `TagSet.AsGCPLabels()`
- ACA `jobspec.go`: replaced `map[string]*string{"sockerless-container-id": ..., "managed-by": ...}` with `TagSet.AsAzurePtrMap()`
- Both backends now include all 5 standard tags instead of just 2

## Files
- `backends/cloudrun/jobspec.go` (MODIFIED — use shared tag builder)
- `backends/cloudrun/containers.go` (MODIFIED — registry register/cleanup)
- `backends/aca/jobspec.go` (MODIFIED — use shared tag builder)
- `backends/aca/containers.go` (MODIFIED — registry register/cleanup)
