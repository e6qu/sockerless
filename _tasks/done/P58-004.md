# P58-004: Docker Image Build Workflow

Created `.github/workflows/docker.yml` — builds and pushes 6 images to GHCR (`ghcr.io/e6qu/sockerless-{name}`).

Matrix: bleephub, frontend, backend-memory, simulator-aws, simulator-gcp, simulator-azure.
Multi-arch via QEMU + BuildKit (`linux/amd64,linux/arm64`). Tags via `docker/metadata-action` (branch, sha, semver). Push on main/tags only; build-only on PR. GHA layer cache.
