# P66-006: OTel Tests

**Status:** Done
**Phase:** 66 — OpenTelemetry Tracing

## What
Created `backends/core/otel_test.go` (3 tests): TestInitTracerNoEndpoint (no-op when unset), TestInitTracerWithEndpoint (real provider when set), TestHTTPMiddlewareCreatesSpan (in-memory exporter verifies span creation).

Created `bleephub/otel_test.go` (5 tests): same 3 as core + TestWorkflowDispatchCreatesSpan (submitWorkflow creates span), TestNoSpansWhenDisabled (no crash with no-op provider).

Uses `tracetest.NewInMemoryExporter()` + `sdktrace.WithSyncer()` for synchronous span capture in tests.

## Tests
All 8 new OTel tests PASS. No regressions in existing tests.
