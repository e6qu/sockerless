# Phase 83-C: In-Process Frontend↔Backend Composition

## Status: DONE

## Tasks Completed

### C1: Implement api.Backend on Docker backend
- Created `backends/docker/backend_impl.go` (~580 lines)
- All 44 interface methods delegate to `s.docker.*` + goverter converters
- Helper types: `hijackedRWC`, `nopRWC`
- Renamed `Info()` → `getInfo()` in client.go

### C2: Implement api.Backend on core BaseServer
- Created `backends/core/backend_impl.go` (~2080 lines)
- All 44 methods extracted from HTTP handlers into typed methods
- Helper types: `pipeRWC`, `pipeConn` (net.Conn adapter for driver Attach/Exec)
- Added `matchImageFilters()` for typed image filtering

### C3: Change frontend to use api.Backend
- `Server.backend` → `api.Backend`, added `httpProxy *BackendClient`
- Rewrote 8 handler files: ~30 typed, ~20 httpProxy
- Created `BackendHTTPAdapter` (~720 lines)
- Added `bufferedConn` for HTTP upgrade data preservation
- Added `parseFilters()` helper

### C4: Update startup composition
- `NewServer(logger, backend api.Backend, backendAddr string)`
- `cmd/main.go` uses `BackendHTTPAdapter` wrapper

## Test Results
- Core tests: 302 PASS
- Frontend tests: 7 PASS
- sim-test-all: 75 PASS (all 6 backends × ~12 tests)

## Files Created
- `backends/docker/backend_impl.go`
- `backends/core/backend_impl.go`
- `frontends/docker/backend_adapter.go`

## Files Modified
- `frontends/docker/server.go`, `containers.go`, `containers_stream.go`, `exec.go`, `images.go`, `networks.go`, `volumes.go`, `system.go`, `pods.go`, `server_test.go`, `cmd/main.go`
- `backends/docker/client.go`
- `api/types.go` (added `Name` to ContainerCreateRequest)
