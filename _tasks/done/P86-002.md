# P86-002: Audit real gaps vs. naming artifacts; honest capability matrix

**Status:** Done
**PR:** (pending — bundled with P86-001)
**Branch:** `phase86-pr1-drop-wasm-routing`

## What

With variant routing removed, audited `ALL_WORKFLOWS` and `ALL_PIPELINES` against the actual `.yml` files on disk. Pruned entries that had no backing file. Renamed `container-action-faas.yml` → `container-action.yml` (test body unchanged; old name implied a synthetic variant that no longer exists). Added `docs/runner-capability-matrix.md` as the going-forward source of truth for per-backend per-pipeline results.

## Files

| File | Change |
|------|--------|
| `tests/e2e-live-tests/github-runner/run.sh` | Removed `services`, `custom-image` from `ALL_WORKFLOWS`. `services-http` remains and exercises a real HTTP service container |
| `tests/e2e-live-tests/gitlab-runner-docker/run.sh` | Removed `services`, `custom-image` from `ALL_PIPELINES`; added `container-action` |
| `tests/e2e-live-tests/github-runner/workflows/container-action-faas.yml` | Renamed to `container-action.yml`; inner `name:` updated |
| `tests/e2e-live-tests/gitlab-runner-docker/pipelines/container-action-faas.yml` | Renamed to `container-action.yml` (no inner change needed) |
| `docs/runner-capability-matrix.md` | New — template matrix with TBD cells, methodology, and caveats |

## Details

- `services.yml` and `custom-image.yml` were deleted in commit `daeff00` ("Remove gitlabhub, WASM remnants…"). That commit did not update the `ALL_*` lists or the variant router, leaving a broken indirection. This PR finishes that cleanup.
- The matrix doc is intentionally TBD — populating every cell requires running `make e2e-github-<backend>` / `make e2e-gitlab-<backend>` for eight backends, which is hours of wall time. Each row is expected to be filled as PRs in Phase 86 land and expose real results.
- No production code touched; only test harness and docs.

## Tests

`bash -n` on changed scripts: clean.
No runtime test run in this PR — matrix population is an ongoing activity.
