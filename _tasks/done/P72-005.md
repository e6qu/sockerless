# P72-005: Lambda Real Execution via Docker API

**Phase**: 72 (Full-Stack E2E Tests)
**Status**: ✅ Complete

## What
Modified Lambda backend `handleContainerStart` to invoke the function for non-tail-dev-null containers instead of auto-stopping. The Lambda simulator already has `ImageConfig.Command` from create.

- Checks `result.FunctionError` for exit code (simulator sets `X-Amz-Function-Error: Unhandled` on non-zero exit)
- Stores response payload in `LogBuffers` for container logs
- Falls back to 500ms auto-stop when no command or in production mode

## Files
- `backends/lambda/containers.go` — modified (non-tail-dev-null path)
- `simulators/aws/lambda.go` — modified (return exit code, set FunctionError header)
