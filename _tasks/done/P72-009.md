# P72-009: Lambda Arithmetic Integration Tests

**Phase**: 72 (Full-Stack E2E Tests)
**Status**: ✅ Complete

## What
Added 6 arithmetic integration tests to Lambda backend:
- TestLambdaArithmeticSuccess: `3 + 4 * 2` → exit 0, logs contain "11"
- TestLambdaArithmeticParentheses: `(3 + 4) * 2` → exit 0, logs contain "14"
- TestLambdaArithmeticInvalid: `3 +` → exit 1, logs contain "ERROR"
- TestLambdaArithmeticDivision: `10 / 3` → exit 0, logs contain "3.333"
- TestLambdaArithmeticWithLabels: `100 - 42` → exit 0, logs contain "58", label filter
- TestLambdaArithmeticEnvVar: `(3 + 4) * 2` with EXPR env → exit 0, logs contain "14"

Uses hard log assertions (Lambda CloudWatch logs work via HTTP).

## Files
- `backends/lambda/arithmetic_integration_test.go` — new, 6 tests
- `backends/lambda/integration_test.go` — modified (evalBinaryPath + eval binary build)
