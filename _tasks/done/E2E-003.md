# E2E-003: GitHub runner Dockerfile + run.sh

**Status:** DONE
**Phase:** 13 — E2E Runner Tests
**Effort:** M

## What Was Built

- `tests/e2e-live-tests/github-runner/Dockerfile` — Multi-stage build: builds all 7 backend binaries + 3 simulator binaries + frontend in builder stage, runtime stage installs act. Single image for all backends.
- `tests/e2e-live-tests/github-runner/run.sh` — Entry script with --backend/--workflow/--mode flags. Starts Sockerless via start-backend.sh, iterates workflows, skips FaaS-incompatible tests, captures logs, writes summary.
