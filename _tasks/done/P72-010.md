# P72-010: GCF Arithmetic Integration Tests

**Phase**: 72 (Full-Stack E2E Tests)
**Status**: ✅ Complete

## What
Added 6 arithmetic integration tests to GCF backend (same matrix as Lambda).
Uses soft log assertions via `checkLogs` helper — GCF's gRPC Cloud Logging
gets "context deadline exceeded" in integration tests (non-gRPC endpoint).

## Files
- `backends/cloudrun-functions/arithmetic_integration_test.go` — new, 6 tests
- `backends/cloudrun-functions/integration_test.go` — modified (evalBinaryPath + eval binary build)
