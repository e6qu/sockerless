# DOC-001: Update spec to document Phase 15 WASM sandbox

## Status: PENDING

## Description

Update `spec/SOCKERLESS_SPEC.md` to document the Phase 15 WASM sandbox implementation in the memory backend.

## Acceptance Criteria

- [ ] Add Phase 15 entry to the "Implementation Phases" section
- [ ] Document WASM sandbox architecture (wazero + mvdan.cc/sh + go-busybox)
- [ ] Update memory backend capabilities (was "no real execution", now "WASM sandbox")
- [ ] Document the `sandbox/` module in the project structure section
- [ ] Note WASM limitations (no network, busybox-only, not for production)
- [ ] Mention `WasmExec` flag on `BackendDescriptor` and that only memory enables it

## Files to Modify

- `spec/SOCKERLESS_SPEC.md`
