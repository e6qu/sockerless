# P67-006: Linux-Specific Tests

**Status:** Done
**Phase:** 67 — Network Isolation (Linux)

Created `netns_linux_test.go` with 3 tests (Available without root, CreateDelete with root, VethPair with root). Linux-only tests skipped on macOS via build tag, root tests skip via os.Getuid().
