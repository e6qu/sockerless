# Standardized Makefile for bleeplab — see docs/MAKEFILE_STANDARD.md.

APP_NAME      := bleeplab
GO_PACKAGE    := ./cmd
DEFAULT_PORT  := 8929
RUN_FLAGS     := -addr :8929
GO_ENV        := CGO_ENABLED=0

REPO_ROOT_REL := ..

include $(REPO_ROOT_REL)/make/go-app.mk
