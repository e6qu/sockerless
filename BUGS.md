# Known Bugs

**1160 filed · 1145 fixed · 15 open · 2 false positives.** Open: BUG-1075 (live-cloud) + BUG-1104 (audit-cadence meta) + BUG-1148..1160 (Phase 178 scope: 1 reopen + 8 community-filed + 4 class-of-bug remediations).

Standing rule: every CI / live-cloud failure lands here with a one-liner *before* any fix attempt. Workarounds, fakes, placeholders, silent fallbacks, skips, and incomplete implementations are all bugs and get the same treatment. Per-bug fix detail beyond the one-liner: `git log <commit>` or the linked PR.

Live status (cells, branch, milestone) lives in [STATUS.md](STATUS.md). Vibe-pattern numbers reference `docs/VIBE_CODING.md`.

## Open

| ID | Sev | Area | Pattern | One-liner |
|----|-----|------|---------|-----------|
| 1075 | P2 | live-cloud cells red for cloudrun Services + ACA Apps + AZF + Lambda service-mesh + ACA/AZF Azure AD | 6 (untested in real cloud) | Lambda is the only backend with a green live-cloud cell. Cloud Run Services + ACA Apps + AZF cloud-DNS + Lambda service-mesh + ACA/AZF Azure AD remain unvalidated against real clouds. The preflight/runbook work is documented, but the cells must not be marked green without a real run against authenticated cloud projects/subscriptions. |
| 1148 | P0 | AWS S3 multipart `ListParts` missing (issue #196 reopened — postmortem of BUG-1138 + BUG-1142) | 36 (surface-table-completeness gap) | Phase 176/177's S3 multipart fix wired Initiate / UploadPart / Complete / Abort / ListMultipartUploads but missed `GET /{bucket}/{key}?uploadId={id}` (ListParts). The route falls through to GetObject → 404 NoSuchKey. The Go SDK's `manager.Uploader` calls ListParts on every retry to learn which parts have already uploaded. **Postmortem:** (a) `s3_subresources_test.go::TestS3_Multipart` exercised the happy-path round-trip but didn't drive `manager.Uploader`-style retry; ListParts never fired. (b) Missed code path is the SDK's retry / resume logic that issues ListParts mid-upload. (c) New regression guard: SDK test using `manager.NewUploader` with a forced mid-upload abort + resume cycle, plus a per-op SDK test calling ListParts directly. Surface table `specs/SIM_SURFACE_TABLES/aws-s3-multipart.md` enumerates all 6 multipart ops; this is the only ✗ row. |
| 1149 | P1 | Azure KV `GET /secrets/{name}/versions` returns SecretBundle (single-record) shape instead of paged SecretListResult — and PUT overwrites instead of versioning (issue #203) | 1, 36 | Two related gaps: (a) the LIST endpoint returns the single-secret envelope `{value,id,attributes,tags}` instead of the paged `{value:[{id,attributes,tags},...],nextLink}` SecretListResult; SDK paged iterators silently return zero items. (b) Sim stores one row per name; every PUT overwrites the bytes. Real KV creates a fresh version UUID per PUT and the secret is a chain of versions. Fix shape: refactor store from `name → SecretBundle` to `name → []SecretVersion`; PUT appends a new version; GET reads latest; `GET /secrets/{name}/{version}` reads specific. LIST returns the paged shape with one entry per version. State machine: each version has `enabled` / `created` / `updated` / `notBefore` / `expires` attributes. |
| 1150 | P0 | AWS routing collision — `POST /v2/apis/{id}/deployments` (API Gateway v2 CreateDeployment) shadowed by S3 multipart dispatcher (issue #204) | mux-overlap | The sim collapses every AWS service onto a single port and dispatches by path. S3's `POST /{bucket}/{key...}` registers a 2-segment wildcard that matches `/v2/apis/{id}/deployments` (path: `/v2/apis/...`) when no v2 API service handler is registered for that subtree, returning S3-style "POST on an object requires ?uploads" error. Fix shape: apply same first-segment-must-be-known-bucket gate to S3's POST handler that GCS got in Phase 176 (catch-all tightening). S3 only claims paths whose first segment matches a registered bucket. Add `mux-overlap-scan` skill (BUG-1158) to surface these collisions at PR time. |
| 1151 | P1 | Azure KV `PATCH /secrets/{name}/{version}` (UpdateSecretProperties) missing + entire `/deletedsecrets/...` soft-delete recovery surface absent (issue #205) | 1, 36, state-machine | Two related gaps: (a) PATCH on a secret version returns `MethodNotAllowed`; real KV uses PATCH to update attributes (enabled, expires, notBefore, tags, contentType) without changing the secret value. (b) Soft-delete surface unimplemented: real KV moves DELETEd secrets to `/deletedsecrets/...` with `scheduledPurgeDate` + `recoveryId` + `recoveryLevel`; the soft-deleted secret is recoverable for the retention period (default 90 days) via `POST /deletedsecrets/{name}/recover` or purgeable via `DELETE /deletedsecrets/{name}`. Sim's current DELETE removes from the live store with no recovery path. State machine: active → deleted (recoverable) → purged. Fix shape: add PATCH handler that updates the targeted version's attributes; rewire DELETE to transition into a deleted-store; implement `/deletedsecrets/` LIST + GET + recover + purge. New `sim-state-machine-completeness` skill (BUG-1160) audits this. |
| 1152 | P1 | Azure App Service / Functions sites missing `/sites/{name}/config/{section}` endpoints — appsettings, connectionstrings, web, metadata, etc. (issue #206) | 36 | ARM PUT creates the site successfully but the data-plane sub-config endpoints return `404 page not found`. Real Azure: `POST /sites/{name}/config/appsettings/list` returns appsettings; `PUT /sites/{name}/config/appsettings` updates; symmetric for `connectionstrings`, `web`, `metadata`, `authsettings`, `authsettingsV2`, `slotConfigNames`, `pushsettings`, `azurestorageaccounts`. terraform-provider-azurerm fans out across these on `Read` for `azurerm_linux_function_app` / `azurerm_app_service`; missing endpoints break the provider's state refresh. Fix shape: table-driven dispatcher mirroring Phase 177's S3 bucket-subresources design — per-site store keyed by `{site}/{section}` with PUT/GET round-trip. |
| 1153 | P1 | AWS sim missing: RDS snapshot family + SNS `SetTopicAttributes` + SQS `PurgeQueue` + sibling round-trip ops (issue #207) | 36, state-machine | RDS: `CreateDBSnapshot` / `DescribeDBSnapshots` / `DeleteDBSnapshot` / `RestoreDBInstanceFromDBSnapshot` unwired; real RDS has a snapshot state machine (creating → available → deleted → failed). SNS: `SetTopicAttributes` returns `InvalidAction`; `GetTopicAttributes` already wired, so the round-trip is broken. SQS: `PurgeQueue` returns `InvalidAction`. Fix shape: per-instance snapshot list with state-machine transitions; SNS/SQS attribute round-trip parity; SDK tests + tf-tests where provider resources exist (`aws_db_snapshot`). |
| 1154 | P0 | AWS awsQuery router dispatches by `Action` only, ignoring `Version` — `ListTagsForResource` / `AddTagsToResource` always route to ElastiCache (issue #208) | 18, mux-overlap | The sim's awsQuery router maps `Action=ListTagsForResource` to one handler regardless of the `Version` parameter that disambiguates services (RDS, SNS, ElastiCache, CloudWatchLogs all define `ListTagsForResource`). First registration wins; everyone else gets routed to ElastiCache. Fix shape: change the router from `routes[action] → handler` to `routes[version][action] → handler`, dispatching by `Version` first. Cross-service blast radius — verify KMS / SecretsManager / SSM / SNS / SQS / RDS / ElastiCache tag operations after the refactor. |
| 1155 | P1 | GCP sim missing ops across Cloud SQL, Memorystore, Pub/Sub, API Gateway (issue #209) | 36, state-machine | Five clusters: (a) Cloud SQL Admin — `backupRuns.insert` / `list` / `get` / `delete` + `instances/{name}/clone` LRO + selfLink scheme `http://` → `https://`. (b) Memorystore Redis — `:upgrade` + `:failover` LROs + per-op state machine (READY → MAINTENANCE → READY for upgrade; READY → FAILING_OVER → READY for failover). (c) Pub/Sub Snapshot CRUD (`snapshots.create` / `list` / `get` / `delete`) — snapshot lifecycle preserved alongside subscriptions. (d) API Gateway — `gateways.getIamPolicy` / `setIamPolicy` / `testIamPermissions` (AIP-130 IAM v1 surface). (e) Cloud SQL selfLink emission fix. Fix shape: per-cluster SDK-driven implementation using `google.golang.org/api/sqladmin/v1`, `redis/v1`, `pubsub/v1`, `apigateway/v1`. State machine assertions per `sim-state-machine-completeness` (BUG-1160). |
| 1156 | P1 | Azure sim missing ops across PG FlexibleServer, APIM, Cache Redis (issue #210) | 36 | Three clusters: (a) PG FlexibleServer — `configurations` ListByServer / Get / Update; provider `azurerm_postgresql_flexible_server_configuration`. (b) APIM — `operations` + `backends` + `namedValues` CRUD; providers `azurerm_api_management_api_operation` / `_backend` / `_named_value`. (c) Cache Redis — `firewallRules` LIST/GET/CRUD + `listKeys` POST; provider `azurerm_redis_firewall_rule`. Fix shape: per-cluster table-driven dispatch where applicable; tf-test parity for each provider resource. |
| 1157 | P1 | Proactive surface-table seeding gap — `surface-table-completeness` skill only fires when a table exists; my Phase 177 created 2 tables, the repo has 30+ services worth of surfaces | meta (BUG-1145 follow-up) | The class of bug from BUG-1145 (partial-table coverage) has a fast-tail in the long-tail: every surface without a table is a silent ✗ everywhere. Issue #196's reopen is the proof: PR #200/#202 fixed bucket subresources but multipart had no table; ListParts dropped through. Fix shape: seed `specs/SIM_SURFACE_TABLES/` proactively for every service the sim implements — populate ops the sim already handles + obvious siblings from the issue text. ~30+ MD files. After seeding, every subsequent PR can be audited against the full enumeration. |
| 1158 | P0 | No mux-overlap scanner — routing collisions (BUG-1150, BUG-1154) ship past CI because no skill enumerates registered route patterns and flags shadowing | meta | The collapsed-port model means every service's mux registrations live in the same `http.ServeMux`. Go's mux uses pattern specificity to pick winners; when a newer registration is less specific than an older one, the older shadows it. The wrong handler responds with a plausible error envelope, so wire-shape tests don't catch it — `aws s3api`-style probe gets "InvalidRequest" instead of the real "InvalidAction" the API Gateway v2 handler would emit. Fix shape: new skill `.claude/skills/mux-overlap-scan/SKILL.md` + scanner script that lists every `mux.HandleFunc` call across `simulators/<cloud>/*.go`, computes specificity ordering, and flags pairs where pattern A shadows B. Wire into `.pre-commit-config.yaml`. Warn initially; gate after the existing collisions clear. |
| 1159 | P1 | `sim-canonical-config-test` skill mandates SDK clients but not paged-iterator drive — single-record envelope shipped from a List endpoint silently passes a `.Value[0]`-style test (issue #203 surfaces it for KV; same shape would mask issues across every paged surface) | meta (BUG-1144 follow-up) | The wire-shape drift class: `GET /secrets/{name}/versions` returns SecretBundle (single record) instead of paged SecretListResult. An SDK test that calls `client.ListSecretVersions(ctx, name)` and reads `.Value[0]` auto-unwraps the wrong shape and passes. A test using `client.NewListSecretVersionsPager(...).NextPage(ctx)` fails because the iterator can't parse the single-record envelope as a page. Fix shape: extend `sim-canonical-config-test` SKILL with a "List* op MUST have a paged-iterator test" rule. Add `paged-shape verified` column to every surface table; backfill where covered. |
| 1160 | P1 | No state-machine-completeness skill — sim stores data flat with no lifecycle, but documented semantics include state transitions (versioning, soft-delete, snapshot states, failover) | meta | The state-machine-fakery class: real KV secret has `active → deleted → purged`; real RDS snapshot has `creating → available → deleted → failed`; real Memorystore has `READY → FAILING_OVER → READY`. Sim stores one row per name with no state field; PUT overwrites; DELETE deletes; nothing transitions through intermediate states the SDKs / providers expect to read back. Fix shape: new skill `.claude/skills/sim-state-machine-completeness/SKILL.md` that audits the stored-shape representation against the documented state machine for every resource type with one. Pairs with `surface-table-completeness` — every row in a surface table whose handler implements a stateful resource must also assert against the state machine. |
| ~~1142~~ | ~~P1~~ | ~~AWS S3 bucket-level PUT/DELETE subresources route to CreateBucket (issue #201)~~ — **FIXED** in Phase 177 commit `ec34835`. Table-driven `bucketSubresourceHandlers` map covers 20+ subresources (versioning / lifecycle / cors / policy / encryption / replication / tagging / website / logging / acl / requestPayment / accelerate / ownershipControls / notification / publicAccessBlock / object-lock / intelligent-tiering / inventory / analytics / metrics) with canonical 200-OK vs 204-No-Content per real-S3; bodies stored in `s3BucketConfigs` so PUT→GET round-trips. 7 canonical SDK tests cover the high-fanout subset. Full enumeration in `specs/SIM_SURFACE_TABLES/aws-s3-bucket-subresources.md`. | 36 |
| ~~1143~~ | ~~P0~~ | ~~Azure KV `WWW-Authenticate: Bearer authorization=...` URL breaks every official Azure SDK's challenge parser (issue #193 reopened — postmortem of BUG-1135)~~ — **FIXED** in Phase 177 commit `2be56ad`. Authorization URL changes from `http://<host>` (3 path segments) to `http://<host>/00000000-0000-0000-0000-000000000000` (4 segments) so the Azure SDK `parseTenant` indexes `parts[3]` without panic and extracts the placeholder UUID tenant. **Postmortem:** (a) `simulators/azure/sdk-tests/blob_keys_certs_test.go::TestKeyVault_AuthChallenge` asserted the WWW-Authenticate response carried `authorization=` + `resource=` keys but never read the URL through the SDK parser. (b) The missed code path was `github.com/Azure/azure-sdk-for-go/sdk/security/keyvault/internal.parseTenant` (`challenge_policy.go:104`) — `parts := strings.Split(url, "/"); tenant := parts[3]`, no bounds check. (c) The new regression guard is `simulators/azure/sdk-tests/keyvault_sdk_test.go::TestKeyVault_SDK_Secrets_ChallengeRoundTrip` + Keys + Certificates siblings, each constructing the official `azsecrets` / `azkeys` / `azcertificates` SDK client and exercising the full challenge-then-retry handshake. | 36 |
| ~~1144~~ | ~~P1~~ | ~~`sim-canonical-config-test` SKILL doesn't refuse raw `net/http` where an official SDK exists~~ — **FIXED** in Phase 177. Skill extended with three sections: "use the SDK, not raw HTTP" (with permitted scenarios + comment requirements), "permitted SDK-config diffs beyond endpoint URL + TLS-skip + static creds" (documenting the `InsecureAllowCredentialWithHTTP` / `DisableChallengeResourceVerification` / scheme-rewriting-transport allowed-diffs introduced for KV), and "terraform-provider tests" (extending the rule to `terraform-tests/`). Phase 177's own new tests (`keyvault_sdk_test.go` + `s3_bucket_subresources_test.go`) already use SDK clients; the existing raw-HTTP tests (storage data planes, ServiceBus REST, IMDS / MSI / Monitor) all have permitted-scenario justifications and stay. Repo-wide sweep of existing raw-HTTP tests for missing justification comments is a maintenance pass for a future phase — the rule now applies going forward. | meta |
| ~~1145~~ | ~~P1~~ | ~~No canonical surface-table to enforce "full enumeration before fixed" on closed operation tables~~ — **FIXED** in Phase 177 commit `60db15f`. `specs/SIM_SURFACE_TABLES/` directory with per-surface MD tables (initial: AWS S3 bucket-subresources, Azure KV data plane). New skill `.claude/skills/surface-table-completeness/SKILL.md` formalises the full-enumeration invariant. | meta |
| ~~1146~~ | ~~P1~~ | ~~Reopened issues have no required postmortem trail; first-fix shape never gets surfaced for the next fix~~ — **FIXED** in Phase 177 (this commit). New skill `.claude/skills/reopen-postmortem/SKILL.md` requires three fields on every reopen BUG: (a) what test passed but should have failed, (b) what SDK code path the test missed, (c) what new canonical-SDK-client test catches the regression. BUG-1134 + BUG-1143 rows backfilled with postmortems. Reopens are P0 by default. | meta |
| ~~1147~~ | ~~P1~~ | ~~Existing terraform-provider tests don't exercise the surfaces being fixed at the SDK layer~~ — **FIXED** in Phase 177. AWS tf-tests gain `aws_s3_bucket_versioning`, `aws_s3_bucket_cors_configuration`, `aws_s3_bucket_server_side_encryption_configuration`, `aws_s3_bucket_website_configuration`, `aws_s3_bucket_public_access_block`, `aws_s3_bucket_ownership_controls` — each round-trips through PUT + GET via tf-provider-aws's Create+Read cycle. Apply success and asserted-value outputs lock in PUT→GET fidelity. Azure tf-tests gain `azurerm_key_vault` + `azurerm_key_vault_secret` — the secret resource fires the data-plane challenge-then-retry handshake, exercising BUG-1143's URL-format fix at the terraform layer (Azure tf-tests are Docker-only on macOS; CI validates). Lifecycle subresource deferred to a follow-up — tf-provider-aws polls `GetBucketLifecycleConfiguration` for 3 min waiting on real-AWS eventual consistency; sim returns the PUT body immediately but the provider's poll-loop comparator never matches because it inspects internal LastModified-style fields the sim doesn't reconstruct. | 36 |
| ~~1134~~ | ~~P1~~ | ~~Azure storage path-style dispatch requires prior ARM registration (issue #190 reopened — postmortem of BUG-1130)~~ — **FIXED** in Phase 176. Permissive path-style dispatch with a protocol-signal discriminator (`x-ms-version` / `x-ms-date` / `x-ms-blob-type` / `restype` / `comp` / `SharedKey`) — any account name resolves to the data plane; non-storage co-tenants (IMDS / MSI / Monitor / dataCollectionRules) keep their own routes on the shared sim port. **Postmortem:** (a) BUG-1130's test created the storage account via ARM PUT *before* exercising path-style, so the strict known-account check passed; the test never modelled the Azurite "no prior registration" path. (b) The missed code path was the Azure SDK / azurerm provider's direct-data-plane dispatch — they hit `BlobEndpoint=http://<sim>/<account>` without an intervening ARM call. (c) The new regression guard is `simulators/azure/sdk-tests/storage_dataplanes_test.go::TestStorageDataPlane_PathStyleBlob` using `azurite-style-acct` — never registered via ARM. | 36 |
| ~~1135~~ | ~~P0~~ | ~~Azure KV data plane missing `WWW-Authenticate: Bearer` 401 challenge (issue #193)~~ — **FIXED** in Phase 176. KV WrapHandler issues `401 + WWW-Authenticate: Bearer authorization="http://<host>", resource="https://vault.azure.net"` on unauthenticated requests; non-RSA key generation upgraded to real ecdsa + crypto/elliptic + rand.Read. | 36 |
| ~~1136~~ | ~~P1~~ | ~~AWS RDS + ElastiCache return empty `<EngineVersion>` when not explicitly set (issue #194)~~ — **FIXED** in Phase 176. Per-engine GA default maps (`rdsDefaultEngineVersion`, `ecDefaultEngineVersion`) populate `EngineVersion` on Create when the request leaves it empty. | 36 |
| ~~1137~~ | ~~P0~~ | ~~Azure Service Bus REST data plane returns 200 + empty body for every operation~~ — **FIXED** in Phase 176. Real `servicebus_dataplane.go` with persistent message store: SendMessage→201, ReceiveAndDelete→200 with body / 204 empty, PeekLock→201 + body + Location pointing at `/messages/{id}/{token}`, CompleteLock DELETE→204. Topic + Subscription equivalents wired. | 1 |
| ~~1138~~ | ~~P1~~ | ~~AWS S3 missing subresource handlers — multipart, tagging, CopyObject (issue #196)~~ — **FIXED** in Phase 176. `s3_subresources.go` dispatches subresources by query (`?uploads`, `?uploadId=&partNumber=`, `?tagging`) and the `x-amz-copy-source` header: full multipart cycle (Initiate / UploadPart / Complete / Abort / List), Object Tagging CRUD, CopyObject, multi-object delete. All silent `io.ReadAll(_)` body reads error-check. | 36 |
| ~~1139~~ | ~~P1~~ | ~~GCP `/v1/operations` returns GCS-shaped 404 — routing leak (issue #197)~~ — **FIXED** in Phase 176. Real `GET /v1/operations` cross-service projection from `crOperations` store with AIP-151 `filter=done:true\|false` + `name` prefix support; GCS catch-all tightened to refuse `{bucket}` segments not in the bucket store. | 36 |
| ~~1140~~ | ~~P1~~ | ~~GCS missing `Objects.compose`, rejects body-form `name` on resumable+multipart upload, emitted URLs use http (issue #198)~~ — **FIXED** in Phase 176. `:compose` POST handler concatenates source-object bytes; resumable + multipart Init parse `name` from body or query; emitted `mediaLink`/`selfLink` hard-coded https via `gcsObjectMetadata`; resumable chunk PUT registered and bodies wrap through `openStreamingBody` for gzip transparency. | 1, 36 |
| ~~1141~~ | ~~P1~~ | ~~AWS Lambda missing subresource handlers (issue #199)~~ — **FIXED** in Phase 176. `lambda_subresources.go` registers PublishVersion / ListVersions / Alias CRUD / AddPermission / GetPolicy / RemovePermission / FunctionUrlConfig CRUD under restJson1; CreateAlias + UpdateAlias validate `FunctionVersion` against the PublishVersion history. | 36 |
| 1104 | P0 | Simulator test infrastructure verifies the sim from the inside, not from the outside — **the meta-bug behind 1098–1103** | 36 (new): test fixtures configured to make the sim look correct | `sdk-tests/` constructed clients with sim-quirk options (`o.BaseEndpoint = aws.String(baseURL + "/s3")` in `simulators/aws/sdk-tests/s3_test.go:16`; `UsePathStyle = true` paired with it) so SDK tests passed against a `/s3/`-prefixed simulator that no stock client could ever reach. No CI step ran an unmodified `aws` / `gcloud` / `az` CLI binary against the sim's printed `*_ENDPOINT_URL`. Sim emitted URLs (advertised endpoints, presigned URLs, LRO operation links) were never asserted to round-trip. Sentinel headers (`Content-Encoding: aws-chunked`, `x-amz-content-sha256: STREAMING-*`, `x-amz-decoded-content-length`) were never logged, so handlers that read raw bodies in streaming mode (BUG-1099) had no operator-visible footprint. Phase 173.0 codifies the four invariants that would have caught 1098/1099/1103: (a) canonical-config invariant in `sdk-tests/`, (b) stock-binary CLI smoke (`aws` / `gcloud` / `az`) installed in the test image, (c) emitted-URL round-trip lint, (d) sentinel-header logging. Also adds README "in-scope / out-of-scope services" section per sim so missing-service issues like #176/177/178 are visible from the project surface before they get filed. |
| ~~1120~~ | ~~P0~~ | ~~Persistence-strip BUGs~~ — **FIXED** in Phase 175 (commit `61d132f`). GCP SM split storage; Azure KV wrapper records. | 36 | (a) `simulators/gcp/secretmanager.go:33` — `SecretVersion.payload []byte` is **unexported**; `sim.Store` JSON-serialises, so every `:access` returns the empty payload after restart. (b) `simulators/azure/keyvault.go:107-109, 304-306, 316-318` — `KeyVaultSecret.Vault/Name`, `KeyVaultKey.Vault/Name`, `KeyVaultCertificate.Vault/Name` all carry `json:"-"`; List endpoints (lines 482, 583, 712-715) filter on `obj.Vault == vault` which after restart always evaluates false → empty lists. Both are load-bearing data losses. Fix shape per BUG-1098: export the field with a non-canonical JSON tag (`Vault string \`json:"_vault,omitempty"\``) that survives persistence without breaching wire-fidelity. |
| ~~1121~~ | ~~P0~~ | ~~Silent error swallows~~ — **FIXED** in Phase 175 (commit `61d132f`). 7 sites: storage_dataplane.go xml, 2x functions.go, 4x bleephub. | 1, 17 (vibe-slop) | (a) `simulators/azure/storage_dataplane.go:505-507` — stacked `data, _ := io.ReadAll(r.Body)` + `_ = xml.Unmarshal(data, &req)` in `handleQueuePutMessage`; malformed `<QueueMessage>` body 201-creates with empty MessageText. Pattern A2 (new). (b) `simulators/azure/functions.go:666, 670` — silent `_ = json.Unmarshal(decoded, &entrypoint)` + `_ = json.Unmarshal(decoded, &cmd)` after base64 decode of `SOCKERLESS_ENTRYPOINT`/`SOCKERLESS_CMD` — BUG-1019 replay. (c-f) `bleephub/timeline.go:34-37`, `bleephub/run_service.go:63, 217`, `bleephub/gh_pulls_rest.go:407` — bleephub handlers with silent decodes. All fixable via the documented `if err := X(...); err != nil { 400 }` shape. |
| ~~1122~~ | ~~P0~~ | ~~Dead-code silencers~~ — **FIXED** in Phase 175 (commit `61d132f`). 5 sites including Phase 174 regression at bleephub:215. | 17, 20 (vibe-slop) | (a) `bleephub/gh_projects_v2_graphql.go:215` — Phase 174's "fix" for BUG-1108 replaced the named-variable silencer with an **anonymous `_ =` silencer over the same dead code**; the comment acknowledges BUG-1020/1021 but kept the artifact. The 5-line `graphql.NewInputObject(...)` is never registered in any schema. Real fix: delete the entire block. (b) `simulators/aws/kms.go:508-511` — dead `for _, keyId := range kmsAliases.List() { _ = keyId }` loop with empty body. (c) `simulators/aws/kms.go:515-516` — `all := kmsAliases.Filter(...); _ = all` — value computed and discarded; the next loop uses a different code path. (d) `github-runner-dispatcher-aws/pkg/scopes/scopes.go:149` — `_ = now` in `WaitFromRateHeaders(h, now)`; the parameter is a **false testability claim** (the body uses runtime `time.Until` regardless). Either wire `now` through or drop the parameter. (e) `api/gen/main.go:113, 123, 125` — `explicitOutDir := false` set in one branch, never read. |
| ~~1123~~ | ~~P1~~ | ~~Wire-quirk drifts~~ — **FIXED** in Phase 175 (commit `61d132f`). SQS cap-at-10, SM TagResource empty body, Memorystore RFC1918 host, KV buildKVURL. | 18 (vibe-slop) | (a) `simulators/aws/sqs.go:352-354` — `if maxN <= 0 \|\| maxN > 10 { maxN = 1 }`: SQS `ReceiveMessage(MaxNumberOfMessages=15)` returns 1 message instead of capping at 10. Real SQS silently caps; SDK pollers batch 10-at-a-time, so the wrong fallback breaks throughput. (b) `simulators/aws/secretsmanager.go:505-506, 536-537` — `handleSMTagResource` / `handleSMUntagResource` write `w.WriteHeader(http.StatusOK)` with no body + no Content-Type. awsJson1_1 SDK expects `{}`. (c) `simulators/gcp/memorystore_redis.go:66` — `Host: fmt.Sprintf("%s.redis.example", id)` placeholder; real Memorystore returns RFC1918 IP. Terraform `google_redis_instance.host` carries the value; `redis-cli -h <host>` gets NXDOMAIN. (d) `simulators/azure/keyvault.go:651-652` — `handleKVSetSecret` ID builder reads `r.URL.Scheme` (empty in server mode); fallback at 661 saves it today, but the primary path is a latent BUG-184 duplicate-host shape — fix to use `buildKVURL` consistently. |
| ~~1124~~ | ~~P1~~ | ~~Azure Storage .web. + .dfs. data planes~~ — **FIXED** in Phase 175 (commit `61d132f`). 501 NotImplemented dispatch wired in storage_dataplane.go WrapHandler. Real handlers deferred until a runner scenario needs them. | 36 (sim-emitted-url-roundtrip) | `simulators/azure/files.go:195-196` — `StoragePrimaryEndpoints.Web` emits `https://<account>.web.<host>:<port>/` and `Dfs` emits `https://<account>.dfs.<host>:<port>/`. No `.web.` or `.dfs.` subdomain handler exists. Same shape as Blob/Queue/Table/File (now closed by `storage_dataplane.go`). Real Azure static-website data plane lives at `.web.`, Data Lake Storage Gen2 at `.dfs.`. Fix shape: extend `storage_dataplane.go` subdomain dispatch to register Web (minimal static-content GET/HEAD) and Dfs (hierarchical namespace POST/PUT/DELETE) handlers, OR remove the advertised endpoints from `StoragePrimaryEndpoints` if neither is in sockerless's runner use case. Pick a primitive that's cloud-acceptable. |
| ~~1125~~ | ~~P1~~ | ~~Emitted-URL annotation gaps~~ — **FIXED** in Phase 175 (commit `61d132f`). 11 sites annotated; Cloud Run CRAddress.URL annotation corrected; GCS bucket selfLink uses gcpSelfLink. | 36 (sim-emitted-url-roundtrip) | (a) Cloud Run `CRAddress.URL` Phase 174 annotation is **wrong** — comment says "same canonical run.app URL" but emission (lines 164/244) is `http://{svc}.{ns}.svc.cluster.local`. (b) `simulators/azure/containerapps_apps.go:83` — `ContainerAppSecret.KeyVaultURL` missed on the App variant (Job sibling got it). (c) `simulators/gcp/artifactregistry.go:28, 36, 614` — `Repository.RegistryURI` + `DockerImage.URI` canonical `pkg.dev` URLs. (d) `simulators/gcp/pubsub.go:60` — `PSPushConfig.PushEndpoint` operator-supplied webhook target. (e) `simulators/aws/sqs.go:67` — `SQSQueue.URL` canonical `sqs.<region>.amazonaws.com`. (f) `simulators/aws/lambda.go:65, 220` — `ImageUri` + `Code.Location`. (g) `simulators/aws/cloudfront.go:660, 828` — Location header. (h) `simulators/aws/apigateway.go:47` + `apigatewayv2.go:42` — `Integration.Uri`. (i) `simulators/gcp/gcs.go:99` — bucket `selfLink` missed `gcpSelfLink` conversion. (j) `simulators/azure/auth.go:77, 144` — OpenID `iss`/issuer claims. |
| ~~1126~~ | ~~P1~~ | ~~Streaming-body handlers missed in Phase 174~~ — **FIXED** in Phase 175 (commit `61d132f`). Azure Files PutFile + readAllRequest now use openStreamingBody; AWS Lambda Invoke refuses Content-Encoding with 415. | 1, 36 | (a) `simulators/azure/storage_dataplane.go:286` — `handleFilesPutFile`: `data, err := io.ReadAll(r.Body)` direct; azcopy/azfileshare can send `Content-Encoding: gzip`. Same file as the 9 sites Phase 174 already wired; this one was missed in the same PR. (b) `simulators/azure/files.go:774` (via `readAllRequest:803`) — `handleAzureFilesPutRange` reads body without consulting `Content-Encoding`. (c) `simulators/aws/lambda.go:365` — `handleLambdaInvoke`: `payload, _ := io.ReadAll(r.Body)`. aws-sdk-go-v2 gates compression via `requestcompression` trait; Invoke isn't currently in that set but is flagged for future inclusion. Fix shape: wrap each with `openStreamingBody(r)` (Azure) or sentinel-header check (Lambda). |
| ~~1127~~ | ~~P1~~ | ~~Skill catalogue extensions~~ — **DONE** in Phase 175 (commit `f1395b3`). Pattern A2 added to silent-error-swallow-scan; positive-confirmation rule added to sim-streaming-body-handler. | meta | The Phase 175 sweep validated the audit cadence but found 2 patterns the skills missed: (1) two-value silent-read form `data, _ := io.ReadAll(...)` followed by silent `_ = xml.Unmarshal(...)` — Pattern A2 added to `silent-error-swallow-scan/SKILL.md`. (2) helper introduced in PR not used by other handlers in the same PR — positive-confirmation rule added to `sim-streaming-body-handler/SKILL.md` (every handler in `*_dataplane.go` / `*blob*.go` / `*storage*.go` / `*files*.go` / `*queue*.go` etc. must call `openStreamingBody` or document why not). Both extensions landed in Phase 175 alongside the BUG fixes. |
| ~~1128~~ | ~~P2~~ | ~~Meta-shape — helpers introduced in a PR get bypassed by other handlers in the same PR~~ — **FIXED** in Phase 175 (commit `f1395b3`). The positive-confirmation rule in `sim-streaming-body-handler/SKILL.md` is the load-bearing fix: every PR's audit pass must positively enumerate every handler that should use a newly introduced helper. Companion `timeless-comments` skill (commit `236b97e`) codifies the comment side of the meta-rule (no narrating diffs). | 36, BUG-1104 |
| ~~1129~~ | ~~P1~~ | ~~GCP Pub/Sub PATCH subscription/topic missing (issue #189)~~ — **FIXED** in Phase 175 (commit pending). PATCH handlers wired with updateMask semantics. | 36 (sim-handler-checklist) | `simulators/gcp/pubsub.go` registers `PUT` (create) + `GET` for subscriptions and topics but not `PATCH`. `projects.subscriptions.patch` + `projects.topics.patch` are the canonical update verbs in GCP Pub/Sub REST. `hashicorp/google` provider's update path for `google_pubsub_subscription` calls PATCH; the `google.golang.org/api/pubsub/v1` SDK's `Patch` emits it. Today every `terraform apply` after initial create errors with 404. Fix shape: register `PATCH /v1/projects/{project}/subscriptions/{name}` + topics handler; parse `subscription`/`topic` body field + `updateMask` query/body parameter; copy each field path from request into stored record (skip unknown paths gracefully); persist + return canonical shape. |
| ~~1130~~ | ~~P1~~ | ~~Azure Storage data plane host-only dispatch (issue #190)~~ — **FIXED** in Phase 175 (commit pending). Path-style fallback wired: bare `/{account}/...` for blob (Azurite default) + `/file/{account}/...`, `/queue/{account}/...`, `/table/{account}/...` for the other three. | 36 (sim-emitted-url-roundtrip + connection-string compatibility) | `simulators/azure/blob.go` + `simulators/azure/storage_dataplane.go` dispatch by `{account}.{service}.<host>` Host header only. The Azure Go SDK + `azurerm` Terraform provider default to **path-style** URLs (`/{account}/{container}/{blob}`) when the configured endpoint isn't `*.core.windows.net` — Azurite-compatible. Today `curl /testacct/c1?restype=container` 404s. macOS doesn't auto-resolve `*.localhost` to 127.0.0.1; consumers without dnsmasq are entirely blocked. Fix shape: extend the WrapHandler dispatchers to also detect `/{account}/...` path-prefix when the Host has no recognised subdomain; strip the account segment and dispatch into the same per-service handler. Cover Blob, Files, Queues, Tables symmetrically. |
| ~~1131~~ | ~~P2~~ | ~~Azure KV ARM `vaultUri` uses request scheme~~ — **FIXED** in Phase 175 (commit pending). ARM `vaultUri` now hard-coded `https://` like the data-plane buildKVURL helper. | 18, 36 |
| ~~1132~~ | ~~P1~~ | ~~`TestCloudRunFaaSE2ESmoke` flaky — file-based polling loop + sleep + 5-min ContainerWait races on busy CI runners~~ — **FIXED** in Phase 175 (commit pending). Signal-based termination wired across cloudrun + gcf + azf + lambda smoke tests: main cmd `trap 'exit 0' TERM; sleep 600 & wait`; step 2's exec sends `kill 1`; ContainerWait timeout dropped from 5 min to 30 s. Eliminates the 1-second polling + filesystem-sync race. ACA kept its existing ContainerStop-based pattern (different shape, also race-free). | 17 (vibe-slop: brittle test design) | `backends/cloudrun/faas_smoke_integration_test.go` uses `while [ ! -f /tmp/sockerless-done ]; do sleep 1; done` as the main container cmd; step 2's exec `touch /tmp/sockerless-done` is supposed to signal exit. The ContainerWait has a 5-min timeout. Observed flake: same test PASSED on commit `8fcf4a6` (phase-175-skill-sweep), FAILED on commit `e81f0aa` (same branch, fix-only diff unrelated to Cloud Run code). The 1-sec poll + filesystem-sync delay + Cloud Run reverse-agent exec dispatch can stack on busy CI runners. Fix shape: replace file-based polling with signal-based termination — main container runs `sleep 600 & wait` and traps SIGTERM; step 2's exec runs `kill 1` (cleanly signals PID 1 / the shell to terminate). Removes the filesystem-sync race + sleep granularity entirely. |
| ~~1133~~ | ~~P2~~ | ~~CI `test` job is too big~~ — **FIXED** in Phase 175 (commit pending). Split into 3 jobs: `test-core` (fast unit + libs), `test-backend` (matrix×6 per-backend integration), `test-faas-smoke` (matrix×5 per-backend FaaS smoke). 11 → 22 total CI jobs; flakes now isolated to one backend's matrix entry. | meta | The `test` GitHub Actions job runs unit tests + integration tests across all 7 backends sequentially. Run times: 8m10s (passing) up to 12m+ on failures. When one backend's integration test flakes (e.g., BUG-1132), the entire job restarts. Fix shape: split into matrix-style per-backend integration jobs (already done for `sim (aws/gcp/azure)` — same split for `test` would parallelise per-backend runs and isolate flakes to one job). Plus a separate fast unit-only job for `make test-unit`. Deferred — needs CI workflow change beyond the current Phase 175 scope; file as P2 follow-up. | Issue #191's data-plane finding (secret `id` http://) was already closed by Phase 175's BUG-1123d (`handleKVSetSecret` switched to `buildKVURL` which hard-codes `https://`). The sibling at `simulators/azure/keyvault.go:171` (ARM vault PUT — emits `properties.vaultUri`) still uses `r.URL.Scheme`-derived scheme. Real Azure ARM `vaultUri` is always `https://`. SDK callers configured with a sim endpoint typically construct data-plane URLs from the vault name not from `vaultUri`, but the inconsistency between ARM-emitted `http://...vault...` and data-plane-emitted `https://...vault...` is itself a fidelity drift. Fix: hard-code `https` for the ARM `vaultUri` too. | Phase 174 introduced `openStreamingBody(r)` and `gcpSelfLink(r, path)` helpers explicitly to replace patterns the sweep had flagged. The same PR's new file `storage_dataplane.go` then used `io.ReadAll(r.Body)` directly in 2 sites (BUG-1126); the same PR's `gcs.go:99` bucket-selfLink still emits the canonical-AR URL (part of BUG-1125). Phase 174's own dead-code-silencer-scan replaced a named silencer with an anonymous silencer over the same dead code (BUG-1122a). Meta-lesson: every PR that introduces a helper must include a positive enumeration of "every handler that should now use this helper" + a single-PR audit pass that confirms each one was actually updated. Codified in `sim-streaming-body-handler/SKILL.md` (Phase 175). |

## False positives

| Area | Finding | Why it's not a bug |
|------|---------|--------------------|
| `backends/aca/azure.go::fakeCredential` | Returns literal `"fake-token"` against simulator endpoints. | Sims don't verify bearer tokens — would require real Azure AD endpoint not emulated. Credential wired only via `newAzureClientsWithEndpoint` (sim path); production uses `azidentity.NewDefaultAzureCredential`. |
| `cmd/sockerless-admin/api_observability.go::envOrDefault` (reclassified from BUG-1010 candidate) | `envOrDefault("OTEL_LOGS_SERVICE_PARAM", "service.name")` returns the canonical OTel resource-attribute name when unset. | This is a documented default-value helper, not an error-hiding fallback. The defaults (`service.name` / `service`) are the canonical OTel attribute names; operator config explicitly overrides. No silent failure mode. |

## Class-of-bug rules (carried forward)

- **Backend ↔ host primitive must match (P0).** ECS in ECS, Lambda in Lambda, Cloud Run in Cloud Run, GCF in CRF, ACA in ACA, AZF in AZF. Cross-pollution is a critical architectural error.
- **No fakes / no fallbacks / no skips.** Synthetic exit codes, silent shims, fake-data fallbacks, conditional `t.Skip` for missing config — all file as bugs and get real fixes. Tests run or fail loud; never skip silently.
- **No legacy-shim fallbacks during development.** Bleephub isn't carrying old behavior — if real GitHub does X, bleephub does X. Don't add a "legacy bph_ token shape" path when the right answer is "match GitHub's `ghp_`".
- **Cross-cloud sweep on every find.** When a pattern is found in one backend, the same code paths in the other 5 backends / 3 sims get checked in the same commit.
- **Pattern B for cloud-specific drivers.** Within a cloud, drivers consolidate into `*-common`; cross-cloud duplication is fine and expected.
- **Doc-only fixes are unsafe when the cloud rejects the config.** BUG-944/987: a documented MountOptions requirement that Cloud Run rejects is no fix. Verify cloud-acceptable, not just sockerless-controllable.
- **HTTP 500 reserved for unexpected panics.** Never return 5xx as a designed failure path; use exit-code header / envelope.
- **External test fixtures must use the real client.** The `gh` CLI test harness uses real `gh repo create` / `gh issue create` against bleephub, not `gh api $URL -f key=val` URL hackery. If bleephub rejects what real GitHub accepts, fix bleephub — don't bend the test.

## Resolved history (compressed)

1059 bugs filed and fixed across phases 86–168 follow-up plus pod-model follow-up plus 173 plus 174 skill-sweep + issue triage.

- **GitHub issues #181..#188 + BUG-1109 / 1110 / 1111** (Phase 174 round 2) — Eight community-filed issues against Phase 173 plus the three Open BUGs from the round-1 skill sweep. Every one a real bug in code I wrote during Phase 173, surfaced by the user actually trying the sim with stock clients. All closed in-branch:
  - **Issue #181 → BUG-1112**: Azure Cache for Redis ARM route case-sensitive (`Microsoft.Cache/redis` 404'd, only `Microsoft.Cache/Redis` worked). Extended `AzurePathNormalizationMiddleware` to canonicalize the 6 provider segments Phase 173 added (Redis, ServiceBus, ApiManagement, DBforPostgreSQL, KeyVault, Storage). azurerm provider's lowercase form + SDK clients' canonical mixed case both now reach the sim.
  - **Issue #182 → BUG-1113**: GCP Pub/Sub subscription response dropped 5 of 7 fields. Added MessageRetentionDuration / RetainAckedMessages / ExpirationPolicy / EnableMessageOrdering / Filter to `PSSubscription` + new `PSExpirationPolicy`; create handler propagates all 5. MessageRetentionDuration defaults to "604800s" (7 days) when unset, matching real-GCP.
  - **Issue #183 → BUG-1114**: GCP Secret Manager `ListSecrets` fell through to GCS catch-all, returned a GCS-shaped 404 with `bucket "v1"` error. Registered an explicit `GET /v1/projects/{p}/secrets` handler.
  - **Issue #184 → BUG-1115**: Azure KV `id` / `kid` URLs had duplicated host + http scheme (`http://kv.vault.kv.vault.azure.net/...`). `buildKVURL` was prepending `<vault>.vault.` to `r.Host` which already carried the full `<vault>.vault.<host>` subdomain. Fixed to emit `https://<r.Host>/<kind>/<name>/<version>` directly — real KV is always https; SDKs reject http-scheme URLs.
  - **Issue #185 → BUG-1116**: Azure KV RSA key create returned literal `"n":"sim-generated-modulus"` placeholder. Fixed to generate a real RSA key via `crypto/rsa.GenerateKey(rand.Reader, key_size)` and emit base64url-encoded `n` + `e` — consumers reconstructing JWK as `crypto/rsa.PublicKey` now succeed. Falls back to placeholder for non-RSA `kty` since the sim doesn't simulate EC / oct curves.
  - **Issue #186 → BUG-1117**: AWS SQS `CreateQueue` Attributes silently dropped — only VisibilityTimeout was honored; MessageRetentionPeriod / DelaySeconds / RedrivePolicy / KmsMasterKeyId / FifoQueue / etc. were lost. Added `Attributes map[string]string` to `SQSQueue`; Create + SetQueueAttributes persist every supplied key; GetQueueAttributes echoes them alongside system-emitted ones (QueueArn, message counts).
  - **Issue #187 → BUG-1118**: GCP Cloud SQL `selfLink` was relative (`/v1/projects/...`). Real GCP emits absolute (`https://sqladmin.googleapis.com/v1/...`). Added `gcpSelfLink(r, path)` helper that derives scheme + host from the request. Used for instance and database selfLink emissions.
  - **Issue #188 → BUG-1119**: GCP Secret Manager `:access` response echoed literal `versions/latest` instead of the resolved version number. Added `accessSecretPayloadResolved` + `resolveLatestVersionID` helpers; `:access` handler + `GetSecretVersion` handler use the resolved version ID in the response `name`.
  - **BUG-1109** (umbrella from round-1): Azure Storage File / Queue / Table data planes were advertised but unrouted. New file `simulators/azure/storage_dataplane.go` adds host-based subdomain dispatch for `<account>.{file,queue,table}.<host>` plus minimal CRUD per service: Files (share + file PUT/GET/HEAD/DELETE + directory list), Queues (queue CRUD + message enqueue/dequeue/peek/ack + XML envelopes), Tables (Tables endpoint + entity CRUD using OData `(PartitionKey='...',RowKey='...')` addressing). 3 SDK tests cover the round-trip per service. The host-dispatch pattern reuses the same WrapHandler the Blob data plane uses.
  - **BUG-1110** (umbrella from round-1): 9 sim handler sites across GCS / Cloud Run invoke / AR blob upload (2) / ACR blob upload (4) / Azure Blob PutBlob did `io.ReadAll(r.Body)` straight to storage without inspecting `Content-Encoding`. Added `openStreamingBody(r) (io.ReadCloser, error)` helper to each cloud's package (`simulators/azure/streaming.go`, `simulators/gcp/streaming.go`) that transparently decodes `gzip` and 415s unsupported encodings. All 9 sites updated. The aws-chunked variant is already handled by `awsChunkedReader` in the AWS sim; OCI Distribution `Content-Range` chunked uploads are still per-handler logic (out of scope for the transparent-decode helper). Sentinel-header logging from Phase 173.0 surfaces every streaming-envelope arrival in operator logs.
  - **BUG-1111** (umbrella from round-1): Azure Functions ScriptHref / ConfigHref / Href / InvokeURLTemplate + AWS Amplify WebhookUrl + AWS ECR RepositoryUri annotated as external via field-level `// external: …` comments + parent-struct docstrings. Per the `sim-emitted-url-roundtrip` skill's "document external" branch — when the URL points at a real-cloud surface the sim cannot reasonably implement, mark it as such on the field rather than trying to route.

  Round-2 self-audit caught 5 additional gaps that hadn't been closed yet; all fixed in the same branch:
  - **#188 part 2** — `:enable` / `:disable` / `:destroy` POST handler in `simulators/gcp/secretmanager.go` was also missing `latest`-alias resolution (only `:access` and plain GetSecretVersion had been updated). Issue body explicitly called this out; now closed.
  - **BUG-1111 round 2** — 4 more emitted-but-undocumented URL fields surfaced by re-running the sim-emitted-url-roundtrip scan: AWS ECR `UpstreamRegistryUrl` (operator-supplied upstream registry — external by design), AWS Amplify `ThumbnailUrl` (real-AWS hosted screenshot, sim doesn't serve), Azure ContainerApps `JobSecret.KeyVaultURL` (operator-supplied KV secret reference; sim KV data plane accepts the shape but ACA Job runtime doesn't auto-resolve), GCP `CRServiceStatus.URL` + `CRAddress.URL` (canonical run.app URLs; sim invokes via `/v2-services-invoke` at the configured endpoint, NOT at the advertised URL). Each annotated with `// external: …` per the skill's "document external" branch.

  All AWS / GCP / Azure SDK regression suites green after the fixes. Continuity docs (STATUS.md / PLAN.md / DO_NEXT.md / WHAT_WE_DID.md) updated.



- **1105 / 1106 / 1107 / 1108** (Phase 174 skill-sweep) — Running the 5 specialist skills committed in Phase 173.0 across the repo surfaced 4 small fixes plus 3 larger follow-ups (filed as 1109 / 1110 / 1111 Open above).
  - **1105** (silent-error-swallow-scan): 23 `_ = sim.ReadJSON(r, &req)` sites across Phase 173 handlers — exactly the BUGS 1016/1017/1018/1019/1025/1033 shape. All replaced with `if err := sim.ReadJSON(...); err != nil { sim.<X>Error(w, ..., http.StatusBadRequest); return }`. Sites: simulators/azure/{apim,servicebus,postgres_flexible}.go (11) + simulators/gcp/{apigateway,memorystore_redis,pubsub,sqladmin}.go (10) + simulators/aws/{sqs,apigateway}.go (2). Each cloud uses its idiomatic error helper (sim.AzureErrorf / sim.GCPErrorf / sim.AWSError). `sim.ReadJSON` returns nil on empty body so the previous "silent" behavior is preserved for legit empty-body create calls; only malformed JSON now produces a 400 envelope instead of being dropped. The fact that I introduced these sites in Phase 173 right after writing the `silent-error-swallow-scan` skill that codifies the exact pattern is the BUG-1104 meta-shape in action — skill caught my own diff after-the-fact, which is the right outcome.
  - **1106** (silent-error-swallow-scan): `simulators/azure/keyvault.go::handleKVCreateCertificate` had `_ = json.NewDecoder(r.Body).Decode(&body)`. Same shape as my secrets handler in the same file that already did the right thing. Fixed with `if err := ...; err != nil && !errors.Is(err, io.EOF) { sim.AzureError(...); return }`. `errors` + `io` imports added.
  - **1107** (dead-code-silencer-scan): `simulators/aws/sdk-tests/metadata_sdk_test.go:94` had `var _ = aws.Config{} // Suppress unused-aws-import warning`. The `aws` package import was used ONLY by that silencer. Deleted both. Pure BUG-1022 / BUG-1024 shape.
  - **1108** (dead-code-silencer-scan): `bleephub/gh_projects_v2_graphql.go:209+224` defined `singleSelectInputValueType` and held it alive with `_ = singleSelectInputValueType // reserved for future strict-typed variants`. Pure BUG-1020 / BUG-1021 shape — dead code held for hypothetical future use, "reserved for" anti-pattern. Inlined the `graphql.NewInputObject` call without the variable assignment (the call's side effects matter for graphql schema registration, but the typed reference doesn't). Comment explains the pattern was removed by the dead-code-silencer-scan sweep so a future contributor doesn't reintroduce it.

  Plus running `sim-canonical-config-test` confirmed the regression-guard added in 173.1 is still green (0 quirk patterns repo-wide). The `backpedal-pattern-audit` skill's catalogue is unchanged — the patterns recurring across BUGS.md (silent-error swallow, dead-code silencer, phase metadata in comments) already have specialist scans; this sweep found instances of those exact patterns and fixed them, validating the audit's "above the codification threshold" criterion.

- **1101 (umbrella close — issue #176 fully covered)** (Phase 173.4–173.6) — Added 6 AWS services across 3 sub-phases.

- **1103 (umbrella close — issue #178 fully covered)** (Phase 173.10–173.12) — Added Azure Blob data plane + Key Vault keys/certs + 4 ARM surfaces across 3 sub-phases. 173.10: Blob data plane via `<account>.blob.<host>` subdomain dispatch (closes the BUG-1103 / `sim-emitted-url-roundtrip` shape — the storage-account ARM response advertised this endpoint URL on every PUT, now it resolves to a real handler) + Key Vault `/keys/*` and `/certificates/*` surfaces alongside the pre-existing `/secrets/*`. 173.11: `Microsoft.Cache/Redis` (5 ops) + `Microsoft.DBforPostgreSQL/flexibleServers` (11 ops including owned Database + FirewallRule sub-resources with cascade-delete on server delete). 173.12: `Microsoft.ServiceBus` namespaces + queues + topics + subscriptions (16 ops with cascade-delete on namespace delete) + `Microsoft.ApiManagement` service + apis + products + subscriptions (16 ops with cascade-delete on service delete). 8 new SDK tests via raw HTTP (the Azure Storage SDK's strict Shared Key signature verifier rejects sim auth-passthrough config; raw HTTP exercises the same wire shape).

- **1102 (umbrella close — issue #177 fully covered)** (Phase 173.7–173.9) — Added 4 GCP services across 3 sub-phases. 173.7: Pub/Sub (12 ops, REST + in-memory FIFO queue + ackId tracking). 173.8: Memorystore Redis (5 ops, LRO via existing newLRO helper) + API Gateway (12 ops across Apis / ApiConfigs / Gateways, LRO). 173.9: Cloud SQL Admin (10 ops across instances / databases / users; sqladmin-specific Operation envelope shape with kind=sql#operation and status=DONE, distinct from the cloud-run.v2 Operation shape Memorystore + APIGW use). Secret Manager was already implemented (per the issue's correction). 5 new SDK tests against `google.golang.org/api/{pubsub,redis,apigateway,sqladmin}/v1` REST clients exercise the canonical lifecycle for each service.

- **1101 (umbrella close — issue #176 fully covered)** (Phase 173.4–173.6) — Added 6 AWS services across 3 sub-phases. 173.4: SQS (awsJson1_0, 12 ops) + SNS (awsQuery, 12 ops). 173.5: API Gateway v2 (restJson1, 10 ops) + API Gateway v1 (restJson1, 10 ops including auto-created root resource on CreateRestApi). 173.6: RDS (awsQuery, 7 ops including engine-port mapping for postgres/mysql/mariadb/sqlserver/oracle) + ElastiCache (awsQuery, 7 ops). All 12 ops integrated into the AWS sim's main.go router map. 12 new SDK tests cover each service's 90th-percentile lifecycle. The cumulative wire-protocol surprises documented (SQS migration to awsJson1_0; APIGW v1's singular `"item"` array field vs v2's plural `"items"`) are the kind of "read the SDK serializer source" lesson skill `sim-handler-checklist` already encodes. All AWS SDK + CLI + Terraform suites still green.

- **1101 (part 1 — SQS + SNS)** (Phase 173.4, from GitHub issue #176) — `simulators/aws/` had no SQS or SNS handlers; stock SDK / CLI / `terraform-provider-aws` for either service got `UnknownOperationException`. Added `sqs.go` (12 ops: CreateQueue, DeleteQueue, GetQueueUrl, ListQueues, GetQueueAttributes, SetQueueAttributes, SendMessage, ReceiveMessage with visibility-timeout, DeleteMessage, TagQueue, UntagQueue, ListQueueTags) and `sns.go` (12 ops: CreateTopic, DeleteTopic, ListTopics, GetTopicAttributes, Subscribe, Unsubscribe, ListSubscriptions, ListSubscriptionsByTopic, Publish, TagResource, UntagResource, ListTagsForResource). Critical gotcha caught mid-implementation: **SQS migrated from awsQuery to awsJson1_0 in late 2023** (aws-sdk-go-v2 service/sqs v1.28+). The issue author's hint "SQS: awsQuery" matched older docs; the current SDK dispatches via `X-Amz-Target: AmazonSQS.<Op>` with JSON request + response bodies. Initial implementation registered SQS via `AWSQueryRouter`; tests returned `UnknownOperationException: Unknown operation: AmazonSQS.CreateQueue`; rewrote SQS handlers to use JSON protocol via `AWSRouter`. SNS still uses awsQuery as the issue described. SNS Publish fans messages out to in-process SQS subscribers via the canonical Notification envelope; SDK MD5 checksum validation forced a real `md5.Sum(envelope)` instead of an empty placeholder. Test coverage: 4 SQS SDK tests (lifecycle / tags / non-existent-queue error / visibility-timeout expiry [short-skip]) + 4 SNS SDK tests (lifecycle / subscribe-unsubscribe / publish-fanout-to-SQS with JSON-envelope assertion / non-existent-topic error), all green. **BUG-1101 stays open** (umbrella) — APIGW v1+v2 (173.5) and RDS + ElastiCache (173.6) still pending. This sub-phase also caught the `sim-handler-checklist` skill's "Read the SDK serializer source" rule in flight: guessing the wire protocol from external docs without reading the SDK source produced a CI-red round-trip on the SQS protocol-migration gotcha. Memory documented for future-me.

- **1100** (Phase 173.3, from GitHub issue #175) — `simulators/aws/secretsmanager.go::registerSecretsManager` registered only `ListSecrets`, not `ListSecretVersionIds`; the SDK's audit / rotation / cross-cloud-sync flows that need to enumerate versions to map a specific timestamp/index → UUID got `UnknownOperationException`. Fix introduces `SMSecretVersion` per-version history on the `SMSecret` struct with `addNewVersion` / `currentVersion` / `versionByIDOrStage` helpers; `CreateSecret` seeds the first AWSCURRENT version; `PutSecretValue` and `UpdateSecret` (when bytes change) prepend a new version, demote the prior AWSCURRENT to AWSPREVIOUS, and drop the AWSPREVIOUS stage off any older version (matching real-SM staging semantics). New `ListSecretVersionIds` handler returns the version list with stage labels; `IncludeDeprecated` controls whether stage-less older entries appear. `GetSecretValue` now honors both `VersionId` (exact UUID) and `VersionStage` (label) selectors. `DescribeSecret`'s `VersionIdsToStages` projection now reflects the full history. Plus `GetRandomPassword` (stateless utility used by rotation logic) supporting `PasswordLength`, `ExcludeCharacters`, `ExcludeNumbers` / `ExcludePunctuation` / `ExcludeUppercase` / `ExcludeLowercase`, `IncludeSpace`, `RequireEachIncludedType`. Verified: new `TestSecretsManager_ListSecretVersionIds` (5-act staging flow: Create A → Put B → Put C → list defaults / IncludeDeprecated / VersionId selector / AWSPREVIOUS stage selector) and `TestSecretsManager_GetRandomPassword` (default length, letters-only, digits-only, length-out-of-range rejection) green; existing `TestSecretsManager_CreateGetUpdateDelete`, `TestSecretsManager_DuplicateNameRejected`, `TestSecretsManager_ListAndTag` still pass.

- **1099** (Phase 173.2, from GitHub issue #174) — `simulators/aws/s3.go::handleS3PutObject` did `io.ReadAll(r.Body)` straight to the object store with no decode of the AWS chunked-encoding envelope. When the aws-sdk-go-v2 SDK uses a non-seekable body (`io.Pipe`, `http.Request.Body`, streaming compressors), it switches to `Content-Encoding: aws-chunked` + `x-amz-content-sha256: STREAMING-AWS4-HMAC-SHA256-PAYLOAD[-TRAILER]` framing on the wire. The sim was storing the chunk-encoding bytes (`<hex-size>\r\n<payload>\r\n0\r\nx-amz-checksum-...\r\n\r\n`) verbatim. The bug was structurally undetectable because (a) the sim listens on plain HTTP and the SDK refuses streaming-signed payloads over HTTP, (b) every existing test used a seekable `bytes.NewReader` which opts out of streaming, (c) no operator-visible log named the streaming envelope when one arrived. Fix adds `simulators/aws/awschunked.go` — an `awsChunkedReader` that unwraps the chunked envelope (size-hex line + payload + CRLF until zero chunk; ignores `;chunk-signature=<hex>` extensions because per-chunk signature replay is out of scope for the sim's auth-passthrough model; consumes the trailer-header block before EOF). `handleS3PutObject` now sniffs `Content-Encoding: aws-chunked` / `x-amz-content-sha256: STREAMING-*` / `x-amz-decoded-content-length` via `isAWSChunkedRequest` and wraps `r.Body` in the decoder before reading. Persisted bytes + MD5 ETag use the decoded payload. The sentinel-header logging added in 173.0 surfaces the streaming envelope on the existing Info-level "request" log line, making this category of bug greppable in operator output going forward. Verified: 6 new decoder unit tests + 7-case sentinel-detection test green; new `TestS3_AWSChunkedPutObject_RoundTrip` integration test hand-crafts a chunked PUT (HTTP-level since SDK refuses streaming-signed over HTTP), then SDK-GETs and asserts decoded bytes + correct MD5 ETag; full AWS SDK + CLI + Terraform suites still green. Skill `sim-streaming-body-handler` codifies the pattern preemptively for the next upload-handling sim (Azure Blob data plane in 173.10, future GCS extension).

- **1098** (Phase 173.1, from GitHub issue #173) — `simulators/aws/s3.go::registerS3` mounted every S3 route under a `/s3/` URL prefix (`GET /s3`, `PUT /s3/{bucket}`, `PUT /s3/{bucket}/{key...}`) instead of the canonical AWS root path. Stock `aws` CLI / `aws-sdk-go-v2` / `terraform-provider-aws` pointed at the documented `AWS_ENDPOINT_URL` got `405 Method Not Allowed` on every S3 op. The bug had been latent since phase 9X because three layers of the project's own test infrastructure independently reconfigured around it: `sdk-tests/s3_test.go:16` set `o.BaseEndpoint = aws.String(baseURL + "/s3")`, `cli-tests/helpers_test.go::awsS3CLI` appended `/s3` to the endpoint URL, and `terraform-tests/main.tf:34` set `s3 = "${var.endpoint}/s3"`. Fix re-mounts S3 routes at the canonical root path (`GET /` for ListBuckets, `PUT /{bucket}` for CreateBucket, `PUT /{bucket}/{key...}` for PutObject, etc.). HEAD requests are routed via Go's net/http auto-fallback to GET handlers — the explicit `HEAD /{bucket}` registration was dropped because it tripped the mux's method-coverage conflict detector against the literal `GET /health` route; HeadBucket / HeadObject dispatch happens by method check inside the GET handler. All three test layers re-pointed at the canonical endpoint URL; new regression-guard `TestCanonicalConfig_NoSimQuirkBaseEndpoint` (scans test sources + `.tf` files for the recurring sim-quirk pattern) prevents the shape from coming back. Verified: full AWS SDK suite (95s), full AWS CLI suite (123s), full AWS Terraform suite (87s) green. Skill `sim-canonical-config-test` codifies the pattern.

- **1097** (pod-model simulator fidelity CI follow-up) — PR #172 CI run `26063005479` caught an AWS simulator regression in `TestECS_CrossTaskDNS`: the multi-container ECS materialization change renamed even the first/main task container from the canonical `sockerless-sim-aws-task-<task>` name to a suffixed `...-app` name, so Cloud Map's real Docker-network alias wiring could no longer find the task container to connect it to the namespace network. The fix preserves the existing canonical task container name for the first container and only suffixes sidecars, which keeps Cloud Map and existing single-container task behavior stable while still giving each sidecar a distinct real workload container. The same CI triage also fixed the sidecar Docker host config: ECS sidecars no longer receive `ExtraHosts` while joining the main container's network namespace, because Docker rejects that combination and sidecars inherit the main container's network stack. Verified with the focused AWS SDK multi-container localhost test, the focused Cloud Map cross-task DNS test, and the full AWS simulator SDK target.

- **1096** (pod-model simulator fidelity follow-up) — The simulators accepted multi-container pod/task specs but several execution paths did not materialize all containers as real workload containers sharing `localhost`: AWS ECS tasks and GCP Cloud Run Jobs/Services started only the first container, while Azure ACA Apps started sidecars on the same Docker bridge network rather than the main container's network namespace. The fix starts every task/revision/job/app container with per-container image/env/command/args/volume config and uses Docker/Podman `container:<mainID>` network mode for sidecars, matching the cloud shared-localhost contract while keeping the simulator cloud-facing APIs unchanged. GCP Cloud Run Services also now honor command/args and GCS volume binds in the HTTP launcher. Regression coverage uses official SDK clients: ECS multi-container task, Cloud Run Service multi-container invoke, Cloud Run Job multi-container execution, ACA Job multi-container execution, and ACA App multi-container logs. The same pass corrected stale AZF pod docs: AZF currently supports single-container Function Apps only and rejects multi-container pods; Azure sidecar workloads belong on ACA Apps until AZF has a real implementation. Real-runner pre-work added `make e2e-github-sim-arithmetic`, `make e2e-gitlab-sim-arithmetic`, and `make e2e-real-runner-sim-arithmetic`, which run official GitHub/GitLab runner flows against a caller-started simulator-backed daemon and compile/test the Go arithmetic module.

- **1095** (PR #170 follow-up) — GCP backends used to treat `SOCKERLESS_ENDPOINT_URL` as "simulator mode" inside image resolution, leaving Docker Hub refs unrewritten when pointed at the simulator. Removing that special case exposed the real missing simulator behavior: Artifact Registry remote-repository refs such as `us-central1-docker.pkg.dev/<project>/docker-hub/library/alpine:latest` still tried to fetch metadata from real GAR instead of the configured endpoint. Fixed by routing cloud-registry HTTP through the same endpoint override while preserving the cloud image ref, and by teaching the GCP simulator's Artifact Registry OCI surface to hydrate docker-hub remote-repo cache misses from the real local Docker image save stream. The regression is now covered through the normal external-client surfaces: official Google Artifact Registry SDK create/list, `gcloud artifacts` create/list with `CLOUDSDK_API_ENDPOINT_OVERRIDES_ARTIFACTREGISTRY`, Terraform `google_artifact_registry_repository` remote-repo apply/destroy, and the standard OCI Distribution `/v2/.../manifests/...` path. Verified with `go test -run TestResolveGCPImageURI` in `backends/gcp-common`, `go test -run TestFetchImageMetadata` in `backends/core`, `GOWORK=off go test -run TestDoesNotExist ./...` in `simulators/gcp`, `GOWORK=off go test -run TestArtifactRegistryDockerHubRemoteRepositorySDKAndOCI ./...` in `simulators/gcp/sdk-tests`, `GOWORK=off go test -run TestArtifactRegistryDockerHubRemoteRepositoryCLIAndOCI ./...` in `simulators/gcp/cli-tests`, `GOWORK=off go test -run TestTerraformApplyDestroy ./...` in `simulators/gcp/terraform-tests`, and `make faas-smoke-test-all`.

- **1072 / 1074 / 1076 / 1094** (PR #170 follow-up) — Added simulator-backed FaaS runner smoke coverage for Lambda, Cloud Run Services, GCF, ACA Apps, and AZF. Each backend now has a `Test*FaaSE2ESmoke` that creates a container, starts the real simulator-backed bootstrap/agent path, runs multiple real `docker exec` steps, waits, and removes the container. The tests are wired through standardized Make targets (`make backends/<backend>/test-faas-smoke` and `make faas-smoke-test-all`) and CI runs the aggregate target after normal backend package tests. The new guards surfaced BUG-1094: service/app-backed containers could finish or stop, but `ContainerRemove` still saw Cloud Run Services as running or ACA Apps as already gone. Cloud Run and ACA remove/stop/wait paths now honor recorded invocation/stop results so `wait` and `rm` preserve Docker lifecycle semantics across service/app-backed resources. BUG-1076's remaining AZF bootstrap gap is closed with focused tests for exec envelope stdin/env/workdir, default invoke, workdir, timeout parsing, and argv decode errors. Live-cloud validation remains open as BUG-1075 only.

Earlier resolved history: 1033 bugs filed and fixed across phases 86–165.

- **1069 / 1091 / 1092 / 1093** (Phase 168.13) — ACA Apps now have a real overlay materialization path instead of deploying raw workload images while waiting for a reverse-agent that cannot exist. With `SOCKERLESS_ACA_USE_APP=1`, non-overlay images are wrapped through ACR Tasks using `SOCKERLESS_ACA_BOOTSTRAP`; the overlay Dockerfile copies the bootstrap into `/opt/sockerless`, sets it as ENTRYPOINT, keeps user entrypoint/cmd/workdir in runtime `SOCKERLESS_USER_*` env, injects the reverse-agent callback/container ID into the App spec, and marks sidecars with `SOCKERLESS_SIDECAR=1` so only the main container binds the App ingress port. Jobs stay on stock images and continue to start one-shot workloads without waiting for a bootstrap. The same chunk fixed CI run `26013037364`: GCF TestMain redundantly pulled `public.ecr.aws/docker/library/alpine:latest` after the eval image build had already pulled the same real base image, and ECR Public timed out. GCF and Cloud Run harnesses now materialize the real ECR Public alpine base with `docker build FROM public.ecr.aws/docker/library/alpine:latest` into the Docker Hub / AR refs the simulator needs; CI run `26013907371` caught that BuildKit does not necessarily leave the source `public.ecr.aws/.../alpine` tag available for retagging. Full ACA validation then filed and fixed BUG-1092: the Azure simulator truncated ACA Job execution container names to the first 12 chars of the execution name, causing real Docker/Podman name collisions such as `sockerless-sim-azure-execution-sockerless-1` under fast sequential tests. The simulator now uses the full execution name for the local container name. Verified with focused GCF + Cloud Run lifecycle tests, ACA arithmetic + full ACA backend simulator package tests, ACA overlay/spec tests through the ACA simulator harness, and backend-core tests.
- **1067 / 1071** (Phase 168.14) — Azure Functions now has a real in-container bootstrap at `agent/cmd/sockerless-azf-bootstrap`. The AZF backend can wrap stock images through ACR Tasks using `SOCKERLESS_AZF_BOOTSTRAP`, preserves user entrypoint/cmd/workdir in runtime `SOCKERLESS_USER_*` env, clears the Function App container argv so the bootstrap remains ENTRYPOINT, and reads `X-Sockerless-Exit-Code` from bootstrap responses so non-zero user exits propagate without HTTP-status guessing. Prebuilt local `sockerless-overlay/*` images take the same runtime env path for simulator coverage. The Azure simulator now invokes overlay Function containers over a real local HTTP listener, with `PORT`/`WEBSITES_PORT`, host metadata env, `host.docker.internal`, streamed AppTraces logs, and cleanup on Function App delete. The AZF bootstrap dials the reverse-agent WebSocket and sends `lifetime_expired` on SIGTERM, closing the AZF lifetime-expired gap. Verified with the full `backends/azure-functions` simulator package, focused AZF lifecycle/arithmetic tests, Azure simulator SDK tests, and the full `agent` package tests.

- **1090** (Phase 168.12) — CI run `26012702755` caught that the Azure SDK monitor tests still read `ContainerAppConsoleLogs_CL.Log_s` by fixed column ordinal after BUG-1068 added the real `ContainerAppName_s` column for ACA Apps. The simulator schema was correct; the test was too brittle. The tests now resolve `Log_s` by returned column name, preserving coverage for both ACA Job rows keyed by `ContainerGroupName_s` and ACA App rows keyed by `ContainerAppName_s`. Verified locally with the focused Azure SDK monitor tests.

- **1068 / 1089** (Phase 168.11) — ACA Apps in the Azure simulator now start real App-replica containers from the `Microsoft.App/containerApps` template instead of only storing ARM state. PUT materializes `minReplicas` containers with real images/commands/env/volume binds, host metadata routing, ACA sandboxing, managed-environment Docker networking when present, and replica handles tracked by App resource ID; update/delete stops the prior handles. `ContainerAppConsoleLogs_CL` now includes real ACA App rows keyed by `ContainerAppName_s`, while ACA Jobs keep `ContainerGroupName_s`. SDK and CLI tests assert a real arithmetic image executes under the App surface and its output is queryable through Log Analytics. The same chunk fixed CI run `26011828766`: ACA Jobs were blocking `docker start` for stock one-shot job images by waiting for a reverse-agent bootstrap that those images do not contain. Jobs now launch real ACA Job executions and return; exec/archive APIs still require a registered reverse-agent and fail loud when absent. Verified locally with focused ACA CI-failure tests, full `backends/aca` simulator package coverage, and Azure Apps SDK coverage.

- **1083 / 1084 / 1085 / 1086 / 1087 / 1088** (Phase 168.10) — Fixed the CI-only regressions from PR #168 runs `26002912832`, `26003621756`, `26003955164`, and `26004958009`. Smoke tests now start Cloud Run/ACA backends on `0.0.0.0:3375`, set a real `SOCKERLESS_CALLBACK_URL` reachable from workload containers, create the smoke containers with Docker's `OpenStdin` contract for non-exec one-shot flows, and preload `alpine:latest` into the host Docker daemon because the simulators execute real workload containers there. The GCP simulator's overlay readiness probe now dials the bootstrap TCP listener instead of issuing `GET /`, because `/` is the real Cloud Run/GCF invocation endpoint and probing it can run the user's command before reverse-agent registration. Cloud Run v2 Service `EnvVar` decoding now accepts both real proto-JSON oneof shapes (`value` and nested `values.value`) so `SOCKERLESS_CALLBACK_URL` / `SOCKERLESS_CONTAINER_ID` cannot be dropped before overlay invocation; SDK coverage asserts those env vars survive Create/Get round-trip. The GCP overlay build platform is now explicit (`SOCKERLESS_GCP_BUILD_PLATFORM` / `gcp.build_platform`, default `linux/amd64`), simulator integration tests set it to the real host platform, and service invocation runs the local overlay image's inspected OS/architecture. The Cloud Run Service simulator now keeps a real per-Service container instance alive until `DeleteService` and returns Cloud Run's HTTP status contract without translating exit headers into 5xx. GCF pod-service cloud state treats a live reverse-agent as authoritative running state only for transport-only invoke errors; real one-shot invocation results still report exited so lifecycle wait/inspect/remove keeps Docker semantics. The simulator now retries only pre-response bootstrap transport errors during Cloud Run/GCF cold start, matching cloud request queuing without replaying completed HTTP responses. Verified locally with focused Cloud Run/GCF overlay exec tests, full GCF backend simulator package coverage, Cloud Run/ACA smoke containers, Cloud Run Services SDK env round-trip coverage, and simulator compile checks.

- **1070 / 1078 / 1079 / 1080 / 1081 / 1082** (Phase 168.9) — Cloud Run + GCF overlay-baked bootstrap exec is now validated end-to-end against the GCP simulator. Added real backend integration tests that build the bootstrap, let the backend overlay-wrap a plain `alpine` image, start the Cloud Run Service / GCF-backed Service path, wait for reverse-agent registration, and run `docker exec` through the WebSocket path. The validation surfaced and fixed three simulator fidelity gaps: Cloud Run v2 Services now accept SDK numeric `VpcAccess.egress` enum values and round-trip canonical strings; overlay invocation starts `linux/amd64` because the GCP backends build overlay images with Cloud Build `Platform: "linux/amd64"`; `/v2-services-invoke/...` now passes the persisted revision container environment into the overlay container so `SOCKERLESS_CALLBACK_URL` and `SOCKERLESS_CONTAINER_ID` reach the bootstrap. Follow-up full-package validation fixed two harness/backend cleanup regressions: the overlay exec e2e tests now run in explicit subprocesses so unrelated package tests keep their original direct paths, and Cloud Run/GCF `ContainerRemove` skips driver-disconnect for Docker's implicit `default` / `bridge` networks while still cleaning explicit managed networks. Verified with SDK, CLI, Cloud Run backend, and GCF backend tests, including full `SOCKERLESS_TEST_TARGET=sim go test -count=1` in both GCP FaaS backend packages.

- **1045** (Phase 166, codex review finding x4) — Codex review of Phase 166.1's new sim handlers found 4 state-persistence gaps where the handler acknowledged a state-changing request but didn't persist the change, so the follow-up Describe / Get / List returned stale state. All four violate the user's "faithful API compliance (identical to real cloud)" directive. Fixed:
  - **DynamoDB PITR** (`simulators/aws/dynamodb.go`): `UpdateContinuousBackups` echoed the new status but `DescribeContinuousBackups` always returned `DISABLED`. Added `PITRStatus` to `DDBTable`; Update persists, Describe reads back.
  - **DynamoDB TTL**: same pattern. Added `TTLStatus` + `TTLAttributeName` to `DDBTable`; Update persists, Describe reads back. terraform's `ttl` block polls Describe after Update for convergence.
  - **DynamoDB tags**: `TagResource` accepted the request but dropped the tags; `ListTagsOfResource` returned empty. Added `Tags` to `DDBTable` + helper `ddbTableByArn` (Tag CRUD takes ResourceArn, not TableName). Upsert semantics match real AWS (re-tag with same Key replaces Value). `UntagResource` now drops keys from the persisted set.
  - **KMS custom key policy** (`simulators/aws/kms.go`): `GetKeyPolicy` always returned the default policy even after `CreateKey` with a custom `Policy` field. Added `PolicyJSON string` field to `KMSKey` (matches real KMS which stores+returns the JSON-encoded string, not a parsed map — old `Policy map[string]any` field was wrong shape). `CreateKey` reads `Policy` from request + persists. New `TrentService.PutKeyPolicy` action also persists. `GetKeyPolicy` returns persisted-or-default.
  Verified: `cd simulators/aws/terraform-tests && GOWORK=off go test -count=1 -run TestStackProductionShape` — PASS locally (no regression). Each fix mirrors real-AWS state-persistence behaviour.

- **1041** (Phase 166) — Phase 165 deferred `google_service_account` because terraform-provider-google's IAM resources appeared to ignore `iam_custom_endpoint` and hit real `iam.googleapis.com`. Root-caused via `gh api` reading the provider source: `google_service_account` actually routes through `iambeta.NewClient` (`google/services/iambeta/client.go`) which uses `iam_beta_custom_endpoint` — a DIFFERENT setting from `iam_custom_endpoint`. Added `iam_beta_custom_endpoint = "${var.endpoint}/v1/"` to the provider config + the `google_service_account` resource to terraform-tests/main.tf. Verified PASS locally. (The remaining `google_cloudfunctions2_function` + Compute instance + Cloud Build + Logging + Pub/Sub follow-ups need their own sub-phase due to multi-resource orchestration / missing sim Pub/Sub.)

- **1040** (Phase 166) — Phase 165 deferred ACA + AZF + ACR + Application Insights + identity + private DNS terraform coverage because `azurestack` provider doesn't expose those resources and `azurerm` needs a different cloud-config bootstrap. The sim already shipped `/metadata/endpoints?api-version=2022-09-01` (single-JSON-object AzureCloud config) + `/<tenant>/oauth2/v2.0/token` (real-shape Azure AD JWT mint) + `/.well-known/openid-configuration` + JWKS endpoints — so wiring `azurerm` against it was a config-only step: `metadata_host = trimprefix(var.endpoint, "https://")` + `subscription_id` + `tenant_id` + the (fake) client_id/client_secret. Added 12 azurerm-driven resources to terraform-tests/main.tf: resource_group, container_registry (Standard SKU), user_assigned_identity, private_dns_zone, log_analytics_workspace, application_insights, container_app_environment, container_app, container_app_job (ACA runner-job primitive!), service_plan, storage_account, linux_function_app (AZF runner-workload primitive!). apply_test.go asserts canonical ARM paths for each. README updated. Test is darwin-blocked locally (Go cgo Security framework ignores SSL_CERT_FILE for both providers; GODEBUG=x509usefallbackroots=1 only kicks in when platform pool is empty, doesn't bridge); CI runs in Docker. Pre-validated each resource shape via `terraform validate` + curl-probed the sim's metadata + token endpoints. The remaining gap (Key Vault data-plane keys/secrets) needs per-vault subdomain routing — separate sub-phase.

- **1042** (Phase 166) — Phase 165 surfaced 5 sim handler gaps that blocked the AWS terraform-provider expansion (S3 + DynamoDB + KMS + SecretsManager + SSM); user directive *"if there's a missing feature, let's add it, let's do it right"* — close them as real handler implementations matching real-AWS API shapes, not stubs. Fixed:
  - **S3** (`simulators/aws/s3.go`) — added 14 sub-resource query handlers to `GET /s3/{bucket}` for `?policy`, `?versioning`, `?accelerate`, `?logging`, `?location`, `?lifecycle`, `?cors`, `?website`, `?replication`, `?encryption`, `?tagging`, `?policyStatus`, `?publicAccessBlock`, `?object-lock`, `?ownershipControls`, `?requestPayment`, `?notification`, `?acl`. Each returns the real-AWS "not-configured" shape (`NoSuchBucketPolicy` / `NoSuchLifecycleConfiguration` / etc.) or an empty configuration document (matching what real S3 returns for a fresh bucket). terraform-provider-aws fans out across these on Create+Read to populate the resource attributes. Provider config gained `s3_use_path_style = true` + endpoint suffix `/s3` so the provider sends `endpoint/s3/{bucket}/...` (matching sim's route) instead of `{bucket}.endpoint/...` (virtual-hosted-style).
  - **DynamoDB** (`simulators/aws/dynamodb.go`) — root-caused via TF_LOG=TRACE: terraform-provider-aws v6's Create flow calls `waitTableActive` (works against existing sim) then `waitTableWarmThroughputActive`. The latter's `statusTableWarmThroughput` returns empty state when `output.WarmThroughput == nil` → waiter loops 21 times → "couldn't find resource". Added `WarmThroughput` field to `DDBTable` (real AWS returns this on every Describe with Status=ACTIVE for on-demand tables; ReadUnitsPerSecond=12000, WriteUnitsPerSecond=4000). Also surfaced + fixed a Content-Type bug: sim used `application/json` for all responses but DynamoDB's awsJson1_0 protocol expects `application/x-amz-json-1.0` — added `writeDDBJSON` wrapper sweeping all 21 sim.WriteJSON sites in dynamodb.go. Added `TableId`, `ProvisionedThroughput` (zero-filled), `TableClassSummary`, `DeletionProtectionEnabled` fields. Registered `DescribeContinuousBackups` + `DescribeTimeToLive` + `ListTagsOfResource` + `UpdateContinuousBackups` + `UpdateTimeToLive` + `TagResource` + `UntagResource` (real-AWS shapes returning the no-config defaults).
  - **KMS** (`simulators/aws/kms.go`) — registered `TrentService.GetKeyPolicy` (returns canonical AWS default key policy with root-account allow-all), `TrentService.ListResourceTags`, `TrentService.GetKeyRotationStatus` (returns `KeyRotationEnabled=false` matching real-AWS new-key default).
  - **Secrets Manager** (`simulators/aws/secretsmanager.go`) — registered `secretsmanager.GetResourcePolicy`. Important: real Secrets Manager OMITS the `ResourcePolicy` field when no policy is set (not empty string!). Returning `ResourcePolicy: ""` makes terraform try to JSON-parse the empty string and crash with "unexpected end of JSON input"; the fix was to only return `{ARN, Name}` when no policy exists. Added `resolveSMSecret` helper to accept Name-or-ARN lookup.
  - **SSM** (`simulators/aws/ssm_parameters.go`) — registered `AmazonSSM.AddTagsToResource` + `RemoveTagsFromResource` + `ListTagsForResource` with real-AWS upsert semantics (re-tag with same Key replaces Value), Parameter-exists validation, empty-list-when-none-set response shape. Added `SSMTag` type + `ssmResourceTags` store keyed by `<ResourceType>/<ResourceId>` (real SSM stores tags out-of-band from the resource).
  - **TF test harness** — `helpers_test.go` now pipes TF_LOG / TF_LOG_PATH so future sim-handler-gap debugging can use `TF_LOG=TRACE go test`.
  Verified: `cd simulators/aws/terraform-tests && GOWORK=off go test -count=1 -run TestStackProductionShape` — PASS locally. main.tf grew from 26 → 33 resources. apply_test.go asserts canonical ARNs for each new resource.

- **1043** (Phase 165, codex review finding) — `simulators/azure/terraform-tests/main.tf:77` set `account_kind = "StorageV2"` on the new `azurestack_storage_account` resource. The `azurestack` provider validates `account_kind ∈ {Storage, BlobStorage}` (StorageV2 is Azure-public-cloud only) and rejects the value at plan time before the request ever reaches the sim. Locally `terraform plan` reproduced the error: `expected account_kind to be one of [Storage BlobStorage], got StorageV2`. Fix: `account_kind = "Storage"` + a comment noting the sim accepts both but the azurestack provider doesn't. Caught by `codex review --base main` non-interactive CLI; would have CI-red'd on first push otherwise (azure terraform test is darwin-blocked, so the local `go test` in Phase 165.5 didn't surface it).

- **1044** (Phase 165, codex review finding) — `simulators/gcp/gcs.go::POST /upload/storage/v1/b/{bucket}/o` + `GET /storage/v1/b/{bucket}/o/{object}` (the BUG-1038-sub-fix selfLink additions) interpolated the raw object name into the URL path without `url.PathEscape`. For object names containing `/`, ` `, `?`, `#`, etc., the resulting `selfLink` / `mediaLink` would point at a different object or be a malformed URL when consumers parse it back. The Phase 165 terraform-test happened to use `tf-test-artifact.txt` so the bug didn't bite; real GCS source-archive workflows commonly nest object names. Fix: `url.PathEscape(objectName)` for both link fields. Caught by `codex review --base main`.

- **(BUG-1038 sub-fix: GCS object selfLink/id/mediaLink)** (Phase 165) — Surfaced + fixed during the BUG-1038 expansion. The sim's `POST /upload/storage/v1/b/{bucket}/o` and `GET /storage/v1/b/{bucket}/o/{object}` handlers omitted the `kind`/`id`/`selfLink`/`mediaLink`/`generation` fields that real GCS returns. terraform-provider-google's `google_storage_bucket_object` reads `selfLink` into the resource's `self_link` attribute on refresh; without the field, the attribute came back empty in terraform state. Added all five fields to both handlers; `self_link` round-trips correctly. Same shape as BUG-1029 — the terraform provider's call sequence surfaces canonical-field omissions the SDK happens to tolerate.

- **1038** (Phase 165) — GCP terraform-tests expanded from 11 → 15 resources with the runner-foundation additions: `google_compute_subnetwork` (pod-attach surface), `google_compute_firewall` (allow-rules from sockerless backends), `google_storage_bucket_object` (build-artifact / function-source staging surface). Two of the user-requested gap targets — IAM service account and Cloud Functions Gen2 — required deeper integration work and were staged forward as BUG-1041 (filed in this PR): terraform-provider-google's `google_service_account` doesn't honour `iam_custom_endpoint` and hits real iam.googleapis.com; `google_cloudfunctions2_function` needs a real GCS-staged source archive (multi-resource orchestration). Cross-resource references converge: subnet.id contains `projects/test-project/regions/us-central1/subnetworks/tf-test-subnet`; firewall.id contains `projects/test-project/global/firewalls/tf-test-fw-allow-ssh`; GCS object self_link references both bucket + object name. Local CI green (`SOCKERLESS_TEST_TARGET` not required for terraform-tests; rebuilds the sim binary each run).

- **1039** (Phase 165) — Azure terraform-tests covered only 5 networking primitives. Expanded with two `azurestack`-supported additions material to the runner pod lifecycle: `azurestack_storage_account` (Azure Files / runner shared volumes) and `azurestack_key_vault` (runner credential storage). Pre-validated via curl against the running sim — PUT `Microsoft.Storage/storageAccounts/tftestsa12345` returns 200 with subdomain blob endpoint (`http://tftestsa12345.blob.<host>:<port>/`); PUT `Microsoft.KeyVault/vaults/tf-test-kv` returns 200 with subdomain vault URI (`http://tf-test-kv.vault.<host>:<port>/`). `apply_test.go` now asserts canonical ARM-id paths + the subdomain-host shape the azurerm storage / keyvault SDK clients parse out of the URL. README updated to list every covered resource + explicitly call out the azurerm-research gap as BUG-1040. CI runs in Docker (the existing darwin-skip is preserved for local runs).

- **1036** (Phase 165) — The BUG-994 / 1014 / 1026 phase-metadata sweep stopped at production code; ~50 test-file docstrings still carried `// Phase NNN (PNNN.x) — …` lineage headers + a handful of `// BUG-NNN` refs in test bodies. Rewrote every header to describe the **contract under test** instead of the phase that introduced it. Examples: `// Phase 154 (P154.1) — Reactions API parity.` → `// Reactions API parity — issue / PR / commit / comment reaction CRUD against the GitHub-compatible /repos/{}/issues/{}/reactions surface.`; `// Phase 84 invariant: two simulators pointed at different SIM_DATA_DIR values share no state.` → `// Per-instance state isolation invariant: two simulators pointed at different SIM_DATA_DIR values share no state.`. Touched 27 test files across `bleephub/`, `simulators/{aws,gcp,azure}/{shared,sdk-tests,cli-tests,terraform-tests}/`, `agent/cmd/sockerless-cloudrun-bootstrap/`, `tests/`, `backends/{aca,ecs,azure-functions,cloudrun-functions}/`, `terraform/modules/ecs/test/`. Preserved `// Phase 1: …` style runner-lifecycle phase labels in `tests/gitlab_runner_e2e_test.go` — those are docker-compose lifecycle phase numbers, not project phases. Verified: `go vet ./...` clean in every touched module.

- **1035** (Phase 165) — Three `w.Write([]byte(...))` / `w.Write(data)` sites omitted the explicit-discard `_, _ = w.Write(...)` form used everywhere else in the same packages: `bleephub/gh_oauth.go:171` (auto-approved-device-flow HTML page), `bleephub/artifacts.go:321` (artifact download), `simulators/azure/functions.go:290` (Azure-Functions crash-response body). Style inconsistency was the real concern — the explicit-discard form is the codebase convention and signals "intentional swallow because response headers are already committed and the client may be gone." Standardised all three on `_, _ = w.Write(...)`. Verified: `go build ./...` clean in both modules.

- **1034** (Phase 165) — `backends/lambda/agent_e2e_integration_test.go:168` carried `var _ = fmt.Sprintf` with the lying comment "fmt is used by the framing demuxer when debugging." The claimed consumer `demuxDockerStream()` (lines 144–164) never calls `fmt`; the only `fmt.` reference in the file was the silencer itself. Same shape as BUG-1022/1024 dead-import silencers. Dropped the silencer + the `fmt` import. If a future debug path needs `fmt`, the import comes back as a one-liner with the print site.

- **1033** (Phase 165) — Five `io.Copy(w, rc)` calls in image-streaming + build response paths in `backends/core/{build.go,handle_images.go}` swallowed the mid-stream copy error silently. Sites: `build.go:515` (build event stream), `handle_images.go:80` (image pull), `:115` (image load non-quiet), `:472` (image push), `:493` (image save tar). Fix shape per site: `if _, err := io.Copy(w, rc); err != nil { s.Logger.Debug().Err(err).Msg("<op> stream copy failed — client likely disconnected") }`. Debug level because by the time io.Copy fails, the response headers are already committed and the client is gone — there's nothing recoverable, but the cause should be greppable (matches the BUG-1025 pktline pattern). Verified: `cd backends/core && go test ./...` green.

- **1032** (Phase 164 terraform-test expansion) — `simulators/azure/terraform-tests/main.tf` had only `azurestack_resource_group` coverage; expanded to also cover `azurestack_virtual_network`, `azurestack_subnet`, `azurestack_network_security_group`, `azurestack_network_security_rule`. Five resources now exercise five sim slices (Microsoft.Resources/resourceGroups, Microsoft.Network/virtualNetworks, /subnets, /networkSecurityGroups, /networkSecurityGroups/securityRules). `apply_test.go` now reads outputs and asserts canonical ARM-id round-trips. Pre-validated each PUT against the running sim via curl — all 5 returned 200/201 with the expected canonical-ARM-id payload. CI runs in Docker (the existing darwin-skip is preserved for local runs).

- **1029** (Phase 164 terraform-test expansion) — `simulators/gcp/secretmanager.go` was missing `POST /v1/projects/{p}/secrets/{s}/versions/{v}:enable` / `:disable` / `:destroy` handlers. `terraform-provider-google` POSTs `:enable` immediately after `:addVersion` on create (versions default to ENABLED but the provider still expects the explicit enable to return 200); without the handler the `google_secret_manager_secret_version` resource always failed apply with 404. Added all three state-transition handlers plus a plain `GET .../versions/{v}` (no `:action` suffix) that the provider uses to read back the version after create.
- **1030** (Phase 164 terraform-test expansion) — `simulators/gcp/terraform-tests/helpers_test.go` carried the same close-then-bind port-allocator race that Phase 160 fixed in `sdk-tests` (BUG-993). Pattern: allocate listener 1, capture port, close it, allocate listener 2 — between the close and the second allocate the OS could reassign the just-freed port to the second listener, causing the sim's HTTP and gRPC servers to want the same port. Fix: allocate both listeners while both are open, then close both — simultaneous-open listeners cannot collide.
- **1031** (Phase 164 terraform-test expansion) — Surfaced + fixed three under-covered GCP terraform-provider slices by expanding `simulators/gcp/terraform-tests/main.tf` from 4 resources (compute_network, compute_disk, public+private DNS zones) to 11 resources covering compute, dns, artifactregistry, cloud_run_v2 (Service + Job), storage, secretmanager. Provider endpoint settings (`artifact_registry_custom_endpoint`, `cloud_run_v2_custom_endpoint`, `secret_manager_custom_endpoint`) added with the right `/v1/` or `/v2/` suffix. Cloud Run v2 Service + Job both require `deletion_protection = false` for terraform destroy to clean up. `TestTerraformApplyDestroy` now asserts on canonical resource-path round-trip for AR repo `.id` (`projects/.../locations/.../repositories/...`), CR Job `.id`, GCS bucket URL prefix (`gs://`), and Secret version ID structure.

- **1026** (Phase 164 third-pass sweep) — Two test files asserted on Phase metadata in error strings (`docs/VIBE_CODING.md` pattern 28 — implementation-coupled tests asserting on bug-tracking metadata): `bleephub/gh_actions_test.go:233` "status = %d, want 422 (Phase 130 doesn't ship dispatch)" and `simulators/azure/auth_test.go:34` `"alg = %q, want HS256 (was alg:none before Phase 121b)"`. Re-derived both assertion messages from the contract — "rerun is unimplemented and must surface that, not silently succeed" and "alg:none is auth-bypass" — so the assertion remains useful long after the bug-tracking lineage is forgotten.
- **1027** (Phase 164 third-pass sweep) — `cmd/sockerless-admin/api_topology_resources_test.go:196` had a naked `t.Skip()` with no message — operators reading CI output had no way to know *why* the test skipped (BUGS.md rule: "conditional `t.Skip` for missing config — all file as bugs and get real fixes. Tests run or fail loud; never skip silently"). Added explanatory message ("skip concurrent rollup test in -short mode (spawns 5 upstream servers)"). The other `t.Skip()` sites in the repo already carry messages.
- **1028** (Phase 164 third-pass sweep) — `simulators/azure/terraform-tests/` docs↔code mismatch (pattern 33): `apply_test.go:11+22` + `README.md:3+29` referenced the `azurerm` provider, but `main.tf` actually uses `azurestack`. Aligned the docs + test comments to reflect the actual provider used (the sibling `azurerm` cloud provider drives the same ARM endpoints — clarified that both work against the sim).

- **1023** (Phase 164 re-verification pass) — `github-runner-dispatcher-gcp/internal/spawner/spawner.go::stringifyJobState` was unused (zero callers) and carried `//nolint:unused // kept for diagnostics`. Same shape as the BUG-1021 `flexInt64` rip-out (dead code held for hypothetical future debug-helper use, pattern 14/27 per `docs/VIBE_CODING.md`). Deleted the function + its preceding doc-comment. If a future operator-facing diagnostic needs Job-definition reconciliation state, that PR adds the one-line helper back.
- **1024** (Phase 164 re-verification pass) — `tools/http-trace/main.go:269` carried `var _ = httputil.DumpRequest // keep import for ad-hoc tweaks` — an explicit "keep the import in case I want to use it later" silencer + the `net/http/httputil` import that no other code path consumed. Same shape as BUG-1022. Dropped the silencer + the import; if a future tracer needs DumpRequest the import comes back as a one-liner.
- **1025** (Phase 164 re-verification pass) — `bleephub/git_http.go` smart-HTTP advertise path silently swallowed `pktline.Encoder.Encodef` + `Flush` errors at three sites (the initial `# service=...` advertisement + the two empty-repo flushes inside the upload-pack / receive-pack branches). If the response connection drops mid-advertisement the operator gets no log entry. Added `s.logger.Debug()` calls on each error (Debug level because the client is gone — there's nothing recoverable, but the cause should be greppable). The post-encode `info.Encode(w)` error paths were already logged via `s.logger.Error()`.

- **1014** (Phase 164) — Continuation of the BUG-994 phase/BUG-ref sweep. 10 production-code sites still carried `Phase NNN` / `Phase NNNb` / `Phase D` / `pre-Phase-132` / `Phase-92-style` / `Future sub-phase` references in code comments. Stripped each — preserved the *why* in every case per the BUG-994 rule:
  - `simulators/aws/wafv2.go:17` "REGIONAL scope out of scope per Phase 159 plan" → "REGIONAL scope intentionally out of scope".
  - `backends/lambda/cloud_state.go:98,198` "Phase 89 / step 2 cross-cloud sibling" → comment header only (the *what* still documented).
  - `simulators/aws/ecs.go:802` "Future sub-phase: derive from taskDef.RuntimePlatform.CpuArchitecture" → "taskDef.RuntimePlatform.CpuArchitecture is not yet honoured here — sim runs a single arch".
  - `simulators/aws/lambda_runtime.go:313` "Future sub-phase: derive from fn.Architectures" → "fn.Architectures is not yet honoured here — sim runs a single arch".
  - `simulators/gcp/iam.go:128` "Phase 126 (Access driver, id-token category) calls generateIdToken" → "Access driver's id-token category calls generateIdToken".
  - `backends/ecs/backend_impl.go:997` "Phase-92-style teardown is a separate concern" → "whole-filesystem reclamation (docker system prune --volumes style) is a separate concern handled by the operator".
  - `bleephub/gh_oauth.go:95` "preserved verbatim from the pre-Phase-132 handler. Auto-approved" → "Auto-approved (sim policy: device codes mint a token on the first poll instead of requiring an out-of-band user confirmation)".
  - `simulators/testdata/lambda-runtime-handler/main.go:31` "ship the real sockerless-lambda-bootstrap in Phase D" → "this testdata fixture is single-shot so the sim's ECS-style 'start a container per invocation' wiring works against it".
  - `bleephub/persistence.go:29` "NOT persisted in this phase — switching to filesystem.Storage is a separate refactor" → "Git storage (go-git in-memory) is not persisted — switching to filesystem.Storage is a separate refactor; repos do not survive a restart even with BLEEPHUB_PERSIST=true".
  - `bleephub/gh_misc_endpoints.go:27` "per-surface depth lands as later phases when a real consumer needs it" → "per-surface depth deepens when a real consumer needs it".
  The `backends/aws-common/build.go:239,240,241` references to `build.Phases` are *not* phase metadata — those are AWS CodeBuild SDK field names (`cbtypes.BuildPhase`). Verified by reading the SDK type.

- **1020** (Phase 164) — `bleephub/webhooks_payloads.go` carried `buildPullRequestPayloadWithInstallation` + `buildIssuesPayloadWithInstallation` since Phase 153, both with zero callers repo-wide. The `//nolint:unused // callers land in the workflow-trigger commit` directive promised the workflow-trigger commit would activate them — that commit never came. Same shape as BUG-1008 (the legacy `InitTracer` entry point ripped in Phase 161). Removed both helpers + the corresponding `var _ = json.Marshal` unused-import silencer (the file's only remaining `json.` reference) + the now-unused `encoding/json` import. AI-as-expansion-engine pattern 27 anti-pattern (no pruning) per `docs/VIBE_CODING.md`.
- **1021** (Phase 164) — Stale `//nolint:unused` pragmas on `bleephub/gh_middleware.go` context helpers (`ghInstallationFromContext`, `ghInstallationTokenFromContext`, `ghUserToServerTokenFromContext`); the "consumers ship in subsequent commits on this branch" comment dated to Phase 153, but consumers DID land at `gh_apps_perms.go:60+61` and `gh_apps_rest.go:676+677`. Dropped the three nolint pragmas; rewrote each helper's docstring to name the actual consumers instead of pointing at a future commit. Also ripped `flexInt64` from `gh_request_decode.go` — its comment "Reserved for int64 fields that gh CLI might send — currently not consumed by any handler" + three `//nolint:unused` pragmas were dead code held for hypothetical future use (pattern 14 / 27); if a future endpoint needs flex int64, the endpoint's PR adds it back as a one-line `type flexInt64 int64` mirror of the existing `flexInt`.
- **1022** (Phase 164) — Six unused-import silencers across `bleephub/` + `simulators/aws/`: `var _ = json.Marshal` in `webhooks_payloads.go` (already ripped as part of BUG-1020) + `gh_pr_threads.go`; `var _ = time.Now` in `gh_app_hooks_rest.go`; `var _ = strings.ToLower` in `simulators/aws/amplify.go`; `_ = plumbing.ZeroHash // silence unused-import false alarm` in `store_workflow_files.go`; `_ = ownerLogin // suppress unused` in `gh_issues_graphql.go`. For each: dropped the silencer + the now-unused import (when the silencer was the only consumer). For `_ = ownerLogin` specifically — the variable was declared from the GraphQL `repo["owner"]["login"]` shape but never participated in the user filter (only `query` argument was used); reworked the resolver to drop the unread assignment and document that real GH would scope to org members / collaborators (bleephub doesn't model that graph yet).

- **1018** (Phase 164) — Two `backends/core` HTTP handlers silently swallowed request-decode errors:
  - `handleExecStart` did `_ = ReadJSON(r, &req)` — a malformed ExecStartConfig body produced an empty struct, then the handler hijacked the connection two statements down. Once hijacked the client parses error bytes as a multiplexed-stream frame and emits `unrecognized stream: <byte>`. Fix: surface `InvalidParameterError` *before* hijacking. `ReadJSON` already returns nil on empty body so the Docker SDK no-body exec-start path stays green.
  - `handleLibpodContainerCreate` decoded the body twice — once into a podman-specgen shape (lowercase fields) and once into Docker `ContainerCreateRequest`. The first decode was silent (`_ = json.Unmarshal`); the second was checked. Because the struct field sets differ (`command` vs `Cmd`), a malformed `command` field could fail the spec decode without failing the req decode, yielding a misleading "no image" downstream error instead of the real parse failure. Fix: check both decodes; propagate either error via `InvalidParameterError`.
- **1019** (Phase 164) — `backends/cloudrun-functions/cloud_state.go::functionToContainer` decoded the authoritative `SOCKERLESS_LABELS` env var as `if raw, err := base64.Decode(...); err == nil { _ = json.Unmarshal(raw, &labels) }` — both error paths silently flowed into the "legacy split-across-labels fallback" branch, so a malformed env var produced a ghost container with reconstructed-from-fallback labels rather than surfacing the inconsistency. Fix: changed `functionToContainer` signature from `api.Container` to `(api.Container, error)`; surfaces explicit "malformed SOCKERLESS_LABELS base64/JSON on function X" errors. Caller in `queryFunctions` logs the warn + skips the inconsistent resource (matches the pattern already used for `queryPodServiceContainers` partial failures). Sentinel `sockerlessLabelsPresent` distinguishes "env var absent → use legacy fallback" from "env var present but malformed → surface error".

- **1017** (Phase 164) — Five silent JSON-decode sites across AWS + GCP sims swallowed errors and emitted success. Fix shape per site, all matching the surrounding handler's existing error envelope:
  - `simulators/aws/wafv2.go::handleWAFUpdateRuleGroup` — `body, _ := readBodyJSON(r.Body); _ = json.Unmarshal(body, &req); _ = json.Unmarshal(body, &idReq)` → check every error and return `WAFInvalidParameterException` envelope, matching siblings `handleWAFCreateRuleGroup` / `handleWAFGetRuleGroup` / `handleWAFDeleteRuleGroup`.
  - `simulators/aws/amplify.go::handleAmplifyStartDeployment` — `_ = json.NewDecoder(r.Body).Decode(&req)` → strict decode with `errors.Is(err, io.EOF)` carve-out (StartJob body has only optional fields per real Amplify); malformed → 400 `BadRequestException`.
  - `simulators/gcp/cloudfunctions.go::invokeCloudFunctionProcess` — `SOCKERLESS_USER_ENTRYPOINT` / `SOCKERLESS_USER_CMD` base64+JSON decode pair silently emitted empty argv on malformed input. The backend↔sim contract is "always base64(json.Marshal(argv))" — a violation means a real bug in the backend → sim handoff. Surface as invocation error (`{"error":"malformed SOCKERLESS_USER_ENTRYPOINT base64: ..."}`, exit code 1) so the test harness sees the failure.
  - `simulators/gcp/artifactregistry.go::registerDockerImageFromManifest` — `_ = json.Unmarshal(data, &manifest)` for `mediaType` extraction. Manifest was validated upstream so this shouldn't fail; if it does, log to stderr + fall back to `contentType` from request header (better than silently recording empty mediaType).
  - `simulators/gcp/cloudrunjobs.go::newLRO` — `data, _ := json.Marshal(resource); _ = json.Unmarshal(data, &responseMap)`. The struct is in-process; marshal can only fail on chan/func/unsafe.Pointer fields — a regression there would be invisible in prod and break every consuming SDK. Panic with the resource typeName so the test failure is loud.
  Verified: `cd simulators/gcp && SOCKERLESS_TEST_TARGET=sim go test ./...` green; `cd simulators/aws/sdk-tests && SOCKERLESS_TEST_TARGET=sim go test -run 'WAF|Amplify|Stack'` green.

- **1016** (Phase 164) — Four bleephub write handlers silently swallowed malformed JSON: `handleOIDCCustomSubPut`, `handlePagesCreate`, `handleBranchProtectionPut`, `handleLockIssue`. A runner shipping `{"include_claim_keys":` (truncated body) would receive 201/200/204 and never learn its mistake. Real GitHub returns 400 with `{"message":"Problems parsing JSON",...}` on malformed JSON; bleephub now matches via `if err := Decode(...); err != nil && !errors.Is(err, io.EOF) { writeGHError(400, "Problems parsing JSON") }`. The `io.EOF` carve-out preserves the legitimate "no body provided" path for issue-lock (where empty body = lock with no reason, matching real `gh issue lock`). Coverage: new `bleephub/gh_misc_endpoints_decode_test.go` with 6 assertions — 4 malformed-JSON → 400, OIDC empty-body → 201, issue-lock empty-body → 204.

- **1015** (Phase 164) — `backends/cloudrun-functions/volume_translator.go:95` embedded the literal `(BUG-944)` in the operator-visible error returned when a `BackingGCSFuse` is requested on Cloud Functions Gen2; `volume_translator_test.go:78` asserted on the `"BUG-944"` substring — exact pattern-28 anti-pattern (test anchored to impl metadata). Dropped the `(BUG-944)` reference from the error message (the *why* — "Gen2 runs on Cloud Run Services which rejects the cache-TTL gcsfuse flags needed for cross-task safety" — and the *fix* — "Use Backing: gcs-sync instead (per-exec tar sync, no FUSE)" — remain). Test assertion re-derived from the contract (`"gcs-fuse"` + `"Cloud Functions"` + `"gcs-sync"`).

- **1013** (Phase 163) — Top-level `Makefile` carried a `# ── Legacy aliases ──` section preserving pre-Phase-79 pure-alias targets (`sim-test-{ecs,lambda,cloudrun,gcf,aca,azf,aws,gcp,azure,all}`, `test-{unit,e2e,agent,core,bleephub}`, `bleephub-test`, `bleephub-gh-test`) — every one of them just `$(MAKE) -s -C <dir> <target>`, redundant with the `%/<target>` path-delegation rule. User directive: "sockerless has no legacy, it's under active development." Deleted the section; reframed the surviving Docker-driven cross-cutting suites (`smoke-test-*`, `tf-int-test-*`, `e2e-{github,gitlab}-*`, `upstream-test-*`, `bleephub-gh-docker-test`, `check-backend-coverage{,-enforce}`) under canonical section headers. Surfaced + fixed a latent defect in the `%/<target>` pattern rule: `make bleephub/test` short-circuited with "up to date" because `bleephub/test/` exists as a real directory and GNU make treated the target as a present file; added `.PHONY: FORCE` + `FORCE:` and depended the pattern rule on `FORCE` so the recipe always delegates. Docs swept across `README.md`, `FEATURE_MATRIX.md`, `ARCHITECTURE.md`, `backends/README.md`, `simulators/README.md`, `bleephub/README.md`, `tests/README.md`, `docs/MAKEFILE_STANDARD.md`, `.claude/skills/manual-test/SKILL.md`. Stale invented targets (`stack-aws-ecs-up`/`-down`, `e2e-github-aws-ecs`, `docker-tf-int-test-azure`) replaced with real names. Stripped "legacy 1-sim + 1-backend tuple" comments from `make/stack.mk` + `make/components.mk` — the pre-canned topology is canonical, not legacy.

- **1001** (Phase 161, third pass + bleephub GraphQL completion sub-phase) — Following the placeholder-resolver work (1st pass) and the real `ReactionStore` wiring (2nd pass), the user requested completing the underlying bleephub GraphQL surfaces. Folded into PR #161 as an in-PR sub-phase:
  - **P161.16 PullRequest.comments** — `Comment.ParentType` added (`"issue"` / `"pull_request"`) so PR + Issue conversation comments share the bleephub `Comments` store like real GitHub. REST `/issues/{n}/comments` resolves to either an Issue or a PR. GraphQL `PullRequest.comments` returns real comments via `prCommentToGQLLocked`; PRComment field resolvers flipped from `unreachableFieldErr` to real data.
  - **P161.17 PullRequest.reviewThreads + P161.21 thread-resolve integration** — New `PullRequestReviewThread` + `PullRequestReviewComment` GraphQL types. Connection wired via `reviewThreadsForGraphQL` using existing `PRReviewCommentStore.ListThreads`. Thread `isResolved` flag flows through automatically (already toggled by the `resolveReviewThread` / `unresolveReviewThread` mutations).
  - **P161.18 ProjectV2** — New `ProjectV2Store` + `createProjectV2` + `addProjectV2ItemById` GraphQL mutations. `Issue.projectItems` connection returns real `ProjectV2Item` nodes when issues are added to a project. ProjectV2 field-value writes are unimplemented (out of scope for the minimum) but the connection-level shape works for `gh project create` + `gh project item-add`.
  - **P161.19 Comment edit history** — `Comment.LastEditedAt` + `EditorID`; REST `PATCH /repos/{}/issues/comments/{id}` mutates; GraphQL `includesCreatedEdit` / `lastEditedAt` / `editor` on `IssueComment` + `PRComment` wired to real metadata.
  - **P161.20 Comment minimization** — `Comment.MinimizedReason` + `MinimizedByID`; GraphQL `minimizeComment` + `unminimizeComment` mutations with classifier enum (OFF_TOPIC / OUTDATED / RESOLVED / DUPLICATE / SPAM / ABUSE).
  - **P161.22 Issue/PR locking** — `Issue.Locked` + `ActiveLockReason` + same on `PullRequest`; REST `PUT/DELETE /repos/{}/issues/{n}/lock` (DELETE via `/issues/{p1}/{p2}` dispatcher to avoid Go 1.22 mux conflict with `/issues/comments/{id}`); GraphQL `lockLockable` + `unlockLockable` mutations; comment-create returns 403 on locked parents.
  - **P161.23 PR.milestone** — wired `pullRequestToGQL` to look up the real Milestone via `pr.MilestoneID`; the existing `alwaysNil` resolver replaced.
  Real `gh` CLI smoke tests landed in `bleephub/test/run-gh-test.sh` under a Phase 161 parity section: `gh pr comment` + `gh pr view --json comments` exercises PR.comments; REST PATCH on a comment + GraphQL re-read exercises edit history (`includesCreatedEdit` flips); direct GraphQL `minimizeComment` exercises minimization; REST PUT lock + assert 403 on locked-conversation comment + REST DELETE unlock exercises locking; `createProjectV2` + `createProjectV2Field` + `addProjectV2ItemById` + `gh issue view --json projectItems` exercises ProjectV2 end-to-end. ProjectV2 field-value writes landed too: `ProjectV2Field` + `ProjectV2SingleSelectOption` + `ProjectV2ItemFieldValue` store types; `createProjectV2Field` + `updateProjectV2ItemFieldValue` GraphQL mutations; `ProjectV2Item.fieldValueByName(name)` resolves to real data when set.
- **1006** (Phase 161) — `cmd/sockerless-admin/config.go::discoverFromContexts` + `listContexts`, and `cmd/sockerless/client.go::activeAddr` + every `context*` subcommand in `cmd/sockerless/context.go`, silently fell back to reading `~/.sockerless/contexts/*/config.json` when `config.yaml` was missing. Per user direction "no legacy / no fallback during active development," ripped the JSON-context fallback branches everywhere. Admin's `discoverFromContexts` now only consults `config.yaml` + `admin.json`; CLI subcommands now error with "no config.yaml present — run `sockerless config init` or `sockerless config migrate` first" instead of silently using legacy state. `contextConfig` type kept in CLI strictly for the operator-initiated `sockerless config migrate` command (the explicit user-driven migration is OK; the silent runtime fallback was not).
- **1007** (Phase 161) — Admin legacy migration scaffolding fully ripped: `DeriveLegacyInstances` + its test deleted; `MigrateLegacyProjects` + its two tests deleted; `TopologyManager.legacyDir` + `LoadOrMigrate` simplified to `Load` (constructor signature: `NewTopologyManager(path string)`); `main.go` no longer threads `defaultProjectStoreDir()` into the topology manager; `TestTopologyManagerLoadMigrate` removed. Eight admin test files updated to the new signature via sed sweep. The remaining `ProjectConfig.SimPort/BackendPort` shape used by `project_manager.go` filed as BUG-1011 — its own substantial rewrite to instance-based lifecycle.
- **1009** (Phase 161, partial) — `github-runner-dispatcher-gcp/cmd/.../main.go` Orphan pod-Service sweep previously left services without owner labels untouched as "legacy (pre-owner-label rollout)." Replaced the silent skip with a clear `log.Printf` contract-violation warning naming the offending service so the operator can investigate or delete manually. Tracked further escalation (hard error or grace-window delete) as continued BUG-1009 (P3 Open).
- **1008** (Phase 161) — `InitTracer` in 6 modules (`cmd/sockerless-admin`, `backends/core`, `bleephub`, `simulators/{aws,gcp,azure}/shared`) was a legacy OTel entry point superseded by `InitObservability`. Zero non-test callers. Per user direction (no legacy support during active development), deleted the function + its imports footprint everywhere; migrated `otel_test.go` in each module to drive `InitObservability` instead.
- **994** (Phase 161) — ~60 production code comments referenced phase numbers (`// Phase 87b`) or BUG IDs (`// BUG-944`) — metadata that rots and belongs in commits / PRs / BUGS.md per the no-phase-refs-in-code rule (guiding principle 14). Repo-wide sweep across `backends/`, `simulators/`, `bleephub/`, `cmd/`, `api/`, `tests/`, `github-runner-dispatcher-*/`. Stripped phase/BUG refs from comments; preserved the *why* context (e.g. "BUG-985 invariant: open-failure must be caught" → "open-failure must be caught at the persistence-open boundary").
- **999** (Phase 161) — `backends/core/tags.go::TagSet.InstanceID` was marked `Deprecated: use Cluster instead for stateless model` but had 27+ active callers and a real purpose distinct from Cluster (sockerless-instance identity vs. cloud resource grouping). Audit confirmed both fields are load-bearing; dropped the misleading deprecation comment and rewrote the comment to clarify each field's role.
- **1001** (Phase 161, two passes) — *First pass:* bleephub GraphQL had `alwaysEmptyString` resolvers on required NonNull fields of `ProjectV2` / `ProjectV2Item` / `ProjectV2ItemFieldSingleSelectValue` / `PRComment` — fake "" / nil data for surfaces bleephub doesn't track. The parent connections return `emptyList`, so these resolvers are unreachable in practice. Replaced with `unreachableFieldErr` which returns a clear GraphQL error if invoked, so any future code path that does reach them surfaces the gap instead of silently delivering "" IDs. *Second pass (re-verification):* found `staticReactionGroups()` + `prStaticReactionGroups()` returning hardcoded zero counts on Issue.reactionGroups + PR.reactionGroups — the real `ReactionStore` was never consulted. Introduced `reactionGroupsForGraphQL(rs, parentType, parentID)` that queries `ReactionStore.ListReactions` and emits the GraphQL-shaped `[ReactionGroup]` with real per-content totals. Wired into `issueToGQL` (parentType `"issue"`), `commentToGQLLocked` (parentType `"issue_comment"`), and `pullRequestToGQL` (parentType `"pull_request"`). PRComment's remaining `alwaysFalse` / `alwaysNil` / `emptyList` resolvers (unreachable since PR.comments returns empty nodes) flipped to `unreachableFieldErr` for an honest contract end-to-end. Removed the now-dead `staticReactionGroups` + `prStaticReactionGroups` helpers.
- **996** (Phase 161) — 18 sites of `_ = sim.ReadJSON(r, &req)` across `simulators/{aws,gcp,azure}/*.go` swallowed JSON-decode errors. Replaced every site with the proper error-propagating pattern using the cloud-appropriate error envelope (`sim.AWSErrorf` / `sim.AzureErrorf` / `sim.GCPErrorf`). No remaining `_ = sim.ReadJSON` calls.
- **998** (Phase 161) — `decodeRegistryAuth` in `backends/core/handle_images.go` returned `("","")` on any base64/JSON decode failure, indistinguishable from "no auth header". Found to be dead code (no production callers; only tests anchored to the broken behavior). Deleted; the inline auth-decode in `handleImagePush` was tightened to return 400 `InvalidParameterError` on either malformed base64 or non-JSON body. Tests rewritten to drive the real handler via httptest (malformed-base64 → 400, valid-base64-non-JSON → 400) — no more Pattern-3 implementation-coupled assertions.
- **1002** (Phase 161) — `simulators/azure/acr.go` Replications list returned `[]` when the parent registry didn't exist. Added the parent-exists check matching the GET-registry handler's shape; returns `ResourceNotFound` when the parent registry is missing.
- **1003** (Phase 161) — `simulators/gcp/artifactregistry.go::buildOCIHandler` was a single-call-site abstraction. Inlined directly into the `srv.Handle("/v2/", ...)` call.
- **1004** (Phase 161) — `bleephub` seeded admin token was `bph_`-prefixed for "backwards compatibility with existing tests + integrations". User direction: no backward compatibility while under active development. Switched seed token to `ghp_` matching real GitHub classic-PAT prefix; swept every fixture / test / doc / UI reference.
- **1005** (Phase 161) — `bleephub/workflows.go` matrix fail-fast resolution had a 3-deep `foundJob.Def != nil && foundJob.Def.Strategy != nil && foundJob.Def.Strategy.FailFast != nil` chain. Extracted into a `JobDef.FailFast()` method that handles every nil case (including nil receiver) and returns the GitHub-spec default of true; runtime path is now `if foundJob.Def.FailFast() { ... }`.
- **995** (Phase 161) — `handleSystemDf`, `handleContainerList`, `handleImagePrune` in `backends/core/` read `s.Store.*` directly instead of dispatching through `s.self.<Method>` (BUG-991/992 lineage). Each handler had inline cloud/store/pending logic parallel to its `api.Backend` impl. Fix: delegate each handler to `s.self.SystemDf` / `s.self.ContainerList` (with HTTP-specific size + before/since / limit post-filters) / `s.self.ImagePrune`. Consolidated the richer prune logic into `BaseServer.ImagePrune` so the delegate doesn't regress. Refactored `handleLibpodContainerList` to use a shared `collectContainers` helper — fixes a latent bug where pending creates were silently dropped in the no-CloudState path. Kept `collectAllContainers` separate for SystemDf's union-of-sources semantics.
- **997** (Phase 161) — 18 sites across `bleephub/{store,store_repos,gh_apps_store,gh_apps_user_tokens,gh_oauth}.go` silently swallowed persistence writes via `_ = st.persist.Put(...)` / `_ = st.persist.Delete(...)`. Fix: add `Persistence.MustPut` + `Persistence.MustDelete` (`log.Fatalf` on write failure, matching `MustNewPersistence`'s open-time invariant). Sweep every site to the Must* variants.
- **1000** (Phase 161) — `bleephub/auth.go::handleOAuthToken` previously returned a valid 1-year `alg:none` JWT for any input — auth-bypass. Fix: validate `grant_type`, `client_assertion_type`, and the RS256 `client_assertion` JWT signature against the agent's registered RSA public key (Modulus + Exponent from `POST /_apis/v1/Agent/{poolId}`). JWT `iss` must match a known agent ClientID; `exp` honored. OAuth-envelope errors (400 / 401). Tests rewritten to drive a real RSA keypair + signed assertion (success / missing-assertion → 400 / unknown-client → 401), replacing the prior Pattern-3 implementation-coupled test that asserted only `access_token != nil` on an empty body.
- **993** (Phase 160) — Close-then-bind port-allocator race in `simulators/gcp/sdk-tests/{helpers_test.go,quota_test.go}`. Pattern was `ln := Listen(:0); port = ln.Addr().Port; ln.Close(); ln2 := Listen(:0); grpcPort = ln2.Addr().Port; ln2.Close()` — between `ln.Close()` and `ln2 := Listen()` the OS could re-assign the just-freed port to `ln2`, so the sim's HTTP and gRPC servers both wanted the same port and the second `bind()` failed with "address already in use". Surfaced on PR #160 CI 2026-05-16 (`TestSDK_RegionalCPUQuota_RejectsCloudFunctionsDeploy`). Fix: allocate both listeners while both are open, then close both — simultaneous-open listeners cannot collide by construction. Verified locally with `count=3` reruns; ports are now guaranteed sequential.
- **992** (Phase 158) — Sibling of BUG-991. `handleImageList` in `backends/core/handle_images.go` read `s.Store.Images.List()` directly with extensive filter logic, never calling `s.self.ImageList()`. For passthrough backends (docker) this returned `[]` even when the upstream daemon had images. Fixed by replacing the 100-line in-handler filtering with a thin delegate to `s.self.ImageList(opts)` — each backend's override knows where the truth lives (docker → upstream daemon; cloud backends → `ImageManager` which merges Store + cloud registry). Verified: `docker images` against `backends/docker` now returns the upstream daemon's images. Volumes + networks already delegated correctly; no other list handler affected. Surfaced during BUG-991 investigation on 2026-05-13.
- **991** (Phase 158) — Classic fallback-hiding-bug. `docker run --rm` against `backends/docker` returned `error waiting for container: No such container` because `handleContainerWait`'s non-CloudState branch checked `s.Store.Containers.Get(id)` directly and short-circuited to 200/StatusCode=0 on `condition=removed`. The wait fires *before* start in the docker CLI's foreground flow, so the local Store lookup races and lies. Fixed by replacing the Store-direct branch with `s.self.ContainerInspect(ref)` (which delegates to upstream on passthrough backends) + `s.self.ContainerWait` for the actual block. Also removed the parallel `condition=removed → StatusCode: 0` fallback in `BaseServer.ContainerWait` itself — callers wanting "already removed = success" semantics must `Inspect` first themselves, never return success on a missing resource. Surfaced 2026-05-13 during Phase 157 docs sample-capture; the symptom directly motivated Phase 158's vibe-coding-anti-pattern doc + skill work.

Phases 154 / 155 / 156 / 157 closed zero new bugs — broad GitHub API sweep + docs refresh + component-adaptor sweep shipped without surfacing regressions. The `google.golang.org/api` v0.278.0 → v0.279.0 bump on PR #156 was upstream dep drift flagged by the `check-latest-deps` pre-push hook, not a sockerless bug.

- **988 + 990** (Phase 153 P153.13) — `gh repo list` + `gh issue list` rejected GraphQL enum names (`CREATED_AT`, `DESC`, `PUBLIC`, `OWNER`). Bleephub declared the args as `String`; gh sends them as enums. Fixed by adding `RepositoryPrivacy` / `RepositoryAffiliation` / `RepositoryOrderField` / `OrderDirection` / `IssueOrderField` / `IssueOrderDirection` enums + adding `repositoryOwner(login)` polymorphic query that gh's repo list uses.
- **989** (Phase 153 P153.13) — `gh issue view` failed because `issueOrPullRequest` returned just `Issue`, not a union with `PullRequest`; PR type missed `milestone`/`comments(last:)`; `PRCommentConnection` missed `nodes`; Issue.milestone resolver returned nil-typed empty map triggering Milestone.number NonNull; Issue.projectItems unimplemented (gh queries Projects v2 as a second round-trip). Fixed by declaring a real `IssueOrPullRequest` union, adding the missing PR fields, returning explicit nil for missing milestones, and adding empty-connection stubs for the Projects v2 surface. Per-bug detail in `git log` / linked PR. Recent ranges:

- **987** (Phase 92, PR #151) — `Backing: gcs-fuse` on cloudrun + gcf produced silently broken cross-task workspaces. Cache-TTL gcsfuse mount flags rejected by Cloud Run; deregister `GCSFuseDriver` and reject `BackingGCSFuse` in the translator with a concrete pointer at `gcs-sync`. Closes the documentation-fix-without-enforcement gap from BUG-944.
- **985–986** (Phase 84, PR #142) — sim shared `NewServer` + `MakeStore[T]` silently fell back to in-memory storage when persistence-open failed. Operator-requested persistence must fail loud. Fix: return error / `log.Fatalf`.
- **975–984** (PR #129) — Phase 135 sim host model + native arm64 CI runners.
- **973–974** (PR #128) — Sim test stability (`Eventually` polling).
- **949 + 972** (PR #123) — GCF `os/exec` workload → `sim.StartContainerSync`; cloudrun/gcf AR-proxy gate on `endpointURL`.
- **877–971** (PR #123) — FaaS pod overlays; 4 GCP runner cells GREEN; cloud-faithful GCP sim; storage-backing driver pilot.
- **845–876** (PR #122) — Phase 110: 4 AWS runner cells GREEN.
- **820–844 + 802** (PR #120) — Driver framework migration + cloud-native typed drivers.
- **786–819** (PR #118) — Round-8/9 AWS sweep; stateless invariant; real layer mirror.
- **770–785** (PR #117) — Round-7 AWS sweep.
- **661–769** (PRs #112–115) — Sim parity; stateless backends; FaaS invocation tracking; reverse-agent exec/cp/diff; Docker pod synthesis.
- **86–660** — Foundation work prior to live-cloud cell validation.
