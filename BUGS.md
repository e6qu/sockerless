# Known Bugs

Status [STATUS.md](STATUS.md) - roadmap [PLAN.md](PLAN.md) - resume [DO_NEXT.md](DO_NEXT.md).

**2969 filed - 2957 fixed - 9 open - 16 false positives.**

Simulator-side bugs (2909, 2932, 2887, 2646, 2712, 2764, 2928, 2924, 1345) moved with the simulators to [sockerless-cloud's BUGS.md](https://github.com/e6qu/sockerless-cloud/blob/main/BUGS.md), and the Bleephub bugs (2523, 2441) moved with Bleephub to [its repository](https://github.com/e6qu/bleephub), all keeping their IDs. BUG-2922 spans both this repository and sockerless-cloud; each carries its own scoped copy.

Every CI failure, live-cloud failure, simulator fidelity gap, or discovered fake/fallback lands here before any fix attempt. Detailed closed-bug history lives in PR descriptions and `git log`.

## Open

| ID | Sev | Area | Pattern | One-liner |
|----|-----|------|---------|-----------|
| 2957 | P3 | The AWS simulator's Lambda host resolves an ECR pull-through-cache reference to a local image name instead of pulling it from the simulator's own registry | In the Lambda cell of the Terraform integration harness the function's image `<account>.dkr.ecr.us-east-1.amazonaws.com/public-ecr-aws/docker/library/alpine:latest` — the reference the Lambda backend builds for `alpine` through the `public-ecr-aws` pull-through cache rule — failed with `image pull public-ecr-aws/docker/library/alpine:latest: pull access denied`: the simulator's `ResolveLocalImage` strips the registry host and expects the rest to be a Docker Hub name, which a cache-rule prefix is not | Real Lambda pulls the image from ECR, where the pull-through cache rule fetches it from its upstream; the simulator's ECR hydrates exactly that on a pull, but its Lambda host never pulls from it. Fix shape, in sockerless-cloud, the same as its Cloud Run hosts' fix: pull from the simulator's own ECR with the credential ECR's data plane accepts, and drop the local-name shortcut. The Lambda cell rejoins the `terraform-integration` CI matrix when the pin advances past that fix; `make tf-int-test-lambda` applies the ECS and Lambda environments and starts the backend today, and stops at the workload image. The Azure cells wait for the same pin: azurerm 4 reads a storage account's migration status (`accountMigrations/default`) on every storage account, which the pinned v0.9.2 simulator answers 404 and sockerless-cloud's main serves. |
 P2 | The Google Cloud simulator's Cloud Run and Cloud Functions hosts pull workload images with no credential, so no workload can start against its own enforcing Artifact Registry | At sockerless-cloud v0.30.3 the Artifact Registry data plane refuses an anonymous `/v2/` request, while `shared/container.go` `pullImage` and `cloudfunctions.go` call the Docker SDK's `ImagePull` with empty options; the CI run of this repository against that release showed every Cloud Run and Cloud Run Functions workload fail with `pull image: … denied: Unauthenticated request. Unauthenticated requests do not have permission "artifactregistry.repositories.downloadArtifacts" on resource "projects/sim-project/locations/us-central1/repositories/docker-hub"` after the harness's pushes and the Cloud Build push of the overlay had succeeded with the credential | Real Cloud Run pulls with the project's service agent identity; the simulator's hosts must pull the same way — a credential the simulator's own token service issues for that principal, passed as the SDK's `RegistryAuth`. The simulator's own push test pulls with the Docker CLI, which reads a login, so the gap is untested there. This repository's pin stays at v0.9.2 until a sockerless-cloud release carries the fix; the harness provisioning for BUG-2943 is otherwise proven against v0.30.3 by that run (repositories created, `docker login` accepted, the harness's pushes and the Cloud Build push of the overlay succeeded). sockerless-cloud has another agent's pull request open, so the fix waits for that repository's one-open-PR slot. |
| 2952 | P3 | Against the v0.30.3 Google-Azure simulator release the Azure Container Apps shared-volume writer exits 1 | `TestACASharedVolumeWorkspaceSharing` in CI against sockerless-cloud v0.30.3: the writer container reports `Container started` / `Execution failed` and exits 1, with no simulator-side error in the log; the same test passes against v0.9.2 and the backend's shared-volume code did not change between the two runs | The writer's command against the mounted Azure Files share fails inside the simulator's workload at v0.30.3, so the simulator's file-share mount behaviour changed between the releases. Reproducing needs a Linux host (the Azure Container Apps harness is Linux-CI-only here); to be diagnosed with the v0.30.3 simulator when the pin next advances (with BUG-2951). |
| 2950 | P3 | The Amazon Web Services simulator can lose a live run's per-VPC Docker network while another simulator process allocates one | One local run of the Amazon ECS integration suite against the v0.30.3 simulators failed `TestECSSharedVolumeWorkspaceSharing` with `container create: unable to find network with name or ID sockerless-sim-vpc-vpc-sim: network not found`, 90 seconds into a run whose earlier tasks had started on that network and whose log carries no `DeleteVpc`; the immediate rerun passed in full | In sockerless-cloud's `simulator-aws/shared/container.go`, `EnsureVPCNetwork` adopts a network by name, and `reclaimOrphanedSubnet` removes any simulator-labelled network that holds the slice a concurrent create wants, has no attached container, and carries a different run id — which a live run's network satisfies between two of its tests, so two simulator-aws processes on one host (this workstation with another session's suites, or CI running the SDK, CLI, and Terraform suites of one cloud at once) can take each other's VPC network. The reclaim's comment says a different run id means the owning process is gone; it does not. Fix shape, in sockerless-cloud: reclaim only from a run that is dead (record the owner's pid and host in the network's labels and check liveness, or never reclaim a slice a live run holds and take the next one), then advance the pin here. Not reproduced in isolation; tracked until the simulator release carries the fix. |
| 2945 | P3 | The Microsoft Azure simulator serves no repository catalog, so `docker images` cannot be exercised against it | `GET /v2/_catalog` answers 404 at the pinned v0.9.2 release and at v0.30.3; the simulator's shared OCI registry routes `/v2/<repo>/tags/list` and the manifest and blob endpoints, and Azure Container Registry's own `/acr/v1/_catalog`, but not the Docker Registry HTTP API v2 catalog that `core.OCIListImages` reads first | Real Azure Container Registry serves it, and the ACA and Azure Functions backends read it to answer `docker images`, so the whole listing path is unreachable end to end against the simulator. The Azure Container Registry round-trip test therefore covers the per-repository half — a `metadata_read`-scoped token against `/v2/<repo>/tags/list` — and reaches the catalog no further. The endpoint belongs to [sockerless-cloud](https://github.com/e6qu/sockerless-cloud); this entry stays open until it is served there and the pin here advances, at which point the round trip should read through `core.OCIListImages` instead. |
| 2925 | P3 | The user-interface CI job has failed twice for reasons that were budget, not defect | first a twelve-minute stall in `turbo run build` with eleven of twelve packages logged and nothing after, killed by the job timeout; then `ResourceHeaderActions.test.tsx > ECSTasksPage` timing out at vitest's 5s default while the same file reported 34s of transform and 67s of import on the runner. Neither reproduces locally, where the whole suite runs in about 15 seconds | Two budgets were wrong rather than two defects. The turbo invocations lost the background daemon a one-shot CI run never needs and the telemetry request a build step should not make, and each step gained its own timeout so a stall is reported as the step it stalled in — which is exactly how the second failure surfaced as a named test rather than an expired job. The five jsdom packages now set `testTimeout: 20_000`, because 5s is sized for a unit test and not for rendering a page built on a full component library while the module graph is still importing; the first test in the heaviest file absorbs that cost. What is still unproven is the original stall's cause, and that remains the reason this stays open. |
| 2922 | P2 | Two Docker Engine advisories have no released fix, and escaping them is an SDK migration | `govulncheck` reports GO-2026-5668 (a `docker cp` symlink race) and GO-2026-4887 (an authorization-plugin bypass) against `github.com/docker/docker`, reached here from the Docker backend (the three simulators and their shared libraries carry their own copy of this bug in sockerless-cloud). Both say `Fixed in: N/A`, and `v28.5.2+incompatible` is the newest published version of that module, so there is nothing to upgrade to | Upstream split the client out: `github.com/moby/moby/client` and `github.com/moby/moby/api` carry no Engine code and `govulncheck` reports them clean, verified against a probe module. Moving to them is not a path rewrite. `api/types` was reorganised into per-domain packages, so `types.Container` becomes `container.Summary`, `types.ContainerJSONBase` becomes a flattened `container.InspectResponse`, `types.Port` becomes `container.PortSummary`, `registry.AuthenticateOKBody` becomes `registry.AuthResponse`, `filters.Args` becomes `client.Filters` with a different constructor, and `HijackedResponse` moves into the client. The Docker backend's goverter-generated converters map those types directly and must be regenerated, and the passthrough backend is the one whose behaviour has to match real Docker exactly, so the migration needs its own pass across all seven modules with the Docker suites run against a real daemon. |
| 2923 | P3 | Nine deployable Terraform modules carry infrastructure hardening findings | Snyk's infrastructure scan reports 31 findings — 3 medium, 28 low — across `terraform/modules/{aca,azf,cloudrun,ecs,gcf,lambda}` | These are the modules that deploy sockerless to a real cloud, so unlike the simulator fixtures the findings are actionable rather than noise: storage buckets without logging, versioning or customer-managed keys, an Azure storage account not enforcing the latest TLS and without geo-replication, container registries allowing mutable tags, log groups unencrypted, a subnet without flow logs, instances receiving automatic public addresses, and a Lambda without tracing. Each changes the behaviour or cost of deployed infrastructure, so they are a decision for whoever owns those deployments rather than a mechanical fix. The fixtures they were buried under are silenced by the `.snyk` policy, which is what made these visible. |
| 1075 | P2 | live-cloud validation | unvalidated real cloud | Lambda is the only backend with a green live-cloud cell. Google Cloud Run, Azure Container Apps, Azure Functions, Lambda service-mesh, and Azure identity-backed cells need authenticated real-cloud validation before they can be marked green. |

## Recently Fixed

| ID | Sev | Area | Pattern | One-liner |
|----|-----|------|---------|-----------|
| ~~2969~~ | P2 | The Azure Functions backend formed a registry name from a login server by stripping `.azurecr.io` only, so any other registry domain produced an invalid image reference | `SOCKERLESS_AZF_REGISTRY` carries the registry's login server; a sovereign cloud's suffix, a data endpoint, or the simulator's advertised `<name>.azurecr.localhost:<port>` survived the strip and the overlay reference became `<login server>.azurecr.io/…`, which `docker build -t` refused | `azfACRName` takes the login server host's first label, which is the registry name under every domain a registry is advertised at (`TestAZFACRNameFromLoginServer`) |
| ~~2968~~ | P2 | The Azure Container Apps and Azure Functions environments provisioned no build-context container, and the harness passed the backends no registry, build-storage or registry-endpoint coordinate | An Azure overlay build uploads its context to a blob container Azure Container Registry Tasks builds from and pushes to the registry host the reference carries; the modules created no container and the harness exported neither `SOCKERLESS_AZURE_ACR_NAME`, `SOCKERLESS_AZURE_BUILD_STORAGE_ACCOUNT`/`SOCKERLESS_AZURE_BUILD_CONTAINER` nor `SOCKERLESS_AZURE_ACR_ENDPOINT`, so the Azure Functions cell failed at `ACR build service is required` and the Container Apps cell's push dialled the real `azurecr.io` host | Both modules create a private `build-context` container on their storage account and output its name; the harness exports the registry, build-context and registry-endpoint coordinates, the last naming the simulator's `/v2/` as the Google cells do |
| ~~2967~~ | P3 | The Terraform integration harness failed outright when the public mirror throttled its host image pull | Five cells pull `public.ecr.aws/docker/library/alpine` at once from one runner address and the Amazon ECR Public Gallery answered `toomanyrequests: Rate exceeded`, failing the Azure Functions cell before act ran | `pull_host_image` retries a refused pull with a growing wait and fails on the fifth refusal; the harness also dumps the host engine's simulator workload containers on failure, so a bootstrap's own output is in the log |
| ~~2966~~ | P2 | The Terraform integration harness ran Azure Container Apps containers as plain Jobs, whose image carries no bootstrap, so act's `docker cp` and `docker exec` found `no reverse-agent session` | The harness set the bootstrap path and the managed environment but not `SOCKERLESS_ACA_USE_APP=1`, the mode in which a container runs as a Container App with the reverse-agent bootstrap baked into its image; a Job runs the plain image and never dials back, so no exec can reach it (`TestACAContainerLifecycle` runs such a Job on purpose) | The harness sets `SOCKERLESS_ACA_USE_APP=1`, as the GitHub Actions runbook prescribes for a kept-alive job container exec'd per step |
| ~~2965~~ | P2 | The Cloud Run smoke stacks worked in a project with no Artifact Registry repositories | The `docker-hub` remote repository the backend pulls Docker Hub images through and the `sockerless-overlay` repository it pushes into had never been created in the smoke and GitLab smoke stacks; at v0.9.2 the simulator served any path, at v0.30.7 it answers `name unknown`, so `docker pull alpine` failed | Both stacks create the project and the two repositories through the Cloud Resource Manager and Artifact Registry APIs with a token from the simulator's OAuth 2.0 token endpoint, as the Terraform integration harness and the backend harnesses do |
| ~~2964~~ | P2 | An Amazon ECS container definition carried no `stopTimeout`, so a stop waited ECS's thirty-second grace where Docker waits ten | The backend registered task definitions without `stopTimeout`; the v0.30.7 simulator honours the definition's grace as ECS does, so every `docker stop` of a workload that ignores SIGTERM took thirty seconds and the cross-backend e2e suite overran its timeout | The definition carries Docker's `StopTimeout` — ten seconds when the container names none — within the 2 to 120 seconds ECS accepts (`ecsStopTimeout`), and the e2e suite's timeout allows the real grace |
| ~~2963~~ | P2 | The Terraform integration harness gave the Azure Container Apps and Azure Functions backends no credential and a plain-HTTP endpoint for a TLS simulator | The harness exported the `ARM_*` service principal Terraform uses but nothing the backends' `DefaultAzureCredential` reads, and named the simulator `http://` where the Azure simulator serves TLS, so every backend request failed with `failed to acquire a token` once the Azure cells joined the CI matrix | The harness exports the managed-identity coordinate the Azure platform injects into a container — `IDENTITY_ENDPOINT` at the simulator's `/msi/token` and `IDENTITY_HEADER` — as the backend integration harnesses do, and names the simulator with the scheme it serves |
| ~~2962~~ | P2 | An Amazon ECS `docker exec` whose working directory was missing ran the command from the image's directory instead of failing | The ExecuteCommand script joined `cd <dir> &&` to the rest with `;`, so a failed `cd` skipped only the environment exports and the command still ran — the Terraform integration harness's ECS cell passed act's step at the pinned simulator, which drops the task's `workingDirectory`, while Docker refuses such an exec with status 126 | `ssmExecScript` emits `cd <dir> \|\| exit 126;` before the exports and the command; `TestSSMExecScriptMissingWorkdirFails` runs the script against a real shell |
| ~~2961~~ | P2 | A pod-Service container on Cloud Run had no working directory until its default command ran, and a `docker exec` naming it failed | The Cloud Run backend only warms a pod Service's bootstrap for `docker exec` until stdin arrives, so nothing had created the container's configured `WorkingDir`; act's step exec with `WorkingDir=/src` failed at `chdir` (`fork/exec /bin/sh: no such file or directory`) in the Terraform integration harness's Cloud Run cell, where Docker creates a container's working directory at creation, before any exec | Every bootstrap (Cloud Run, Cloud Run Functions, Azure Functions, Lambda) creates `SOCKERLESS_USER_WORKDIR` at startup, after Lambda's bind links are materialised, so the directory exists whether or not the workload has started |
| ~~2960~~ | P1 | An Amazon ECS `docker exec` reported exit status 0 whatever the command returned | The ECS ExecuteCommand stream decoder ended the session on the exit-code frame or `channel_closed` without recording a status, and the exec adapter read the untouched store entry, so `docker exec` inspect showed 0 for a failed command and act marked a step whose `bash` did not exist as passed — the Terraform integration harness's ECS cell was green for that reason; the one-shot SSM path already printed an exit marker | A non-TTY exec's shell prints the one-shot path's `__SOCKEXIT:<status>:__` marker after its output and `ssmDecoder` strips it, holding back only the stdout bytes that could begin one, while a TTY exec takes the session's exit-code frame; the decoder records the status in the exec instance when the session ends and −1 when neither arrived. Covered by `TestSSMDecoder_NonTTYMarkerAcrossFrames`, `TestSSMDecoder_TTYExitCodeFrame` and the failing-command case in `TestECSContainerExec` |
| ~~2959~~ | P2 | The reverse-agent `PutArchive` path extracted into a destination directory that had to exist already, where Docker creates it | act copies its runtime files into `/var/run/act/` without creating the directory first; on Cloud Run, Cloud Run Functions, Azure Container Apps, Azure Functions and Lambda the exec `tar -xf - -C /var/run/act/` failed with `can't change directory`, and the Terraform integration harness's Google cells failed at the first `docker cp`, while Amazon ECS's SSM path already ran `mkdir -p` first | `core.RunContainerPutArchiveViaAgent` runs `mkdir -p "$1" && tar -xf - -C "$1"` with the destination as a positional shell parameter; `TestPutArchiveArgvCreatesMissingDestination` runs that command against a real shell |
| ~~2958~~ | P2 | The FaaS and Container Apps bootstraps started a workload in a working directory that did not exist, where Docker would have created it | act creates a job container with `WorkingDir` set to the checkout path, which the `alpine` image does not hold; on Cloud Run and Cloud Run Functions the bootstrap's `chdir` failed, the workload exited −1 at start, and every following exec answered 409 `not running` — the Terraform integration harness's Google cells showed it, while on Amazon ECS the engine had created the directory | Docker creates a container's configured working directory at start, so the bootstraps do the same: the exec envelope carries a `workload` mark on the container's own command, and `agent.EnsureWorkdir` creates the directory that command starts in, at every workload-start site of the Cloud Run, Cloud Run Functions, Azure Functions and Lambda bootstraps — the environment-baked one and the envelope the Cloud Run Functions backend sends (an exec's own `-w` is left to fail as Docker's does). Covered by a unit test of the helper and by the harness's Google cells. |
| ~~2955~~ | P3 | The Terraform integration harness (`make tf-int-test-*`) authenticated nothing against simulators that verify bearers, and had decayed past that | Its Google provider carried a made-up `access_token`, the harness pointed the provider's IAM client at the simulator but not the beta client `google_service_account` speaks through, applied into a project nobody created, never set the backend's build bucket, registry coordinate, Cloud Logging endpoint, bootstrap binary, callback address, or workload architecture, ran an `act` smoke from a workflow directory that no longer existed with a binary the image did not carry, and its Lambda environment read the live ECS environment's state from a real S3 bucket | The harness mints the provider's token from the simulator's OAuth 2.0 token endpoint (the JWT-bearer grant the real provider performs) and hands it over as `GOOGLE_OAUTH_ACCESS_TOKEN`, creates the project through the Cloud Resource Manager API, and exports every coordinate the backends require — the build bucket from the outputs, `SOCKERLESS_GCP_AR_ENDPOINT`, the Cloud Logging endpoint, the bootstrap paths, the callback address, the workload platform and architecture, and `AWS_ENDPOINT_URL` for the AWS CLI the ECS module's destroy sweep runs. The simulator environments route every provider client at the simulator (`iam_beta`, `resource_manager`, `logging`, `storage`, `cloudfunctions2`), address S3 path-style, and the Lambda environment reads the ECS environment's state from the simulator's own local state file (`ecs_state_local_path`, beside `read_ecs_state` and the S3 state's bucket, key and region as module variables), so the harness applies ECS first, hands the runner Lambda the subnets, security group and file system it shares with ECS, and destroys ECS last. The image carries `act`, the Docker CLI, the AWS CLI, `ip` and `nft`, and the four bootstrap binaries, with a workflow the smoke runs; the container runs privileged on the host network and pid namespace, since the simulators' Linux network fabrics (Compute Engine networks, the Amazon ECS `awsvpc` task namespace) program namespaces, bridges, nftables and sysctls the way they do on a host, and its dnsmasq answers on `127.0.0.2` beside the host's own resolver. The Azure environments name azurerm 4's `resource_provider_registrations` instead of the removed `skip_provider_registration`, and the smoke's image is pulled onto the host engine first, where the simulators' Docker Hub remote repositories serve it from. Amazon ECS runs end to end on this host (apply, backend, act smoke, destroy); AWS Lambda reaches the workload's reverse-agent dial-back and the Google environments the overlay build's registry pull, both of which need a Linux Docker engine sharing the host's network, and run in the new `terraform-integration` CI job, one cell per backend. |
| ~~2956~~ | P3 | The harnesses chose their ports one probe at a time, so a simulator listener could land on the port just chosen for the backend | `tests/main_test.go` probed a free port for the simulator, one for the backend, and one for the simulator's Route 53 DNS listener in turn, releasing each before the next; a CI run of the e2e suite had the DNS listener bind `[::]:37169` and the Amazon ECS backend then fail `listen tcp :37169: bind: address already in use`. The ECS, Lambda, Cloud Run, and Cloud Run Functions harnesses and `tests/sim_harness_test.go` probed the same way | `core.PortReservation` (and the tests module's own copy, since it does not depend on core) holds every listener until the whole set is chosen, so no two of a harness's own choices coincide; every harness that chooses several ports before starting a process reserves them together. Covered by a core test that the ports of one reservation are distinct, held until release, and free after. |
| ~~2954~~ | P4 | The `make tf-int-test-*` targets could not start the simulator at all | `make tf-int-build` ran `docker build` without `--load`, so on a buildx docker-container driver the image stayed in the build cache and `docker run sockerless-tf-int` reached for Docker Hub; the targets passed `--backend <name>` to an entrypoint that takes the backend positionally; and they mounted no Docker socket, so the simulator inside could not run workloads | The image is built with `--load`, the backend is passed positionally, and the container runs with the Docker socket mounted, `--security-opt label=disable`, and `--network host` — the same topology as the CI smoke container, whose steps gained `--network host` in the same change: the simulator serves its registry at its own loopback address and the host daemon that runs the workloads must share that loopback to pull from it. The simulator now starts and Terragrunt applies; the apply itself fails on credentials the harness never provisions (BUG-2955). |
| ~~2953~~ | P3 | The Azure backends listed repository tags with the `metadata_read` action, which the registry's `/v2/` tag listing refuses | `acaCloudState.ListImages` and `azfCloudState.ListImages` minted `repository:<repo>:metadata_read` tokens for `core.OCIListImages`, and the Azure Container Registry round trip did the same; the v0.30.3 simulator's data plane answers `GET /v2/<repo>/tags/list` with 401 `insufficient_scope` for it, because a v2 GET is a read of the repository the registry challenges for as `pull` — `metadata_read` belongs to Azure Container Registry's own `/acr/v1/` surface | Both backends and the round trip request `pull` for the v2 tag listing; the scope-rendering test keeps `metadata_read` as the `/acr/v1/` case. |
| ~~2946~~ | P3 | Google Artifact Registry's endpoint coordinate was split across two settings that agreed only by accident | `NewARAuthProvider` was wired with `config.EndpointURL` (the Google Cloud API endpoint) while `OverlayRegistryHost` read `SOCKERLESS_GCP_AR_ENDPOINT` from the environment, `CheckTagExists` answered a bool and reported every error as a cache miss, and `ResolveGCPImageURI` ignored the endpoint it was passed | One reading of `SOCKERLESS_GCP_AR_ENDPOINT` into `Config.ARRegistryEndpoint` in both Google backends now names the host image references carry (`gcpcommon.OverlayRegistryHost`) and the URL registry HTTP is dialed at (`gcpcommon.RegistryEndpointURL`, `https://` for a bare host) — for the auth provider (`IsCloudRegistry` recognises the relocated host through `IsRelocatedRegistry`, `RegistryEndpoint` returns the coordinate's URL), the tag probe, the multi-arch index writer, and the image resolver, which no longer take or read anything else. `CheckTagExists` returns `(bool, error)`: 200 present, 404 absent, everything else — a credential that cannot be minted or is refused, an unreachable repository, a transport failure — an error that fails the overlay start instead of costing a silent Cloud Build. Both backends fail at startup when a configured Cloud Build service cannot be constructed, rather than dropping the error and running without one. Covered by `gcp-common` unit tests for the coordinate, the auth provider, the resolver, and the probe's reference check. |
| ~~2944~~ | P3 | `BaseServer.ImagePull` fetched image metadata anonymously and discarded the credential it was handed | the shared `ImagePull`, `ImageManager.Pull` for a registry that is not one of the backend's cloud registries, and `ImagePushToEndpoint` all passed the client's `X-Registry-Auth` either nowhere or verbatim, and a `Basic` challenge from a registry was handled as a token-service realm | `core.RegistryAuthorizationFromDockerAuth` decodes the Engine API `AuthConfig` the client sends (username and password or the pre-joined `auth` become a Basic credential presented to the token service the way the Docker CLI presents it, a `registrytoken` is the registry's Bearer, an `identitytoken` is refused as unusable rather than downgraded to anonymous, a cloud provider's minted token passes through) and the three paths present it; a registry that challenges with `Basic` receives the credential directly. Covered by a core test that runs a TLS registry with a token service and a Basic-only repository and pulls a private image through `BaseServer.ImagePull` and `ImageManager.Pull` with the client's credential, after the anonymous pull of the same image fails. |
| ~~2943~~ | P2 | The Google Cloud integration harnesses pushed to Artifact Registry with no credential and never created the repository | `pushLocalImageToRegistryCloudrun` and `pushLocalImageToRegistryGCF` ran `docker tag` + `docker push` against the simulator coordinate with no `docker login` and no repository, which the v0.30.3 Artifact Registry data plane refuses exactly as the real service does | Both harnesses now provision the way Terraform and the operator do against the real service, through `gcp-common/registrytest`: the service-account key is minted first, an access token is minted from it by the JWT-bearer grant at the key's own `token_uri`, the `docker-hub` and `sockerless-overlay` repositories are created through `POST /v1/projects/{p}/locations/us-central1/repositories?repositoryId=…`, and the Docker CLI is logged in with `docker login -u oauth2accesstoken --password-stdin` — in a Docker configuration directory of the harness's own that the simulator process inherits, since its Cloud Build executor and Cloud Run host push and pull with the simulator's Docker CLI the way a Cloud Build worker and the Cloud Run service agent carry the project's registry credential. The backend's coordinate carries its scheme (`http://127.0.0.1:<port>`), and the smoke, GitLab, e2e, and `make stack-*` simulator stacks export the same coordinate. Proven against the enforcing v0.30.3 simulator in CI — repositories created, the login accepted, the harness's pushes and the Cloud Build push of the overlay all succeeded — before that release's own workload pull (BUG-2951) sent the pin back to v0.9.2. The Terraform modules for Cloud Run and Cloud Run Functions gained the `sockerless-overlay` repository the overlay path pushes to and the `gitlab-registry` remote repository the resolver names, neither of which they had created. |
| ~~2949~~ | P2 | The Azure Container Apps backend read Log Analytics over plain HTTP with no credential | `httpLogsClient` posted the Kusto query with no `Authorization` header whenever the endpoint coordinate was `http://`, beside the SDK client used otherwise, and `logs.go` and `app_log_tail.go` each chose between the two at query time | Two code paths for one query, one of them unauthenticated. `azurecommon.NewLogsQuerier` now picks one client at construction from the endpoint coordinate: the SDK client, or over plain HTTP (which the SDK's bearer policy refuses) a client that requests the same Microsoft Entra token for the Log Analytics scope and sends it itself. Both Azure backends use it; the unauthenticated client and the runtime branch are gone. |
| ~~2948~~ | P2 | A buffered attach on Google Cloud Run and Cloud Run Functions blocked forever when the invocation never published | the four per-backend copies of the attach stream had diverged: Azure Container Apps and Azure Functions bounded the read with a bootstrap-plus-run deadline, Cloud Run and Cloud Run Functions blocked on a bare channel receive | A stalled or lifetime-capped service stranded a `docker attach` / gitlab-runner reader for good. The stream is now `core.BufferedAttachStream`, one implementation with the deadline for all four backends; Cloud Run and Cloud Run Functions size it as bootstrap window plus run budget like the Azure backends. Covered by core's deadline, publish-before-deadline, close-releases-reader, and single-publish tests. |
| ~~2937~~ | P4 | Documentation links to Bleephub docs that left with the Bleephub extraction | `ARCHITECTURE.md` linked `docs/BLEEPHUB_GH_CLI.md` and `specs/README.md` linked `BLEEPHUB_GITHUB_API_PARITY.md`; neither file exists in this repository | Both now link the documents where they live, in the Bleephub repository, and the `docs/VIBE_CODING.md` mentions name that repository too. |
| ~~2947~~ | P2 | Six harness image builds left their result in the build cache instead of the container runtime's image store | `docker build` without `--load` against a buildx builder that is not the docker driver warns "Build result will only remain in the build cache" and exits 0, so the harness saw a successful build and the workload that was told to run the image could not pull it | The AWS Lambda suite failed five tests at their full 90-second bootstrap timeout, reporting a callback that never arrived, when what had actually happened is that `sockerless-lambda-agent-test:v1` was never in the image store to begin with — the symptom named the last thing to time out rather than the first thing to go wrong. One of the seven harness builds already passed `--load`; the other six now do too, across the Amazon ECS, AWS Lambda, Azure Container Apps, Google Cloud Run and Cloud Run Functions harnesses. |
| ~~2942~~ | P3 | Azure Container Registry recognition stopped at the public cloud, so a sovereign-cloud registry was treated as somebody else's | `ACRAuthProvider.IsCloudRegistry` matched only the `.azurecr.io` suffix | A registry in Azure China (`.azurecr.cn`) or Azure Government (`.azurecr.us`) is ours and needs our credential, and answering false for it means every read and write against it goes out anonymously. All three login-server suffixes are recognized, with a regression over each. |
| ~~2941~~ | P3 | The Amazon Web Services simulator harnesses bound the Amazon Route 53 resolver on a fixed port, so they could not start on a host running an mDNS responder | `backends/ecs` and `backends/lambda` launched the simulator without `SIM_DNS_PORT`, leaving it on its 5353 default, and the simulator panics with `bind: address already in use` before serving anything | Any macOS host — and any host running Chrome, whose helper holds UDP 5353 — failed the whole integration suite at startup for a reason that has nothing to do with the code under test. Both harnesses now reserve a port free on both TCP and UDP and hand it to the simulator, which is what the `tests` harness already did. |
| ~~2940~~ | P3 | A failure to mint a cloud registry credential was swallowed and the operation retried without one | `ImageManager.Pull` logged "cloud auth failed for pull, proceeding without" and continued; `ImageManager.Push` discarded the error with `if token, err := …; err == nil`; `ImageManager.Build` never asked for a credential at all; and `OCIListImages` skipped a repository whose tag listing failed | Each of these turns a credential problem into a different, wrong symptom: a registry that refuses the anonymous retry reports a missing image, and a listing reports the repositories it happened to be able to read as though they were everything the registry holds. All four now surface the failure. `SetOCIAuth` also leaves a request anonymous for an empty token rather than sending a bare `Authorization: Bearer`, which is a credential a registry rejects rather than the absence of one. |
| ~~2939~~ | P3 | The registry endpoint coordinate was honored on some registry paths and ignored on others | `core.OCIPush`, `core.OCIListImages`, `core.AssembleMultiArchManifest` and `gcpcommon.CheckTagExists` each built `https://<registry>` directly, so an operator who relocated the registry — a sovereign cloud, or a harness reaching it at another address — could tag and delete images there but could not push, list, write a multi-architecture index, or probe for an existing tag | The coordinate is now one thing: `core.OCIRegistryBaseURL` resolves it and `core.SetOCIHost` keeps the Host header naming the registry, so a request routed to a relocated address still reaches the registry the reference names — which is what a registry serving several login servers behind one address routes on, and what a registry's token service matches its `service` parameter against. The image-metadata path in `registry.go` honored the coordinate but sent the wrong Host, and now sends the registry's. |
| ~~2938~~ | P1 | Azure Container Registry was handed a raw Microsoft Entra token, which it refuses | `ACRAuthProvider.GetToken` asked Microsoft Entra for `https://<registry>/.default` — an audience Microsoft Entra does not issue, because Azure Container Registry has no per-registry audience — and put the result straight on `/v2/` as a Bearer; `ACRBuildService.AssembleMultiArchManifest` did the same with an Azure Resource Manager token, beside a comment claiming the token exchange was only for tools needing long-lived credentials | Azure documents the opposite: a token is acquired for the container registry *service* audience, `https://containerregistry.azure.net/.default`, exchanged for an Azure Container Registry refresh token at `POST /oauth2/exchange`, and traded with the requested scope for a scoped access token at `POST /oauth2/token` — and only that access token is a Bearer the data plane honours. Both hops now run, behind one token service that caches each token until the `exp` claim of the JWT the registry issued says it expires, re-authenticates when the registry answers 401, and asks for the access the operation needs in the Docker Registry HTTP API v2 scope grammar: `pull` to read a manifest, `pull,push` to write one, `pull,delete` to remove one, `metadata_read` to list a repository's tags, and `registry:catalog:*` for the registry itself. `core.AuthProvider.GetToken` carries the repository and actions to make that possible, which the AWS and Google Cloud providers document as unused because their credentials are registry-wide. Covered end to end against the Microsoft Azure simulator: a registry provisioned through Azure Resource Manager, an image pushed, tagged, listed and removed over `/v2/`, and an assertion that the credential presented is not the Microsoft Entra token it was exchanged for. |

## Compressed Fixed History

- **BUG-2391 through BUG-2937 (this repository's share):** the simulator fidelity, conformance, persistence, and console waves that then moved to sockerless-cloud; the Shauth relying-party and console credential work; registry authentication and the registry endpoint coordinate (2938–2942, kept above); CI budget, dependency-freshness, and harness hardening; and the simulator extraction itself. Per-bug detail lives in the PR descriptions and `git log` of the merged branches.
- **BUG-2380 through BUG-2390:** dependency freshness, CI required-check naming/budgets, Bleephub UI auth/repository inventory, pull request fork/head repository fidelity, GraphQL pull request/repository/tag metadata, and public GitHub path cleanups were fixed in the #779-#782 arc.
- **BUG-2341 through BUG-2379:** Bleephub object storage, Actions logs/artifacts/caches, workflow ref resolution, audit-log paging/search, issue/project GraphQL fidelity, OAuth/App Manifest flows, public runner/workflow endpoints, package registry data plane, CodeQL URLs, SQLite-only persistence, and several dependency/CI issues were fixed across the #781-era branches.
- **BUG-2214 through BUG-2339:** simulator conformance gates, AWS/GCP/Azure service ratchets, CI flake hardening, Bleephub API/UI parity waves, fuzz/load/concurrency hardening, and UI redesign work were fixed in earlier merged PRs. See PR descriptions and `git log` for per-service detail.

## False Positives

The repository had 16 historical false positives. Their details stayed in git history; keep the header count exact if any are reclassified.
