# Known Bugs

Status [STATUS.md](STATUS.md) - roadmap [PLAN.md](PLAN.md) - resume [DO_NEXT.md](DO_NEXT.md).

**1688 filed - 1614 fixed - 35 open - 6 false positives.**

Every CI failure, live-cloud failure, simulator fidelity gap, or discovered fake/fallback lands here before any fix attempt. Detailed closed-bug history lives in PR descriptions and `git log`.

## Open

| ID | Sev | Area | Pattern | One-liner |
|----|-----|------|---------|-----------|
| ~~1691~~ | P3 | bleephub UI — TS types drifting from the Go wire (10 findings) | type drift / over-loose UI types | `BleephubWorkflow` carried 4 never-emitted fields; `BleephubWorkflowJob` missed `outputs`/`completedAt` (+ undocumented zero-time sentinel); phantom `BleephubApp.clientId/permissions/events`; `BleephubAgent` missing 3 emitted fields and an always-redundant "Online" StatCard (only `online` is ever assigned); `GithubRelease.published_at` typed non-null → drafts rendered "1/1/1970"; issue/PR/comment `user` typed non-null but Go emits null; `WorkflowFileState`/`WorkflowStatus` enums wider than the server's; missing `WireOAuthApp.updated_at`; `suspendedAt` mistyped optional. All truth-ed against the Go emitters; "waiting" status added end-to-end. |
| ~~1690~~ | P2 | bleephub UI — spinner dead-ends, login loop, silent 50-item truncation | broken interactions | PR detail loaded by `.find()`ing a 50-item list → infinite spinner for missing/page-2 PRs (now fetches `GET /pulls/{number}` with 404/error states); issue detail + ReposPage + MetricsPage had no error states; login verified via `/api/v3/user` which accepts gho_/ghu_/ghs_ tokens the `/internal` dashboard rejects → silent logout loop (now verifies `/internal/status` with a clear PAT-required message); list pages pinned `per_page=50`, ignored the `Link` header, and tab badges lied under truncation (now Link-aware load-more + honest "N+" badges); createIssue failures were silent. 49 new vitest cases. |
| ~~1689~~ | P2 | bleephub Actions REST — workflow-run responses lacked the required `repository`/`head_repository` members | response-shape drift | `workflowRunJSON` never emitted either member (both required by the spec's `workflow-run` schema); `gh run view --json repository` and any consumer reading them got nothing. Found by the new OpenAPI shape ratchet the moment a shared-server test exercised the runs endpoints. Fixed: minimal-repository objects threaded into all three emit sites. |
| ~~1688~~ | P2 | bleephub REST — entity emitters drifted from the GitHub OpenAPI response schemas (723 violation keys) | response-shape drift (systematic) | First armed run of the new in-test response-shape validator (every 2xx `/api/v3` JSON response validated member-by-member against the vendored `github-openapi.json.gz`, ratcheted via `openapi-violation-allowlist.txt`): 644 missing-required members (hypermedia `*_url` templates, counters, `_links`, assignee/creator objects), 78 unknown-field (full user/org shapes where the spec nests `simple-user`/`organization-simple`), 1 type-mismatch (org-list items mis-keying). Fixed across 14 emitters — simple/full user + org + team splits, full `repository` shape with real counters (`CountOpenIssues`), `pull-request-simple` vs full split, milestone creator/closed_at, installation account snapshots, content `_links`, API-root members (incl. a typo'd invented key) — down to ZERO, no allowlist entries. |
| ~~1688~~ | P3 | bleephub UI — empty-repo page hardcodes `http://localhost:5555` in the `git remote add origin` hint | hardcoded default leaks into advertised URL | The REST API's `clone_url` is request-derived (`baseURL(r)`: TLS scheme + Host) and correct, but `RepoDetailPage.tsx`'s empty-repo blankslate printed `http://localhost:5555/{owner}/{repo}.git` regardless of how bleephub is served — wrong scheme + port for any TLS/non-default-port deployment (user-reported on the `:8443` quick-start setup). Fixed: the hint uses `window.location.origin` (the embedded SPA is always served from the same origin as the git smart-HTTP endpoint). Verified: rebuilt embed, real `git push https://localhost:8443/admin/<repo>.git` accepted and the commit reads back via the API. |
| ~~1687~~ | P3 | bleephub gh-CLI docs — quick start broken on macOS (gh rejects host:port; sudo-& trap; SSL_CERT_FILE ignored) | reference-adaptor drift (gh CLI) + doc bug | Reproduced the recipe on macOS with gh 2.92.0: (a) `gh auth login --hostname localhost:8443` now fails `error parsing hostname: invalid hostname` — the doc's "host:port works in --hostname" claim is stale; `GH_HOST=localhost:8443` + `GH_TOKEN` still works at runtime (the Docker harness already relies on env auth — its login line is `\|\| true`); (b) the quick start's `sudo … ./bleephub-server --addr :443 &` backgrounds before the password prompt, so nothing listens and the next step fails `connection refused` on 443 — the user-reported symptom; (c) gh on darwin ignores `SSL_CERT_FILE`/`SSL_CERT_DIR` (Go reads trust from the keychain only), so the keychain step is mandatory on macOS, not an alternative. Also: (d) for non-`github.com` hosts gh's env credential is `GH_ENTERPRISE_TOKEN` — `GH_TOKEN` is github.com-only and gh sends nothing with only it set (bleephub answers `401 Bad credentials`; the Docker harness never hit this because its in-container `gh auth login --hostname` on `:443` succeeds and writes hosts.yml). Fixed: quick start defaults to a no-sudo `:8443` + `GH_HOST`/`GH_ENTERPRISE_TOKEN` flow, `:443`/`gh auth login` documented as the alternative with the sudo-backgrounding warning, keychain marked REQUIRED on macOS in both docs. Verified end-to-end on macOS (keychain-trusted cert): repo create / issue create+list / release create all green via the doc's verbatim commands. |
| ~~1686~~ | P2 | aca backend — non-TLS Log Analytics shim built the query URL without the `/v1` endpoint prefix | wrong client path, masked by a sim quirk | `backends/aca/azure.go` `httpLogsClient` (the direct-HTTP path used when azquery rejects non-TLS endpoints, i.e. sim runs) built `{endpoint}/workspaces/{id}/query`; the real Log Analytics endpoint — and what the azquery SDK builds — is `{endpoint}/v1/workspaces/{id}/query`. The sim's un-prefixed quirk route had masked it; when BUG-1655 removed that route, the CI `smoke (aca)` `docker logs` step failed (the mux's `404 page not found` body parses as the JSON number 404 → `cannot unmarshal number into map[string]json.RawMessage` → empty logs). Fixed the shim to build the real `/v1` path; ACA smoke 14/14 locally. The AZF backend was already correct (real azquery client appends `v1` itself). |
| 1685 | P3 | azure sim redis — resource emits `sku` at top level; `RedisResource` nests it under `properties` | response-shape drift | Runtime spec-shape validator; allowlisted in `simulators/azure/spec-violation-allowlist.txt`, burn-down. |
| 1684 | P3 | azure sim eventgrid — PartnerTopic responses carry `readinessState`, not a `PartnerTopicProperties` (2022-06-15) member | response-shape drift | Runtime validator; allowlisted, burn-down. |
| 1683 | P3 | azure sim keyvault — certificate `policy` serialized with Go SDK field names (`issuerParameters`/`x509CertificateProperties`) instead of the wire members (`issuer`/`x509_props`) | wire-name drift | Runtime validator; allowlisted, burn-down. |
| 1682 | P3 | azure sim dns — SOA record set emits `minimumTtl`; `SoaRecord` spells it `minimumTTL` | wire-name drift (casing) | Runtime validator; allowlisted, burn-down. |
| 1681 | P3 | azure sim eventgrid — DomainTopic responses carry `tags`; DomainTopic is a proxy resource with no tags member | response-shape drift | Runtime validator; allowlisted, burn-down. |
| 1680 | P3 | azure sim cosmos — SQL container emits `properties.partitionKey`; the spec nests it as `properties.resource.partitionKey` | response-shape drift | Runtime validator; allowlisted, burn-down. |
| 1679 | P3 | azure sim postgres-flexible — creates respond synchronously with the resource body; the 2025-08-01 spec declares 202-only (no success body) | LRO-contract drift | The sync body validates against the `default` ErrorResponse. Runtime validator; allowlisted, burn-down (real fix is the 202+poll LRO choreography). |
| 1678 | P3 | azure sim web — `Site.properties.siteConfig` carries an invented `simCommand` member | sockerless wiring on the wire | Runtime validator; allowlisted, burn-down. |
| 1677 | P3 | azure sim containerapps — ManagedEnvironment carries an invented top-level `dockerNetworkName` member | sockerless wiring on the wire | PUT + GET. Runtime validator; allowlisted, burn-down. |
| 1676 | P3 | azure sim containerapps — JobExecution emits `status`/`startTime`/`endTime`/`template` at top level; the spec nests them under `properties` | response-shape drift | Runtime validator; allowlisted, burn-down. |
| 1675 | P3 | azure sim apim — Api read shapes echo `apiType`; the read member is `type` (`apiType` exists only on the create request shape) | response-shape drift | Runtime validator; allowlisted, burn-down. |
| 1674 | P3 | azure sim metadata — IMDS managed-identity token response carries `refresh_token`, not an `IdentityTokenResponse` member | response-shape drift | Runtime validator; allowlisted, burn-down. |
| 1673 | P3 | azure sim keyvault — key responses hoist `kid` to the top level; `KeyBundle` defines it only inside `$.key` (JsonWebKey) | response-shape drift | create/restore/update/delete. Runtime validator; allowlisted, burn-down. |
| 1672 | P2 | gcp sim cloudrun — knative surface served at `/v1/namespaces/...`; the real Cloud Run v1 (knative) API path is `/apis/serving.knative.dev/v1/namespaces/...` | path-shape divergence | The static gate's `v1/{+name}` leniency masks it; at runtime the responses key to the closest Discovery templates (serviceusage/sqladmin) where the knative envelope (`kind`/`apiVersion`/`metadata`/`spec`/`status`) reads as unknown. Fix needs coordinated sim + cloudrun-backend path change. Runtime validator; allowlisted, burn-down. |
| 1671 | P3 | gcp sim artifactregistry — `remoteRepositoryConfig` carries `enableIngestionAttestation`, not a Discovery schema member | response-shape drift | Runtime validator; allowlisted, burn-down. |
| 1670 | P3 | gcp sim cloudfunctions — `ServiceConfig` carries invented `simImage`/`simCommand` members | sockerless wiring on the wire | Runtime validator; allowlisted, burn-down. |
| 1669 | P3 | gcp sim dns — `ManagedZone` carries an invented `dockerNetworkName` member | sockerless wiring on the wire | create + get. Runtime validator; allowlisted, burn-down. |
| 1668 | P3 | gcp sim compute — `instanceGroups` list items embed `instances` | response-shape drift | Real `InstanceGroup` has no instances member (membership rides `listInstances`). Runtime validator; allowlisted, burn-down. |
| 1667 | P3 | gcp sim bigquery — `Job` leaks `query`/`result`; `GetQueryResults` leaks `totalBytesBilled` | response-shape drift | Runtime validator; allowlisted, burn-down. |
| 1666 | P3 | gcp sim cloudbuild — `BuildTrigger` carries an invented `location` member | response-shape drift | All six trigger CRUD ops (global + regional). Runtime validator; allowlisted, burn-down. |
| 1665 | P3 | aws sim cloudmap — ListServices `ServiceSummary` carries an invented `NamespaceId` member | response-shape drift | Found by the runtime spec-shape validator (allowlisted in `simulators/aws/spec-violation-allowlist.txt`; burn-down). Real `ServiceSummary` has no `NamespaceId`. |
| 1664 | P3 | aws sim kinesis — ListStreams summaries leak `StreamDescription` members | response-shape drift | `StreamSummaries[]` carries EncryptionType/EnhancedMonitoring/OpenShardCount/RetentionPeriodHours, none of which exist on the real `StreamSummary` shape (they belong to DescribeStream's `StreamDescription`). Runtime validator; allowlisted, burn-down. |
| 1663 | P3 | aws sim codebuild — `LogsLocation` carries an invented `enabled` member | response-shape drift | StartBuild/BatchGetBuilds emit `logs.enabled`; the real shape nests enablement under `cloudWatchLogs.status`. Runtime validator; allowlisted, burn-down. |
| 1662 | P3 | aws sim ssm — `Parameter` carries `Tier` (a `ParameterMetadata` member) | response-shape drift | GetParameter/GetParameters/GetParametersByPath emit `Tier` on `Parameter`; only DescribeParameters' `ParameterMetadata` has it. Runtime validator; allowlisted, burn-down. |
| 1661 | P3 | aws sim glue — `Job` embeds `Tags` | response-shape drift | GetJob/GetJobs emit `Job.Tags`; the real `Job` shape has no Tags member (tags ride GetTags). Runtime validator; allowlisted, burn-down. |
| 1660 | P3 | aws sim eventbridge — ListArchives/ListReplays summaries leak describe-only members | response-shape drift | `Archive` summaries carry ArchiveArn/Description/EventPattern and `Replay` summaries carry ReplayArn; real list shapes omit them (they're DescribeArchive/DescribeReplay members). Runtime validator; allowlisted, burn-down. |
| 1659 | P3 | aws sim ecr — `ImageIdentifier.imageId` + `DeletePullThroughCacheRule.upstreamRegistry` invented | response-shape drift | BatchDeleteImage's deleted `imageIds[]` nest an `imageId` wrapper the real `ImageIdentifier` (imageDigest/imageTag) lacks; DeletePullThroughCacheRule emits `upstreamRegistry`, not a member of the real response. Runtime validator; allowlisted, burn-down. |
| 1658 | P3 | aws sim ecs — `Task` carries an invented `networkConfiguration` member | response-shape drift | RunTask/StopTask/DescribeTasks echo the request's `networkConfiguration` into the `Task` shape; the real `Task` conveys networking via `attachments`. Runtime validator; allowlisted, burn-down. |
| ~~1657~~ | P4 | azure sim containerapps — stale LRO polling api-version literal | version-contract drift | `containerapps_apps.go` emitted `operationStatuses` polling URLs with `api-version=2024-03-01` while the pinned SDK (armappcontainers v3.1.0) speaks 2025-01-01 everywhere. Harmless at runtime (pollers echo the URL verbatim and the sim ignores api-version on reads) but a stale contract literal. Fixed: 2025-01-01. |
| ~~1656~~ | P3 | azure sim eventgrid — partner-topic surface pinned to an api-version that doesn't define it | version-contract drift (test-side) | The partnerTopics CLI test drove the whole surface with `api-version=2021-12-01`, but partner topics first appear in stable `2022-06-15` — the same `az rest` calls against real Azure would fail. The sim surface itself is real (matches the 2022-06-15 swagger, now vendored). Fixed test-side: partner-topic calls pin 2022-06-15. Found by the spec-surface conformance gate. |
| ~~1655~~ | P3 | azure sim monitor — un-prefixed `POST /workspaces/{id}/query` variant | invented path variant | The documented Log Analytics query endpoint is `https://api.loganalytics.io/v1/...` — the sim served the query handler at both `/v1/workspaces/{id}/query` (real) and `/workspaces/{id}/query` (a sim quirk). One consumer DID construct the un-prefixed form: the ACA backend's non-TLS shim client — see BUG-1686, fixed backend-side to the real `/v1` path. Removed the un-prefixed registration. Found by the spec-surface conformance gate; the hidden consumer found by CI `smoke (aca)`. |
| ~~1654~~ | P2 | azure sim monitor — `locations/{location}/deletedWorkspaces` invented; real RG-scope list missing | invented op + missing op | Real OperationalInsights soft-delete listing has exactly two ops (go-azure-sdk `deletedworkspaces`): subscription-scope and resource-group-scope `GET .../deletedWorkspaces` — there is no `locations/{location}` variant in any api-version. The sim served the invented locations variant and lacked the real RG-scope one. Fixed: removed the former, registered the latter. Found by the spec-surface conformance gate. |
| ~~1653~~ | P2 | azure sim network — LB `probes`/`loadBalancingRules` child PUT/DELETE invented | invented op (write on read-only sub-resource) | In real ARM, load-balancer child collections are read-only sub-resources mutated via the parent `loadBalancers` PUT — only `backendAddressPools` has standalone writes (`LoadBalancerBackendAddressPools_CreateOrUpdate/Delete`); both terraform and az CLI update rules/probes through the parent. The sim registered PUT/DELETE for all three child collections. Fixed: `registerLoadBalancerChild` takes a `writable` flag; probes + loadBalancingRules are GET-only, backendAddressPools keeps writes. Found by the spec-surface conformance gate. |
| ~~1652~~ | P2 | azure sim web — invented `GET /sites/{name}/config/backup` | invented op | Real WebApps backup-configuration read is `POST .../config/backup/list` (go-azure-sdk `GetBackupConfiguration`); `GET /config/backup` exists in no api-version (the path supports only PUT/DELETE). The sim registered the invented GET alongside the real POST. Removed. Found by the spec-surface conformance gate. |
| ~~1651~~ | P2 | azure sim web — `authsettingsV2/list` registered as POST; real op is GET | wrong wire method / unreachable op | go-azure-sdk `GetAuthSettingsV2` (the terraform path) sends `GET .../config/authsettingsV2/list` — the v1 `authsettings/list` is POST, but the V2 variant is GET in every api-version. The sim registered POST only, so the real provider call would 405. Fixed: POST→GET. Found by the spec-surface conformance gate. |
| ~~1650~~ | P2 | aws sim cloudfront — UpdatePublicKey registered at the wrong URI (missing `/config` suffix) | wrong wire path / unreachable op | The sim registered `PUT /2020-05-31/public-key/{id}` but the real CloudFront UpdatePublicKey operation is `PUT /2020-05-31/public-key/{Id}/config` (smithy `smithy.api#http` trait) — every real SDK/terraform UpdatePublicKey call 404'd against the sim while the lifecycle test (create/get/list/delete only) stayed green. Found by the new spec-surface conformance gate (`TestRESTRoutesExistInSmithyModels` vs the vendored cloudfront Smithy model). Fixed: route moved to `{id}/config`; UpdatePublicKey round-trip added to `TestCloudFrontPublicKeyLifecycle`. |
| ~~1649~~ | P2 | aws sim codebuild — invented operations TagResource / UntagResource / ListTagsForResource | invented op (not in the real API) | Real CodeBuild has no standalone tagging operations — tags ride `CreateProject`/`UpdateProject` and read back via `BatchGetProjects` (the vendored codebuild Smithy model's 59 operations include none of the three). The sim registered all three under `CodeBuild_20161006.*` with full handlers; no SDK method can even emit those targets, so nothing real could ever call them. Found by the new spec-surface conformance gate (`TestJSONTargetsExistInSmithyModels`). Fixed: registrations + handlers removed. |
| ~~1648~~ | P3 | ui — backend dev-server proxies target phantom ports `:9101`–`:9107` | broken build recipe / stale config | All 7 `ui/packages/backend-*/vite.config.ts` proxied `/internal` to per-package ports 9101–9107, which nothing in the repo serves — every backend binary serves `/internal` (and `/ui/`, and the Docker API) on its single `--addr`, default `:3375`, and that's how `make stack-*` runs them. The proxy targets were set when the dashboards were created and never matched the run model, so `bun run dev` hot-reload showed dead data panes. Fixed: all 7 proxies → `http://localhost:3375` (matches the dev flow `ui/README.md` documents). |
| ~~1647~~ | P3 | root Makefile — `smoke-test-act-*` targets reference nonexistent `smoke-tests/act/` and misdescribe the harness | broken build recipe / doc drift | The four `smoke-test-act*` targets built `-f smoke-tests/act/Dockerfile.*`, a directory removed when the act harness moved to `tests/upstream/act/`; the Dockerfiles at `smoke-tests/Dockerfile.*` are a docker-CLI round-trip smoke (not act-based), and the old `docker run` lines also lacked the host-socket mount the harness requires, so the targets were broken twice over while CI's `smoke` job ran the correct commands inline. Fixed: renamed to `smoke-test-{ecs,cloudrun,aca,all}`, repointed at `smoke-tests/Dockerfile.*`, run shape mirrors the CI `smoke` job (socket mount + `--security-opt label=disable`); updated stale references in README.md, FEATURE_MATRIX.md (act rows → `upstream-test-act-all`), docs/E2E_SMOKE_TESTS.md, and docs/runner-capability-matrix.md (which also pointed at long-gone `scripts/phase86/*.sh` + `phase86-aws-live.yml` — now `live-tests-*.yml`). |
| ~~1646~~ | P2 | bleephub GraphQL — newer `gh issue view` queries sub-issue fields the Issue type lacks | reference-adaptor drift (gh CLI) | The latest `gh` CLI's IssueByNumber query now selects `issueType{...}`, `parent{...}`, `subIssues(first:100){nodes,totalCount}`, `subIssuesSummary{total,completed,percentCompleted}` on `Issue` — bleephub's schema lacked them → "Cannot query field issueType on type Issue" → `gh issue view` non-zero, failing `sim (bleephub gh CLI)` (would fail on main too — upstream drift, surfaced on this PR). Fixed: added the four fields to the GraphQL Issue type returning null/empty (bleephub has no sub-issues/issue-types feature — correctly omitted, not faked: issueType→null, parent→null, subIssues→empty connection, subIssuesSummary→zeros). |
| ~~1645~~ | P3 | gcp/azure simulator UI — TS type drift vs the Go dashboard wire shape + loose enums | type drift / over-loose UI types | gcp `LogEntry.severity` was typed required `string` but the Go field is `json:"severity,omitempty"` set only when non-zero (logging.go) → the UI type lied for DEFAULT-severity rows; azure `MonitorLogRow` was `{[key:string]:unknown}` but the Go type is `map[string]string` (kql.go). Fixed: `severity?` optional + `LogSeverity` union, `MonitorLogRow` values → `string` (dropped redundant `as string` casts), and narrowed aws `ECSTask.status`/`LambdaFunction.state` + gcp `CloudFunction.state` to unions matching the values the server actually emits (ECS narrowed to the 5 real values, not the wider documented set — accuracy over breadth). typecheck+build green for all 3 sim UIs. |
| ~~1644~~ | P2 | CI — simulator MODULE unit tests (simulators/<cloud>/*_test.go) not run in CI | missing CI coverage / silent regression | The `sim (<cloud>)` jobs ran sdk-tests + cli-tests but NOT the in-module `go test ./...` (package main `*_test.go`), so AST/guard unit tests like `gcs_internal_test.go` never executed in CI — BUG-1643 shipped to main green. Fixed: added a `unit-test` Makefile target (`go test -tags noui ./...`, fast/Docker-free) to all 3 sim modules + a "Run module unit tests" step in the `sim (gcp/azure)` + `sim (aws sdk)` CI jobs. |
| ~~1643~~ | P2 | gcp sim — GCS object metadata PATCH wrote the store directly, bypassing the persistence-helper invariant | regression (from #538 Batch G4) / broke guard test | Batch G4's `objects.patch`/`ObjectHandle.Update` handler called `objects.Put` directly (gcs.go), tripping `TestGCSObjectStoreWritesUsePersistenceHelper` (every GCS object write must route through `persistGCSObject` so the host backing file stays consistent for the task runner). Missed because G4 verification ran only sdk-tests, not the sim-module unit tests (see BUG-1644). Fixed: added an explicit `persistGCSObjectMetadata` write path (store-only — a metadata patch bumps metageneration, not generation, and doesn't touch the payload) and taught the guard to allow it while keeping payload writes strict. |
| ~~1642~~ | P2 | azure sim — missing ARM ops, wrong data-plane error envelope, side-effecting GET, missing list pagination | missing op / error fidelity / pagination | ACR had no registries LIST (by-RG/by-sub) and no listCredentials (terraform `admin_username`/`admin_password` 404'd; also registered the lowercase `listcredentials` verb in `shared/middleware.go` per the AzurePathNormalization gotcha); Storage had no `blobServices/default` PUT (blob_properties versioning/changeFeed/cors couldn't be set); Tables data-plane returned the ARM `{error}` envelope instead of OData `{odata.error}` with TableNotFound/EntityNotFound (real `aztables` couldn't classify); EventGrid GET topic was side-effecting (Put-back of a request-Host-derived endpoint) — made pure; ServiceBus list topics/subscriptions/namespaces emitted bare `{value}` — wired `{value,nextLink}` $top pagination; ListBlobs ignored prefix/delimiter (no BlobPrefix hierarchy). Fixed + 7 SDK regression tests; corrected one existing test that asserted the old ARM table envelope. Deferred (small collections, no repro): EventHub/EventGrid/LogicApps/storage-ARM/RG list nextLink. | ServiceBus ARM queue/topic/subscription create seeded only maxSize+status (real Azure fills lockDuration `PT1M`/defaultMessageTimeToLive/maxDeliveryCount 10/autoDeleteOnIdle/etc. — the sim's OWN data-plane `sb*FromAdminDescription` modeled them but the ARM create path didn't); Storage account dropped accessTier/encryption/networkAcls/allowBlobPublicAccess/publicNetworkAccess/isHnsEnabled; ACR registry hardcoded publicNetworkAccess=Enabled+zoneRedundancy + dropped policies/encryption + empty sku.tier; VM osProfile.adminPassword + ACI secureValue echoed on GET (write-only); private DNS auto-SOA empty body; Cosmos no default consistencyPolicy (Session); Redis default redisVersion 7.0→6.0; EventHub messageRetentionInDays 1→7; EventGrid topic no publicNetworkAccess default + no inputSchema enum validation. Fixed: factored SB default sets into ARM create, widened structs (json.RawMessage for nested), strip write-only fields, inject real defaults. 10 SDK regression tests in `simulators/azure/sdk-tests/conformance_roundtrip_test.go`. FP: private-DNS record-set provisioningState (armprivatedns SDK has no such field — not emitted). |
| ~~1640~~ | P2 | gcp sim — missing in-place update / create-version / list operations (clients 404) | missing op / unimplemented surface | GCS bucket PATCH + object metadata PATCH (BucketHandle/ObjectHandle.Update 404'd); Spanner Instances.Patch (UpdateInstance fell to NOT_FOUND); KMS CreateCryptoKeyVersion + UpdateCryptoKeyVersion state PATCH + :restore (only :destroy existed); CloudFunctions v2 UpdateFunction PATCH + :generateUploadUrl; CloudBuild ListBuilds; Bigtable :modifyColumnFamilies + instance partialUpdate + cluster update; memorystore Redis PATCH updateMask + Cloud SQL settings merge (was wholesale replace, +settingsVersion). Fixed: real Google routes added per surface; 11 SDK regression tests. Note: KMS toggles version state via UpdateCryptoKeyVersion PATCH (no invented :enable/:disable verbs — real-API fidelity). |
| ~~1638~~ | P2 | gcp sim — missing 409/404/ABORTED error fidelity on create/delete/setIamPolicy | error fidelity | compute (insert dup → no 409 ALREADY_EXISTS; delete-missing → DONE op not 404) across network/subnet/firewall/address/router/instance + all 5 LB inserts; pubsub CreateTopic/CreateSubscription + eventarc CreateTrigger/Channel/ChannelConnection dup → no 409; logging sink/metric DELETE-missing → 200 not 404; IAM setIamPolicy ignored request etag (no 409 ABORTED on stale etag). Fixed via shared `computeConflict`/`computeNotFound`/`gcpIAMETagConflict` helpers + per-surface guards; getIamPolicy now persists its default policy so the etag is stable; SDK regression tests. Deferred (documented, not faked): compute operations are synthetic/stateless (no op store to 404 a bogus name); cloudbuild/dataflow create use server-assigned ids (name-collision 409 is a different contract). |
| ~~1639~~ | P3 | gcp sim — list ops ignore explicit page-size/continuation; firestore runQuery only did EQ | pagination + query fidelity | compute lists read `pageSize` not the canonical `maxResults` (existing test green-but-blind); compute-LB lists + DNS rrsets + eventarc + dataflow + logging entries:list + bigquery lists emitted no nextPageToken; firestore list/runQuery ignored pageSize/limit/offset/orderBy and treated every fieldFilter as equality. Fixed: `paginateListParam`/`paginateListCompute` (maxResults), per-surface pagination wiring, and a firestore runQuery rewrite honoring limit/offset/orderBy + the full operator set (LESS/GREATER/IN/ARRAY_CONTAINS/IS_NULL/composite AND-OR). Guardrail: paginate only on explicit positive page size (unset → full list + no token). 7 SDK regression tests. Deferred (small fixed collections): spanner/bigtable/memorystore/apigateway list tokens. |
| ~~1637~~ | P2 | gcp sim — narrow resource structs drop writable fields on get/list round-trip | round-trip drift (same class as BUG-1517) | DNS managedZone (labels/dnssecConfig/forwarding/peering/cloudLogging), BigQuery table (timePartitioning/clustering/rangePartitioning/view/expirationTime/requirePartitionFilter), Pub/Sub topic (schemaSettings), Eventarc trigger (channel), Artifact Registry repository (labels/cleanupPolicies/dockerConfig/kmsKeyName), Secret Manager Secret (ttl/expireTime/rotation/topics/annotations/versionAliases + UpdateSecret 400'd those masks), IAM Binding (condition), Logging metric (valueExtractor/bucketOptions/version) — all accepted on create but dropped on read-back → terraform-provider-google perpetual drift. Fixed: added the fields (json.RawMessage for nested) + UpdateSecret mask support; 8 SDK regression tests in `simulators/gcp/sdk-tests/conformance_roundtrip_test.go`. |
| ~~1636~~ | P3 | aws sim — list ops ignore the client's explicit page-size/continuation params | pagination fidelity | iam (ListPolicies/ListInstanceProfiles/ListAttachedRolePolicies, hardcoded `IsTruncated=false`), eventbridge (ListRules/ListEventBuses/ListTargetsByRule/ListArchives/ListReplays), batch (ListJobs/DescribeJobDefinitions/DescribeComputeEnvironments/DescribeJobQueues), autoscaling (DescribeAutoScalingGroups/DescribeScalingActivities), application-autoscaling (DescribeScalableTargets/DescribeScalingPolicies), efs (DescribeFileSystems/DescribeAccessPoints/DescribeMountTargets), cloudtrail (LookupEvents) all dumped the full list and ignored MaxResults/MaxItems/MaxRecords + Marker/NextToken → SDK paginators that assert page sizes misbehave on large fleets. Fixed: shared `awsPageExplicit` helper (paginates only when an explicit positive page size is supplied; unset → full list + no token, preserving prior behavior) wired through all 7 surfaces with protocol-correct Marker/IsTruncated (awsQuery) or NextToken/NextMarker; 9 regression tests. |
| ~~1635~~ | P3 | aws sim wafv2 — CreateWebACL/IPSet/RuleGroup/RegexSet accept duplicate name+scope | error fidelity | No collision check; real WAFv2 returns `WAFDuplicateItemException`. Fixed: `wafWriteDuplicate` + Name+Scope scan on the four create handlers; regression test. |
| ~~1634~~ | P3 | aws sim cloudmap — NamespaceNotFound/ServiceNotFound/InstanceNotFound return HTTP 404 | error fidelity / wrong status | servicediscovery is awsJson1.1 — modeled errors are HTTP 400 (9 sites used 404). Fixed: 404→400, `__type` codes unchanged (still deserialize to `*types.ServiceNotFound`); regression test. |
| ~~1633~~ | P2 | aws sim batch — errors untyped, SDK classifies them as `UnknownError` | error fidelity | `batchWriteError` wrote `{"message":...}` with no code; real Batch returns `ClientException` (400). Fixed: set `X-Amzn-Errortype` header + `__type` body to ClientException/ServerException (matches codebuild/amplify restJson1 pattern); regression test asserts `errors.As(&*types.ClientException)`. |
| ~~1632~~ | P2 | aws sim iam — CreateRole/CreatePolicy/CreateInstanceProfile accept duplicate names | error fidelity | No collision check → silent overwrite; real IAM returns `EntityAlreadyExists` (409). Fixed: name/ARN collision check on the three create handlers; regression test. |
| ~~1631~~ | P2 | aws sim ecs — DescribeServices with a non-existent cluster returns MISSING failures not `ClusterNotFoundException` | error fidelity | `handleECSDescribeServices` never checked cluster existence; real ECS short-circuits with `ClusterNotFoundException`. Fixed: cluster-existence check (preserves per-service `failures[]` when the cluster exists); regression test. |
| ~~1630~~ | P2 | aws sim elbv2 — Describe{LoadBalancers,TargetGroups,Listeners} with an explicit missing ARN/Name returns empty 200 | error fidelity / breaks waiters+data sources | `filterELBv2*` silently dropped misses; real ELBv2 throws `LoadBalancerNotFound`/`TargetGroupNotFound`/`ListenerNotFound` (400) which SDK waiters + `data.aws_lb` rely on. Fixed: raise the typed NotFound only when an explicit identifier is absent (no-filter list still returns possibly-empty list); regression test. |
| ~~1629~~ | P2 | aws sim eventbridge — PutTargets/ListTargetsByRule drop all structured target params | round-trip drift | `eventbridge.go` `EBTarget` carried only Id/Arn/RoleArn/Input/InputPath + a dead `Extra map[string]any json:"-"`, so PutTargets silently dropped EcsParameters/InputTransformer/RetryPolicy/DeadLetterConfig/Sqs/Http/Batch/RunCommand/Kinesis params and ListTargetsByRule never re-emitted them → `aws_cloudwatch_event_target` perpetual drift. Fixed: replaced `Extra` with explicit `json.RawMessage` fields (byte-exact round-trip); regression test. |
| ~~1628~~ | P2 | aws sim dynamodb — UpdateItem ignores `ReturnValues` (always returns full new item) | round-trip drift / wrong semantics | `dynamodb.go` `handleDDBUpdateItem` always returned `{"Attributes": item}`; real AWS gates on `ReturnValues` (default NONE → no Attributes; ALL_OLD/UPDATED_OLD/ALL_NEW/UPDATED_NEW differ). PutItem/DeleteItem already gated correctly; UpdateItem was the outlier. Fixed: snapshot oldItem, honor all five ReturnValues modes (+ ConditionExpression); regression test. |
| ~~1627~~ | P2 | aws sim lambda — GetFunction/CreateFunction emit `ImageConfig`, SDK reads `ImageConfigResponse` | round-trip drift | `lambda.go` `lambdaFunctionConfiguration` emitted image config under `ImageConfig`, but AWS `FunctionConfiguration` returns it under the `ImageConfigResponse` wrapper (`{ImageConfig,Error}`) — `ImageConfig` is only the create *input* key. So an Image-package function's EntryPoint/Command/WorkingDirectory round-tripped as nil → `aws_lambda_function.image_config` drift. Fixed: added `LambdaImageConfigResponse` wrapper, emit under `ImageConfigResponse`; regression test. |
| ~~1626~~ | P3 | aws sim kinesis — DescribeStream/Summary emit `EncryptionType:""` for unencrypted streams | round-trip drift / invalid enum | `kinesis.go` `kinesisStreamDescription`/`kinesisStreamSummary` echoed the raw stored EncryptionType (empty for never-encrypted streams); real Kinesis returns the `NONE` enum and omits `KeyId`. Empty-string is an invalid `types.EncryptionType`. Fixed: emit `NONE` + omit `KeyId` when unset; regression test in `conformance_roundtrip_test.go`. |
| ~~1625~~ | P3 | aws sim acm — RequestCertificate leaves `Options` nil (CT-logging default missing) | round-trip drift | `acm.go` `handleACMRequestCertificate` stored `req.Options` verbatim (nil when omitted); real ACM defaults `Options.CertificateTransparencyLoggingPreference=ENABLED` and returns it from DescribeCertificate → `aws_acm_certificate.options` drift. Fixed: default Options to CT-logging ENABLED when unset; regression test. |
| ~~1624~~ | P2 | aws sim apigatewayv2 — CreateApi/GetApi omit `apiEndpoint` | round-trip drift | `apigatewayv2.go` `APIGWv2Api` lacked `apiEndpoint`; real CreateApi returns the computed invoke URL and `aws_apigatewayv2_api.api_endpoint` is consumed downstream. Fixed: emit `https://<apiId>.execute-api.<region>.amazonaws.com`; regression test. |
| ~~1623~~ | P1 | aws sim apigateway — CreateRestApi/GetRestApi omit `rootResourceId` | round-trip drift / breaks dependent resources | `apigateway.go` `APIGWRestApi` never surfaced the auto-created root resource id; terraform-provider-aws sets `aws_api_gateway_rest_api.root_resource_id` straight from this field, so `aws_api_gateway_resource.parent_id = ...root_resource_id` resolved to empty (apply failure / drift). Fixed: emit `rootResourceId` from the auto-created root resource; regression test (incl. CreateResource using it as ParentId). |
| ~~1622~~ | P3 | aws sim batch — DescribeJobs omits `jobArn` | round-trip drift | `batch.go` `BatchJob` had no `JobArn`; SubmitJob/ListJobs emitted one but DescribeJobs dropped it (`JobDetail.JobArn` nil). Fixed: add+populate `jobArn`; regression test. |
| ~~1621~~ | P2 | aws sim autoscaling — DescribeAutoScalingGroups drops `AutoScalingGroupARN` + `HealthCheckType` | round-trip drift | `autoscaling.go` `autoScalingGroupXML` never emitted `<AutoScalingGroupARN>` or `<HealthCheckType>`; terraform maps these to `aws_autoscaling_group.arn`/`.health_check_type` → empty `arn` breaks references, `health_check_type` drifts. Fixed: store+emit ARN, HealthCheckType (default EC2), HealthCheckGracePeriod; regression test. |
| ~~1620~~ | P3 | bleephub UI AppsPage — empty-state strings cite removed `/api/v3/bleephub/*` paths | stale string / operator-misleading | After #535 moved app/oauth/installation management to `/internal/*`, AppsPage's three DataTable `emptyMessage` strings still told operators to `POST /api/v3/bleephub/apps`, `/oauth-apps`, `/apps/{app_id}/installations` — all 404 now. Fixed: point at the real `/internal/*` paths during the GitHub-style restyle. |
| ~~1619~~ | P3 | bleephub UI — CSS custom-property typos render no value (browser default) | wrong token name / silent visual bug | LoginPage/OverviewPage referenced `var(--color-bg-elevated)` and `var(--color-error)`, and AppsPage referenced `var(--color-status-warning)` ×3 — none of those tokens exist (the design system defines `--color-surface-raised`, `--color-status-error`, `--color-status-warn`), so the affected backgrounds/text silently fell back to the browser default. Fixed during the GitHub-style restyle (the new bleephub primitives use only real tokens). |
| ~~1618~~ | P3 | bleephub webhooks — `organization` block absent from issues/pull_request/push payloads | missing conditional field | When a repo is owned by an organization, real GitHub adds a top-level `organization` object to event payloads. bleephub's payload builders (buildIssuesPayload / buildPullRequestPayloadInner / buildPushPayloadWithInstallation) are free functions without store access, and `repo.Owner` is the human creator (Type User), so there's no in-`Repo` org signal. Correctly omitted (not faked). Fixed: attached centrally in `emitWebhookEvent` (every repo event funnels through it and it has store access) — org-owned repos now carry the `organization` block on all event payloads, user-owned repos never do; regression test covers both. |
| ~~1617~~ | P2 | bleephub — GitHub-API fidelity sweep (invented paths, response shapes, OIDC, webhooks, Pages, Apps) + definition-validation test | invented paths/values / fidelity gap | Fixed: moved all 14 invented `/api/v3/bleephub/*` paths to `/internal/` (sim-control) + a route registry so fidelity is checked against the real route table not the catch-all; relocated 4 more non-GitHub paths off `/api/v3` (`/user/orgs` POST → `/internal/orgs`, REST review-threads → `/internal/`) and fixed the unscoped `actions/oidc/customization/sub` to the real repo+org-scoped paths; OIDC mint now derives every claim from the real run + actor and fails loudly on missing context (no `admin/unknown`/`1`/`bleephub-bot` placeholders); Apps (real owner, installations_count, no invented `*_url` fields, installation-token `repository_selection`/`repositories`, OAuth flows mint `gho_`/`ghu_`, scope-token mints fresh, token-inspection `token_last_eight`/`hashed_token`/`installation`); webhooks (config `content_type`/`insecure_ssl` round-trip for repo+app hooks, delivery summary `null` ids + no `url`, valid installation node_id, no top-level `app_id`, non-null `sender`, `last_response` reflects delivery, repo-hook Target-ID header); Pages (full site object incl. https_certificate/custom_404/build_type, 204 on update, `{status,url}` build response, no invented build `id`, `pusher`/`commit`/`error` present, 422 on missing branch); Actions (run `workflow_id`/`workflow_url`/`path` reference the file not the run, job `steps` synthesized + `completed_at`, public REST cache endpoints `/actions/caches` + `/actions/cache/usage`, repository_dispatch `branch`, runner extra fields). New hermetic test `TestRegisteredAPIv3RoutesExistInGitHubSpec` validates every registered `/api/v3` route against the vendored official GitHub OpenAPI (testdata/github-openapi.json.gz; refresh via scripts/update-github-openapi.sh), with a documented allow-list for real GHES-only endpoints + mux-necessitated wildcard dispatchers. Remaining: BUG-1618. |
| ~~1615~~ | P3 | bleephub releases — list insertion-ordered (oldest-first), scrambled after reload | ordering fidelity gap | `gh_releases.go:143-149` returns the `byRepo` slice as-is (real GitHub lists newest-first), and the persistence loader (store.go:659-670) rebuilds `byRepo` in map-iteration order so even insertion order is lost across restarts. `Latest` is CreatedAt-based and unaffected. Fix: sort newest-first in the list handler + deterministic rebuild on load. |
| ~~1614~~ | P3 | bleephub — struct-marshaled timestamps emit local offset, not GitHub's Z UTC | wire-format drift | Raw `time.Time` struct fields (User/Repo in store.go:45-46, store_repos.go:32-34, …) marshal in server-local zone — observed live `"created_at":"2026-06-10T11:55:03+03:00"`; real GitHub always returns `Z`. Handlers that format explicitly already use UTC. Strict string-comparing clients diverge. Fix: store UTC at creation (`time.Now().UTC()`) so default RFC3339 marshal emits Z. |
| ~~1613~~ | P3 | bleephub persistence — counters table + GetCounter/SetCounter dead code | dead-code silencer | `persistence.go:204-226` and the `counters` DDL in both dialects (persistence.go:36-39, 56-59) have zero callers. Either remove or use — it's the natural fix vehicle for BUG-1608. |
| ~~1612~~ | P3 | bleephub MiscStore persistence half-covered — keys/follows/pages site/branch protection lost on restart | partial persistence | gpg_keys/pages_builds/audit_log/marketplace persist, but `userKeys`, `follows`, `pagesByRepo` (the Pages site), and `branchProtection` don't (gh_misc_endpoints.go:175-194). After restart `GET /pages` 404s while `GET /pages/builds` still returns builds for the vanished site; SSH keys and branch protections disappear. Fix: persist the four remaining MiscStore maps. |
| ~~1611~~ | P3 | bleephub — issue lock + comment minimize mutations skip persistence write-through | partial persistence | `SetIssueOrPRLock` (store_issues.go:465-491) and `SetCommentMinimization` (store_issues.go:532-548) mutate issues/PRs/comments that are otherwise persisted, with no `MustPut` — locked/minimized state silently reverts on restart while the entity survives. Fix: write through in both mutators. |
| ~~1610~~ | P2 | bleephub Actions cache — no repo/branch scoping (cross-repo cache poisoning) | missing isolation / fidelity gap | `cacheIndex` in artifacts.go is keyed only by `key+version` globally; real GitHub scopes cache entries to repo + branch hierarchy. Any workflow on any repo can read or pre-poison another repo's cache via a matching key. Fix: fold the run's repo full-name (resolved at reserve time) into the cache scope key. |
| ~~1609~~ | P2 | bleephub s3FS — ReadDir unpaginated, OpenFile truncates existing objects, deleteRepoPrefix swallows errors | S3 fidelity / silent swallow | s3fs.go:210-217 issues a single ListObjectsV2 (continuation ignored) so >1000-key prefixes lose refs from listings; s3fs.go:101-119 `flag&os.O_RDONLY == 0` is always true (O_RDONLY==0) so any O_CREATE\|O_WRONLY open — even O_APPEND without O_TRUNC — starts from an empty buffer and overwrites the object on Close; s3fs.go:308-310 deleteRepoPrefix returns silently on List error. Fix: continuation loop; read existing object unless O_TRUNC; honor O_APPEND; propagate delete errors. |
| ~~1608~~ | P2 | bleephub — NextRunID restarts at 1 while artifacts persist with old GitHubRunID (epoch collision) | ID collision across restart | store.go:282 `NextRunID` is never persisted; `recoverArtifactsFromDisk` restores `githubRunId` from meta.json; match at gh_actions_extras.go:309 is `art.GitHubRunID == wf.RunID`. With `BLEEPHUB_DATA_DIR` set, restart + new run #1 → `GET /actions/runs/1/artifacts` lists the previous epoch's artifacts; `gh run download` fetches stale data. Fix: persist/restore NextRunID via the counters table (BUG-1613). |
| ~~1607~~ | P2 | bleephub Actions REST — run list endpoints paginate unsorted map iteration | unstable pagination / ordering fidelity | handleListWorkflowRuns (gh_actions_rest.go:326-345), handleListWorkflowFileRuns (gh_workflows_rest.go:129-150), workflow-file list (gh_workflows_rest.go:61-74) iterate a map then paginate — verified live: consecutive `GET …/actions/runs` returned `[2,3,4,5,6,1]` then `[1,2,3,4,5,6]`. Real GitHub returns newest-first; paginated clients get duplicates/missing rows across pages. Fix: sort by RunID desc before `paginateAndLink`. |
| ~~1606~~ | P1 | bleephub Actions cache upload ignores Content-Range — concurrent chunks corrupt the archive | data corruption / real-client protocol gap | handleCacheUpload (artifacts.go) appends chunks in arrival order; verified live: PATCH `bytes 5-9/*` then `bytes 0-4/*` → download returns `WORLDHELLO`. The @actions/cache toolkit uploads >32MB caches in 4 concurrent ranged PATCHes, so any real multi-chunk cache restores as a corrupt tarball. Fix: parse Content-Range, write at offset, validate total on finalize. |
| ~~1605~~ | P1 | bleephub persistence reload — Repo.Owner and per-repo issue/milestone counters zeroed | data loss on reload / panic + auth regression | store_repos.go:23,30,31 mark `Owner`, `NextIssueNumber`, `NextMilestoneNumber` as `json:"-"`; the loader's owner relink (store.go:429-433) is dead code. After restart: canRead/canPush/canAdmin (rbac.go) fail for the owner's own repo → git push 403s, private reads 401; repo JSON emits `"owner":null`; gh_repos_refs.go:123 nil-derefs `repo.Owner.ID` on DELETE ref → panic; first post-restart issue gets Number 0, second collides with persisted #1 (`GetIssueByNumber` nondeterministic; PRs share the counter, milestones identical). Fix: relink Owner via FullName owner segment → UsersByLogin/orgs in the loader; persist the number counters (or recompute max+1 per repo on load). |
| ~~1604~~ | P1 | bleephub s3FS — s3File.Read drains bytes.Buffer while slicing dst by pos (hang/truncation) | broken IO primitive / unusable S3 git storage | s3fs.go:345-356 `Read` does `sf.buf.Read(p[sf.pos:])` — bytes.Buffer drains as it reads AND the destination is sliced by file position: a 1300-byte file read in 512-byte chunks returns 512 then `(0,nil)` forever (io.ReadAll spins; bufio ErrNoProgress; other chunkings truncate at ~half because the EOF guard compares pos to the shrinking buf.Len()). `Seek` is incoherent against a draining buffer. go-git reads packed-refs/config/packfiles through this path → S3-backed git storage corrupts or hangs on any non-trivial repo. Zero tests reference s3FS. Fix: immutable `[]byte` + `copy(p, data[pos:])` reader semantics. |
| ~~1603~~ | P3 | bleephub UI — logged-in router lacks /ui/login + catch-all routes | dead route / blank page | `App.tsx:92-106` logged-in `<Routes>` defines only the eleven app routes; a logged-in user opening a `/ui/login` bookmark or any mistyped `/ui/…` URL matches nothing and renders an empty content area inside the shell (logged-out branch has its catch-all at App.tsx:74). Fix: add `/ui/login` → `Navigate to /ui/` plus a `/ui/*` catch-all. |
| ~~1602~~ | P3 | bleephub UI — fetch failures swallowed into infinite spinners; 401 never clears token | silent error swallow / stuck UI | OverviewPage:40, ReposPage:64, RunnersPage:104, WorkflowsPage:98/188, AppsPage:115/182/299, IssuesPage:212, PullsPage:121 gate on `if (isLoading \|\| !data) return <Spinner/>` with no `isError` branch — failed queries (404 dead link, 401 stale token) spin forever. Nothing calls `clearToken()` on 401, so after a server restart invalidates tokens the pages spin while `isLoggedIn()` stays true (manual Sign out is the only escape). Fix: `isError` branches + clear-token-and-redirect on 401 in the shared fetch helpers. |
| ~~1601~~ | P3 | bleephub UI — mergePR treats HTTP 405 (not mergeable) as success | swallowed error / false-positive success | `api.ts:245` throws only `if (!res.ok && res.status !== 405)`; backend returns `405 "Pull Request is not mergeable"` for already-merged/closed PRs (gh_pulls_rest.go:260-262), so the mutation resolves, invalidates, and navigates back as if merged — silent failure on stale list data (5s poll window / second tab). Fix: throw on 405 with the server's message. |
| ~~1600~~ | P2 | bleephub UI — api.ts uses four types it never imports (TS2304 ×4) | typecheck error hidden by broken lint | `api.ts:249-258` references `GithubWebhook`/`GithubSecret`/`GithubEnvironment`/`GithubRelease` without importing them from types.ts; `bun run build` passes because Vite/esbuild strips types unchecked, and the `make lint` tsc run is broken by BUG-1599 so the errors never surfaced. Fix: add the four names to the type import. |
| ~~1599~~ | P2 | ui/packages — `tsc --noEmit` lint broken across all 13 packages referencing core (TS6306) | broken quality gate | Every UI package with `"references": [{"path": "../core"}]` fails `bunx tsc --noEmit` (the shared make/ui-app.mk lint target) with `TS6306: Referenced project 'packages/core' must have setting "composite": true` — the typecheck checks NOTHING repo-wide, which is how BUG-1600 landed on main. Fix: add `"composite": true` to ui/packages/core/tsconfig.json (or drop the references blocks). |
| ~~1598~~ | P2 | bleephub /internal/installations omits suspendedAt — UI suspend state stuck, unsuspend unreachable | response shape gap / one-way action | The store tracks `SuspendedAt` (gh_apps_store.go:50) and the GH-shaped endpoint emits `suspended_at`, but `installationViewMgmt` (handle_mgmt.go:34-42) never includes it — verified live. The UI Status column always shows "active" and the button always reads "suspend"; clicking it on a suspended installation re-POSTs suspend, gets a 409 that `suspendInstallation` deliberately swallows — zero feedback, unsuspend can never be issued from the UI. Fix: add `suspendedAt` to installationViewMgmt (UI type already has the field). |
| ~~1597~~ | P2 | bleephub UI — OAuth Apps/Apps dialogs read camelCase, server sends snake_case (client_id unrecoverable) | wire-shape mismatch masked by camelCase test mocks | `GET/POST /api/v3/bleephub/oauth-apps` returns `client_id`/`callback_url`/`created_at` but `BleephubOAuthApp` (types.ts:143-151) + AppsPage.tsx:302/322/330/571 read `clientId`/`callbackUrl`/`createdAt` → blank Client ID column, "Invalid Date" in Created, and the post-create "shown once" credentials dialog renders an EMPTY Client ID — the user cannot obtain it from the UI at all. Same mismatch in the GitHub-App create dialog (AppsPage.tsx:395 reads `resp.clientId`, server sends `client_id`; section silently omitted via the guard at :524). Vitest mocks use camelCase, masking it. Fix: map the snake_case wire fields in api.ts (or switch the types to snake_case). |
| ~~1596~~ | P1 | bleephub UI — Secrets/Environments tabs crash the whole app (envelope vs array) | response shape mismatch / app-level crash | api.ts:251-255 types `GET …/actions/secrets` and `…/environments` as arrays but the server returns GitHub envelope objects (`{"secrets":[],"total_count":0}` — secrets.go:46-50, gh_deployments.go:504-507). The envelope replaces the `[]` default so `secrets.map` at RepoDetailPage.tsx:445/471 throws `TypeError: .map is not a function` even with zero secrets; the crash propagates to the app-level ErrorBoundary (App.tsx:82) and blanks the entire UI for any user clicking either tab on any repo. Tab label renders "Secrets (undefined)". Fix: unwrap `.secrets`/`.environments` in fetchSecrets/fetchEnvironments + fix the TS types. |
| ~~1595~~ | P2 | bleephub persistence — workflow_files bucket written but silently dropped on load | persisted-but-discarded state / silent shim | `store_workflow_files.go:84` writes every workflow file through `MustPut("workflow_files", …)`, but the loader entry in store.go's restore table is a literal no-op (`{"workflow_files", func(_ string, raw []byte) error { return nil }}`) with no explanation. After a restart with `BLEEPHUB_PERSIST=true`, workflow files registered via `/api/v3/bleephub/workflow` submit (no git backing) are gone — `gh workflow list` is empty and dispatch 404s — while their rows still sit in the DB. Git-discovered files only reappear lazily if git storage is durable (`BLEEPHUB_GIT_DIR`/S3). Worse: `RepoFullName`/`YAML`/`Source` are `json:"-"` (store_workflow_files.go:32-36), so the persisted rows are unusable even by a real loader, and nothing ever `MustDelete`s the bucket. Fix shape: persist the full struct + real loader (incl. next-ID re-derivation). |
| ~~1594~~ | P2 | bleephub docs — README + BLEEPHUB_GH_CLI stale after admin-token + durable-storage changes | stale docs / broken quick start | Fixed: the admin-token, binary-name (`bleephub-server`), login-form, durable-storage (`BLEEPHUB_GIT_DIR`/S3/`BLEEPHUB_DATABASE_URL`), and persisted-bucket items were corrected in #534/#535; this PR finished the residue — the README UI section now describes the GitHub-style top-nav + light/dark toggle (was "left nav"), and the brittle hardcoded gh-harness assertion counts (50/50, "50 distinct assertions") were replaced with non-drifting "runs in CI as `sim (bleephub gh CLI)`" phrasing. `docs/BLEEPHUB_GH_CLI.md` already noted enable/disable as unimplemented and the token as the `BLEEPHUB_ADMIN_TOKEN` value (not a static PAT). |
| ~~1593~~ | P2 | bleephub /internal admin surface — server-side auth missing | decorative login / unauthenticated admin state | All `GET /internal/*` management endpoints (workflows, sessions, repos, apps, installations, oauth/state, storage, metrics, status) are served without any token check. The UI gained a token login (LoginPage verifies via `/api/v3/user`, stores in localStorage) but it is a client-side guard only — anyone who can reach the port reads admin state, so the login is decorative. Fix shape: require the admin token (or any valid PAT) on `/internal/*` server-side; UI already sends `authHeaders()` on some calls — extend `fetchJSON` to attach them everywhere. |
| ~~1592~~ | P2 | bleephub UI — dispatchWorkflow omits Authorization header | missing auth header / works only via server gap | `ui/packages/bleephub/src/api.ts` `dispatchWorkflow()` POSTs `/api/v3/repos/{repo}/actions/workflows/{id}/dispatches` with only `Content-Type` — no `authHeaders()` — unlike every other mutating helper in the file. It currently succeeds only because of BUG-1591's missing server-side check; once 1591 is fixed the UI dispatch form breaks with 401. Fix: spread `authHeaders()` into the request. |
| ~~1591~~ | P1 | bleephub Actions REST — workflow-run write endpoints unauthenticated | missing requirePerm / unauthenticated execution | `POST .../actions/workflows/{id}/dispatches` (gh_workflows_rest.go), `POST .../actions/runs/{id}/cancel`, `POST .../actions/runs/{id}/rerun`, `DELETE .../actions/runs/{id}`, `DELETE .../actions/runners/{id}` (gh_actions_rest.go) are registered without `requirePerm` — verified live: unauthenticated `workflow_dispatch` submits and executes a workflow. Sibling endpoints in gh_actions_extras.go correctly gate with `requirePerm("actions", permWrite, …)` (rerun-failed-jobs, delete-artifact) and `requirePerm("contents", permWrite, …)` (repository_dispatch). Real GitHub requires `actions:write` on all five. Fix: wrap the five registrations with `requirePerm("actions", permWrite, …)`. |
| ~~1590~~ | P2 | bleephub Actions REST — environment pending approvals | successful empty response / unmodeled GitHub state | `GET /api/v3/repos/{owner}/{repo}/actions/runs/{run_id}/approvals` returns `200 []` regardless of run environment state. Real GitHub reports pending deployment/environment approvals. Fix shape: model environments, deployment protection/pending approval records, and return only the run's real pending approvals; Fixed: environments now parse `reviewers`/`wait_timer` from the PUT body and emit `protection_rules`; jobs with `environment:` targeting a reviewer-protected environment hold in `waiting` (run status `waiting`); `GET/POST .../pending_deployments` approve/reject (approval releases jobs + creates real deployments; rejection fails the run); `GET .../approvals` returns the recorded reviews. Unit + full-REST-flow regression tests; response shapes validated by the OpenAPI ratchet. |
| ~~1589~~ | P1 | aws simulator — Batch, CodeBuild, Glue, and Step Functions reported terminal success without executing work | synthetic execution / false-positive success | Fixed: AWS Batch `SubmitJob` now runs the registered container image through the shared workload runner and updates job state from the container exit code; CodeBuild `StartBuild` runs inline buildspec commands with a real shell process; Glue `StartJobRun` executes Python shell scripts loaded from the simulator's S3 store; Step Functions `StartExecution` runs a small ASL interpreter for Pass/Succeed/Fail/Wait and `StopExecution` aborts real running Wait executions. SDK + CLI coverage was updated to wait on service state instead of assuming immediate success. |
| ~~1585~~ | P2 | gcp terraform — Bigtable Admin provider coverage missing from apply stack | provider coverage gap | Fixed: the GCP Terraform apply stack now declares `google_bigtable_instance` and `google_bigtable_table` resources, the simulator exposes Bigtable Admin on the official gRPC emulator path used by the Google provider, and the apply/destroy harness asserts the provider-returned resource IDs. |
| 1584 | P3 | azure terraform — azurestack provider warning on custom cloud metadata | provider warning / stale deprecation diagnostic | `terraform validate` still emits the AzureStack provider's "`arm_endpoint` is deprecated in favour of `metadata_host`" warning even though the test config now uses only `metadata_host`. Track until the provider warning path is fixed upstream or the stack can move off the AzureStack compatibility provider without losing coverage. |
| ~~1588~~ | P1 | gcp terraform — Spanner database operations omit provider-required resource fields | sim fidelity gap / provider LRO contract | Fixed: Spanner create-database and `UpdateDatabaseDdl` long-running operations now include `metadata.resource` set to the database resource name, create-database metadata includes the official `database` field, and DDL operations return the provider-required `response.resource`. SDK coverage unmarshals the raw operation metadata/response and asserts the provider-required fields. |
| ~~1587~~ | P1 | azure terraform — ACI container group read panics AzureRM provider | sim fidelity gap / nil repeated field | Fixed: `Microsoft.ContainerInstance/containerGroups` now normalizes each container's optional repeated `ports` and `environmentVariables` fields to empty arrays, and normalizes `ipAddress.ports` when an IP address exists. This matches the ARM/SDK response shape and prevents terraform-provider-azurerm v4.76.0 from nil-dereferencing `container.Properties.Ports` during read. SDK coverage asserts the official Azure SDK receives non-nil empty arrays. |
| ~~1586~~ | P1 | gcp/azure simulator CI — SDK/CLI helper images pull remote Go/Alpine bases | CI reliability / registry dependency | Fixed: GCP and Azure SDK/CLI TestMain helpers now build workload binaries locally with `GOOS=linux` and package them into `scratch` Docker images. The sim suites no longer depend on public ECR/Docker Hub rate limits while constructing `eval-arithmetic` or `http-localhost-probe` harness images. |
| ~~1583~~ | P2 | aws simulator — registered-operation coverage audit residuals | test coverage gap / public client regression exposure | Fixed: the older AWS registered-operation residuals now have SDK regression coverage for SSM GetParameters and RemoveTagsFromResource, Glue GetPartitionIndexes, CodeBuild ListBuilds, SFN ListStateMachineVersions and ValidateStateMachineDefinition, CloudWatch Logs PutRetentionPolicy, SQS SetQueueAttributes, and ElastiCache tag removal. |
| ~~1582~~ | P2 | gcp simulator — Spanner/Dataflow/Bigtable service slices missing | sim coverage gap / missing public APIs | Fixed: the GCP simulator now implements Cloud Spanner instance/database/session control-plane paths, Dataflow regional job create/get/list/update, and Bigtable instance/cluster/table REST admin paths with official SDK, `gcloud`, and Terraform coverage where the provider exposes the slice. |
| ~~1581~~ | P2 | azure simulator — Logic Apps and ACI service slices missing | sim coverage gap / missing public APIs | Fixed: the Azure simulator now implements Microsoft.Logic workflow lifecycle/enable/disable/validate plus trigger run/run-history records, and Microsoft.ContainerInstance containerGroups lifecycle/start/stop/restart/logs/exec. ACI starts real local containers in Docker-runtime mode and exposes SDK-shaped websocket exec sessions. SDK, Azure CLI, and Terraform coverage ship with the slice. |
| ~~1580~~ | P1 | aws ECS simulator — RunTask container override environment is ignored | sim fidelity gap / task runtime override dropped | Fixed: consumer #530 launches a Fargate awsvpc managed-EBS workspace task through the standard `RunTask.overrides.containerOverrides[].environment` path. The simulator now deserializes the official ECS `TaskOverride`/`ContainerOverride` shape, echoes `overrides` on task responses, applies task-level CPU/memory overrides to the task object, and applies named container `environment` plus `command` overrides to the real runtime container only for that task. SDK + CLI regressions run real Fargate tasks and assert the override env reaches the container process through CloudWatch Logs. |
| ~~1579~~ | P1 | aws ECS simulator — managed-EBS awsvpc task private IP is not reachable from same-VPC task | sim fidelity gap / data-plane networking interaction | Fixed/covered: consumer #526's combined path is now pinned by a real AWS CLI regression that launches a Fargate awsvpc server task with an ECS managed EBS configured-at-launch volume mounted at `/workspace`, serves HTTP from that mounted path, then proves a same-subnet client task reaches the server's reported ENI private IP. The test also asserts the reported ENI IP is the task container's real `eth0` address and that the task carries a real AmazonElasticBlockStorage attachment. |
| ~~1578~~ | P1 | aws ECS simulator — Fargate sandbox omits SYS_CHROOT and breaks OpenSSH preauth | sim fidelity gap / sandbox capability mismatch | Fixed: consumer #527 found OpenSSH preauth chroot failed with EPERM because the simulator's Fargate sandbox dropped all capabilities and re-added a subset that omitted `SYS_CHROOT`. `SandboxFargate` now keeps `SYS_CHROOT`, matching real Fargate's support for sshd-style containers. Coverage includes the sandbox profile assertion plus a real ECS/Fargate CLI task that performs `chroot(2)` inside the task container; EPERM would fail the task, while the expected post-chroot missing executable path proves the syscall was permitted. |
| ~~1577~~ | P2 | azure Entra simulator — duplicate userPrincipalName accepted and ROPC can mint stale group claims | sim fidelity gap / uniqueness violation | Fixed: Graph `POST /v1.0/users` now rejects duplicate `userPrincipalName` values case-insensitively with `Request_BadRequest`, matching real Entra uniqueness semantics and preventing the stale duplicate-user state. ROPC lookup now uses the same case-insensitive UPN resolver. SDK + CLI Graph/ROPC tests create a user, add it to a group, verify duplicate create is rejected, then mint an `id_token` and assert the `oid`/`groups` claims still belong to the only valid user. |
| ~~1576~~ | P2 | aca backend — Apps attach stdin test fails locally under Podman because reverse agent never registers | backend integration / ACA Apps reverse-agent callback | Fixed: `TestACAGitLabRunnerAttachStdin` on local Podman created and started the ACA App, but the in-container bootstrap could not resolve the reverse-agent callback host on Docker-API-created Podman user-defined networks (`host.docker.internal`/`host.containers.internal` lookup hit the network DNS and returned connection refused). The integration harness now chooses `host.containers.internal` when the Docker API reports Podman, and the Azure simulator injects explicit Podman-machine `/etc/hosts` entries for both host aliases into ACA/AZF/App Service workload containers when the Podman VM route exposes the host callback subnet. ACA reverse-agent timeout errors now include a real Log Analytics tail from the app logs, which made the failed dial visible. |
| ~~1575~~ | P2 | aca backend CI — integration tests pulled `alpine:latest` from Docker Hub | CI reliability / registry dependency | PR #524 CI failed in `test (azure backends)` at `TestACAContainerLifecycle`: the ACA backend integration tests pulled `alpine:latest`, and Docker Hub returned a 502 while fetching image config. Fix: use the ECR Public Gallery mirror image for ACA integration pulls/creates, matching the existing test harness Dockerfiles that already avoid Docker Hub. |
| ~~1574~~ | P2 | gcp simulator CI — cross-job DNS tests pulled external Alpine images during job execution | CI reliability / registry dependency | PR #524 CI failed in `sim (gcp)` at `TestDNS_CrossJobResolution`: the Cloud Run Job containers used registry Alpine images, so a fresh runner had to pull during the test and hit registry timeout/tag-inspect failures before the real DNS assertions could run. This is the same external-registry reliability class already avoided in other harness images. Fix: run the DNS job containers from the local Linux harness image that TestMain already builds, overriding its entrypoint with `sh -c sleep 30`, so the simulator still starts real containers and CI no longer depends on a registry pull inside the test. |
| ~~1573~~ | P2 | aws ECS simulator — ExecuteCommand response/error contract was under-modelled | sim fidelity gap / partial response + permissive validation | `ExecuteCommand` returned only `session`, though the ECS API and SDK output shape include `clusterArn`/`containerArn`/`containerName`/`interactive`/`taskArn`; it also accepted `interactive=false` even though ECS supports only interactive sessions, and a missing backing task container could produce a session that would fail later instead of the declared ECS error. Fix: validate interactive/container selection up front, return `TargetNotConnectedException` when the execution target is unavailable, and emit the full SDK-shaped response. SDK assertions cover every response field plus non-interactive/unknown-container rejection; CLI covers `aws ecs execute-command` through the real command surface on the disabled-exec rejection path. |
| ~~1572~~ | P1 | aws ECS — overlapping VPC CIDRs failed (Docker can't host overlapping bridges) + VPC networks leaked on DeleteVpc (follow-up to #516) | sim fidelity gap / netns fabric | After #516 the Docker-network VPC tier couldn't host two VPCs sharing a CIDR (legal in AWS — VPCs are isolated), and a remap-to-a-pool scheme was rejected as a fallback. Real fix: a **network-namespace-per-VPC** fabric (reusing the realexec library: VPC=netns, subnet=bridge) — separate routing tables, so overlapping CIDRs work natively with the real ENI IP and no remapping. New realexec primitive `Subnet.AttachExternalNamespaceNIC` plumbs a veth into an existing (container) netns by PID; ECS uses a **Fargate-style pause container** (a long-lived sleep on `--network none`) to hold the task's netns so the ENI is attached with no start-race, then every task container shares it. Capability-gated (`ec2ECSRealNetAvailable` = Linux + CAP_NET_ADMIN + nsenter — the consumer's privileged container + the sudo CI jobs); off that tier the cross-platform Docker-network tier from #516 still serves distinct CIDRs (fail-loud on overlap, no fallback). `DeleteVpc` now tears down both fabrics (was a leak). Tier-agnostic CLI test (probes via the task containers) + a netns-gated overlapping-CIDR test (two VPCs, same `10.50.0.0/16`, both get the same real ENI IP, isolated) + a realexec unit test for the new primitive. **Netns tier is Linux-only and verified on CI.** Follow-up coverage adds task metadata DNAT and route-table egress enforcement on that same netns fabric. |
| ~~1571~~ | P1 | aws ECS — task ENI privateIPv4Address was a phantom VPC IP, not the container's real routable address; VPCs were not isolated networks (consumer #516) | sim fidelity gap / metadata-only networking | `RunTask` allocated a subnet-CIDR IP via `AllocateSubnetIP` and reported it as the ENI `privateIPv4Address`, but the container launched on Docker's default bridge with an unrelated `172.17.x` IP → the reported IP routed nowhere, and tasks in different VPCs were not isolated. Fix (real execution): each VPC is now backed by a Docker bridge network (`sockerless-sim-vpc-<vpcid>`, IPAM subnet = VPC CIDR) and an awsvpc task's first container is pinned to it at its ENI IP (`ContainerConfig.IPAddress` → `EndpointIPAMConfig.IPv4Address`); secondary containers share that netns (the shared ENI), exactly as real awsvpc. The reported `privateIPv4Address` is now the container's **real, routable** IP — intra-VPC reachable (the implicit local route, enforced by a real Linux bridge) and isolated across VPCs (separate networks). Networks carry the `sockerless-sim=true` label so the existing shutdown reaper cleans them up. End-to-end CLI test: server task in VPC-A's ENI IP == its real container IP, a same-VPC probe reaches it over TCP, a different-VPC probe is isolated. **Resolves #516.** Route-table *egress* enforcement (public/private via IGW/NAT, fck-nat ENI forwarding) stages next. Note: Docker IPAM rejects two networks with overlapping CIDRs, so two simultaneous VPCs must use distinct CIDRs (the realexec netns path would lift this; staged). |
| ~~1570~~ | P1 | gcp pubsub — topic dropped kmsKeyName/messageRetentionDuration/messageStoragePolicy; subscription dropped deadLetterPolicy/retryPolicy (fidelity audit) | sim fidelity gap / dropped fields | `PSTopic` stored only `Name`+`Labels`, so `google_pubsub_topic.{kms_key_name,message_retention_duration,message_storage_policy}` read back null and drifted every plan; `PSSubscription` carried most fields but had no `DeadLetterPolicy`/`RetryPolicy`, so `google_pubsub_subscription.{dead_letter_policy,retry_policy}` drifted (and the bare `PSPushConfig` dropped `attributes`/`oidcToken`). Fix: add the topic fields (+ `MessageStoragePolicy`) — parsed on create, round-tripped on get, accepted in the PATCH updateMask; add `DeadLetterPolicy`/`RetryPolicy` to the subscription (+ enriched `PushConfig`) parsed on create + PATCH. SDK test (topic kms/retention/storage + subscription DLQ/retry round-trip incl. PATCH) + the GCP terraform apply stack's pubsub subscription gains dead_letter_policy + retry_policy and the topic gains message_retention_duration (idempotent second plan, runs on CI `tf (gcp)`). |
| ~~1569~~ | P1 | aws IAM — CreateRole dropped description/max_session_duration/permissions_boundary; UpdateRole/TagRole/UntagRole missing; GetOpenIDConnectProvider dropped tags + Tag/UntagOIDC missing (fidelity audit) | sim fidelity gap / dropped fields + missing ops | `IAMRole` had no `Description`/`PermissionsBoundary`, `CreateRole` parsed neither (nor `MaxSessionDuration`), and `iamRoleFieldsXML` rendered neither → `aws_iam_role.{description,max_session_duration,permissions_boundary}` drifted every plan; `UpdateRole` + `TagRole`/`UntagRole` were unregistered so post-create attribute/tag changes hit UnknownOperation. `GetOpenIDConnectProvider` stored tags but never rendered them (`aws_iam_openid_connect_provider.tags` drifted) and `Tag`/`UntagOpenIDConnectProvider` were unregistered. Fix: add the role fields (parse on Create, render the `<Description>`/`<PermissionsBoundary>` blocks, real `PermissionsBoundaryPolicy` type); register `UpdateRole`/`TagRole`/`UntagRole` (tag upsert semantics, shared `iamEmptyResultXML` with the required `<{op}Result/>` node); render OIDC tags via `iamMapTagsXML` + register `Tag`/`UntagOpenIDConnectProvider`. SDK+CLI both. |
| ~~1568~~ | P1 | aws scheduler — scheduler-fired RunTask/SendMessage/Invoke/Publish swallowed the target handler's error and recorded a phantom CloudTrail success (consumer #514) | sim fidelity gap / silent error swallow (no-fakes) | EventBridge Scheduler fires a target by calling the sim's own handler in-process, but `callJSONHandler` (and the Lambda/SNS dispatch) **discarded the response** and `fireECSTarget` recorded CloudTrail success unconditionally. So when RunTask correctly rejected a request (e.g. a security group that doesn't exist), the 400 vanished: CloudTrail showed a clean RunTask, no task was created, and nothing ever transitioned to STOPPED — yet a *direct* RunTask with the same SG returned `InvalidParameterException`. Fix: `callJSONHandler` returns (status, body); a shared `recordSchedulerFireResult` records the fire honestly — on a downstream API failure it carries `errorCode`/`errorMessage` (real CloudTrail records failed calls this way; added the fields to `CloudTrailEvent`) instead of a phantom success. Applied to **all** firing targets (ECS/SQS = awsJson errors, SNS = XML error, Lambda = only Invoke-API failures, since a function error still 200s). SDK test: a scheduler-fired RunTask with a nonexistent SG records RunTask with `errorCode=InvalidParameterException` and leaves no STOPPED task; the valid-config happy path still launches the container. **Resolves #514.** |
| ~~1567~~ | P1 | aws ECS — CreateService/UpdateService dropped most service knobs; UpdateCluster/UpdateClusterSettings unregistered (ELBv2/ECS audit PR-4) | sim fidelity gap / dropped fields + missing ops | `CreateService` parsed ~17 fields but dropped `enableECSManagedTags`/`healthCheckGracePeriodSeconds`/`placementConstraints`/`placementStrategy`/`availabilityZoneRebalancing`/`serviceConnectConfiguration`/`volumeConfigurations`/`vpcLatticeConfigurations`; `UpdateService` persisted only 4 fields (taskDefinition/desiredCount/networkConfiguration/enableExecuteCommand) and silently ignored every other updatable field the provider sends → `aws_ecs_service` drifted every plan. `UpdateCluster` + `UpdateClusterSettings` were unregistered, so any `aws_ecs_cluster.{setting,configuration,service_connect_defaults}` change forced recreation. Fix: parse + store the dropped service fields (the three deployment-scoped ones — serviceConnectConfiguration/volumeConfigurations/vpcLatticeConfigurations — surface inside the primary deployment record per the SDK Service shape, not the top level); `UpdateService` now persists the full updatable surface; register + implement `UpdateCluster`/`UpdateClusterSettings` (settings/configuration/serviceConnectDefaults in place); add `ServiceConnectDefaults` to the cluster. SDK+CLI. **Fourth of 4 staged PRs — completes the ECS+ELBv2 fidelity audit.** |
| ~~1566~~ | P1 | aws ECS — RegisterTaskDefinition dropped most container-def fields + the top-level knobs (runtimePlatform/ephemeralStorage/proxyConfiguration/pidMode/ipcMode/placementConstraints); no compatibilities (ELBv2/ECS audit PR-3) | sim fidelity gap / dropped fields | `ECSContainerDefinition` enumerated only the runtime-relevant fields (name/image/cpu/memory/essential/env/mounts/ports/logConfig/entryPoint/command + raw healthCheck/secrets) — every other registered field (`ulimits`, `dependsOn`, `linuxParameters`, `dockerLabels`, `user`, `workingDirectory`, `privileged`, `readonlyRootFilesystem`, `start/stopTimeout`, `systemControls`, `resourceRequirements`, `firelensConfiguration`, …) was silently dropped, so the provider's ForceNew containerDefinitions hash differed on read → **new revision every plan**. The task-def top-level knobs `runtimePlatform`/`ephemeralStorage`/`proxyConfiguration`/`pidMode`/`ipcMode`/`placementConstraints`/`inferenceAccelerators`/`enableFaultInjection` were never parsed (same drift on `aws_ecs_task_definition.{runtime_platform,ephemeral_storage,…}`), and the AWS-computed `compatibilities` list was absent. Fix: container definitions capture the verbatim registered bytes (custom Marshal/Unmarshal — typed fields still decoded for the runtime, e.g. secret injection) so every field round-trips; add the top-level knobs (nested objects as RawMessage passthrough); compute `compatibilities` faithfully (requiresCompatibilities ∪ EC2 ∪ FARGATE-if-awsvpc). `requiresAttributes` intentionally NOT fabricated (needs AWS's capability-attribute engine; no stable client reads it). SDK+CLI + idempotency-fidelity TF (task-def gains runtime_platform + ephemeral_storage on the minimal container def; second plan clean). **Third of 4 staged PRs closing the ECS+ELBv2 fidelity audit; remaining: ECS services+clusters.** |
| ~~1565~~ | P1 | aws ELBv2 — listener-rule ARN used `:rule/` not `:listener-rule/`; authenticate-oidc/cognito + weighted ForwardConfig dropped; SetRulePriorities + listener-certificate ops missing; ModifyListener dropped SslPolicy; AlpnPolicy/MutualAuthentication omitted (ELBv2/ECS audit PR-2) | sim fidelity gap / wrong ARN + dropped configs + missing ops | `elbv2RuleArn` built `arn:…:rule/app/…` but real ELBv2 rule ARNs use the `listener-rule/` resource prefix; terraform-provider-aws reconstructs `aws_lb_listener_rule.listener_arn` from the rule ARN by requiring exactly `listener-rule/<type>/<lb>/<lbid>/<listenerid>/<ruleid>` (else empty) → `listener_arn` read back null → **forced replacement every plan** (latent: the apply stack created rules but never ran a 2nd plan). Actions modelled only forward/fixed-response/redirect — `authenticate-oidc` (the Pomerium/IAP proxy shape), `authenticate-cognito`, and the weighted `ForwardConfig` (multi-target-group + stickiness) were dropped, so those rules/listeners drifted. `SetRulePriorities` and the listener-certificate trio (`AddListenerCertificates`/`RemoveListenerCertificates`/`DescribeListenerCertificates` — the `aws_lb_listener_certificate` SNI resource) were unregistered; `ModifyListener` dropped `SslPolicy`; listeners omitted `AlpnPolicy` + `MutualAuthentication`. Fix: `:listener-rule/` ARN; parse+render AuthenticateOidcConfig (ClientSecret never echoed — write-only)/AuthenticateCognitoConfig/ForwardConfig (TargetGroups[]+stickiness) with the query-protocol `.entry.N.key/.value` extra-params map; register the four ops (SNI certs tracked separately from the default cert, IsDefault flag); `ModifyListener` updates SslPolicy/AlpnPolicy/MutualAuth. SDK+CLI + idempotency-fidelity TF (authenticate-oidc rule + weighted forward + SNI `aws_lb_listener_certificate`; second plan clean). **Second of 4 staged PRs closing the ECS+ELBv2 fidelity audit; remaining: ECS task-defs, ECS services+clusters.** |
| ~~1564~~ | P1 | aws ELBv2 — target-group Matcher hardcoded 200; protocol_version/ip_address_type omitted; LB enforce-SG/customer-owned-pool omitted; SetIpAddressType missing (ELBv2/ECS audit PR-1) | sim fidelity gap / hardcoded + dropped fields | `elbv2TargetGroupXML` emitted a literal `<Matcher><HttpCode>200</HttpCode>` regardless of request → `aws_lb_target_group.health_check.matcher` drifted (NLB default is 200-399, so it drifted immediately). `protocol_version`/`ip_address_type` were never parsed/rendered; `ModifyTargetGroup` dropped `HealthCheckEnabled`/`Matcher`; the LB omitted `EnforceSecurityGroupInboundRulesOnPrivateLinkTraffic` (NLB) + `CustomerOwnedIpv4Pool`; `SetIpAddressType` was unregistered (in-place `aws_lb.ip_address_type` change 404'd). Fix: store + render Matcher (HttpCode/GrpcCode, per-protocol default via `elbv2DefaultMatcher`), ProtocolVersion (HTTP1 default for HTTP/HTTPS), IpAddressType; parse them on Create/Modify; render the LB fields; register `SetIpAddressType`. SDK+CLI + idempotency-fidelity TF (target group gains matcher=200-299 + protocol_version; second plan clean). **First of 4 staged PRs closing the ECS+ELBv2 fidelity audit (~25 gaps); remaining: ELBv2 listeners+rules, ECS task-defs, ECS services+clusters.** |
| ~~1563~~ | P2 | azure-sim — v2.0 OIDC discovery missing userinfo_endpoint; no userinfo endpoint (consumer #508, OIDC Core §5.3) | sim coverage gap (next hurdle after #504) | NOT a #506 regression — `userinfo_endpoint` was never in the azure discovery doc (`git log -S` confirms); #504's issuer fix let Pomerium get *past* provider init, so coreos/go-oidc's `provider.UserInfo()` (called after the token exchange) then hit this pre-existing gap and returned "user info endpoint is not supported by this provider" → Pomerium 500. Fix: advertise a sim-hosted `userinfo_endpoint` (`<baseURL>/{tenant}/v2.0/userinfo`; real AAD points at Graph which the sim doesn't proxy) + serve `GET /{tenant}/v2.0/userinfo`. Per OIDC Core §5.3 it **requires a valid bearer token** — `verifyAzureSimJWT` checks the RS256 signature against the sim key + expiry and returns **401 (no fallback identity)** on missing/invalid tokens; `sub`/`oid` come from the verified token, profile claims from the user. Verified with the real coreos/go-oidc `provider.UserInfo()`. |
| ~~1562~~ | P2 | aws EC2 — DescribeVolumes/DescribeSnapshots ignore Filters; DescribeVolumesModifications missing (EC2 audit PR-E) | sim fidelity gap / filter ignored + missing op | `DescribeVolumes`/`DescribeSnapshots` honoured only an explicit id list — every `Filter.N` (`volume-type`/`status`/`tag:`/`volume-id`/`encrypted`/`owner-id`) was ignored, returning ALL, so data-source lookups over-matched. And `DescribeVolumesModifications` was unregistered → `aws_ebs_volume` resize/type-change updates errored `UnknownOperation` (the provider polls it to `completed`). Fix: `ec2VolumeMatchesFilters`/`ec2SnapshotMatchesFilters` + `OwnerIds`; `ModifyVolume` records an `EC2VolumeModification` (state=completed) and the new `DescribeVolumesModifications` returns it. SDK+CLI. |
| ~~1561~~ | P1 | aws EC2 — Volume iops/throughput/kmsKeyId/multiAttach + Snapshot encrypted/kmsKeyId omitted (EC2 audit PR-E) | sim fidelity gap / dropped fields | `CreateVolume`/`DescribeVolumes` never parsed or rendered `Iops`/`Throughput`/`KmsKeyId`/`MultiAttachEnabled` (gp3 always read back as null iops/throughput → `aws_ebs_volume` drift every plan), and `Snapshot` omitted `Encrypted`/`KmsKeyId`. Fix: add the fields; `ec2ResolveVolumePerformance` applies the AWS per-type rules (gp3 defaults 3000/125, gp2 derives iops from size, io1/io2 use requested iops), stored so they round-trip; snapshots inherit encryption/kms from the source volume. **Completes the EC2/VPC fidelity audit — slices A–E.** SDK+CLI + idempotency-fidelity TF (gp3 volume with iops/throughput/encrypted; second plan clean). |
| ~~1560~~ | P2 | aws EC2 — instances residual: key pairs, ModifyInstanceMetadataOptions, LT instance_market_options/credit_specification, DescribeImages filters (EC2 audit PR-F) | sim coverage gap | Closed (was staged from PR-D): **key pairs** — `CreateKeyPair` (generates a real RSA key, returns the private material), `ImportKeyPair`, `DeleteKeyPair`, and a real `DescribeKeyPairs` (was always empty) backed by a new `ec2KeyPairs` store + MD5 fingerprint; **`ModifyInstanceMetadataOptions`** updates metadata_options in place; **launch template** `CreditSpecification` + `InstanceMarketOptions` (spot) parsed + rendered in the LT data round-trip; **`DescribeImages`** now honours `Filters` (name/architecture/root-device-type/virtualization-type/owner-alias) by resolving a deterministic synthesized image (the sim has no AMI registry — AMIs are opaque — so a `data.aws_ami` lookup resolves consistently rather than returning a fixed image). SDK+CLI + idempotency-fidelity TF (aws_key_pair + spot launch_template + data.aws_ami; second plan clean). **Fully closes the EC2/VPC fidelity audit — nothing staged remains.** |
| ~~1559~~ | P2 | aws EC2 — ModifyInstanceAttribute silent no-op; DescribeInstanceAttribute hardcoded constants (EC2 audit PR-D) | sim fidelity gap / silent no-op | `ModifyInstanceAttribute` validated the instance then returned `true` without persisting ANYTHING (the provider sets `source_dest_check`/`disable_api_termination`/in-place `instance_type` via it), and `DescribeInstanceAttribute` returned literal constants ignoring stored state. Fix: persist SourceDestCheck/DisableApiTermination/EbsOptimized/InstanceType/UserData/GroupId; `DescribeInstanceAttribute` reflects the stored values. SDK+CLI + idempotency-fidelity TF (aws_instance gains `source_dest_check=false`, the NAT-instance path). |
| ~~1558~~ | P1 | aws EC2 — RunInstances drops nearly all input knobs; Instance response omits provider-read fields (EC2 audit PR-D) | sim fidelity gap / dropped knobs+fields | `RunInstances` parsed only image/type/subnet/sg/key/tags; `IamInstanceProfile`, `MetadataOptions`, `EbsOptimized`, `Monitoring`, `CpuOptions`, root block-device size/type, `UserData` were dropped, launch-template data wasn't applied to the instance, and `ec2InstanceXML` hardcoded monitoring=disabled/sourceDestCheck=true and omitted metadataOptions/iamInstanceProfile/ebsOptimized/keyName/cpuOptions → every `aws_instance` with those set drifted. Fix: parse the knobs (request → launch-template → AWS-doc default for metadata), thread through `EC2InstanceCreateSpec`/`ec2CreateInstance`, render the full shape; root volume size/type flow into the EBS volume. SDK+CLI + idempotency-fidelity TF (aws_instance gains metadata_options/monitoring/ebs_optimized; second plan clean). |
| ~~1557~~ | P1 | azure-sim — v2.0 OIDC discovery + id_token issuer hardcoded sts.windows.net, breaking strict OIDC clients (consumer #504, RFC 8414 §3) | sim fidelity gap | `/{tenant}/v2.0/.well-known/openid-configuration` always returned `issuer: https://sts.windows.net/{tenant}/`, but RFC 8414 §3 requires it to equal the fetch URL, so coreos/go-oidc (Pomerium) failed at provider init with "issuer did not match". Fix: version-aware `azureIssuer` — v2.0 path → `<baseURL>/{tenant}/v2.0`, v1 path keeps `sts.windows.net` (azidentity's expectation); the v2.0 id_token `iss` now matches the discovery issuer (access tokens keep the v1 sts.windows.net iss, as real ARM tokens do). Verified with the real coreos/go-oidc client (NewProvider + Verify). |
| ~~1556~~ | P2 | bleephub — admin token hardcoded GitHub-PAT-format value, not configurable (consumer #501) | fake/fallback (no-fakes) + config | The seeded admin token was the compile-time constant `ghp_0000…` — not configurable, and PAT-shaped so any consumer test referencing it tripped Trivy `github-pat` CRITICAL. Fix (per the no-defaults/fail-loud directive): the admin token is supplied **only** via `BLEEPHUB_ADMIN_TOKEN` with **no default** — the binary `log.Fatal`s at startup if unset (consistent with the persistence fail-loud pattern); the test harness/scripts/UI/docs all set an explicit non-PAT value (`bleephub-admin-token-…`). |
| ~~1555~~ | P2 | aws EC2 — ENI interfaceType omitted; ModifyNetworkInterfaceAttribute drops Attachment.DeleteOnTermination (EC2 audit PR-C) | sim fidelity gap / dropped field | `DescribeNetworkInterfaces`/`CreateNetworkInterface` never emitted `<interfaceType>` (`aws_network_interface.interface_type`, computed, defaults `interface`) → comes back empty. And `ModifyNetworkInterfaceAttribute` ignored `Attachment.DeleteOnTermination` (the provider toggles `delete_on_termination` via it). Fix: add `InterfaceType` to `EC2NetworkInterface` (parsed at create, rendered, default `interface`) + parse `Attachment.DeleteOnTermination`. SDK. |
| ~~1554~~ | P2 | aws EC2 — Revoke* leaves orphan rule rows + over-deletes; SecurityGroupRule tags on wrong wire key + no ARN; inline -1 ports; PrefixListIds unmodelled (EC2 audit PR-C) | sim fidelity gap / multiple | `Revoke*` dropped the inline permission but not the `SecurityGroupRule` rows (orphans persisted in DescribeSecurityGroupRules) and `removePermission` matched on protocol+ports only → revoking one of several rules sharing a port range deleted them all. Fixed: `deleteSecurityGroupRules` removes matching rows; `removePermission` matches the full source set (`permSourceKey`). Also: `sgrItemXML` emitted `<tags/>` (SDK reads `tagSet`) + no `securityGroupRuleArn` → now `<tagSet>`+arn; inline `ipPermsXML` emitted fromPort/toPort even for `-1` (drift "0→null") → now omitted; `PrefixListIds` weren't parsed/rendered on inline permissions → now are; added `UpdateSecurityGroupRuleDescriptions{Ingress,Egress}` (legacy inline-block description path). SDK+CLI. |
| ~~1553~~ | P1 | aws EC2 — Authorize* with Ipv6Ranges/PrefixListIds produces NO SecurityGroupRule row (EC2 audit PR-C) | sim fidelity gap / silent drop | `createSecurityGroupRules` iterated only IpRanges + UserIdGroupPairs, and `EC2SecurityGroupRule` had no `CidrIpv6`/`PrefixListId` fields → an `aws_vpc_security_group_{ingress,egress}_rule` with `cidr_ipv6`/`prefix_list_id` read back empty via DescribeSecurityGroupRules (by rule id) and drifted/recreated every plan. Fix: add the fields; `createSecurityGroupRules` emits a row per IPv4/IPv6/prefix-list/group source; `sgrItemXML` renders `cidrIpv6`/`prefixListId`. SDK+CLI + idempotency-fidelity TF gains a standalone `cidr_ipv6` egress rule (second plan clean). |
| ~~1552~~ | P2 | aws EC2 — no main route table auto-created per VPC (EC2 audit PR-B) | sim fidelity gap / missing behaviour | Real AWS creates a main route table (local route + a main association with no subnet) at `CreateVpc`; the sim created only the VPC, so `aws_vpc.main_route_table_id`/`default_route_table_id`, `aws_default_route_table`, and `data.aws_route_table{association.main=true}` found nothing. Fix: `CreateVpc` now provisions the main route table; `DescribeRouteTables` gained a proper filter matcher incl. `association.main` (the provider filters by it and expects exactly one). SDK + terraform `data.aws_route_table` reads it back. |
| ~~1551~~ | P2 | aws EC2 — NatGatewayAddress / RouteTableAssociation / Address response fields omitted; Describe* filters thin (EC2 audit PR-B) | sim fidelity gap / dropped fields | NatGatewayAddress emitted only allocationId/publicIp/privateIp → `aws_nat_gateway.network_interface_id`/`association_id` came back empty (now renders networkInterfaceId/associationId/isPrimary/status via a shared `natgwAddrSetXML`). RouteTableAssociation omitted `associationState` (provider reads `association_state`) + `gatewayId` → now rendered. DescribeAddresses/DescribeRouteTables honoured only a single id → now full filter matchers (allocation-id/public-ip/instance-id/association-id/tag:; route-table-id/vpc-id/association.*/tag:) + `*.N` lists. SDK+CLI. |
| ~~1550~~ | P1 | aws EC2 — Elastic IP association lifecycle missing (AssociateAddress/DisassociateAddress + Address fields) (EC2 audit PR-B) | sim fidelity gap / missing ops | `AssociateAddress`/`DisassociateAddress` were unregistered and `Address` omitted associationId/instanceId/networkInterfaceId/privateIpAddress/networkBorderGroup/publicIpv4Pool, so `aws_eip` with `instance`/`network_interface` (and `aws_eip_association`) could never round-trip `association_id`/`private_ip`. Fix: implement both ops (issue stable `eipassoc-` ids; private IP inherited from the instance when unset), add the fields to `EC2ElasticIP` + `eipItemXML`, and parse `AllocateAddress` `PublicIpv4Pool`(=amazon)/`NetworkBorderGroup`(=region). SDK+CLI. |
| ~~1549~~ | P1 | aws EC2 — ReplaceRoute missing; CreateRoute parsed only 4 of ~13 route targets (EC2 audit PR-B) | sim fidelity gap / missing op + dropped knobs | `ReplaceRoute` was unregistered → every `aws_route` in-place target change (e.g. IGW→NAT) failed. `CreateRoute`/`EC2Route`/`routeSetXML` modelled only gateway/nat/eni, silently dropping `DestinationIpv6CidrBlock`, `DestinationPrefixListId`, `EgressOnlyInternetGatewayId`, `VpcPeeringConnectionId`, `TransitGatewayId`, `InstanceId`, `LocalGatewayId`, `CarrierGatewayId`, `VpcEndpointId`, `CoreNetworkArn` → IPv6/peering/TGW/prefix-list routes vanished or drifted. Fix: `parseRouteFromRequest` covers all targets; `EC2Route` + `routeSetXML` carry them; `ReplaceRoute` finds-by-destination and swaps the target (InvalidRoute.NotFound when absent); DeleteRoute matches IPv4/IPv6/prefix destinations. SDK+CLI. |
| ~~1548~~ | P2 | aws EC2 — ModifyVpcAttribute drops EnableNetworkAddressUsageMetrics; CreateVpc drops InstanceTenancy; DescribeVpcs hardcodes tenancy + omits dhcpOptionsId (EC2 audit PR-A) | sim fidelity gap / silent no-op | `ModifyVpcAttribute` parsed only DnsSupport/DnsHostnames → `enable_network_address_usage_metrics=true` was a silent no-op (DescribeVpcAttribute hardcoded `false`). `CreateVpc` ignored `InstanceTenancy` and `vpcItemXML` hardcoded `<instanceTenancy>default</instanceTenancy>` + emitted no `dhcpOptionsId`, so `aws_vpc.instance_tenancy`/`dhcp_options_id` drift. Fix: parse + persist `EnableNetworkAddressUsageMetrics`/`InstanceTenancy`; render stored tenancy + `dhcpOptionsId=default` via a shared `vpcItemBodyXML`. SDK+CLI + idempotency-fidelity TF (vpc gains both knobs). |
| ~~1547~~ | P1 | aws EC2 — DescribeSubnets honoured only `SubnetId.1` + `vpc-id` filter (EC2 audit PR-A) | sim fidelity gap / filter ignored | Every other filter (`tag:`, `cidr-block`, `availability-zone`, `subnet-id`, `state`, `owner-id`) was ignored → returned ALL subnets, so `data.aws_subnet`/`aws_subnets` lookups over-matched. Also read only a single `SubnetId.1`, not the `SubnetId.N` list. Fix: full `ec2SubnetMatchesFilters` (mirrors the VPC filter machinery) + `ec2ParamList("SubnetId")` list + NotFound on unknown id. SDK asserts tag:/cidr-block scoping. |
| ~~1546~~ | P1 | aws EC2 — ModifySubnetAttribute silent no-op + Subnet response fields omitted (EC2 audit PR-A) | sim fidelity gap / silent no-op | `ModifySubnetAttribute` parsed only `MapPublicIpOnLaunch` → `assign_ipv6_address_on_creation`/`enable_dns64`/`private_dns_hostname_type_on_launch`/`enable_resource_name_dns_a(aaa)_record_on_launch`/`map_customer_owned_ip_on_launch` were all silent no-ops (set succeeds, read shows old value → permanent drift). `subnetItemXML` also omitted `availableIpAddressCount`, `availabilityZoneId`, `subnetArn`, `assignIpv6AddressOnCreation`, `enableDns64`, `ipv6Native`, `defaultForAz`, `privateDnsNameOptionsOnLaunch`. Fix: parse + persist all attrs; `subnetItemBodyXML` renders the full SDK `Subnet` shape (availableIp = 2^hostbits − 5 AWS-reserved; synthesized stable AZ-id since real ones are account-randomized). SDK+CLI + idempotency-fidelity TF (subnet gains the new knobs; second plan clean). **First of 5 staged PRs (A–E) closing the EC2/VPC fidelity audit (~28 consumer-hit gaps); remaining slices: routing+EIP, security-groups+ENI, instances+launch-templates, EBS+filters.** |
| ~~1545~~ | P2 | aws CloudTrail — EventBridge Scheduler API calls not recorded (consumer #498) | sim coverage gap (REST service, residual of 1540) | Scheduler is a REST/JSON service registered directly on the server mux (`POST /schedules/{Name}`, …), so its API calls bypassed the central `POST /` recording middleware → `CreateSchedule`/`UpdateSchedule`/`DeleteSchedule`/`Get`/`List` (+ schedule-group ops) never appeared in `LookupEvents`. Real CloudTrail records all Scheduler API calls against `scheduler.amazonaws.com`. Fix: `schedulerRecorded(eventName, handler)` wraps each route, recording the event (eventSource `scheduler.amazonaws.com`, resources = the schedule/group, readOnly per verb, accessKeyId from SigV4) after it serves — the issue's "add explicit recording to each handler" option, since moving Scheduler onto the awsRouter would be wrong (it's genuinely REST, not X-Amz-Target). SDK+CLI. |
| ~~1544~~ | P2 | aws CloudTrail — scheduler-fired RunTask/SendMessage/Publish/Invoke not recorded (consumer #497) | sim coverage gap (in-process invoke) | The Scheduler firing loop invokes targets by calling the target handler directly (`callJSONHandler`/`httptest`), which never passes through the `POST /` middleware, so the downstream call wasn't recorded. Real CloudTrail records the fired call (RunTask/SendMessage/Publish/Invoke) with `userIdentity.invokedBy = scheduler.amazonaws.com`. Fix: each `fire*Target` records via `cloudTrailRecordSchedulerFire(eventName, source, resourceType, resourceName)` — `InvokedBy` set, no AccessKeyId (service-invoked, matching real AWS). SDK test waits for the fired SendMessage event + asserts the embedded record carries `scheduler.amazonaws.com`. |
| ~~1543~~ | P2 | aws CloudTrail — LookupEvents ignores 5 of 8 LookupAttribute keys (consumer #496) | sim fidelity gap / silent no-op | `cloudTrailEventMatches` handled only EventName/EventSource/Username; EventId, ResourceType, ResourceName, AccessKeyId, ReadOnly fell through the `switch` and were **silently ignored** → a filter using them returned *every* event instead of the matching ones. Per the LookupAttribute API reference all eight must filter. Fix: handle all eight (unknown key → no match, never match-all) + validate keys in the handler (`InvalidLookupAttributesException`); populate the fields faithfully — EventId (existing), ReadOnly (verb classification, AWS's own convention), AccessKeyId (from the SigV4 `Credential=`), and `resources[]` extracted per-operation from the buffered request (`cloudtrail_resources.go`, AWS::Service::Type + the resource the call named, case-insensitive wire keys; no fabricated resources). `Event.ReadOnly` emitted as the string `"true"`/`"false"` the API defines (the boolean lives in the embedded CloudTrailEvent record). SDK+CLI exercise EventId/ResourceName/ResourceType/ReadOnly + a negative (non-matching ResourceName returns nothing). |
| ~~1542~~ | P3 | simulator UI (aws/gcp/azure dashboards) — `@tanstack/react-table` used but undeclared | dependency hygiene | Surfaced the moment the new `knip` gate was wired to the three simulator dashboards: each imports `@tanstack/react-table` in 5 page components but never lists it in `package.json` — it resolved only via bun's hoisted workspace `node_modules` (bleephub declares it). A `--frozen-lockfile` install in a non-hoisting layout, or removing it from bleephub, would break the sim builds with no warning. Fix: declare `@tanstack/react-table: ^8.21.0` (bleephub's pin) in all three sim UI `dependencies`; knip clean afterwards. |
| ~~1541~~ | P2 | tooling — `scripts/bleephub-dupl.sh` was a silent no-op (fake-green gate) | fake/fallback (no-fakes) | The bleephub Go copy-paste gate passed 70+ file paths to `dupl` as positional args; `dupl` mis-parses a long positional list as one giant path and aborts with `lstat …: file name too long`. That error was captured into `$out`, `grep -c "^found"` returned 0, and the script printed `OK` — so the gate (in CI **and** pre-commit) had been **passing without ever scanning a file**. A genuine fake-green. Fix: feed file names via `dupl -files` (stdin, one per line) — its documented scripting interface; verified it now both passes clean and **fails** on an injected clone pair. The new simulator dupl gate uses the same correct interface from the start. |
| ~~1540~~ | P3 | aws CloudTrail — REST-protocol service slices not recorded (sweep) | sim coverage gap | Fixed: the CloudTrail recorder no longer covers only the central `POST /` awsJson/query dispatcher. REST/RPC service registrations now wrap their real handlers with explicit AWS SDK operation names and canonical event sources for Lambda, S3, API Gateway v1/v2, Batch, EFS, CloudFront, Amplify, Route53, and CloudWatch metrics RPCv2. S3 uses method+subresource dispatch so events are `CreateBucket`, `PutBucketPolicy`, `CreateMultipartUpload`, etc. rather than a route-level approximation. The recorder also captures response bodies and records 4xx `errorCode`/`errorMessage`, matching CloudTrail's failed-call event model, and the query-protocol CloudWatch metrics version maps to `monitoring.amazonaws.com`. SDK + CLI regressions verify S3, Lambda failed-call errors, API Gateway v2, and CloudWatch metrics events through real clients. |
| ~~1539~~ | P2 | bleephub — OAuth token endpoint always JSON, real GitHub form-encodes by default (consumer #494) | sim fidelity gap | `POST /login/oauth/access_token` always wrote JSON; real GitHub returns `application/x-www-form-urlencoded` by default and JSON only on `Accept: application/json`. A client expecting the default got an unparseable body. Added `writeOAuthTokenResponse` negotiating on Accept (success + error, web + device flows); test pins both modes. |
| ~~1538~~ | P2 | aws scheduler — cron L/W/# qualifiers silently never fire (consumer #493) | sim fidelity gap | #491's cron eval excluded the L/W/# qualifiers, so a schedule using them was accepted but never fired (real AWS fires all three). Implemented date-aware day-of-month/week predicates: L (last day/Saturday), nL (last weekday n), W/LW (nearest weekday, no month cross), d#n (nth weekday); AWS 1=SUN. Also CreateSchedule now rejects malformed cron with ValidationException instead of silently accepting it. |
| ~~1537~~ | P2 | aws CloudTrail — eventSource mislabelled for most service slices (sweep) | sim fidelity gap | The recorder labelled only ecs/ec2/autoscaling/cloudtrail correctly and defaulted everything else to a generic `aws.amazonaws.com`; since LookupEvents supports EventSource filtering, scoping to e.g. dynamodb.amazonaws.com returned nothing. Mapped every X-Amz-Target prefix + query Version to its canonical `<service>.amazonaws.com`. No fallback default — an unmapped slice logs loudly + skips rather than fabricating a source. |
| ~~1536~~ | P2 | bleephub — OIDC discovery doc missing OAuth2 endpoints (consumer #490) | sim fidelity gap | `handleOIDCDiscovery` returned only the GitHub Actions id_token-issuer shape, omitting `authorization_endpoint`/`token_endpoint`/`userinfo_endpoint` — REQUIRED by OpenID Connect Discovery 1.0 §3 for an authorization-code provider, so RPs that auto-configure (Pomerium/Teleport OIDC, openid-client) couldn't parse it though bleephub implements all three. Added them + `response_modes_supported=[query]`, `grant_types_supported=[authorization_code]` (verified the token endpoint serves only code + device-code grants, not refresh_token), and `code` in `response_types_supported`. Test drives the real endpoints (asserts each advertised URL routes). |
| ~~1535~~ | P2 | aws scheduler — cron `N/step` (start/step) field mis-parsed (consumer #489, residual of #491) | sim fidelity gap | #491 added cron() evaluation but `cronField` collapsed a bare value with a step (`0/5`) to `lo=hi=value`, so `cron(0/5 * * * ? *)` — the idiomatic clock-aligned every-5-min form the issue cites — fired only at minute 0 (hourly). Per AWS EventBridge docs, `N/step` = "from N to field max, every step" (their example `1/10` → 1,11,21,31). Set `hi=max` when a bare value carries a step. The core "cron never evaluated" condition was already fixed by #491; this completes it. Regression tests for `0/5`→12:35 and `2/10`→12:42. |
| ~~1534~~ | P2 | gcp + azure simulator — terraform read-back drift (idempotency, surfaced by BUG-1532) | sim fidelity gap / drift | Batch fix of the gcp + azure `tf` drift the new second-plan assertion found. **gcp:** logging sink/metric `name` → short identifier + new `resourceName` full-path field (verified vs logging/v2 `LogSink.name` doc; the SDK test asserted the full path against a false comment — corrected); memorystore redis `connectMode`/`transitEncryptionMode` defaults; GCS bucket `location` upper-cased; api_gateway api-config `openapiDocuments` round-trip; compute router NAT `type=PUBLIC` (the `type=PUBLIC` forces-replacement was Cloud NAT, not the network); compute network `networkFirewallPolicyEnforcementOrder`; cloudfunctions2 `allTrafficOnLatestRevision=true`+`ingressSettings=ALLOW_ALL`; DNS managed zone drops an empty `privateVisibilityConfig` (provider sends `networks:[]` even for public zones → flatten materialised a phantom block). **gcp drift reproduced + fixed locally** (DNS works on Mac); compute verified blind via `tf (gcp)`. **azure (11 resources):** public IP/prefix/LB `sku_tier=Regional`+`tags={}`+`zones=[]`+`ip_tags=[]`+`idle_timeout=4`; storage `account_tier` (from sku name)+`supportsHttpsTrafficOnly=true`+`minimumTlsVersion=TLS1_2`+`primaryLocation`; application_insights `application_type=web`+retention/sampling/public-network/`tags={}`; cosmosdb `properties.locations` round-trip (`isZoneRedundant` was dropped)+write/readLocations; key_vault `softDeleteRetentionInDays` round-trip; key_vault_key `key_ops` request order; container_app scale `cooldownPeriod=300`/`pollingInterval=30`; linux_function_app `clientCertMode=Optional`+siteConfig defaults; apim api `apiRevision=1`; container_registry `networkRuleBypassOptions=AzureServices`; virtual_network `privateEndpointVNetPolicies=Disabled`; eventgrid_system_topic `tags={}`. **Second pass** (after the cosmos provider-panic fix unblocked the apply): cosmos geo_location rebuilt with `failoverPolicies` + shared id + `provisioningState` (the read/writeLocations the first pass added lacked `provisioningState` → provider create-poll nil-panic that aborted the whole apply); application_insights `WorkspaceResourceId`; container_app_environment `log_analytics_workspace_id` resolved via a new subscription-scope Log Analytics workspace LIST (matched by customerId); linux_function_app appsettings mirrored into /config/appsettings, siteConfig TLS/SCM defaults, and backup config 404s when unconfigured (POST /config/backup/list empty `{}` → provider materialised a phantom `backup{enabled=false}` block). **DONE — `tf (gcp)` + `tf (azure)` both green; full PR #491 CI passes.** Azure verified blind via CI + per-resource curl wire-shape checks (azure terraform is Docker-only). |
| ~~1533~~ | P3 | CI — test-harness image builds pull base images from Docker Hub (throttle/timeout flake) | CI flaky infra | `sim (gcp)` failed building the eval-arithmetic image: `Head https://registry-1.docker.io/.../alpine i/o timeout` — the TestMain `docker build` Dockerfiles pulled `golang:1.25-alpine`/`alpine:latest` from Docker Hub, which throttles/times out on CI runners. Fix: pull the base images from the ECR Public Gallery mirror (`public.ecr.aws/docker/library/...`) in every harness build Dockerfile (gcp+azure sdk/cli helpers + ecs/aca/lambda/azf backend integration tests). |
| ~~1532~~ | P2 | gcp + azure terraform stacks — no second-plan idempotency/drift assertion | test coverage gap | The aws stack had an `idempotency-fidelity` subdir asserting a clean `plan -detailed-exitcode`, but the gcp and azure apply stacks applied + asserted output IDs and never ran a second plan — so a resource that drifts on re-plan (the #469–#473 / #1517 class) would pass CI. Fix: add `terraform plan -detailed-exitcode` after apply in both apply tests (exit 2 = drift → `runTimed` surfaces the error). Runs in `tf (gcp)` / `tf (azure)` CI. |
| ~~1531~~ | P2 | aws scheduler simulator — cron(...) expressions stored but not evaluated | sim fidelity gap (follow-on of #486) | After #486 added `at()`/`rate()` firing, `cron(...)` schedules were still stored but never fired. Fix: `scheduler_cron.go` evaluates AWS 6-field cron (`cron(min hr dom mon dow year)`) — `*`, `?`, values, lists, ranges, steps, named months/days; dow 1–7 with 1=Sunday; `L`/`W`/`#` unsupported (won't fire rather than fire wrong). Wired into the firing loop as a recurring expression (recomputes next after each fire). Unit-tested (next-fire computation + firing-loop wiring); `rate(5 minutes)` and the rest are unaffected. |
| ~~1530~~ | P1 | aws scheduler simulator — schedules never fire their targets (issue #486) | sim fidelity gap / missing behaviour | EventBridge Scheduler stored schedules + round-tripped CRUD but never invoked the `Target` when the `ScheduleExpression` became due, so a cron/rate `→ ECS RunTask` (or Lambda/SQS/SNS) reconciler was configured-but-never-launched. Fix: `scheduler_firing.go` — a 1s evaluation loop parses `at(...)` (one-time) and `rate(N minute/hour/day)` (recurring) and invokes the target via the sim's own handlers in-process (ECS RunTask, Lambda Invoke, SQS SendMessage, SNS Publish), exactly as real Scheduler does. `cron(...)` is stored faithfully but not yet evaluated by the loop. SDK test: an `at(now+4s)` ECS target launches the task (appears in ListTasks). |
| ~~1529~~ | P1 | aws ecs simulator — task definition `secrets` not resolved/injected (issue #488) | sim fidelity gap / unresolved passthrough | A container definition's `secrets` array (`valueFrom` → SecretsManager/SSM ARN) was stored as opaque passthrough and never resolved, so the container never received the secret env var. Real ECS resolves each `valueFrom` at launch (`secretsmanager:GetSecretValue` / `ssm:GetParameter`) and injects it as the named env var. Fix: `resolveECSContainerSecrets` resolves each ref (SecretsManager whole-string or `:jsonKey` selector; SSM by name/ARN) and merges into the container env at RunTask launch. SDK test: a busybox task echoes the injected `$EDD_AGENT_SECRET` and the resolved value appears in CloudWatch logs. |
| ~~1528~~ | P3 | backend-azf — `TestAZFGitLabRunnerAttachStdin` still flaky after BUG-1505 (attach deadline < bootstrap window) | CI flaky timeout | The buffered-attach read deadline (`SOCKERLESS_AZF_ATTACH_TIMEOUT_SEC`, 60s in integration) was shorter than the function-bootstrap probe (`SOCKERLESS_AZF_BOOTSTRAP_TIMEOUT_SEC`, default 90s). The deadline clock starts at ContainerStart (before bootstrap), but the response can only publish after bootstrap + invoke — so a healthy-but-slow bootstrap (60–90s on a contended runner) made the reader EOF with empty output before the function could publish. BUG-1461/1505 tuned adjacent knobs but never reconciled the deadline with the bootstrap window. Fix: `attachDeadline()` = `azfBootstrapTimeout()` + attach budget, so the read covers bootstrap + invoke. |
| ~~1527~~ | P2 | azure container-apps simulator — listSecrets emits `{"value":null}` for a configured app/job with no secrets | sim fidelity gap / null-vs-empty-array (residual of #313) | Container Apps + Container App Jobs `listSecrets` initialised `secrets := []T{}` but then overwrote it with `Properties.Configuration.Secrets`, which is nil when the resource has a configuration block but no secrets → `{"value":null}`. `SecretsCollection.value` is a REQUIRED array in armappcontainers/v3, so real Azure returns `[]`. (#313 fixed the standard ARM **list** endpoints but missed these two action-style listSecrets handlers, whose nil path only triggers for a configured-but-secretless resource — verified by curl: no-config app → `[]`, config-no-secrets app → `null`.) Fix: only overwrite when `Configuration.Secrets != nil`. Other azure list handlers verified clean (Store.Filter/List return `make([]T,0)`, never nil). |
| ~~1526~~ | P2 | aws cloudwatch-logs simulator — DescribeLogStreams + GetLogEvents miss/misreport group existence (same locus as #483) | sim fidelity gap / missing validation | Found auditing the same locus as #483: `DescribeLogStreams` filtered streams without checking the group, returning `{"logStreams":[]}` for a non-existent group instead of `ResourceNotFoundException` (a declared error for the op per aws-sdk-go-v2 cloudwatchlogs@v1.75.1). And `GetLogEvents` checked only the stream key, so a missing **group** reported "log stream does not exist" — real AWS checks the group first ("log group does not exist"). Fix: add the group-existence guard to DescribeLogStreams; check the group before the stream in GetLogEvents. |
| ~~1525~~ | P2 | aws cloudwatch-logs simulator — FilterLogEvents returns empty instead of ResourceNotFoundException for a missing group (issue #483) | sim fidelity gap / missing validation | `FilterLogEvents` searched `cwLogStreams` without first checking the group exists, so a non-existent `logGroupName` returned `200 {"events":[]}` instead of `ResourceNotFoundException`. Confirmed against aws-sdk-go-v2 cloudwatchlogs@v1.75.1: the op declares exactly `InvalidParameterException` + `ResourceNotFoundException` (awsJson1.1, HTTP 400). Every other group-scoped handler already checked. Fix: add the same `cwLogGroups.Get` guard returning `ResourceNotFoundException` ("The specified log group does not exist: %s"). |
| ~~1524~~ | P2 | aws ecs simulator — ExecuteCommand allowed on tasks not run with enableExecuteCommand | sim fidelity gap / too permissive | Found while building the ECS ExecuteCommand running-task fixture (the last untested AWS op): the handler validated only `LastStatus == "RUNNING"`, never the task's `enableExecuteCommand`. Real ECS rejects exec on a task not started with `--enable-execute-command` (`InvalidParameterException`: "execute command was not enabled when the task was run...") since the SSM exec agent is only injected then. Fix: reject with that error when `!task.EnableExecuteCommand`. New SDK tests run a long-lived busybox task (enabled → session returned; disabled → rejected). |
| ~~1523~~ | P2 | gcp pubsub simulator — subscriptions.seek not implemented | sim fidelity gap / missing verb | Found by the coverage-audit snapshot probe (snapshots were the one untested pubsub cluster): `projects.subscriptions.seek` (seek-to-snapshot / seek-to-time, the snapshot-replay path) returned `400 Unknown verb: seek` — the subscription-verb switch only handled pull/acknowledge/modifyAckDeadline/IAM. Fix: add a `seek` case + `handlePSSeek` (validates the subscription exists, returns the empty SeekResponse the contract specifies; the sim doesn't model per-message ack cursors). |
| ~~1522~~ | P2 | gcp pubsub simulator — snapshot records subscription name as its `topic` | sim fidelity gap / wrong field | Found by the snapshot probe: `projects.snapshots.create` stored `topic = <subscription name>` instead of the subscription's source topic, so `snapshots.get` returned the wrong `topic` (a `google_pubsub_snapshot` consumer reads it back). Fix: resolve the subscription's `Topic` from the store at create time. |
| ~~1521~~ | P1 | azure apim simulator — no soft-delete/purge surface (terraform destroy purge fails) | sim fidelity gap / missing lifecycle | Found after the BUG-1520 delete-status fix let destroy reach the purge step: terraform-provider-azurerm purges APIM by default — after `DELETE service` it `GET .../locations/{loc}/deletedServices/{name}` then `DELETE` (purge). The sim had no `deletedServices` routes → `retrieving the deleted Service ... to be able to purge it: unexpected status 404 ... 404 page not found`. Fix: model APIM soft-delete — service DELETE moves the record to an `apimDeleted` store; `GET/DELETE /subscriptions/{sub}/providers/Microsoft.ApiManagement/locations/{location}/deletedServices/{name}` serve the `DeletedService` contract (200, with `properties.serviceId`) and purge it. Pre-create GET still 404s (name free) so create proceeds. |
| ~~1520~~ | P1 | azure apim simulator — service/api/product/subscription DELETE returns 202 (terraform destroy fails) | sim fidelity gap / wrong status | Found by the same APIM terraform validation (apply + assertions passed; `terraform destroy` failed): `azurerm_api_management{,_api,_product,_subscription}` delete clients got `unexpected status 202 (202 Accepted) received with no body`. The sub-resource delete clients require a synchronous **200**, and the service `DeleteThenPoll` treats a bare 202-with-no-Location as a broken LRO. The sim's four delete handlers returned `http.StatusAccepted` (202). Fix: return 200 from all four (operation/backend/namedValue already did); updated the raw-`armReq` lifecycle test's service-delete assertion 202→200. |
| ~~1519~~ | P1 | azure apim simulator — subscription `listSecrets` not implemented (blocks terraform) | sim fidelity gap / missing op (issue #210 follow-on) | Found by validating APIM through the real terraform-provider-azurerm client (the stable consumer of these routes; the raw-`armReq` test never exercised the provider's create choreography): `azurerm_api_management_subscription` create does `PUT /subscriptions/{sid}` (200) then `POST /subscriptions/{sid}/listSecrets` to read the keys — the sim had no `listSecrets` route, so it 404'd and the provider hung on the subscription until the apply deadline. Fix: implement `POST .../service/{name}/subscriptions/{sub}/listSecrets` returning the ARM `SubscriptionKeysContract` (`primaryKey`/`secondaryKey`) with deterministic per-subscription keys. (Service/api/product already round-trip; the Developer/Standard/Premium SKUs additionally gate create behind `/portalsettings/*`+`/tenant/access`, which no sockerless consumer uses — the consumer provisions Consumption, validated here.) |
| ~~1518~~ | P2 | gcp bigquery simulator — jobs.list items omit top-level `state` | sim fidelity gap | Found by the gcp test-coverage audit (bigquery had only 4 of 19 ops tested): `jobs.list` emitted full `BQJob` objects whose running state lives nested under `status.state`, but the SDK `JobListJobs` reads a **top-level `state`** field — so a client enumerating jobs sees `state == ""` on every entry. Fix: `handleBQListJobs` renders light list items carrying top-level `state` (derived from `status.state`) plus `id`/`jobReference`/`statistics`/`status`/`configuration`/`user_email`. |
| ~~1517~~ | P1 | gcp bigquery simulator — tables drop `labels` (silent) | sim fidelity gap / dropped field | Found by the gcp test-coverage audit: `BQTable` had no `Labels` field, so a table created with labels comes back from `tables.get`/`tables.list`/`tables.patch` with none — `google_bigquery_table.labels` would drift every plan (the dataset path already round-trips labels). Fix: add `Labels map[string]string` to `BQTable`, preserve it through `bqApplyTableDefaults`, merge it on `tables.patch`, and surface it in `tables.list` items. |
| ~~1516~~ | P2 | aws ecr simulator — BatchDeleteImage deletes only the addressed alias | sim fidelity gap / incomplete delete | Found via the new DescribeImages coverage: each image is stored under its digest key + one key per tag, but `BatchDeleteImage` deleted only the addressed (tag) key, so the digest alias survived and `describe-images`/`list-images` still surfaced the "deleted" image. Same alias-delete class as the OCI manifest DELETE. Fix: delete every alias (digest + all tags) for the matched image. |
| ~~1515~~ | P2 | aws ecr simulator — DescribeImages + ListImages not implemented | sim fidelity gap / missing op | Found while writing the BatchDeleteImage coverage test: `ecr describe-images` / `list-images` return `UnknownOperationException` — core read ops clients use to enumerate a repo's images aren't registered (only `BatchGetImage` by explicit id was). Fix: implement both off the existing `ecrImages` store — `ListImages` → `imageIds[]` (one per tag, deduped by digest); `DescribeImages` → `imageDetails[]` (digest, tags, pushedAt, size) honouring an optional `imageIds` filter. |
| ~~1514~~ | P1 | aws dynamodb simulator — UpdateItem ignores UpdateExpression (silent no-op) | sim fidelity gap / silent no-op | Found by the test-coverage audit: `UpdateItem` only implemented the **legacy** `AttributeUpdates` param and ignored `UpdateExpression` (`SET #a = :v`, `ADD`, `REMOVE`) — which every modern client (aws CLI / SDK / Terraform) uses. So `update-item --update-expression` returned 200 but persisted nothing; a subsequent `get-item` showed the attribute absent. Forbidden synthetic-success. Fix: real `UpdateExpression` evaluator (SET assignment + `+`/`-` arithmetic + `if_not_exists`, REMOVE, ADD numeric/string-set, DELETE set-element) resolving `#name`/`:val` placeholders, applied to the stored item; legacy `AttributeUpdates` kept. |
| ~~1513~~ | P1 | aws cloudwatch simulator — metrics unreachable from current aws CLI (awsJson1.0) | sim fidelity gap | Surfaced once BUG-1512 made `TestCloudWatchCLI_QueryMetrics` actually run in CI: it failed `UnknownOperationException`. Current botocore/aws CLI send CloudWatch metrics over **awsJson1.0** (`X-Amz-Target: GraniteServiceVersion20100801.<Op>`, `application/x-amz-json-1.0`, `x-amzn-query-mode:true`) — the same awsQuery→awsJson migration SQS went through — but the sim only served the legacy query protocol (old CLI) + rpc-v2-cbor (Go SDK). Reproduced locally with the latest botocore (1.43.24) in a venv. Fix: `cloudwatch_metrics_json.go` registers PutMetricData/GetMetricStatistics/ListMetrics on the awsJson router off the SAME `cwMetrics` store; stats emitted as decimal-bearing JSON numbers so botocore reads them as Double (`42.0`). Verified with both old + new CLI and a wire-level awsJson SDK test. |
| ~~1512~~ | P2 | CI — 16 AWS CLI tests silently never ran (no shard regex match) | CI coverage gap | Fixed: the AWS CLI test job fans out across edge/compute/appdata shards via `-run '^Test(...)'`; 16 tests matched **no** shard so never ran in CI — `TestBatch_*`, `TestCloudWatch*`, `TestCodeBuild_*`, `TestECRCLI_*` (the shard listed the stale `CLI_ECR` prefix, which only matches `TestCLI_ECR_*`), `TestGlue_*`, `TestResourceTagsCLI_*`, `TestSFN_*`. All 16 verified passing — they were simply unrun. Fix: extended the shard regexes so every CLI test matches exactly one shard (balanced across edge/compute/appdata) + added `scripts/check-cli-shard-coverage.sh` (pre-commit) that fails on any orphan/double-matched test so this can't recur. |
| ~~1511~~ | P2 | azure acr simulator — missing /oauth2/exchange + /oauth2/token | sim fidelity gap (issue #469) | ACR's registry-token auth endpoints aren't implemented (`POST /oauth2/exchange` 404s, `/oauth2/token` 400s), so ACR-shaped clients that do the Bearer challenge → exchange Entra token for refresh token → exchange for scoped Bearer can't push/pull (raw unauthenticated `/v2/` works). Fix: implement both endpoints returning ACR-shaped envelopes (deterministic local tokens) + accept them on `/v2/` + `/acr/v1/`. |
| ~~1510~~ | P1 | aws elbv2 simulator — DescribeListeners drops SslPolicy | sim fidelity gap (issue #473) | An HTTPS listener created with `SslPolicy` comes back from Create/Describe with no `SslPolicy`, so `aws_lb_listener` plans an in-place update every idempotency plan. Fix: parse `SslPolicy` at CreateListener, store on `ELBv2Listener`, render in `elbv2ListenerXML`. |
| ~~1509~~ | P1 | aws ec2 simulator — DescribeSecurityGroups egress drops Ipv6Ranges | sim fidelity gap (issue #472) | An egress rule created with both `IpRanges` + `Ipv6Ranges` comes back with `Ipv6Ranges` empty, so `aws_security_group` egress `ipv6_cidr_blocks` drifts every plan. `EC2IpPermission`/`parseIpPermission` don't model/parse IPv6 CIDRs and the SG describe render omits `ipv6Ranges`. Fix: add `Ipv6Ranges`, parse `*.Ipv6Ranges.N.CidrIpv6`, render `ipv6RangeSet`. |
| ~~1508~~ | P1 | aws ec2 simulator — DescribeRouteTables route drops NetworkInterfaceId | sim fidelity gap (issue #471) | A route created with `NetworkInterfaceId` comes back from `DescribeRouteTables` without it, so `aws_route` drifts every plan. `EC2Route` has no `NetworkInterfaceId`; `CreateRoute` doesn't parse it; the route render omits `networkInterfaceId`. Fix: add the field, parse at CreateRoute, render. |
| ~~1507~~ | P1 | aws ec2 simulator — instance launch-template provenance not recorded | sim fidelity gap (issue #470) | Fixed: an instance launched via `RunInstances` with a `LaunchTemplate` spec left `aws_instance.launch_template` (ForceNew) absent on refresh → destroy+create every plan. **Key finding:** the AWS SDK `Instance` shape has NO launch-template field — DescribeInstances doesn't carry it; the provider flattens `launch_template` from the `aws:ec2launchtemplate:id` / `aws:ec2launchtemplate:version` **system tags** AWS auto-applies. Fix: `runInstancesLaunchTemplate` resolves the template (inheriting ImageId/InstanceType when the request doesn't override) and RunInstances stamps those two system tags on the instance (returned in the existing tagSet). Verified idempotent via `terraform plan -detailed-exitcode`. |
| ~~1506~~ | P1 | aws ecs simulator — DescribeTaskDefinition --include TAGS returns no tags | sim fidelity gap (issue #467) | After #466/#462, `ListTagsForResource` round-trips task-def tags (verified), but the TF provider reads them via `DescribeTaskDefinition --include TAGS` → **response top-level `tags`**, which the handler never emitted (it ignored `include` and the sim leaked tags inside `taskDefinition.tags`, a field the SDK model doesn't have so it's silently dropped). Result: `aws_ecs_task_definition.tags`/`tags_all` re-add every plan (cascades to `aws_iam_role_policy.scheduler`). Fix: `ECSTaskDefinition.Tags`→`json:"-"` (internal only; real AWS taskDefinition has no tags field), and emit top-level `tags` from RegisterTaskDefinition (always) + DescribeTaskDefinition (when `include` has TAGS). |
| ~~1505~~ | P3 | backend-azf integration — `test (azure backends)` 5-min timeout (flaky) | CI flaky timeout | Fixed: `TestAZFGitLabRunnerAttachStdin` panicked `test timed out after 5m0s`. Root cause (traced via the panic goroutine dump): the buffered-attach reader (`attachStream.Read`) blocked on `respReady`, which only closes when the invoke goroutine publishes — and that POST has a 600s (`SOCKERLESS_AZF_TIMEOUT`) client timeout. On a contended CI runner the FaaS pod hits its lifetime cap and the instant workload doesn't complete promptly, so the reader stranded minutes; the test's own `5*time.Minute` guards equalled the global `-timeout 5m`, so the binary panicked instead of one test failing. Fix: bound `attachStream.Read` with a deadline (`SOCKERLESS_AZF_ATTACH_TIMEOUT_SEC`, default = invoke Timeout so a healthy invoke always publishes first — pure safety net) → returns EOF if the invoke doesn't publish in time; the integration backend runs with a 60s bound so a stall fails fast under the global timeout. Verified with deterministic unit tests (`attach_stream_test.go`): Read returns EOF at the deadline with no publish, and a normal publish before the deadline is delivered intact. |
| ~~1504~~ | P2 | aws/gcp/azure OCI simulator — `/v2/` responses omit `Docker-Distribution-Api-Version` on non-base routes | sim fidelity gap (issue #465) | Reported as "ECR manifest HEAD returns 400 for a missing tag" via go-containerregistry. Could NOT reproduce the 400 on current main (commit 1859adf): `HEAD /v2/<repo>/manifests/<missing>` returns 404 `MANIFEST_UNKNOWN` and the full g-c-r push succeeds across 1/2/3-segment repos. Real defect found + fixed: only the base `GET /v2/` set `Docker-Distribution-Api-Version: registry/2.0`; manifest/blob/error responses omitted it, where real Docker Distribution/ECR send it on every `/v2/` response (a strict client or proxy can reject the bare 404). Fix: emit the header on every OCI response in the shared `serve()` (all 3 cloud copies) + a regression test locking missing-manifest HEAD→404. Header hardening, not a proven repro of the consumer's 400 — g-c-r itself doesn't key on it for manifest HEAD. |
| ~~1503~~ | P2 | aws elbv2 simulator — DescribeListeners omits Certificates for HTTPS listeners | sim fidelity gap (issue #464) | Fixed: an HTTPS (443) listener created with an ACM `certificate_arn` came back from `DescribeListeners` with no `Certificates` array (the read-cert-off-the-listener pattern got `None`). `CreateListener` parsed `Certificates` via `queryList` (flat `Certificates.member.N`), but the cert arrives as the nested `Certificates.member.N.CertificateArn` structure. Fix: parse via the existing `parseELBv2Certificates` (reads `.CertificateArn`); the render already emits the array. |
| ~~1502~~ | P1 | aws simulator — tags not returned by ListTags* for CW Logs / DynamoDB / ECR | sim fidelity gap (issue #462) | Fixed: tags set at create (`--tags`) were dropped — `logs:ListTagsForResource`, `dynamodb:ListTagsOfResource`, `ecr:ListTagsForResource` returned empty, so every plan re-added them. (ECS `ListTagsForResource` already round-tripped task-def tags — verified, no change.) Fix: parse create-time `Tags` on CreateLogGroup/CreateTable/CreateRepository + store; replace the Logs/ECR stub list handlers with real ARN→resource lookups (and real TagResource merges). |
| ~~1501~~ | P2 | aws elbv2 simulator — DescribeCapacityReservation emits MinimumLoadBalancerCapacity.capacity_units=0 | sim fidelity gap (issue #461) | Fixed: an ALB with no minimum-capacity config returned `MinimumLoadBalancerCapacity.CapacityUnits=0` from `DescribeCapacityReservation` (issue mis-titled `DescribeLoadBalancerAttributes`; the sim never emitted it there). Real AWS omits the field unless configured, so the provider planned `capacity_units = 0 -> null` every plan. The sim has no `ModifyCapacityReservation` setter, so a minimum can never be configured: drop the `MinimumLoadBalancerCapacity` element entirely. |
| ~~1500~~ | P1 | aws ecs simulator — RegisterTaskDefinition/DescribeTaskDefinition drop healthCheck + secrets | sim fidelity gap (issue #460) | Fixed: `ECSContainerDefinition` was a typed struct without `healthCheck`/`secrets`, so those fields were silently dropped on round-trip; the provider saw a containerDefinitions diff (ForceNew) → a new task-def revision every plan, cascading to the ECS service + IAM policies. Fix: add `HealthCheck`/`Secrets json.RawMessage` to `ECSContainerDefinition` (opaque byte-for-byte passthrough — no field enumeration). |
| ~~1499~~ | P1 | aws ec2 simulator — NatGateway ConnectivityType not persisted | sim fidelity gap (issue #459) | Fixed: `CreateNatGateway --connectivity-type public` was accepted but `DescribeNatGateways` omitted `ConnectivityType`; `connectivity_type` is ForceNew in the provider, so a missing value vs config `"public"` planned destroy+create every plan (cascades to private route `nat_gateway_id`). Fix: add `ConnectivityType` to `EC2NatGateway`, parse at create (default `public`), render in Create + Describe XML (legacy rows default to `public`). |
| ~~1498~~ | P2 | aws ec2 simulator — referenced_security_group_id returns account-prefixed id | sim fidelity gap (issue #458) | Fixed: the API returned the bare `sg-…` + a separate `userId` (=owner account), but terraform-provider-aws prefixes `userId/sg-…` whenever `ReferencedGroupInfo.UserId != meta.AccountID` — and the fck-nat provider runs with `skip_requesting_account_id` (empty account), so it always prefixed, drifting `"123456789012/sg-…" -> "sg-…"` every plan. Fix: omit `<userId>` from `referencedGroupInfo` for same-account references (the sim is single-account), matching real AWS's same-account shape so the provider keeps the bare id under any account config. |
| ~~1497~~ | P2 | aws ec2 simulator — SG rule from_port/to_port=0 for ip_protocol=-1 | sim fidelity gap (issue #457) | Fixed: an all-traffic rule (`ip_protocol=-1`, no ports) came back from `DescribeSecurityGroupRules` with `FromPort=0,ToPort=0`; real AWS omits both for `-1`, so the provider planned `from_port = 0 -> null` / `to_port = 0 -> null` every plan. `sgrItemXML` always emitted `<fromPort>`/`<toPort>`. Fix: omit both when `IpProtocol == "-1"`. |
| ~~1496~~ | P1 | aws ec2 simulator — ModifySecurityGroupRules unimplemented | sim fidelity gap (issue #455) | Fixed: `ModifySecurityGroupRules` returned `InvalidAction`; terraform-provider-aws v6 calls it for in-place updates to `aws_vpc_security_group_{ingress,egress}_rule` (e.g. fck-nat's tasks-SG egress rule refreshed when toggling enable_dns), so the update failed. Fix: a handler that updates each `SecurityGroupRule.N` (by `SecurityGroupRuleId`) in the `ec2SecurityGroupRules` store — Description/IpProtocol/FromPort/ToPort/CidrIpv4/ReferencedGroupId — and returns `<return>true</return>`; NotFound for an unknown rule id. |
| ~~1495~~ | P2 | aws ecs simulator — DescribeServices drops deploymentConfiguration | sim fidelity gap (issue #454) | Fixed: `CreateService` accepted `deploymentConfiguration` (deploymentCircuitBreaker / maximumPercent / minimumHealthyPercent) but `ECSService` had no field, so `DescribeServices` returned null — the deployment-safety control couldn't be verified post-apply. Fix: add `DeploymentConfiguration json.RawMessage` to `ECSService` (same pattern as networkConfiguration/loadBalancers), parse + store it at create, echo on describe. |
| ~~1494~~ | P2 | aws dynamodb simulator — DescribeTable drops SSEDescription | sim fidelity gap (issue #453) | Fixed: `CreateTable --sse-specification Enabled=true,SSEType=KMS,KMSMasterKeyId=…` was accepted but `DescribeTable` returned null for `SSEDescription`, so `aws_dynamodb_table server_side_encryption` couldn't be verified (and risks a perpetual diff). Fix: add `SSEDescription{Status,SSEType,KMSMasterKeyArn}` to `DDBTable`, parse `SSESpecification` at CreateTable (Enabled → Status=ENABLED, SSEType default KMS, KMSMasterKeyArn=KMSMasterKeyId), echo on Create + Describe. |
| ~~1493~~ | P1 | azure acr simulator — OCI blob-upload start 404s (+ no chunked PATCH) | sim fidelity gap (issue #452) | Fixed (shared OCI library): `POST /v2/{repo}/blobs/uploads/` 404'd for multi-segment repos and chunked PATCH was unrouted, so `docker push` through the shim failed at upload start. Replaced ACR's per-method `/v2/{path...}` handlers (which split init/chunk across verbs and mis-parsed multi-segment repos) with the shared `sim.OCIRegistry`; the `/acr/v1/_catalog` + `_tags` data-plane APIs now read `reg.Manifests`. |
| ~~1492~~ | P1 | gcp artifact-registry simulator — OCI chunk upload PATCH 405s | sim fidelity gap (issue #451) | Fixed (shared OCI library): `handleOCIBlobUpload` implemented POST (init) + PUT (monolithic) but not PATCH, so the shim's chunked push (`POST → PATCH → PUT`) failed at the chunk. Replaced AR's inline `/v2/` handler + `handleOCIManifest`/`handleOCIBlob`/`handleOCIBlobUpload` with the shared `sim.OCIRegistry` (which does chunked PATCH); kept AR's pull-through docker-hub hydration via the `HydrateManifest` hook and DockerImage registration via `OnManifestPut`. |
| ~~1491~~ | P1 | aws ecr simulator — no Docker Registry /v2/ data plane | sim fidelity gap (issue #450) | Fixed (shared OCI library): ECR served only the awsJson control plane; `GET /v2/` → 404, so no through-shim `docker push`/`pull` was possible. Added a shared, spec-conformant OCI Distribution data plane (`shared/oci.go`: base route, blob upload start/**chunked PATCH**/finalize with sha256 digest verification, blob GET/HEAD, manifest PUT/GET/HEAD/DELETE stored by tag+digest, tags/list) mounted per-method on the `/v2/` subtree (avoids the awsJson `POST /` and apigatewayv2 `/v2/apis` mux conflicts via GET-implies-HEAD). Wired into ECR with an `OnManifestPut` hook that registers an ECR image row. Same library now backs GCP AR (#451) and Azure ACR (#452); the duplicated per-cloud OCI handlers/types were retired. |
| ~~1490~~ | P2 | aws iam simulator — ListRoles + role/policy tag reads unimplemented | sim fidelity gap (issue #447) | Fixed: `ListRoles` returned InvalidAction (blocks `aws iam list-roles` audit enumeration + `data.aws_iam_roles`), and `ListRoleTags`/`ListPolicyTags` were absent; roles/policies didn't even store tags. Fix: `ListRoles` paginates `iamRoles` (path-prefix filter); added Tags to IAMRole/IAMPolicy parsed from `Tags.member.N` at CreateRole/CreatePolicy and rendered in the role/policy XML; `ListRoleTags`/`ListPolicyTags` return the stored tags. (`ListPolicies` already existed.) |
| ~~1489~~ | P2 | aws ecs simulator — DescribeClusters drops settings + configuration | sim fidelity gap (issue #446) | Fixed: `CreateCluster` ignored `settings` (containerInsights) and `configuration` (executeCommandConfiguration KMS key); `DescribeClusters --include SETTINGS CONFIGURATIONS` returned null for both — CI can't verify container-insights or exec-session audit encryption. Fix: store `Settings`/`Configuration` on `ECSCluster` at create; `DescribeClusters` honours `include` (SETTINGS/CONFIGURATIONS) and returns them. |
| ~~1488~~ | P2 | aws cloudwatch-logs simulator — CreateLogGroup drops kmsKeyId | sim fidelity gap (issue #445) | Fixed: `CreateLogGroup --kms-key-id` succeeded but `DescribeLogGroups` returned null for `kmsKeyId` (the struct had no field, the request wasn't parsed), so `aws_cloudwatch_log_group kms_key_id` silently dropped — masking an encryption-at-rest compliance control. Fix: add `KmsKeyId` to `CWLogGroup`, parse it at create, echo it on describe; register `AssociateKmsKey`/`DisassociateKmsKey` for the post-create path. |
| ~~1487~~ | P1 | aws ec2 simulator — DescribeSecurityGroups ignores vpc-id filter | sim fidelity gap (issue #443) | Fixed: `DescribeSecurityGroups` had no filter handling — only `GroupId.1` else return ALL SGs, so `--filters Name=vpc-id,Values=X` returned SGs from every VPC (cross-VPC contamination across sequential test runs). Fix: route through `ec2Filters()` (vpc-id/group-name/group-id/tag filters) + multi-id `GroupId.N`/`GroupName.N`, mirroring the BUG-1470 pattern. |
| ~~1486~~ | P1 | aws ec2 simulator — DescribeVpcs filtering broken + CidrBlockAssociationSet null | sim fidelity gap (issue #442) | Fixed: `DescribeVpcs` only honoured `VpcId.1` (multi-id + the `Name=vpc-id`/tag filters ignored → wrong VPC, often the default `vpc-sim`), and never rendered `cidrBlockAssociationSet` (always null) — so `data.aws_vpc` reads the wrong VPC and fck-nat's `cidr_block_associations[*].cidr_block` ingress rule gets an empty CIDR. Fix: multi-id `VpcId.N` + `ec2Filters()` (vpc-id/cidr/tag); render `cidrBlockAssociationSet` (associationId + cidrBlock + associated state) from the primary CIDR. |
| ~~1485~~ | P1 | aws iam simulator — ListPolicyVersions unimplemented | sim fidelity gap (issue #441) | Fixed: `ListPolicyVersions` returned InvalidAction; the Terraform AWS provider calls it on `aws_iam_policy` plan/refresh/**destroy**, so `terraform destroy` failed and left the policy stuck in state (fck-nat NAT-instance policy). Fix: return the policy's versions — the default model has one (`v1`, IsDefaultVersion=true, CreateDate) — looked up by `PolicyArn`, NotFound if absent. |
| ~~1484~~ | P2 | aws cloudwatch simulator — metrics have no CLI (query-protocol) surface | sim fidelity gap (#432 follow-up) | Fixed: CloudWatch metrics were only served over rpc-v2-cbor (the Go SDK path), so `aws cloudwatch put-metric-data` / `get-metric-statistics` / `list-metrics` (botocore query protocol) returned InvalidAction. Fix: `cloudwatch_metrics_query.go` registers PutMetricData / GetMetricStatistics / ListMetrics on the query router, backed by the SAME `cwMetrics` store and `cwApplyStat` period-bucketing, so data round-trips through either protocol. XML at `monitoring.amazonaws.com/doc/2010-08-01`. |
| ~~1483~~ | P2 | aws ecr simulator — aws_ecr_repository read-back incomplete | sim fidelity gap (#435 follow-up) | Fixed: CreateRepository/DescribeRepositories returned only arn/name/uri/registryId/createdAt — not imageTagMutability, encryptionConfiguration, or imageScanningConfiguration — so the `aws_ecr_repository` Terraform resource (deferred from #435 for this reason) would drift on those computed/optional fields. Fix: extend `ECRRepository` with the three config blocks; CreateRepository parses + defaults them (MUTABLE / AES256 / scanOnPush=false) and DescribeRepositories echoes them; add `aws_ecr_repository` to the production-shape Terraform stack. |
| ~~1482~~ | P2 | aws ec2 simulator — launch-template in-place update unimplemented | sim fidelity gap (#433 follow-up) | Fixed: `CreateLaunchTemplate`/`Describe`/`Delete` covered apply+destroy, but an in-place change to `aws_launch_template` makes the provider call `CreateLaunchTemplateVersion` + `ModifyLaunchTemplate` (both InvalidAction). Fix: `CreateLaunchTemplateVersion` appends a new version (becomes latest, not default) parsing the full `RequestLaunchTemplateData`; `ModifyLaunchTemplate` moves the default version (numeric / `$Latest` / `$Default`, VersionNotFound on a bad selector). Refactored `ltVersionXML` into a shared inner renderer for the `<launchTemplateVersion>` wrapper. |
| ~~1481~~ | P1 | aws elbv2 simulator — listener rules + ModifyListener unimplemented | sim fidelity gap (issue #438) | Fixed: `CreateRule`/`DescribeRules`/`ModifyRule`/`DeleteRule`/`ModifyListener` returned `InvalidAction`, so listeners were read-only after creation — the `aws_alb_listener_rule` resource (host-header/path-pattern routing, the IAP-proxy ALB shape) couldn't apply, and Terraform couldn't update a listener's default action/certs. Fix: a rule store keyed by listener ARN; `CreateRule`/`ModifyRule`/`DeleteRule`/`DescribeRules` CRUD over conditions (host-header/path-pattern/http-request-method/source-ip/http-header/query-string — parsed from both the CLI legacy `Values` and the Terraform typed `*Config`, rendered back as both) and actions (forward/fixed-response/redirect, with typed config round-trip); `ModifyListener` updates the listener's default actions + certs. Rendered at exact `elasticloadbalancingv2@v1.55.3` locationNames. |
| ~~1480~~ | P1 | aws ec2 simulator — DescribeInstanceTypeOfferings unimplemented | sim fidelity gap (issue #437) | Fixed: `DescribeInstanceTypeOfferings` returned `InvalidAction`; the fck-nat module calls it to validate the chosen instance type is offered in the target AZs before provisioning, so the apply failed even after #433. Fix: a handler mirroring `handleDescribeInstanceTypes` — for each requested instance-type × AZ it emits an `InstanceTypeOffering` (`instanceType`/`location`/`locationType`), honouring the `instance-type` and `location` filters and `LocationType` (availability-zone/region). |
| ~~1479~~ | P2 | aws ecs simulator — DescribeCapacityProviders + ListTaskDefinitionFamilies unimplemented | sim fidelity gap (issue #436) | Fixed: both returned `UnknownOperationException`. `DescribeCapacityProviders` is the read-back for `aws_ecs_cluster_capacity_providers` — without it `terraform plan` after apply shows spurious capacity-provider drift; `ListTaskDefinitionFamilies` is the family aggregation companion to `ListTaskDefinitions`. Fix: `DescribeCapacityProviders` returns the built-in `FARGATE`/`FARGATE_SPOT` (always ACTIVE) plus any cluster-referenced custom providers, honouring the `capacityProviders` name filter; `ListTaskDefinitionFamilies` dedups families over `ecsTaskDefinitions` with `familyPrefix`/`status` filters + pagination. |
| ~~1478~~ | P1 | aws ecr simulator — repository policy + image-layer push/pull unimplemented | sim fidelity gap (issue #435) | Fixed: `SetRepositoryPolicy`/`GetRepositoryPolicy` + the layer data plane (`InitiateLayerUpload`/`UploadLayerPart`/`CompleteLayerUpload`/`GetDownloadUrlForLayer`) returned `UnknownOperationException`, so `aws_ecr_repository_policy` Terraform applies failed and no real `docker push`/`pull` layer transfer could be tested against the sim. Fix: a repo-policy store (repo name → policy JSON) backing Set/Get/Delete; a real stateful layer pipeline — `InitiateLayerUpload` returns an `uploadId`, `UploadLayerPart` appends bytes to the session buffer, `CompleteLayerUpload` verifies `sha256(buffer)==layerDigest` (DIGEST_INVALID on mismatch) and stores the blob, `GetDownloadUrlForLayer` serves it; `BatchCheckLayerAvailability` now reports real availability from the layer store instead of unconditional AVAILABLE. |
| ~~1477~~ | P1 | aws kms simulator — grants + secondary crypto ops unimplemented | sim fidelity gap (issue #434) | Fixed: `CreateGrant`/`ListGrants`/`RevokeGrant` and `GenerateDataKeyWithoutPlaintext`/`ReEncrypt` returned `UnknownOperationException`. AWS services create grants on customer CMKs for encryption-at-rest (the `aws_kms_grant` resource); the two crypto ops back SSE-KMS data-key generation and key-rotation re-encryption. Fix: a per-key grant store with `GrantId`/`GranteePrincipal`/`Operations`/`Constraints`/`Name`, CreateGrant→GrantId+GrantToken, ListGrants (KeyId filter + pagination), RevokeGrant (by GrantId); `GenerateDataKeyWithoutPlaintext` returns the encrypted data key only (no plaintext echo), `ReEncrypt` decrypts under the source key envelope and re-wraps under the destination key. |
| ~~1476~~ | P1 | aws ec2 simulator — Launch Template ops unimplemented | sim fidelity gap (issue #433) | Fixed: All EC2 Launch Template operations returned `InvalidAction` — `CreateLaunchTemplate`, `DescribeLaunchTemplates`, `DescribeLaunchTemplateVersions`, `DeleteLaunchTemplate`. Blocks the fck-nat NAT-instance Terraform path (`nat_mode = "instance"` uses `aws_launch_template` as the ASG launch config); `terraform apply` fails at the launch-template resource. The next onion layer after the #430 ENI fix. Fix: a launch-template store keyed by `lt-…` ID holding auto-incrementing versions (default = `$Default`); `CreateLaunchTemplate` parses + persists the full `RequestLaunchTemplateData` (ImageId, InstanceType, KeyName, UserData, EbsOptimized, IamInstanceProfile, NetworkInterfaces+groupSet, SecurityGroupIds, BlockDeviceMappings+Ebs, TagSpecifications, MetadataOptions, Monitoring, Placement) and template tags; `DescribeLaunchTemplates` honours `LaunchTemplateId`/`LaunchTemplateName` filters; `DescribeLaunchTemplateVersions` returns stored versions (with `$Latest`/`$Default`/numeric selectors) rendering `launchTemplateData` back at exact SDK locationNames (verified against `ec2@v1.305.2` deserializers); `DeleteLaunchTemplate` removes + echoes the template. |
| ~~1475~~ | P2 | aws cloudwatch simulator — fabricated ECS metrics + no statistic aggregation | sim fidelity gap (fidelity audit) | Fixed: `GetMetricData` special-cased `ECS/ContainerInsights` and returned `computeECSMetric` — invented values (`CpuUtilized = 0.15×CPU`, `MemoryUtilized = 0.25×mem`) with no relation to anything, which a target-tracking autoscaler would compute against. It also ignored the requested `Period`/`Stat`, dumping raw datapoints. Fix: serve REAL metrics — drop the fabrication so ECS reads stored `PutMetricData` like every other namespace (empty if nothing was pushed — the API-only sim can't measure container CPU), and do real CloudWatch period-bucketing + statistic (Average/Sum/Minimum/Maximum/SampleCount) over `[StartTime, EndTime)`. |
| ~~1474~~ | P1 | aws iam simulator — policy simulation unimplemented | sim fidelity gap (issue #427) | Fixed: `SimulateCustomPolicy`/`SimulatePrincipalPolicy` returned `InvalidAction`; the IAM sim stored policy documents as opaque strings with no evaluation engine, so least-privilege IAM couldn't be asserted below the real-AWS tier. Fix: a real policy evaluator (parse IAM JSON with string-or-list Action/Resource; Effect with explicit-deny-wins; `*`/`?` wildcard Action (case-insensitive) + Resource ARN matching; NotAction/NotResource; Condition operators StringEquals/NotEquals/Like/NotLike/EqualsIgnoreCase, Bool, ArnLike/ArnEquals, `…IfExists`, with MissingContextValues), returning `EvaluationResults[].EvalDecision` (allowed/explicitDeny/implicitDeny). `SimulateCustomPolicy` evaluates `PolicyInputList`; `SimulatePrincipalPolicy` resolves the role's inline + attached managed policies from the store then reuses the same evaluator. |
| ~~1473~~ | P1 | aws ec2 simulator — standalone ENI ops unimplemented | sim fidelity gap (issue #428) | Fixed: ENIs were only materialized as a side-effect of `RunInstances`; the standalone lifecycle ops returned `InvalidAction`: `CreateNetworkInterface`, `AttachNetworkInterface`, `DetachNetworkInterface`, `DeleteNetworkInterface`, `ModifyNetworkInterfaceAttribute`, `AssignPrivateIpAddresses`. Blocks the fck-nat NAT-instance Terraform path (`aws_network_interface` + attachment + `source_dest_check=false`). Fix: register the ops over the existing `ec2NetworkInterfaces` store (control-plane modeling, like `CreateNatGateway`) — create→available, attach/detach toggle status+attachment, modify sets SourceDestCheck/description/groups, assign adds secondary IPs. |
| ~~1468~~ | P1 | gcp firestore simulator — Patch/Commit ignore updateMask | sim fidelity gap (fidelity audit) | Fixed: `handleFSPatchDocument` (`current.Fields = req.Fields`) and `handleFSCommit` (`fsPutDocument(*wr.Update)`) replace the whole document, ignoring `updateMask`/`update.updateMask`. The Firestore SDK `DocumentRef.Update(...)` and `Set(..., MergeAll)` send masked writes carrying only changed fields → the sim silently drops every unmentioned field (data loss). Fix: merge only masked field paths into the existing doc. |
| ~~1469~~ | P2 | gcp secret-manager simulator — :access ignores version State | sim fidelity gap (fidelity audit) | Fixed: `accessSecretPayloadResolved` serves any version's payload regardless of `State`, and `latest` picks the highest-numbered version without skipping DISABLED/DESTROYED. Real GCP: accessing a DISABLED version → `FAILED_PRECONDITION` (400); `latest` resolves to the latest ENABLED version. Fix: check state on explicit access; skip non-ENABLED for `latest`. |
| ~~1470~~ | P2 | aws ec2 simulator — Describe{NatGateways,Subnets,RouteTables} only honor Filter.1 | sim fidelity gap (fidelity audit) | Fixed: These check `r.FormValue("Filter.1.Name")=="vpc-id"` literally; a `vpc-id` filter sent as `Filter.2` (or combined with another filter) is ignored and the handler returns ALL resources. `handleDescribeInstances` uses the general `ec2Filters()` matcher correctly. Fix: route these through `ec2Filters()` so any filter position/combination works. |
| ~~1471~~ | P2 | azure App Insights simulator — billing-features PUT ignores body | sim fidelity gap (fidelity audit) | Fixed: `PUT .../currentbillingfeatures` never reads `r.Body`; both GET and PUT return a static closure-captured map (`Cap:100`, `CurrentBillingFeatures:["Basic"]`). Real Azure echoes the submitted `DataVolumeCap.Cap`/`CurrentBillingFeatures` and persists them, so `azurerm_application_insights daily_data_cap_in_gb = 200` drifts perpetually. (Distinct from the BUG-1467 casing false positive — the shape is PascalCase and correct; the bug is static-ignores-input.) Fix: parse + persist per-component billing state, echo on PUT, serve on GET. The existing `TestAppInsights_BillingFeatures` masks it (asserts NotEmpty, not the echoed value) — tighten it. |
| ~~1472~~ | P2 | azure acr simulator — blob PUT doesn't verify content digest | sim fidelity gap (fidelity audit) | Fixed: The monolithic blob-upload completion (`PUT /v2/{...}?digest=`) stores `BlobData{Digest: digest, Data: data}` using the client-supplied `digest` without computing `sha256(data)` and comparing. Real OCI/ACR returns `400 DIGEST_INVALID` on mismatch (the manifest PUT a few lines up computes the digest correctly). Fix: compute and compare; reject mismatches. |
| 1467 | P2 | azure App Insights simulator — billing-features JSON casing | FALSE POSITIVE (audit — Azure coverage-gap PR) | Suspected the `currentbillingfeatures` response used wrong (PascalCase) JSON casing. It does NOT: the App Insights billing-features API is one of the legacy Azure APIs that genuinely uses **PascalCase** (`CurrentBillingFeatures`/`DataVolumeCap`/`Cap`/…) — confirmed against `armapplicationinsights@v1.2.0` `models_serde.go` (`populate(objectMap, "CurrentBillingFeatures", …)`), and `insights.go` already documents this for the component properties too. A camelCase "fix" broke the SDK round-trip; reverted. The new SDK test (`TestAppInsights_BillingFeatures`) locks the correct PascalCase shape and guards against that regression. |
| ~~1466~~ | P2 | azure-keyvault simulator — version-less key crypto returns 405 | sim fidelity gap (issue #423) | Fixed: `handleKVKey` only routed crypto to `handleKVCryptoKey` for the 4-segment `/keys/{name}/{version}/{verb}` form; the version-less 3-segment `/keys/{name}/{verb}` (`encrypt`/`decrypt`/`sign`/`verify`/`wrapkey`/`unwrapkey`) fell through to 405. Real Key Vault (and `azkeys.Encrypt(ctx, name, "", …)`) accepts the no-version form and applies the key's current version. Fix: route the 3-segment crypto verbs to `handleKVCryptoKey(..., version="" , verb)`; `findVersion("")` already resolves to `latest()` (same as the version-less GET). |
| ~~1464~~ | P1 | aws-acm simulator — DNS cert never reaches ISSUED | sim fidelity gap (issue #420) | Fixed: `RequestCertificate` (DNS, `AMAZON_ISSUED`) stores `Status=PENDING_VALIDATION` and nothing ever transitions it; `DescribeCertificate` returns it verbatim (only `ImportCertificate` sets ISSUED). Terraform's `aws_acm_certificate_validation` polls `DescribeCertificate` until `ISSUED` and hangs to its 45-min timeout. Fix: on `DescribeCertificate`, reconcile a pending AMAZON_ISSUED DNS cert to ISSUED once every domain's `_acm-challenge` CNAME exists in the Route53 sim store (honest signal — record presence is "validation done"; the sim can't do real public DNS validation), setting `IssuedAt`/`NotBefore`/`NotAfter` and each DVO `ValidationStatus=SUCCESS`. |
| ~~1465~~ | P1 | aws-acm simulator — wildcard SAN validation record keeps literal `*` | sim fidelity gap (issue #421) | Fixed: For a wildcard domain/SAN (`*.devbox.example.com`), the DVO `ResourceRecord.Name` was `_acm-challenge.` + domain verbatim → `_acm-challenge.*.devbox.example.com.` (literal `*`). Real ACM strips `*.` and validates the base (`_acm-challenge.devbox.example.com.`). The star-bearing record name makes `aws_acm_certificate_validation`'s consistency check fail (`missing ... DNS validation record`). Fix: build the record name from `strings.TrimPrefix(domain, "*.")`; the DVO `DomainName` still echoes the original wildcard. |
| ~~1463~~ | P1 | gcp simulator — Cloud KMS service missing | sim fidelity gap (issue #419) | Fixed: `simulators/gcp/` had no `cloudkms.googleapis.com` service — keyRings/cryptoKeys/cryptoKeyVersions/encrypt/decrypt absent, so a consumer exercising the GCP Cloud KMS REST API has no endpoint (AWS KMS + Azure KV keys are simulated; GCP is the odd one out). Fix shape: `cloudkms.go` implementing keyRings (create with `keyRingId`, get, list; honest 404/409), cryptoKeys (create with `cryptoKeyId`+`purpose=ENCRYPT_DECRYPT`, get, list, patch rotation; 404 on missing ring), cryptoKeyVersions (list, get, destroy → `DESTROY_SCHEDULED`+`destroyTime`), and `cryptoKeys:encrypt`/`:decrypt` with real AES-256-GCM per-version key material and the CRC32C integrity fields `gcloud kms` verifies (`ciphertextCrc32c`, `verifiedPlaintextCrc32c`, `plaintextCrc32c`). SDK + CLI (+ terraform) coverage. |
| ~~1462~~ | P1 | aws-cloudfront simulator — Function tagging unimplemented | sim fidelity gap (#418 flaky `tf (aws)`) | Fixed: `handleCFListTags`/`handleCFTagResource`/`handleCFUntagResource` only resolved **distribution** ARNs (`cfDistributionIDFromARN` → `cfDistributions`); a **function** ARN (`arn:aws:cloudfront::<acct>:function/<name>/LIVE`) missed and returned `404 NoSuchResource`. `aws_cloudfront_function.tf_fn` (publish=true) read tags via `ListTagsForResource` on the LIVE ARN — the AWS provider tolerates the 404 on some read paths but not others, so `TestStackProductionShape` failed intermittently (passed runs 26984053264/26985224721, failed 26987472514 on identical sim code). Real AWS returns the function's (possibly empty) tag set. Added a `Tags []CFTag` field to `cfStoredFunction`, a `cfFunctionNameFromARN` resolver, and function-ARN branches to all three tag handlers (list returns the stored tags, tag/untag persist on the function). |
| ~~1461~~ | P1 | azf backend — attach-stdin invoke race + unbounded timeout | flaky CI hang (#418 `test (azure backends)`) | Fixed: `TestAZFGitLabRunnerAttachStdin` flaked in CI (`Post .../api/function: EOF` then `panic: test timed out after 5m0s`; same code passed an earlier run). The buffered-attach invoke goroutine in `ContainerStart` POSTed the `/bin/sh` exec envelope to the Function App HTTP trigger immediately, racing the in-container bootstrap binding its port; with the 600s invoke-client timeout, an early/failed POST stranded the attached reader (which blocks until `publishAZFAttachResponse`) past go-test's 5-min limit → opaque panic. Added `waitAZFFunctionListening`: a bounded (90s azf bootstrap timeout) TCP-readiness probe that waits for the listener before POSTing and publishes a clear `function app not ready` error on timeout instead of stranding the reader. The buffered HTTP-invoke path deliberately does **not** depend on the reverse-agent — host→container is reachable even where the container cannot dial back (`ws://host.docker.internal/...` is unreachable under local Podman, so every reverse-agent path is CI-only) — so the attach path stays locally validatable (passes in ~21s). A test reorder would have masked this and wrongly imposed the local-Docker start-then-write contract on the buffered cloud-exec model (where write-before-start is correct); not done. |
| 1075 | P2 | live-cloud validation | unvalidated real cloud | Lambda is the only backend with a green live-cloud cell. Cloud Run Services, ACA Apps, AZF cloud-DNS, Lambda service-mesh, and ACA/AZF Azure AD remain unvalidated against authenticated real clouds. Do not mark these green without real cloud runs. |
| 1104 | P0 | simulator audit cadence | meta | Keep re-checking SDK/CLI/Terraform surface claims during simulator phases. This remains open while meaningful simulator work continues; stale "not applicable" rows are treated as real bugs when public clients exist. |
| 1345 | P2 | azuread terraform provider | upstream blocker | The `hashicorp/terraform-provider-azuread` provider has no supported way to redirect Microsoft Graph API calls to a custom endpoint (no `microsoft_graph_endpoint` override). Feature request open upstream: https://github.com/hashicorp/terraform-provider-azuread/issues/1837. Entra provisioning via Terraform (`azuread_group`, `azuread_user`, `azuread_group_member`) cannot be tested against the sim until this is resolved upstream. |
| ~~1457~~ | P1 | aws-dynamodb simulator — UpdateTable missing | audit (ECS/DDB sim sweep) | Fixed: `UpdateTable` was unregistered, so terraform's post-create GSI lifecycle (add/modify/delete via `GlobalSecondaryIndexUpdates`), throughput, billing-mode, and deletion-protection changes all 404'd. Added `handleDDBUpdateTable` (GSI create/update/delete with merged AttributeDefinitions; ProvisionedThroughput; BillingMode; DeletionProtectionEnabled). |
| ~~1458~~ | P2 | aws-dynamodb simulator — batch/transact ops missing | audit | Fixed: `BatchWriteItem`/`BatchGetItem`/`TransactWriteItems`/`TransactGetItems` were unregistered (UnknownOperation), blocking batch/transactional app workloads. Added all four; TransactWriteItems evaluates every ConditionExpression under the item lock before applying any mutation (atomic) and cancels with `TransactionCanceledException` on failure. |
| ~~1459~~ | P2 | aws-dynamodb simulator — expression + response gaps | audit | Fixed: `ConditionExpression` only recognized `attribute_not_exists`; replaced the substring hack with a real evaluator (`attribute_exists`/`attribute_not_exists`, `begins_with`, comparison operators, AND) shared by Put/Delete/Transact. Added `ScannedCount` to Query/Scan, `ReturnValues=ALL_OLD` to PutItem, and `IndexName` validation (unknown index → ValidationException). Verified GSI queries already worked via the generic matcher (audit false-positive). Pruned dead `containsCI`/`indexOfFold`. |
| ~~1460~~ | P2 | aws-ecs simulator — tags + pagination + list ops | audit | Fixed: `TagResource`/`UntagResource`/`ListTagsForResource` errored on cluster and service ARNs (so `aws_ecs_cluster`/`aws_ecs_service` with `tags` failed) — now supported (cluster gains a `tags` field; CreateCluster parses tags). `ListServices` ignored `maxResults`/`nextToken` — now paginated. Registered `ListClusters` and `ListTaskDefinitions` (data-source lookups). |
| ~~1455~~ | P1 | aws-dynamodb simulator — GSIs dropped | sim fidelity gap (issue #416) | Fixed: `DDBTable` had no `GlobalSecondaryIndexes` field, `CreateTable` never parsed GSIs from the request, and `DescribeTable` returned `GlobalSecondaryIndexes: null` — so terraform-provider-aws waited for each GSI's `IndexStatus` to reach ACTIVE forever (~21 retries then error). Added `DDBGlobalSecondaryIndex`/`DDBLocalSecondaryIndex`/`DDBProjection`, parse GSIs/LSIs in CreateTable, and echo them (GSIs with `IndexStatus: ACTIVE` + `IndexArn`) in both the CreateTable response and DescribeTable. |
| ~~1456~~ | P1 | aws-ecs simulator — Service family + capacity providers missing | unimplemented ops (issue #417) | Fixed: `CreateService`/`DescribeServices`/`ListServices`/`UpdateService`/`DeleteService` and `PutClusterCapacityProviders` were unregistered (`UnknownOperationException`), so `aws_ecs_service` (the common Fargate-service case) and `aws_ecs_cluster_capacity_providers` could not apply. Added `ecs_service.go` modeling the ECS Service as a control-plane object that reaches ACTIVE with `runningCount == desiredCount` and a COMPLETED PRIMARY deployment; `PutClusterCapacityProviders` stores the providers + default strategy on the cluster and `DescribeClusters` echoes them. Real task placement stays reserved for the container runtimes. |
| ~~1452~~ | P1 | aws-kms simulator — tagging broken | wrong tag wire shape + missing ops (issue #413) | Fixed: `CreateKey` decoded tags into the Secrets Manager `[]SMTag` shape (`Key`/`Value`), but KMS uses `TagKey`/`TagValue`, so tags round-tripped empty; `TagResource`/`UntagResource` were unregistered (`UnknownOperationException`). The provider polls `ListResourceTags` until tags propagate, so a tagged `aws_kms_key` hung 10m. Added a KMS-specific `KMSTag` struct, fixed `CreateKey` decode + `ListResourceTags` emit, and registered `TagResource`/`UntagResource`. |
| ~~1453~~ | P1 | aws ec2 simulator — host-cap hard gates | API-only modeling gap (issue #414) | Fixed: `CreateNatGateway`, `CreateRoute` (NAT route), `RunInstances`, `StartInstances`, `AttachVolume`/`DetachVolume`/`ModifyVolume`, and Auto Scaling instance launch hard-gated on host CAP_NET_ADMIN/nft/KVM and returned retryable 503s, so Terraform hung until timeout in `SIM_RUNTIME=process`. Modeled all of these at the control plane (matching the existing `CreateVpc` pattern) — describable, reaching `available`/`running` — and reserved real fabric/VM/EBS execution for hosts that actually have the capabilities. `DescribeInstances` no longer reconciles modeled "running" to "stopped" on an API-only host. Removed the now-dead `ec2RequireNetworkHost`/`ec2RequireVMHost`. NAT gateway data plane proven by a new `realexec_host` test. |
| ~~1454~~ | P1 | sim container runtime — Podman image resolution | local container-exec gap | Fixed: `createAndStartContainer` (aws/azure/gcp `shared`) passed the image short name to `ContainerCreate`. Podman's docker-compat API resolves a short name on inspect/pull but not on create, so a locally-built `name:tag` inspected fine yet create reported `no such image` — Lambda/ACA/Cloud Run/GCF invoke failed on Podman (macOS), though Docker (CI) resolved it. Now resolves the image to its unambiguous ID before create (no-op on Docker). |
| ~~1451~~ | P1 | azure keyvault sim — version list ordering | sim fidelity gap (issue #407) | Fixed: `handleKVListSecretVersions` (and the key/cert equivalents) sorted versions by the random version UUID, which bears no relation to creation order — so a client reconstructing version order from the list got the wrong version whenever two versions shared a second-precision `Created` timestamp. Replaced the UUID sort with a stable sort by `Created`; since versions are stored append-ordered, the stable sort preserves creation order for same-second ties, returning oldest-first like real Azure. No synthetic tags. |
| ~~1396~~ | P1 | bleephub webhooks | schema gap | Fixed: `hookToJSON` now includes `url`, `test_url`, `ping_url`, `deliveries_url`, `last_response`. API URLs derived from `r.Host`. |
| ~~1397~~ | P1 | bleephub webhooks | schema gap | Fixed: `deliveryToJSON` now includes `status`, `url` (target), `installation_id`, `repository_id`; `deliveryFullJSON` no longer double-sets the fields. |
| ~~1398~~ | P1 | bleephub webhooks | schema gap | Fixed: `WebhookDelivery.TargetURL` field added; set in `doDeliverAttempt` from `hook.URL`. |
| ~~1399~~ | P1 | aws-glue simulator | wrong X-Amz-Target prefix | Fixed: registered `AmazonWebServiceGlue.<Op>` but SDK sends `AWSGlue.<Op>`; changed all registrations to `AWSGlue.*`. |
| ~~1400~~ | P1 | aws-batch simulator | wrong HTTP routing | Fixed: implemented as REST paths (`POST /v1/computeenvironments`) but SDK sends operation-specific POST paths (`POST /v1/createcomputeenvironment`); rewrote all route registrations. |
| ~~1401~~ | P2 | aws-codebuild SDK test | TagResource not in SDK client | Fixed: `codebuild.Client` in SDK v2 does not expose `TagResource`/`ListTagsForResource`/`UntagResource` as separate methods; SDK test now verifies tags via `BatchGetProjects` instead. |
| ~~1402~~ | P1 | aws-batch terraform | wrong HCL attribute name | Fixed: `aws_batch_compute_environment` uses `name =` not `compute_environment_name =` in hashicorp/aws provider v6. CI failed with "An argument named 'compute_environment_name' is not expected here." |
| ~~1403~~ | P1 | aws-stepfunctions sim | missing ValidateStateMachineDefinition | Fixed: TF provider calls `AWSStepFunctions.ValidateStateMachineDefinition` before CreateStateMachine; sim returned 400 UnknownOperationException. Added handler that always returns `{"result":"OK"}`. |
| ~~1404~~ | P1 | aws-stepfunctions sim | missing ListStateMachineVersions | Fixed: TF provider calls `AWSStepFunctions.ListStateMachineVersions` after CreateStateMachine; added handler returning empty versions list. |
| ~~1405~~ | P1 | aws-codebuild terraform | buildspec required for NO_SOURCE | Fixed: TF provider v6 requires `buildspec` in source block when `type = "NO_SOURCE"`; added placeholder buildspec to `aws_codebuild_project` TF resource. |
| ~~1406~~ | P1 | aws-glue terraform | empty catalog_id in resource ID | Fixed: `aws_glue_catalog_database` ID format is `catalog-id:database-name`; when `catalog_id` is not explicit the provider stored empty string. Added `catalog_id = data.aws_caller_identity.current.account_id` to TF resource. |
| ~~1407~~ | P1 | aws-batch sim | ARN vs name in describe operations | Fixed: TF provider passes ARNs to DescribeJobQueues/DescribeComputeEnvironments/Update/Delete after creation; sim was looking up by name only. All handlers now resolve name via `batchNameFromARN()`. |
| ~~1408~~ | P1 | aws-codebuild sim | BatchGetProjects receives ARN not name | Fixed: TF provider calls `BatchGetProjects` with project ARN after creation; handler only did direct name lookup. Fixed: resolve via `cbNameFromARN()` when direct lookup misses. |
| ~~1409~~ | P1 | aws-glue sim | GetTags/TagResource only handle job ARNs | Fixed: TF provider calls `AWSGlue.GetTags` with database ARN (`arn:...:database/name`); handler looked in `glueJobs` only. Fixed: `glueResourceFromARN()` detects resource type; `handleGlueGetTags`, `handleGlueTagResource`, `handleGlueUntagResource` dispatch to database or job by type. `GlueDatabase` gains `Tags map[string]string`. |
| ~~1410~~ | P1 | aws-glue terraform | empty catalog_id in glue table resource ID | Fixed: `aws_glue_catalog_table` ID format is `catalog-id:database-name:table-name`; without explicit `catalog_id` the provider stored empty string. Added `catalog_id = data.aws_caller_identity.current.account_id` to TF resource (same fix as BUG-1406 for database). |
| ~~1411~~ | P1 | aws-glue sim | missing GetPartitionIndexes handler | Fixed: TF provider calls `AWSGlue.GetPartitionIndexes` on catalog table refresh; sim returned 400 UnknownOperationException. Added handler returning empty `PartitionIndexDescriptorList`. Added to tests-exempt.txt (TF-only, no SDK/CLI test shape). |
| ~~1414~~ | P1 | bleephub git persistence | volatile in-memory git storage | Fixed: `GitStorages` used `*memory.Storage`; git repos were lost on every restart. Replaced with `gitStorer.Storer` interface + `filesystem.Storage` backend when `BLEEPHUB_GIT_DIR` is set, so repos survive restarts and can be backed by Docker volumes. |
| ~~1415~~ | P1 | bleephub SQLite persistence | orgs/teams/issues/PRs/releases/webhooks not persisted | Fixed: SQLite write-through only covered user/token/app/installation. Extended to orgs, teams, memberships, issues, labels, milestones, comments, pull requests, releases, webhooks + all load-from-disk at startup. |
| ~~1416~~ | P2 | bleephub UI | no repo detail page | Fixed: Repos list rows are not clickable; no route for repo detail. Added `RepoDetailPage` with code browser (tree/file view), README renderer, branch picker, and commit history. |
| ~~1417~~ | P2 | bleephub UI | no issues UI | Fixed: GitHub Issues REST + GraphQL fully implemented but no UI. Added `IssuesPage` (list + create) and `IssueDetailPage` (detail + comments + reactions). |
| ~~1418~~ | P2 | bleephub UI | no pull requests UI | Fixed: GitHub PRs REST + GraphQL fully implemented but no UI. Added `PullsPage` (list) and `PullDetailPage` (detail + reviews + merge). |
| ~~1419~~ | P2 | bleephub Playwright | nav-link test misses Apps and OAuth | Fixed: `bleephub.spec.ts` "sidebar has all nav links" checked only 5 of 7 nav items. Extended to check all 7 (Overview, Workflows, Runners, Repos, Apps, OAuth, Metrics). |
| ~~1420~~ | P2 | bleephub Playwright | no screenshot capture | Fixed: Playwright test suite had no `page.screenshot()` calls. Added screenshots at every page and state, saved to `temp/screenshots/`. |
| ~~1421~~ | P1 | bleephub git HTTP | `listWorkflowFiles` takes concrete `*memory.Storage` | Fixed: `listWorkflowFiles(stor *memory.Storage)` leaks the concrete storage type; breaks when `GetGitStorage` returns filesystem storage. Changed parameter to `gitStorer.Storer` interface. |
| ~~1422~~ | P2 | bleephub Playwright | no coverage for AppsPage or OAuthPage | Fixed: Added Playwright tests for AppsPage (list apps, open create-app modal) and OAuthPage (device flow, web flow fields). |
| ~~1423~~ | P1 | bleephub git persistence | loaded repos have no git storage after restart | Fixed: `loadFromPersistence` for the "repos" bucket populated `st.Repos` + `st.ReposByName` but never called `openOrInitGitStorage`, leaving `st.GitStorages` empty for all loaded repos. `GetGitStorage` returned nil for every repo after restart. Added storage re-open in the repos loader. |
| ~~1424~~ | P1 | bleephub oauth | `r.ParseForm()` errors silenced | Fixed: `gh_oauth.go` used `_ = r.ParseForm()` in 3 login/token handlers; parse failures were silently swallowed and subsequent form field reads returned empty strings, causing auth to always fail with misleading "incorrect password" instead of surfacing a real error. |
| ~~1425~~ | P2 | bleephub Go duplication | 52 identical JSON-decode boilerplate blocks | Fixed: extracted `decodeJSONBody(w, r, &req) bool` helper in `gh_rest.go`; all 52 handlers now call it instead of repeating `json.NewDecoder(r.Body).Decode`+error check+response. |
| ~~1426~~ | P3 | bleephub Go dead code | `_ = slug` dead assignment in `store.go` | Fixed: removed the immediately-discarded assignment. |
| ~~1427~~ | P3 | bleephub Go style | `fmt.Sprintf("%d", id)` scattered 34 times | Fixed: replaced all 34 instances with `strconv.Itoa(id)` which is idiomatic and avoids a format string parse. |
| ~~1428~~ | P2 | bleephub TypeScript | dynamic type imports in `api.ts` | Fixed: `api.ts` used `import("./types.js").GithubIssue` inline instead of a static import. Added the six missing types to the top-level import group. |
| ~~1429~~ | P2 | bleephub TypeScript | dynamic type imports in `RepoDetailPage.tsx` | Fixed: three prop types used `import("../types.js").T` inline. Replaced with static `import type { ... }` at the top. |
| ~~1430~~ | P2 | bleephub TypeScript | `any[]` column definitions in all 6 DataTable pages | Fixed: replaced `// eslint-disable ... any[]\nconst columns: any[]` with properly typed `ColumnDef<T>[]` in ReposPage, RunnersPage, WorkflowDetailPage, OverviewPage, AppsPage (3×), WorkflowsPage (2×). |
| ~~1431~~ | P2 | bleephub UI duplication | `CommentCard` copy-pasted in PullsPage | Fixed: `PullsPage.tsx` reimplemented comment rendering inline instead of importing the `CommentCard` component from IssuesPage. Extracted `CommentCard` to `src/components/CommentCard.tsx`, imported in both pages. |
| ~~1432~~ | P2 | bleephub UI duplication | hover style handlers duplicated 6× across pages | Fixed: extracted `rowHoverProps` constant in `src/components/RowHover.ts`; all 6 call sites now spread it instead of repeating the `onMouseEnter`/`onMouseLeave` inline. |
| ~~1433~~ | P2 | bleephub UI | useQuery calls have no error display | Fixed: IssuesPage, PullsPage, RepoDetailPage all showed a blank/empty list on query failure with no user feedback. Added `isError` + error message rendering for all data fetches. |
| ~~1434~~ | P2 | bleephub TypeScript | missing `GithubPR` type import in PullsPage | Fixed: `PullsPage.tsx` used PR data without importing the type; parameter typed implicitly as `any`. Added `import type { GithubPR, GithubComment }` at the top. |
| ~~1435~~ | P2 | bleephub TypeScript | `(e.currentTarget as HTMLElement)` casts on every hover | Fixed: the HTMLElement cast was required because React's event type isn't narrowed. The extracted `rowHoverProps` in RowHover.ts is typed as `{ onMouseEnter: React.MouseEventHandler<HTMLAnchorElement>; onMouseLeave: React.MouseEventHandler<HTMLAnchorElement> }` so no cast is needed at call sites. |
| ~~1436~~ | P2 | bleephub Go dead code | `Server.Store()` unreachable — `deadcode` confirmed | Fixed: accessor method exported for tests but no test or caller ever calls it (tests access `s.store` directly). Removed. |
| ~~1437~~ | P3 | bleephub TypeScript dead export | `GithubTreeEntry` type never imported — `knip` confirmed | Fixed: defined in `types.ts` but never consumed anywhere. Removed. |
| ~~1438~~ | P2 | bleephub TypeScript dead export | `createInstallation` exported from `api.ts` but never imported — `knip` confirmed | Fixed: function removed; no UI consumes it and the bleephub management endpoint `/api/v3/bleephub/apps/{id}/installations` is already covered by Go-level tests. |
| ~~1439~~ | P2 | bleephub Go duplication | `handleGetOrgInstallation` / `handleGetUserInstallation` are verbatim clones — `dupl` confirmed | Fixed: extracted `findInstallationByTarget(s, targetLogin, targetType string)` helper; both handlers call it. |
| ~~1440~~ | P2 | bleephub Go duplication | `PRReviewCommentStore.Update` and `PRReviewCommentStore.Delete` share identical nil-check + lock boilerplate — `dupl` confirmed | Fixed: the nil-check pattern is idiomatic and correct; marked with a comment and consolidated the read-pattern. |
| ~~1441~~ | P2 | bleephub UI duplication | OAuthPage DeviceCodesCard / AuthCodesCard are 53-line copies of each other — `jscpd` confirmed | Fixed: extracted `OAuthCodesTable` component parameterized on title, empty message, and row renderer; both card sections use it. |
| ~~1442~~ | P2 | bleephub UI duplication | IssuesPage and PullsPage have near-identical state-toggle + mutation-pending + empty-state structure — `jscpd` confirmed | Fixed: extracted `StateToggle` component and shared `EmptyListPlaceholder` component used by both pages. |
| ~~1443~~ | P2 | bleephub UI duplication | OverviewPage and MetricsPage share identical `useQuery` loading skeleton — `jscpd` confirmed | Fixed: extracted `useMetricsData()` hook returning `{ metrics, status, isLoading, isError }`; both pages use it. |
| ~~1444~~ | P2 | bleephub tooling | No automated dead-code check in pre-commit or CI | Fixed: added `deadcode` hook in `.pre-commit-config.yaml` (runs on Go file changes in bleephub) and a `bleephub-quality` CI job running `deadcode`, `dupl`, and `knip`. |
| ~~1445~~ | P2 | bleephub tooling | No copy-paste detection in pre-commit or CI | Fixed: added `dupl -t 60` for Go and `jscpd --min-tokens 50` for TypeScript to the new `bleephub-quality` CI job. |
| ~~1446~~ | P3 | bleephub knip config | `knip` flagged `dist/` build artifacts as unused files | Fixed: scoped knip's `project` universe to `src/**` so build output (`dist/`) is never a candidate for "unused file" — no `dist/**` ignore that would dangle (and emit an unused-ignore config hint the CI check treated as failure) in a clean checkout with no build output. |
| ~~1447~~ | P1 | aws-kms simulator | unimplemented op (issue #411) | Fixed: `EnableKeyRotation`/`DisableKeyRotation` returned `UnknownOperationException` and `GetKeyRotationStatus` was hardcoded `false`, so `aws_kms_key { enable_key_rotation = true }` (the common case) could not apply. Added `EnableKeyRotation` (with `RotationPeriodInDays` default 365, 90–2560 validation) + `DisableKeyRotation`; `GetKeyRotationStatus` now returns real per-key state. |
| ~~1448~~ | P1 | aws application-autoscaling simulator | entire service missing (issue #411) | Fixed: the `AnyScaleFrontendService` API (distinct from EC2 Auto Scaling) was absent, so `aws_appautoscaling_target` returned `UnknownOperationException`, blocking ECS-service autoscaling. Added `application_autoscaling.go` with RegisterScalableTarget, DeregisterScalableTarget, DescribeScalableTargets, PutScalingPolicy, DeleteScalingPolicy, DescribeScalingPolicies, and Tag/Untag/ListTagsForResource. |
| ~~1449~~ | P1 | aws eventbridge-scheduler simulator | service unrouted, bare 404 (issue #411) | Fixed: EventBridge Scheduler (`scheduler.amazonaws.com`, REST-JSON, distinct from EventBridge rules/buses) was unrouted; `aws_scheduler_schedule` got a bare 404 the SDK could not decode. Added `scheduler.go` with CreateSchedule/GetSchedule/UpdateSchedule/DeleteSchedule/ListSchedules + schedule-group CRUD, path-addressed with the implicit `default` group. |
| ~~1450~~ | P1 | terraform-tests harness flakiness | orphaned subprocess leak | Fixed: `terraformCmd`/`runTimed` ran terraform via `CombinedOutput()` with no process group and no per-command timeout. When `go test -timeout` fired (SIGQUIT, which skips `t.Cleanup`), terraform and its provider-plugin grandchildren were orphaned (reparented to init) and kept spinning, starving later runs into cascading timeouts (observed directly: 3 orphaned `terraform-provider-aws` processes; the AWS stack test passed once they were killed). Fixed across aws/azure/gcp `terraform-tests` and `terraform/modules/ecs/test`: `Setpgid` puts terraform in its own process group and a deadline watchdog kills the whole group ~20s before the test deadline, returning a clean error instead of leaking orphans. Also brought aws/azure CLI `runCLI` to parity with gcp's existing per-command kill-timer. |

## Recently Closed

This phase closed BUG-1338 through BUG-1344 and BUG-1346/1347 (coverage audit):

- BUG-1338: `gcp-compute.md` missing instance template routes. Added 5 rows: POST/GET(single)/GET(list)/DELETE `/compute/v1/projects/{p}/global/instanceTemplates` and `GET .../aggregated/instanceTemplates`.
- BUG-1339: `gcp-iam.md` missing SA key routes. Added 4 rows: POST/GET(single)/GET(list)/DELETE `/v1/projects/{p}/serviceAccounts/{email}/keys`.
- BUG-1340: No `azure-entra.md` surface table. Created with Graph group/user provisioning, membership CRUD, `memberOf` delegated read, ROPC grant, and sim-seed back-compat endpoints.
- BUG-1341: No `azure-private-dns.md` surface table. Created with zone CRUD, A-record CRUD, generic record types (AAAA/CNAME/MX/PTR/SRV/TXT), virtual network links.
- BUG-1342: `azure-acr.md` missing `DELETE /v2/{path...}`, `GET /acr/v1/_catalog`, `GET /acr/v1/{path...}`, `PATCH /v2/{path...}`.
- BUG-1343: `azure-monitor.md` missing App Insights component routes (`PUT/GET/DELETE .../Microsoft.Insights/components/{name}`).
- BUG-1344: Coverage matrix `gcp-compute` row missing `client_surface_audit_test.go`; no `azure-entra` row; no `azure-private-dns` row. All fixed.
- BUG-1346: Same as BUG-1344 (gcp-compute evidence). Closed together.
- BUG-1347: Subsumed by BUG-1339 fix. Closed together.

Earlier phase closed BUG-1335 through BUG-1337:

- BUG-1335: bleephub `POST /user/orgs` non-standard surface. Added `POST /api/v3/admin/organizations` — the real GHES endpoint for org provisioning where the admin user is specified explicitly in the body (`login`, `admin`, `profile_name`).
- BUG-1336: Azure/Entra group and user provisioning was only possible via `PUT /sim/v1/entra/users/{oid}`. Added standard Microsoft Graph provisioning: `POST/GET/DELETE /v1.0/groups`, `POST/GET/DELETE /v1.0/users`, `POST/GET/DELETE /v1.0/groups/{id}/members[/{userId}/$ref]`. `handleGraphMemberOf` updated to return groups from both the standard membership store and the legacy inline sim-seed path.
- BUG-1337: No ROPC grant in the Azure/Entra sim. Added `handleAzureROPC`: looks up user by `userPrincipalName`, mints `access_token` + `id_token` carrying that user's identity and group memberships; 400 for unknown user. `mintAzureSimJWTForUser` / `mintAzureSimIDTokenForUser` added as explicit-user variants. OIDC discovery updated to advertise `"password"` in `grant_types_supported`.

Earlier phase closed BUG-1327 through BUG-1334:

- BUG-1327: SA key routes added to `iam.go` — `POST/GET/LIST/DELETE /v1/projects/{p}/serviceAccounts/{email}/keys`; gcloud's project=`-` wildcard resolved by extracting project from email; `gcpMakeSAKeyJSON` generates a real RSA-2048 PKCS8 key returned as base64-encoded JSON credential file (privateKeyData only on create, absent on get/list per real GCP spec).
- BUG-1328: Compute instance template routes added to `compute.go` — `POST/GET/LIST/DELETE /compute/v1/projects/{p}/global/instanceTemplates` plus `GET .../aggregated/instanceTemplates` (used by `gcloud compute instance-templates list`); DELETE uses `operationType: "delete"` so gcloud does not re-fetch the deleted resource.
- BUG-1329: Cloud Build trigger CRUD SDK tests added to `build_test.go` via `google.golang.org/api/cloudbuild/v1` — Create/Get/List/Patch/Delete.
- BUG-1330: Cloud Logging sink CRUD SDK tests added to `logging_test.go` via `google.golang.org/api/logging/v2` REST client — Create/Get/List/Update/Delete; assertions use full resource name `projects/{p}/sinks/{id}` matching real GCP wire shape.
- BUG-1331: Cloud Logging metric CRUD SDK tests added to `logging_test.go` — Create/Get/List/Update/Delete; same full resource name convention.
- BUG-1332: Project IAM SDK tests added to `iam_test.go` via `google.golang.org/api/cloudresourcemanager/v1` — GetIamPolicy (empty) and SetIamPolicy/GetIamPolicy round-trip.
- BUG-1333: CLI tests added to `client_surface_audit_test.go` for logging sinks (`gcloud logging sinks`), logging metrics (`gcloud logging metrics`), project IAM (`gcloud projects get-iam-policy` + `add-iam-policy-binding`), SA keys (`gcloud iam service-accounts keys`), and compute instance templates (`gcloud compute instance-templates`); added `CLOUDSDK_API_ENDPOINT_OVERRIDES_CLOUDRESOURCEMANAGER` to `gcloudCLI` env.
- BUG-1334: `google_project_iam_member "tf_sa_viewer"` added to GCP Terraform test stack — binds the existing `google_service_account.tf_sa` to `roles/viewer`.

Earlier phase closed BUG-1324 through BUG-1326 (issue #387):

- BUG-1324: `mintAzureSimIDToken` emitted fixed static claims with no `groups` field. Fixed by reading the active seeded user from `entraUsersStore` and including `groups: [id, ...]` in the id_token claims when the user has seeded groups.
- BUG-1325: No Microsoft Graph `GET /v1.0/me/memberOf` or `transitiveMemberOf` endpoint in the Azure sim. Fixed by adding both routes in `entra.go`; they parse the bearer token's `oid` claim and return the seeded user's groups as OData `directoryObjects`.
- BUG-1326: No user/group seeding endpoint; `oid`/`sub`/`preferred_username`/`name` were hardcoded constants. Fixed by adding `PUT /sim/v1/entra/users/{oid}` (seed/replace), `GET /sim/v1/entra/users/{oid}` (read), and `DELETE /sim/v1/entra/users/{oid}` (reset to defaults); the last PUT also sets the active oid so future auth flows mint tokens with the seeded identity.

Earlier phase closed BUG-1316 through BUG-1323:

- BUG-1316: `armapplicationinsights.ComponentsClient` SDK tests were missing. Added `TestAppInsights_ComponentCRUD` and `TestAppInsights_InstrumentationKeyStableOnUpsert` in `sdk-tests/insights_test.go`. Also fixed `insights.go` to preserve the instrumentation key across upserts (was generating a new UUID each time), and corrected JSON field casing to PascalCase (`InstrumentationKey`, `ConnectionString`) which App Insights ARM API requires.
- BUG-1317: No `az monitor app-insights component` CLI coverage. Added `TestAppInsights_CreateAndShow` and `TestAppInsights_InstrumentationKeyInConnectionString` in `cli-tests/monitor_test.go`.
- BUG-1318: `armprivatedns.RecordSetsClient` A-record SDK tests were missing. Added `TestPrivateDNS_RecordSetsCRUD` (Create/Get/List/Delete) and `TestPrivateDNS_RecordSetsUpdate` in `sdk-tests/dns_private_test.go`.
- BUG-1319: No Private DNS record-set list/delete CLI coverage. Added `TestPrivateDNS_RecordSetListAndDelete` in `cli-tests/dns_test.go`.
- BUG-1320: `azcontainerregistry` data-plane SDK tests were missing. Added `TestACR_ImageManifestPushGetDelete` in `sdk-tests/acr_test.go` covering `UploadManifest` → `GetManifest` → `NewListRepositoriesPager` → `NewListTagsPager` → `DeleteManifest` full round-trip.
- BUG-1321: ACR simulator missing `DELETE /v2/{name}/manifests/{reference}`. Added `DELETE /v2/{path...}` handler in `acr.go`; deletes the direct manifest entry and all digest aliases in the same store pass.
- BUG-1322: ACR simulator missing `/acr/v1/_catalog` and `/acr/v1/{name}/_tags`. Added both routes in `acr.go`. Extended `OCIManifest` with `Repo` and `Ref` fields so the manifest store can enumerate distinct repositories and non-digest tags without a separate index.
- BUG-1323: No CLI coverage for ACR image catalog/tag operations. Added `TestACR_ImageCatalogAndTags` in `cli-tests/acr_test.go`.

This phase closed BUG-1315:

- BUG-1315: `GET /api/v3/user/teams` returned 404 because the route was never registered in `registerGHTeamRoutes`. The store had `AddTeamMember`/`team.MemberIDs` but no reverse query. Added `ListTeamsByUser(userID int) []*Team` and `GetOrgByID(id int) *Org` to `store_orgs.go`, wired `handleListAuthUserTeams` in `gh_teams_rest.go`, registered the route, and added `TestListUserTeams` covering the full org-create → team-create → add-member → GET /user/teams flow with embedded `organization` assertion.

This phase closed BUG-1310:

- BUG-1310: The BUG-1104 AWS EC2/EBS, Auto Scaling, and CloudTrail coverage audit found stale canonical coverage docs rather than a missing implementation. Auto Scaling and CloudTrail were implemented and exercised through real SDK, AWS CLI, and Terraform provider flows, but they had no `specs/SIM_SURFACE_TABLES/` entries and therefore no rows in `specs/SIM_TEST_COVERAGE_MATRIX.md`. The EC2 surface table also omitted the already implemented EBS volume/snapshot operations. The surface tables and matrix now list `aws-autoscaling`, `aws-cloudtrail`, and the EC2 EBS operations with evidence pointing at `simulators/aws/sdk-tests/autoscaling_cloudtrail_test.go`, `simulators/aws/cli-tests/autoscaling_cloudtrail_test.go`, `simulators/aws/sdk-tests/ec2_test.go`, `simulators/aws/cli-tests/ec2_test.go`, and the Terraform root stack.

Previous phase closed BUG-1309 / issue #378:

- BUG-1309 / issue #378: EC2 `AttachVolume` was metadata-only for Firecracker-backed instances, so guest writes to attached EBS volumes could not persist into snapshots or restored volumes. The AWS simulator now prepares sparse file-backed EBS block images under each volume host path, pre-registers Firecracker EBS drive slots for EC2 guests, patches the running microVM drive backing file on `AttachVolume`, patches back to an empty slot on `DetachVolume`, and refreshes the drive after `ModifyVolume` resize. `CreateSnapshot` and `CreateVolume(SnapshotId)` continue to copy the real EBS host path, now including the raw block image written by the guest. The mandatory Firecracker smoke now performs a real guest block-device write, snapshots/restores the backing image, and reads the payload back inside the guest.

Previous phase closed BUG-1267:

- BUG-1267 / issues #332 and #338: The cross-cloud VM/networking real-execution umbrella was audited after the VPC/NAT, firewall/security, managed load-balancer, VM lifecycle, and guest metadata phases had merged. The audit found no remaining metadata-only #332 subfamily: AWS/GCP/Azure VPC/network/NIC/public-IP/NAT paths use the realexec netns/bridge/veth/IPAM/SNAT substrate; AWS security groups, GCP firewalls, and Azure NSGs compile to nftables on the packet path; AWS ELBv2, GCP load balancing, and Azure Load Balancer use real health probes and proxying; AWS EC2, GCP Compute Engine, and Azure VMs boot Firecracker guests; and guest metadata routes through provider-shaped link-local addresses/hostnames to instance-specific metadata handlers. Issue #338's LocalStack comparison was no longer accurate where it described VM/networking as metadata-only, so it was closed as a stale meta/reference tracker rather than kept open as a deliverable.

Previous phase closed BUG-1299 and BUG-1300 through BUG-1308:

- BUG-1299 / issue #371: Firecracker-backed AWS EC2, GCP Compute Engine, and Azure VM guests could boot on provider-private subnet IPs but could not reach the provider metadata service from inside the guest. The realexec substrate now programs nftables DNAT for `169.254.169.254:80` inside each cloud network namespace to the simulator's existing metadata handlers, preserving the guest private source IP so handlers can resolve instance-specific metadata. AWS EC2, GCP Compute Engine, and Azure VM startup all install that metadata route before boot. GCP guest rootfs setup writes the real `metadata.google.internal` / `metadata` aliases to `169.254.169.254`. Metadata handlers return instance-specific EC2/GCE/Azure fields when the request source IP matches a running Firecracker guest, while direct SDK-route tests still receive defaults. Regression coverage exercises real namespace HTTP DNAT and the mandatory Firecracker guest smoke now reads `http://169.254.169.254/metadata-probe` from inside the microVM.
- BUG-1300: Moving VM-backed simulator SDK/CLI/Terraform CI jobs to KVM-capable x86_64 hosted runners exposed stale simulator workload execution paths that still forced Docker platform `linux/arm64`. GCP Cloud Functions/Cloud Run Jobs, AWS ECS, and Azure Functions now inspect the resolved local workload image platform before container execution. AWS Lambda translates the public Lambda `Architectures` field (`x86_64` / `arm64`) to Docker's platform strings. Simulator tests still build native Linux workload images for the runner platform, so x64 KVM CI does not require emulation and arm hosts still run arm64 images.
- BUG-1301: Firecracker VM rootfs image creation used `os.Truncate` as if it created `rootfs.ext4`, but Go's truncate call fails when the file does not already exist. Deterministic old workdirs could mask the bug when a previous image file was present; unique launch workdirs exposed it reliably as `truncate .../rootfs.ext4: no such file or directory`. The default workdir then had to be shortened because long cloud resource IDs could exceed Firecracker's Unix socket path budget. The official Firecracker CI S3 listing also includes `vmlinux-*.config` sidecars, and the old sorter could select a config file instead of the ELF kernel. Rootfs image creation now opens the image with create/truncate flags before sizing it, default Firecracker launches allocate a unique hash-prefixed workdir under the common temp root, kernel asset selection rejects config/debug sidecars, and cached/downloaded kernels are validated as ELF images before the launcher records or reuses them. Explicit caller-provided workdirs remain explicit and isolated by the caller.
- BUG-1302: Simulator VM boot used the Firecracker guest MAC on the host-side TAP endpoint and relied on distro network config/kernel `ip=` handling for guest addressing. The direct arithmetic Firecracker smoke passed because it configured the guest NIC after boot, while VM-backed simulator CI timed out waiting for provider-private guest reachability. TAP host endpoints no longer reuse the guest MAC, and Firecracker rootfs setup installs a deterministic boot-time network configurator for the assigned cloud-private IP, gateway, and resolver.
- BUG-1303 / issue #374: Firecracker VM rootfs images were fixed-size 3 GiB sparse files, which risked runner disk exhaustion when a single simulator job booted multiple VMs. Rootfs image sizing now uses `du -sk` on the copied real rootfs, doubles measured payload size, adds bounded headroom, aligns the result, and keeps a 1 GiB floor.
- BUG-1304 / issue #375: Firecracker kernel/rootfs assets were downloaded and unsquashed inside each fresh Actions run while VM-backed jobs used a floating hosted runner label. The workflow now pins VM-backed Firecracker jobs to `ubuntu-24.04` x86_64 and caches `~/.cache/sockerless/firecracker-ci` by Firecracker version plus runner architecture.
- BUG-1305 / issue #376: The official Firecracker CI rootfs preconfigures MAC-derived guest networking through `fcnet-setup.sh`, which conflicted with AWS/GCP/Azure simulator VM paths that assign provider-private `10.x` cloud IPs. Firecracker rootfs preparation now masks/removes the stock `fcnet` systemd/SysV/script/interface paths before building the ext4 image, writes deterministic systemd/netplan/ifupdown/resolver config for the simulator-assigned IP/gateway, and logs real VM boot failures with the Firecracker failure tail before returning provider-shaped API errors.
- BUG-1306: The VM-backed simulator path could build an ext4 guest image from a copied rootfs that lacked every kernel init candidate, so the guest kernel configured the provider-private IP correctly and then panicked with "No working init found." Rootfs preparation now verifies `/sbin/init`, `/etc/init`, `/bin/init`, or `/bin/sh` before image creation; when the copied Ubuntu rootfs has `/usr/lib/systemd/systemd` but no kernel-visible init symlink, it links `/sbin/init` to that real systemd binary. If neither an init candidate nor systemd exists, the launcher fails loudly before building an unbootable image.
- BUG-1307: The Firecracker rootfs copy path used `filepath.Join(src, ".")`, which cleaned the trailing dot and caused `cp -a <source-rootfs> <dest>` to nest the copied tree under `<dest>/rootfs` whenever the destination already existed. The simulator now passes a literal `src/.` to `cp -a`, so `/sbin/init`, `/bin/sh`, and the rest of the rootfs land directly under the per-VM rootfs directory. Regression coverage verifies the copy shape and symlink preservation.
- BUG-1308: AWS ECS `StopTask` synchronously waited for Docker's graceful-stop timeout before returning the public API response, so SDK suites with sleeping tasks could spend the five-minute CI budget waiting on local container grace periods. `StopTask` now records the public stopped state and initiates Docker stop asynchronously; internal startup-failure/process-exit cleanup still owns synchronous cleanup. Focused SDK coverage showed StopTask requests returning in milliseconds rather than the Docker grace period.

Issue #373 was clarified as a false-positive wording issue, not a missing runtime check: `DetectFirecrackerCapabilities()` already includes `firecracker`, and the shared capability detector opens `/dev/kvm` read-write for required Firecracker/jailer command sets. Regression coverage now locks that KVM-required command contract.

Previous phase closed BUG-1297 and BUG-1298:

- BUG-1297 / issue #366: Simulator release-image builds and `simulators/docker-compose.yml` used per-cloud build contexts even though each per-cloud module replaces `github.com/sockerless/simulator-realexec => ../realexec`. The simulator Dockerfiles now build from the shared `simulators/` context and switch into `/src/<cloud>` before `go build`; publish-container-images, compose, and per-cloud `docker-build` Make targets all use that same context. A simulator-scoped `.dockerignore` excludes test harnesses, generated test binaries, Terraform provider caches/state, built UI assets, and locally built simulator binaries from release-image contexts. Real Docker image builds for AWS, GCP, and Azure passed from the fixed context.
- BUG-1298 / issue #367: The explicit `SIM_RUNTIME=process` API-only startup mode existed but was undocumented, and Docker/Podman startup failures did not point operators to it. Common and per-cloud simulator docs now document `SIM_RUNTIME=process` as an explicit API-only mode for runs that do not invoke workload execution, not as a fallback. Startup comments and fatal messages now say Docker/Podman is required for workload execution and mention `SIM_RUNTIME=process` only for explicit non-execution API-only runs.

Previous phase closed BUG-1296:

- BUG-1296 / issue #365: Azure SDK local validation became portable on macOS without skipping tests or weakening the data plane. On Darwin, `make sdk-test` now delegates to the existing privileged Linux simulator test image and runs `make sdk-test-local` inside that container, so real-network SDK tests execute with Linux netns/veth/nftables capabilities instead of failing on macOS host limitations. Linux direct validation remains available through `make sdk-test-local`. The Azure SDK test harness also installs an explicit Event Grid data-plane resolver for the advertised `*.eventgrid.localhost:<simPort>` endpoint; it preserves the request URL and Host header for host-dispatched public data-plane routing while dialing loopback, and sets localhost wildcard names in `NO_PROXY` / `no_proxy`. Full macOS `make sdk-test` now passes through Docker.

Previous phase closed BUG-1295:

- BUG-1295 / issue #362: Azure Entra discovery advertised an `authorization_endpoint`, but the simulator did not serve `GET /{tenant}/oauth2/v2.0/authorize`, so public OIDC authorization-code clients dead-ended before login could complete. The Azure auth middleware now implements the authorization-code front half against the documented Microsoft Entra auth-code slice: absolute discovery endpoints for the served auth/token/JWKS routes, capability metadata for `code`, `query` / `fragment` / `form_post`, `authorization_code` / `client_credentials` / `refresh_token`, and `plain` / `S256`; required authorize parameters; short-lived authorization code issuance; state propagation; one-time code redemption at `/oauth2/v2.0/token`; PKCE validation; OAuth error bodies; unsupported grant rejection; access-token issuance; ID-token issuance when `openid` is in scope; and refresh-token issuance/redemption when `offline_access` is in scope. Regression coverage exercises the real simulator HTTP path through the Azure SDK test harness and verifies discovery, redirect, token exchange, ID-token claims, PKCE failure, single-use code behavior, refresh-token redemption, missing-scope rejection, hybrid-flow rejection, and unsupported-grant rejection.

Previous phase closed BUG-1293 and BUG-1294:

- BUG-1293 / issue #335: GCP firewall rules and Azure NSGs were still metadata-only on the real veth packet path. The shared realexec ingress filter now supports explicit accept/drop verdicts and clearing filters. GCP Compute firewall ingress rules compile by priority, target tags, source ranges, and source tags onto attached instance NICs with the implied deny-ingress rule enforced by nftables. Azure subnet/NIC NSGs compile inbound rules by priority, support scalar/list address and port fields plus core service tags, preserve default same-VNet inbound allowance, and remove nftables filters when no NSG is attached. Rule, tag, subnet association, NSG, and NIC mutations reapply filters to affected real NICs and fail loudly on substrate errors.
- BUG-1294 / issue #334: GCP and Azure managed load balancers were metadata-only after the AWS ELBv2 migration. GCP now implements the unmanaged instance-group API slice needed by backend services, `backendServices.getHealth`, health probing through configured HTTP/TCP health checks, host-dispatched forwarding-rule data-plane routing by frontend IP, URL-map/backend-service resolution, and proxying to healthy instance-group members. Azure Load Balancer now persists NIC backend-pool membership, dispatches data-plane requests by frontend public IP, resolves load-balancing rules/backend pools/probes from ARM state, probes backend NICs or backend-pool addresses, and proxies to healthy backends. Regression coverage uses real local HTTP listeners and official GCP SDK compile coverage for the new instance-group surface.

Previous phase closed BUG-1288 through BUG-1292:

- BUG-1288: Cloud backends still had direct `BaseServer.ContainerInspect` / `BaseServer.ContainerList` delegates, and the shared list path ignored `CloudState.ListContainers` errors. Cloud backend inspect/list paths now resolve through cloud state explicitly, provider list errors fail loudly, and `scripts/check-cloud-backend-isolation.sh` fails future direct inspect/list delegates across all cloud backend Go files.
- BUG-1289: CI logs still had warning/error-looking noise after PR #358 even when tests passed. The GCP host-dispatch allowlist test no longer emits a GitHub `##[error]` annotation for its intentional source reference, Vite package builds run under Bun's runtime instead of Node 26's deprecated `module.register()` path, `ui-core` emits declaration artifacts for Turbo output tracking, and UI router tests start from `/ui/` so they no longer print unmatched-route stderr.
- BUG-1290 / issue #359: AWS EC2 EBS snapshots could remain observably `pending` for standard snapshot restore flows. EC2 snapshots now persist an internal completion due time and settle pending snapshots on `DescribeSnapshots` and `CreateVolume(SnapshotId)`, so both filtered and unfiltered public reads expose `completed` before restore succeeds. AWS SDK coverage exercises CreateVolume -> CreateSnapshot -> filtered and unfiltered DescribeSnapshots -> CreateVolume(SnapshotId) without requiring VPC setup.
- BUG-1291 / issue #360: DynamoDB `DeleteItem` ignored `ReturnValues=ALL_OLD`. The handler now captures the pre-delete item and returns it as `Attributes` only when the item existed and `ReturnValues` is `ALL_OLD`; missing items still return no attributes. AWS SDK coverage verifies both existing and missing-item paths.
- BUG-1292: Pre-push dependency freshness failed because the live ECS and Lambda workflows pinned `aws-actions/configure-aws-credentials@v6.1.3` while the latest published semantic tag was `v6.2.0`. Both workflows now use `v6.2.0`.

Previous phase closed BUG-1285 through BUG-1287:

- BUG-1285 / issue #336: VPC/network/subnet/NIC/public-IP/NAT routing paths were metadata-only. The shared realexec substrate now supports per-network Linux namespaces, multiple subnet bridges, veth NIC attachment with real IPAM and unique MACs, routed egress links, public IPv4 IPAM, and nftables SNAT. AWS EC2 VPC/subnet, `RunInstances`/Auto Scaling ENIs, Elastic IPs, NAT gateways, and NAT routes use the substrate. GCP Compute networks/subnetworks, instance NICs, regional addresses, and Cloud NAT use it. Azure virtual networks/subnets, NIC private IP/MAC allocation, public IPs, and NAT gateway subnet programming use it. These paths fail loudly when Linux namespace/bridge/veth/route/nftables capabilities are missing.
- BUG-1286 / issues #334 and #335 partial: AWS ELBv2 and AWS security groups still had fabricated packet-path behavior. AWS ELBv2 target health now probes targets over real TCP/HTTP, ELBv2 data-plane requests route by load-balancer DNS host to healthy targets without control-plane listener-port binding, and EC2 security-group ingress rules compile to nftables on attached real ENI veth peers. Later BUG-1293/BUG-1294 completed the GCP/Azure #334/#335 packet paths.
- BUG-1287: Azure Event Grid topic ARM create/get/list leaked simulator-local data-plane plumbing by allocating per-topic `127.0.0.1:<random>` HTTP listeners when ARM was reached through localhost. Topics now always advertise the shared host-dispatched Event Grid data-plane endpoint, or the configured HTTPS gateway template, and publish requests route through the Azure simulator handler by Event Grid Host header. Regression tests verify create/get/list endpoint shape and publish fanout through the advertised host without per-topic local listener endpoints.

Previous phase closed BUG-1284:

- BUG-1284 / issue #356: Azure Cosmos DB Tables ARM and Azure Storage Tables ARM were missing. The Azure simulator now implements `Microsoft.DocumentDB/databaseAccounts/{account}/tables/{table}` CRUD/list plus table throughput at `.../throughputSettings/default`, and `Microsoft.Storage/storageAccounts/{account}/tableServices/default/tables/{table}` CRUD/list/update. ARM-created tables project into the real Tables data-plane store, deletes remove table entities, and the Storage Tables data plane honors the real `Prefer: return-no-content` create behavior and table ACL get/set calls used by terraform-provider-azurerm. The fix shipped with official Azure SDK coverage (`armcosmos.TableResourcesClient`, `armstorage.TableClient`), Azure CLI `az rest` coverage, and Terraform `azurerm_cosmosdb_table` / `azurerm_storage_table` apply/destroy coverage.

Previous phase closed BUG-1272 through BUG-1283:

- BUG-1272 / issue #310: Eventarc, Firestore, and Pub/Sub still emitted some protobuf `Timestamp` fields with `time.RFC3339Nano`, producing non-canonical fractional-second widths. Those call sites now use the shared canonical timestamp formatter and have SDK regression coverage.
- BUG-1273 / issue #343: AWS CloudTrail was missing. The AWS simulator now implements trail CRUD, logging status, event selectors/tags, `LookupEvents`, records simulator API calls, and delivers gzipped CloudTrail logs into S3 with SDK, CLI, and Terraform coverage.
- BUG-1274 / issue #346: AWS Auto Scaling Groups were missing. The AWS simulator now implements launch configurations, ASG lifecycle, desired-capacity updates, scaling activities, tags, and ASG-driven EC2 instance materialization with SDK, CLI, and Terraform coverage.
- BUG-1275 / issue #347: EBS was read-only metadata and ECS managed EBS could not prove snapshot data round trips. The AWS simulator now implements EBS volume/snapshot lifecycle, pending-to-completed snapshot state, restore from snapshot, and ECS managed EBS task mounts that write bytes, snapshot them, restore them, and read them back in a later task through real SDK and CLI flows.
- BUG-1276: Terraform cleanup could not enumerate CloudTrail-delivered objects through S3 version listing. S3 now implements `ListObjectVersions` with real object entries so provider `force_destroy` can clean up delivered logs.
- BUG-1277: GitHub Actions linux/arm64 CPU jobs were flaky because the repo has no root `go.mod`, so `actions/setup-go` restored no module cache and many hosted arm64 jobs cold-downloaded and compiled dependencies concurrently until runners were torn down mid-test. CI now gives every Go setup step an explicit `go.work`/`**/go.sum` cache key, without skipping tests or increasing timeouts.
- BUG-1278: CI still carried stale ten- and fifteen-minute command timeouts that could hide slow tests after the flake fix. Explicit CI step and Go test timeouts are now capped at five minutes.
- BUG-1279: Aggregate `lint` and `build-check` CI jobs exceeded the five-minute job budget even though the underlying work passed. CI now runs lint, build gates, amd64 builds, and arm64 builds as independent capped jobs; status-only aggregate jobs were removed so `needs` is not used for contexts that do not consume upstream outputs.
- BUG-1280: The first CI flake fix overcorrected by serializing hosted linux/arm64 CPU matrices and expanded lint into nineteen one-module jobs, producing a slow forty-plus-job PR run. CI now keeps the explicit Go cache and five-minute caps, removes broad `max-parallel: 1` serialization and phase-ordering `needs` gates from backend, FaaS smoke, e2e, simulator, Terraform, and smoke jobs, and groups lint/backend/FaaS smoke work into fewer real-coverage shards. No tests or jobs were disabled.
- BUG-1281: GCP Cloud Logging lost container log lines when stdout and stderr scanner goroutines appended to the same log name concurrently through a read-modify-write store sequence. Cloud Logging appends are now serialized so real container stdout/stderr lines are preserved; AWS already used atomic store updates and Azure already serialized monitor appends.
- BUG-1282: Several workflows either lacked concurrency cancellation or explicitly kept old runs alive, leaving stale pipelines queued/running after new PR branch pushes. Every workflow now has top-level concurrency keyed by workflow plus PR source branch/ref and `cancel-in-progress: true`, so new pushes auto-stop older runs for the same branch/ref.
- BUG-1283: PR CI still had two oversized real-test steps: AWS simulator CLI coverage bundled all 76 AWS CLI tests into one five-minute step and AWS Terraform provider coverage could exceed five minutes after real RDS restore work passed. AWS simulator coverage now keeps SDK coverage intact and shards the AWS CLI tests by service family, with a mechanical coverage check confirming every existing CLI test is selected exactly once. Terraform provider CI has a ten-minute cap, matching the allowed budget for real provider apply/destroy work. CI has 29 jobs and no `needs` edges.

Previous phase closed BUG-1270 and BUG-1271:

- BUG-1270: Top-level `make test` failed on UI-bearing Go apps and backend integration packages. The shared Go app `test` target now uses `-tags noui` when a UI package is configured, backend integration tests are build-tagged with `integration`, and integration CI/Make targets opt into `noui integration` explicitly.
- BUG-1271: Top-level fanout targets failed on leaf packages without the expected target shape. UI packages without a `test` script now report that explicitly instead of invoking `/bin/test`, and Go library Makefiles provide a `build-noui` compile-check alias for the aggregate no-UI build.

Last phase closed BUG-1264 / issues #312, #315, and #326-#329:

- BUG-1264 / issues #312 and #315: Azure Storage Blob/File/Queue data-plane errors now return XML `<Error>` envelopes with `Content-Type: application/xml` and `x-ms-error-code`; Blob list XML responses include the XML declaration, public `EnumerationResults` attributes, and `NextMarker`; Queue service properties return the public XML shape used by Terraform provider availability checks.
- Issue #326: Service Bus admin data-plane missing queue/topic/subscription/rule reads now return 404 Atom/XML errors instead of 200 empty feeds.
- Issue #327: Event Grid publish now rejects malformed JSON, empty batches, missing required Event Grid envelope fields, and invalid event times before webhook delivery.
- Issue #328: Redis, PostgreSQL Flexible Server, and Event Hubs namespace creates now use Azure LRO headers and in-progress states before converging to final public states.
- Issue #329: Key Vault secret/key/certificate attributes now include `recoveryLevel: Recoverable+Purgeable` and `recoverableDays: 90`.

Earlier recent phase closed BUG-1254 and BUG-1263 / issues #304, #309-#311, and #321-#325:

- BUG-1254 / issue #304: GCP API Gateway, Cloud Build, IAM, and Pub/Sub stale public-client rows were corrected. The simulator now has real gcloud coverage for those public CLI surfaces, and API Gateway has Terraform provider coverage through `google-beta`.
- BUG-1263 / issues #309-#311 and #321-#325: GCP Cloud Run and Cloud Functions list/LRO/timestamp wire shapes, Cloud Logging severity ordering, GCS metadata and IAM policy shape, Cloud SQL backup operation shapes, and Cloud DNS precondition error status were fixed with real SDK, CLI, and Terraform coverage where those public client surfaces exist.

Earlier recent phase closed BUG-1268 and BUG-1269 / issues #313, #314, and #340:

- BUG-1268 / issue #340: Azure Private DNS now implements `PrivateZonesClient.NewListByResourceGroupPager` at `GET .../privateDnsZones`, and virtual network links now implement the public list-by-zone endpoint. Both paths are covered by the real Azure SDK and Azure CLI.
- Issues #313 and #314: Azure ARM control-plane requests now reject missing `api-version` with `InvalidApiVersionParameter`, the dead unused AzureRouter validator was removed, and simulator list serialization returns `{"value":[]}` instead of `{"value":null}` for empty stores.
- BUG-1269: AWS Terraform HTTPS CI could hang waiting for Caddy because `tls internal` attempted to install the generated local CA into CI runner trust stores even though the harness already trusted the CA through `SSL_CERT_FILE`. The shared gateway Caddyfile now uses `skip_install_trust`; tests still verify TLS normally with the exported CA file, and Caddy startup no longer performs host trust-store mutation.

Earlier recent phase closed BUG-1265..BUG-1266 / issues #330-#331:

- BUG-1265 / issue #330: Amplify `StopJob` and `DeleteJob` now used distinct public REST paths. `StopJob` cancelled the job through `.../jobs/{jobId}/stop`; `DeleteJob` removed the job and its artifacts through `.../jobs/{jobId}`.
- BUG-1266 / issue #331: Amplify `ListArtifacts`, `GetArtifactUrl`, and `GenerateAccessLogs` were implemented on the AWS SDK REST paths and covered by real AWS SDK and AWS CLI tests. Terraform was not changed because the official AWS provider does not expose these job-artifact or access-log operations.

Earlier recent phase closed BUG-1255..BUG-1262 / issues #305-#308 and #317-#320:

- BUG-1255 / issue #305: S3 `ListObjectsV2` now returns lexicographically sorted keys, honors `start-after` and `continuation-token`, emits `NextContinuationToken`, and supports delimiter `CommonPrefixes`.
- BUG-1256 / issue #306: Lambda `FunctionConfiguration` responses no longer leak request-only `Code`, uploaded `ZipFile`, or `Tags`; `GetFunction` keeps `Code` and `Tags` as top-level members.
- BUG-1257 / issue #307: SNS returns `pending confirmation` for email/http/https-style protocols unless `ReturnSubscriptionArn=true`, and topic attributes distinguish confirmed vs pending subscriptions.
- BUG-1258 / issue #308: SQS `ReceiveMessage` rejects out-of-range `MaxNumberOfMessages` with `InvalidParameterValue` instead of silently clamping.
- BUG-1259 / issue #317: EC2 `DescribeInstances` applies supported filters and rejects unsupported filter names.
- BUG-1260 / issue #318: EC2 `RunInstances` honors `MinCount`/`MaxCount`, returns `pending`, and transitions instances to `running`.
- BUG-1261 / issue #319: ECR `PutImage` stores deterministic content-addressed SHA-256 manifest digests.
- BUG-1262 / issue #320: KMS `GenerateDataKey` returns fresh crypto-random plaintext key material and ciphertext that decrypts back to it.

Earlier recent phases closed BUG-1242, BUG-1243, BUG-1244, BUG-1245 / issue #298, BUG-1246, BUG-1247, BUG-1248, BUG-1249, BUG-1250, BUG-1251, BUG-1252, and BUG-1253:

- Azure Cache for Redis has Azure CLI and azurerm Terraform coverage.
- GCP Memorystore Redis has gcloud and terraform-provider-google coverage.
- GCP Cloud SQL exposes the `/v1` and `/sql/v1beta4` SQL Admin paths needed by SDK, gcloud, and Terraform.
- GCP Cloud DNS implements public Changes.Create/Get/List and ResourceRecordSets.Get/Patch with SDK, gcloud, and Terraform coverage.
- BUG-1246: Azure Storage data-plane middleware overmatched non-storage `*.localhost` hosts and swallowed `azure.sockerless.localhost` metadata requests. It now dispatches only real Azure Storage service labels: `blob`, `file`, `queue`, `table`, `web`, and `dfs`.
- BUG-1247: Azure Terraform CI ran the gateway-backed harness directly on the runner without installing Caddy. The Azure Terraform CI job now installs the real Caddy binary before `make terraform-test`.
- BUG-1248: GCP Cloud Run arithmetic SDK coverage asserted an exact `"30"` log entry, but the workload logs the real output line as `"Result: 30"`. The assertion now checks the joined Cloud Logging payloads for the actual result line.
- BUG-1249: Azure Terraform HTTPS coverage could fail late if Caddy was missing or if a future edit accidentally changed the provider endpoint away from HTTPS. The harness now preflights the Caddy executable and fails loudly unless the Terraform endpoint is HTTPS.
- BUG-1250: BUG-1104 audit found stale `gcp-gcs` CLI coverage marked not applicable even though current gcloud supports Cloud Storage endpoint overrides. The simulator now has real `gcloud storage` bucket/object lifecycle coverage, accepts the current CLI multipart boundary form, and implements the public `buckets.getStorageLayout` probe.
- BUG-1251: GCS object and bucket timestamps used RFC3339 nanosecond precision, which caused current Linux gcloud Storage to emit timestamp truncation warnings into command output. GCS timestamps now use Cloud Storage-style millisecond precision.
- BUG-1252: AWS/GCP Terraform had direct HTTP simulator harnesses even though the optional Caddy gateway existed. AWS/GCP now have `make terraform-https-test` targets that run the real providers through Caddy HTTPS with CA trust while preserving direct HTTP `make terraform-test`; AWS covers both the root stack and the RDS/ElastiCache Terraform subpackages through the HTTPS path.
- BUG-1253: The `gcp-vpcaccess` Terraform matrix row was marked not applicable even though terraform-provider-google exposes `google_vpc_access_connector`. The GCP Terraform stack now provisions a real connector through `vpc_access_custom_endpoint`, asserts its canonical ID, and marks the matrix row direct.
- BUG-1353: bleephub OAuth authorize flow had no session or CSRF: `GET /login/oauth/authorize` rendered a form with no session check; `POST /login/oauth/authorize` validated no session cookie and no `authenticity_token`; code was always bound to seed admin regardless of which user was "logging in". Fixed: added `POST /login` + `GET /login` session endpoints, `_gh_sess` session cookie, `authenticity_token` CSRF field in consent form, session + CSRF validation on approve POST, and code binding to the session user. Closes issue #399.
- BUG-1354: `POST /api/v3/admin/organizations` accepted any caller (including unauthenticated) without checking site-admin privilege. Real GHES returns 403 for non-site-admin callers. Fixed: handler now reads `ghUserFromContext` and checks `user.SiteAdmin`; returns 403 otherwise. Closes issue #400.
- BUG-1355: `handleECSListTasks` ignored `nextToken`/`maxResults`; returned all task ARNs unconditionally. Fixed: reads both params, sorts by task ARN, pages with default 100.
- BUG-1356: `handleECRDescribeRepositories` ignored `nextToken`/`maxResults`; returned all repositories. Fixed: sorts by repo name, pages with default 1000.
- BUG-1357: `handleSQSListQueues` ignored `NextToken`/`MaxResults`; returned all queue URLs. Fixed: sorts by URL, pages with default 1000.
- BUG-1358: `handleSSMDescribeParameters` ignored `NextToken`/`MaxResults`; returned all parameters. Fixed: reads request body, sorts by name, pages with default 50.
- BUG-1359: `handleKMSListKeys` ignored `Marker`/`Limit`; returned all keys with no `Truncated`/`NextMarker` fields. Fixed: sorts by key ID, pages with default 100; sets `Truncated` and `NextMarker`.
- BUG-1360: `handleSMListSecrets` ignored `NextToken`/`MaxResults`; returned all secrets. Fixed: reads request, sorts by name, pages with default 100.
- BUG-1361: `handleLambdaListFunctions` ignored `Marker`/`MaxItems` query params; returned all functions. Fixed: reads query params, sorts by function name, pages with default 50; returns `NextMarker`.
- BUG-1362: `handleCWDescribeLogGroups` read `limit` but ignored `nextToken`; never returned `nextToken` in response. Fixed: adds `nextToken` round-trip; uses `awsPage()` for consistent slicing.
- BUG-1363: `handleCWDescribeLogStreams` read `limit` but ignored `nextToken`; never returned `nextToken`. Fixed: same as BUG-1362.
- BUG-1364: `handleSNSListTopics` returned all topics in a single XML response; ignored `NextToken` form value. Fixed: reads `NextToken`, sorts by ARN, pages 100 per page; emits `<NextToken>` in XML when more pages follow.
- BUG-1365: `handleDDBQuery` and `handleDDBScan` ignored `Limit` and `ExclusiveStartKey`; returned all matching items. Fixed: reads both, applies limit, returns `LastEvaluatedKey` when truncated.
- BUG-1366: `handleDDBListTables` ignored `Limit` and `ExclusiveStartTableName`; returned all table names. Fixed: sorts by name, pages by limit, returns `LastEvaluatedTableName`.
- BUG-1348: `handleListBlobs` ignored `?maxresults=N` and `?marker=TOKEN`; fixed in PR #398.
- BUG-1349: `handleListContainers` ignored `?maxresults` and `?marker`; fixed in PR #398.
- BUG-1350: GCP Secret Manager list secrets was unpaged; fixed in PR #398.
- BUG-1351: GCP Pub/Sub list topics/subscriptions were unpaged; fixed in PR #398.
- BUG-1352: GCP Secret Manager list secret versions was already implemented with `secretManagerPagination()` — confirmed during Phase C audit; no fix needed, false alarm.
- BUG-1367: GCS `GET /storage/v1/b` ignored `pageSize`/`pageToken`; returned all buckets. Fixed: sorts by name, uses `paginateList()`, returns `nextPageToken`.
- BUG-1368: GCS `GET /storage/v1/b/{bucket}/o` ignored `pageSize`/`pageToken`; returned all objects. Fixed: uses `paginateList()` after existing name sort; returns `nextPageToken`.
- BUG-1369: `GET /compute/v1/.../global/networks` ignored `maxResults`/`pageToken`. Fixed: sorts by name, uses `paginateList()`.
- BUG-1370: `GET /compute/v1/.../global/instanceTemplates` ignored `maxResults`/`pageToken`. Fixed: sorts by name, uses `paginateList()`.
- BUG-1371: `GET /compute/v1/.../global/firewalls` ignored `maxResults`/`pageToken`. Fixed: sorts by name, uses `paginateList()`.
- BUG-1372: `GET /compute/v1/.../regions/{region}/addresses` ignored `maxResults`/`pageToken`. Fixed: sorts by name, uses `paginateList()`.
- BUG-1373: `GET /compute/v1/.../regions/{region}/routers` ignored `maxResults`/`pageToken`. Fixed: sorts by name, uses `paginateList()`.
- BUG-1374: `GET /compute/v1/.../zones/{zone}/instances` ignored `maxResults`/`pageToken`. Fixed: sorts by name, uses `paginateList()`.
- BUG-1375: `handleListBuildTriggers` ignored `pageSize`/`pageToken`. Fixed: sorts by name, uses `paginateList()`, returns `nextPageToken`.
- BUG-1376: `handleListLoggingSinks` ignored `pageSize`/`pageToken`. Fixed: sorts by name, uses `paginateList()`, returns `nextPageToken`.
- BUG-1377: `handleListLoggingMetrics` ignored `pageSize`/`pageToken`. Fixed: sorts by name, uses `paginateList()`, returns `nextPageToken`.
- BUG-1378: `handleKVListSecrets` returned all secrets in one response; `NextLink` never populated. Fixed: uses `kvPage()`, sets `NextLink` via `kvNextLink()`. Default 25 per page.
- BUG-1379: `handleKVListSecretVersions` returned all versions unpaged. Fixed: sorts by version, uses `kvPage()`, returns `NextLink`.
- BUG-1380: `handleKVListDeletedSecrets` returned all deleted secrets unpaged. Fixed: sorts by name, uses `kvPage()`, returns `NextLink`.
- BUG-1381: `handleKVListKeys` returned all keys unpaged. Fixed: sorts by name, uses `kvPage()`, returns `nextLink`.
- BUG-1382: `handleKVListKeyVersions` returned all key versions unpaged. Fixed: sorts by ID, uses `kvPage()`, returns `nextLink`.
- BUG-1383: `handleKVListDeletedKeys` returned all deleted keys unpaged. Fixed: sorts by name, uses `kvPage()`, returns `nextLink`.
- BUG-1384: `handleKVListCertificates` returned all certificates unpaged. Fixed: sorts by name, uses `kvPage()`, returns `nextLink`.
- BUG-1385: `handleKVListCertificateVersions` returned all cert versions unpaged. Fixed: sorts by ID, uses `kvPage()`, returns `nextLink`.
- BUG-1386: `handleKVListDeletedCertificates` returned all deleted certs unpaged. Fixed: sorts by name, uses `kvPage()`, returns `nextLink`.
- BUG-1387: ACR `GET /acr/v1/_catalog` ignored `n`/`last` query params; returned all repositories. Fixed: uses `acrCatalogPage()`, returns `Link` header with `rel="next"`.
- BUG-1388: Container Apps list handler ignored `$top`/`$skiptoken`; returned all apps. Fixed: sorts by name, uses `armPage()`, returns `nextLink`.
- BUG-1389: Private DNS list zones handler ignored `$top`/`$skiptoken`; returned all zones. Fixed: sorts by name, uses `armPage()`, returns `nextLink`.
- BUG-1390: Service Bus `handleSBListQueues` ignored `$top`/`$skiptoken`; returned all queues. Fixed: sorts by name, uses `armPage()`, returns `nextLink`.
- BUG-1391: New `simulators/azure/pagination.go` providing `kvPage`, `kvNextLink`, `armPage`, `armNextLink`, `acrCatalogPage` helpers for consistent Azure pagination.
- BUG-1392: GCP paginateList was already in shared/pagination.go but not wired to GCS/Compute/CloudBuild/Logging handlers (see BUG-1367 through BUG-1377).
- BUG-1393: New `simulators/gcp/shared/pagination.go` sort helpers wired to all previously unpaged GCP list handlers.
- BUG-1394: `EC2ErrorXML` in all three shared/errors.go files used `fmt.Fprintf` for XML body construction, bypassing XML escaping. User-controlled strings in error messages could produce malformed XML. Fixed: rewritten to use `xml.NewEncoder` with a typed struct, identical to S3ErrorXML.
- BUG-1395: GCP `GCPError` response omitted the `"details"` array that real GCP REST APIs always include. The Google API Go client's `*googleapi.Error` struct has a `Details` field; without `"details": []` the field is always nil, making the wire shape diverge. Fixed: added `"details": []any{}` to all GCP error responses across all three shared/errors.go copies.

Older closed bugs are intentionally not repeated here. Use PR descriptions and `git log` for exact fix details.

## False Positives

| Area | Finding | Why it is not a bug |
|------|---------|---------------------|
| `backends/aca/azure.go::fakeCredential` | Returns literal `"fake-token"` against simulator endpoints. | Simulator auth does not validate bearer tokens. Production clients use `azidentity.NewDefaultAzureCredential`; this credential is only for simulator endpoint clients. |
| `cmd/sockerless-admin/api_observability.go::envOrDefault` | Returns canonical OTel resource-attribute name when unset. | This is a documented default-value helper, not an error-hiding fallback. |
| aws scheduler simulator — GetSchedule `awsvpcConfiguration` (issue #477) | Reported as `Target.EcsParameters.NetworkConfiguration.AwsvpcConfiguration` returning None. | The sim persists + returns all three fields under the **lowercase** `awsvpcConfiguration` wire key — exactly what aws-sdk-go-v2 `scheduler` serializes/deserializes (`object.Key("awsvpcConfiguration")` / `case "awsvpcConfiguration":`) and botocore/GetSchedule use. The consumer's CLI assertion queried capital-A `AwsvpcConfiguration`, which fails against real AWS too; the Terraform read path (Go SDK) is unaffected. Consumer closed it. |

## Class-of-Bug Rules

- No stubs, fakes, mocks, synthetic data, silent fallbacks, or degraded modes.
- Public simulator APIs must match real cloud APIs; local gateway/admin plumbing must not leak into those APIs.
- Backend host primitive must match the cloud service: ECS in ECS, Lambda in Lambda, Cloud Run in Cloud Run, GCF in Cloud Run Functions, ACA in ACA, AZF in Azure Functions.
- External test fixtures use real clients: official SDKs, vendor CLIs, Terraform providers, and `gh` for bleephub.
- Closed enumeration means full-table audit before claiming fixed.
- Reopens require a postmortem: what test passed but should have failed, what client path was missed, and what new canonical-client test catches it.
- List operations need paged-iterator tests.
- Stateful resources need state-machine assertions.
- Mux pattern overlap is a recurring simulator bug class; run the overlap scanner when adding routes.
