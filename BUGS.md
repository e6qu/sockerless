# Known Bugs

726 total. 721 fixed + 2 partially fixed (724/725/726 — Phase 89 in progress). 3 open: BUG-715 (Phase 87 cloudrun rewrite), BUG-716 (Phase 88 aca rewrite), BUG-724 (Phase 89 PodList for non-ECS backends). Phase 89 progress: `specs/CLOUD_RESOURCE_MAPPING.md` covers all 7 backends; `Store.Images` disk persistence removed; all 4 cloud backends gained `resolve*State` cache+cloud-fallback helpers refactored into Stop/Kill callsites; ECS network creation tags Cloud Map namespaces with `sockerless:network-id`; **`docker images` is fully cloud-derived across all 6 cloud backends via `CloudImageLister`** (ECR for AWS, Artifact Registry for GCP, ACR for Azure — the last four via the shared `core.OCIListImages` OCI-distribution helper); ECS `ListPods` groups tasks by `sockerless-pod` tag. Remaining: `ListPods` for cloudrun+ACA (blocked on Phase 87/88), `resolveNetworkState` for cloudrun+ACA, per-backend restart-resilience integration tests.

Cross-cloud sweep notes (per user directive: "when you find a bug in a piece of code make sure to search in similar locations or other backends, clouds etc."):
- BUG-708 ECS-only — Azure uses ACR cache rules with managed-identity auth (different mechanism); GCP uses Artifact Registry pull-through.
- BUG-709 ECS-only — Azure ACA's `PollUntilDone` SDK helper sleeps internally; GCP's google.golang.org/api SDK handles polling.
- BUG-710 swept all 7 backend mains + CLI + READMEs + example terraform + tools/http-trace.
- BUG-711 ECS-only — no other backend sets explicit DNS search domains; GCP/Azure rely on per-network DNS zones with FQDN resolution.
- BUG-712 → also found BUG-713 in `backends/cloudrun/network_cloud.go::cloudNetworkCreate`. Azure ACA's `BeginCreateOrUpdate` is PUT-style and idempotent at the SDK level. Lambda + GCF + AZF have no cloud-side network creation (delegate to BaseServer).
- BUG-714 → cross-cloud sweep found same pattern in cloudrun (BUG-715) and aca (BUG-716). All three backends seed `c.NetworkSettings.Networks[*].IPAddress = "0.0.0.0"` at create time; later, `cloudServiceRegister` blindly uses that placeholder. ECS gets a real IP via Fargate ENI (`extractENIIP`); the proper fix lands here. For cloudrun + aca, Jobs may not have a stable per-execution IP in the same way — need deeper investigation before fixing (Cloud Run Jobs and ACA Jobs are short-lived and the per-execution IP may not be reachable from other containers). Filed but not fixed in this session.

| ID | Sev | Summary | Status |
|----|-----|---------|--------|
| 726 | High | **Phase 89.** Per stateless directive: NetworkState (per-network SG ID + Cloud Map namespace ID for ECS; per-network ManagedZone for cloudrun; per-network DNS zone + NSG for aca) was stored in-memory only. After backend restart, networks created in a prior session can't be reused. Phase 89 step (ECS): new `Server.resolveNetworkState(ctx, networkID)` derives `SecurityGroupID` via `DescribeSecurityGroups Filters=[tag:sockerless:network-id=<id>]` and `NamespaceID` via `ListNamespaces` filter by `tag:sockerless:network-id=<id>` (`cloudNamespaceCreate` now tags the namespace at create time). Cloudrun + aca pending. | partially fixed (ECS done; cloudrun + aca pending) |
| 725 | High | **Phase 89.** Per stateless directive: in-memory per-backend state maps (ECS `s.ECS`, Lambda `s.Lambda`, Cloud Run `s.CloudRun`, ACA `s.ACA`) were the canonical lookups for many cloud calls (ContainerStop / Kill / Remove / exec). After restart, the maps are empty until repopulated lazily. Phase 89 step landed `resolve*State` helpers across all four backends that wrap cache + cloud-derived fallback: ECS `Server.resolveTaskState` (calls `resolveTaskARN`, list+describe by tag), Lambda `Server.resolveLambdaState` (calls `resolveFunctionARN`, list functions + tag filter), Cloud Run `Server.resolveCloudRunState` (calls `resolveJobName` via `ListJobs` + label filter, plus `resolveActiveExecution` for stop targets), ACA `Server.resolveACAState` (calls `resolveJobName` via `NewListByResourceGroupPager` + tag filter, plus `resolveActiveExecution` via `Executions.NewListPager`). Refactored callsites: ECS `ContainerStop/Kill/Remove/cloudExecStart`, Lambda `ContainerStop/Kill`, Cloud Run `ContainerStop/Kill`, ACA `ContainerStop/Kill`. Remaining: extend the helpers to all other in-backend callsites that currently do `.Get(id)` directly; implement parallel `resolveNetworkState` for cloudrun + aca. | partially fixed (4 backends covered for container state; network state + full per-backend callsite sweep pending) |
| 724 | Med | **Phase 89.** Per stateless directive: docker pods (libpod) were tracked in `Store.Pods` only. Step 1: `core.CloudPodLister` interface; `BaseServer.PodList` merges cloud-derived pods. Step 2: ECS `ecsCloudState.ListPods` groups tasks by `sockerless-pod` tag — one PodListEntry per unique pod name, earliest `CreatedAt` → `Created`, container projection via `queryTasks`. Cloudrun (groups via service sidecar labels), ACA (groups via App sidecar tags) pending — they currently don't support pods natively on the Jobs path, so meaningful implementations land with Phase 87/88. | partially fixed (interface + ECS done; others blocked on Phase 87/88) |
| 723 | High | **Phase 89.** `Store.Images` was persisted to `~/.sockerless/state/images.json` (BUG-697 fix). Step 1 fixed: disk persistence removed. Step 2 landed across all 6 cloud backends: `core.CloudImageLister` interface on `BaseServer.ImageList`; ECS + Lambda query ECR (`DescribeRepositories` + `DescribeImages`); Cloud Run + GCF query GCP Artifact Registry via new shared `core.OCIListImages(ctx, OCIListOptions)` helper (OCI `/v2/_catalog` + `/v2/<repo>/tags/list` with bearer token from ARAuthProvider); ACA + AZF query Azure Container Registry via the same OCI helper with ACRAuthProvider tokens. After restart `docker images` against any cloud backend reflects live registry contents. Docker backend delegates to local daemon so no action needed. | fixed |
| 722 | High | After backend restart, `s.ECS.Get(containerID)` returned empty `ECSState` because the in-memory map is rebuilt only at create time — not on recovery. cloudExecStart used `ecsState.TaskARN` to call `ECS.ExecuteCommand`; with empty TaskARN, returned error `no ECS task associated with container <id>`. Error string written to hijacked HTTP conn; first byte `n`=110 → docker CLI: `unrecognized stream: 110`. Fixed in `backends/ecs/cloud_state.go`: new `resolveTaskARN(ctx, containerID)` queries cloud (ListTasks + DescribeTasks + tags filter) and returns `(arn, clusterARN)`. `cloudExecStart` calls it as fallback when in-memory state is empty, then writes to `s.ECS` so subsequent calls in the same backend lifetime hit the cache. Phase 89 will replace this lazy recovery with consistent cloud-derived lookups across all callsites. | fixed |
| 721 | Med | After BUG-717 fix, single `docker exec svc echo single` returned 10 copies of `single` instead of one. Root cause: SSM Session Manager agent retransmits each `output_stream_data` frame until it sees a valid `acknowledge` AgentMessage; sockerless's ack format isn't yet recognized by the agent (despite matching session-manager-plugin source layout for HeaderLength + MessageType + UUID + JSON body — exact divergence not yet identified, may be Flags or PayloadDigest semantics). Pragmatic fix in `backends/ecs/exec_cloud.go::ssmDecoder`: dedupe received frames by `MessageID` UUID before forwarding to `pending` buffer. The agent still wastes bandwidth retransmitting until session ends, but docker sees correct output. Long-term: nail down the agent's exact ack-validation rules so retransmits stop. | fixed (workaround; proper ack-acceptance still needed) |
| 720 | High | Cross-cloud sweep of BUG-719: terraform task role lacked `ssmmessages:CreateControlChannel/CreateDataChannel/OpenControlChannel/OpenDataChannel` permissions required by ECS Exec for the in-task SSM agent to dial back to Session Manager. Without these, ECS.ExecuteCommand reports the session as opened but the data channel immediately closes. Fixed in `terraform/modules/ecs/main.tf::data.aws_iam_policy_document.task` — new `ECSExecSSMMessages` statement allowing all four actions. Applied to live infra mid-session via `terragrunt apply`. | fixed |
| 719 | High | ECS docker exec returned `unrecognized stream: 110` even after BUG-717 SSM-decoder fix. Root cause: `backends/ecs/containers.go::startTask` called `ECS.RunTask` without `EnableExecuteCommand: true`. Per AWS docs, ECS Exec must be enabled at task launch — there's no way to enable it after the fact. Fixed: added `EnableExecuteCommand: true` to `RunTaskInput`. New tasks launched after the fix support `docker exec`; existing pre-fix tasks remain non-execable until replaced. | fixed |
| 718 | High | Cross-cloud sweep of BUG-708 found `backends/lambda/image_resolve.go` had the same docker-hub upstream issue (AWS rejects unauthenticated rules) AND a separate silent fallback: when pull-through cache failed, Lambda's `resolveImageURI` logged WRN and called `pushToECR` to push from the local Store instead — silently substituting the image source without operator awareness. Fixed: applied the ECS BUG-708 pattern (read `SOCKERLESS_ECR_DOCKERHUB_CREDENTIAL_ARN`, pass as `CredentialArn`, error explicitly when unset). Removed the `pushToECR` fallback path entirely — it only worked when the image happened to be in the local store and the swap was undocumented. Now Lambda fails clearly when pull-through cache can't be set up; operator decides next step. Build + lambda unit tests pass. | fixed |
| 717 | High | `docker exec <ecs-container> echo hello` against the live ECS backend returned `unrecognized stream: 69` because `cloudExecStart` piped SSM Session Manager binary frames through `wsBridge → muxBridge` directly, so Docker CLI mistook embedded SSM framing bytes (e.g. 0x45 'E' from a field) as bogus Docker mux stream IDs. Fixed: new `backends/ecs/ssm_proto.go` implements the AWS AgentMessage binary protocol (120-byte fixed header — HeaderLength, MessageType padded to 32, SchemaVersion, CreatedDate, SequenceNumber, Flags, MessageId as two int64 halves, PayloadDigest SHA-256, PayloadType, PayloadLength — followed by the variable Payload). `parseSSMFrame` deserializes by exact offset; `buildSSMAck` builds the JSON-bodied `acknowledge` AgentMessage that keeps the session alive on every received `output_stream_data`; `buildSSMInput` wraps stdin in `input_stream_data` with PayloadType=1. `ssmDecoder` (in `exec_cloud.go`) reads frames with `io.ReadFull` (header+payload as separate reads — message-boundary safe), routes `output_stream_data` payloads to its pending buffer (PayloadType 1=stdout→Docker stream id 1, PayloadType 11/2=stderr→2), terminates on `channel_closed` or PayloadType=12 (exit code), drains acks back through the wire. `muxBridge` now consults `streamTagger.lastStream()` so each chunk gets the right Docker stream id. Tests: `TestParseSSMFrame_*` (4 cases), `TestBuildSSMAck_RoundTrip`, `TestSSMTextStreamID_NonOutputType`, `TestSSMDecoder_StdoutFlow` (verifies decoder + ack emission against a fakeWire). Live verification queued in the resumed runbook. Cross-cloud sweep: ECS-only — Cloud Run/GCF have no exec path; ACA exec uses Container Apps console with its own proto; Lambda exec goes through the agent overlay (no SSM). | fixed |
| 716 | High | ACA backend's `NetworkConnect` calls `cloudServiceRegister(containerID, hostname, ep.IPAddress, net.ID)` with `ep.IPAddress` taken from the local container state (seeded as `"0.0.0.0"` placeholder in `backends/aca/backend_impl.go:136`). The Private DNS A-record gets the placeholder IP — cross-container DNS can never work. Cross-cloud sibling of BUG-714. Minimum fix landed in `backends/aca/service_discovery_cloud.go::cloudServiceRegister`: early-return with INF log when ip ∈ {"", "0.0.0.0"} so the SDK isn't called with a useless A-record value. Proper fix is deferred (ACA Job executions don't have addressable per-execution IPs reachable from other Jobs; would need ACA Apps with ingress for real cross-container DNS). | open |
| 715 | High | Cloud Run backend's `NetworkConnect` calls `cloudServiceRegister(containerID, hostname, ep.IPAddress, net.ID)` with `ep.IPAddress` taken from the local container state (seeded as `"0.0.0.0"` placeholder in `backends/cloudrun/backend_impl.go:126`). The Cloud DNS A-record gets the placeholder IP — cross-container DNS can never work. Cross-cloud sibling of BUG-714. Minimum fix landed in `backends/cloudrun/service_discovery_cloud.go::cloudServiceRegister`: early-return with INF log when ip ∈ {"", "0.0.0.0"} so DNS doesn't get a useless A-record. Proper fix is deferred (Cloud Run Job executions don't have addressable per-execution IPs reachable from other Jobs; would likely need Cloud Run Services with internal-only ingress + VPC connector). | open |
| 714 | High | ECS backend registered each container in Cloud Map with `ep.IPAddress` from the local in-memory container state, but that field is seeded as the placeholder `"0.0.0.0"` (`backends/ecs/backend_impl.go:127`) and never updated with the real ENI IP that Fargate assigns once the task reaches RUNNING. Effect: Cloud Map A-records resolve to 0.0.0.0, breaking cross-container DNS even by FQDN — `discover-instances` returns `0.0.0.0`, `dig svc.skls-<net>.local` returns `0.0.0.0`, `curl http://svc.skls-<net>.local:8080` connects to nothing. Diagnosed in session 2 by querying Cloud Map directly after `docker run -d --name svc --network skls2 alpine nc-loop`: namespace + service + instance all present, but `Attributes.AWS_INSTANCE_IPV4 = 0.0.0.0`. Fix: in `backends/ecs/backend_impl.go::startSingleContainerTask`, move the `cloudServiceRegister` loop to AFTER `waitForTaskRunning(...)` returns the task's `ip:9111` address (already extracted via `eni.go::extractENIIP`); strip the port and pass that IP into the register call. The earlier in-memory `ep.IPAddress` is no longer used as the registration source. | open |
| 713 | High | Cross-cloud sibling of BUG-712 found via the sweep directive: `backends/cloudrun/network_cloud.go::cloudNetworkCreate` calls `s.gcp.DNS.ManagedZones.Create(...)` directly. On retry against existing zone, GCP returns `googleapi.Error{Code: 409}` "already exists" and the network is left in an unusable state. Fix: catch HTTP 409 from the Create call, fall back to `ManagedZones.Get(project, zoneName)` to fetch the existing zone, and store its `Name` + `DnsName` into `NetworkState`. Azure ACA's `BeginCreateOrUpdate` is naturally idempotent (PUT semantics), so no fix needed there. Lambda + GCF + AZF backends have no cloud-side network creation. | open |
| 712 | High | `cloudNetworkCreate` (and its companion `cloudNamespaceCreate`) was non-idempotent: a retry of `docker network create skls-net` after a partial failure (or any leftover AWS state from a prior run that wasn't cleaned up) crashed at `CreateSecurityGroup` with `InvalidGroup.Duplicate` and at `CreatePrivateDnsNamespace` with `ConflictingDomainExists` (Route53 hosted zone already linked to VPC). The network was still returned 201 with warnings (BUG-700 plumbing) but every container on it then failed with `failed to register in Cloud Map: network has no Cloud Map namespace`. Fix in `backends/ecs/network_cloud.go::cloudNetworkCreate`: catch `InvalidGroup.Duplicate`, look up the existing SG by name+VPC via `DescribeSecurityGroups`, and reuse its GroupId. Tolerate `InvalidPermission.Duplicate` on the self-ingress add. Fix in `backends/ecs/service_discovery_cloud.go::cloudNamespaceCreate`: before calling `CreatePrivateDnsNamespace`, call `findNamespaceByName(nsName)` (new helper using `ListNamespaces` with `NAMESPACE_TYPE=DNS_PRIVATE` filter) and reuse the existing `Id` if found. Together these make `docker network create` survive transient failures and operator-level cleanup gaps without leaving the network in an unusable state. | open |
| 711 | High | P86-003 set `containerDef.DnsSearchDomains` so awsvpc-mode containers could resolve bare short names against their network's Cloud Map namespace. AWS ECS `RegisterTaskDefinition` rejects this for awsvpc unconditionally on FARGATE. Fixed in `backends/ecs/taskdef.go::buildContainerDef`: when the container is on at least one user-defined network with a Cloud Map namespace AND has an explicit entrypoint or command, wrap the original argv in a `/bin/sh -c` shim that rewrites `/etc/resolv.conf` (preserving existing `nameserver` lines for VPC DNS, adding the namespaces as a `search` line) then `exec`s the original argv with proper POSIX shell quoting via the new `shellQuoteArgs` helper. Image-default CMD without an explicit override is left alone (would require re-pulling the image manifest to reconstruct argv). Tested via `TestShellQuoteArgs`. Live verification of cross-container short-name DNS scheduled in the resumed runbook. | fixed |
| 710 | Med | Sockerless CLI default `addr` is `:2375`, which collides with the Docker daemon's well-known TCP port (and Podman's listen-on-2375 mode). Running `sockerless server start` while a local Docker/Podman socket is also exposed on 2375 either bind-fails or — if the user adopts `DOCKER_HOST=tcp://127.0.0.1:2375` — silently routes traffic to the wrong daemon. Backend binaries already default to `:9100`, but the CLI shipped with `:2375` and several READMEs/examples reference 2375. Fix: change every default to `:3375` (CLI server start, all backend `cmd/*/main.go` `-addr` flags, READMEs, example terraform outputs). Pick 3375 because it's free, "Docker-shaped" (memorable), and lets users keep DOCKER_HOST=tcp://127.0.0.1:3375 muscle memory without colliding with the real Docker daemon. | open |
| 709 | High | `backends/ecs/service_discovery_cloud.go::waitForOperation` polls Cloud Map's `GetOperation` 60 times with no `time.Sleep` between iterations. Real Cloud Map private DNS namespace creation needs ~30-60s; the loop burns through 60 API calls in <10s and reports `timeout waiting for operation` while the namespace is still PENDING. Result: `cloudNamespaceCreate` returns an error, `cloudNetworkCreate` surfaces it as a Warning (BUG-700 plumbing), but the Docker network is left with `NamespaceID == ""`. All later container starts hit `failed to register in Cloud Map: network <id> has no Cloud Map namespace`, breaking cross-container DNS by short name. Repro: `docker network create skls-net && docker run -d --name svc --network skls-net … && docker run --rm --network skls-net … curl http://svc:8080` — second container can't resolve `svc`. Fix: add `time.Sleep(2 * time.Second)` between polls (60 × 2s = 120s headroom) and broaden status switch to surface `OperationStatusSubmitted`/`OperationStatusPending` debug logs so the wait is observable. | open |
| 708 | Low | ECS backend's `resolveImageURI` calls `ensurePullThroughCache` on every container create. AWS now rejects docker-hub upstream cache rules with `UnsupportedUpstreamRegistryException` unless a `CredentialArn` (Secrets Manager ARN with `{username,accessToken}`) is wired into the rule. Fixed in `backends/ecs/image_resolve.go`: new `dockerHubCredentialARN()` reads `SOCKERLESS_ECR_DOCKERHUB_CREDENTIAL_ARN`; `ensurePullThroughCache` passes it as `CredentialArn` when upstream is `registry-1.docker.io`; if the env var is unset, returns explicit error `docker-hub pull-through cache requires SOCKERLESS_ECR_DOCKERHUB_CREDENTIAL_ARN ...` (no silent fallback). Removed the per-prefix `skipCacheOnce` memo + INF demotion (no-fallback principle). Removed `Server.skipCacheOnce sync.Map` field. | fixed |
| 707 | Med | GCP Cloud Build Secret Manager integration. Fixed: `backends/gcp-common/build.go` now populates `Build.AvailableSecrets.SecretManager[]` from `opts.Secrets` (env-name → `projects/P/secrets/S/versions/V` reference) and adds each env-var name to every step's `SecretEnv`. Simulator's `CreateBuild` handler (see BUG-704) calls `resolveSecretManagerReference` on each binding via the new `simulators/gcp/secretmanager.go` slice (`CreateSecret`, `AddSecretVersion`, `AccessSecretVersion`, incl. `"latest"` alias) and exposes the payload as an env var to the build step subprocess. | fixed |
| 706 | Med | Azure ACR Cache Rules slice. Fixed: `simulators/azure/acr.go` extended with cacheRule CRUD (PUT/GET/LIST/DELETE) under `Microsoft.ContainerRegistry/registries/{registry}/cacheRules/{rule}` — 200 sync on create (SDK accepts), 204 on delete (SDK's `BeginDelete` requires 202/204). `simulators/azure/shared/container.go` now strips both `docker-hub/` and `library/` prefixes from `.azurecr.io/` refs on `ResolveLocalImage`, parallel to AWS ECR. Backend: `backends/aca/azure.go` gains `Registries` + `ACRCacheRules` clients (via `armcontainerregistry.ClientFactory`); `backends/aca/go.mod` elevates `armcontainerregistry` v1.2.0 from indirect → direct. New `backends/azure-common/ResolveAzureImageURIWithCache` lists cache rules on the configured ACR, matches `sourceRepository` prefix (exact + wildcard `/*`) against the incoming ref, and rewrites to `<acrName>.azurecr.io/<targetRepository>:<tag>`; `backends/aca/backend_impl.go` calls it from `ContainerCreate`. Tests: SDK `TestACR_CacheRuleCRUD` + `TestACR_CacheRuleIdempotentUpsert`; CLI `TestACR_CacheRuleCRUD` (skips gracefully if `az` absent); resolver unit tests `TestResolveAzureImageURIWithCache_*` + `TestTrimWildcardSuffix`. No terraform — azurestack provider (in use for sim terraform tests) lacks `container_registry_cache_rule` resource. | fixed |
| 705 | High | AWS Lambda simulator bypassed the real Lambda Runtime API. Fixed: new `simulators/aws/lambda_runtime.go` implements the AWS Runtime API slice as a per-invocation HTTP sidecar on an ephemeral `127.0.0.1:<port>` — routes `GET /2018-06-01/runtime/invocation/next`, `POST /invocation/{id}/response`, `POST /invocation/{id}/error`, `POST /runtime/init/error`. `handleLambdaInvoke` now allocates a sidecar, launches the function container with `AWS_LAMBDA_RUNTIME_API=host.docker.internal:<port>` + standard Lambda env vars (`FUNCTION_NAME`, `FUNCTION_VERSION`, `MEMORY_SIZE`, `_HANDLER`, `LOG_GROUP_NAME`, `LOG_STREAM_NAME`, `AWS_REGION`), waits for `/response` or `/error`, and surfaces the result to the SDK caller (including `X-Amz-Function-Error: Unhandled` on error). Response headers on `/next` match the cloud (`Lambda-Runtime-Aws-Request-Id`, `-Deadline-Ms`, `-Invoked-Function-Arn`, `-Trace-Id`). Container-to-host networking uses `--add-host host.docker.internal:host-gateway` on Docker and Podman's native exposure. Shared `sim.ContainerConfig` gained `ExtraHosts []string`. New `simulators/testdata/lambda-runtime-handler/` provides a test handler image that speaks the real Runtime API. Tests: 4 SDK tests (`TestLambda_InvokeRoundTrip`, `TestLambda_InvokeHandlerError`, `TestLambda_InvokeContainerCrash`, `TestLambda_RuntimeAPILogsToCloudWatch`), 2 CLI tests (`TestLambda_InvokeRuntimeAPI_CLI`, `TestLambda_InvokeRuntimeAPIError_CLI`). No terraform test — Runtime API isn't a terraform-exposed resource. | fixed |
| 704 | Med | GCP Cloud Build slice. Fixed in `simulators/gcp/cloudbuild.go`: REST handlers for CreateBuild (LRO with done=true), GetBuild, CancelBuild (via `{id}:cancel` Go-mux workaround), GetOperation for build LROs. CreateBuild executor extracts the source tarball from `gcsObjects`, runs `gcr.io/cloud-builders/docker` build steps against it, streams output, resolves `availableSecrets.secretManager` bindings via the new Secret Manager slice (see BUG-707). Build state machine QUEUED→WORKING→SUCCESS/FAILURE/CANCELLED matches the real API. Test `simulators/gcp/sdk-tests/build_test.go` replaces the skip with `TestCloudBuild_DockerBuildAndPush` + `TestCloudBuild_SecretEnvExpansion` + `TestCloudBuild_MissingSecretFails`. | fixed |
| 703 | High | Azure NSG integration. Fixed in commit 1b14668: `backends/aca/network_cloud.go` rewritten — `cloudNetworkCreate` now calls `armnetwork.SecurityGroupsClient.BeginCreateOrUpdate` (NSG); `cloudNetworkDelete` tears down NSG before DNS zone; `cloudNetworkAddRule` calls `SecurityRulesClient.BeginCreateOrUpdate` with priority = 100 + existing-rule count. `AzureClients` gains `NSG` + `NSGRules` clients (via `armnetwork.ClientFactory`). `backends/aca/go.mod` imports `armnetwork/v7` v7.2.0. Simulator extension: `simulators/azure/network.go` adds `securityRules` sub-resource endpoints (PUT/GET/DELETE/LIST) that round-trip with the NSG's `Properties.SecurityRules` array — new SDK test `TestNetwork_NSGSecurityRulesCRUD` covers the client. | fixed |
| 702 | High | Azure Private DNS Zones integration. Fixed in commit 5c8780b: `backends/aca/service_discovery_cloud.go` rewritten to call real `armprivatedns.RecordSetsClient` (CreateOrUpdate/Delete/Get A records); `backends/aca/network_cloud.go` creates a per-network Private DNS zone `skls-<name>.local` via `armprivatedns.PrivateZonesClient.BeginCreateOrUpdate` and drops it symmetrically on network remove. `AzureClients` gains `PrivateDNSZones` + `PrivateDNSRecords` clients. In-memory `serviceRegistry` struct removed entirely. `backends/aca/go.mod` now imports `armprivatedns` v1.3.0. Simulator side was already present in `simulators/azure/dns.go`; no simulator changes this commit. | fixed |
| 700 | Med | Backend's `cloudNamespaceCreate` + VPC security-group failures were silent — `docker network create` returned 201 with an empty Warning even when cross-container DNS would be broken. Fixed: ECS, Cloud Run, and ACA backends' `NetworkCreate` now populate the response `Warning` field with a semicolon-separated list of cloud-side failures (VPC security group, Cloud Map namespace, Cloud DNS zone, NSG state). The ACA path also surfaces the in-memory NSG placeholder state (pointing at open BUG-703) so clients see the limitation without reading backend logs. Unit tests in `backends/ecs/network_warning_test.go`. | fixed |
| 701 | High | Simulators ran each ECS task / Cloud Run Job / ACA job as a standalone host-Docker container with no shared Docker user-defined network → Docker's embedded DNS couldn't resolve cross-container hostnames. Cross-cloud; each cloud fixed at its own DNS slice. **AWS (fixed)**: `simulators/aws/cloudmap.go` — `handleCMCreatePrivateDnsNamespace` creates Docker network `sim-<nsId>`; `handleCMRegisterInstance` resolves task via `sockerless-container-id` tag and connects with service name alias; `handleCMDeleteNamespace` removes network. **GCP (fixed)**: `simulators/gcp/dns.go` — private managed zones back themselves with `sim-<zoneId>`; A records on private zones look up container by Rrdatas[0] IP via `FindContainerByIP` and connect with record short name alias. **Azure (fixed)**: `simulators/azure/containerappsenv.go` — ACA environments back themselves with `sim-env-<envName>`; `simulators/azure/containerapps.go` resolves env at job-start and passes `Network`+`NetworkAliases=[jobName]` into `ContainerConfig` (new field on shared lib's ContainerConfig wiring into `EndpointsConfig.Aliases`). Shared-lib helpers `EnsureDockerNetwork` / `ConnectContainerToNetwork` / `DisconnectContainerFromNetwork` / `RemoveDockerNetwork` + `CleanupContainers` network pruning added to all three per-cloud `shared/container.go` files. Tests: AWS has SDK + CLI + terraform; GCP has SDK + CLI + terraform (private zone terraform); Azure has SDK + CLI (terraform omitted; azurestack provider lacks ACA env resource). | fixed |
| 699 | Med | AWS simulator's EC2 API didn't pre-register `subnet-sim` (the conventional placeholder subnet used by `SOCKERLESS_ECS_SUBNETS=subnet-sim` in the smoke-test harness). Sockerless's `cloudNetworkCreate` + `cloudNamespaceCreate` called `DescribeSubnets` → returned empty → VPC-ID resolution failed → Cloud Map namespace never got made. Fixed in `simulators/aws/ec2.go` by auto-creating `vpc-sim` + `subnet-sim` entries on simulator startup (idempotent). | fixed |
| 698 | Critical | Docker CLI's `docker run -d` flow sends POST /wait?condition=next-exit *before* POST /start, and blocks on reading the wait response's status line. Sockerless's wait handler called `CloudState.WaitForExit` (or the Store-based wait) *before* writing any response, so Go's http server never sent headers, so docker CLI's `cli.post` never returned, so /start was never sent. Fixed by adding an early `flushWaitHeaders` call that commits the 200 + Content-Type before the handler blocks on the exit event; the response body (exit code JSON) is written after the exit lands. Verified: `docker run -d` and `docker run --rm` both succeed end-to-end against the simulator. | fixed |
| 697 | Med | sockerless image store didn't persist `docker pull` state across backend restarts. Fixed in `backends/core/store.go` with `PersistImages` + `RestoreImages` methods that read/write `$SOCKERLESS_STATE_DIR/images.json` (default `$HOME/.sockerless/state/images.json`). `NewBaseServer` auto-restores on startup and `StoreImageWithAliases` auto-persists on every image write. Applies uniformly across AWS, GCP, Azure backends (all pass through `NewBaseServer`). 4 unit tests (round-trip, missing file, empty path, atomic replace). | fixed |
| 696 | Med | AWS simulator missing ECR pull-through-cache APIs. Fixed in `simulators/aws/ecr.go` by implementing `handleECRCreatePullThroughCacheRule`, `handleECRDescribePullThroughCacheRules`, `handleECRDeletePullThroughCacheRule` with a dedicated `ecrPullThroughCacheRules` store, real AWS error shapes (`PullThroughCacheRuleAlreadyExistsException`, `PullThroughCacheRuleNotFoundException`), and the full CRUD lifecycle. Tested via 4 SDK tests (`simulators/aws/sdk-tests/ecr_test.go`), 3 CLI tests (`simulators/aws/cli-tests/ecr_test.go` — new file), and a terraform test that applies + destroys `aws_ecr_pull_through_cache_rule` against the simulator. Launch-time URI resolution was already handled by the existing `sim.ResolveLocalImage` which strips `docker-hub/`+`library/` prefixes from ECR URIs — no changes needed there. | fixed |
| 695 | High | `StreamCloudLogs` rejects containers in `created` state unconditionally — breaks `docker run` create→attach→start flow where attach opens before start. Fixed with new `AllowCreated` option in `StreamCloudLogsOptions`; ECS `ContainerAttach` passes it. | fixed |
| 694 | High | `StreamCloudLogs` follow loop exits as soon as `!c.State.Running`, but `created` state is also non-running — attach before start exits the stream on first tick. Fixed by switching exit condition to `isTerminalState(status)` which fires only on `exited`/`dead`/`removing`. | fixed |
| 693 | High | ECS task definition registered with unqualified image ref (e.g. `alpine`) — Fargate cannot pull, task stays PENDING forever. `backends/ecs/taskdef.go:buildContainerDef` used `config.Image` raw; fixed in P86-015 by porting Lambda's `resolveImageURI` to ECS. | fixed |
| 692 | Critical | `docker run` hangs after POST /containers/create against live ECS backend. Root cause: `ContainerAttach` delegated to `BaseServer.ContainerAttach` which returned an EOF pipe because `LocalStreamDriver` had no local driver. Fixed in P86-016 by implementing ECS-specific attach that streams CloudWatch logs. | fixed |
| 662 | High | Auto-agent delegates to BaseServer.ContainerStart which reads Store. | fixed |
| 663 | High | docker wait hangs in auto-agent mode. Fix: check local WaitChs first. | fixed |
| 664 | High | CloudState only queried RUNNING tasks. Fix: query RUNNING + STOPPED. | fixed |
| 665 | Med | docker logs 404 after restart — task ID from local state only. Fix: query cloud for task ARN by container ID tag when local state missing. | fixed |
| 666 | Low | docker run -d blocks ~30s on ECS provisioning. Fix: async wait for RUNNING — return immediately after RunTask, poll in background. | fixed |
| 667 | High | Lambda backend missing CloudStateProvider — docker ps, inspect, stop all broken after PendingCreates.Delete. Fix: implement lambdaCloudState querying ListFunctions + tags. | fixed |
| 668 | High | StreamCloudLogs uses Store.ResolveContainerID + Store.Containers.Get — fails in stateless mode (docker logs 404 on all cloud backends). Fix: use ResolveContainerAuto. | fixed |
| 669 | High | ECS ContainerLogs falls back to BaseServer.ContainerLogs when taskID unknown — stateless violation. Fix: return clear error instead of BaseServer delegation. | fixed |
| 670 | High | BaseServer.ContainerInspect uses Store.ResolveContainer — fails in stateless mode when called via delegates. Fix: use ResolveContainerAuto. | fixed |
| 671 | High | BaseServer.ContainerList uses Store.Containers.List — returns empty on cloud backends. Fix: use CloudState.ListContainers when available. | fixed |
| 672 | High | BaseServer.ContainerTop uses Store.ResolveContainerID + Store.Containers.Get — fails in stateless mode. Fix: use ResolveContainerAuto. | fixed |
| 673 | High | BaseServer.ContainerUpdate uses Store.ResolveContainerID + Store.Containers.Update — fails in stateless mode. Fix: use ResolveContainerIDAuto. | fixed |
| 674 | High | BaseServer.ContainerStart/Stop/Kill/Remove/Restart/Logs/Wait/Attach/Stats/Rename/Pause/Unpause all use Store.ResolveContainerID — fail in stateless mode. Fix: use ResolveContainerAuto. | fixed |
| 675 | High | BaseServer.ExecCreate uses Store.ResolveContainerID + Store.Containers.Get — fails in stateless mode. Fix: use ResolveContainerAuto. | fixed |
| 676 | High | BaseServer.NetworkConnect/Disconnect use Store.ResolveContainerID. Fix: use ResolveContainerIDAuto. | fixed |
| 677 | Med | CloudRun ContainerTop both branches delegate identically to BaseServer.ContainerTop. Fix: return NotImplemented when no agent connected. | fixed |
| 678 | Med | CloudRun ContainerUpdate delegates to BaseServer without resolving container first. Fix: add ResolveContainerAuto check. | fixed |
| 679 | Med | StreamCloudLogs follow-mode checks Store.Containers.Get for running status — fails in stateless mode. Fix: use ResolveContainerAuto. | fixed |
| 680 | Med | Handler files (handle_containers_query, handle_containers, handle_extended, handle_exec, handle_libpod) use Store.ResolveContainerID/ResolveContainer directly. Fix: use ResolveContainerAuto/ResolveContainerIDAuto. | fixed |
| 681 | High | CloudRun/ACA/GCF/AZF CloudState implementations were stubs reading Store.Containers instead of querying cloud APIs. Fix: implement real cloud API queries (ListJobs, ListFunctions, etc.). | fixed |
| 682 | High | GCP label value limit (63 chars) truncates 64-char container IDs — CloudState can't match containers. Fix: store full ID in GCP annotations; read from env var for GCF. | fixed |
| 683 | High | Auto-agent (SpawnAutoAgent/StopAutoAgent) used in ECS, CloudRun, ACA backends — stateless violation (spawns local processes, reads Store.Containers). Fix: remove from all cloud backends. | fixed |
| 684 | High | GCP simulator missing LatestCreatedExecution on Job — CloudState can't determine execution state. Fix: add ExecutionReference, set on RunJob, update CompletionTime on finish. | fixed |
| 685 | High | Azure simulator missing SystemData on ContainerAppJob — CloudState can't read creation time. Fix: add SystemData struct, populate on job creation. | fixed |
| 686 | High | Simulators use os/exec for workloads instead of real containers — exec, archive, and filesystem operations impossible. Fix: migrate to Docker SDK container execution. | fixed |
| 687 | High | `docker logs` returns empty for CloudRun containers — simulator writes to Cloud Logging but backend log query doesn't find entries. Root cause: simulator tried to pull cloud registry URI (AR) locally. Fix: ResolveLocalImage maps AR/ECR/ACR URIs back to Docker Hub. | fixed |
| 688 | High | `docker ps` shows running CloudRun container as not running — GCP simulator missing GET /executions/{id} endpoint, CloudState couldn't fetch execution state. Fix: add GET execution endpoint. | fixed |
| 689 | Med | `docker logs` for short-lived containers may miss output — container exits before log sink flushes. Fix: wait for log drain channel before returning ProcessResult from waitAndCaptureLogs. | fixed |
| 690 | Med | Smoke test `docker stop` returns 304 for running containers — same root cause as BUG-688 (execution state not fetched). Fixed by GET execution endpoint. | fixed |
| 691 | Med | Smoke test long-running container shows empty `docker ps` — same root cause as BUG-688. Fixed by GET execution endpoint. | fixed |
