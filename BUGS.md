# Known Bugs

**911 total — 911 fixed, 0 open, 1 false positive.** Cells 5/6 (PR #123 + temp PR #124) in flight live as of 2026-05-03; running fourth iteration with all of BUG-907..911 closed. **BUG-911**: dispatcher-spawned Cloud Run Job hit Cloud Run's default 10-min `task_timeout` killing the runner-task with the github-runner mid-pipeline (cell 5 third run: 22:02:34 backend ready → 22:12:33 `Terminating task because it has reached the maximum timeout of 600 seconds`). Fix: spawner sets `TaskTemplate.Timeout = durationpb.New(3600 * time.Second)` so the Cloud Run side and the bash `RUNNER_IDLE_SECONDS=3600` are aligned. **BUG-909** (closed Phase 122d): cloudrun + gcf backends lack bind-mount → managed-volume translation (Phase-110b-equivalent for GCP). Mirror `backends/ecs/config.go::SharedVolume` + `backend_impl.go::ContainerCreate` translator using GCS buckets as the volume backing (Cloud Run Job `Volume{Gcs{Bucket}}`). Dispatcher (`github-runner-dispatcher-gcp`) adds the volume mount + `SOCKERLESS_GCP_SHARED_VOLUMES=...` env on the spawned runner Cloud Run Job. **BUG-910** (closed Phase 122d): bootstrap.sh wrapped run.sh in `timeout "${RUNNER_IDLE_SECONDS:-60}"` killing runners 60s after registration regardless of in-flight job state. Cell 5 hit it at 21:30:07 → 21:31:04 (`Container called exit(124)`). Bumped default to 3600s + dispatcher sets the env explicitly. BUG-907 (bash apostrophe), BUG-908 (`Cloud Run Jobs.CreateJob` rejects nested `Job.Name`) closed in earlier dispatcher revs. Phase 122c added the dispatcher serverless deployment (Cloud Run Service + Secret Manager `github-pat` + cell-5/6 stub PR #124).

(Older roll-up below is preserved verbatim — supersede with the line above.)

**907 total — 907 fixed, 0 open, 1 false positive.** Phase 120 (4 GCP runner cells, docker executor — no k8s/GKE/ARC) is in flight on PR #123; CI surfaced BUG-887 (sim `:` operator) and BUG-892 (integration test setup needed to build sockerless-gcf-bootstrap), both fixed. BUG-888/889/890 were attempted backend-side sim shims — REVERTED per BUG-891. Phase 121a (gcpcommon BuildService routes via REST + endpointURL) shipped clean — no backend-side sim awareness. Live-cell runs will land BUG-893+. Phase 118 closes the live-GCP track: cloudrun BUG-877..883/885 fixed; gcf BUG-884 fixed via the documented Cloud Functions Gen2 escape hatch (stub-Buildpacks-Go-source CreateFunction + post-create `Run.Services.UpdateService(image=overlay)`) with content-addressed AR overlay cache + label-based stateless reuse pool. Live-verified end-to-end on `sockerless-live-46x3zg4imo`: `docker run --rm alpine echo gcf-final-output` against gcf returns `gcf-final-output\n` (subprocess argv=[echo gcf-final-output] exit=0, stdout=17B captured by Cloud Logging). cloudrun smoke-clean: zero ghost containers, zero leaked Cloud Run Jobs. **ALL 4 RUNNER CELLS GREEN.** Cell 4 GL × Lambda closed at https://gitlab.com/e6qu/sockerless/-/pipelines/2490478943 (job 14156021860, status: success, 260.5 s) — `echo "hello from sockerless lambda"` + `date -u` ran inside a sub-task Lambda built at runtime by overlay-inject. Phase 117 closed BUG-875 (stdin-pipe-vs-/start race; Lambda's empty default Invoke payload `{}` was being piped to bash, producing `/bin/bash: line 1: {}: command not found` and Lambda `Unhandled` errors on every gitlab-runner predefined-helper container) and BUG-876 (`docker.io/library/<name>` rejected as "Docker Hub user/org image" because the `library/` prefix wasn't stripped before the user/org check). The Unhandled diagnostic itself was added in the same round: `LogType=Tail` on every `lambda.Invoke` returns the function's last 4 KB of stderr inline, and the backend now logs `payload=…` + `log_tail=…` on `FunctionError`. Cell 3 GL × ECS green at pipeline 2489246177; cell 2 GH × Lambda green at run 25113565115; cell 1 GH × ECS green at run 25075259911. Three sections: [Open](#open) / [False positives](#false-positives) / [Resolved](#resolved). Per-project rule: no bug deferral, no fakes, no fallbacks — every filed bug ships a fix in the same round. Fix detail beyond the one-liner: see `git log <commit>` or the linked PR.

**Class-of-bug — backend ↔ host primitive mismatch (CRITICAL).** Each cloud backend MUST run on its own native primitive. ECS backend in ECS, Lambda backend in Lambda, Cloud Run backend in Cloud Run, Cloud Run Functions in CRF, ACA in ACA, Azure Functions in AZF. Baking a backend into a host of a different primitive (e.g. `sockerless-backend-ecs` inside a Lambda function "to avoid Lambda-in-Lambda recursion") is a critical architectural error — the cloud's own primitives are the answer for sub-task dispatch on that cloud, even when it's harder. Future audits should re-flag any cross-pollution as a P0 bug. Documented in `MEMORY.md` workflow rules and `specs/CLOUD_RESOURCE_MAPPING.md` § runner-on-FaaS dispatch.

For narrative context see [WHAT_WE_DID.md](WHAT_WE_DID.md) and [PLAN.md](PLAN.md). Architecture-level state derivation is in [specs/CLOUD_RESOURCE_MAPPING.md](specs/CLOUD_RESOURCE_MAPPING.md) and [specs/BACKEND_STATE.md](specs/BACKEND_STATE.md).

Standing workflow rule: every CI / live-cloud failure lands here with a short root-cause line before it's fixed. Workarounds, fakes, placeholders, silent fallbacks, and incomplete implementations are all bugs and get the same treatment.

## Open

(none — BUG-909 + BUG-910 + BUG-911 closed Phase 122d. Cells 5+6 fourth iteration in flight.)

## Resolved this session (Phase 122d — BUG-909/910/911 cells 5/6 unblock)

| ID | Sev | Area | One-liner |
|----|-----|------|-----------|
| 911 | H | github-runner-dispatcher-gcp/spawner (Cloud Run Job task_timeout default 600s) | After BUG-910 lifted the bash `timeout` to 3600s, the runner-task ran 22:02:34 → 22:12:33 (~10 min) and was killed with `Terminating task because it has reached the maximum timeout of 600 seconds`. Cloud Run Jobs default `TaskTemplate.Timeout` is 10 minutes. **Fix** (`internal/spawner/spawner.go`): set `Timeout: durationpb.New(3600 * time.Second)` on `TaskTemplate` so the Cloud Run side and the in-image bash timeout are aligned. Live evidence: cell 5 third iteration https://github.com/e6qu/sockerless/actions/runs/25262761120. |
| 910 | H | tests/runners/github bootstrap.sh + dispatcher (runner timeout kills mid-job) | `exec sudo -u runner -E timeout "${RUNNER_IDLE_SECONDS:-60}" ./run.sh --once` wraps the WHOLE run.sh process in a 60s SIGTERM. The variable name implies idle-only but the timeout fires unconditionally — runners get killed 60s after registration regardless of whether a job is in flight. Cell 5 surfaced live: 21:30:00 backend ready → 21:30:07 job picked up → 21:31:04 `Container called exit(124)` (timeout's SIGTERM exit code) — 57s into the pipeline, mid `Initialize containers`. Outer GitHub Actions run hit its 10-min ceiling because the runner-task died but the workflow_run kept waiting. **Fix**: bumped default to `${RUNNER_IDLE_SECONDS:-3600}` (1h, generous upper bound for one CI pipeline; --once / --ephemeral exits naturally on job completion) on both github×{cloudrun,gcf} bootstrap.sh; dispatcher's spawner.go now sets `RUNNER_IDLE_SECONDS=3600` env on the runner Cloud Run Job explicitly so future operators don't need to know the env var. Live evidence: https://github.com/e6qu/sockerless/actions/runs/25262211481. |
| 909 | H | cloudrun + cloudrun-functions (bind-mount → GCS volume translation) | `Server.ContainerCreate` rejected every host bind mount the github-runner emits (`-v /var/run/docker.sock`, `-v /tmp/runner-work:/__w`, `-v /opt/runner/externals:/__e:ro`, plus subpaths). Phase-110b-equivalent for GCP. **Fix**: mirror `backends/ecs/config.go::SharedVolume{Name,ContainerPath,AccessPointID,FileSystemID}` + `parseSharedVolumes` + `LookupSharedVolumeBySourcePath` + `isSubPathOfSharedVolume` onto cloudrun + gcf with `SharedVolume{Name,ContainerPath,Bucket}` (GCS bucket replaces EFS access point). `ContainerCreate` translator drops `/var/run/docker.sock`, rewrites matching ContainerPath → named-volume ref, drops sub-paths, rejects anything else. The named volume → Cloud Run `Volume{Gcs{Bucket}}` plumbing already existed in cloudrun's `jobspec.go::buildJobSpec` + gcf's `volumes.go::attachVolumesToFunctionService`; `bucketForVolume` now short-circuits via `LookupSharedVolumeByName` to skip BucketManager auto-provisioning when the volume is operator-pinned. Dispatcher's `spawner.go` adds Volume{Gcs{Bucket}} + VolumeMount + `SOCKERLESS_GCP_SHARED_VOLUMES=runner-workspace=/tmp/runner-work=<bucket>,runner-externals=/opt/runner/externals=<bucket>` env on every spawned runner Cloud Run Job. Config gains `runner_workspace_bucket` REQUIRED TOML field (fail-loudly). New GCS bucket `sockerless-live-46x3zg4imo-runner-workspace` provisioned + runner SA granted `roles/storage.admin`. Live evidence (first surfaced): https://github.com/e6qu/sockerless/actions/runs/25261423675/job/74069263500. Verified via second iteration: `Initialize containers` step passed, `Set up job` reached. |

## Reverted this session (BUG-888/889/890 — backend-side sim shims violate "no fakes" rule)

| ID | Sev | Area | One-liner |
|----|-----|------|-----------|
| 908 | M | github-runner-dispatcher-gcp/spawner (Cloud Run Jobs.CreateJob nested Job.Name rejection) | Dispatcher's first poll after going live spawned cells 5+6's queued workflow_jobs but every CreateJob returned `400 Violation in CreateJobRequest.job: job.name must be empty on CreateJobRequest`. **Root cause**: `spawner.go::Spawn` set both `CreateJobRequest.JobId` (correct) AND `CreateJobRequest.Job.Name = fullName` (wrong) — the API requires the nested struct's `Name` be empty since the resource name comes from `parent + "/jobs/" + JobId`. **Fix**: drop `Name: fullName` from the nested `runpb.Job{}` literal. Verified live: post-fix dispatcher rev 00002 successfully spawned cell 5 as `gh-277cc002-263500` (https://github.com/e6qu/sockerless/actions/runs/25261423675/job/74069263500). |
| 907b | M | tests/runners/github/dockerfile-gcf (gcf runner image not rebuilt after BUG-907 text fix) | The BUG-907 apostrophe fix landed in source but the gcf runner image in AR (`runner:gcf-amd64`) was still the pre-fix build — so cell 6's spawned runner crashed with the same `unexpected EOF while looking for matching '` at line 12 of bootstrap.sh. **Fix**: `make -C tests/runners/github/dockerfile-gcf push-amd64` rebuilds + pushes; new digest `941e329988bc6eff25afe7cb1504640c19ff356762bcb7e3833e9a3a10abea0b`. Cloudrun runner had been rebuilt earlier in the session; gcf was missed. Process improvement: any bootstrap.sh / Dockerfile change must trigger `make push-amd64` for ALL 4 runner images (cloudrun + gcf × github + gitlab). |
| 907 | M | tests/runners/github (bash bootstrap apostrophe parse error) | After tightening bootstrap.sh to fail-loudly on missing required env (`: "${SOCKERLESS_GCR_PROJECT:?...the dispatcher's gcp_project label config sets this}"`), the runner image's bash entrypoint died at startup with `unexpected EOF while looking for matching '`. The apostrophe in `dispatcher's` inside the `${var:?msg}` form crosses the embedded-message lexing boundary in bash 5.2 and is treated as an unmatched single quote, even though the whole expression sits inside double quotes. Surfaced live on `gcloud run jobs execute test-runner-cloudrun` against `sockerless-live-46x3zg4imo`. **Fix**: rephrase error messages to remove apostrophes (`set by github-runner-dispatcher-gcp from the gcp_project label config`). All 4 bootstrap.sh files (github×{cloudrun,gcf}, gitlab×{cloudrun,gcf}) now `bash -n` clean. Direct `gcloud run jobs execute` retest reaches the GitHub runner-registration step (404 on dummy token, expected), proving the in-image sockerless-backend boot path works end-to-end. |
| 906 | H | simulators/gcp (ListFunctions ignored `?filter=` query) | After BUG-905 fixed image-config resolution, 4 of 12 GCF tests still failed (`TestGCFArithmetic{Invalid,Parentheses,Division,EnvVar}`) — all expecting different outputs but getting "11" (the result of `TestGCFArithmeticSuccess`'s expression). **Root cause**: sim's `GET /v2/.../functions` handler did `functions.Filter(prefix-only)` and ignored the `?filter=labels.sockerless_allocation:"<short(id)>"` query the backend's `resolveGCFFromCloud` sends. Backend's `it.Next()` returned the FIRST function (oldest = TestSuccess's), so every subsequent test invoked the WRONG function URL, ran TestSuccess's bootstrap-baked CMD, returned "11". **Fix** (`simulators/gcp/cloudfunctions.go::matchesFunctionFilter`): parse Cloud Logging-style filters: `labels.<k>:"v"` (substring/has), `labels.<k>="v"` (exact), `-labels.<k>:*` (negation+wildcard, used by `claimFreeFunction` to find free pool entries), AND-joined chains. Reuses `parseClause` + `filterClause` from `logfilter.go`. |
| 905 | H | core (parseImageTarFull misses OCI-layout tarballs) | After BUG-904 fixed the hang, GCF arithmetic tests still failed with `/bin/sh: 3 + 4 * 2: not found` — bootstrap ran user CMD as a shell command because `SOCKERLESS_USER_ENTRYPOINT` wasn't baked into the overlay. Backend's `ContainerCreate` only inherits image ENTRYPOINT when `Store.ResolveImage` finds the image — and the test-side preload (`docker save \| dockerClient.ImageLoad`) silently lost the ENTRYPOINT. Diagnostic verification step (`[testmain] backend image inspect: ... entrypoint=[] cmd=[]`) pinned this. **Root cause**: `parseImageTarFull` only indexed config-blob JSONs at the tar root (`<digest>.json`) — classic docker save layout. Modern docker (BuildKit) writes OCI v1 layout where the config blob lives under `blobs/sha256/<digest>` and `manifest.json`'s Config field points there. The classic-only filter dropped the blob silently and Config came back zero-valued. **Fix** (`backends/core/handle_images.go::parseImageTarFull`): index every file in the tar by full path, look up the config blob by the path manifest.json supplies. Same change handles layer files. Refactored to reuse the canonical `ociImageConfig` schema from `registry.go` so the in-tar (`docker save`) and over-the-wire (registry-pull) parsers share one source of truth (also picks up `User` and `ExposedPorts` that the local duplicate struct dropped). |
| 904 | H | simulators/gcp (cloudfunctions invocation: HTTP-invoke overlay, not direct exec) | After BUG-901..903 the GCF backend's overlay-and-swap completed cleanly; tests then hung on `TestGCFContainerLogs (301.35s — exact 5min `ContainerWait` timeout). **Root cause**: sim's `invokeCloudFunctionProcess` ran `docker run <overlay-uri> echo …` directly. The overlay's `ENTRYPOINT` is `/opt/sockerless/sockerless-gcf-bootstrap` (an HTTP server), so the container never exits. `handle.Wait()` blocked → backend's `invokeFunction` hung up to 10min → `WaitChs` never closed → test ContainerWait timed out. Real Cloud Run keeps the bootstrap container alive and HTTP-POSTs each invocation. **Fix** (`simulators/gcp/cloudfunctions.go` + `shared/container.go`): sim now mirrors real Cloud Run — `invokeOverlayContainerHTTP` starts the overlay container detached on a host-mapped port, polls until the bootstrap's HTTP listener is up, POSTs the invocation, reads the response body + `X-Sockerless-Exit-Code` header (so subprocess exit code propagates correctly), then stops + removes the container. New helpers `StartHTTPContainer` / `StopAndRemoveContainer` / `StreamContainerLogs` in `shared/container.go`. Process path (SimCommand-based SDK tests) unchanged — those tests don't have an overlay image. |
| 903 | H | simulators/gcp (proto-JSON enum on Condition.state) | After BUG-902 unblocked launchStage, the next 400 was `cannot unmarshal number into Go struct field Condition.terminalCondition.state of type string`. Same root cause: run/apiv2 REST PATCH sends `Condition.State` as a numeric proto-JSON enum (e.g. `2` for `CONDITION_SUCCEEDED`). **Fix** (`cloudrunjobs.go::Condition`): switch `State` from `string` to `enumString`. Existing producers in cloudrunjobs.go's execution-completion path now wrap string literals with `enumString(state)`. |
| 902 | H | simulators/gcp (proto-JSON enum on launchStage) | After BUG-901 wired the backing service, the next CI failure was `PATCH /v2/.../services/<name>` returning `400: cannot unmarshal number into Go struct field ServiceV2.launchStage of type string`. The Go run/apiv2 REST client serializes enum fields as proto-JSON numeric form on PATCH (e.g. `"launchStage": 2` for GA) — the sim already handled this for `Ingress` via the `enumString` type but `LaunchStage` was plain `string`. **Fix** (`cloudrunservices.go`, `cloudrunjobs.go`): change `LaunchStage` to `enumString` on both `ServiceV2` and `Job`. Dashboard projection cast updated to `string(j.LaunchStage)`. Same fix shape covers Jobs PATCH preemptively (run/apiv2 emits the same numeric enum for both). |
| 901 | H | simulators/gcp (cloudfunctions ↔ cloudrun service linkage) | After BUG-900 unblocked Cloud Build, the next CI failure was `failed to swap service image error="function has no underlying service"`. Real GCP Cloud Functions Gen2 are backed by a Cloud Run service that GCP creates server-side as part of CreateFunction; the gcf overlay-and-swap path relies on `function.ServiceConfig.Service` being populated to hit `Run.Services.GetService` / `UpdateService`. The sim's CreateFunction set `ServiceConfig.Uri` but not `ServiceConfig.Service`, and didn't seed a backing `ServiceV2` row — so swap-time `GetService` 404'd. **Fix** (`simulators/gcp/cloudfunctions.go::CreateFunction`): stamp `ServiceConfig.Service`, then call a new shared `seedServiceV2Defaults` helper (extracted from `cloudrunservices.go::CreateService`) to insert a backing service with the stub image. `invokeCloudFunctionProcess` now reads the container image from the underlying service (`crv2Services.Get`), which is exactly how real GCF Gen2 invocation pulls the image — the swap happens via UpdateService PATCH so the sim sees the overlay image after the gcf backend completes its swap. |
| 900 | H | cloudrun-functions (integration test setup) | CI `test` job: GCF integration tests (`TestGCFContainerStopNoOp`, `TestGCFContainerExec`, `TestGCFContainerLifecycle`) failed because the overlay Dockerfile generated by the backend has `FROM us-central1-docker.pkg.dev/sockerless-test/docker-hub/library/alpine:latest` (the AR-rewritten form of the test's plain `alpine:latest` request). The sim's Cloud Build executor runs real `docker build` against the local daemon, which tries to fetch metadata from real AR for `sockerless-test` (a non-existent project) and gets `403 Forbidden`. **Fix** (`integration_test.go::TestMain`): pre-pull `alpine:latest` and `docker tag` it to the AR URL. Cloud Build's `FROM` then resolves locally without an AR network call. Mirrors the existing eval-arithmetic AR-tag step (BUG-898). |
| 893..899 | M-H | sim (gcp) hardening + integration test scaffolding | Phase 121 sim hardening sweep: BUG-893 absolute-path for sockerless-gcf-bootstrap; BUG-894 pre-create `sockerless-test-build` GCS bucket via REST POST in TestMain; BUG-895 sim OAuth2 endpoint `/token` + `/oauth2/v4/token` mints real-shape HS256 JWT (header.payload.signature) so `cloudbuild.NewRESTClient` accepts the access token; BUG-896 sim GCS persists object bytes on disk (sim.Store JSON encoding strips unexported `data` field — survives round-trip via `<gcsHostRoot>/<bucket>/<object>`); BUG-897 sim cloudbuild `bytesReader` had broken value-receiver Read semantics (replaced with `bytes.NewReader`); BUG-898 eval-arithmetic image AR-tagged so Cloud Build `FROM` resolves locally; BUG-899 sim `runDockerStep` intercepts `docker push` and verifies local image presence (sim is local-only, no AR push). All seven landed sim/test side per the no-backend-shims rule (BUG-891). |
| 892 | M | cloudrun-functions (integration test setup) | Phase 121a unblocked the gcf integration tests' build-service path; next failure was `tar overlay context: open /opt/sockerless/sockerless-gcf-bootstrap: no such file or directory`. The backend's overlay-build path stages the gcf bootstrap binary into the Cloud Build context tar (real Phase 118 design); the test setup wasn't building it. **Fix** (`integration_test.go::TestMain`): build `sockerless-gcf-bootstrap` to a tempfile, set `SOCKERLESS_GCF_BOOTSTRAP=<path>` in the backend env. Mirrors the existing eval-arithmetic + simulator-gcp + sockerless-backend-gcf build steps already in TestMain. |
| 891 | H | cloudrun-functions (revert sim shims) | The sim-mode bypasses I added in BUG-888/889/890 (EndpointURL-gated branches in Validate, ContainerCreate, invokeFunction) violate the project's no-fakes / no-fallbacks / no-shims rule. Backends MUST never know whether they're talking to a real cloud or a simulator. The simulator MUST present a faithful API surface so backends behave identically against either. **Revert** all three EndpointURL branches; the gcf integration tests are now expected to fail until **Phase 121** adds the missing sim pieces (Cloud Build endpoint, IAM/token endpoint, invocation auth acceptance). Lambda's existing EndpointURL bypass (`backends/lambda/backend_impl.go:257-267`) is also tech debt and is flagged for removal as part of Phase 121's sister sim hardening pass. BUG-887 (sim `:` operator) stays — that fix is on the simulator side, which is the correct location. |

## Resolved this session (Phase 120 CI smoke fix — BUG-887)

| ID | Sev | Area | One-liner |
|----|-----|------|-----------|
| 887 | H | simulators/gcp (logfilter) | The BUG-878 fix changed the cloudrun + gcf backends to use Cloud Logging's `:` substring operator (`logName:"run.googleapis.com"`) to filter out Cloud Audit Logs. Real Cloud Logging supports `:`; the sim's `parseClause` did not. Without that case in the operator switch, the third clause of every `docker logs` filter fell through to the wildcard branch and matched ZERO entries — silently dropping every container's stdout in the smoke test (`docker logs smoke-short` returned empty after `echo "hello from smoke test"` exited cleanly). Surfaced as a smoke-job CI failure on PR #123 immediately after the Phase 118 commit landed. **Fix** (`simulators/gcp/logfilter.go`): add `opHas` to the operator enum, parse `:` as a clause operator (after `=`/`>`/`>=` so timestamp values containing `:` still parse as equality), and implement `opHas` in `matchesFilter` as `strings.Contains(val, c.value)`. Tests cover the `logName:"run.googleapis.com"` clause, the compound real-cloudrun-filter shape, the negative case (`logName:"cloudaudit.googleapis.com"` → false), and the timestamp-with-colon edge case. |

## Resolved this session (Live-GCP cloudrun manual sweep — BUG-877..886)

| ID | Sev | Area | One-liner |
|----|-----|------|-----------|
| 886 | H | core (cloud-logs attach burst loss) | `core.StreamCloudLogs` follow-mode loop intermittently lost entries from a fast-exit container emitting many lines in a tight burst (cloudrun manual sweep bundle-O: 11 markers all in Cloud Logging at sub-millisecond-spread timestamps, but only the first reached the docker client). Combined fix: cursor refactored from strict `timestamp>lastTS` to `>=lastTS` to avoid dropping tied-timestamp entries; per-entry `seen[<unix-nano>:<message>]` dedup so the `>=` change doesn't emit duplicates; final-fetch settle window bumped to 6×3s = 18s after terminal-state detection (Cloud Logging worst-case ingestion lag for batched final-flush is ~15s on Cloud Run); pipe-close detection on every `io.WriteString` so we exit the loop on closed pipe instead of silently swallowing entries. Verified live: `manual-test-real-workloads.sh cloudrun` ALL 16 ROWS PASS including bundle-O fast-burst case. |


| ID | Sev | Area | One-liner |
|----|-----|------|-----------|
| 885 | H | core (cloud-logs final-fetch race) | `core.StreamCloudLogs` follow-mode loop's "Final fetch before exit" branch fired immediately when `ResolveContainerAuto` returned a terminal state — but Cloud Logging (and CloudWatch, Log Analytics) all have ~1-2 s ingestion latency, so a fast-exit container's last stdout line wasn't yet queryable when the final fetch ran. Repro: 3 parallel `docker run --rm` invocations (`pwd`, `echo MYV=$MYV`, `echo retest-1`) — all three completed cleanly on Cloud Run (verified via `gcloud logging read` showing the stdout lines) but `docker run` only surfaced the line for the slowest (longest-running) container. Two of three docker clients got only `Container called exit(0).` with no actual stdout. **Fix** (`core/log_driver.go::StreamCloudLogs`): on terminal-state detection, sleep `2 * time.Second` BEFORE the final fetch, then fetch with the cursor. Lets Cloud Logging finish ingesting the container's final lines. Same pattern as the existing post-stop `WaitFor*` polls for execution exit. |
| 883 | H | cloudrun (--rm cleanup) | `docker run --rm` did NOT delete the Cloud Run Job after the execution completed: 4 sequential `--rm` runs left 4 Jobs alive in the project. The docker CLI's post-wait DELETE doesn't reliably fire when /attach holds the connection open, so cleanup must be backend-driven. **Fix** (`cloudrun/containers.go::pollExecutionExit`): on `exec.CompletionTime != nil`, after closing the wait channel, call `maybeAutoRemove(id)` which reads `HostConfig.AutoRemove` from CloudState and invokes `ContainerRemove(id, false)` on a true value. AutoRemove rides via `core.TagSet.AutoRemove` → GCP label/annotation `sockerless_auto_remove=true` so it survives `PendingCreates.Delete(id)` and is observable by the poller through `ResolveContainerAuto`. Verified live: post-fix, three parallel `--rm` runs leave zero ghost containers and zero leaked Cloud Run Jobs. |
| 882 | M | cloudrun (cloud-state synthesis) | `docker ps -a` showed empty `""` Command on terminal Cloud Run Job entries served from CloudState (post `PendingCreates.Delete(id)`). The docker `COMMAND` column reads from `Container.Path + Container.Args`, but `cloud_state.go::jobToContainer` populated only `Config.Cmd` / `Config.Entrypoint`. **Fix**: synthesise `Path` from entrypoint[0] (or cmd[0]) and `Args` from the remainder, mirroring `backend_impl.go::ContainerCreate`. |
| 881 | H | cloudrun (cloud-state stale) | After a CreateJob rollback (image-not-found pre-BUG-877 fix; or any RunJob failure), the container row stayed in `docker ps -a` indefinitely as `Created` because rollback paths only deleted the cloud-side resource and not `PendingCreates` / `CloudRun`. **Fix** (`backend_impl.go::ContainerStart`): the three rollback paths (createOp.Wait error, RunJob error, runOp.Wait error) now also `s.PendingCreates.Delete(id)` + `s.CloudRun.Delete(id)` after `deleteJob`, keeping local state in sync with the cloud-side teardown. The success-path equivalent (BUG-883) is handled by `pollExecutionExit::maybeAutoRemove`. |
| 880 | H | cloudrun (cloud-state list) | `docker ps -a` returned duplicate rows for the same container ID. `cloud_state.go::ListContainers` merged `PendingCreates` + `queryJobs` (+ optional `queryServices`) but only deduplicated `cloud → pending`, not within-source. **Fix**: maintain a `seen[id]` set across all three sources; skip any ID we've already returned. |
| 879 | H | cloudrun (logs) | After a Cloud Run Job execution finished, `docker logs <id>` returned empty. Indirectly fixed by BUG-878 — the same broken `logName` filter applied to the post-mortem fetch path; once the filter restricts to `run.googleapis.com` runtime logs, the Cloud Logging Entries iterator returns the actual stdout entries. Verified live: `docker logs post-mortem` returns `this-is-the-real-output\nContainer called exit(0).` cleanly. |
| 878 | H | cloudrun + gcf (logs filter) | `buildCloudLogsFetcher` filter was `resource.type="cloud_run_job" AND resource.labels.job_name="<name>"` — no `logName` clause. Cloud Audit Logs (`cloudaudit.googleapis.com/{activity,data_access}`) for Cloud Run Jobs share that resource.type, so they matched and fell through to `extractLogLine`'s `fmt.Sprintf("%v", payload)` which textproto-dumped the `google.cloud.audit.AuditLog` payload into `docker run` stdout — a 7 KB flood of `service_name:"run.googleapis.com" method_name:"…" …` for every container. **Fix**: add `AND logName:"run.googleapis.com"` (substring match — the canonical exact form `logName=("projects/…/logs/run.googleapis.com%2Fstdout" OR …)` was attempted first but the cloud.google.com/go logadmin client double-encodes `%`, so the canonical form silently matched zero entries; substring works because `cloudaudit.googleapis.com` is the only other family at that scope). Hard-rejected non-string Payloads in `extractLogLine` (returns `""` instead of `%v`-stringifying) so a misconfigured filter can never leak audit-log protos again. Applied to both cloudrun and gcf backends. Verified live: `docker run --rm alpine echo hello-after-fix` now returns clean `hello-after-fix\nContainer called exit(0).` (284 bytes vs 7235 bytes pre-fix). |
| 877 | H | cloudrun + gcf (image-resolve infra) | `gcpcommon.ResolveGCPImageURI` rewrites Docker Hub refs to `{region}-docker.pkg.dev/{project}/docker-hub/{repo}:{tag}` and the source comment said the remote repository "must be pre-configured at the project level" — but nothing in the project actually created it. `terraform/modules/cloudrun/main.tf` created a regular `${name_prefix}-repo` AR repo, not a remote-Docker-Hub-proxy AR repo named `docker-hub`. Live effect: every `docker run alpine` against the cloudrun backend produced `Cloud Run Job: Image '…/docker-hub/library/alpine:latest' not found` — 100% broken on first contact with real GCP. **Fix**: `terraform/modules/{cloudrun,gcf}/main.tf` now create a second `google_artifact_registry_repository "docker_hub"` with `mode = "REMOTE_REPOSITORY"` + `remote_repository_config { docker_repository { public_repository = "DOCKER_HUB" } }`. Verified live (after manual `gcloud artifacts repositories create docker-hub --mode=remote-repository --remote-docker-repo=DOCKER-HUB`): pull succeeds, alpine runs, exit 0. |

## Resolved this session (Phase 117 — Cell 4 GL × Lambda)

| ID | Sev | Area | One-liner |
|----|-----|------|-----------|
| 876 | H | lambda (image-resolve) | `resolveImageURI` rejected `docker.io/library/alpine:latest` as "Docker Hub user/org image" because `parseDockerRef` returns `repo="library/alpine"` (with slash), and the user/org guard `strings.Contains(repo, "/")` fired before the library-image rewrite. gitlab-runner emits the fully-qualified `docker.io/library/<name>` form when scheduling stage containers, so cell 4's build-stage container creation always failed with the user/org error. **Fix**: strip `library/` prefix immediately when `registry` is `""`, `docker.io`, or `registry-1.docker.io`, then run the user/org check on the trimmed repo. The library-image rewrite below the check still re-prepends `docker/library/` for the AWS Public Gallery routing. Verified live: cell 4 pipeline 2490478943 green. |
| 875 | H | lambda (start/attach race) | `Server.ContainerStart` did a one-shot `stdinPipes.Load(id)` to capture the attach pipe before firing the Invoke goroutine, but the Docker SDK's standard sequence is /create → /start → /attach — so `/start` arrives BEFORE `/attach` registers the pipe. With OpenStdin set but no pipe captured, the goroutine invoked Lambda with `Payload: nil`, which AWS encodes as `{}`. The bootstrap's `runUserInvocation` then piped `{}` into bash's stdin (the gitlab-runner shell-finder Cmd execs `bash`, which reads commands from stdin), bash tried to execute `{}` as a command, exited 1, and Lambda classified the result as `Unhandled`. Every gitlab-runner predefined-helper container hit this; cycles 1+2 (volume-permission containers without OpenStdin) didn't because the natural Cmd is a one-shot `chown`. **Fix** (`backends/lambda/backend_impl.go`): move pipe lookup INSIDE the goroutine and poll `stdinPipes` for up to 5 s (50 ms tick) before deciding whether OpenStdin callers will deliver a script. If no pipe shows up by then we log a warning + fall through to empty-payload Invoke. Diagnostic ride-along: `LogType=Tail` on the Invoke API call, plus a payload/log_tail dump on FunctionError — that's how the `{}: command not found` line surfaced. Verified live: cell 4 pipeline 2490478943 GREEN at `--- PASS: TestGitLab_Lambda_Hello (310.15s)`. |

## Resolved earlier (PR #122 — CI green at `88aca1e`)

| ID | Sev | Area | One-liner |
|----|-----|------|-----------|
| 868 | H | ecs (gitlab-runner lifecycle) | **Cell 3 GREEN** at https://gitlab.com/e6qu/sockerless/-/pipelines/2489246177 (job 14148678472, status: success, 270.8 s). gitlab-runner's docker-executor reuses one container ID across stages with /attach + /start per stage; on ECS that maps to a fresh per-stage Fargate task via the existing `launchAfterStdin` deferred-RunTask flow (each stage = new task with the stage's script baked into Cmd). Three live-AWS bugs were preventing cycle 2+ from working — all fixed in commit `aa2419a`: (1) `launchAfterStdin` short-lived AND running branches kept `PendingCreates`, so /inspect returned stale `Status="created"` instead of CloudState's `Status="exited"` and the cloud-logs follower in /attach never EOFed; both branches now drop PendingCreates after the per-stage task is observable. (2) Cycle 2's cloud-logs follower started polling /inspect BEFORE /start fired `markRunning`, saw the previous stage's `Status="exited"`, and EOFed immediately — fixed via a `ContainerInspect` override that forces `Status="running"` while `Store.WaitChs[id]` is open. (3) /attach-vs-/start race — added a 5-s stage-boundary barrier in the attach driver that waits for /start to register a fresh WaitCh before kicking off the cloud-logs follower. Plus a 30-s `pipe.Done()` timeout in `launchAfterStdin` for log-streaming /attach callers that never half-close. Verified live: prepare_script → get_sources → restore_cache → download_artifacts → step_script (`echo "hello from sockerless ecs"` + `env | sort`) → after_script → archive_cache → upload_artifacts_on_success → cleanup_file_variables all succeeded on 9 sequential per-stage Fargate tasks. The earlier BUG-867 `-predefined` filter is no longer needed and was removed. |

| ID | Sev | Area | One-liner |
|----|-----|------|-----------|
| 874 | H | lambda (start/exec lifecycle) | Lambda has no synchronous "start" primitive and no inbound channel — so the GitHub runner's `docker create + start + exec` (and gitlab-runner's `start-attach-script`) fundamentally don't map to a single Lambda invocation. Phase 116 fix: register a single deployment-time exec driver. When `SOCKERLESS_CALLBACK_URL` is set, sockerless wires `core.ReverseAgentExecDriver` (Path A — bootstrap dials back via WebSocket). When unset, sockerless wires the new `lambdaInvokeExecDriver` (Path B — each `docker exec` triggers a fresh `lambda.Invoke` whose payload is a JSON envelope `{"sockerless":{"exec":{"argv":[...],...}}}`; the bootstrap parses, runs the cmd, returns `{"sockerlessExecResult":{"exitCode":N,"stdout":"…","stderr":"…"}}`). The lambda backend tunnels stdout+stderr into the docker-exec hijacked conn with proper multiplexed-stream framing for non-tty execs. Single-option-per-deployment, no runtime fallback. ContainerStart now blocks synchronously on `FunctionActiveV2Waiter` so the runner's exec doesn't race a Pending function. Workdir + bind-link symlinks moved off Lambda's ImageConfig (read-only root fs, pre-bootstrap chdir) and onto bake-time RUN ln -sfn directives in the overlay Dockerfile. Verified live: cell 2 GH × Lambda GREEN at workflow run https://github.com/e6qu/sockerless/actions/runs/25113565115; sub-task Lambda log shows `bootstrap exec: argv=[sh -e /__w/_temp/...sh] workdir=/__w/sockerless/sockerless env=44-vars stdin=0-bytes` then `hello from sockerless lambda` then `Wed Apr 29 14:04:48 UTC 2026` (real subprocess output, two warm invocations, exit 0). |
| 873 | H | lambda (image-mode + image-inject) | Lambda image-mode rejects OCI manifests AND requires a Runtime API client at the entrypoint. Both fall to the same architectural fix (Phase 115): route ALL Lambda CreateFunction calls through `BuildAndPushOverlayImage` overlay-inject, swapping `os/exec docker build` for `awscommon.CodeBuildService` so it works inside the runner-Lambda (no docker daemon). Cache converted images by source-content hash via `OverlayContentTag(spec)` so identical inputs reuse a tag. The overlay's plain `docker build` + `docker push` produces Docker schema 2 (resolves manifest gap); `sockerless-lambda-bootstrap` is the ENTRYPOINT (resolves Runtime-API gap); user's original `Entrypoint`+`Cmd` ride along as `SOCKERLESS_USER_*` env vars. Verified live (workflow run 25105165208): CodeBuild SUCCEEDED + CreateFunction returned the ARN. (Cell 2 still blocks on BUG-874 — start/exec lifecycle.) |
| 872 | H | lambda (image-resolve / pull-through cache prefix) | `resolveImageURI` hardcoded the pull-through cache prefix as `ecr-public`, while the ECS resolver (and the convention used elsewhere in the codebase) computes prefix as `strings.ReplaceAll(registry, ".", "-")` — so `public.ecr.aws` → `public-ecr-aws`. The two prefixes give two SIBLING ECR cache repos with the same upstream but separate caches; only `public-ecr-aws` had been populated (from prior cell-1 runs). Lambda CreateFunction looking under `ecr-public/docker/library/alpine` got `InvalidParameterValueException: Source image … does not exist`. **Fix**: derive lambda's prefix the same way as ECS — `strings.ReplaceAll(upstreamURL, ".", "-")`. Now both backends share one cache repo. Verified live: Lambda's CreateFunction now reaches the manifest-format check (BUG-873) instead of failing on missing image. |
| 871 | H | lambda (volumes / FileSystemConfigs) | Lambda allows AT MOST 1 FileSystemConfig per function and `localMountPath` must match `/mnt/[A-Za-z0-9_.\-]+` (verified live with a 3-line `ValidationException`). The runner's bind-mount translation produced 2 entries (workspace + externals) at `/__w` + `/__e`, which Lambda rejects on every count. **Fix** (specs/CLOUD_RESOURCE_MAPPING.md § "Lambda bind-mount translation"): (1) `backends/lambda/volumes.go::fileSystemConfigsForBinds` collapses entries with identical AP ARN into one FSC mounted at canonical `/mnt/sockerless-shared`; rejects multiple distinct ARNs with a clear error pointing at the Lambda single-FSC constraint. (2) `SharedVolume` gains `EFSSubpath`; the parser accepts a 5-part `name=path=apID==subpath` form. (3) Lambda backend emits `SOCKERLESS_LAMBDA_BIND_LINKS` env on sub-task `CreateFunction` with `<dst>=<mnt-target>` mappings. (4) `agent/cmd/sockerless-lambda-bootstrap` parses BIND_LINKS and creates symlinks before exec'ing user entrypoint (idempotent across execution-environment reuse). (5) runner-Lambda `terraform/modules/lambda/runner.tf` SHARED_VOLUMES now declares subpaths (`workspace=…==_work`, `externals=…==externals`) and `bootstrap.sh` writes both onto EFS subpaths under `/mnt/runner-workspace/{_work,externals}` so the sub-task Lambda — which mounts the same access point — sees the same files. Doc + code + bootstrap unit tests landed; live verification gated on BUG-872. |
| 870 | H | aws-common (EFS) / lambda (volumes) | `accessPointARN` resolved access-point IDs by iterating `ListManagedAccessPoints`, which only returns access points carrying the `sockerless-managed=true` tag. Operator-provisioned access points (e.g. `sockerless-live-runner-workspace` from terraform) don't carry that tag, so cell 2's GitHub-on-Lambda runner failed at `Initialize containers`: `resolve access point ARN fsap-…: access point fsap-… not found on filesystem fs-…`. **Fix**: add `EFSManager.DescribeAccessPoint(apID)` in `backends/aws-common/volumes.go` that calls `efs.DescribeAccessPoints(AccessPointId=apID)` directly — no managed-tag filter, no full-list scan. Lambda backend's `accessPointARN` now uses it. The managed-tag filter is still right for sockerless-owned volume CRUD; it was wrong for ARN resolution of operator-pre-provisioned APs. |
| 869 | H | terraform / codebuild buildspec | `aws lambda update-function-code` rejected images produced by the runner-Lambda CodeBuild project: `InvalidParameterValueException: The image manifest, config or layer media type for the source image … is not supported.` Root cause: the inline buildspec used `docker buildx build --push` which defaults to OCI media types + provenance attestation. Lambda image-mode requires Docker schema 2 manifests. **Fix**: in `terraform/modules/lambda/codebuild.tf` switch the build line to `docker buildx build --provenance=false --output type=image,name=$IMAGE_URI,oci-mediatypes=false,push=true …`. Applied. |
| 862 | **CRITICAL** | architecture / runner-lambda | Runner-Lambda image baked `sockerless-backend-ecs` and dispatched `container:` sub-tasks via `ecs.RunTask` to Fargate — backend ↔ host primitive mismatch. Project rule (now top of BUGS.md + MEMORY.md + CLOUD_RESOURCE_MAPPING.md universal rule #9): each backend runs on its own native primitive. Source fix landed (Dockerfile, bootstrap, terraform IAM + env vars, agent + bootstrap binaries staged into image, CodeBuild + S3 build-context for runtime image-inject). Verified on a live runner-Lambda invocation past the pre-fix wall (Initialize containers reaches the docker create step). |
| 861 | H | runner-lambda image + lambda backend | Bundled with BUG-862. Cell run surfaced "host bind mounts not supported on ECS backend" for `/tmp/runner-state/externals:/__e:ro`; root cause was BUG-862 (wrong backend baked); fix lands together with BUG-862's terraform `SOCKERLESS_LAMBDA_SHARED_VOLUMES` carrying both workspace + externals paths. |
| 867 | H | ecs / lambda (attach stdin) | BUG-859/866 deferred-stdin path captured stdin and baked into the task command for ANY container with `OpenStdin && AttachStdin && pipe-loaded`. Worked for the gitlab-runner user-script container (the bytes ARE the script) but broke the gitlab-runner predefined helper container — gitlab-runner uses `/attach` for log streaming on the helper, not script delivery, so capturing those bytes and baking them as the helper's command terminated it after the brief probe instead of leaving it long-running. The gitlab-runner pipeline hit GitLab's 1-h cap waiting for `Preparing environment` to finish. **Fix**: skip the deferred path when the container's name has the gitlab-runner `-predefined` suffix. New `isGitlabRunnerPredefined(name)` helper in both backends; the predefined helper goes through synchronous `RunTask` (ECS) / `Invoke` (Lambda) without script-baking. The user-script container (different name, no `-predefined`) still gets the deferred-stdin path. |
| 866 | H | ecs / lambda (attach stdin) | Two-version fix. **v1**: when `ECSState.OpenStdin` was set but no attach happened within 2 s, `ContainerStart` returned `InvalidParameterError` — gitlab-runner's predefined helper container (created with `OpenStdin=true` for consistency but never piped stdin) hung pipelines with "system failure: aborted". v1 fix replaced the error with fall-through to synchronous flow — but produced a worse symptom: start returned in 10 µs (no synchronous RunTask), runner stuck in "Preparing environment". Root cause was indeterminate; the 2-second poll loop should have either found a non-nil pipe or timed out cleanly. **v2** (commit `88aca1e`): drop the blocking wait entirely. Only enter the deferred-stdin path when a pipe is **already** registered at start time (`stdinPipes.Load(id)` non-nil + `pipe.IsOpen()`); otherwise take the synchronous flow. Containers gitlab-runner pipes stdin to (the user-script container) sequence /attach far enough ahead of /start that the pipe is always there. Cell-3 verified: helper container's start now takes 25 s (real synchronous RunTask) and proceeds through all script stages. Surfaced live by `tests/runners/gitlab` cell-3 run pipeline 2486787794 (2026-04-29). |
| 865 | H | ecs (image-resolve) | BUG-846 routes Docker Hub library refs (`alpine`, `node:20`, etc.) to `public.ecr.aws/docker/library/<name>:<tag>`. The routing fires for ANY unqualified image name without checking the local Store first. Locally-built test images (`sockerless-eval-arithmetic:test`) get rewritten to `public.ecr.aws/docker/library/sockerless-eval-arithmetic:test` — Public ECR doesn't host that, the simulator's `ResolveLocalImage` only strips ECR pull-through cache URIs (not Public Gallery), and the container ends up running a fallback/wrong image. Symptom: arithmetic integration tests on PR #122 CI (job `test`) and locally on macOS Podman fail with exit code -1 + logs containing only the input echoed (eval-arithmetic binary never ran). **Fix**: in `resolveImageURI`, check `s.Store.ResolveImage(ref)` BEFORE routing to Public Gallery; if the image is in the local Store, return the ref as-is so Fargate/Podman uses the local copy. Same project rule: don't reroute through a public registry when the image is already available locally. |
| 864 | L | terraform tests | `TestECSModule_PlanResourceCount` uses `assert.NotContains(t, planLine, "0 to add")` to verify the plan creates resources, but substring-matches `"0 to add"` against `"40 to add"` (the "0" in "40"), causing a false-positive failure when the module legitimately plans 40 adds. **Fix**: replace with regex `(^|\s)0 to add` so the leading word boundary blocks the substring match. |
| 863 | M | tests/integration + smoke | `tests/.../TestMain` (eval-arithmetic, others) and `smoke-tests/Dockerfile.ecs` launch `sockerless-backend-ecs` without setting `SOCKERLESS_ECS_CPU_ARCHITECTURE`, which BUG-848 made mandatory (no default). Backend exits at startup: `invalid configuration ... SOCKERLESS_ECS_CPU_ARCHITECTURE must be set to X86_64 or ARM64`. Test failures: `backend not ready: timeout waiting for http://localhost:57234/internal/v1/info` (e2e) + `Timed out waiting for http://127.0.0.1:3375/_ping` (smoke). Surfaced live by PR #122 CI on 2026-04-29 (jobs `test`, `test (e2e)`, `smoke`). **Fix**: set `SOCKERLESS_ECS_CPU_ARCHITECTURE=X86_64` (and `SOCKERLESS_LAMBDA_ARCHITECTURE=x86_64`) in tests/main_test.go + tests/sim_harness_test.go + smoke-tests/Dockerfile.ecs's entrypoint that builds + launches each backend. |

## False positives

Findings that look like bugs under the "no fakes, no workarounds" principle but are legitimate by design. Listed so future audits don't re-flag them.

| Area | Finding | Why it's not a bug |
|------|---------|--------------------|
| `backends/aca/azure.go::fakeCredential` | Returns a literal `"fake-token"` as bearer token against simulator endpoints. | Simulators intentionally don't verify bearer tokens — token verification requires a real Azure AD endpoint that the sim doesn't emulate. The credential is only wired via `newAzureClientsWithEndpoint` (the simulator path); production uses `azidentity.NewDefaultAzureCredential`. |

## Resolved

One-liner per bug. Latest first; pre-Round-7 entries are extra-terse (full detail in `git log` / PR descriptions).

### Phase 110 prep (PR #122 — open) (BUG-845..858)

| ID | Sev | Area | One-liner |
|----|-----|------|-----------|
| 860 | H | lambda (attach stdin) | Same root cause as BUG-859 in the Lambda backend: gitlab-runner pipes user-script bytes through `docker attach` stdin; sockerless's `core.NewCloudLogsAttachDriver` (read-only) discarded them and the Lambda invocation ran with no script. **Fix**: mirror the ECS pattern — typed `lambdaStdinAttachDriver` captures stdin into a per-cycle `stdinPipe`; ContainerStart, when LambdaState records `OpenStdin`, waits briefly for the pipe to open then defers `lambda.Invoke` via the same goroutine; on stdin EOF the buffered bytes go straight into `InvokeInput.Payload`. The bootstrap's existing `runUserInvocation` already pipes Payload to the user entrypoint as stdin, so `Cmd=[sh]` + Payload=script naturally runs the script — no bootstrap changes needed. Unit tests cover the pipe mechanics. |
| 859 | H | ecs (attach stdin) | gitlab-runner's docker executor delivers each script step to the user container by piping the script through the hijacked `docker attach` connection's stdin. Sockerless's ECS attach driver was `core.NewCloudLogsAttachDriver` (read-only — `cloudAttachStream.Write` discards) so every byte gitlab-runner wrote vanished and the container's `sh` exited 1 in <1 s with no stdout. **Fix**: a custom typed attach driver (`ecsStdinAttachDriver`) that reads CloudWatch logs out and pumps stdin into a per-cycle `stdinPipe`. ContainerStart, when ECSState records OpenStdin, defers RunTask via a goroutine that waits for the pipe to close (caller half-close after streaming the script), then bakes the buffered bytes into the task definition's `Entrypoint=[sh,-c]` + `Cmd=[<script>]` and runs the task. Per-cycle: each gitlab-runner script step gets a fresh pipe (`launchAfterStdin` deletes the pipe after consuming, so the next attach creates a new one); PendingCreates is preserved across cycles because `CloudState.containerFromTask` synthesises Config without Binds/VolumesFrom/OpenStdin. ECSState gains an `OpenStdin` field (persists the flag across the start/stop/start cycles since CloudState doesn't roundtrip it). Unit tests cover the pipe mechanics; integration verified by the cell-3 GitLab × ECS pipeline. |
| 858 | M | ecs (container lifecycle) | gitlab-runner does `docker create + docker start + docker stop + docker start` (re-runs the predefined helper container across script stages). The second start returned 404 because `ContainerStart` only checked PendingCreates; once cleared, no fallback. **Fix**: `ContainerStart` falls back to `ResolveContainerAuto` (which checks PendingCreates → CloudState → Store) and restores the resolved container to PendingCreates so the rest of the flow proceeds. Also: PendingCreates is preserved through `waitForTaskRunning` instead of being cleared at the start of the wait — eliminates a related post-start-pre-CloudState-visibility race. Cell 3 GitLab × ECS now progresses all the way through helper image pull, env prep, git source fetch, cleanup_file_variables, and into user-script container start (next failure is BUG-859). |
| 857 | M | tests/runners (gitlab) | gitlab-runner's `setVolumePermissions` step needs the gitlab-runner-helper image to fix volume permissions on cache mounts. The runner default pulls it from `registry.gitlab.com/gitlab-org/gitlab-runner/gitlab-runner-helper:<tag>`, which AWS ECR pull-through cache can't proxy without Secrets Manager auth (sockerless project rule: no-credentials-on-disk). Fix: pre-push the helper image to the live ECR repo (`sockerless-live:gitlab-runner-helper-amd64`) at harness setup time and configure gitlab-runner with `--docker-helper-image=<ecr-uri>` so it pulls from sockerless-routable ECR. Mirror image; same shape as the runner-image-on-ECR pattern used for cell 1's runner-task. |
| 856 | M | terraform / runner-lambda | The Lambda runner-image bootstrap stages `actions/runner` to `/tmp/runner-state/` (Lambda's image filesystem is read-only; `/tmp` is the only writable area outside EFS), so the runner's bind-mount sources for the `container:` directive end up as `/tmp/runner-state/_work`, `/tmp/runner-state/externals`, etc. But Terraform's `SOCKERLESS_ECS_SHARED_VOLUMES` env var on the Lambda function mapped the legacy `/home/runner/_work` path (copied from cell 1's ECS shape). Result: sockerless inside the Lambda saw the runner's `docker create -v /tmp/runner-state/_work:/__w …` and rejected as host-bind-mount because the source path didn't match any configured shared volume. Fix: align the env var to `/tmp/runner-state/_work` and add a separate entry for `/tmp/runner-state/externals` (same access point root since Lambda's single `file_system_config` constraint forces a combined access point). |
| 855 | M | aws-common / volumes | sockerless's per-volume EFS access points use rootDirectory.path = `/sockerless/volumes/<volname>`. Volume names from gitlab-runner's docker executor follow `runner-<id>-project-<id>-concurrent-<n>-<hex>-cache-<sha>` — typically 90+ chars — and the prepended prefix pushes the path past AWS EFS's 100-char limit on `rootDirectory.path`, surfacing as `ValidationException: Member must have length less than or equal to 100`. Fix: when the full sanitised path would overflow, fall back to `/sockerless/v/<sha256(volname)[:16]>` — a deterministic, short, collision-resistant alternative. Existing access points keep their long paths; only newly-created ones use the short form when needed. |
| 854 | M | ecs / image-resolve | gitlab-runner's `setVolumePermissions` step (a small helper container that fixes permissions on the cache volume before each job) references its image by sha256 digest only (e.g. `sha256:50d8f7aab0bdec1b0a39ef47c23983d1ab0089e57cea32549b4bd80919ee8610`). Sockerless's `resolveImageURI` (BUG-846) routes Docker Hub library refs to `public.ecr.aws/docker/library/<name>:<tag>` — but for a digest-only ref with no name, the routing produced `public.ecr.aws/docker/library/sha256:50d8...` which 404s and triggered a 7×-retry pull failure on Fargate. Fix: in `backends/ecs/image_resolve.go`, detect digest-only refs (`sha256:` prefix with no `/` or `@`) and (a) try to resolve via sockerless's local image Store (caller may have pulled the image previously with name+digest, in which case sockerless has the canonical RepoTag and recurses into the normal path), or (b) surface a clear error pointing the caller at the `name@sha256:...` form so they can specify the canonical reference. Misrouting via Docker Hub is no longer possible. Surfaced in cell 3 (GitLab × ECS) on the gitlab-runner cache-permission helper; cell 3 architecture otherwise works (gitlab-runner registered, pipeline triggered, runner picked it up). |
| 853 | H | ecs (exec) | `cloudExecStart` (called by the typed exec dispatcher when the GitHub Actions runner does `docker exec` to run a workflow step) called `ExecuteCommand` immediately after the spawned sub-task reached RUNNING. AWS Fargate's `ExecuteCommandAgent` managed agent takes 5–30 s after the task transitions to RUNNING to come up; calling `ExecuteCommand` before then returned `InvalidParameterException: ... execute command agent isn't running. Wait and try again`. The runner saw exit code 255 from docker exec and failed every workflow step. Fix: call existing `waitForExecuteCommandAgentReady` (which polls `DescribeTasks` for `ManagedAgents[ExecuteCommandAgent].LastStatus == RUNNING`) before invoking `ExecuteCommand`. Same wait was already used by `RunCommandViaSSM`; adding it to the typed exec path closes the gap. 60-s upper bound; on the simulator (no managed-agent reporting) the wait returns immediately. |
| 852 | M | ecs (network) | When sockerless creates a docker network, it provisions a per-network VPC security group for peer isolation. Sub-tasks attached to user-defined networks were placed on ONLY the per-network SG — losing access to the operator's default SG (which is what EFS mount targets allow-list, what the runner-task uses, and what carries the operator's egress rules). Effect: sub-task EFS mounts timed out with `mount.nfs4: Connection timed out`, blocking every `container:` workflow that needs the runner's workspace. Fix: include both the per-network SG(s) AND the operator's default SG on sub-tasks. Per-network SG still gates peer reachability (peer SGs aren't in the EFS allow-list); the default SG provides shared infrastructure access (EFS, allow-listed services). Trade-off documented in the comment: operators relying on per-network isolation for *all* peer-to-peer reachability now need the operator default SG to be appropriately scoped. |
| 851 | M | ecs (network driver) | `BaseServer.InitDrivers` installs `LinuxNetworkDriver` on Linux (creates real `ip netns add` + veth pairs alongside the synthetic store entry). Right for the local docker backend; wrong for ECS, where docker networks map to *cloud* primitives (VPC security groups, Cloud Map namespaces — provisioned by `cloudNetworkCreate`/`cloudNamespaceCreate`). Manifested when sockerless ran inside an ECS task (the runner-task pattern): `ip netns add` failed with `exit status 1` (no `CAP_SYS_ADMIN` in Fargate by default) — `docker network create` returned `netns creation failed`. Fix: ECS server overrides `s.Drivers.Network` with `SyntheticNetworkDriver` (metadata-only — *not* a fake-data driver, the name is historical). Cloud-side networking (SG + Cloud Map) handled separately by the ECS NetworkCreate wrapper. |
| 850 | H | ecs (bind mounts) | GitHub Actions' `Initialize containers` step does `docker create -v /home/runner/_work:/__w -v /home/runner/externals:/__e:ro -v /var/run/docker.sock:/var/run/docker.sock ...` — eight host-bind-mount specs the runner uses to share its workspace + JS-action runtime with the spawned job container. Sockerless ECS rejected all of them (Fargate has no host filesystem to bind-from), so the `container:` directive on workflows running through sockerless-dispatched runners always failed at the `docker create` step. Fix: new `Config.SharedVolumes` config (`SOCKERLESS_ECS_SHARED_VOLUMES=name=path=fsap-XXX[,...]`) declaring shared EFS access points the calling task already mounts at known container-paths. `ContainerCreate` translates bind mounts whose source matches a SharedVolume.ContainerPath into a named-volume reference for the spawned sub-task; sub-paths of mapped sources get dropped (the parent EFS mount already exposes them); `/var/run/docker.sock` is dropped silently (Fargate has no socket). The runner-task's task definition mounts the EFS access points at the canonical paths (`workspace` + `externals`); the entrypoint pre-populates the `externals` access point from an image-baked `/home/runner/externals.staged` copy on first start (so JS actions / `node20` are visible to job containers). End-to-end: runner-task and spawned job-task see the same workspace + externals via shared EFS — `container:` works on Fargate without fakes. |
| 845 | M | terraform | `terraform/environments/lambda/live/terragrunt.hcl` was pinned to `us-east-1` + bucket `sockerless-terraform-state` + dynamodb_table — drift from the ECS live env (`eu-west-1` + `sockerless-tf-state`). `manual-tests/01-infrastructure.md` documents Lambda reusing the ECS subnets + security group, which is impossible cross-region. Fix: realign Lambda live env to `eu-west-1` + `sockerless-tf-state`; mirror the ECS env's `provider "aws"` generate block; drop the dynamodb_table reference (ECS env doesn't use one). |
| 846 | M | core (image resolve) + docs | Live AWS test pass against an empty account fails immediately on first `docker run alpine:latest` because the ECS + Lambda backends previously routed Docker Hub library refs through an ECR pull-through cache rule with `registry-1.docker.io` upstream — and AWS rejects that without a Secrets Manager-backed Docker Hub PAT. Fix: drop the Docker Hub PAT path entirely (the project's no-credentials-on-disk discipline avoids it by design). Sockerless's `resolveImageURI` now rewrites Docker Hub library refs (`alpine`, `node:20`, `nginx:alpine`) to the AWS Public Gallery mirror at `public.ecr.aws/docker/library/<name>:<tag>`, which Fargate pulls directly with no credentials. Lambda routes the same refs through an ECR pull-through cache pointed at `public.ecr.aws` (Lambda needs single-arch ECR-hosted images, but the cache still doesn't need credentials since it caches public images). Other public registries (`ghcr.io`, `quay.io`, `registry.k8s.io`, `mcr.microsoft.com`) get their own pull-through cache rules — also no credentials. Docker Hub user/org refs (`myorg/myapp`) are rejected with a clear error pointing the operator at `docker push <ecr-uri>`. The `SOCKERLESS_ECR_DOCKERHUB_CREDENTIAL_ARN` env var and the equivalent terraform variable are gone. `manual-tests/01-infrastructure.md` documents the new "Image source policy" section (AWS Public Gallery / ECR pull-through / operator-owned ECR — three credential-free routes). |
| 847 | L | tests/runners | `tests/runners/github/harness_test.go` failed `download runner: HTTP 404` because (a) `actions/runner` uses `osx` not `darwin` in its release-asset naming convention for macOS and the harness was passing `runtime.GOOS` ("darwin") through verbatim; (b) the pinned default version `2.319.1` is gone from the releases page (latest is now `2.334.0`). Fix: translate `darwin` → `osx` when building the asset URL, bump default version to a known-good release, document the `SOCKERLESS_GH_RUNNER_VERSION` override. |
| 848 | M | ecs + lambda | `docker info` against the ECS or Lambda backend returned `Architecture: amd64` as a hardcoded constant in BackendDescriptor, regardless of the operator's RuntimePlatform / Architectures setting. That violates the docker client/server model — sockerless's own host arch is irrelevant; the reported server arch must reflect the cloud workload (Fargate task, Lambda function) so clients pull single-arch images that actually run. Fix: ECS Config gains a required `CpuArchitecture` field (`X86_64` / `ARM64`, no default — Validate() rejects empty/unknown); Lambda Config gains a required `Architecture` field (`x86_64` / `arm64`, no default — Validate() rejects empty/unknown). Both backends translate the AWS spelling into Docker's amd64/arm64 spelling for `BackendDescriptor.Architecture`. ECS RegisterTaskDefinition now passes `RuntimePlatform.CpuArchitecture`; Lambda CreateFunction now passes `Architectures: [<arch>]`. Operator sets `SOCKERLESS_ECS_CPU_ARCHITECTURE` and `SOCKERLESS_LAMBDA_ARCHITECTURE` explicitly per-deployment. |
| 849 | M | tests/runners | `tests/runners/github/harness_test.go::commitWorkflow` PUT'd workflow YAMLs to `.github/workflows/<file>` against the *default* branch (the `gh api PUT /contents/...` call had no `branch=` parameter). Earlier failed runs left 3 polluting commits on origin/main (`test: harness workflow update`). Fix: GH harness now creates a throwaway per-cell branch (`sockerless-gh-<label>-<timestamp>`) via the GitHub Refs API, commits the workflow there, dispatches `workflow_dispatch` against that branch via `ref=<branch>`, and deletes the branch on cleanup — same shape as the GitLab harness. Default branch stays clean. |

### PR #120 — typed-driver migration CI pass (BUG-836..844)

| ID | Sev | Area | One-liner |
|----|-----|------|-----------|
| 836 | H | sim/aws | ECS task lifecycle skipped real container start when no awslogs configured (synthetic RUNNING-forever). Fix: container starts unconditionally; `discardLogSink` carries the path when no log driver. |
| 837 | L | tests/aws-sdk | `TestECS_TagResource_RejectsStoppedTask` flaked under CI podman contention with a fixed 8s sleep. Fix: replaced with a 60s poll-for-STOPPED loop. |
| 838 | M | sim/gcp | `ServiceV2.Ingress` typed `string` couldn't decode the Go REST client's numeric proto-JSON enum form. Fix: introduced an `enumString` type that accepts both numeric and string forms, round-trips as string. |
| 839 | M | sim/azure | Every site shared `r.Host` as DefaultHostName so multi-site routing collided. Fix: per-site `<name>.azurewebsites.net`; invoke handler routes by Host header. |
| 840 | L | tests/e2e | `TestImageBuild` asserted on the synthetic local-build path BUG-822 removed. Fix: rewrote to assert the 501 + `cloud build service` error contract. |
| 841 | H | ecs | `handleContainerKill` routed every signal through SignalDriver; `ssmSignalDriver` only handles SIGSTOP/SIGCONT. Fix: branch on signal — pause/cont via SSM, everything else via legacy `ContainerKill` (`ECS.StopTask`). |
| 842 | H | sim/aws | SSM session ignored `input_stream_data` AgentMessage frames so binary stdin (tar archives) never reached the user process. Fix: decode AgentMessage frames; forward `input_stream_data` payload to exec stdin; close stdin on FIN. |
| 843 | M | ecs | `ContainerPutArchiveViaSSM` didn't pre-create destination directory; `tar -xf -C <missing>` failed. Fix: `mkdir -p <path> && tar -xf - -C <path>`. |
| 844 | H | sim/aws | `RunTask` returned hardcoded `subnet-sim00001` and `10.0.x.x` IPs ignoring the request. Fix: `AllocateSubnetIP(subnetID)` reads the registered subnet's CidrBlock; 400 on unknown subnet. |

### PR #120 audit pass — fallback / synthetic-data findings (BUG-820..835)

| ID | Sev | Area | One-liner |
|----|-----|------|-----------|
| 820 | H | core (registry) | `FetchImageMetadata` for multi-arch fell back to `Manifests[0]` when no `linux/amd64` entry. Fix: drop fallback; return error listing every available os/arch. |
| 821 | M | core (ipam) | `IPAllocator.AllocateIP` returned hardcoded `172.17.0.2/16` when networkID wasn't registered (default bridge). Fix: register bridge subnet on init; unknown-network returns zero values. |
| 822 | H | core (build) | `ImageManager.Build` silently fell back to local Dockerfile parser (drops RUN steps) when no cloud build configured. Fix: return `NotImplementedError`; `SOCKERLESS_LOCAL_DOCKERFILE_BUILD=1` opt-in for parse-only. |
| 823 | H | core (network) | `LinuxNetworkDriver.Create/Connect` silently continued in synthetic-only mode on netns failure. Fix: roll back + return real error; `SOCKERLESS_NETNS_BEST_EFFORT=1` opt-in for degradation. |
| 824 | M | core (network) | `buildEndpointForNetwork` synthesised `172.17.0.<n>` for unknown networks, colliding with bridge allocations. Fix: returns nil; callers skip nil endpoints so `network not found` surfaces honestly. |
| 825 | M | core (registry) | `ImageManager.Remove` silently logged warnings when cloud-side delete failed. Fix: aggregate errors into `ServerError` listing them so operators can rerun rmi. |
| 826 | M | core / all-cloud | `docker stop` / `rm -f` / `restart` / `pod stop` emitted synthetic `container.die` with `exitCode=0` regardless of how the container died. Fix: 143 (SIGTERM) / 137 (SIGKILL) per `core.SignalToExitCode`; persisted via `Store.ForceStopContainer`. |
| 827 | M | sim/azure + sim/gcp | Job log streams emitted `"Execution completed successfully"` even when user container exited non-zero. Fix: branch on `succeeded` flag — `"Execution failed"` on failure. |
| 828 | H | core (network) | `NetnsManager.CreateVethPair` ignored errors from `ip addr add` / `ip link set up`. Fix: each step error-checked, rolls back on first failure. |
| 829 | M | gcp/azure-common (auth) | `ARAuthProvider.OnRemove` / `ACRAuthProvider.OnRemove` silently `continue`d on per-tag delete failures. Fix: collect failures into combined error so `ImageManager.Remove` aggregator surfaces them. |
| 830 | M | docker | Hardcoded `NCPU: 4, MemTotal: 8 GB, OSType: "linux"` in `BackendDescriptor` survived as fallback when daemon unreachable. Fix: query daemon `/info` at startup with 5s deadline; fail startup on unavailable. |
| 831 | L | core / all-cloud | `EndpointSettings.IPAddress` seeded as literal `"0.0.0.0"` for cloud containers without a routable IP — read as real address in `docker inspect`. Fix: empty string instead. |
| 832 | M | sim/aws | (Phase 108) ECS missing `TagResource` / `UntagResource` handlers — backend tag writes silently 404'd. Fix: added both handlers; STOPPED-task gate mirrors real ECS. |
| 833 | H | sim/gcp | (Phase 108) Cloud Run v2 Services routes missing — `UseService` mode 404'd against sim. Fix: `simulators/gcp/cloudrunservices.go` covers v2 Create/Get/List/Update/Delete. |
| 834 | H | sim/azure | (Phase 108) ContainerApps Apps surface entirely absent — `UseApp` mode 404'd. Fix: `simulators/azure/containerapps_apps.go` covers PUT/GET/LIST/DELETE on `Microsoft.App/containerApps`. |
| 835 | M | sim/azure | (Phase 108) `WebApps.UpdateAzureStorageAccounts` handler missing — function-app named volumes failed at start. Fix: added handler + symmetric `GET .../config/azurestorageaccounts/list`. |

### Round-9 (PR #118) live-AWS sweep (BUG-801..819 + retroactives)

| ID | Sev | Area | One-liner |
|----|-----|------|-----------|
| 801 | L | ecs | `docker inspect` returned `HostConfig.Memory: 0` / `NanoCpus: 0`. Fix: `taskToContainer` parses task.Memory (MB) and task.Cpu (1024-shares) into bytes / nanoCPUs. |
| 803 | L | docs | `specs/CLOUD_RESOURCE_MAPPING.md` matrix vs §Notes inconsistent for `ContainerExport`. Fix: aligned to "⚠ via SSM" for ECS and "⚠ agent only" for FaaS+CR+ACA. |
| 805 | M | ecs | `docker stop` / `pod stop` timed out at 60s on Fargate STOPPING transitions. Fix: default `stopTimeout` 60→120s; user-supplied gets 60s grace instead of 30s. |
| 807 | H | lambda | `docker run` returned `ResourceConflictException: Pending` — backend invoked the function before AWS finished VPC ENI provisioning. Fix: invoke goroutine waits on `awslambda.NewFunctionActiveV2Waiter` (5-min cap) before `Invoke`. |
| 808 | H | lambda | `SOCKERLESS_LAMBDA_PREBUILT_OVERLAY_IMAGE` was dead code without `SOCKERLESS_CALLBACK_URL`. Fix: split the switch — prebuilt-image alone routes user CMD via `SOCKERLESS_USER_CMD/ENTRYPOINT`. |
| 809 | H | core | `docker exec` against backend returning NotImplementedError emitted `unrecognized stream: 100` because handler hijacked + sent `101 UPGRADED` before calling backend. Fix: call ExecStart first, return `WriteError` on failure, only hijack on success. |
| 810 | L | core | Startup log `loaded resource registry from disk` survived after BUG-800 made `Registry.Load()` a no-op. Fix: dropped dead call, reworded log line. |
| 811 | M | lambda | After backend restart, every Lambda function reported `Up Less than a second` because `InvocationResults` is in-memory only. Fix: invoke goroutine writes result to function tags; `ReplayInvocationsFromCloudWatch` rebuilds map on startup. |
| 812 | L | lambda | `docker ps -a` reported `292 years ago` — AWS Lambda's `LastModified` format isn't RFC3339-parseable. Fix: parse AWS-specific format in `cloud_state.go::queryFunctions`, re-emit RFC3339Nano. |
| 813 | M | ecs | `TestECSArithmeticInvalid` flaked because `waitForTaskRunning` only treated exit-code-0 STOPPED as success. Fix: `taskHasContainerExitCode` — any user exit code = start succeeded; only platform failure (no ExitCode) = start failed. |
| 815 | H | ecs | `docker exec <ecs-cid> echo hi` returned no output — `cloudExecStart` passed shell-feature script directly to ECS exec. Fix: wrap script in `sh -c <quoted>`. |
| 816 | M | ecs | `docker diff` produced busybox find help text — `find … -printf` is GNU-only. Fix: use `for t in d f l; do find … -type "$t" \| sed …` (busybox + GNU compatible). |
| 817 | M | ecs | `docker cp` returned `unexpected stat output: "/path\\t…"` — busybox `stat` doesn't interpret `\t` in single-quoted format. Fix: literal tab characters. |
| 818 | H | sim/aws | After BUG-815's `sh -c` wrap, simulator's exec WebSocket left surrounding single quotes intact when handing the script to docker exec. Fix: drop unwrap branch; always wrap (double-wrapping is correct). |
| 819 | H | terraform | Round-9 ECS teardown hung 30+ min on hyperplane-ENI release. Fix: per-cloud `null_resource sockerless_runtime_sweep` clears VpcConfig before delete; ECS module polls for AWS-Lambda ENIs to flip available. |
| 789 / 798 | H | ecs | `docker top` / `docker diff` returned `ps failed (exit -1)` — SSM `Interactive=true` doesn't send exit-code frames for one-shot commands. Fix: wrap every command in `sh -c '<cmd>; printf "__SOCKEXIT:%d:__" $?'`; `extractSSMExitMarker` recovers exit code. |
| 802 | — | (withdrawn) | C5 export 0-byte tar — was a `timeout 60` artefact, not a code bug. Subsumed by BUG-789/798 fix. |
| 638 / 640 / 646 / 648 | H/M | core (registry) | (Retroactive) ECR push only stored manifests, never blobs; `ImagePush` returned synthetic "Pushed"; OCI push fell back to empty-gzip layers; `FetchImageMetadata` synthesised metadata on registry unreachable. All closed by BUG-788 (real layer mirror) + Phase 90 no-fakes audit. |

### Round-8 (PR #118) (BUG-786..800)

| ID | Sev | Area | One-liner |
|----|-----|------|-----------|
| 786 | M | core | `docker rmi <tag>` reported untagged but tag reappeared in `docker images`. Root: `StoreImageWithAliases` puts under multiple keys; partial-untag missed alias entries. Fix: sweep every Store entry with matching ID. |
| 787 | L | docs | `specs/CLOUD_RESOURCE_MAPPING.md` lagged implementation across multiple phases. Fix: per-cloud spelling rule, Phase 91-94 Volume row updated, ECS Container* ops via SSM, Acceptable-gaps section added. |
| 788 | H | core | `docker push <ecr-uri>` failed with `image has no layer data available` — `ImagePull` only stored metadata, not blob bytes. Fix: `FetchLayerBlob` downloads each layer to `Store.LayerContent`; `OCIPush` uses cached compressed bytes verbatim. |
| 790 | M | ecs | `docker stop` returned success before ECS reached STOPPED, breaking `docker rm`. Fix: `waitForTaskStopped` blocks until LastStatus=STOPPED (caller-supplied timeout + 30s grace). |
| 791 | L | core | `docker cp` returned misleading `404 Not Found for API route`. Fix: `handleGetArchive` / `handleHeadArchive` route through `WriteError` like `handlePutArchive`. |
| 792 | L | ecs | `docker commit` error message contained `Phase 98b` reference. Fix: rewritten to describe architectural prerequisite without phase name. |
| 793 | H | lambda + tf | Lambda failed `CreateNetworkInterface` permission when `SOCKERLESS_LAMBDA_SUBNETS` set. Fix: terraform attaches `AWSLambdaVPCAccessExecutionRole`. |
| 794 | H | ecs | Cross-network isolation broken — default SG attached regardless of network membership. Fix: when `SecurityGroupIDs` non-empty, use ONLY those SGs. |
| 795 | M | core | `podman ps --filter name=svc` returned empty even when `svc1` running. Fix: `name` filter uses `strings.Contains` not `==`. |
| 796 | M | ecs (libpod) | `podman pod rm` failed with running-containers error after `pod stop`. Fix: transitively closed by BUG-790's `waitForTaskStopped`. |
| 797 | M | lambda | `public.ecr.aws/...` images failed with `manifest not supported` because Lambda rewrote through ECR pull-through. Fix: short-circuit `public.ecr.aws/` like ECS (BUG-776). |
| 799 | M | ecs | After backend restart, `docker rmi` returned conflict for STOPPED-task images. Fix: `ScanOrphanedResources` skips STOPPED/DEPROVISIONING tasks; only RUNNING/PENDING become "active orphans". |
| 800 | H | core | **Stateless invariant violation.** `core.ResourceRegistry` persisted to `./sockerless-registry.json` by default — operator's CWD became load-bearing. Fix: `Save`/`Load`/`autoSave` collapsed to no-ops; cloud is the source of truth via `sockerless-managed=true` tag scan. |

### Round-7 (PR #117) (BUG-770..785)

| ID | Sev | Area | One-liner |
|----|-----|------|-----------|
| 770 | M | core | `ImageRemove` reported phantom untag/delete for multi-tagged images. Fix: resolve user ref against `RepoTags`; only matching tag returns Untagged. |
| 771 | M | ecs | `ContainerInspect` returned empty Path/Args/Cmd/Entrypoint for cloud-derived containers. Fix: lazy `describeTaskDefinition` cache populates fields from TaskDefinition. |
| 772 | M | ecs | `docker restart` didn't bump RestartCount. Fix: `sockerless-restart-count` tag on RunTask; `taskToContainer` reads it back. |
| 773 | H | ecs | `docker rename` desynced in-memory Store from cloud. Fix: `Server.ContainerRename` calls `TagResource` to overwrite `sockerless-name` tag. |
| 774 | H | ecs | `docker ps -a` returned duplicate rows after `docker restart`. Fix: `queryTasks` dedupes by `sockerless-container-id` tag, prefers RUNNING over STOPPED. |
| 775 | H | ecs | `docker rm` no-op'd because ECS keeps STOPPED visible ~1h. Fix: `ContainerRemove` writes to `ResourceRegistry`; `queryTasks` filters cleaned-up ARNs. |
| 776 | H | ecs | `ContainerCreate` silently fell back to raw image ref on ECR pull-through failure. Fix: `buildContainerDef` returns the resolution error; `public.ecr.aws/` short-circuits. |
| 777 | H | core (auth) | `docker push <ecr-uri>` always returned 401. Fix: `ImageManager.Push` always replaces caller-supplied auth with fresh `Auth.GetToken(registry)`. |
| 778 | M | core (libpod) | `podman inspect` failed with `parsing time ""`. Fix: `normalizeContainerTimes` fills empty stamps with zero-time. |
| 779 | H | core (libpod) | `GET /libpod/containers/json` returned `[]` on stateless backends. Fix: `handleLibpodContainerList` queries `CloudState.ListContainers`. |
| 780 | H | core (libpod) | `podman create --name` → `start` failed `no such container` because libpod specgen JSON is flat. Fix: handler reads specgen fields directly. |
| 781 | L | ecs | `docker kill -s SIGTERM` reported raw container exit code, not Docker's 128+signum. Fix: `sockerless-kill-signal` tag; `mapTaskStatus` overrides ExitCode. |
| 782 | L | ecs | `docker stats` NAME column showed `"--"`. Fix: `buildStatsEntry` uses `ResolveContainerAuto`. |
| 783 | H | ecs | Per-network SG never attached to Fargate ENIs. Fix: `ContainerCreate` calls `cloudNetworkConnect` at create time when initial network is user-defined. |
| 784 | H | ecs | `docker compose up` failed `Some tags contain invalid characters` because Docker labels were raw JSON in ECS tags. Fix: `sockerless-labels-b64` URL-safe base64, chunked when >256 chars. |
| 785 | L | core | `ResourceRegistry.autoSave` logged rename warnings on every op. Fix: `MkdirAll` parent before writing. |

### Pre-Round-7 highlights — Phases 86-105 (BUG-661..769)

Compressed; full per-bug detail in `git log` and PR descriptions for #112 / #113 / #114 / #115. Per-cloud ordering preserved.

| ID range | Theme | One-liner roll-up |
|----------|-------|-------------------|
| 762..769 | Phase 102 / FaaS auth / OCI push | ECS exec via SSM ExecuteCommand for `top`/`stat`/`tar`/`pause`/`unpause`; pidfile convention; OCIPush gets real `Architecture`/`OS`/`Config`/`diff_ids`; auth flow always replaces caller token with fresh `Auth.GetToken`. |
| 749..761 | Phase 96 / 98 / 99 / 100 / 101 / GCF/AZF parity | Reverse-agent for Cloud Run + ACA; pause/unpause via `/tmp/.sockerless-mainpid`; bootstrap teest stdout to container stdout; Lambda invocation tracking; ACA console exec WebSocket; podman pods for docker backend. |
| 744..748 | Phase 94 / 95 / 97 | Lambda EFS volumes; FaaS invocation-lifecycle tracker (`InvocationResult` + tag-based replay); GCP label-value charset compliance via annotations. |
| 738..743 | Phase 91-93 / Lambda runtime | EFS access points (ECS); GCS volumes (Cloud Run); Azure Files (ACA); Lambda short-lived task short-circuit; Lambda overlay-image pre-resolved when configured. |
| 731..737 | Phase 90 no-fakes audit | `VolumeCreate` returns NotImplemented across cloud backends; `ImagePullWithMetadata` propagates registry errors (no synthetic placeholder); `getNamespaceName` returns errors instead of silent ID substitution; `stats` PIDs=0 instead of fabricated 1. |
| 727..730 | CI integration tests | Unit job CI wiring (`SOCKERLESS_INTEGRATION=1`); `noui` build tag; per-backend integration tests; SSM ack format matches AWS Java-style `putUuid`. |
| 720..726 | Round-6 ECS exec + state | SSM AgentMessage parser/ack writer; full SSM permissions in IAM; `EnableExecuteCommand: true`; cloud-derived NetworkState/Pods/Images; Cloud Map post-RUNNING registration with real ENI IP. |
| 712..719 | Phase 86 ECS networking | Idempotent network/namespace create; `DnsSearchDomains` via `/bin/sh -c`-wrapped argv; default port `:3375`; `SOCKERLESS_ECR_DOCKERHUB_CREDENTIAL_ARN` wired with explicit error. |
| 700..711 | Phase 86 cross-cloud sims + DNS | Per-namespace docker network in sims; `pre-register subnet-sim` in EC2 sim; ACA Private DNS Zones via `armprivatedns`; `LinkResource.OnPush` no-op variant; ACR cache-rule CRUD; Cloud Build secret manager. |
| 685..699 | Phase 86 simulator parity | Real Lambda Runtime API; sim workloads via Docker SDK; ServiceV2/JobV2 LatestCreatedExecution; SystemData on ContainerAppJob; `ResolveLocalImage` registry-URI mapping; container-status conditional fixes; `AllowCreated` for `StreamCloudLogs`. |
| 661..684 | Stateless invariants | Auto-agent removed across cloud backends; CloudState replaces Store reads in 16 callsites; `ResolveContainerAuto` migration; `CloudState.ListContainers` for list ops; CloudPodLister merged into PodList. |

Pre-661 entries are summarised in the original git log + per-PR descriptions. Per-bug detail before BUG-661 is too terse to be useful here.

## Cross-cloud sweep notes

When a bug is found, similar code paths in other backends / clouds / simulators are checked too. Notable sweep outcomes:

- BUG-708 ECS-only — Azure uses ACR cache rules with managed-identity auth; GCP uses Artifact Registry pull-through (different mechanism).
- BUG-709 ECS-only — Azure ACA's SDK helper sleeps internally; GCP's SDK handles polling.
- BUG-710 swept all 7 backend mains + CLI + READMEs + example terraform + `tools/http-trace`.
- BUG-711 ECS-only — no other backend sets explicit DNS search domains; GCP/Azure rely on per-network DNS zones with FQDN resolution.
- BUG-712 → found BUG-713 in cloudrun (same non-idempotent-create pattern). Azure ACA's `BeginCreateOrUpdate` is PUT-style and idempotent; Lambda/GCF/AZF have no cloud-side network creation.
- BUG-714 → found BUG-715 (cloudrun) and BUG-716 (aca) with the same placeholder-IP symptom. All three backends seed `ep.IPAddress = "0.0.0.0"` at create time; each needed a different structural fix (ECS got the real Fargate ENI IP; cloudrun + aca moved to Services/Apps with stable FQDNs in Phase 87/88).
- BUG-826 swept all 6 cloud backends — the synthetic `exitCode=0` pattern was identical in `core/backend_impl.go`, `core/backend_impl_pods.go`, `ecs/backend_impl.go`, `aca/backend_impl.go`, `cloudrun/backend_impl.go`, `cloudrun-functions/backend_impl.go`, `azure-functions/backend_impl.go`, `azure-functions/backend_impl_pods.go`.
- BUG-829 swept gcp-common and azure-common — same per-tag silent-continue pattern as BUG-825.
