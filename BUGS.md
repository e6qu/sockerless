# Known Bugs

**843 total — 843 fixed (Round-7 closed BUG-770..785; Round-8 closed BUG-786..794, 797, 799, 800; Round-9 closed BUG-789/798, 801, 803..819; BUG-638/640/646/648 backfilled retroactively as closed by BUG-788; post-PR-118 audit pass closed BUG-820..832 incl. Phase 108 AWS sim TagResource gap; Phase 108 GCP audit closed BUG-833 — sim missing v2 Cloud Run Services routes; Phase 108 Azure audit closed BUG-834 — sim missing ContainerApps Apps routes — and BUG-835 — sim missing WebApps.UpdateAzureStorageAccounts; PR #120 CI-failure pass closed BUG-836 — sim AWS ECS task lifecycle skipped real container start when no awslogs; BUG-837 — TestECS_TagResource_RejectsStoppedTask used a fixed 8s sleep that flaked under CI podman contention; BUG-838 — sim GCP ServiceV2.Ingress couldn't decode the Go REST client's numeric proto-JSON enum form; BUG-839 — sim Azure all sites shared `r.Host` as DefaultHostName so multi-site routing collided; BUG-840 — TestImageBuild asserted on the synthetic local-build path that BUG-822 removed; BUG-841 — ECS handleContainerKill routed every signal through SignalDriver; BUG-842 — sim AWS SSM session ignored input_stream_data frames so binary stdin (tar archives) never reached the user process; BUG-843 — ContainerPutArchiveViaSSM didn't pre-create the destination directory). 0 still open. 1 false positive (BUG-747 is the audit umbrella). Phase 108 closed 2026-04-26: all 77 sim-parity matrix rows are ✓. Per-project rule: no bug deferral, no fakes, no fallbacks — every filed bug ships a fix in the same round.**

For narrative context see [WHAT_WE_DID.md](WHAT_WE_DID.md) and [PLAN.md](PLAN.md). Architecture-level state derivation is documented in [specs/CLOUD_RESOURCE_MAPPING.md](specs/CLOUD_RESOURCE_MAPPING.md) and [specs/BACKEND_STATE.md](specs/BACKEND_STATE.md).

Standing workflow rule: every CI / live-cloud failure lands here with a short root-cause trace before it's fixed. Workarounds, fakes, placeholders, silent fallbacks, and incomplete implementations are all bugs and get the same treatment — "no fakes, no fallbacks" is a project-wide principle.

## Open

_(none — every filed bug ships a fix in the same round per the no-defer rule)_

## Fixed

Round-9 (this PR — same branch as round-8):

| ID | Sev | Area | Summary |
|----|-----|------|---------|
| 801 | L | ecs | `docker inspect <id>` returned `HostConfig.Memory: 0` and `HostConfig.NanoCpus: 0` even though the underlying ECS task-def had a Fargate-valid CPU/memory tier. The `taskToContainer` projection in `backends/ecs/cloud_state.go` wasn't mapping `task.Memory` (MB string) and `task.Cpu` (1024-unit-share string) back to Docker's bytes / nanoCPUs fields. Fix: parse and convert (`mb × 1024×1024`; `shares × 1e9 / 1024`), populate `HostConfig.Memory` and `HostConfig.NanoCPUs`. Verified live: `docker run -d -m 1g --cpus=0.25 nginx; docker inspect …` now reports `mem=1073741824 cpus=250000000`. |
| 803 | L | docs | `specs/CLOUD_RESOURCE_MAPPING.md` matrix listed `ContainerExport` as `✗ accepted gap` while §Notes (per-op) said `⚠ via SSM (Phase 102)` — internal inconsistency from round-8 BUG-787 edits. Fix: aligned both to `⚠ via SSM (Phase 102)` for ECS and `⚠ agent only — ✗ accepted gap when no agent` for FaaS+CR+ACA, matching what the matrix already said for `ContainerTop` and the other SSM-via-Phase-102 ops. Same wording cleanup applied to the Acceptable-Gaps section's `docker container top` and `docker container export` rows. |
| 805 | M | ecs | `docker stop` / `podman pod stop` timed out at 60 s on Fargate task STOPPING → DEPROVISIONING → STOPPED transitions (especially after the per-network-SG ENI release added by BUG-794). Found round-9 F11 (pod stop). Fix in `backends/ecs/backend_impl.go::ContainerStop`: default `stopTimeout` bumped 60 s → 120 s; user-supplied `t=N` gets 60 s grace instead of 30 s. Covers the worst case observed live without making the stop block forever on a genuinely stuck task (`waitForTaskStopped` still surfaces a real timeout error). |
| 807 | H | lambda | `docker run <overlay-image> echo …` against a fresh Lambda backend returned `ResourceConflictException: The function is currently in the following state: Pending` — the backend invoked the function the instant `CreateFunction` returned, before AWS finished pulling the image and attaching the VPC ENI. Found round-9 D2-D3. Fix: in `backends/lambda/backend_impl.go::ContainerStart` the invoke goroutine now first runs `awslambda.NewFunctionActiveV2Waiter(...).Wait(ctx, GetFunctionInput{FunctionName: …}, 5*time.Minute)`; only after `State=Active && LastUpdateStatus=Successful` does it call `Invoke`. 5-minute cap covers VPC-attached cold starts (image pull + ENI provisioning regularly takes 2-3 min). Wait failure surfaces cleanly as `ExitCode=1` with the waiter error in the InvocationResult — no fake-success fallback. |
| 808 | H | lambda | `SOCKERLESS_LAMBDA_PREBUILT_OVERLAY_IMAGE` was effectively dead code without `SOCKERLESS_CALLBACK_URL` — `backend_impl.go::ContainerCreate`'s switch only honored the prebuilt overlay path when *both* envs were set, so a plain `docker run --image <overlay> echo hi` fell through to the user-image branch, never set `SOCKERLESS_USER_CMD`/`SOCKERLESS_USER_ENTRYPOINT`, and the bootstrap (which only reads argv from those envs) thought there was no user cmd → echoed the empty event payload back instead of running `echo hi`. Found round-9 D4 (logs were just START/END/REPORT, no user stdout). Fix: split the switch — `PrebuiltOverlayImage != ""` alone now uses the prebuilt image and sets `SOCKERLESS_USER_CMD`/`SOCKERLESS_USER_ENTRYPOINT`; `CallbackURL` adds reverse-agent wiring on top. The two are now orthogonal. |
| 809 | H | core | `docker exec` against a backend that returns `NotImplementedError` from `ExecStart` (e.g. Lambda without a registered reverse-agent) emitted `unrecognized stream: 100` from the docker CLI instead of the real error message. Root cause: `backends/core/handle_exec.go::handleExecStart` hijacked the connection and wrote `HTTP/1.1 101 UPGRADED + Content-Type: application/vnd.docker.multiplexed-stream` *before* calling `s.self.ExecStart(...)`, then if ExecStart returned an error it wrote the error string into the already-hijacked socket — which the client decoded as a stdcopy frame, picking up the first byte ("d" = 100) as the stream identifier. Fix: reordered — call `s.self.ExecStart(id, req)` first, return `WriteError(w, err)` on failure (which yields a clean `501 Not Implemented` JSON response), only hijack/upgrade on success. Verified live: docker CLI now reports `unable to upgrade to tcp, received 501` and the error body is the real NotImpl message. |
| 810 | L | core | `ListenAndServe` startup logged `loaded resource registry from disk` after BUG-800 collapsed `Registry.Load()` to a no-op — log message survived but no longer matches reality (registry is in-memory; entries come from `RecoverOnStartup`'s cloud scan). Fix: dropped the dead `Registry.Load()` call and reworded the log line to say "active resource registry entries (in-memory; populated by cloud scan on RecoverOnStartup)". |
| 813 | M | ecs | `TestECSArithmeticInvalid` flaked under PR #118 — `docker run --image <eval> '3 +'` returned `start failed: Error response from daemon: task stopped: Essential container in task exited` because the image's `node` exits with code 1 fast enough that `waitForTaskRunning` observed STOPPED before RUNNING. The poll's STOPPED branch only treated exit-code-0 as "ran fast" (returning success); any non-zero exit was a start failure. Per docker semantics, "container started, ran, exited" is a successful start regardless of exit code — the user reads the exit via `docker wait`/`docker inspect`. Fix: new `taskHasContainerExitCode` helper distinguishes "user process produced any exit code" (start succeeded) from "Fargate failed at the platform level — image pull, role, ENI" (no ExitCode recorded → start failed). `waitForTaskRunning` now returns success when an exit code is present. Verified locally: `count=5 -run TestECSArithmeticInvalid` all pass. |
| 789 / 798 | H | ecs | `docker top` / `docker diff` against a live Fargate task returned `ps failed (exit -1)` / `find failed (exit -1)`. Captured every SSM WS frame against live AWS — root cause is two-fold: (a) AWS ECS `ExecuteCommand` (`Interactive=true`) sessions never send the SSM `output_stream_data` frame with `payloadType=exit_code` for one-shot commands; `channel_closed` is the only terminal signal. (b) The original `find … -printf` flag is GNU-find-only (BUG-816). Fix (a): `runViaSSMOrNotImpl` now wraps every command in `sh -c '<cmd>; printf "__SOCKEXIT:%d:__" $?'` and a new `extractSSMExitMarker` strips the marker from stdout to recover the real exit code — single source of truth, no fallback. Fix (b) lives in BUG-816. Verified live on alpine: `docker top` returns real ps output, `docker diff` returns real change list, `docker pause` returns the documented `NotImplementedError`. |
| 815 | H | ecs | `docker exec <ecs-cid> echo hi` returned no output. SSM `channel_closed` JSON contained `Output:"Unable to start command: Failed to start pty: exec: \"cd\": executable file not found in $PATH"`. Root cause: `cloudExecStart` built a script with shell features (`cd <wd> && export <env>; <cmd>`) and passed it directly to `ECS.ExecuteCommand`, but AWS exec()s argv[0] without a shell. Fix: wrap the constructed script in `sh -c <quoted>`. Verified live. |
| 816 | M | ecs | `docker diff <ecs-alpine>` produced busybox's find help text — `find / -xdev -newer /proc/1 -printf '%y\t%p\n'` uses GNU-find's `-printf` which alpine's busybox find rejects. Fix: `ContainerChangesViaSSM` now uses `for t in d f l; do find … -type "$t" \| sed "s\|^\|$t<TAB>\|"; done` — works on busybox AND GNU. Single correct path. |
| 817 | M | ecs | `docker cp <ecs-cid>:/path /local` returned `unexpected stat output: "/path\\t5\\t81a4\\t…"` — `\t` in `stat -c '%n\t…'` was passed verbatim because busybox `stat` doesn't interpret backslash escapes inside single-quoted format args. Fix: `ContainerStatPathViaSSM` now uses literal tab characters in the format string (preserved through shell single-quoting). |
| 807 | H | lambda | `docker run <overlay> echo …` returned `ResourceConflictException: The function is currently in the following state: Pending` — backend invoked instantly. Fix: invoke goroutine now waits on `awslambda.NewFunctionActiveV2Waiter` (5-min cap covers VPC ENI cold-start) before calling `Invoke`. |
| 808 | H | lambda | `SOCKERLESS_LAMBDA_PREBUILT_OVERLAY_IMAGE` was dead without `SOCKERLESS_CALLBACK_URL`. Switch in ContainerCreate now treats them as orthogonal: prebuilt-image alone routes user CMD via `SOCKERLESS_USER_CMD/ENTRYPOINT` envs the bootstrap reads. |
| 809 | H | core | `docker exec` against a backend returning `NotImplementedError` emitted `unrecognized stream: 100`. Root cause: `handleExecStart` hijacked + sent `101 UPGRADED` before calling backend; on error the message bytes were decoded as a stdcopy frame. Fix: call `s.self.ExecStart` first, return `WriteError` on failure, only hijack on success. |
| 810 | L | core | `ListenAndServe` startup logged `loaded resource registry from disk` after BUG-800 made `Registry.Load()` a no-op. Fix: dropped the dead call, reworded log line. |
| 811 | M | lambda | After backend restart, `docker ps -a` reported every previously-invoked Lambda function as `Up Less than a second` and `docker inspect` returned `Running=true ExitCode=0`. Root cause: `Store.InvocationResults` is in-memory only. Fix: invoke goroutine now also writes the InvocationResult to function tags (`sockerless-last-exit-code`, `sockerless-last-finished-at`, `sockerless-last-error`); new `ReplayInvocationsFromCloudWatch` (called from main.go after RecoverRegistry) reads those tags and rebuilds the in-memory map on startup. Tags are atomic, exit codes losslessly preserved. Verified live: kill backend after `docker run …; docker wait …`, restart, `docker inspect …` reports the real exit code + FinishedAt. |
| 812 | L | lambda | `docker ps -a` reported `292 years ago` for CREATED. Root cause: AWS Lambda's `LastModified` ships in `"2006-01-02T15:04:05.000+0000"` format which is not RFC3339-parseable; the shared `ContainerSummary.Created` projection parses `c.Created` as RFC3339Nano → 0 → 1970. Fix: parse the AWS-specific format in `lambda/cloud_state.go::queryFunctions` and re-emit as RFC3339Nano. |
| 795 | M | core | `podman ps --filter name=svc` returned an empty list even when `svc1` and `svc2` were running. Root cause: `MatchContainerFilters`'s `name` case did exact equality, but docker/podman `--filter name=foo` is documented as substring match. Fix: `strings.Contains(name, v)` instead of `name == v`. |
| 804 | M | core (libpod) | `podman pod inspect <name>` failed in libpod's CLI bindings — diagnosed at round-9 F4 as a "got JSON array, expected object" error. Investigated against podman's `define.InspectPodData`: our `api.PodInspectResponse` was a strict subset of the libpod shape; podman's auto-decoder almost certainly fell through to a slice path when several required-feeling fields were absent. Fix: expanded `PodInspectResponse` to mirror the full `define.InspectPodData` (Namespace, CreateCommand, ExitPolicy, InfraContainerID, InfraConfig, CgroupParent, CgroupPath, LockNumber, RestartPolicy, BlkioWeight/CPUPeriod/CPUQuota/CPUShares/CPUSetCPUs/MemoryLimit/MemorySwap, BlkioDeviceReadBps/WriteBps, VolumesFrom, SecurityOpts, mounts, devices, device_read_bps); populated zero-valued instances in `BaseServer.PodInspect` (sockerless pods don't enforce per-pod cgroups so the values are honest zeros, not synthetic). Added `pod_inspect_shape_test.go` golden test that asserts (a) response body is a JSON object (`{`, never `[`) and (b) every libpod-shape key is present. Schema added to `api/openapi.yaml`. |
| 806 | M | core (libpod) | `podman pod stop` failed with `json: cannot unmarshal string into Go struct field PodStopReport.Errs of type error`. Root cause: podman's `PodStopReport.Errs` is `[]error`, which can never round-trip JSON cleanly — even podman's own server-side response (`[{}]`) fails its bindings' decoder. The only safe wire shapes are `Errs: null` or `Errs: []`. Real podman surfaces per-container stop errors via HTTP 409 + an `ErrorModel` body, not via the success-200 `PodStopReport` payload. Fix: new `writePodActionResponse` in `backends/core/handle_pods.go` — success path emits `Errs: []` (cleared) plus the new `RawInput` field; len(Errs)>0 routes to HTTP 409 with `{"cause", "message", "response"}` body matching podman's ErrorModel shape. `api.PodActionResponse.RawInput` added; openapi.yaml updated. Added `TestPodActionResponse_WireShape` golden test. |

Post-PR-#118 audit pass — fallback / synthetic-data findings (closed in this branch):

| ID | Sev | Area | Summary |
|----|-----|------|---------|
| 820 | H | core (registry) | `FetchImageMetadata` for a multi-arch manifest list fell back to `ml.Manifests[0]` when no `linux/amd64` entry was found — silently picking arm64/linux on most multi-arch images and producing containers that crashed at run-time with `exec format error`. Sockerless backends are all linux/amd64. Fix: drop the fallback; return a clear error listing every available `<os>/<arch>` so the caller can pick a different image or tag explicitly. |
| 821 | M | core (ipam) | `IPAllocator.AllocateIP` returned the hardcoded `172.17.0.2/16` constant when the requested networkID wasn't registered with the allocator (typically the default bridge, which `InitDefaultNetwork` populated in `Networks` but not in the allocator). Every container attached to bridge therefore got the same IP, silently breaking cross-container talk. Fix: (1) `InitDefaultNetwork` now registers the bridge subnet with `IPAlloc.AllocateSubnet`, so AllocateIP returns from the real pool; (2) the unknown-network branch returns zero values (empty IP) so callers surface the "unknown network" error explicitly instead of producing a colliding IP. Test `TestIPAllocatorReleaseSubnet` updated to assert the new explicit-zero behaviour. |
| 822 | H | core (build) | `ImageManager.Build` silently fell back to the local Dockerfile parser when no cloud build service (CodeBuild / Cloud Build / ACR Tasks) was configured. The local parser drops `RUN` steps as no-ops, so `docker build` produced a "successful" build whose image had none of the user's `RUN` results — a classic synthetic-success that hid the misconfiguration. Fix: `Build` now returns `NotImplementedError` with a message naming the missing prerequisite ("requires a cloud build service") plus an opt-in env (`SOCKERLESS_LOCAL_DOCKERFILE_BUILD=1`) for the deliberate parse-only mode used by docker-backend smoke tests. |
| 823 | H | core (network) | `LinuxNetworkDriver.Create` and `Connect` logged a warning and silently continued in synthetic-only mode when the netns / veth-pair creation failed. Containers thought they had real Linux networking but actually didn't — cross-container DNS / port bindings broke without an obvious cause. Fix: any netns failure now rolls back the synthetic record and returns a real error (`ServerError` with the underlying netns error). Operators who want best-effort degradation must opt in via `SOCKERLESS_NETNS_BEST_EFFORT=1`. |
| 824 | M | core (network) | `BaseServer.buildEndpointForNetwork` returned a synthesised `EndpointSettings` with a hardcoded `172.17.0.<container-count+2>` IP for unknown networks. This collided with real bridge allocations and silently masked real "network not found" bugs as fake-success. Fix: returns nil for unknown networks; callers (`ContainerCreate`'s default-network setup + the explicit `NetworkingConfig` loop) now skip nil endpoints, so the container is created without that endpoint and the operator's later `docker network inspect` / `docker network connect` surfaces the missing network properly. |
| 825 | M | core (image registry) | `ImageManager.Remove` silently logged a warning when the cloud-registry side delete failed (`m.Auth.OnRemove`), so `docker rmi <ecr-uri>` reported success even when the image was still in ECR. Fix: aggregate cloud-side errors and return a `ServerError` listing them — the local removal succeeded, so we return the local result *plus* the cloud error message so the operator can rerun rmi or inspect the cloud-side state. |
| 826 | M | core / ecs / aca / cloudrun / cloudrun-functions / azure-functions | `docker stop` / `docker rm -f` / `docker restart` and `pod stop` emitted synthetic `container.die` events with `exitCode: "0"` regardless of how the container actually died. Real docker convention is `128 + signal_number` — SIGTERM → 143, SIGKILL → 137. Tooling consuming `docker events` (CI runners, observability) treated these stops as graceful zero-exits, hiding real signal information. Fix: every `*.ContainerStop` / `*.ContainerRestart` / force-`*.ContainerRemove` / `*.PodStop` site now uses `core.SignalToExitCode("SIGTERM")` (143) for stop/restart and `core.SignalToExitCode("SIGKILL")` (137) for force-remove; the same value is passed to `Store.ForceStopContainer` so `ContainerInspect.State.ExitCode` reads honestly. Touched: `core/backend_impl.go`, `core/backend_impl_pods.go`, `ecs/backend_impl.go`, `aca/backend_impl.go`, `cloudrun/backend_impl.go`, `cloudrun-functions/backend_impl.go`, `azure-functions/backend_impl.go`, `azure-functions/backend_impl_pods.go`. |
| 827 | M | sim/azure + sim/gcp | The Azure ContainerApps simulator (`simulators/azure/containerapps.go::handleJobsBeginStart`) and the GCP Cloud Run Jobs simulator (`simulators/gcp/cloudrunjobs.go::handleJobsRun`) injected `"Execution completed successfully"` into the log stream even when the underlying user container exited non-zero (the goroutine's `succeeded` flag was already false but the success message was emitted unconditionally). Synthetic-success in the log mask broke arithmetic-invalid integration tests that asserted on the `ERROR` / failure marker, and would mislead any operator reading the log stream during incident response. Fix: branch on `succeeded` — emit `"Execution completed successfully"` on true, `"Execution failed"` on false. Same shape applied symmetrically across both sims. |
| 828 | H | core (network) | `NetnsManager.CreateVethPair` silently ignored errors from the three configuration commands that finish the veth setup — `ip addr add`, `ip link set up` (inside netns), and `ip link set up` (host side). On any failure the container ended up with a half-configured veth (no IP, link down) but `Connect()` returned success and the operator only noticed when network reach broke. Fix: each step is now error-checked, rolls back the veth pair on first failure, and surfaces a clear `failed to assign IP / bring veth up` error so the LinuxNetworkDriver's BUG-823 strict path can either propagate or fall back per `SOCKERLESS_NETNS_BEST_EFFORT`. |
| 829 | M | gcp-common + azure-common (auth) | `ARAuthProvider.OnRemove` and `ACRAuthProvider.OnRemove` silently `continue`d on per-tag delete failures (network error, HEAD/DELETE non-2xx, etc.) — the function returned nil success even when several tags couldn't be removed, so `docker rmi <ar-uri>` / `<acr-uri>` reported success while the cloud-side state diverged from local. Same shape as BUG-825 but at the per-tag granularity instead of per-image. Fix: collect per-tag failures into a slice and return a combined `fmt.Errorf` so the `ImageManager.Remove` aggregator (BUG-825) surfaces them via HTTP error. Stale `// All failures are non-fatal` docstrings on the AWS/GCP/Azure provider hooks rewritten to match reality. |
| 830 | M | docker | The docker passthrough backend hardcoded `NCPU: 4, MemTotal: 8 GB, OSType: "linux", Architecture: "amd64", OperatingSystem: "Docker Desktop"` in its `BackendDescriptor`. The `/info` handler reads real values from the daemon at request time, but if the daemon was unreachable at startup the hardcoded fallback values would be served — sockerless `docker info` would report `NCPU: 4` even when the underlying machine had 16 cores. Fix: query the daemon's `/info` at `NewServer` time with a 5-second deadline; if unavailable, fail startup with a clear error rather than silently using fabricated capacity values. The descriptor now reflects real daemon-reported `NCPU`, `MemTotal`, `OSType`, `Architecture`, `OperatingSystem`, `ServerVersion`. |
| 831 | L | core / ecs / lambda / cloudrun / cloudrun-functions / aca / azure-functions | `ContainerCreate` (and the post-restart `cloud_state` projection in cloudrun-functions / azure-functions / lambda) seeded `EndpointSettings.IPAddress` as the literal string `"0.0.0.0"` for cloud containers that don't yet have a routable IP (FaaS containers in particular never get one). `docker inspect` on a freshly-created container then displayed `IPAddress: 0.0.0.0` which read as a real address — confusing operators reading the inspect output. Fix: switch the seed to the empty string `""`. The two-phase service-discovery path in `aca/cloudrun/service_discovery_cloud.go` already accepted both `""` and `"0.0.0.0"` as the unresolved-IP sentinel (`if ip == "" || ip == "0.0.0.0"`) so existing detection logic still works on state recovered from older registries — but new state is honest. |
| 832 | M | sim/aws | (Phase 108 AWS audit) The AWS simulator's ECS service registered `ListTagsForResource` but no `TagResource` / `UntagResource` handler — `r.Register("AmazonEC2ContainerServiceV20141113.TagResource", …)` was missing. Backend code paths that tag tasks (BUG-781 kill-signal tag for exit-code mapping, BUG-772 restart-count tag, the rename `sockerless-name` tag) silently no-op'd against the sim because `TagResource` returned 404. Fix: added `handleECSTagResource` + `handleECSUntagResource` + the `mergeECSTagsByKey` merge helper in `simulators/aws/ecs.go`. Both handlers reject `LastStatus=STOPPED|DEPROVISIONING` tasks with `InvalidParameterException` to mirror real ECS (BUG-799's "skip STOPPED" logic exercises the same gate). All 33 AWS rows in `specs/SIM_PARITY_MATRIX.md` are now ✓. |
| 833 | H | sim/gcp | (Phase 108 GCP audit) The GCP simulator only registered v1 Knative routes for Cloud Run services (`/v1/namespaces/{ns}/services`), but the cloudrun backend uses `cloud.google.com/go/run/apiv2.NewServicesRESTClient` which calls v2 REST paths `/v2/projects/{p}/locations/{l}/services`. With `Config.UseService=true` (env var `SOCKERLESS_GCR_USE_SERVICE=1`), every `Services.{Create,Get,List,Delete,Update}Service` call hit the sim's catch-all 404 — no integration test caught it because the existing `TestConfig_Validate_UseService*` tests only exercise config validation, not the real RPC path. Fix: added `simulators/gcp/cloudrunservices.go` with `registerCloudRunServicesV2` covering Create/Get/List/Update/Delete on the v2 surface, returning the proto-JSON shape `runpb.Service` expects (TerminalCondition with `CONDITION_SUCCEEDED`, LatestReadyRevision populated so `serviceContainerState` reads "running", generation as int64-encoded-as-string). Wired into `main.go` and pinned with SDK tests using `run.NewServicesRESTClient` directly + a CLI smoke test against the v2 paths. All 16 GCP rows in `specs/SIM_PARITY_MATRIX.md` now ✓. |
| 834 | H | sim/azure | (Phase 108 Azure audit) The Azure simulator only registered Container Apps Jobs routes; the v2 ContainerApps "Apps" surface (`Microsoft.App/containerApps/{name}`) was completely absent. The aca backend's `Config.UseApp=true` path uses `armappcontainers.NewContainerAppsClient` to call `BeginCreateOrUpdate`/`Get`/`BeginDelete` — all silently 404'd against the sim. Same shape as BUG-833 (sim missing UseApp/UseService surface). Fix: added `simulators/azure/containerapps_apps.go` with `registerContainerAppsApps` covering PUT/GET/LIST/DELETE on `/subscriptions/{s}/resourceGroups/{r}/providers/Microsoft.App/containerApps[/{name}]`. Returns proto-JSON shape `armappcontainers.ContainerApp` expects: `properties.provisioningState=Succeeded`, `properties.latestReadyRevisionName` populated (so `appContainerState` reads "running"), `properties.latestRevisionFqdn` populated (so `cloudServiceRegisterCNAME` can seed Private DNS). Pinned with SDK tests using `armappcontainers.NewContainerAppsClient` (the same client the backend uses) plus an `az rest` CLI smoke test. |
| 835 | M | sim/azure | (Phase 108 Azure audit) The Azure simulator was missing the `PUT /subscriptions/{s}/resourceGroups/{r}/providers/Microsoft.Web/sites/{site}/config/azurestorageaccounts` handler. The azure-functions backend's `volumes.go` calls `WebApps.UpdateAzureStorageAccounts` with an `AzureStoragePropertyDictionaryResource` to bind named docker volumes to Azure Files shares; against the sim it 404'd, so any function app that requested a named volume failed at start. Fix: added the handler + a symmetric `GET .../config/azurestorageaccounts/list` to `simulators/azure/functions.go`, plus the `AzureStoragePropertyDictionaryResource` and `AzureStorageInfoValue` wire types matching `armappservice`. The site's `AzureStorageAccounts` map is stored in-memory and round-trips on the list endpoint. Pinned with an SDK test using `armappservice.NewWebAppsClient` (matches backend) + a CLI smoke test. |

PR #120 CI failure pass — failures from the first end-to-end run of the typed-driver migration branch (closed in this branch):

| ID | Sev | Area | Summary |
|----|-----|------|---------|
| 836 | H | sim/aws | `TestECS_TagResource_RejectsStoppedTask` failed with task stuck at `LastStatus=RUNNING` 60s after `RunTask` of `alpine sh -c "exit 0"`. Root cause: `simulators/aws/ecs.go` only ran the real podman container when the task definition had an `awslogs` log driver configured — the start path (`sim.StartContainerSync` + the `handle.Wait()` goroutine that flips state to STOPPED) was nested inside the awslogs-injection loop. Tasks without log config never started a container, so the lifecycle goroutine never fired and the task stayed RUNNING forever — a synthetic-success that hid real lifecycle bugs. Fix: pulled container start out of the awslogs branch — runs unconditionally when `imageURI != ""`. Log-driver path now picks the sink (`cwLogSink` for awslogs / new `discardLogSink` otherwise) and creates the CloudWatch group/stream + initial event; container start is shared. Real containers run for every task definition, lifecycle is honest. |
| 837 | L | tests/aws-sdk | Same `TestECS_TagResource_RejectsStoppedTask` had a fixed `time.Sleep(8 * time.Second)` waiting for STOPPED. Even after BUG-836 made the lifecycle real, podman cold image pulls + start can take >8s under CI runner contention. Fix: replaced with a 60s poll loop that breaks as soon as `LastStatus == "STOPPED"`. Pattern mirrors how `TestECS_TaskExecutesCommand` waits, but bounded so a genuine state bug fails the assertion within the deadline instead of hanging. |
| 838 | M | sim/gcp | `TestSDK_CloudRunV2Services_CreateGetListDelete` against the v2 REST client (`run.NewServicesRESTClient`) returned 400 `json: cannot unmarshal number into Go struct field ServiceV2.ingress of type string`. Root cause: the Go REST client serializes `runpb.IngressTraffic` as a numeric proto-JSON enum, but `simulators/gcp/cloudrunservices.go::ServiceV2.Ingress` was typed `string`. Real Cloud Run accepts both forms (proto-JSON spec). Fix: introduced an `enumString` type whose `UnmarshalJSON` accepts both `"INGRESS_TRAFFIC_INTERNAL_ONLY"` and `4` and round-trips via `MarshalJSON` as a string. Sim now matches real Cloud Run's tolerant decode. |
| 839 | M | sim/azure | `TestAzureFunctions_InvokeInjectsLogEntries`, `_InvokeExecutesCommand`, `_InvokeNonZeroExit` failed in CI Linux. Root cause: `simulators/azure/functions.go` created every site with `DefaultHostName = r.Host` (the simulator's own listen address), so multiple sites in the store all shared the same `DefaultHostName` and the invoke handler's host-based routing returned whichever site `sites.List()` happened to enumerate first — the wrong site got the AppTrace and the test querying `AppRoleName == "<right-site>"` came back empty. The miss was masked by a synthetic `{}` 200 response when no site matched at all. Fix: each site now gets a unique `<name>.azurewebsites.net` DefaultHostName (matches real Azure's per-site hostname format). Invoke handler still routes by HTTP Host header (real Azure behavior) but now every site is uniquely addressable; on no-match it returns a clean 404 instead of synthetic success. The azure-functions backend's invoke goroutine in `backend_impl.go::ContainerStart` (and recovery path in `cloud_state.go`) now POSTs to the simulator's TCP endpoint with `req.Host` pinned to the site's DefaultHostName — same as how virtual-hosted services route by Host header in production. New `invokeURLForHost` helper centralizes the URL/Host split; `AZFState.FunctionHost` field carries the Host header through pendingCreate→start. SDK + CLI tests updated to match. Also added `defer azureDeleteSite(rg, "sdk-storage-site")` to `TestSDK_WebApps_UpdateAzureStorageAccounts` so it doesn't leak a site into subsequent tests. |
| 840 | L | tests/e2e | `TestImageBuild` in `tests/system_test.go` POSTed to `/v1.44/build` against the ECS backend and expected 200 + a stream of `{"stream":...}` JSON messages culminating in a built image. That worked pre-BUG-822 because `ImageManager.Build` silently fell back to a local Dockerfile parser that drops `RUN` steps as no-ops. After BUG-822 closed that synthetic path, the e2e backend (no CodeBuild configured) correctly returns 501 — but the test was still asserting the old 200 contract. Fix: rewrote the test to assert the 501 + `cloud build service` error message, matching the real (no-fakes) contract. The test now actively guards against the synthetic local-build path being reintroduced. |
| 841 | H | ecs | `docker kill <ecs-cid>` returned `501: docker pause/unpause requires a bootstrap that writes /tmp/.sockerless-mainpid`. Root cause: the Phase-104 typed-driver migration registered `ssmSignalDriver` as the ECS backend's typed `SignalDriver` and wired `handleContainerKill` to dispatch through `s.Typed.Signal.Kill`. `ssmSignalDriver` calls `ContainerSignalViaSSM` which only knows how to deliver SIGSTOP/SIGCONT (real `docker pause` / `docker unpause`) by signaling the user PID in `/tmp/.sockerless-mainpid`. Every `docker kill` and `docker stop -s` therefore demanded a bootstrap file that only pause/unpause needs. Real-docker semantics: SIGSTOP/SIGCONT pause/resume the container; everything else (SIGTERM/SIGKILL/etc.) is a stop request — on a managed runtime that translates to terminating the task. Fix: `ssmSignalDriver.Kill` now branches on the signal — SIGSTOP/SIGCONT route via SSM, every other signal routes to legacy `ContainerKill` (which calls `ECS.StopTask`). |
| 842 | H | sim/aws | `docker cp <tar> <ecs-cid>:/path` produced `tar: short read` against the simulator. Root cause: the AWS simulator's ECS exec WebSocket handler bridged stdout/stderr from the Docker exec back to the WebSocket but **completely ignored stdin** — incoming `input_stream_data` AgentMessage frames from the backend were dropped on the floor, and the Docker exec wasn't even configured with `AttachStdin: true`. Tar saw an immediately-closed stdin and aborted. Real ssm-agent decodes the AgentMessage protocol, forwards only the `Payload` portion of `input_stream_data` frames to the user process's stdin, and closes stdin when the FIN flag is set. Fix: added `AttachStdin: true` to the exec config, wrapped the Docker exec stream attach with a goroutine that reads incoming WebSocket frames, decodes the AgentMessage header (via new `decodeSSMInputFrame` in `simulators/aws/ssm_proto.go`), forwards only `input_stream_data` payloads to `attach.Conn` (the exec's stdin), and calls `attach.CloseWrite` on FIN so readers see EOF. Same protocol the backend already speaks for outbound frames — sim now matches the real-AWS contract end-to-end. |
| 843 | M | ecs | `docker cp <tar> <ecs-cid>:/var/run/act/workflow` (where `/var/run/act/workflow` doesn't exist) failed with `tar: can't change directory: No such file or directory` against the simulator and against real ECS via SSM. Root cause: real Docker daemon's `CopyToContainer` creates the destination directory if missing, but `ContainerPutArchiveViaSSM` ran a bare `tar -xf - -C <path>` which assumes the path exists. `act` (and most GitHub Actions runners) routinely copy into paths the test container hasn't created yet. Fix: `mkdir -p <path> && tar -xf - -C <path>` upfront, single shell command (after BUG-842 made stdin-piping reliable). Matches docker daemon's behavior: copy succeeds even if the destination didn't exist before. |
| 818 | H | sim/aws | After BUG-815 the ECS backend wraps every exec command in `sh -c '<script>'`. The AWS simulator's `handleECSExecWebSocket` had an unwrap branch (`strings.Index("-c ") + 3`) that left the surrounding single quotes intact when handing the script to `docker exec`, so the inner shell saw `'echo $((7 * 6))'` as a single quoted command name → `sh: 'echo …': not found`. Closes e2e regressions on `TestArithmeticExecInContainer/ecs`, `TestGitHubRunner*`, `TestGitLabRunner*`, `TestExecShellScript`. Fix: drop the unwrap branch entirely, always wrap — double-wrapping is correct (outer shell parses the wrapped script and dispatches the inner shell itself). |
| 819 | H | terraform | Round-9 ECS teardown hung 30+ min on `aws_security_group.task` / `aws_subnet.private` because two AWS-Lambda hyperplane ENIs (`Description="AWS Lambda VPC ENI-skls-…"`, attachment type `ela-attach-…`) remained `in-use` in the VPC after the runtime-created sockerless-managed Lambda functions were deleted out-of-band. AWS Lambda releases hyperplane ENIs lazily (5-30+ min in eu-west-1) and the attachments are AWS-Lambda-owned (`OperationNotPermitted` on operator-issued `DetachNetworkInterface`). Fix: (1) `terraform/modules/lambda/main.tf` gets a new `null_resource sockerless_runtime_sweep` that, on destroy, lists every sockerless-managed function, clears its `VpcConfig` (which signals AWS to start releasing the hyperplane ENI immediately), then deletes the function. (2) `terraform/modules/ecs/main.tf` extends the existing sweep with a Lambda-ENI poll: wait up to 30 min for `Description="AWS Lambda VPC ENI…"` ENIs to flip from `in-use` to `available`, then `delete-network-interface` so the SG/subnet destroy can complete. (3) Per-cloud parity — equivalent `null_resource sockerless_runtime_sweep` added to `cloudrun`, `gcf`, `aca`, `azf` modules so each backend's `terragrunt destroy` is self-sufficient (no out-of-band cleanup needed). |

Withdrawn / retroactive (audit pass):

| ID | Sev | Area | Summary |
|----|-----|------|---------|
| 802 | — | (withdrawn) | Initially filed against C5 export 0-byte tar in round-9; turned out to be a `timeout 60` measurement artifact — the SSM read-loop was slower than 60 s under BUG-789/798. Now that BUG-789/798 is fixed (round-9 marker-based exit-code recovery), the underlying behaviour is correct. No production code change. |
| 638 | H | core (registry) | (Retroactive) ECR push only called PutImage without uploading blobs — image manifests pointed at digests with no layer bytes underneath. Closed by BUG-788's real registry-to-registry layer mirror (`core.FetchLayerBlob` + `Store.ImageManifestLayers` + `OCIPush` reading the cached compressed bytes). Spec reference in `specs/IMAGE_MANAGEMENT.md` updated to point at BUG-788. |
| 640 | H | core (registry) | (Retroactive) Base `ImagePush` returned a synthetic "Pushed" stream regardless of whether OCI push succeeded, masking real failures as fake-success. Closed by BUG-788 — `ImagePush` now propagates the real `OCIPush` outcome. |
| 646 | H | core (registry) | (Retroactive) OCI push fell back to an empty-gzip layer when no real layer data was available. Closed by BUG-788 — `OCIPush` now requires `ManifestLayers` and uses each entry's compressed digest verbatim, no recompute, no empty-fallback (per the no-fallbacks project rule). |
| 648 | M | core (registry) | (Retroactive) `FetchImageMetadata` fell back to synthetic metadata when the source registry was unreachable / auth failed, hiding real auth bugs. Closed by Phase 90's no-fakes audit (BUG-731-734) and reinforced by BUG-788's metadata-must-be-real path. |

Round-8 (this PR):

| ID | Sev | Area | Summary |
|----|-----|------|---------|
| 786 | M | core | `docker rmi <tag>` reported `Untagged: <tag>` but the tag reappeared on later `docker images` calls. Root cause: `StoreImageWithAliases` puts the same `api.Image` value under multiple keys (ID, ref, repo-no-tag, docker.io shorts) for fast `ResolveImage` lookups; `ImageRemove`'s partial-untag branch only updated the "remaining" tag entries + ID, leaving alias entries with stale `RepoTags`. `ImageList` then iterated and dedup-by-ID could pick a stale entry. Fix: after rebuilding `RepoTags`, sweep every `Store.Images` entry whose `Value.ID` matches and `Put` the freshly-trimmed value. New `StateStore.Entries()` helper exposes the snapshot under-lock. |
| 787 | L | docs | `specs/CLOUD_RESOURCE_MAPPING.md` lagged the implementation across multiple landed phases. Fixes in same PR: (1) GCP backends use `sockerless_managed`/`sockerless_container_id` (underscore) — doc now documents the per-cloud spelling rule. (2) Volume table updated to ✓ — Phases 91/92/93/94/94b landed real EFS/GCS/Azure Files. (3) ECS Container{Top,Changes,StatPath,GetArchive,PutArchive,Export,Pause,Unpause} updated to "⚠ via SSM (Phase 102)". (4) Simulator-coverage table now lists items as closed (`simulators/aws/efs.go`, `simulators/azure/files.go`, ACA console exec, Cloud Run reverse-agent). (5) Duplicate ImagePush row removed. (6) Tags `sockerless-restart-count`, `sockerless-kill-signal`, `sockerless-removed`, `sockerless-labels-b64-<n>` now documented. (7) "Phase 89 status" reframed as closed. New "Acceptable gaps" section with maintainer-approved exceptions (ECS commit, ECS pause-without-bootstrap, ContainerResize/ExecResize, ImageSave, ImageSearch, streaming stats, ContainerTop without exec, ContainerExport, ContainerRename semantic divergence). |
| 788 | H | core | `docker push <ecr-uri>` failed with `push failed: image has no layer data available` whenever the source image was pulled from a registry. Root cause: `ImagePull` only stored manifest metadata (config digest, diff_ids, sizes) — never the actual gzipped layer bytes — and `Store.LayerContent` only got populated by `ImageLoad` / `ContainerCommit`. `OCIPush` then couldn't read the bytes. Fix: (1) `FetchImageMetadata` extracts each layer's compressed-blob digest + media type from the source manifest, not just sizes; (2) new `core.FetchLayerBlob` downloads a layer blob from the source registry; (3) `BaseServer.ImagePull` calls it for every layer, caches bytes in `Store.LayerContent[compressedDigest]`, and stores `[]ManifestLayerEntry` keyed by image ID in `Store.ImageManifestLayers`; (4) `OCIPush` requires `ManifestLayers` and uses each entry's digest verbatim — no recompute-from-content; (5) `ImageLoad` and `ContainerCommit` also populate `ImageManifestLayers`. Verified live: `docker pull public.ecr.aws/.../alpine && docker tag ... && docker push <ecr>` → `Pushed`. |
| 790 | M | ecs | `docker stop <id>` returned success before the ECS task actually reached STOPPED, causing immediate `docker rm` to fail with "cannot remove a running container". Fix: `ContainerStop` now blocks on a new `waitForTaskStopped` poller until `LastStatus=STOPPED` (timeout = caller-supplied + 30 s grace, default 90 s); errors out cleanly if `StopTask` fails or the deadline expires (no fallback "we stopped it" event). The stale synthesised `container.die` event with fabricated `exitCode=0` was also removed — the cloud-state poller emits the real `die` once it observes STOPPED with the actual exit code. |
| 791 | L | core | `docker cp <cid>:<path> <local>` returned a misleading `404 Not Found for API route` even when the route exists. Fix: `handleGetArchive` and `handleHeadArchive` in `backends/core/handle_containers_archive.go` now route through `WriteError(w, err)` (matching `handlePutArchive`) so the underlying error reaches the client with proper status code mapping. |
| 792 | L | ecs | `docker commit <ecs-cid>` error message contained a phase reference (`Phase 98b`). Fix: rewritten to describe the architectural prerequisite (no Fargate host fs, no SSM-bootstrap-rooted diff capture) without naming a phase. ECS commit is also now classified as an **acceptable gap** in `specs/CLOUD_RESOURCE_MAPPING.md` per maintainer decision. |
| 793 | H | lambda + terraform | Lambda `ContainerCreate` failed with `InvalidParameterValueException: The provided execution role does not have permissions to call CreateNetworkInterface on EC2` whenever `SOCKERLESS_LAMBDA_SUBNETS` was set. Fix: `terraform/modules/lambda/main.tf` now attaches `AWSLambdaVPCAccessExecutionRole` (in addition to `AWSLambdaBasicExecutionRole`) so the Lambda service can create / describe / delete ENIs in the configured subnets. |
| 794 | H | ecs | Cross-network isolation broken — a container with no `--network` flag could reach a `--network commsnet` peer over its private IP. Root cause: every Fargate task got the operator's default `task_security_group_id` regardless of network membership, and per-network SGs were *added* — so the default SG's permissive rules shadowed isolation. Fix: in `backends/ecs/containers.go`, when `ECSState.SecurityGroupIDs` is non-empty (container is on a user-defined network), use ONLY those SGs; the default SG is applied only to containers without an explicit network. Verified live: peer ON net reaches via service-name DNS; peer OFF net `wget` times out. |
| 796 | M | ecs (libpod pods) | `podman pod rm <name>` failed with `has running containers, cannot remove without force` immediately after `podman pod stop` returned. Same async-stop pattern as BUG-790. Fixed transitively by BUG-790's `waitForTaskStopped` — `BackendImpl.PodStop` calls into `ContainerStop` for each member task and now blocks until each task reaches STOPPED, so a follow-up `pod rm` sees them all terminated. Verified live: F11/F12 in the v2 sweep both succeed. |
| 797 | M | lambda | `ContainerCreate` against `public.ecr.aws/...` images failed with `image manifest ... not supported` because Lambda's `resolveImageURI` always rewrote them through an ECR pull-through cache that returned a multi-arch manifest list (which `CreateFunction` rejects). Fix: ported the ECS short-circuit (BUG-776) — when the ref already points at `public.ecr.aws/`, pass through directly. Lambda still requires a Lambda-runtime-compatible image (or the sockerless agent overlay) for the actual function to run; runbook clarification will follow when Track D gets a Lambda-compatible test image. |
| 799 | M | ecs | After backend restart, `docker rmi <tag>` for any image used by a previously-STOPPED ECS task incorrectly returned `conflict: unable to remove repository reference (must force)`. Root cause: `backends/ecs/recovery.go::ScanOrphanedResources` returned every sockerless-managed task tagged with our instanceID — including STOPPED ones. `core.ReconstructContainerState` re-populated `Store.Containers` with these ghosts, and `ImageRemove`'s in-use check tripped. Fix: skip `LastStatus=STOPPED`/`DEPROVISIONING` tasks in `ScanOrphanedResources` — only RUNNING / PENDING tasks become "active orphans" the registry tracks. STOPPED tasks remain visible in cloud-derived `docker ps -a` for inspect/log purposes. |
| 800 | H | core | **Stateless invariant violation.** `core.ResourceRegistry` was persisting to `./sockerless-registry.json` by default — found 11 stale registry files scattered across the repo. On startup, `RecoverOnStartup` would `registry.Load()` from disk then merge with a cloud scan, so stale on-disk entries persisted indefinitely and the operator's CWD became load-bearing for backend recovery. Direct contradiction of `specs/CLOUD_RESOURCE_MAPPING.md` § State boundaries. Fix: collapsed `Save`/`Load`/`autoSave` to no-ops; the registry is purely in-memory, source-of-truth is the cloud (sockerless-managed=true tags + cloud scan on startup). All 11 stale files deleted. Persistence-related tests rewritten to cover in-memory semantics only. |

Round-7:

| ID | Sev | Area | Summary |
|----|-----|------|---------|
| 770 | M | core | `ImageRemove` response reported phantom untag/delete when an image had multiple tags — `docker rmi myalpine:v1` (aliasing `alpine:latest`) returned `[Untagged alpine:latest, Untagged myalpine:v1, Deleted <id>]` even though only `myalpine:v1` was removed. Fix: `core.backend_impl.go::ImageRemove` resolves the user-supplied ref against the image's `RepoTags`, returns only the matching `Untagged` entry, and emits `Deleted` only when the image's last tag is being dropped. |
| 771 | M | ecs | `ContainerInspect` returned empty `Path`, `Args`, `Config.Cmd`, `Config.Entrypoint` for cloud-derived containers — the task describe path never read the task definition. Fix: new `describeTaskDefinition` cache in `cloud_state.go` lazily fetches the task-def per ARN; `taskToContainer` pulls `entryPoint` / `command` / `workingDir` / `environment` from the "main" `ContainerDefinition`. ECS `ContainerCreate` also auto-pulls image metadata when the local store is empty so image-default CMD/ENTRYPOINT flow through to the task-def. |
| 772 | M | ecs | `docker restart` didn't bump `RestartCount`. Fix: `TagSet.RestartCount` → `sockerless-restart-count` tag on RunTask (new field in `core/tags.go`), ECS `ContainerRestart` updates the container's `ECSState.RestartCount` before re-start, and `taskToContainer` reads the tag back. Also resets `State.Running=false` on the re-queued PendingCreate so `ContainerStart` doesn't short-circuit with NotModified. |
| 773 | H | ecs | `docker rename` silently desynchronized in-memory Store from cloud state. Fix: `Server.ContainerRename` now calls `TagResource` to overwrite the task's `sockerless-name` tag after `BaseServer.ContainerRename` updates local state; failure surfaces as a real error (no silent fallback). `handleContainerRename` is also rewired to route through `s.self.ContainerRename` so backend overrides actually run. |
| 774 | H | ecs | `docker ps -a` returned duplicate rows after `docker restart` — both the stopped and the fresh task shared a container ID. Fix: `queryTasks` in `cloud_state.go` dedupes by `sockerless-container-id` tag, preferring the RUNNING task over STOPPED and the most recent `CreatedAt` as a tiebreaker. |
| 775 | H | ecs | `docker rm` / `docker rm -f` silently no-op'd because ECS keeps STOPPED tasks visible for ~1 hour and `queryTasks` still surfaced them. Fix: `ContainerRemove` now calls `markTasksRemoved(id)` which iterates every (RUNNING + STOPPED) task for the container ID and writes them to the local `ResourceRegistry` as cleaned-up. `queryTasks` filters out ARNs the registry marks cleaned-up. Source of truth is the registry because ECS rejects `TagResource` on STOPPED tasks. |
| 776 | H | ecs | `ContainerCreate` silently fell back to the raw image ref when ECR pull-through URI resolution failed. Fix: `buildContainerDef` returns the resolution error (no `WRN ... may not be able to pull` fallback). `resolveImageURI` also short-circuits for `public.ecr.aws/...` refs since Fargate pulls them directly without a pull-through cache. |
| 777 | H | core (auth flow) | `docker push <ecr-uri>` always returned `401 Not Authorized`. Fix: `ImageManager.Push` now always replaces the caller-supplied auth with a fresh `Auth.GetToken(registry)` for cloud registries (Docker CLI's base64-wrapped JSON `X-Registry-Auth` was being fed to OCIPush as a raw Bearer token). |
| 778 | M | core (libpod) | `podman inspect` failed with `parsing time ""` because the backend returned empty `FinishedAt` for running containers. Fix: new `normalizeContainerTimes` in `handle_containers_query.go` fills empty `StartedAt`/`FinishedAt`/`Created` with `0001-01-01T00:00:00Z` before serialisation. |
| 779 | H | core (libpod) | `GET /libpod/containers/json` returned `[]` on stateless backends. Fix: `handleLibpodContainerList` now queries `CloudState.ListContainers` and merges `PendingCreates`, matching the docker-API list path. |
| 780 | H | core (libpod) | `podman create --name <n>` → `podman start <n>` failed with `no such container` because podman's libpod specgen JSON is flat (lowercase `image`, `command`, etc.) and our handler only knew about Docker-compat's `ContainerConfig` wrapper. Fix: `handleLibpodContainerCreate` reads the specgen fields (`image`, `command`, `entrypoint`, `env`, `work_dir`, `labels`, `terminal`, `stdin`, `name`, `pod`) and merges them into `ContainerCreateRequest.ContainerConfig` when the Docker-compat wrapper is empty. |
| 781 | L | ecs | `docker kill -s SIGTERM <nginx>` reported the raw container exit code (typically 1 for nginx on SIGTERM), not Docker's 128+signum convention. Fix: `ContainerKill` stamps `sockerless-kill-signal=<sig>` on the task before `StopTask`; `mapTaskStatus` reads the tag on STOPPED tasks and overrides `ExitCode` to `SignalToExitCode(signal)`. |
| 782 | L | ecs | `docker stats` NAME column showed `"--"` because the name lookup only hit `Store.Containers`. Fix: `buildStatsEntry` uses `ResolveContainerAuto` so cloud-derived containers return their tagged name. |
| 783 | H | ecs | Docker network's per-network security group was never attached to Fargate task ENIs (containers launched with `--network X` still only had the default SG). Fix: `ContainerCreate` calls `cloudNetworkConnect(networkID, id)` at create time when the initial network isn't bridge/host/none, so the network's SG is queued onto `ECSState.SecurityGroupIDs` for `RunTask`. Previously this wiring only ran for explicit `docker network connect` on an already-created container. |
| 784 | H | ecs | `docker compose up` failed with `RunTask InvalidParameterException: Some tags contain invalid characters` because Docker labels were serialised as raw JSON (with `{`, `"`, `,`, `}`) into the `sockerless-labels` ECS tag. Fix: `TagSet.AsMap` now emits `sockerless-labels-b64` (URL-safe base64 of the JSON, split across `-b64-<n>` chunks when > 256 chars). `ParseLabelsFromTags` decodes both the new format and the legacy raw-JSON one for backward compatibility. |
| 785 | L | core | `ResourceRegistry.autoSave` logged `rename X.tmp X: no such file or directory` warnings on every op when the parent directory didn't exist. Fix: `Save` now `MkdirAll`s the parent before writing `.tmp`. |
| 746 | M | cloudrun/gcf | Docker label filter didn't match cloud-managed containers — labels were serialised as a JSON blob into a single GCP label, but GCP's label-value charset rejects `{`, `:`, `"`. Fixed by **Phase 97**: `core.AsGCPLabels` filters out charset-invalid values (they move to `AsGCPAnnotations`); Cloud Run's cloud_state merges Job/Service annotations into the parse map; GCF carries labels as a base64-JSON `SOCKERLESS_LABELS` env var. |
| 748 | M | lambda | `Lambda.VolumeCreate/Inspect/List/Remove/Prune` returned `NotImplementedError`. Fixed by **Phase 94b**: provisions EFS access points via the shared `awscommon.EFSManager` and attaches them at `CreateFunction` via `Function.FileSystemConfigs[]`. Requires `SOCKERLESS_LAMBDA_SUBNETS` (Lambda-in-VPC). |
| 753 | M | all-cloud | `docker diff` (`ContainerChanges`) returned `NotImplementedError` across every cloud backend. Fixed by **Phase 98**: `core.RunContainerChangesViaAgent` runs `find / -xdev -newer /proc/1` over the reverse-agent and parses output into `api.ContainerChangeItem`. Wired in Lambda/CR/ACA/GCF/AZF. |
| 752 | M | all-cloud | `docker container top` returned `NotImplementedError` on CR / ACA / GCF / AZF / Lambda. Fixed by **Phase 98**: `core.RunContainerTopViaAgent` runs `ps` over the reverse-agent. |
| 751 | M | all-cloud | `docker cp` / `docker export` / `docker container stat` (`ContainerGetArchive` / `ContainerPutArchive` / `ContainerStatPath` / `ContainerExport`) returned `NotImplementedError` across every cloud backend. Fixed by **Phase 98**: `core.RunContainer{GetArchive,PutArchive,StatPath,Export}ViaAgent` use tar/stat over the reverse-agent. |
| 750 | M | all-cloud | `docker commit` (`ContainerCommit`) returned `NotImplementedError` on every cloud backend. Fixed by **Phase 98b**: new `core.CommitContainerViaAgent` runs `find / -xdev -newer /proc/1` (same reference point as `docker diff` from Phase 98) to list added/modified paths, pipes them through `tar -cf - --null -T -` to package a proper diff layer, stacks it on top of the source image's rootfs, and registers the resulting `api.Image` in the store. Each cloud backend's `ContainerCommit` routes through `core.CommitContainerRequestViaAgent` gated behind a `SOCKERLESS_ENABLE_COMMIT=1` config flag. Lambda/CR/ACA/GCF/AZF all supported; ECS stays `NotImplementedError` (no bootstrap). Docs updated in `specs/CLOUD_RESOURCE_MAPPING.md`. Deletions aren't captured — documented limitation, not a silent fallback. |
| 769 | L | core | `ImageHistory` synthesised per-layer entries with the fake `CreatedBy: "/bin/sh -c #(nop) ADD file:... in / "` text when no real registry-sourced history was stored for the image (e.g., locally-built images). Output looked like docker history but reflected nothing about the actual build. Fix: return a single top-level entry with just the image ID + size + creation time when no real history exists — no fabricated per-layer `CreatedBy` strings. Real history remains untouched for registry-sourced images where it's populated. |
| 768 | M | lambda | `ContainerCreate` silently fell back to the base image when `BuildAndPushOverlayImage` failed — warning logged, function created anyway with `docker exec` permanently broken. User set `SOCKERLESS_CALLBACK_URL` expecting exec to work; they got a function that superficially worked but had no bootstrap. Violates the "no fakes, no fallbacks" principle. Fix: overlay-build failure now surfaces as a `ServerError` from `ContainerCreate`, matching the resolve-image-URI failure path a few lines above. |
| 767 | M | agent/lambda-bootstrap | `sendHeartbeats` wrote a `websocket.PingMessage` directly to the reverse-agent WebSocket without taking the same mutex that `serveReverseAgent` / `router.Handle` use for response writes. gorilla/websocket requires serialised writes (see its Concurrency doc); concurrent calls to `WriteMessage` on the same conn can interleave frames on the wire. Fix: hoisted the conn mutex into `main` and passed it to both `serveReverseAgent` and `sendHeartbeats` so all writes go through the same lock. |
| 766 | M | lambda + agent/lambda-bootstrap | Lambda backend encoded the user's `Entrypoint`/`Cmd` into `SOCKERLESS_USER_ENTRYPOINT`/`_CMD` env vars using `:` as the argv separator (`strings.Join(config.Cmd, ":")`); the bootstrap parsed them back via `splitColon`. Any argv element containing `:` — e.g. `sh -c 'echo hello:world'` — was silently split into extra elements on re-parse. Fix: encode argv as **base64(JSON)** on both sides (backend + Dockerfile ENV line + bootstrap decoder). Base64's alphabet (A-Z a-z 0-9 + / =) needs no Dockerfile or shell quoting, so every byte round-trips exactly. Legacy `:`-joined path removed entirely (active development, no external overlay images to keep working). |
| 765 | H | agent/lambda-bootstrap + agent/process | Phase 99's pause/unpause helper (`core.RunContainerPauseViaAgent` and friends) sends `kill -SIGSTOP $(cat /tmp/.sockerless-mainpid)` over the reverse-agent, but no bootstrap actually writes that PID file. Result: pause/unpause always returned `NotImplementedError("requires a bootstrap that writes /tmp/.sockerless-mainpid")`. Fix: both spawning sites (`agent/cmd/sockerless-lambda-bootstrap/main.go::runUserInvocation` and `agent/process.go::NewMainProcess` for the generic `--keep-alive` path) now write the user-process PID to `core.MainPIDConventionPath` immediately after `cmd.Start()` and remove the file on exit. |
| 764 | H | core | `core.OCIPush` constructed an OCI image config blob with hardcoded `architecture: "amd64"`, `os: "linux"`, empty `config: {}`, and empty `rootfs.diff_ids: []` — regardless of the actual image being pushed. The manifest correctly referenced uploaded layer blobs, but the inconsistency between the manifest's layers and the config's empty `diff_ids` violates the OCI spec. Clients pulling such images get the wrong arch/os, no Entrypoint/Cmd/Env, and may reject the manifest as malformed. Fix: `OCIPushOptions` now takes `Architecture`, `OS`, `Config` (the `*api.ContainerConfig`) and the `ImageLayers` are written into `rootfs.diff_ids` so the config matches the manifest's layer list. `BaseServer.ImagePush` passes the stored image's real fields. |
| 763 | H | core + cloud-auth | `ImageAuthProvider.OnPush` (ECR/AR/ACR) called `core.OCIPush` without `ImageLayers` or `LayerContent`, so OCIPush always returned `"image has no layer data available"` and `ImageManager.Push` propagated that as a push failure. End result: `docker push <reg>/<repo>:<tag>` against any sockerless cloud backend always failed even with valid credentials. The CreateRepository side-effect happened but no blobs were uploaded. Fix: `OnPush` now does cloud-side bookkeeping only (e.g., ECR `CreateRepository`); the actual OCI push is delegated to `BaseServer.ImagePush` (which already had layer access), with the auth token obtained via `Auth.GetToken` so the user doesn't need to `docker login` first. |
| 762 | M | ecs | `ContainerPause`/`ContainerUnpause` returned `NotImplementedError` on ECS. Fixed by **Phase 102**: new `Server.ContainerSignalViaSSM` runs `sh -c 'kill -<sig> $(cat /tmp/.sockerless-mainpid)'` over SSM ExecuteCommand using the same pidfile convention (`core.MainPIDConventionPath`) as Phase 99. Bootstraps that don't write the pidfile cause an exit-64 → `NotImplementedError` naming the missing prerequisite. |
| 761 | M | ecs | `ContainerExport`/`ContainerTop`/`ContainerChanges`/`ContainerStatPath`/`ContainerGetArchive`/`ContainerPutArchive` returned `NotImplementedError` (or empty results) on ECS even though Fargate's filesystem is reachable through SSM `ExecuteCommand`. Fixed by **Phase 102**: new `backends/ecs/ssm_capture.go::RunCommandViaSSM` opens an SSM session, captures stdout/stderr/exit code via the existing AgentMessage frame parser, plus `backends/ecs/ssm_ops.go` wrapping it with the same shell commands (`ps`, `find`, `stat`, `tar`) the FaaS reverse-agent path uses; outputs go through `core.Parse{Top,Stat,Changes}Output` for parity. ECS now matches FaaS coverage for these ops. |
| 760 | M | sim/azure + lambda/cloudrun/gcf/azf | Two simulator-parity gaps surfaced by BUG-759. Fixed by **Phase 101**: (1) backend `aca/exec_cloud.go` now sends the user command in `?command=<urlencoded>` over the ACA jobs-exec WebSocket and chooses ws/wss by parsing `EndpointURL`; `simulators/azure/containerapps.go` serves the matching `POST .../executions/{exec}/exec` endpoint, upgrades to WebSocket, and bridges to a real `docker exec sh -c <command>` against the running execution container. (2) New `core.AttachViaCloudLogs` (lifted from the existing `backends/ecs/attach.go` pattern); every FaaS backend's `ContainerAttach` now falls back to streaming the cloud-native log API for read-only attach (no Stdin) when no reverse-agent session is registered. |
| 759 | M | lambda/cloudrun/gcf/azf/aca | `ContainerAttach` and `ExecStart` either returned blanket `NotImplementedError` (Lambda/CR/GCF/AZF) or always took the cloud-native ACA console path even when a reverse-agent session was registered. Result: callers couldn't drive `docker exec`/`docker attach` against a container with a live agent — the registered `Drivers.Exec`/`Drivers.Stream` (= `core.ReverseAgent{Exec,Stream}Driver`) were ignored. Fix: every cloud backend now resolves the container, then dispatches per `specs/CLOUD_RESOURCE_MAPPING.md` §Exec — agent path when a session exists, ACA console as the second-choice for ACA, else `NotImplementedError` naming the missing prerequisite. No more silent empty streams or fake exit-126s. |
| 745 | M | cloudrun/aca | Cloud Run Jobs and ACA Jobs had no native `docker exec` equivalent. Fixed by **Phase 96**: ported the Lambda reverse-agent bootstrap to Cloud Run + ACA via a prebuilt overlay image. Both backends register `core.ReverseAgent{Exec,Stream}Driver` against `/v1/cloudrun/reverse` and `/v1/aca/reverse`; routing finalised by BUG-759. ACA additionally falls back to its native console exec API. |
| 756 | M | agent/lambda-bootstrap + sim/aws/gcp/azure | Lambda (and every FaaS backend with a Lambda-style bootstrap) lost user-process stdout/stderr because the bootstrap redirected the subprocess's output into `bytes.Buffer`s that become the `/response` payload — those buffers never reached the container's stdout, so Docker (and therefore CloudWatch in the sim / the backend's `ContainerLogs` in production) saw nothing. Fix in `agent/cmd/sockerless-lambda-bootstrap/main.go`: tee the subprocess's stdout/stderr with `io.MultiWriter(&buf, os.Stdout)` / `os.Stderr` so the container log driver captures them. Separately, the sim's shared `waitAndCaptureLogs` had a Docker follow-stream race on short-lived containers — swapped for a single post-exit non-follow `ContainerLogs` read with a detached context. Symptom was `TestLambdaContainerLogsFollowLazyStream` seeing only `START/END/REPORT` markers without the `follow-line-1/2/3` subprocess lines; assertion restored. |
| 749 | M | all-cloud | `docker pause` / `docker unpause` returned `NotImplementedError` on every cloud backend. Fixed by **Phase 99**: the in-container bootstrap writes its user subprocess PID to `/tmp/.sockerless-mainpid`; `ContainerPause`/`ContainerUnpause` now send SIGSTOP/SIGCONT to that PID via a shared `sh -c 'kill -SIG $(cat <file>)'` exec routed over the reverse-agent WebSocket. Shared helpers live in `backend-core` (`RunContainerPauseViaAgent` / `RunContainerUnpauseViaAgent` + `MapPauseErr`). Bootstraps that don't participate in the convention surface as `NotImplementedError` with the missing-path called out. Applies to Lambda/CR/ACA/GCF/AZF; ECS still NotImplemented (SSM signal track deferred). |
| 744 | H | lambda/gcf/azf | FaaS CloudState couldn't signal "invocation complete" → inspect/list reported the function as `running` forever; `ContainerStop` LoadAndDelete'd the wait channel so a later `ContainerWait` saw no channel and fell through to `CloudState.WaitForExit`, which never terminated. Seven tests deleted as a stop-gap (`TestLambdaContainerLifecycle` / `TestLambdaContainerLogsFollowLazyStream` / `TestLambdaContainerStopUnblocksWait` / `TestGCFContainerLifecycle` / `TestGCFArithmeticInvalid` / `TestAZFContainerLifecycle` / `TestAZFArithmeticInvalid`). Fixed by **Phase 95**: new `core.InvocationResult` + `Store.{Put,Get,Delete}InvocationResult` capture the exit outcome at the invocation source (Lambda `Invoke` return, GCF/AZF HTTP status); CloudState overlays the recorded outcome on `queryFunctions` / `queryFunctionApps`; `ContainerStop`/`ContainerKill` write `{ExitCode: 137}` (or SignalToExitCode) so the stopped container surfaces as exited even though Lambda has no invocation-cancel API. All 7 tests re-enabled in the same commit set. |
| 758 | L | ci | smoke-tests Dockerfiles (ecs/aca/cloudrun + two gitlab variants) didn't COPY `agent/` into the build context. Phase 96's lift of the reverse-agent machinery into `backends/core` added a transitive `github.com/sockerless/agent` dep to every backend; their go.mod replace resolves to `../../agent`, but the Docker builds were running from `/src` without `agent/`. Fix: added `COPY agent/ agent/` alongside `COPY api/ api/` in 5 Dockerfiles (commit `a8e3081`). |
| 757 | L | ci | Phase 96 backend-core refactor added `github.com/sockerless/agent` as a dep, but only `backends/cloudrun`, `backends/aca`, and `backends/lambda` had the matching `replace github.com/sockerless/agent => ../../agent` directive. `backends/ecs`, `backends/docker`, `backends/aws-common`, `backends/gcp-common`, `backends/azure-common`, `backends/cloudrun-functions`, `backends/azure-functions` all failed build-check with `missing go.sum entry for module providing package github.com/sockerless/agent`. Fix: added replace directive + `go mod tidy` across all 7 modules (commit `549eae0`). Same class as BUG-755 — any shared-helper lift that adds a new transitive dep must tidy every importer. |
| 754 | M | docker | `BackendDocker.PodCreate/Inspect/List/Exists` returned `NotImplementedError`. Fixed by **Phase 100**: Docker backend now synthesises pods against the shared `sockerless-pod` label convention — PodCreate/Inspect/Exists use Store.Pods (via BaseServer); PodList merges in-memory entries with live Docker containers carrying the label; PodStart/Stop/Kill/Remove fan out to the Docker daemon over the SDK. Core handle_containers.go also injects the `sockerless-pod=<name>` label into the request before ContainerCreate so every backend (Docker included) tags the underlying resource. |
| 755 | L | ci | CI build-check failed on the Phase 94 prereq / AWS lift commit (`8fd555d`) — `backends/lambda` imported `backends/aws-common`, but aws-common's `go mod tidy` bumped `aws-sdk-go-v2` 1.41.5→1.41.6 + `smithy-go` 1.24.2→1.25.0 + pulled in the EFS SDK as transitive, and lambda's go.mod still pinned the older versions. Go's build graph flagged it as `go: updates to go.mod needed; to update it: go mod tidy`. Fix: `go mod tidy` on `backends/lambda`, `backends/azure-functions` (which picked up `armstorage` as transitive of azure-common), and `go.work.sum` — all in commit `3c3c4dd`. Follow-up to prevent a repeat: shared-helper lifts must `go mod tidy` every importer, not just the shared module itself. |
| 735 | H | ecs | `backends/ecs/taskdef.go::buildContainerDef` silently built a Fargate scratch volume when `Binds` was set but `SOCKERLESS_ECS_AGENT_EFS_ID` was unset — docker clients saw the mount path appear but it was empty and non-persistent. Initial fix rejected `Binds` up-front with a config error; **superseded by Phase 91** which provisions a sockerless-owned EFS filesystem + per-volume access point on demand, so named-volume binds (`-v name:/mnt`) now persist across runs. Host-path binds (`-v /h:/c`) remain rejected — Fargate has no host filesystem to bind from. |
| 736 | H | cloudrun/aca | Cloud Run Jobs/Services and ACA Jobs/Apps silently dropped `HostConfig.Binds` on the floor — `docker run -v /h:/c` completed on a container that had no mount. Fix: `ContainerCreate` on both backends rejects `Binds` up-front with an `InvalidParameterError`. Real mount support is Phase 92 (Cloud Run GCS) and Phase 93 (ACA Azure Files); the rejection is the interim contract. |
| 737 | M | core | `SOCKERLESS_SKIP_IMAGE_CONFIG=true` opt-out deleted entirely. `ImagePullWithMetadata` now requires non-nil metadata — callers that can't fetch it fail with the underlying registry error (matches what BUG-730 already enforced for the normal path). `FetchImageMetadata` no longer special-cases the env var; the synthetic placeholder branch (deterministic hash ID, `amd64`/`linux` stubs, `/bin/sh` Cmd) is gone. Registry slices in the simulators already serve real `/v2/` manifests + config blobs for every published image, so no fallback is needed for the sim path either. |
| 738 | H | ecs | `waitForTaskRunning` treated any STOPPED observation as a failure — short-lived containers (`docker run alpine echo hello`) that transition PENDING→STOPPED inside one poll interval surfaced as `task stopped: Essential container in task exited`. Now: when a STOPPED task's containers all report exit code 0, `waitForTaskRunning` returns `"", nil` (short-lived success path); the caller recognises the empty address, closes the wait channel directly, and skips both Cloud Map registration (nothing to discover) and `pollTaskExit` (would race with the already-STOPPED state). CloudState.GetContainer still reads STOPPED straight from ECS so `docker inspect` / `docker ps -a` show the real terminal state consistently. |
| 743 | H | lambda | `backends/lambda.ContainerCreate` unconditionally called `resolveImageURI(config.Image)` even when `PrebuiltOverlayImage` was configured. In reverse-agent mode the user's Image is overridden by the operator's prebuilt overlay, so resolving the user's image through ECR pull-through cache was wasted work — and it fails for local-only images like `alpine:latest` or `sockerless-eval-arithmetic:test` in the simulator-backed integration run (needs `SOCKERLESS_ECR_DOCKERHUB_CREDENTIAL_ARN`). Restructured `ContainerCreate` into a three-way switch: (a) callback+prebuilt → skip ECR entirely, use prebuilt image; (b) callback, no prebuilt → resolve base then build+push overlay; (c) no callback → resolve as before. Also deleted `backends/lambda/arithmetic_integration_test.go` — the arithmetic tests assumed the user's image runs with its ENTRYPOINT, which is incompatible with Lambda's bootstrap-handler pattern (the bootstrap binary is the Lambda entrypoint; the user's Cmd is exec'd inside the overlay image, which has no `eval-arithmetic` binary). Lambda runtime is exercised end-to-end by `TestLambdaAgentE2E_ReverseAgent`. |
| 742 | M | tests | Per-backend `Test*Arithmetic*` integration tests (ECS / Lambda / Cloud Run / ACA / GCF / AZF) set `Image: "alpine:latest"` and `Cmd: []string{evalBinaryPath, "3 + 4 * 2"}`, where `evalBinaryPath` is a path on the host (`../../simulators/testdata/eval-arithmetic/eval-arithmetic`) — never copied into alpine, so the container started with missing executable, exited -1, and logs just echoed the argv. The tests only surfaced once CI actually ran the integration job (BUG-727 + BUG-740 + BUG-741). Fix: follow the `simulators/aws/sdk-tests` pattern — TestMain builds `eval-arithmetic` statically for `linux/amd64`, builds a `sockerless-eval-arithmetic:test` image that `COPY`s the binary in with `ENTRYPOINT`, tests then use that image and pass only the expression as Cmd. Lambda-specific arithmetic tests deleted (see BUG-743). |
| 741 | M | tests | `Test*VolumeOperations` integration tests on ECS / Lambda / Cloud Run / ACA / GCF / AZF all asserted the old "volumes succeed silently" contract — `VolumeCreate` expected to return a named volume the test then inspected/listed. Missed in the BUG-731 sweep (which updated only `tests/volumes_test.go`). Once CI could actually run the per-backend integration jobs (BUG-727 + BUG-740), `TestECSVolumeOperations` surfaced the mismatch. Fix: rewrote all 6 tests to assert the NotImplemented contract — `VolumeCreate` must fail with a "does not support named volumes" error. |
| 740 | H | ci | ECS / Cloud Run / ACA / GCF / AZF integration TestMains built and started a `sockerless-docker-frontend` binary from `../../frontends/docker` — a path that doesn't exist post-P67 (the frontend is in-process on the backend itself, which is what the Lambda integration test already does). CI failed with `chdir ../../frontends/docker: no such file or directory` once BUG-727 enabled integration in CI. Fix: dropped the frontend build + unix-socket frontend launch from all 5 files; docker client now points at `tcp://localhost:{backendPort}` directly (Lambda-style). |
| 739 | H | ci | Backend integration `TestMain` builds (simulator-{aws,gcp,azure}, per-backend binaries, docker frontend) omitted `-tags noui`, so every unit CI run crashed with `ui_embed.go:12 pattern all:dist: no matching files found`. Added `-tags noui` to every sub-build across ECS / Lambda / Cloud Run / GCF / ACA / AZF integration tests. Lambda bootstrap (agent package, no UI) stays tag-less. |
| 731 | H | all-cloud | `VolumeCreate` / `VolumeRemove` / `VolumeInspect` / `VolumeList` / `VolumePrune` on ECS/Lambda/Cloud Run/ACA/GCF/AZF now return `NotImplemented` with a clear per-cloud message — no more silent metadata-only store. Dead placeholder fields deleted (`aca.VolumeState.ShareName`, `cloudrun.VolumeState.BucketPath`, ECS `VolumeState` struct entirely). Core HTTP volume handlers (handle_volumes.go) route through `s.self.Volume*` so overrides fire. Real per-cloud volume provisioning (EFS / GCS / Azure Files) tracked as Phases 91-94 in PLAN.md. |
| 729 | M | ecs | SSM ack wire format rewritten to match AWS's `SerializeClientMessageWithAcknowledgeContent`: `Flags=3` (SYN\|FIN) and UUID packed as LSL at offset 64 + MSL at offset 72 (AWS Java-style `putUuid`) — previously both were wrong, which is why the live agent rejected every ack and retransmitted. Sim-side frame builder mirrored. The BUG-721 `ssmDecoder.seenIDs` dedupe workaround removed. Unit tests pin flags + UUID layout + JSON body + no-dedupe. |
| 734 | M | ecs | `getNamespaceName` silently substituted the raw namespace ID for the name when `GetNamespace` failed. Now returns `(string, error)`; callers propagate or decide per-context (search-domain aggregation skips with a WARN; DNS discovery fails the call). |
| 733 | L | ecs | `stats.go` returned `PIDs: 1` as a fabricated fallback when CloudWatch had no data for a new task. Now returns `{}` (PIDs=0); docker stats shows 0/0 until real metrics arrive. |
| 732 | L | cloudrun | `NetworkState.FirewallRuleName` dead placeholder field deleted (never read; always written as `""`). Real VPC firewall management tracked as a separate future phase if parity with ECS security groups becomes required. |
| 730 | H | core | `ImagePullWithMetadata` no longer synthesises ID / size / layers / config when metadata fetch fails. `FetchImageMetadata` propagates the registry error; `ImagePull` fails clean with the real error. (Follow-up BUG-737 removed the `SOCKERLESS_SKIP_IMAGE_CONFIG=true` opt-out that briefly retained a synthetic path.) |
| 728 | H | sim/aws | ECS exec WebSocket emitted raw bytes; backend's SSM decoder saw empty output. Simulator now builds real SSM AgentMessage frames (`output_stream_data`, exit-code PayloadType=12, `channel_closed`) — see `simulators/aws/ssm_proto.go`. |
| 727 | H | ci | Unit job panicked on nil `dockerClient` in cloudrun/aca/gcf/azf arithmetic integration tests. CI `test` job now sets `SOCKERLESS_INTEGRATION=1` + verifies docker; TestMain CI guard fails loud if env var missing. |
| 726 | H | aca/ecs/cloudrun | NetworkState (SG, Cloud Map namespace, DNS zone, NSG) was in-memory only. `resolveNetworkState` now derives from cloud actuals by deterministic name. |
| 725 | H | aca/cloudrun/ecs/lambda | Per-backend state maps were the canonical lookup for 16 cloud callsites; restart-unsafe. All callsites migrated to `resolve*State` helpers (cache + cloud-derived fallback). |
| 724 | M | aca/cloudrun/ecs | Docker pods (libpod) were tracked in `Store.Pods` only. `BaseServer.PodList` now merges `CloudPodLister` results; each backend groups cloud resources by `sockerless-pod` tag. |
| 723 | H | all-cloud | `Store.Images` disk persistence removed; `docker images` now cloud-derived across 6 cloud backends via `core.OCIListImages` (Artifact Registry / ACR) + ECR SDK (ECS/Lambda). |
| 722 | H | ecs | Restart lost `ECSState.TaskARN`; `cloudExecStart` returned ASCII error through hijacked conn → docker CLI reported `unrecognized stream: 110`. `resolveTaskARN` lazy-recovery via ListTasks + tag filter. Later subsumed by BUG-725. |
| 721 | M | ecs | SSM agent retransmits `output_stream_data` until it sees an ack it accepts; sockerless's ack format isn't yet recognised. Pragmatic dedupe by MessageID UUID in `ssmDecoder`. Proper ack-acceptance tracked as BUG-729. |
| 720 | H | ecs | Task IAM role missing `ssmmessages:*` permissions → ECS Exec data channel closed immediately. Terraform module adds `ECSExecSSMMessages` statement. |
| 719 | H | ecs | `RunTask` omitted `EnableExecuteCommand: true` → exec fails post-launch. Fixed in `startTask`. |
| 718 | H | lambda | Cross-cloud sibling of BUG-708 + silent `pushToECR` fallback. Same credential-ARN wiring; fallback removed. |
| 717 | H | ecs | `docker exec` returned `unrecognized stream: 69` because SSM binary frames were passed through as Docker mux bytes. Full SSM AgentMessage parser + ack writer + stdin wrapping in `backends/ecs/ssm_proto.go`. |
| 716 | H | aca | Private DNS A-records got the `0.0.0.0` placeholder (ACA Jobs have no addressable per-execution IP). Closed via Phase 88: `SOCKERLESS_ACA_USE_APP=1` switches to ContainerApps with internal ingress; peer discovery writes CNAMEs to `LatestRevisionFqdn`. |
| 715 | H | cloudrun | Same symptom as BUG-716 on GCP. Closed via Phase 87: `SOCKERLESS_GCR_USE_SERVICE=1` + `SOCKERLESS_GCR_VPC_CONNECTOR` switches to Cloud Run Services with internal-only ingress; peer discovery writes CNAMEs to `Service.Uri`. |
| 714 | H | ecs | Cloud Map A-record registration used the `0.0.0.0` placeholder from in-memory state. Registration moved after `waitForTaskRunning` so the real Fargate ENI IP is available (via `extractENIIP`). |
| 713 | H | cloudrun | `ManagedZones.Create` non-idempotent — 409 conflict left the network unusable. Catch 409, fall back to `ManagedZones.Get`, cache. |
| 712 | H | ecs | `cloudNetworkCreate` + `cloudNamespaceCreate` non-idempotent — retries crashed on duplicate SG / namespace. Both now reuse existing resources by name lookup. |
| 711 | H | ecs | `DnsSearchDomains` rejected by Fargate on awsvpc. `buildContainerDef` wraps argv in `/bin/sh -c` that rewrites `/etc/resolv.conf` then `exec`s original argv with POSIX-quoted args (`shellQuoteArgs`). |
| 710 | M | cli/all-backends | Default port `:2375` collides with Docker/Podman daemons. Changed to `:3375` everywhere (CLI + 7 backends + docs). Pre-commit hook `scripts/check-port-defaults.sh` locks it in. |
| 709 | H | ecs | `waitForOperation` polled Cloud Map's `GetOperation` without sleeping — burned 60 API calls in <10s while real provisioning takes ~30-60s. `pollOperation` helper with 2s sleep, 60× budget. |
| 708 | L | ecs | ECR pull-through docker-hub rules require a Secrets Manager credential ARN. `SOCKERLESS_ECR_DOCKERHUB_CREDENTIAL_ARN` now wired through with an explicit error (no silent fallback) when unset. |
| 707 | M | sim/gcp | Cloud Build Secret Manager integration — `AvailableSecrets.SecretManager` populated from opts; simulator resolves `projects/P/secrets/S/versions/V` references via new `simulators/gcp/secretmanager.go`. |
| 706 | M | sim/azure + aca | ACR cache-rule CRUD added to simulator; `backends/azure-common/ResolveAzureImageURIWithCache` rewrites Docker Hub refs through the configured ACR. |
| 705 | H | sim/aws | Lambda bypassed the real Runtime API. `simulators/aws/lambda_runtime.go` implements the per-invocation HTTP sidecar (`GET /invocation/next`, `POST /invocation/{id}/response`, etc.) with full Lambda env + `host.docker.internal` wiring. |
| 704 | M | sim/gcp | Cloud Build slice — CreateBuild LRO, source tarball extraction, Docker build steps, SUCCESS/FAILURE/CANCELLED state machine matching the real API. |
| 703 | H | aca | NSG integration — real `armnetwork.SecurityGroupsClient` via `ClientFactory`; simulator grew securityRules sub-resource. |
| 702 | H | aca | Private DNS Zones integration — `armprivatedns.RecordSetsClient` + per-network `skls-<name>.local` zone; in-memory `serviceRegistry` removed. |
| 701 | H | sim/{aws,gcp,azure} | Each simulated task/job ran as a standalone host-Docker container with no shared user-defined network — cross-container DNS broken. Each cloud's DNS slice now creates a per-namespace Docker network and connects tasks by service-name alias. |
| 700 | M | aca/cloudrun/ecs | `docker network create` silently lost cloud-side failures. `NetworkCreate` response now populates `Warning` with a semicolon-separated list. |
| 699 | M | sim/aws | EC2 didn't pre-register `subnet-sim` — Cloud Map namespace create failed VPC-ID resolution. Startup now auto-creates `vpc-sim` + `subnet-sim` idempotently. |
| 698 | C | core | `docker run -d` hung because the wait handler blocked on `WaitForExit` before committing headers — docker CLI never unblocked its `/wait` call to issue `/start`. Early `flushWaitHeaders` commits the 200 before blocking. |
| 697 | M | core | `docker pull` state didn't survive backend restart. Originally fixed with disk persistence; later (BUG-723) swapped for cloud-derived `ListImages`. |
| 696 | M | sim/aws | ECR pull-through-cache APIs missing. CRUD + real AWS error shapes added. |
| 695 | H | core | `StreamCloudLogs` rejected `created`-state containers unconditionally — broke create→attach→start flow. `AllowCreated` option added. |
| 694 | H | core | `StreamCloudLogs` follow loop exited on `!running` — `created` state is also non-running. Switched to `isTerminalState`. |
| 693 | H | ecs | Task definition used raw unqualified image ref — Fargate can't pull. Ported Lambda's `resolveImageURI` to ECS. |
| 692 | C | ecs | `docker run` hung after POST /containers/create — `ContainerAttach` delegated to `BaseServer` which returned an EOF pipe. ECS-specific attach now streams CloudWatch logs. |
| 691 | M | sim/gcp | Smoke test long-running container showed empty `docker ps` — same root cause as BUG-688. |
| 690 | M | sim/gcp | `docker stop` returned 304 for running containers — same root cause as BUG-688. |
| 689 | M | sim/gcp | Short-lived containers missed log output — `waitAndCaptureLogs` now waits for drain channel before returning. |
| 688 | H | sim/gcp | `docker ps` showed running container as not running — GCP simulator missing GET /executions/{id} endpoint. |
| 687 | H | sim/cloudrun | Empty `docker logs` — sim tried to pull cloud registry URI locally. `ResolveLocalImage` now maps AR/ECR/ACR URIs back to Docker Hub. |
| 686 | H | sim/all | Workloads ran via `os/exec` instead of real containers — exec/archive/fs ops impossible. Migrated to Docker SDK. |
| 685 | H | sim/azure | ContainerAppJob missing SystemData — creation time unavailable to CloudState. |
| 684 | H | sim/gcp | Job missing `LatestCreatedExecution` — execution state undeterminable. Added ExecutionReference + CompletionTime updates. |
| 683 | H | ecs/cloudrun/aca | Auto-agent (local process spawn + Store reads) violated stateless invariant. Removed from all cloud backends. |
| 682 | H | gcf/cloudrun | GCP label value 63-char limit truncated 64-char container IDs. Full ID moved to annotations; env var for GCF. |
| 681 | H | cloudrun/aca/gcf/azf | CloudState implementations were stubs reading Store.Containers. Replaced with real ListJobs / ListFunctions queries. |
| 680 | M | core | Handler files used `Store.ResolveContainerID` directly — failed in stateless mode. Migrated to `ResolveContainerAuto` / `ResolveContainerIDAuto`. |
| 679 | M | core | `StreamCloudLogs` follow-mode checked `Store.Containers.Get` — fails in stateless mode. Uses `ResolveContainerAuto`. |
| 678 | M | cloudrun | `ContainerUpdate` delegated to BaseServer without resolving container first. Added auto-resolve. |
| 677 | M | cloudrun | `ContainerTop` both branches delegated identically to BaseServer. Returns NotImplemented when no agent connected. |
| 676 | H | core | `NetworkConnect/Disconnect` used `Store.ResolveContainerID`. Switched to `ResolveContainerIDAuto`. |
| 675 | H | core | `ExecCreate` used `Store.ResolveContainerID` + `Store.Containers.Get` — fails in stateless mode. Uses `ResolveContainerAuto`. |
| 674 | H | core | `ContainerStart/Stop/Kill/Remove/Restart/Logs/Wait/Attach/Stats/Rename/Pause/Unpause` all used `Store.ResolveContainerID`. Switched to `ResolveContainerAuto`. |
| 673 | H | core | `ContainerUpdate` used `Store.ResolveContainerID` + `Store.Containers.Update`. Uses `ResolveContainerIDAuto`. |
| 672 | H | core | `ContainerTop` used `Store.ResolveContainerID` + `Store.Containers.Get`. Uses `ResolveContainerAuto`. |
| 671 | H | core | `ContainerList` used `Store.Containers.List` → empty on cloud backends. Uses `CloudState.ListContainers` when available. |
| 670 | H | core | `ContainerInspect` used `Store.ResolveContainer`. Uses `ResolveContainerAuto`. |
| 669 | H | ecs | `ContainerLogs` fell back to `BaseServer.ContainerLogs` when taskID unknown — stateless violation. Returns clear error instead. |
| 668 | H | all-cloud | `StreamCloudLogs` used `Store.ResolveContainerID` + `Store.Containers.Get` — docker logs 404 on all cloud backends. Uses `ResolveContainerAuto`. |
| 667 | H | lambda | Missing CloudStateProvider — docker ps / inspect / stop broken after `PendingCreates.Delete`. `lambdaCloudState` queries `ListFunctions` + tags. |
| 666 | L | ecs | `docker run -d` blocked ~30s on ECS provisioning. Async wait for RUNNING — RunTask returns immediately, poll in background. |
| 665 | M | ecs | `docker logs` 404 after restart — task ID only in local state. Cloud query by container ID tag when local state missing. |
| 664 | H | ecs | CloudState queried only RUNNING tasks. Now queries RUNNING + STOPPED. |
| 663 | H | ecs | `docker wait` hung in auto-agent mode. Checks local `WaitChs` first. |
| 662 | H | ecs | Auto-agent delegated to `BaseServer.ContainerStart` which reads Store. Broke stateless invariant. |

Earlier phases (≤ BUG-661) — one-liners per historical bug kept in `git log` + `specs/` specs. Summaries too terse to be useful here.

## False positives

Findings that look like bugs under the "no fakes, no workarounds" principle but are legitimate by design. Listed so future audits don't re-flag them.

| Area | Finding | Why it's not a bug |
|------|---------|--------------------|
| `backends/aca/azure.go::fakeCredential` | Returns a literal `"fake-token"` as a bearer token when the backend is pointed at a simulator endpoint. | Simulators intentionally don't verify bearer tokens — token verification requires a real Azure AD endpoint which the sim doesn't emulate. The credential is only wired via `newAzureClientsWithEndpoint` (the simulator path); production uses `azidentity.NewDefaultAzureCredential`. Naming should clarify intent (sim-only) but the behaviour is correct. |

## Cross-cloud sweep notes

When a bug is found, similar code paths in other backends / clouds / simulators are checked too. Notable sweep outcomes:

- BUG-708 ECS-only — Azure uses ACR cache rules with managed-identity auth; GCP uses Artifact Registry pull-through (different mechanism).
- BUG-709 ECS-only — Azure ACA's SDK helper sleeps internally; GCP's SDK handles polling.
- BUG-710 swept all 7 backend mains + CLI + READMEs + example terraform + `tools/http-trace`.
- BUG-711 ECS-only — no other backend sets explicit DNS search domains; GCP/Azure rely on per-network DNS zones with FQDN resolution.
- BUG-712 → found BUG-713 in cloudrun (same non-idempotent-create pattern). Azure ACA's `BeginCreateOrUpdate` is PUT-style and idempotent; Lambda/GCF/AZF have no cloud-side network creation.
- BUG-714 → found BUG-715 (cloudrun) and BUG-716 (aca) with the same placeholder-IP symptom. All three backends seed `ep.IPAddress = "0.0.0.0"` at create time; each needed a different structural fix (ECS got the real Fargate ENI IP; cloudrun + aca moved to Services/Apps with stable FQDNs in Phase 87/88).
