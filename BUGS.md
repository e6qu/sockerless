# Known Bugs

Status [STATUS.md](STATUS.md) - roadmap [PLAN.md](PLAN.md) - resume [DO_NEXT.md](DO_NEXT.md).

**1788 filed - 1743 fixed - 5 open - 7 false positives.**

Every CI failure, live-cloud failure, simulator fidelity gap, or discovered fake/fallback lands here before any fix attempt. Detailed closed-bug history lives in PR descriptions and `git log`.

## Open

| ID | Sev | Area | Pattern | One-liner |
|----|-----|------|---------|-----------|
| 1345 | P2 | azuread terraform provider | upstream blocker | The `hashicorp/terraform-provider-azuread` provider has no supported way to redirect Microsoft Graph API calls to a custom endpoint (no `microsoft_graph_endpoint` override). Feature request open upstream: https://github.com/hashicorp/terraform-provider-azuread/issues/1837. Entra provisioning via Terraform (`azuread_group`, `azuread_user`, `azuread_group_member`) cannot be tested against the sim until this is resolved upstream. |
| 1075 | P2 | live-cloud validation | unvalidated real cloud | Lambda is the only backend with a green live-cloud cell. Cloud Run Services, ACA Apps, AZF cloud-DNS, Lambda service-mesh, and ACA/AZF Azure AD remain unvalidated against authenticated real clouds. Do not mark these green without real cloud runs. |
| 1785 | P2 | sims (azure ACR Tasks + gcp Cloud Build) — a build lands the image only in the host's local docker daemon; it is never pushed to the registry, and the workload runs the local copy instead of pulling from the registry | local-daemon shortcut vs build→push→pull | Real ACR Tasks / Cloud Build (`IsPushEnabled`) push the built image to the registry; the cloud's compute then pulls it from the registry over the public `/v2/` API. The sims instead `docker build -t <ref>` into the local daemon and rely on `StartContainerSync` finding it locally, so the sim's registry diverges from the cloud after a build and the run uses a non-faithful shortcut. **Fix faithfully, with sim services agnostic** (no in-process coupling between the workload runner and the registry — compute pulls over the network like any client): the build does a real `docker push <ref>` + `docker rmi`; the run does a plain `docker pull <ref>`; the *harness* makes `<reg-host>` a reachable registry endpoint (publish the sim `/v2/`, DNS the registry host to it, insecure/TLS trust on the host engine). Applies to the azure ACR Tasks slice and the gcp Cloud Build slice. **Azure half done** (real `docker push`/`pull`, `SOCKERLESS_AZURE_ACR_ENDPOINT` override, validated by ACA harness TEST 12 + the ACR Tasks SDK test); the gcp Cloud Build half remains — it must carry the same pattern through the cloudrun/gcf overlay flows and their integration tests (which today rely on the local-daemon shortcut), so it is a separate, larger change. |
| 1784 | P2 | bleephub ACA topology harness — service containers (`services:`) not yet reachable by alias from the job container | multi-container service discovery (partial) | With TEST 12 (container job) green on ACA, TEST 13 (service container) still fails: the job's in-step `wget http://web/` exits 1. **Partial fix landed (mechanism in place, not yet green):** the backend now captures the runner's `--network-alias web` at ContainerCreate and registers the explicit aliases (not the hostname) in Azure Private DNS at ContainerStart (`registerContainerServiceDiscovery` — App-mode CNAME to the service App's `LatestRevisionFqdn`); the azure sim realizes a Private DNS CNAME→App-FQDN as a Docker embedded-DNS alias on the target App's env-network container (`realizeCNAMEAsDockerAlias`), re-attaching with `[appName, FQDN, <alias>]`. This restored TEST 12 (the earlier approach disconnected the *job's own* App mid-exec, severing its reverse-agent; now only service aliases are realized — services start before the job, so their re-attach is early and idle). The realize fires successfully (alias set, `err=nil`) but the job still can't resolve `web` end-to-end — remaining issue is **resolution**, not registration: likely the job's `.local` DNS search-domain detour (`web.skls-…local` queried before bare `web`) or a job/nginx env-network attachment detail. Next: `BLEEPHUB_HOLD=1` + `exec` into the job container (`getent hosts web`, inspect the nginx container's aliases/network). The container-backend analog of BUG-1781's FaaS work. |
| 1781 | P2 | backends lambda/gcf/azf — multiple containers sharing one pod aren't assembled from cloud primitives (FaaS backends reject or can't run multi-container pods) | not-yet-assembled abstraction | A FaaS platform that doesn't run multiple containers in one function must still present full pod semantics — sockerless's job is to *assemble* the container-pod execution model (with its docker network) out of cloud primitives, so the user's experience matches local Docker/Podman (and the container backends), **including localhost / shared-loopback networking between pod members** (a sibling reachable on `localhost:<port>`). Today lambda/gcf reach pod members only by name (no shared loopback) and azf rejects multi-container pods outright, so GitHub `services:` / sidecar `container:` jobs and GitLab service containers don't run on the FaaS backends. Assemble per backend: native sidecar containers where the platform offers them (Azure Functions sidecars), else **a pod assembled from multiple functions (one per container)** wired by cloud DNS/service-mesh (Cloud Map / Cloud DNS / Private DNS) + a shared workspace volume, with the **agent proxying `localhost:<port>` to the sibling member** to deliver shared-loopback semantics over the cloud network. Staged in [PLAN.md](PLAN.md) § Next #1; replaces the interim fail-fast rejections (BUG-1779). |

## Recently fixed (cont.)

| ID | Sev | Area | Pattern | One-liner |
|----|-----|------|---------|-----------|
| ~~1788~~ | P2 | azure sim sdk-tests — `TestACRTasks_ScheduleRunDockerBuild` flaked CI with `docker run … registry:2` exit status 125 | flaky image pull | `startThrowawayRegistry` ran `docker run -d … public.ecr.aws/docker/library/registry:2` which inline-pulls the image on first use; a transient public-mirror throttle made the pull (and so the `run`) exit 125, failing `sim (azure)` on PR #564 (it passed on #563 — flaky, not deterministic). Fixed by pulling the registry image (and the build's `alpine:3.20` base) up front via a new `pullImageWithRetry` helper (bounded exponential backoff, mirroring the sim pull paths' strict rate-limit posture) and retrying the `docker run` itself a few times. Clarified that 127.0.0.1:<port> is auto-insecure on Docker (CI) but a local Podman host needs an insecure-registries drop-in. |
| ~~1787~~ | P2 | bleephub — no standard / config-only way to obtain a usable pre-registered GitHub App (fixed id + a *known* private key) for coordinate-only consumers (consumer issue #559) | missing operator seeding | App creation was only available via `POST /internal/apps` (operator surface a coordinate-only consumer must not call) or the manifest-conversion flow (needs an interactive approval code); `store.CreateApp` always generated the key internally, so a harness couldn't hand the consumer a fixed app id + private key + org the way real GitHub does (app registered out of band, consumer holds id + PEM). Fixed by seeding one or more pre-registered Apps at startup from operator config (`BLEEPHUB_SEED_APPS` inline JSON / `BLEEPHUB_SEED_APPS_FILE`): caller-chosen id + slug, caller-supplied RSA private key (PKCS1 or PKCS8, normalized to the PKCS1 the app-JWT verifier expects), permissions/events, and optional pre-created installation(s) on given org(s) — unknown install accounts are created as real orgs, never fabricated ids. Idempotent across restarts; fails loud on a malformed spec. The seed is **operator-only**; the consumer's client stays 100% coordinate-only (base URL + app id + private key + org through the standard `/api/v3/app/...` surface) — no bleephub-specific client config. Regression test `TestSeedPreRegisteredApp` drives the full Octokit-style flow (sign app JWT → GET /app → list installations → mint installation token). |
| ~~1786~~ | P2 | aws sim — ECS ExecuteCommand WebSocket skipped the SSM `OpenDataChannel` token handshake real clients send (consumer issue #562) | missing connection handshake | `handleECSExecWebSocket` upgraded the WebSocket and immediately bridged stdin/stdout, treating the **first** inbound frame as `input_stream_data`; the `tokenValue` ExecuteCommand returned was never read or validated. Real AWS (session-manager-plugin, and any faithful client) sends an `OpenDataChannel` JSON message — carrying `TokenValue` — as the first frame, which the service validates before streaming. So a coordinate-only consumer couldn't drive ECS-Exec identically against the sim and real AWS. Fixed: the sim reads the first frame as `OpenDataChannel`, validates `TokenValue` against the session (10s read deadline; rejects with ClosePolicyViolation on mismatch/absence), then bridges. The sim's own ECS backend already sent this handshake (`exec_cloud.go`) so it keeps working. SDK regression test `TestECS_ExecDataChannel{Handshake,RejectsBadToken}` drives the data channel like a real client; `TestECSContainerExec` (backend integration) stays green. |
| ~~1783~~ | P2 | bleephub harness — the reverse-agent bootstrap (and agent) were built glibc-dynamic, so they failed to exec inside musl/alpine workload images | non-static binary | The bleephub `Dockerfile` built `sockerless-cloudrun-bootstrap` + `sockerless-agent` under `golang:1.25` with CGO on by default → dynamically linked against glibc. Baked into an alpine overlay (the ACA App image), the bootstrap failed to exec with `No such file or directory` (the missing dynamic loader), so it never dialed back and ACA container-job exec timed out. The canonical agent Makefile already builds with `CGO_ENABLED=0`; the harness Dockerfile was the anomaly. Fixed by adding `CGO_ENABLED=0` to both builds. Found by direct-running the overlay image; it unblocked ACA TEST 12. |
| ~~1782~~ | P2 | backends/azure-common `NewACRBuildService` ignored `SOCKERLESS_ENDPOINT_URL` — the ACA/AZF App-overlay bootstrap-build path couldn't reach a custom or simulated Azure endpoint | missing endpoint override | `NewACRBuildService` built both the ARM `RegistriesClient`/`RunsClient` and the `azblob` client with default options targeting real Azure, unlike `newAzureClientsWithEndpoint` which threads a `cloud.Configuration` override + `InsecureAllowCredentialWithHTTP`. So `SOCKERLESS_ACA_USE_APP=1` (the overlay path that injects the reverse-agent bootstrap, needed for ACA container-job exec) only worked against real Azure. Fixed: thread `endpointURL` into `NewACRBuildService`; build the ARM clients with the cloud-config override; build the azblob client lazily against the storage account's advertised `primaryEndpoints.blob` (discovered via `armstorage` GetProperties). Validated end-to-end by ACA TEST 12 going green. |
| ~~1780~~ | P2 | backends lambda/cloudrun/gcf/aca/azf — `docker network create` did real Linux netns (`ip netns add` + veth), 400ing without iproute2 and provisioning a meaningless kernel netns where it succeeded | wrong network driver | Only the ecs backend overrode `Drivers.Network` to the metadata-only `SyntheticNetworkDriver`; the other five stateless cloud backends fell through to `BaseServer.InitDrivers`' Linux platform driver. But their docker networks map to *cloud* primitives (Lambda VPC ENIs, Cloud Run/GCF VPC-connector + Cloud DNS, ACA + AZF NSG / Private DNS) provisioned by each backend's NetworkCreate wrapper — the kernel netns inside the runner-task is irrelevant, exactly as the ecs comment documents. On a host without iproute2 (the bleephub ubuntu:22.04 harness) `docker network create github_network_<hex>` 400'd, blocking every GitHub container-mode job; on a host with iproute2 it silently created and leaked a kernel netns per network. Found by the Arc-2 ACA topology harness (TEST 12). All five now mirror ecs's `SyntheticNetworkDriver`. |
| ~~1779~~ | P3 | backend-azf — multi-container pod rejected late/asymmetrically (only at the 2nd ContainerStart) | late fail | `PodCreate` + both `ContainerCreate`s succeeded; the single-invocation rejection only fired when the 2nd member's `ContainerStart` ran (deferred materialize), surfacing as a buried per-container error. `PodStart` now rejects a multi-container pod up front with one clear `InvalidParameterError` (the ContainerStart guard stays as the per-container backstop); documented in the azf README's out-of-scope list. |
| ~~1778~~ | P2 | backends lambda+gcf — `docker pod stop/kill/rm` (and lambda `pod start`) leaked the cloud resource; isolation lint didn't catch it | stateless violation | Lambda delegated all four Pod lifecycle methods, and GCF delegated Stop/Kill/Remove, to `BaseServer.Pod*` — which read `Store.Containers` + call `Store.ForceStopContainer` (local in-memory only, NO cloud call), so the Lambda function / Cloud Run Service kept running. ECS/Cloud Run/ACA already override these to loop their cloud-aware `Container*` methods; lambda+gcf now mirror that. `scripts/check-cloud-backend-isolation.sh` forbade `BaseServer.Container*` but not `BaseServer.Pod{Start,Stop,Kill,Remove}` — gap closed so the class can't recur. |

## Recently fixed

The current arc (BUG-1745 onward). Older fixed bugs are struck out in `git log` and the PR descriptions that closed them — this section is a rolling window for context, not the full ledger; the header counts above are the cumulative tally.

| ID | Sev | Area | Pattern | One-liner |
|----|-----|------|---------|-----------|
| ~~1777~~ | P3 | backends — cloud-state recovery WaitForExit re-listed every cloud resource per poll; Cloud Run service-URI poll was a flat 2s loop | needless poll volume | A pod-model review flagged the recovery-path `WaitForExit` (cloudrun + aca) re-running a full `ListJobs`+`ListServices` (with per-pod GetService follow-ups) on every tick to check one container — narrowed to resolve the backing Job once then poll just that job's state (the identical resolveExecutionState/resolveJobState derivation, single resource), with a list fallback for Service/App-backed and vanished jobs. Cloud Run's `waitForServiceURL` flat 2s/150-call loop → exponential backoff (1s→15s). GCF's WaitForExit left alone (already event-driven hot path; FaaS has no persistent exit state). Recovery-path only; hot path was already single-resource (pollTaskExit/pollExecutionExit). |
| ~~1776~~ | P2 | backend-aca — buffered-attach Read had no deadline; a stalled/lifetime-capped Job strands an attached reader forever | unbounded wait | ACA's `attachStream.Read` did a bare `<-respReady`; the AZF twin bounds the identical buffered-attach wait with `select { respReady / time.After(deadline) }` (the BUG-1505 fix) but it was never ported to ACA. If the ACA Job stalls or hits its replica-timeout before publishing output, a `docker`/`StdCopy` reader blocks indefinitely. Fixed by porting the AZF deadline pattern (bootstrap window + job-run budget). Found by the pod-model review. |
| ~~1775~~ | P3 | gcp sim — rrsets.list ignores the `name` and `type` query params; returns the whole zone | dropped filter | Real Cloud DNS `rrsets.list` filters on `?name=&type=`, which the Go SDK's `.Name(fqdn).Type("A")` sets — the Cloud Run service-discovery path uses exactly this (`backends/gcp-common/network_discovery_clouddns.go`). The sim returned every record in the zone; the backend re-filters client-side so it isn't broken today, but the sim diverges from real GCP and a filter-trusting consumer would over-read. Found by real-SDK probe. Fix: honor name+type in the list handler. |
| ~~1774~~ | P2 | aws sim — AuthorizeSecurityGroup{Ingress,Egress} never rejects a duplicate rule (no InvalidPermission.Duplicate); silently appends a second identical IpPermission | missing idempotency error | The ECS backend re-applies its self-referencing ingress rule on every network-create and swallows exactly `InvalidPermission.Duplicate`; the sim instead appends the permission again, so DescribeSecurityGroups read-back accumulates duplicate rules and diverges from real AWS (which 400s the duplicate). Found by a real-SDK probe mirroring `backends/ecs/network_cloud.go`. Fix: detect an existing equivalent permission and return InvalidPermission.Duplicate; egress mirror too. |
| ~~1773~~ | P2 | aws sim — CreateSecurityGroup never rejects a duplicate name in the same VPC (no InvalidGroup.Duplicate); always mints a fresh sg-id | missing idempotency error | The ECS backend's idempotent network-create (`backends/ecs/network_cloud.go:43`) relies on `InvalidGroup.Duplicate` to detect an existing SG and reuse it by name+VPC; against the sim the second create silently succeeds with a NEW id, so a retried/partial network-create leaks duplicate security groups instead of reusing. Real AWS rejects same-name groups within one VPC (different VPCs may reuse). Found by real-SDK probe. Fix: reject same name+VPC with InvalidGroup.Duplicate. |
| ~~1772~~ | P2 | sims (all 3) — image pull fails hard on transient registry throttle (toomanyrequests/429/503) | missing rate-limit handling | A moment of public-mirror throttling (public.ecr.aws "Rate exceeded" on PR #552's `sim (aws sdk)` — TestAmplifyHostingSSRComputeE2E) turns into a red suite because the pull path surfaces the first failure immediately (visible thanks to BUG-1744's propagation). The strict rate-limit rule applies: bounded exponential backoff retries on explicitly-transient registry errors in all three shared pull paths; non-transient errors still fail immediately. |
| ~~1771~~ | P3 | dispatcher-aws — spawned runner containers can't resolve host.docker.internal on Linux Docker | engine-conditional flag | Runner images spawned via the docker shape dial back to host-published services (bleephub, sockerless) via host.docker.internal — native on Docker Desktop/Podman 4+, absent on Linux Docker unless `--add-host host.docker.internal:host-gateway` is passed, which Podman 5 REJECTS (the GH-8 pair). Spawn now detects the engine (docker version components ~ "Podman") and adds the flag only where it's both needed and legal — the same runtime detection the sims use. |
| ~~1770~~ | P2 | bleephub — job messages bake the SUBMITTER's Host as the server URL; off-host runners cannot complete jobs | wrong URL provenance | `serverURL = scheme://r.Host` at workflow submit means a run triggered via `127.0.0.1` hands the runner a job whose Worker dials `127.0.0.1/_apis/connectionData` — connection refused for any runner not sharing the server's network namespace (the listener half works because IT uses the configured registration URL). Real GHES embeds its configured external URL. Fix: `BLEEPHUB_EXTERNAL_URL` honored by baseURL/serverURL derivation; request-Host remains the default when unset. Found by TEST 14 — the dispatcher-spawned runner ran the job but could never report completion. |
| ~~1769~~ | P2 | bleephub — agent registration drops `ephemeral`; `config.sh --ephemeral` refuses ("server does not support… 'Ephemeral' flag") | round-trip fidelity | The Agent struct has the field but handleRegisterAgent never copies it from the request, so the response omits it and the official runner aborts ephemeral configuration — the dispatcher's one-runner-per-job model registers all runners `--ephemeral`. Round-trip the flag and deregister ephemeral agents after their job completes (real GitHub auto-removes them). Found by TEST 14. |
| ~~1768~~ | P2 | bleephub — runs with no started job report `in_progress`, invisible to `?status=queued` pollers | run-status fidelity | Internal Workflow.Status goes "running" at submission; the REST mapping translated that to `in_progress` even when every job was still pending/queued. Real GitHub keeps the run `queued` until a job STARTS — the github-runner dispatcher (and any ARC-style autoscaler) polls `runs?status=queued` and so never saw label-stranded runs. runStatus now consults job states. Found by TEST 14 (dispatcher→bleephub). |
| ~~1767~~ | P3 | bleephub — admin token advertises X-OAuth-Scopes without `workflow`/`admin:org` | scope-header fidelity | The all-powerful admin token hardcodes `repo, read:org, gist` in the scope header while honoring every operation — classic-scope clients that gate on the header (the github-runner dispatcher requires `workflow`; gh checks `read:org`) wrongly refuse it. Advertise the scopes the token actually honors. Found by TEST 14 (dispatcher→bleephub). |
| ~~1766~~ | P2 | bleephub — `container:` object form loses env/ports/options/volumes | partial wire shape | `buildJobMessageFromDef` reduces an object container to `ContainerImage()` (bare string), silently dropping the object's env/ports/volumes/options/credentials — the runner provisions the job container without them. Emit the full mapping TemplateToken. Found alongside 1765. |
| ~~1765~~ | P1 | bleephub — jobServiceContainers emitted as plain JSON; the official runner rejects the job ("The template is not valid. Unexpected value ''") | wrong wire encoding | The runner deserializes `jobContainer`/`jobServiceContainers` as TemplateTokens (mapping = `{"type":2,"map":[{"Key":…,"Value":…}]}`, sequence = type 1, literals via `lit`); `buildServiceContainers` returned a raw `map[alias]spec` (with a nonexistent `environment` key, vs the runner's `env`), so every `services:` workflow failed at job start. Found by the first official-runner service-container run in the topology harness — the shape was never exercised before. |
| ~~1764~~ | P2 | dispatcher scopes.Verify — classic-PAT-only: tokens without X-OAuth-Scopes are rejected | impossible requirement | `Verify` requires the `X-OAuth-Scopes` response header to cover repo+workflow, but fine-grained PATs and GitHub App installation tokens never send that header — the error text even suggests "a new fine-grained PAT including: …", which can never pass. Fix: when the header is absent, verify by CAPABILITY instead — mint a registration token (`POST …/actions/runners/registration-token`), the exact permission the dispatcher needs. |
| ~~1763~~ | P2 | dispatchers — api.github.com hardcoded; GHES/bleephub targets impossible | missing endpoint override | `scopes.Verify` hardcodes `https://api.github.com/user` and the dispatchers expose no API-base flag, so a GHES instance (or bleephub) can never be targeted even though `poller.Client.APIBase` exists for tests. Fix: `--api-base` flag threaded through poller + scopes + registration-token mint (GHES shape `https://host/api/v3`). Found wiring the runner-as-cloud-task harness (dispatcher polling bleephub). |
| ~~1762~~ | P3 | dispatchers gcp+azure — runner registration token rides plain control-plane env | secret hygiene | `RUNNER_REG_TOKEN` is set as a plain `EnvVar` on the Cloud Run Job template (`-gcp/internal/spawner/spawner.go:125`) and a plain `EnvironmentVar` on the ACA Job template (`-azure/internal/spawner/spawner.go:115`) — readable by anyone with `run.jobs.get` / ARM reader on the resource for the token's ~1h validity. Both platforms have first-class secret bindings (Secret Manager env binding; ACA `secretRef`); use them. The AWS/docker variant is laptop-local (`docker inspect` exposure only) — same hygiene applies when the daemon is shared. |
| ~~1761~~ | P3 | specs/CLOUD_RESOURCE_MAPPING.md — dispatcher table documents the broken cleanup keying + stale poll cadence | spec drift | The per-cloud dispatcher table documents GCP cleanup keyed off `TerminalCondition.State` `CONDITION_*` — the exact approach the code REMOVED because it deleted runner-tasks right after spawn (see `executionStateForJob`'s comment) — and documents the Azure `ProvisioningState` keying that IS bug 1752. Also says the poll loop runs "every 15 s" while `poller.New` deliberately sets 60 s with a quota rationale (`pkg/poller/poller.go:83-88`). Spec must follow the fixed semantics. |
| ~~1760~~ | P3 | dispatchers (all 3) — no max-concurrent-runners cap | unbounded fan-out | A burst of N queued jobs spawns N runner containers / Cloud Run Jobs / ACA Jobs in one poll cycle with no ceiling — straight into ENI/concurrency/regional-CPU quota walls (predicted hurdles P-1/P-7 for sub-tasks apply equally to runner-tasks; ARC's `maxRunners` is the equivalent knob). Add a per-label `max_concurrent` config gating Spawn on the live count ListManaged already computes. |
| ~~1759~~ | P3 | dispatchers (all 3) — spec'd `runner_job_timeout` TOML knob unimplemented; per-cloud timeouts hardcoded/absent | missing config | CLOUD_RESOURCE_MAPPING.md § timeout knobs says the dispatcher TOML carries `runner_job_timeout` (default 3600) applied as the cloud-API timeout on the runner-task. No config schema has the field: GCP hardcodes `Timeout: 3600s` (kills any CI job >1h; GitHub allows 6h), Azure hardcodes `ReplicaTimeout: 21600`, AWS/docker applies no bound at all. |
| ~~1758~~ | P3 | dispatcher-azure — deployable hardening missing (healthz, env fallback, verify retry, rate-limit loop); zero tests | parity gap | The GCP variant binds `$PORT` with `/healthz`, falls back to `$REPO`, retries scope-verify with backoff, and honors rate-limit windows across poll+cleanup ticks (`rateLimitedUntil`). The Azure main.go is the bare laptop loop: deployed as an ACA App it fails probes and exits on a transient 403/429 at startup — crashlooping into the abuse window the GCP comment explicitly warns about. The module also has no `_test.go` at all (AWS has 7 test files, GCP has spawner tests). |
| ~~1757~~ | P3 | dispatcher-azure — runner container spawned with default ACA resources | undersized runner | The ACA Job template sets no `Resources`, so executions get ACA defaults (0.5 vCPU / 1 Gi). The GCP twin explicitly sets 2 cpu / 4 Gi because the default "is too small for a runner that compiles real workloads" (`-gcp/internal/spawner/spawner.go:120-135`). Real CI on the Azure path will OOM/crawl. Set parity resources using a valid ACA cpu/memory combination. |
| ~~1756~~ | P2 | dispatcher-gcp — Jobs whose RunJob failed (NO_EXECUTION) are never reaped | leak vs. own contract | `Spawn` deliberately leaves the Job orphaned when `RunJob` errors, with the comment "State recovery sweep will catch it" (`spawner.go:217-221`) — but the sweep's `isTerminalJobState` treats only `EXECUTION_SUCCEEDED/FAILED` as reapable, so `NO_EXECUTION` Jobs accumulate forever (and count as live owners' siblings in every ListManaged). Reap NO_EXECUTION Jobs older than a grace window via `Job.CreateTime`. |
| ~~1755~~ | P2 | dispatcher-aws ↔ ECS/Lambda runner images — env contract mismatch: spawn can't compose | name drift | The spawner injects `RUNNER_REG_TOKEN` + `RUNNER_REPO` (owner/repo) (`-aws/internal/spawner/spawner.go:79-83`), but `dockerfile-ecs/entrypoint-ecs.sh` and `dockerfile-lambda/bootstrap.sh` require `RUNNER_TOKEN` + `RUNNER_REPO_URL` (full URL) and fail-loudly on absence — a dispatcher-spawned ECS/Lambda runner dies at `${RUNNER_REPO_URL:?}` before registering. Only the vanilla/cloudrun/gcf images speak the dispatcher's names. The live harnesses spawn images directly with their own env, which is why cells 1+2 never exposed it. One contract, one set of names, every image. |
| ~~1754~~ | P2 | runner images — idle-timeout contract unimplemented or wrong-semantics; duplicate-spawn race not actually benign | claimed-but-absent mechanism | The dispatcher's statelessness story rests on "the runner image's entrypoint enforces a 60-s no-job timeout" (`-aws/internal/spawner/spawner.go:13`, README). No image implements an IDLE timeout: vanilla/ecs/lambda entrypoints run `run.sh --once` unbounded (a runner whose job was taken by a sibling waits forever — on cloud backends burning a 2cpu/4Gi task until the platform timeout), while cloudrun/gcf bootstraps wrap the WHOLE run in `timeout ${RUNNER_IDLE_SECONDS:-3600}` — a total-lifetime kill that would terminate an in-flight job at N s (with the AWS spawner's `RUNNER_IDLE_SECONDS=60`, any job >60 s). Real fix: bound only the pre-pickup window (watch for the Runner.Worker child / job-started marker), then let the job run to the job-level timeout. |
| ~~1753~~ | P2 | dispatchers gcp+azure — offline GitHub runner registrations never reaped | half-ported cleanup | CLOUD_RESOURCE_MAPPING.md requires the cleanup loop to deregister offline `dispatcher-*` runners (ephemeral runners that die without completing leave zombie entries), and both variants' `--cleanup-only` help text claims "(… + GitHub runners)" — but only the AWS loop calls `ListRunners`/`DeleteRunner`; the GCP and Azure `Cleanup` sweeps touch only cloud resources. Crash-killed runner-tasks accumulate offline registrations indefinitely. Fix in both loops; while there, add pagination to the shared `ListRunners` (silently truncates at 100 — acknowledged in its own comment). |
| ~~1752~~ | P1 | dispatcher-azure — cleanup sweep deletes runner Jobs while their execution is RUNNING | wrong state dimension | `isTerminalProvisioningState` keys reap-ability off ACA Job `Properties.ProvisioningState` (`Succeeded/Failed/Canceled`) — but that tracks the ARM RESOURCE provisioning, which goes `Succeeded` as soon as `BeginCreateOrUpdate` completes. Every spawned runner Job is therefore reap-eligible on the next 2-min sweep; `BeginDelete` on the Job kills its in-flight execution → CI jobs die ≤2 min after spawn. Exact Azure mirror of the GCP `TerminalCondition` bug already fixed by `executionStateForJob` (query the latest EXECUTION's status, not the resource state); fix the same way via the Job executions API. Never manifested only because Azure cells (Phase 123) haven't run live yet. |
| ~~1751~~ | P2 | backend-aca harness — nested TestMain deleted the parent suite's binaries | re-exec cleanup stomp | TestACAGitLabRunnerAttachStdin re-execs the test binary (apps-E2E mode), running TestMain AGAIN as a subprocess; the nested run's cleanup os.Remove'd the shared backend/sim binaries from under the parent, so any LATER parent test exec'ing the backend hit fork/exec ENOENT — exactly what the new shared-volumes test exposed in CI (and reproduced locally only in full-suite order). Nested runs now skip repo-path artifact removal. Also: the ACA shared-volumes test used the FROM-scratch eval image whose lack of a shell made `sh -c` scripts exit 1 with empty logs — switched to alpine like its passing ECS twin, and nonzero-exit failures now include container logs. |
| ~~1750~~ | P2 | backends — bind-mount→shared-volume translation parity incomplete (the runner-on-cloud gate) | partial mechanism | ECS/Cloud Run/GCF translated runner host binds to shared cloud volumes (`SOCKERLESS_*_SHARED_VOLUMES`), but lambda had the config UNWIRED into its bind path, and ACA/AZF had no mechanism at all (rejection message didn't even name a config). Parity completed across all six container backends with consistent sub-path-drop semantics; per-backend unit tests + ECS/ACA sim-backed integration tests prove the actual contract (container A writes via named volume, container B reads via translated host bind). What remains for runner-as-cloud-task is topology (runner images mounting the shared volume), not translation. |
| ~~1749~~ | P3 | bleephub — runner's "Canceled" result spelling leaked through unmapped | result normalization | The official runner's TaskResult uses the US spelling; normalizeResult only mapped "Cancelled"/"cancelled", so runner-cancelled jobs carried Result "canceled" — invisible to every ResultCancelled comparison (fail-fast, rollups, conclusions). Found by the harness's new cancellation e2e asserting the strict value. |
| ~~1748~~ | P3 | bleephub — actions hosted on bleephub itself can't resolve (`uses: owner/repo@ref` 502s) | action-source fidelity | The action tarball proxy serves the in-memory cache or fetches from real api.github.com; a repo ON bleephub holding an action (composite or otherwise) is never consulted, so self-hosted actions — GHES's bread and butter — fail with 502. Serve tarballs from bleephub's own git storage first (GitHub tarball layout: top-level `<owner>-<repo>-<sha>/` prefix), falling back to github.com for external actions. |
| ~~1747~~ | P3 | bleephub actions — invalid workflows refuse the run instead of a startup_failure shell | trigger-failure fidelity | Real GitHub creates a run with conclusion `startup_failure` (no jobs) when a triggered workflow has an invalid reusable-workflow ref or unparseable expansion; bleephub logs and creates nothing, so the failure is invisible to the runs API/UI. |
| ~~1746~~ | P3 | bleephub actions — runner groups unmodeled | missing ops | Single hardcoded pool; no `/orgs/{org}/actions/runner-groups` family (CRUD + group runners membership), `runner_group_id` always 1. |
| ~~1745~~ | P2 | bleephub actions — cancellation never reaches running jobs; always()/cancelled() jobs don't run after cancel | cancellation semantics | Cancel marks pending/queued jobs cancelled but a job executing on a runner runs to completion (no `JobCancellation` broker message — the runner's open mid-job poll exists exactly for this), and pending jobs gated on `always()`/`cancelled()` are force-cancelled instead of dispatched (real GitHub runs them with cancelled()==true). |

## False Positives

| Area | Finding | Why it is not a bug |
|------|---------|---------------------|
| `backends/aca/azure.go::fakeCredential` | Returns literal `"fake-token"` against simulator endpoints. | Simulator auth does not validate bearer tokens. Production clients use `azidentity.NewDefaultAzureCredential`; this credential is only for simulator endpoint clients. |
| `cmd/sockerless-admin/api_observability.go::envOrDefault` | Returns canonical OTel resource-attribute name when unset. | This is a documented default-value helper, not an error-hiding fallback. |
| aws scheduler simulator — GetSchedule `awsvpcConfiguration` (issue #477) | Reported as `Target.EcsParameters.NetworkConfiguration.AwsvpcConfiguration` returning None. | The sim persists + returns all three fields under the **lowercase** `awsvpcConfiguration` wire key — exactly what aws-sdk-go-v2 `scheduler` serializes/deserializes (`object.Key("awsvpcConfiguration")` / `case "awsvpcConfiguration":`) and botocore/GetSchedule use. The consumer's CLI assertion queried capital-A `AwsvpcConfiguration`, which fails against real AWS too; the Terraform read path (Go SDK) is unaffected. Consumer closed it. |

## Class-of-Bug Rules

- No stubs, fakes, mocks, synthetic data, silent fallbacks, or degraded modes.
- Public simulator APIs must match real cloud APIs; local gateway/admin plumbing must not leak into those APIs.
- Backend host primitive must match the cloud service: ECS in ECS, Lambda in Lambda, Cloud Run in Cloud Run, GCF in Cloud Run Functions, ACA in ACA, AZF in Azure Functions.
- External test fixtures use real clients: official SDKs, vendor CLIs, Terraform providers, and `gh` for bleephub.
- Closed enumeration means full-table audit before claiming fixed.
- Reopens require a postmortem: what test passed but should have failed, what client path was missed, and what new canonical-client test catches it.
- List operations need paged-iterator tests.
- Stateful resources need state-machine assertions.
- Mux pattern overlap is a recurring simulator bug class; run the overlap scanner when adding routes.
