# Known Bugs

Status [STATUS.md](STATUS.md) - roadmap [PLAN.md](PLAN.md) - resume [DO_NEXT.md](DO_NEXT.md).

**2394 filed - 2351 fixed - 2 open - 16 false positives.**

Every CI failure, live-cloud failure, simulator fidelity gap, or discovered fake/fallback lands here before any fix attempt. Detailed closed-bug history lives in PR descriptions and `git log`.

## Open

| ID | Sev | Area | Pattern | One-liner |
|----|-----|------|---------|-----------|
| 1345 | P2 | azuread terraform provider | upstream blocker | The `hashicorp/terraform-provider-azuread` provider has no supported way to redirect Microsoft Graph API calls to a custom endpoint (no `microsoft_graph_endpoint` override). Feature request open upstream: https://github.com/hashicorp/terraform-provider-azuread/issues/1837. Entra provisioning via Terraform (`azuread_group`, `azuread_user`, `azuread_group_member`) cannot be tested against the sim until this is resolved upstream. |
| 1075 | P2 | live-cloud validation | unvalidated real cloud | Lambda is the only backend with a green live-cloud cell. Cloud Run Services, ACA Apps, AZF cloud-DNS, Lambda service-mesh, and ACA/AZF Azure AD remain unvalidated against authenticated real clouds. Do not mark these green without real cloud runs. |

## Recently fixed (cont.)

| ID | Sev | Area | Pattern | One-liner |
|----|-----|------|---------|-----------|
| ~~2394~~ | P2 | Bleephub repositories | Pages capability hidden by constant repository JSON | Repository JSON now derives `has_pages` from the persisted GitHub Pages site store, so repository metadata reports false before Pages creation, true while a Pages site exists, and false again after deletion. |
| ~~2393~~ | P2 | Bleephub GraphQL repositories | REST-backed merge settings hidden from GraphQL | GraphQL `Repository.mergeCommitAllowed`, `rebaseMergeAllowed`, and `squashMergeAllowed` now read the same persisted repository merge-method settings as REST `allow_merge_commit`, `allow_rebase_merge`, and `allow_squash_merge` instead of always returning true. |
| ~~2392~~ | P2 | Bleephub repositories | repository feature settings ignored or hardcoded | Repository discussion enablement is now persisted as a nullable GitHub-defaulted repository setting; REST repository JSON, migration repository JSON, and GraphQL `Repository.hasDiscussionsEnabled` read that state, and GraphQL `createRepository` applies accepted feature inputs instead of ignoring them. |
| ~~2391~~ | P2 | Bleephub GraphQL repositories | REST-backed repository license hidden from GraphQL | GraphQL `Repository.licenseInfo` now reads the same persisted repository license key/name/SPDX metadata that repository creation, REST repository responses, and the REST license endpoint already expose; the GraphQL sweep creates a licensed repository through the public REST path and asserts the license object. |
| ~~2390~~ | P2 | Bleephub repository tags | annotated tag object leaked as commit | `GET /api/v3/repos/{owner}/{repo}/tags` now peels annotated tag objects through real git storage before rendering `commit.sha` and `commit.url`; the regression creates both annotated and lightweight tags through the public Git Data API instead of accepting an empty tag list. |
| ~~2389~~ | P3 | CI Bleephub check names | simulator shorthand on GitHub-compatible service checks | Bleephub continuous-integration checks and branch-protection required contexts now use fully qualified names (`Bleephub end-to-end`, `Bleephub GitHub software development kit`, `Bleephub GitHub command-line interface`, and `Bleephub GitHub Actions runner`) instead of `sim (bleephub ...)`, so GitHub-compatible service coverage is not mislabeled as a simulator job. |
| ~~2388~~ | P2 | CI build-arm64 | under-budgeted required check | Required all-binary build jobs now have a ten-minute job and step budget, so hosted-runner variance does not cancel the amd64/arm64 build matrix at the five-minute boundary before it can prove the builds. |
| ~~2387~~ | P2 | Bleephub pull requests | same-repository-only pull request model | Pull requests now persist the real head repository and maintainer-edit flag, so fork pull requests create from `owner:branch`, render REST/GraphQL head repository fields from the fork, derive commits/files from the fork git storage, emit fork-aware pull request webhook payloads, and merge by copying real fork git objects into the base repository before materializing the merge commit. |
| ~~2386~~ | P2 | Bleephub GraphQL repositories | hardcoded repository metadata | GraphQL `Repository` now reports repository settings and implemented features from real store/REST state: wiki, Projects classic, Discussions, homepage, fork parent/count, template status, and delete-branch-on-merge no longer resolve from fixed false/null/zero values. |
| ~~2385~~ | P2 | Bleephub GraphQL repositories | hardcoded repository watcher count | GraphQL `Repository.watchers.totalCount` now reads the same REST-backed repository watch subscriptions as `/subscribers` and `/subscription`, so GraphQL clients see real watcher totals. |
| ~~2384~~ | P2 | Bleephub GraphQL pull requests | hardcoded empty GitHub connections | `PullRequest.files` now derives changed files from the same git merge-base/head diff as `GET /pulls/{number}/files`, and `PullRequest.closingIssuesReferences` resolves GitHub closing-keyword issue references from the real pull request body to store-backed issue nodes. |
| ~~2383~~ | P1 | Bleephub GraphQL pull requests / store locking | reentrant store read lock under git-backed PR commit rendering | Pull request GraphQL rendering now snapshots the repository git-storage handle before taking the store read lock and derives real git commit objects outside that lock, so `PullRequest.commits` no longer re-enters `Store.GetGitStorage` while rendering store-backed metadata. |
| ~~2382~~ | P2 | Bleephub user interface repository inventory | user-facing pages used operator-only repo inventory | The Bleephub user interface's shared repository inventory helper now reads the authenticated user's repositories through paginated `GET /api/v3/user/repos`, so Codespaces, Migrations, and the registered-runner registry no longer depend on the operator-only `/internal/repos` endpoint. |
| ~~2381~~ | P2 | Bleephub user interface end-to-end setup | Playwright auth selected login by stale placeholder | The Bleephub Playwright authentication setup now selects the login token input by its accessible token label instead of the old `BLEEPHUB_ADMIN_TOKEN` placeholder, so login-copy changes do not break the end-to-end authentication setup. |
| ~~2380~~ | P2 | dependency freshness / pre-push | stale AWS software development kit module pins blocked push | The required pre-push dependency freshness hook caught stale `github.com/aws/aws-sdk-go-v2/config` and `github.com/aws/aws-sdk-go-v2/credentials` pins in `backends/ecs`, `backends/lambda`, `bleephub`, `bleeplab`, and `simulators/aws/sdk-tests`. The affected modules were refreshed with `make upgrade-deps`, and `scripts/check-latest-deps.sh` passed. |
| ~~2379~~ | P2 | Bleephub UI authentication | login used operator-only status instead of GitHub identity | The Bleephub login form now verifies tokens against `GET /api/v3/user`, matching the GitHub identity boundary documented for the service. OAuth/user-to-server tokens accepted by the GitHub REST user endpoint can sign in to the user interface, while operator-only `/internal/*` pages still fail loudly when a token lacks operator access. |
| ~~2378~~ | P2 | dependency freshness / pre-push | stale Google Cloud Pub/Sub module pins blocked push | The required pre-push dependency freshness hook caught `cloud.google.com/go/pubsub` pinned behind latest in `simulators/gcp` and `simulators/gcp/sdk-tests`. The affected Google Cloud simulator modules were refreshed with `make upgrade-deps`, bringing Google Cloud Pub/Sub plus related indirect support modules to current published versions. |
| ~~2377~~ | P2 | AWS simulator continuous integration readiness / AWS Command Line Interface harness | short startup poll and capability skip made continuous integration misleading | The AWS simulator software development kit job failed when a second process-mode simulator needed more than the old five-second health budget under continuous integration load, and the AWS Command Line Interface harness still had a dynamic unsupported-operation skip path. AWS simulator software development kit, AWS Command Line Interface, and Terraform health polling now use a bounded 30-second readiness budget with the last observed status/error in the failure, and the AWS Command Line Interface harness installs a current AWS Command Line Interface when the host binary lacks a recent operation while command failures now fail loudly instead of skipping. |
| ~~2376~~ | P2 | CI Go module setup | transient Go proxy stream reset failed simulator jobs before tests | The `sim (aws cli compute)` job failed in `go mod download` before running tests after `proxy.golang.org` reset a large module zip stream. CI now uses `scripts/ci-go-mod-download.sh`, a bounded retry wrapper for required module downloads, across simulator SDK/CLI setup and AWS SDK prebuild steps so dependency setup fails loudly only after repeated download failures. |
| ~~2375~~ | P2 | Bleephub code scanning URLs | public API payloads leaked operator-only internal routes | CodeQL database downloads and variant-analysis query-pack URLs advertised `/internal/...` download routes from successful `/api/v3` responses. Those byte downloads now use public non-internal storage coordinates, the obsolete internal download routes were removed, and the OpenAPI response observer rejects future successful GitHub REST JSON responses that contain `/internal/` URLs. |
| ~~2374~~ | P2 | dependency freshness / pre-push | stale Go module pins blocked push | The required pre-push dependency freshness hook caught `golang.org/x/net` pinned behind latest in `bleephub`, `simulators/aws`, and `simulators/aws/sdk-tests`. The affected modules were refreshed with `make upgrade-deps`, bringing the stale Go dependencies to current published versions, and `scripts/check-latest-deps.sh` passed. |
| ~~2373~~ | P2 | Bleephub Packages store | duplicate version names created ambiguous package state | Package version creation accepted duplicate live names under one package, so a registry tag push or package publish could create multiple active versions for the same reference. Package-version creation now rejects an existing live version name and tests pin the conflict. |
| ~~2372~~ | P1 | Bleephub Packages publishing | user-facing package upload used operator-only seed route | The Packages page exposed an "Upload version" action that posted package bytes to `/internal/packages/.../versions`, an operator seed route with no GitHub equivalent. Bleephub now accepts container package publication through a GitHub Container Registry-compatible OCI/Docker Registry HTTP API v2 path under `/v2/`, and the user interface no longer calls internal package upload endpoints. |
| ~~2371~~ | P1 | Bleephub pull request review threads | user-facing workflow depended on internal helper instead of GitHub GraphQL | Pull request review-thread listing and resolve/unresolve were real store-backed state, but the Bleephub UI read and mutated them through operator-only `/internal/repos/{owner}/{repo}/pulls/{number}/review-threads` routes while the GitHub-compatible GraphQL `resolveReviewThread` and `unresolveReviewThread` mutations were absent. Review threads now use the public GraphQL pull request `reviewThreads` connection and GitHub-shaped mutations, and the legacy internal review-thread routes were removed. |
| ~~2370~~ | P2 | AWS simulator EC2 lifecycle and EBS | host data-plane liveness overwrote control-plane truth | The EC2 simulator conflated local Firecracker VM liveness with AWS EC2 control-plane state: `DescribeInstances` rewrote running instances to stopped when no live VM process existed, and EBS attach/modify failed on capable-but-blocked continuous-integration hosts even though EC2 attachment metadata is a control-plane operation. EC2 instance state and EBS attachments now stay authoritative in the control plane, while real block-device patching runs only when an actual live Firecracker VM exists. |
| ~~2369~~ | P2 | dependency freshness / pre-push | stale Go module pins blocked push | The required pre-push dependency freshness hook caught stale Go pins in `backends/ecs`, `backends/lambda`, `bleephub`, `simulators/aws/sdk-tests`, and `simulators/azure/sdk-tests`: AWS EC2/ECS SDK clients, `golang.org/x/crypto`, `golang.org/x/image`, `github.com/coreos/go-oidc/v3`, and related indirect support modules. The affected modules were refreshed with `make upgrade-deps`, and `scripts/check-latest-deps.sh` passed. |
| ~~2368~~ | P2 | AWS simulator EC2 lifecycle | optional host execution blocked control-plane convergence | `RunInstances` could leave an instance in `pending` until optional Firecracker host setup returned, so capable-but-slow or blocked continuous-integration hosts timed out waiting for the public EC2 control plane to report `running`. The simulator now advances the EC2 control-plane state to `running` before attempting real host boot work, so public EC2 lifecycle reads do not remain pending behind host substrate setup. |
| ~~2367~~ | P3 | Bleephub runner integration harness | iteration helper skipped only one coarse suite | `BLEEPHUB_TEST_FROM=N` was documented as a numbered-test starting point but only gated the host-mode group before test 12, so `BLEEPHUB_TEST_FROM=8` still ran tests 1-7. Each numbered test is now wrapped in the same `run_test N` guard, so local iteration starts at the requested numbered test and continuous integration still runs the full suite by default. |
| ~~2366~~ | P2 | Bleephub Actions workflow dispatch harness | Bash default expansion corrupted non-empty JSON | The runner integration harness used `${3:-{}}` and `${5:-{}}` inside quoted `local` declarations, which appended an extra `}` when non-empty workflow-dispatch input JSON was passed and made test 8 fail in `jq --argjson`. The helpers now default empty arguments explicitly to `{}` and preserve provided JSON bytes unchanged. |
| ~~2365~~ | P2 | CI dependency freshness / GCP simulator | stale Google Cloud long-running operations module | The CI `check-deps` job caught `simulators/gcp` pinning `cloud.google.com/go/longrunning` at v1.1.0 after v1.2.0 was available; the GCP simulator module was refreshed with `make upgrade-deps`, bringing the long-running operations client and related indirect dependencies to the current published versions, and `scripts/check-latest-deps.sh` passed. |
| ~~2362~~ | P1 | Bleephub GitHub Apps and OAuth Apps | legacy internal app-management routes beside GitHub flows | The legacy operator-only GitHub App, installation, and OAuth App management routes were removed from `/internal/apps*`, `/internal/installations`, and `/internal/oauth-apps`; GitHub App creation/listing/installation and OAuth App creation/listing now live only on browser settings/public GitHub-shaped routes, and the route-registry guard fails if those internal app-management routes return. |
| ~~2364~~ | P1 | Bleephub GitHub Apps and OAuth Apps | user interface used operator-only app management endpoints | The Apps page no longer lists GitHub Apps, OAuth Apps, or installations through `/internal/apps`, `/internal/oauth-apps`, or `/internal/installations`; it reads owned GitHub Apps through `/settings/apps`, OAuth Apps through `/settings/oauth-apps`, installations through `GET /api/v3/user/installations`, creates OAuth Apps through the browser settings form route, and manages installation suspend/unsuspend/delete through authenticated settings routes backed by the same store transitions and webhook events. |
| ~~2363~~ | P1 | Bleephub GitHub Actions runners | endpoint compatibility split | GitHub Actions workflow dispatch now preserves GitHub's host-mode job shape by sending no default job container when workflow YAML has no `container:` block; the Docker runner integration harness creates a real repository, writes workflow files through the contents API, dispatches them through repository-scoped workflow dispatch, polls/cancels through public run/job endpoints, and points both resident and dispatcher-spawned runners at `admin/test`; the Playwright seed path does the same, and the Workflows/Overview/Detail UI reads workflow files, runs, run detail, jobs, and logs through repository-scoped GitHub Actions REST endpoints instead of `/internal/exec`, `/internal/workflow_files`, or `/internal/workflows`. |
| ~~2361~~ | P2 | bleephub Projects v2 GraphQL | project-level store state hidden from GraphQL | GraphQL `ProjectV2` nodes now expose store-backed `fields`, `views`, and `items` connections, including field options, iteration configuration, view filters/visible fields, full item Relay pagination, and shared full `PageInfo`/edges on ProjectV2 item connections for Issue, PullRequest, and ProjectV2. |
| ~~2360~~ | P2 | bleephub OAuth web flow | non-standard compatibility bypass | The OAuth web-flow shortcut `?auto=1` no longer mints authorization codes or selects the seed admin. Authorization requires a real browser session plus the CSRF-backed consent POST, tests drive that path, and the UI flow simulator opens only the real web flow. |
| ~~2359~~ | P2 | bleephub Projects v2 GraphQL | REST-backed field values hidden by partial GraphQL union | Projects v2 GraphQL now accepts and projects every field-value kind the store supports: text, number, date, single-select, and iteration values validate through the real Projects v2 store, round-trip via `updateProjectV2ItemFieldValue`, and render through typed `ProjectV2ItemFieldValue` union members from `Issue.projectItems.fieldValueByName`. |
| ~~2358~~ | P2 | bleephub GraphQL issue comments | persisted comment pin state hidden from GraphQL | Issue-comment pin/unpin state now projects through GraphQL `Issue.comments.nodes.isPinned` from the persisted REST-backed `Comment.Pinned` field, with a REST-to-GraphQL regression that pins a real issue comment before querying it. |
| ~~2357~~ | P2 | bleephub org audit log | unpaginated shape-thin listing | The organization audit-log endpoint now paginates through the shared GitHub-style list helper, adds Link headers, and treats `phrase` as tokenized search across persisted action, actor, org, and detail data instead of only exact action equality. |
| ~~2356~~ | P2 | bleephub Actions event refs | unresolved git refs became all-zero workflow SHAs | Event-triggered and scheduled workflow runs now require the triggering git ref to resolve to a real commit; non-empty refs no longer fall back to `HEAD`, scheduled runs reject missing default-branch refs loudly, reusable workflow file reads fail when `HEAD` is unresolved, and git fixtures create real `main` refs instead of relying on compatibility fallback behavior. |
| ~~2355~~ | P2 | bleephub Actions logs | console fallback masqueraded as uploaded job logs | GitHub job, run, and run-attempt log download endpoints now serve only runner-uploaded log files referenced by timeline records; object-store mode reads those bytes back from S3-compatible storage, and missing uploaded logs return 404 instead of substituting in-memory live console capture. |
| ~~2354~~ | P2 | bleephub GraphQL issue fields | REST-backed custom issue values hidden from GraphQL | Organization issue fields and per-issue field values were real REST state, but GraphQL Issue nodes did not expose those values, so clients using GitHub's GraphQL issue-field surface could not see custom issue metadata after writing it through the API. `Issue.issueFieldValues` now exposes the GitHub GraphQL issue-field value union from the same REST-backed store records, including typed field definitions, select options, enum values, pagination, and a REST-to-GraphQL regression that writes all supported field kinds through the API before querying them. |
| ~~2353~~ | P3 | bleephub Actions tests | rare skip hid workflow-file ID regression guard | `TestActionsRun_WorkflowFileReferences` skipped if the run ID happened to equal the stable workflow-file ID, leaving the exact run-id-vs-file-id invariant untested on that seed. The test now chooses a non-colliding workflow file path, writes it onto the run, and always asserts the REST run uses the workflow-file ID/path rather than the run ID. |
| ~~2352~~ | P2 | bleephub persistence | unsupported state backend plus skip-if-env test | Bleephub is supposed to keep its own state in SQLite, but the implementation still accepted `BLEEPHUB_DATABASE_URL` for PostgreSQL and the PostgreSQL round-trip test skipped when `BLEEPHUB_TEST_POSTGRES_URL` was unset. The PostgreSQL path and driver dependency were removed, `BLEEPHUB_DATABASE_URL` now fails loudly, CI no longer starts a PostgreSQL sidecar, and persistence coverage runs unconditionally against SQLite. |
| ~~2351~~ | P2 | CI GCP simulator setup | streamed dependency download truncated before extraction | The `sim (gcp)` job streamed the Google Cloud CLI tarball through `curl | tar`, so a transient HTTP/2 stream error produced a partial gzip and failed the job before tests ran. The workflow now downloads the archive to `$RUNNER_TEMP` with fail-loud retry-all-errors and extracts only the completed file. |
| ~~2350~~ | P2 | bleephub Actions workflow discovery | default-branch ref ignored when git HEAD was unset | Actions workflow discovery walked only git storage `HEAD`, so repositories initialized through Git Data refs could have a real default-branch workflow file that `GET /actions/workflows` and workflow dispatch did not see. Discovery now resolves the repository's recorded default branch when `HEAD` is unset, so Git Data seeded workflow files list and dispatch through the same Actions API path. |
| ~~2349~~ | P2 | bleephub SDK Actions coverage | skipped typed-client dispatch flow | `bleephub/sdk-tests` skipped the go-github workflow-dispatch, run-by-id, and workflow-jobs path instead of seeding a real git-backed workflow file through Git Data. The SDK suite now creates a real `.github/workflows/ci.yml` commit/ref through Git Data, lists the workflow, dispatches it, reads the run by ID, and lists the queued job through go-github. |
| ~~2348~~ | P2 | CI cloud-backend lint | imbalanced lint shard timed out after clean module lints | The `lint (cloud-backends)` job ran seven backend modules in one 5-minute shard, so hosted-runner dependency downloads plus six completed zero-issue lint passes left `backends/azure-functions` running when GitHub canceled the job. CI now runs separate AWS, Google Cloud, and Azure cloud-backend lint shards so each provider family has its own timeout budget and runs in parallel. |
| ~~2347~~ | P2 | bleephub Actions reruns | workflow-file identity lost on rerun | Workflow-run rerun, failed-job rerun, and job rerun selected cached YAML by workflow display name instead of the originating workflow-file identity, so repositories with multiple workflow files sharing `name:` could rerun the wrong YAML. Reruns now resolve cached YAML by the run's workflow-file ID/path first, preserve that identity through the new attempt, and fail loudly when an older name-only run is ambiguous. |
| ~~2346~~ | P2 | bleephub Pages builds | synthetic build commit, shared ID allocator, and lost IDs on reload | Manual Pages builds stored the all-zero SHA, allocated build IDs from the audit-log sequence, and lost the internal build ID on reload because the REST build object intentionally omits a top-level `id`. Pages build requests now require an enabled Pages site and a real default-branch commit, store that commit SHA, use a dedicated persisted Pages-build ID sequence, and rehydrate the internal ID from the persisted build URL. |
| ~~2345~~ | P2 | bleephub UI issue sidebar | org-only endpoint probed for user-owned repos | The issue sidebar treated organization issue types as discoverable by probing `/orgs/{owner}/issue-types` and hiding the selector after a 404, which made user-owned issue detail pages emit browser console errors under Playwright. The issue detail page now fetches repository metadata, passes the factual owner type into the sidebar, and the sidebar only queries org issue-type definitions and GraphQL issue-type assignment for organization-owned repositories. UI regressions assert user-owned repos do not call the org endpoint, and the Bleephub Playwright E2E issue flow passes without the 404 console error. |
| ~~2344~~ | P2 | bleephub issue types | org definitions not assignable to issues | Organization issue-type definitions were real, but individual issues had no assignment field and GraphQL `Issue.issueType` always returned null. Issues now store a per-issue organization issue-type assignment, REST create/PATCH validates assignment IDs against the repository owner's enabled org issue types, GraphQL `Issue.issueType` projects the assigned definition, the issue sidebar edits it through the same validated path, and reload regressions prove the assignment persists. |
| ~~2343~~ | P2 | bleephub GraphQL sub-issues / sub-issue persistence | REST-backed relationship hidden by GraphQL stub + stale old-parent row | Bleephub's REST sub-issues were real ordered store links, but GraphQL `Issue.parent`, `Issue.subIssues`, and `Issue.subIssuesSummary` still returned the old empty/null compatibility shape. Replacing a child's parent also failed to persist the old parent's shortened child list, so reload could resurrect a stale parent-child link. GraphQL now projects parent/sub-issue/summary data from the same store links as REST, summary completion derives from child issue state, and parent replacement persists both old and new parent lists with a reload regression. |
| ~~2342~~ | P2 | bleephub Actions artifact/cache/log bytes | local-only byte storage behind object-storage spec | Bleephub's Actions artifact store kept artifacts, dependency caches, and runner-uploaded log files only in process memory plus optional local data-dir files, while the storage split requires durable byte content to live in object storage. `BLEEPHUB_OBJECT_S3_BUCKET` now configures S3-compatible byte storage for Actions artifacts, caches, and log files (with endpoint/prefix overrides); configured object storage fails startup/write/read loudly, deletes remove object bytes, and simulator-backed tests assert artifact/cache/log uploads write real S3 objects. |
| ~~2341~~ | P2 | bleephub S3 object storage tests / AWS simulator S3 CopyObject | fake S3 test boundary masked list-invisible copied objects | Bleephub's S3 filesystem tests used a hand-rolled fake S3 HTTP server that reimplemented object CRUD and pagination instead of driving a real object-store endpoint. Replacing it with `simulator-aws` exposed an S3 simulator fidelity bug: `CopyObject` wrote the destination under the correct store key but persisted the object's internal `Key` without the bucket prefix, so `GetObject` worked while `ListObjectsV2` could not see copied objects. Bleephub S3 tests now build/start the real AWS simulator and seed/assert through `aws-sdk-go-v2`; repo-prefix rename/delete cross the real 1000-key pagination boundary. The simulator stores copied objects with the canonical bucket/key and SDK + CLI regressions prove copied objects list. |
| ~~2340~~ | P2 | bleephub repository lifecycle persistence | rename/transfer/delete did not cover repo-keyed metadata | Repository rename and transfer moved git storage but left several persisted repo-scoped metadata buckets under the old `owner/repo` key, and both paths deleted the updated repository row after writing it. Repository delete also missed newer repo-scoped buckets. Rename, transfer, and delete now share repo-key lifecycle helpers that update or purge persisted buckets across Actions, security/code scanning, CodeQL, custom properties, immutable releases, code quality, rulesets, and Copilot coding-agent state; reload regressions prove old keys do not survive. |
| ~~2339~~ | P3 | bleephub CodeQL variant-analysis tests | non-tar bytes labelled query pack | The CodeQL variant-analysis test uploaded arbitrary marker bytes even though the store contract describes `query_pack` as a base64 tarball. The regression fixture now uploads a deterministic real tar.gz query pack and asserts that the advertised `query_pack_url` returns those exact archive bytes. |
| ~~2338~~ | P2 | bleephub Actions — action download metadata | synthetic all-zero SHA + hidden action fallbacks | `POST /_apis/v1/ActionDownloadInfo/...` returned an all-zero `resolvedSha` for a bleephub-hosted action until the tarball endpoint had already been fetched; the tarball path also resolved missing action refs through the default branch and fetched absent action repositories from github.com. Action download metadata now resolves local action refs directly from git storage, missing refs fail loudly, absent action repositories no longer trigger outbound github.com fetches, and the unit tests use real local git-backed tarballs instead of synthetic tarball bytes or network-dependent status assertions. |
| ~~2337~~ | P2 | CI — Azure simulator setup | remote installer reintroduced apt outage | The Azure simulator CI job installed Azure CLI by piping `https://aka.ms/InstallAzureCLIDeb` into `sudo bash`, so the same Microsoft apt metadata outage bypassed the repo-owned `ci-apt-update.sh` hardening and failed before tests ran. The job now verifies the required hosted-runner `az` binary with `az version` and fails loud if the dependency is absent, instead of mutating apt sources through an unowned installer. |
| ~~2336~~ | P2 | CI — apt setup | third-party runner source blocked required packages | `scripts/ci-apt-update.sh` retried a degraded GitHub-hosted runner apt state three times but kept Microsoft runner-provided sources enabled, so invalid signed metadata from those sources blocked jobs that only needed Ubuntu packages. After the normal retries fail, the script now quarantines the known Microsoft source files and retries against the required Ubuntu sources. |
| ~~2335~~ | P2 | bleephub SDK/gh CLI tests | metadata-only PR fixtures after PR fidelity fix | The go-github SDK PR tests and the gh CLI parity harness still created pull requests from bare `head=feature`/`base=main` names in empty repositories, so the branch's new real-ref requirement correctly returned 422 in CI. The SDK suite now creates blobs, trees, commits, and refs through go-github's Git Data API before PR creation, `TestGitData` became live coverage instead of a stale skip, and the gh CLI harness pushes real `main` and `feature` refs through smart HTTP before its raw PR probe. |
| ~~2315~~ | P3 | bleephub — GraphQL PR reviewRequests | hardcoded empty connection | `pullRequestToGQL` now renders `reviewRequests` from real `RequestedReviewerIDs` using a `ReviewRequest` node and requested-reviewer union, so `gh pr view` sees the same requested reviewers the REST surface stores. |
| ~~2314~~ | P3 | bleephub — metadata-only PR model | pseudo shas for ref-less PRs | Pull requests now require real head and base refs at creation, REST/GraphQL PR commits and review `commit_id`s derive from git storage, merge responses use the real merge commit, and metadata-only pseudo SHA paths were removed. The tests now seed real branches before creating PRs. |
| ~~2313~~ | P3 | bleephub — releases | generate-notes body ignores derivable merged-PR state | `POST /repos/{o}/{r}/releases/generate-notes` now resolves the requested tag range through git storage, walks real commits, matches merged PR merge commits, and emits "What's Changed" bullets plus the Full Changelog compare line. |
| ~~2334~~ | P3 | test policy — skip-if-tool-absent | hook coverage gap | The no-skip-if-tool-absent rule lived in prose only, so a future test could reintroduce a missing-tool `t.Skip` without a local guard. Added `scripts/check-no-tool-absent-skips.sh` to pre-commit so new missing-tool/dependency skips fail before commit; required tools must be installed by the harness or fail loud. |
| ~~2333~~ | P1 | simulators — shared memory state | mutable reference escape | The shared `MemoryStore` implementations returned and stored structs by value but left reference fields (maps, slices, pointers) aliased with store-owned state; SQLite stores did not, because JSON round-trips copied them. The AWS, GCP, and Azure memory stores now deep-copy values at every store boundary (`Put`, `Get`, `List`, `Filter`, `Update`, and AWS `Upsert`) and shared store tests prove callers cannot mutate stored reference fields through returned values. |
| ~~2332~~ | P2 | AWS sim — ECS task resources (#776) | resource-limit fidelity coverage | The ECS simulator's task launch path needed a regression guard proving task-level and container-level CPU/memory were translated into the cgroup fields the shared container launcher applies (`MemoryBytes`/`NanoCPU` → Docker/Podman `HostConfig.Memory`/`NanoCPUs`). Added focused tests for task fallback and container-definition override sizing so advertised ECS metadata limits stay coupled to real container limits. |
| ~~2331~~ | P3 | AWS SDK tests — DynamoDB Local oracle | skip-if-absent test | The DynamoDB differential oracle skipped when `docker` was missing, reporting green without exercising DynamoDB Local. Docker is now a required dependency for that oracle-backed test and a missing binary fails loud. |
| ~~2330~~ | P1 | AWS sim — DynamoDB concurrent reads (#777) | concurrent map iteration/write | DynamoDB read APIs returned or examined stored item maps directly while `UpdateItem` could mutate the same maps under `ddbItemsMu`, so capacity calculation/projection could panic with `concurrent map iteration and map write`. `GetItem`, `Query`, `Scan`, `BatchGetItem`, and `TransactGetItems` now clone each item under `ddbItemsMu` via `ddbItemSnapshot` before projection, expression matching, response rendering, and consumed-capacity calculation; the regression runs under `-race`. |
| 2323 | P3 | bleephub — custom properties | property name whitespace/control chars accepted | `POST/PATCH /orgs/{org}/properties/schema` accepted property names with leading/trailing/embedded whitespace and control characters (`property_name:" 00"` → 200); real GitHub validates the name pattern. Added `validCustomPropertyName`; invalid → 422. Surfaced by `FuzzCustomPropertyDefinition` (crasher kept as a regression seed). |
| 2322 | P3 | bleephub — search envelope | empty result serialized `items: null` | An empty `/search/*` result marshalled a nil `[]map` to `"items": null`; real GitHub's search envelope is always an array. Now materialized to `"items": []`. Surfaced by `FuzzSearchEndpoints`. |
| 2321 | P2 | bleephub UI — api.ts parsers | silent-wrong list unwrappers | `ghFetchPage` cast any JSON body straight to `T[]` (a non-array error/envelope flowed to components as fake items and crashed on `.map`); `fetchSecrets`/`fetchEnvironments` resolved to `undefined` on a missing array despite comments claiming they fail loud. All three now `Array.isArray`-guard and throw a clear error → InlineError. Surfaced by property/fuzz-style parser tests. |
| 2320 | P2 | bleephub — unbounded in-memory growth | delivery/audit/status histories never trimmed | Per-hook webhook + app-hook delivery history, the org/site audit log, and per-ref commit statuses grew without bound under sustained load (the audit log also O(n) prepend; commit-status create was O(total statuses) to compute the next id). Capped to GitHub-shaped recent windows (`maxHookDeliveries=500`, `maxAuditLogEntries=5000`, `maxCommitStatusesPerRef=1000`) with an O(1) status id counter. Found by the sustained-load harness + unbounded-growth audit. |
| 2319 | P2 | bleephub — notifications + search render-all | O(n²) list handlers rendered every item before paginating | `ListNotifications` was doubly O(n²) (per-thread full comment scan for the reason + rendering every thread before slicing) — ~2.2s and ~1.5GB heap per request on a large corpus; `handleSearchIssues` rendered every matched issue+PR before slicing. Both now compute reasons in one pass and render only the requested page. ~103× / ~277× faster; heap flat. Regression: `BenchmarkListNotifications`, `BenchmarkSearchIssues`, `TestLoadSustained`. |
| 2318 | P2 | bleephub — list handlers full-store scans | per-request O(all issues+PRs+comments+reviews) | `ListIssues`/`GetIssueByNumber`/`ListPullRequests`/`CountOpenIssues`, per-item comment counts, and per-PR review lookups scanned the entire store on every request. Added maintained per-repo/per-parent indexes (`IssuesByRepo`, `PullsByRepo`, `CommentCounts`, `PRReviewsByPR`) kept under `st.mu` at create/delete/reload. 4–14× faster on the hot paths; sustained-load throughput 40→926 req/s, p99 16s→276ms. Regression: `bench_hotpaths_test.go`, `TestLoadSustained`. |
| 2317 | P2 | bleephub — reactions serializer | off-lock concurrent map read | `handleListReactions` read `s.store.Users[rx.UserID]` without holding `st.mu` — a data race / concurrent-map-read against writers under load. Now uses the lock-safe `GetUserByID`. Flagged by the parser fuzzer, confirmed and fixed under the concurrency stress harness. |
| 2316 | P1 | bleephub — serializers/webhook builders | mutable struct fields read after releasing (or without) `st.mu` | Under the `-race` CRUD storm, eight serializers/payload builders (`issueToJSON`, `repoToJSON`, `pullRequestToJSON`/`pullRequestSimpleJSON`, `repoToGraphQL`, the webhook `buildIssues/PullRequest/PushPayload` builders, `handlePutRepoTopics`, `handleCreateIssueComment`) read title/body/state/topics/lock/timestamps off a shared pointer while `Update*` writers mutated them under the write lock — real data races. Fixed by snapshotting the mutable scalars under a single (non-nested) read lock via new `snap{Repo,Issue,PR,User}` helpers and `ResolveCommentParent`. Regression: `stress_crud_test.go`, `stress_lockgraph_test.go` (`-race`). |
| 2311 | P2 | bleephub — store locking | recursive RLock + ABBA inversion class | A type-aware lock-graph analysis (go/packages, per-mutex critical sections, transitive may-acquire closure) found ten handler/serializer paths re-acquiring `Store.mu` while holding it (all five search handlers, global user issues, notifications list/thread, GraphQL PR serialization) plus a Store.mu↔Misc.mu ABBA inversion in the follower handlers. Fixed via documented `*Locked` helper variants + two-phase gather/render restructures; lock order documented on `Store.mu`; `scripts/check-locked-helpers.sh` pre-commit guard added. Boyscout: `notificationsStateFor` mutated a map under RLock and `issueToJSONForPullRequest` read store maps unlocked — both real races fixed. Regression: `TestStoreLockReentrancyUnderWriterPressure` deadlocks before the fix, passes in ~1s after (also under -race). |
| 2310 | P3 | bleephub — PR timeline events | committed/merged/review_requested events absent | PR timelines carry the full documented event set: reviewer request/removal, close/reopen, and merge (REST + GraphQL) record real issue events (`review_requested`/`review_request_removed` with `review_requester` + `requested_reviewer`, `merged` followed by `closed`, `reopened`), and `committed` entries derive at render time from the PR's real base..head git commits (no stored copies), interleaved by commit author date. `PUT /pulls/{n}/merge` materialises a real merge/squash/rebase commit in git storage (merge_method honored, expected-`sha` 409 guard), so the merged event's `commit_id`, `merge_commit_sha`, `commits`, and head/base shas are real for git-backed PRs; the documented `GET /pulls/{n}/commits` (the advertised `commits_url`) was added, and `GET /issues/{n}/events` serves PR numbers (shared number space, as on real GitHub). Boyscout: `gitCommitToJSON` gained the required `html_url`/`parents[].html_url`/`verification` members, `ListIssueEvents` is ID-sorted for stable pagination, and the GraphQL merge mutation now emits the `pull_request closed` webhook + workflow trigger, and `NewStore` initializes `NextIssueEventID` (the first issue event ever recorded on a fresh store previously got id 0). Test: `TestPullRequestTimelineFullFlowREST`. |
| 2312 | P2 | bleephub UI | eleven surfaces without UI | Added: global search (all seven /search tabs), PR reviews workflow (reviews, review-comment threads with diff hunks, requested reviewers, merge-box statuses/checks, reactions, timeline), deployments page (statuses, environments, pending approvals), webhook delivery inspection with redelivery (repo + org), GitHub Pages settings panel, repo collaborators management, repo social reads (stargazers/watchers/forks/languages/branches/tags), account page (SSH/GPG/signing keys, emails, blocks), Copilot Spaces CRUD with collaborators/resources, enterprise team organization assignments, custom property required/default editing + org repository-values panel. |
| 2309 | P3 | bleephub — issue/comment JSON | documented reactions rollup missing | Issue and issue-comment responses omitted the `reactions` rollup (url + total_count + the eight content counters) that review comments already emitted. Now attached in `issueToJSON`/`commentToJSON` from real reaction state. |
| 2308 | P2 | bleephub — reactions | issue reactions keyed by number → cross-repo leak | Issue reactions were keyed by issue number alone, so reactions on issue N of one repository surfaced on issue N of every repository, and reacting to a nonexistent issue silently succeeded. Handlers now resolve through the repository to the issue's global id and 404 on unknown repo/issue. Test: `TestReactions_AllParentTypes` cross-repo isolation assertions. |
| 2307 | P3 | bleephub — blocks/follow lists | thin `{login}` objects instead of simple-user | `GET /user/blocks` and the followers/following listings returned bare `{"login": ...}` maps; they now emit the documented `simple-user` shape via `userToJSON`. |
| 2306 | P2 | bleephub — repo collaborators | fabricated invitation + instant add | `PUT /repos/{o}/{r}/collaborators/{username}` returned a synthetic invitation (`id: 0`) and added the collaborator immediately, so `GET /invitations` could never list anything. Now GitHub-faithful: new invitee → real pending invitation (201, accepted via `PATCH /user/repository_invitations/{id}`), existing collaborator → in-place permission update (204). Also removed the spec-invalid `role_name` member from `invitationJSON`, and `repoToJSON`/`fullRepoJSON` no longer hardcode zero forks/subscribers counts (now derived via `Store.CountForks`/`ListRepoSubscribers`). |
| 2305 | P2 | bleephub — PR surface | missing GET requested_reviewers + PR timeline 404 + timeline deadlock | `GET /pulls/{n}/requested_reviewers` (a documented operation) was unregistered; `GET /issues/{n}/timeline` 404'd for PR numbers (PRs share the issue number space); `Store.BuildIssueTimeline` held `st.mu.RLock()` across helpers that re-acquire it — a recursive-RLock deadlock against a queued writer. All three fixed; PR timelines include submitted reviews as `reviewed` events. |
| 2304 | P3 | bleephub — webhooks/environments | repo hook deliveries unpaginated + missing branch_policy rule | `GET /repos/{o}/{r}/hooks/{id}/deliveries` returned everything with no Link header while the org/app variants paginate; `environmentToJSON` omitted the `branch_policy`-type protection rule real GitHub adds when a deployment branch policy is configured. Both fixed. |
| 2303 | P3 | bleephub — emoji image assets | advertised URL unserved | `GET /api/v3/emojis` advertised `<base>/images/icons/emoji/…png?v8` URLs (the GHES instance-host shape) but nothing served those paths. `GET /images/icons/emoji/{path...}` now serves every advertised image from an embedded archive (`emoji_assets.tar.gz`): Twemoji v17.0.3 72×72 rasters (jdecked/twemoji, CC-BY 4.0, attribution inside the archive) for the 1870 unicode paths — gemoji↔Twemoji filename conventions reconciled by stripping leading zeros and fe0f/200d tokens — and deterministic bleephub-original badge artwork (`internal/emojiart`) for the 23 GitHub-custom names whose GitHub originals are not redistributable. Archive rebuilt reproducibly by `internal/emojigen` (go:generate). Tests: `TestEmojiCatalogImagesAllServed` (full-catalog enumeration, 200 + image/png + 72×72 decode), `TestEmojiImageUnknownPath404`, `TestCustomEmojiArtworkMatchesGenerator`, `TestBadgePNGDeterministic`. |
| 2302 | P2 | bleephub — Projects v2 store | views never persisted + reload ordering | `CreateView` had no persistence write, so Projects v2 views vanished on restart; reloaded per-project field/view/item slices kept map-iteration order and iteration IDs did not resume the shared option-ID seed. Views now persist in a `project_v2_views` bucket, reload re-sorts by ID, and the seed resumes past iteration IDs. Test: `TestArtifactMetadataAndAttestationPersistenceReload`. |
| 2301 | P2 | bleephub — Marketplace accounts | fabricated purchase for any account | `handleMarketplaceAccount` synthesized a "Free plan" purchase for every account id and omitted required `url`/`login` members; `handleMarketplacePlans` emitted a thin plan shape missing `url`/`accounts_url`/`number`/`unit_name`. Both now serve real store-backed purchase state (404 when none) in the full documented shapes. |
| 2300 | P1 | bleephub — PUT /repos/{o}/{r}/contents | nonexistent branch silently advanced the default branch | `handlePutContents` routed a write to an unknown branch on a non-empty repository through the empty-repo init path, committing to and advancing the current branch instead. Now answers 404 "Branch not found" like real GitHub. |
| 2299 | P2 | bleephub — PATCH /repos/{o}/{r}/issues/{number} | milestone/labels/assignees silently dropped | `handleUpdateIssue` ignored the documented `milestone`, `labels`, and `assignees` request members. Now resolves the milestone number (422 on unknown, null clears), labels (strings or `{name}` objects), and assignees, validated before mutation. Test: `TestUpdateIssueMilestoneLabelsAssigneesREST`. |
| 2298 | P1 | bleephub — packages persistence | linkage fields dropped → packages lost on restart | `Package`/`PackageVersion`/`PackageFile` persisted with `json:"-"` on owner/package-id/version-id/storage-path, so reloaded rows lost their linkage, and package files were never written to their bucket at all. All linkage now round-trips; files persist. |
| 2297 | P2 | bleephub — codespaces store | metadata not persisted | Codespaces and user codespace secrets (+ selected-repo sets) were in-memory only while the rest of the metadata surface persists. Now persisted in `codespaces`/`codespace_secrets` buckets with write-through at every mutation and honest Docker-derived state on reload (a lost container reports `Unavailable`). Test: `TestPersistenceReload_CodespacesAndSecrets`. |
| 2296 | P2 | bleephub — /users/{username}/events | fabricated event ids/timestamps at render time | `ListUserEvents`/`eventToJSON` minted `time.Now()` timestamps and synthetic ids per request. All user/global/org event feeds now share the org feed's derivation from stored issues/PRs/comments (stable ids, recorded timestamps); the synthetic path is deleted. Test: `TestUserExtras_Events` asserts identical ids and `created_at` across requests. |
| 2295 | P2 | bleephub — code scanning default setup | hardcoded "configured" state + PATCH discarded body | `handleGetCodeScanningDefaultSetup` returned a fixed `configured` payload regardless of state and the PATCH handler dropped its request (`_ = req`). Now real per-repo state: languages derived from actual git content via Linguist detection mapped to CodeQL languages, spec-validated PATCH (422/409 semantics), persisted round-trip. Tests: `TestCodeScanning_DefaultSetup*`, `TestPersistenceReload_CodeScanningDefaultSetup`. |
| 2294 | P2 | bleephub — Store.ListOrgsByUser | unstable order under offset pagination | The membership map was iterated in random order while `/user/orgs` paginates over the result, so pages could skip or duplicate organizations across requests. Now sorted ascending by org id like real GitHub. Several first-page-only test assertions that masked this were rewritten to walk pagination like real clients. |
| 2293 | P2 | bleephub UI | org context unreachable + orphaned routes + impossible action | Org logins rendered as plain text (the entire org context — org repos, rulesets, org packages — had no inbound link); repo Secrets settings, repo Codespaces, repo Packages, and the Workflows list were routed but unreachable; the package-version Restore button was offered for repository scope, where no restore endpoint exists on GitHub or bleephub. All linked/gated; dead code pruned. |
| 2292 | P1 | bleephub — GitHub REST surface | 542 of 1190 vendored operations unimplemented | The `GET /api/v3/` catch-all masked every missing GET, so route-table diffs against the vendored OpenAPI description undercounted the gap. A mux-routability probe found 542 truly missing operations, implemented across 12 work packages (enterprises, Copilot + Spaces, coding-agent secrets/variables/tasks, code scanning autofix/CodeQL, Actions hosted runners/permissions/concurrency/run controls, Projects v2 REST, attestations, artifact metadata, user surface, legacy teams, org people/roles, code security configurations, custom properties, issue types/fields, campaigns, private registries, network configurations, immutable releases, billing budgets/usage, API insights, PAT admin, org events, pages deployments, environment policies, branch protection extras, template generate, repo import, sub-issues, issue dependencies, dependency graph SBOM, repo reads/stats/traffic/archives, markdown, emojis, zen/octocat, codes of conduct, global advisories, classroom, search commits/labels/topics, marketplace). Registered surface now covers all 1190 spec operations; every response validated by the OpenAPI shape ratchet with zero allowlist additions. |
| 2291 | P2 | Azure sim — Container Apps PATCH | wholesale Configuration/Template replacement instead of merge | The PATCH handler replaced Configuration and Template wholesale, so a partial PATCH body (e.g. only `template.containers`) wiped secrets, ingress, volumes, and scale. Fixed by merging sub-fields individually via `patchContainerAppConfig` and `patchContainerAppTemplate`. Tags now merge key-by-key instead of replacing the whole map. SDK test: `TestSDK_ContainerAppsApps_PatchMergesNotReplaces`. |
| 2290 | P2 | Azure sim — Container Apps DELETE | wrong status code + missing LRO headers | DELETE returned 200 OK with no headers. Real Azure ARM returns 202 Accepted with `Azure-AsyncOperation` and `Location` headers. Fixed to match the PUT handler's LRO envelope. Existing `TestSDK_ContainerAppsApps_CreateGetDelete` confirms the SDK poller still completes. |
| 2289 | P3 | AWS sim — ECS RunTask | `startedBy` dropped on the wire | `handleECSRunTask` did not parse `startedBy` from the request body, so the field was always empty on stored tasks. Fixed by adding the field to the request struct and passing it through `ecsRunTaskInput`. `ListTasks` filtering by `startedBy` now works. SDK test: `TestECS_ListTasks_StartedByAndServiceFilters`. |
| 2288 | P3 | AWS sim — ECS ListTasks | missing `serviceName` and `startedBy` filters | `handleECSListTasks` only filtered by `cluster`, `family`, `desiredStatus`, and `launchType`. Real AWS also supports `serviceName` and `startedBy`. Fixed by adding both to the filter predicate (`serviceName` matches `group` prefixed `service:`). SDK test: `TestECS_ListTasks_StartedByAndServiceFilters`. |
| 2287 | P3 | AWS sim — Lambda ListFunctions | `FunctionVersion=ALL` ignored | `handleLambdaListFunctions` always returned `$LATEST` only, ignoring the `FunctionVersion` query parameter. Real AWS returns all published versions when `FunctionVersion=ALL`. Fixed by expanding the result to include published versions with version-qualified ARNs. SDK test: `TestLambda_ListFunctions_FunctionVersionAll`. |
| 2290a | P2 | GCP sim — Cloud Run v2 PATCH | sub-path updateMask swapped whole template | `updateMask=template.containers` replaced the entire `RevisionTemplate` instead of merging only `Containers`, wiping `Volumes`, `Scaling`, `VpcAccess`, and `Timeout`. Fixed by distinguishing exact `template` mask (full swap) from sub-path masks (per-field merge via `mergeRevisionTemplate`). SDK test: `TestCloudRun_UpdateServiceSubPathMask`. |
| 2286 | P3 | AWS sim — ECS DescribeTasks | `Include=[TAGS]` ignored — tags always present | `handleECSDescribeTasks` did not parse `Include`, so task `tags` always appeared regardless of the request. Real AWS surfaces tags only when `Include=[TAGS]`. Fixed by adding `includeTags` to `ecsTaskWire` and gating the field. SDK test: `TestECS_DescribeTasksTagsIncludePath`. |
| 2285 | P2 | AWS sim — SQS ReceiveMessage | empty `Messages` array with no queue-state diagnostics | Downstream probes reported SNS→SQS delivery logs showed success but `ReceiveMessage` returned empty for 30 seconds, with no visibility into queue depth or message visibility. Added Debug-level logging in `sqsEnqueueBody` (queue name, post-enqueue message count) and `handleSQSReceiveMessage` (queue name, total/visible/picked/redrived counts, visibility timeout). Added `TestCloudWatchCLI_AlarmSNSActionToSQS_ProcessMode_PollLoop` to mirror the downstream repeated-poll probe. Closes GitHub issue #766. |
| ~~1785~~ | P2 | GCP sim — Cloud Build push→pull | faithful build→push already shipped in PR #755 | `simulators/gcp/cloudbuild.go` already performs a real `docker push` + `docker rmi` (no shortcut); `SOCKERLESS_GCP_AR_ENDPOINT` is wired into `backends/gcp-common/` (overlay_registry.go, image_resolve.go, ar_auth.go); `simulators/gcp/sdk-tests/cloudbuild_push_test.go` proves the faithful push→pull round-trip with a `registry:2` stand-in; Cloud Run and Cloud Run Functions integration tests set the coordinate and push through the sim `/v2/`. Closed by PR #755; no remaining code gap. |
| 2284 | P2 | bleephub — POST /api/v3/orgs/{org}/teams | creator not auto-added as team maintainer | Real GitHub's Create Team API makes the authenticated creator a maintainer automatically. bleephub's `handleCreateTeam` only added users from the optional `maintainers` array, so downstream OAuth web-flow tests that created a team and then called `/user/teams` saw an empty list. Fixed by calling `SetTeamMembership(..., user.ID, TeamRoleMaintainer)` for the authenticated creator after team creation. Added `TestListAuthUserTeams_ViaOAuthWebFlow_ReadOrgScope` to lock in the `read:org` OAuth path. Closes GitHub issues #763 and #765. |
| 2283 | P2 | bleephub — GET /api/v3/user/teams | missing X-OAuth-Scopes for OAuth tokens + no OAuth web-flow test coverage | After #756 removed the `read:org` scope gate, downstream Auth.js probes reported `/user/teams` returning an empty list. Investigation showed the endpoint returns the correct teams when the OAuth web flow authenticates the expected user. Fixed the related fidelity gap where `ghHeadersMiddleware` did not emit `X-OAuth-Scopes` for `gho_`/`ghu_` user-to-server tokens (real GitHub emits it), and added `TestListAuthUserTeams_ViaOAuthWebFlow` to lock in the full authorize→access_token→/user/teams path. Added a debug log recording the authenticated user and team count. If the empty list persists, the log will show whether the wrong user was authenticated or the team membership was not seeded. Closes GitHub issue #763. |
| 2282 | P2 | AWS sim — CloudWatch→SNS→SQS internal dispatch | opaque fan-out path hides why delivery fails | After #761 made the evaluator atomic, downstream probes still saw no SQS message even though the evaluator logged "dispatching actions". The SNS fan-out path silently did nothing when a topic had no subscriptions, and silently skipped deliveries denied by resource policy or missing queues. Fixed by adding Info-level logging at every fan-out decision point: subscription count, matching subscriptions, each delivery attempt, policy-denied skips, missing-target skips, and successful SQS enqueue / Lambda invoke. Added `TestCloudWatch_AlarmSNSActionToSQS_NoSubscription` and `TestCloudWatch_AlarmSNSActionToSQS_PolicyDenied` to lock in the failure modes. Closes GitHub issue #762. |
| 2281 | P2 | AWS sim — CloudWatch alarm evaluator | race between evaluator tick and PutMetricAlarm replacement | `cwEvaluateAlarmsOnce` evaluated alarm state from a `cwAlarms.List()` snapshot, then dispatched actions and wrote the new state back via a separate `Update`. A concurrent `PutMetricAlarm` could replace the alarm record between the snapshot and the write, causing a freshly-created alarm to inherit the old `StateValue` (e.g. `ALARM`) and skip dispatching `AlarmActions`. Fixed by moving the state read, dispatch, and state write into a single `cwAlarms.Update` callback and deriving `newState` from the live alarm record inside that callback. Added `TestCloudWatch_AlarmSNSActionToSQS_AfterDeleteAndRecreate`. Closes GitHub issue #760 (the same race also manifested as #758). |
| 2280 | P3 | tests — eval-arithmetic image build | CI-caught test infra | `tests/main_test.go` built the `linux/arm64` `sockerless-eval-arithmetic:test` image with `docker build`, but the BuildKit `docker-container` driver leaves the result in the build cache unless `--load` is passed. CI `test (core)` and local runs then failed with `No such image` because the test backend could not resolve the locally-built tag. Fixed by adding `--load` to the `docker build` invocation so the image is loaded into the local store. |
| 2279 | P2 | bleephub — GET /api/v3/user/teams | permission regression | Parity tranche #750 wrapped `GET /api/v3/user/teams` in `requirePerm(scopeMembers, permRead)`, so OAuth tokens without `read:org` (e.g., Auth.js defaults) received 403. Real GitHub does not require `read:org` for this endpoint because it returns the authenticated user's own teams. Fixed by removing the permission gate from that route only; other team routes keep their gates. Regression test `TestListAuthUserTeams_RequiresAuthNotReadOrgScope` verifies a `repo`-only OAuth token can list the user's teams. |
| 2278 | P2 | AWS sim — CloudWatch alarm evaluator | background goroutine death | After #751 reset `cwAlarmLastState` on `PutMetricAlarm`, integrated terraform-sim probes still saw no `SNS.Publish` even though `DescribeAlarms` reported `ALARM`. The evaluator tracked last-dispatched state in a standalone `sync.Map` that had to be manually reset on every `PutMetricAlarm` path; a panic while evaluating/dispatching one alarm could also kill the background goroutine and silently break all subsequent alarm actions. Fixed by moving the last-dispatched state onto each alarm's own `StateValue` field (so `PutMetricAlarm` replacement naturally resets it) and adding per-alarm panic recovery in the evaluator loop. Added `TestCloudWatch_AlarmSNSActionToSQS_ResilientToOneBadAlarm`. |
| 2277 | P2 | AWS sim — CloudWatch metric alarms | stale state across alarm re-creation | `PutMetricAlarm` did not reset `cwAlarmLastState`, so re-creating an alarm that had previously reached `ALARM` inherited the old remembered state and skipped dispatching `AlarmActions` on the next breach. Fixed by deleting the remembered state in all three `PutMetricAlarm` handlers (awsJson1.0, rpc-v2-cbor, awsQuery). Regression test `TestCloudWatch_AlarmSNSActionToSQS_RecreatedAlarmResetsState` verifies the second `ALARM` transition still delivers to SQS. |
| ~~2276~~ | P3 | bleephub OpenAPI ratchet | response-shape mismatch on user events and social accounts | Restoring `gh_misc_endpoints_users.go` exposed user-events and social-accounts responses to the OpenAPI shape validator. `GET /users/{}/events` reported unknown `node_id`/`html_url`/`type`/`site_admin` on the event actor, and `GET /users/{}/social_accounts` reported unknown `id` on account objects. Fixed by slimming `eventActorToJSON` to the GitHub event-actor schema (`login`, `id`, `avatar_url`, `gravatar_id`, `url`) and removing the synthetic `id` field from `SetUserSocialAccounts`; `handleCreateMySocialAccounts` now reads the request body once and supports both URL arrays and `{url}` objects. |
| ~~2275~~ | P3 | bleephub tests | non-hermetic assertion in shared-server test | `TestRepoStargazersREST` asserted `len(user/starred) == 1`, but the shared TestMain server inherits stars from earlier tests (e.g., `TestUserExtras_StarredRepo`). The assertion failed whenever another test had starred a repo that still exists. Fixed by asserting the target repo `admin/stargazers-rest` is present in the starred list rather than requiring a singleton list. |
| ~~2274~~ | P3 | bleephub API-definition ratchet | invented /api/v3 paths for ProjectsV2 REST extensions | The new ProjectsV2 REST convenience endpoints (`/orgs/{}/projectsV2/*` and `/users/{}/projectsV2/*`) are GraphQL-only in real GitHub; registering them under `/api/v3` violated the rule that every `/api/v3` path must be a real GitHub endpoint. Fixed by removing the invented ProjectsV2 REST routes, the `allowedBleephubExtensions` escape hatch, and their OpenAPI allowlist entries. The underlying GraphQL ProjectV2 surface remains available at `/api/graphql`. |
| ~~2273~~ | P3 | bleephub OpenAPI ratchet | response-shape mismatch on issue events / timeline | `TestOpenAPIResponseShape` reported 9 violations on `GET /repos/{}/{}/issues/{}/events` (missing `performed_via_github_app`, unknown `label`/`milestone`) and `GET /repos/{}/{}/issues/{}/timeline` (missing `actor`/`author_association`/`performed_via_github_app`, unknown `assignee`/`assigner`/`milestone`). Fixed by adding event-specific converters (`issueEventForIssueToJSON`, `issueEventForTimelineToJSON`, slim label/milestone shapes) and including `actor`, `author_association`, and `performed_via_github_app` in timeline comments. |
| ~~2272~~ | P3 | bleephub OpenAPI ratchet | response-shape mismatch on branch rename / repo transfer | `TestOpenAPIResponseShape` reported 15 violations for `POST /repos/{}/{}/branches/{}/rename` (minimal response with `url`/`job_url` instead of a full branch object) and `POST /repos/{}/{}/transfer` (full repository emitted extra fields beyond the `minimal-repository` schema). Fixed by returning a canonical `branch-with-protection` shape from `handleRenameBranch` (name, commit, `_links`, protected/protection/protection_url) and a `minimal-repository` shape from `handleTransferRepo`. Updated `gh_repos_test.go` assertions accordingly. |
| ~~2271~~ | P2 | AWS sim — CloudWatch alarm history | racy Update-then-Put fallback storage | `cwRecordAlarmHistory` in `simulators/aws/cloudwatch_alarm_ops.go` read an alarm-history row with `Update`, mutated it, and wrote it back with `Put`. Concurrent evaluator ticks and API calls could race and drop history entries. Fixed by replacing the read-modify-write sequence with atomic `cwAlarmHistory.Upsert`. |
| ~~2270~~ | P2 | AWS sim — CloudWatch metrics | racy Update-then-Put fallback storage | `handleCWQueryPutMetricData` in `simulators/aws/cloudwatch_metrics_query.go` read a metric row with `Update`, appended a datapoint, and wrote it back with `Put`. Under concurrent `PutMetricData` calls and alarm evaluator reads this could drop datapoints. Fixed by replacing the read-modify-write sequence with atomic `cwMetrics.Upsert`. |
| ~~2269~~ | P2 | AWS sim — SNS | malformed alarm→SNS→SQS JSON | `snsNotificationEnvelope` in `simulators/aws/sns.go` built the message body with `fmt.Sprintf("...%q...")`, which produced invalid JSON for control characters and omitted `Timestamp`. Fixed by marshaling the envelope with `json.Marshal` and including `Timestamp`. Unit test in `simulators/aws/sns_envelope_test.go` asserts valid JSON for payloads containing newlines and quotes. |
| ~~2268~~ | P2 | AWS sim — KMS | synthetic / deterministic encryption | KMS `Encrypt`/`Decrypt`/`GenerateDataKey`/`GenerateDataKeyWithoutPlaintext`/`ReEncrypt` used a synthetic envelope with no per-key secret; `ImportKeyMaterial` accepted arbitrary bytes. New `simulators/aws/kms_crypto.go` implements real AES-256-GCM with random 32-byte key material per key, an authenticated opaque blob, and RSA-OAEP unwrapping of imported key material. Key policies are mirrored into `iam_resource_policies` so IAM `Deny` statements are enforced at call time. SDK test in `simulators/aws/sdk-tests/kms_policy_test.go` verifies a `Deny` policy rejects `Encrypt`. |
| ~~2267~~ | P2 | AWS sim — Route 53 DNS | wildcard record lookup failure | `simulators/aws/route53_dns.go` only matched exact labels, so wildcard records such as `*.example.com` never resolved. Fixed by adding `findWildcardMatch` with exact-match precedence over wildcard expansion. SDK tests in `simulators/aws/sdk-tests/route53_dns_test.go` cover wildcard A records and exact-match precedence. |
| ~~2266~~ | P3 | AWS sim CLI tests — `TestEC2CLI_TGWMeteringPolicy` | skip-if-absent / CLI version drift | `TestEC2CLI_TGWMeteringPolicy` failed because the host `aws` CLI (2.26.6 locally, and possibly CI images) did not recognize `create-transit-gateway-metering-policy`; the command printed the CLI help/invalid-choice banner and exited 252. Rather than leave the suite dependent on whatever CLI version happens to be installed, `simulators/aws/cli-tests/helpers_test.go` now installs the latest AWS CLI v2 into a temporary directory in `TestMain` and prepends it to `PATH`. This satisfies the no-skip-if-absent rule: the test suite controls its own reference adaptor version. The shared `runCLI` helper still detects the "Invalid choice" banner and skips the individual test with a clear message naming the installed CLI version, so a genuinely unsupported operation surfaces explicitly instead of failing cryptically. `TestEC2CLI_TGWMeteringPolicy` now passes against the installed latest CLI. |
| ~~2261~~ | P2 | bleephub GraphQL | panic on malformed variable type | `FuzzGraphQLWithVariables` found that `query($A:){A}` (empty variable type) panics inside `github.com/graphql-go/graphql` validation with a nil-pointer dereference in `typeFromAST`/`TypeInfo.Enter`. The upstream library does not guard against an empty `ast.Named` name, and the panic escapes `graphql.Do`, crashing the request handler. Fixed in `bleephub/gh_graphql.go` by adding `graphqlValidateNoPanic`, which parses and validates the query AST before `graphql.Do` and recovers from any validation panic, returning a GraphQL-formatted `errors[]` envelope with HTTP 200. The crash input is preserved as a regression seed in `bleephub/testdata/fuzz/FuzzGraphQLWithVariables/897d11eedb038f37`. |
| ~~2263~~ | P2 | AWS sim — CloudWatch Logs | PutMetricFilter/PutSubscriptionFilter stored FilterPattern without validation | `handleCWPutMetricFilter` and `handleCWPutSubscriptionFilter` in `simulators/aws/cloudwatch_logs_ops.go` stored `FilterPattern` without validating it, so invalid patterns (e.g. `{`) were accepted and only failed later when evaluated. Fixed by calling `cwCompileLogPattern` in both handlers and returning `InvalidParameterException` for malformed patterns, matching the CloudWatch Logs Smithy model. Also fixed `cwCompileLogPattern` in `simulators/aws/cloudwatch_filter_pattern.go` so that an unbalanced `{` is rejected as a malformed structured pattern instead of being treated as an unstructured term. SDK tests in `simulators/aws/sdk-tests/cloudwatch_logs_failloud_test.go` and CLI tests in `simulators/aws/cli-tests/cloudwatch_logs_ops_test.go` assert both operations reject `{` and still accept valid patterns. |
| ~~2262~~ | P2 | AWS sim — EC2 | RevokeSecurityGroupIngress/Egress succeeded for non-existent rules | The AWS SDK for Go v2 doc for `RevokeSecurityGroupIngress` states that for a non-default VPC, a request whose values do not match an existing rule returns `InvalidPermission.NotFound` and no rules are revoked. The sim's `handleRevokeSecurityGroupIngress` and `handleRevokeSecurityGroupEgress` in `simulators/aws/ec2.go` unconditionally removed the permission and returned success. Fixed by checking `ec2PermissionExists` before mutating the security group and returning `InvalidPermission.NotFound` when the rule does not exist. SDK tests in `simulators/aws/sdk-tests/ec2_networking_coverage_test.go` and CLI tests in `simulators/aws/cli-tests/ec2_networking_coverage_test.go` assert both ingress and egress re-revoke fail with the correct code. |
| ~~2264~~ | P2 | AWS sim — EC2 | VPC security groups created without the default ALLOW ALL egress rule | Real AWS creates every VPC security group with a default egress rule that allows all IPv4 traffic (`IpProtocol=-1`, `CidrIp=0.0.0.0/0`). The sim created VPC security groups with no egress rules, so `terraform-provider-aws` failed when it revokes the default rule before applying user rules. Fixed in `simulators/aws/ec2.go` by initializing `IpPermissionsEgress` with the default rule in `handleCreateSecurityGroup` when `VpcId` is present. Existing SDK tests (`TestEC2_SecurityGroupRuleFidelity`, `TestEC2_DescribeSecurityGroupRulesFilters`, `TestEC2_SecurityGroupEgressIpv6Ranges`) and the revoke-not-found SDK/CLI tests were updated to revoke the default rule first, matching the real AWS workflow; `TestStackProductionShape` now applies cleanly. |
| ~~2260~~ | P3 | Azure sim SDK test — `TestCosmosScripts_DifferentialVsEmulator` flaky with `Post "http://127.0.0.1:8081/dbs": EOF` | flaky test / emulator transient error | `sim (azure)` failed on the post-#720 continuity PR (#721) with EOF from the Cosmos vNext emulator while creating a database in the `sproc-missing-404` subtest. The emulator passed readiness and the first two scenarios ran, then the data-plane connection was reset. Fixed by adding a transient-error retry loop to `cosmosRESTReq` in `simulators/azure/sdk-tests/cosmos_scripts_differential_test.go` for `io.EOF`, `io.ErrUnexpectedEOF`, and temporary/timeout `net.Error`, matching the resilience the official `azcosmos` SDK already applies for the SDK-based differential test. |
| ~~2259~~ | P2 | bleephub — REST PR `head.user`/`base.user` used GraphQL camelCase field names instead of REST snake_case | wire-shape fidelity | `pullRequestSimpleJSON` assigned the `repoOwnerGraphQL` map (camelCase `nodeID`, `databaseId`, `avatarUrl`, `createdAt`, `updatedAt`) to `head.user` and `base.user`. The vendored GitHub OpenAPI description expects the REST `simple-user` shape (`node_id`, `avatar_url`, `created_at`, `updated_at`, etc.), causing response-shape violations. Fixed by introducing `repoOwnerREST`, which resolves the owner from `repo.FullName` (organization-aware) and emits snake_case keys; updated `TestCreatePullRequestREST` to assert the REST shape. |
| ~~2258~~ | P2 | bleephub — GraphQL `PullRequest.headRepositoryOwner` returned User-shaped data for org-owned repos | fake data / type mismatch | `prToGraphQLLocked` built `headRepositoryOwner` with `userToGraphQL(repo.Owner)`. For org-owned repos `repo.Owner` is the creating user, so the field reported a personal login instead of the org, and the returned shape was `User` rather than the `Organization` type real GitHub uses. This broke `gh pr list`/`view` when the repo is under an organization because `headRepositoryOwner.login` did not match the repository owner. Fixed by deriving the owner from `repo.FullName`, looking up the org when present, and converting via `orgToGraphQL` (User-shaped fields); otherwise keeping `userToGraphQL`. Added `TestPRGraphQL_OrgOwnedHeadRepositoryOwner`. |
| ~~2256~~ | P2 | bleephub — runner broker lacked `AgentRefreshMessage` delivery | missing surface / runner auto-update | Real GitHub pushes an `AgentRefreshMessage` to a runner when a newer package is available, but bleephub's V1 broker only dispatched `PipelineAgentJobRequest` and `JobCancellation`. Added `sendAgentRefreshMessage` in `broker.go` and a sim-control endpoint `POST /internal/agents/{agent_id}/refresh-message` (site-admin gated) that delivers the message to every open session for the target agent with the real `{agentId, targetVersion, timeout}` body shape the actions/runner self-updater expects. Added `broker_refresh_test.go` covering delivery, admin-only auth, and missing-agent 404. |
| ~~2255~~ | P2 | AWS sim — Budgets Terraform lifecycle gaps (#714) | fidelity / terraform idempotency | The AWS Budgets service slice from #713 worked via CLI/SDK, but `aws_budgets_budget` failed in Terraform: with `skip_requesting_account_id = true` the provider omits `AccountId` and `CreateBudget` returned `InvalidParameterException` instead of deriving the account from caller credentials; with account-id lookup enabled the provider called `ListTagsForResource` and got `UnknownOperationException`. Fixed by deriving `AccountId` from `awsAccountID()` in all Budgets envelope handlers when omitted, and implementing `ListTagsForResource`/`TagResource`/`UntagResource` keyed by budget ARN. Added the `budgets` endpoint alias and a budget resource to the terraform-tests production-shape stack; SDK tests cover tags and the implicit-account raw-HTTP path. |
| ~~2254~~ | P2 | AWS sim — EC2 SG host-firewall denies all traffic when no SG is attached (CI-caught) | fidelity / host enforcement | #712's `ec2ApplyRealECSTaskSecurityGroups` wired the real-exec ingress filter, but it built and applied an empty ruleset when a task/ENI had no explicit security groups. The realexec layer ends every filter chain with a `drop` rule, so an empty ruleset silently dropped **all** inbound traffic to awsvpc tasks on Linux + CAP_NET_ADMIN hosts, breaking `TestECSVPCNetworking`, `TestECSManagedEBSAwsvpcReachability`, and `TestECSVPCOverlappingCIDR` in CI. Fixed by treating an empty SG list as default-allow: `ec2ApplyRealECSTaskSecurityGroups` now calls `ClearIngressFilter`, and `ec2ApplyRealNICSecurityGroups` clears both namespace and tap NIC filters when no SGs are attached. A non-empty SG list with no permissions still installs a deny-all ruleset, matching AWS behavior. |
| ~~2253~~ | P2 | AWS sim — ECS service scheduler DescribeServices RunningCount races task status (CI-caught) | flaky test / consistency | `TestECS_ServiceScheduler_ReconcilesDesiredCount` failed in CI because `DescribeServices.RunningCount` is refreshed by the scheduler's 2s reconciliation tick, while `DescribeTasks.LastStatus` transitions to `RUNNING` asynchronously in the task-lifecycle goroutine. A caller that polls `DescribeTasks` until two tasks are `RUNNING` can then immediately call `DescribeServices` and see `RunningCount=0`. Fixed by deriving `RunningCount`/`PendingCount` from the live task set inside `handleECSDescribeServices` (via `ecsServiceWithLiveCounts`) so the response is always consistent with `DescribeTasks`; the scheduler still refreshes the cached counts on its tick for CreateService/UpdateService responses. |
| ~~2251~~ | P2 | AWS sim — EC2 security-group host-firewall enforcement (#712) | stored-but-not-enforced | `AuthorizeSecurityGroupIngress`/`Egress` validated and stored rules, but on Linux + CAP_NET_ADMIN hosts the rules were never applied to the task namespace's nftables ingress chain. Added `ec2ApplyRealECSTaskSecurityGroups`/`ec2ReapplyRealSecurityGroup` plus `ec2BuildIngressPacketRules` (expands CIDRs, IPv6 ranges, and SG-reference pairs into concrete packet rules) and wired them into the real-exec ECS task NIC attach path. The metadata-only tier (validation, storage, SG-reference resolution, Describe round-trips) is covered by a cross-platform SDK test; the nftables application tier is covered by a `realexec_host` Linux-only unit test that verifies the ruleset contains the expected dport/CIDR entries. |
| ~~2250~~ | P2 | AWS sim — ECS service scheduler (#711) | stored-but-not-enforced | `CreateService` stored desired count but never launched or reconciled tasks, so `DescribeServices` always reported `runningCount=0` regardless of actual tasks. Added a background scheduler (`ecs_service_scheduler.go`) that ticks every 2s, aligns `RUNNING`/`PENDING` counts with the live task set, launches replacements for stopped tasks, scales up/down on `DesiredCount` changes, and drains tasks on service deletion. SDK test verifies desired-count convergence through the real ECS client; unit tests verify count derivation, scale-down, INACTIVE skip, and launch metadata (Group/StartedBy/TaskDefinition). |
| ~~2249~~ | P2 | AWS sim — ELBv2 HTTPS/TLS listener termination (#709) | stored-but-not-enforced | `CreateListener` with `Protocol=HTTPS`/`TLS` accepted a certificate ARN but terminated TLS nowhere; the listener was reachable only as plain TCP. Added `elbv2_tls_proxy.go`: a real TLS listener using the ACM PEM, SNI matching the listener's certificates, and ALPN-aware forwarding to the target group. Terminates TLS for HTTPS (HTTP/1.1 inside TLS) and TLS (raw TLS proxy) listeners. SDK test creates an ACM cert, an ALB with an HTTPS listener, and asserts HTTPS requests succeed and present the cert; a TCP TLS listener test asserts raw TLS handshake + bytes round-trip. |
| ~~2248~~ | P2 | AWS sim — Route 53 real DNS server (#710) | stored-but-not-enforced | `ChangeResourceRecordSets` stored records but did not serve them over the DNS protocol, so `dig`/`nslookup` against the zone returned nothing. Added `route53_dns.go`: a UDP+TCP DNS server on a configurable port (`SIM_DNS_PORT`) that answers A/AAAA/CNAME/NS/SOA/MX/TXT/SRV/PTR queries from the hosted-zone stores, including wildcard expansion, alias resolution, and apex NS/SOA synthesis. SDK test sends real DNS queries via `miekg/dns` and asserts A/AAAA/CNAME/TXT round-trips; Terraform idempotency stack unchanged. |
| ~~2247~~ | P2 | AWS sim — ACM `AMAZON_ISSUED` certificates mint real PEM (#708) | fake data | `RequestCertificate` returned a certificate ARN with no actual PEM, so `DescribeCertificate`/`GetCertificate` served placeholder or empty data and downstream consumers (ELBv2 HTTPS listeners) had nothing to terminate TLS with. Added real RSA key generation + self-signed X.509 PEM minting in `acm.go`: the cert carries a random 128-bit serial, 13-month validity, SANs from the request, and RSA 2048. `GetCertificate` returns the PEM + matching private key (real AWS only returns cert+chain, but the sim needs the key for its own TLS termination). SDK test asserts the PEM parses, the SANs match, and the validity window is present. |
| ~~2246~~ | P2 | AWS sim — Application Auto Scaling target tracking scales ECS services (#707) | stored-but-not-enforced | `RegisterScalableTarget`/`PutScalingPolicy` stored target-tracking policies but never evaluated them, so ECS service `DesiredCount` never changed in response to CloudWatch metrics. Added `application_autoscaling_eval.go`: a background evaluator that reads target-tracking policies, fetches the metric from CloudWatch, computes the adjustment using the same `TargetValue`/`*AdjustmentType` math real AWS uses, and calls `UpdateService` to change `DesiredCount`. SDK test registers a scalable target + target-tracking policy, pumps a metric through `PutMetricData`, and polls until `DesiredCount` changes. |
| ~~2245~~ | P2 | AWS sim — CloudWatch alarm actions dispatch canonical alarm JSON to SNS (#705) | stored-but-not-enforced | `PutMetricAlarm` accepted `AlarmActions` containing SNS topic ARNs but never sent anything when the alarm transitioned to `ALARM`. Added `cloudwatch_alarm_evaluator.go`: a background evaluator that polls alarm metrics, transitions state (`OK`→`ALARM`→`OK`) with proper datapoint-count semantics, and publishes the canonical AWS alarm JSON payload to each SNS topic in `AlarmActions`. SDK test asserts the SNS topic receives a message whose body contains the alarm name, state, and reason. |
| ~~2244~~ | P2 | AWS sim — CloudWatch Logs metric filters publish real metrics (#706) | stored-but-not-enforced | `PutMetricFilter` stored filter patterns but never evaluated incoming log events, so `PutMetricData` was never called and dashboards/alarms saw no metric. Wired metric-filter evaluation into the CloudWatch Logs ingestion path (`cloudwatch_logs_ops.go`): on `PutLogEvents`, each metric filter compiles its pattern, matches incoming events, extracts the metric value (or counts events), and calls the shared `cwStoreDatum` path so the metric is queryable through `GetMetricData`. SDK test puts a filter, ingests matching events, and reads the metric back. |
| ~~2243~~ | P2 | AWS sim — SQS DLQ auto-redrive on `maxReceiveCount` (#704) | stored-but-not-enforced | SQS queues with a dead-letter queue configured via `RedrivePolicy` tracked `ApproximateReceiveCount` but never moved messages to the DLQ after `maxReceiveCount` failures. Added DLQ redrive logic in `sqs.go`: on each failed receipt (visibility timeout expiry or negative receipt), if the receive count exceeds `maxReceiveCount` the message is removed from the source queue and enqueued in the DLQ with a `DeadLetterQueueSourceArn` attribute. SDK test asserts messages land in the DLQ after the configured number of receives. |
| ~~2242~~ | P2 | AWS sim — Budgets service slice (#703) | missing service | The AWS simulator had no Budgets service, so `aws-sdk-go-v2/service/budgets` could not target the sim. Added `budgets.go` implementing `CreateBudget`/`DeleteBudget`/`DescribeBudget`/`DescribeBudgets`/`UpdateBudget`/`CreateNotification`/`DescribeNotificationsForBudget`/`DeleteNotification`/`CreateSubscriber`/`DescribeSubscribersForNotification`/`DeleteSubscriber` over awsJson1.1 with the real `AWSBudgetServiceGateway` X-Amz-Target prefix. Because Budgets is not in the vendored Smithy corpus, `spec_conformance_test.go` gained an `allowedNonSpecTargets` allowlist with justified entries. SDK test exercises CRUD through the real Budgets client. |
| ~~2240~~ | P2 | GCP sim — second gRPC round (Cloud KMS + Secret Manager) + Compute v1 control-plane tranche #2 | conformance / fidelity / gRPC-transport | Two more native gRPC data planes plus a Compute metadata tranche, bundled per user request. **Cloud KMS gRPC** (`cloud.google.com/go/kms/apiv1.KeyManagementClient` is gRPC-only): `google.cloud.kms.v1.KeyManagementService` admin surface (KeyRings/CryptoKeys/CryptoKeyVersions CRUD incl. Destroy/Restore/UpdatePrimary) AND data-plane Encrypt/Decrypt/AsymmetricSign/AsymmetricDecrypt/MacSign/MacVerify/RawEncrypt/RawDecrypt/GenerateRandomBytes — sharing REST `cloudkms.go` stores AND reusing its **real Go-stdlib crypto** (not synthetic). **Secret Manager gRPC** (`cloud.google.com/go/secretmanager/apiv1.Client` is gRPC-only): `google.cloud.secretmanager.v1.SecretManagerService` — Create/Get/List/Delete secrets + versions + AccessSecretVersion (real payload retrieval from `smSecretPayloads`)/Destroy/Disable/Enable, sharing `smSecrets`/`smSecretVersions`/`smSecretPayloads`, IAM triplet via `iampb`. **Compute v1 control-plane tranche #2** (`compute_more3.go`): commitments, regional targetTcpProxies, regional forwardingRules + setTarget/setLabels, global/regional/zonal networkEndpointGroups + membership verbs, global/regional firewallPolicies + rules/associations/IAM + aggregated, router update/aggregatedList/route-policy/BGP/NAT/preview verbs; the router store is shared with `registerCompute` via a new `gcpRouters` package variable. Coverage floor 864→1108/1994. Cloud Logging gRPC was investigated and found ALREADY implemented. Each slice ships SDK tests via real high-level clients. |
| ~~2239~~ | P2 | GCP sim — native gRPC data planes (Firestore/Pub/Sub/Spanner) + Compute v1 control-plane tranche | conformance / fidelity / gRPC-transport | Three gaps folded into one branch per user request. (1) **Native gRPC data planes**: the high-level Go SDK clients `firestore.NewClient`, `pubsub.NewClient`, `spanner.NewClient` are gRPC-only and previously could NOT target the sim at all (only the low-level REST `*Service` clients worked); extending the Bigtable gRPC transport pattern (#656) to three more services unlocked the canonical client surface. **Firestore** (`firestore_grpc.go`, ~980 lines): `google.firestore.v1.Firestore` sharing `fsDocuments` — Get/Create/Update(updateMask merge)/Delete(precondition)/ListDocuments(pagination+mask)/ListCollectionIds/BatchGetDocuments(streaming)/RunQuery(streaming, reuses the REST query evaluator)/RunAggregationQuery(real count/sum/avg)/BeginTransaction/Commit/Rollback/BatchWrite/PartitionQuery; Listen + Write streaming RPCs left Unimplemented with in-file justification (no REST analog; target-id/stream-token bookkeeping). **Pub/Sub** (`pubsub_grpc.go`, ~1300 lines): `google.pubsub.v1.Publisher`+`Subscriber`+`SchemaService` sharing `psTopics`/`psSubscriptions`/`psQueues`/`psInFlight` — Topic+Subscription CRUD, Publish (real fan-out to all subscription queues), Pull+StreamingPull (real dequeue + inflight tracking), Acknowledge, ModifyAckDeadline, Seek, a background ack-deadline sweeper goroutine (real at-least-once redelivery of expired-inflight messages back to the queue), Schema CRUD. Review caught + fixed **four name-normalization bugs** (CreateSchema/ListSchemas/ListTopics/ListSubscriptions/ListSnapshots built `projects/projects/...` double-prefixes from the full-path project/parent fields the gRPC client sends; routed through `psNormalizeProject`). **Spanner** (`spanner_grpc.go`, ~520 lines): `google.spanner.v1.Spanner` sharing `spannerSessions` — the defining ExecuteSql/Read RPCs need real table storage, which the sim has none of, so the slice materializes a per-database in-memory SQLite DB (`modernc.org/sqlite`, the same driver `sim.Store` uses) lazily on first session, reconciles `spannerDDLs` incrementally (CREATE TABLE type translation INT64→INTEGER, STRING(N)→TEXT, BYTES→BLOB, FLOAT64/NUMERIC→REAL, BOOL→INTEGER, ARRAY<…>→TEXT-as-JSON, PRIMARY KEY hoist, INTERLEAVE/ON DELETE drop), and ExecuteSql/ExecuteStreamingSql/Read/StreamingRead run REAL parameterized queries against it; BeginTransaction/Commit/Rollback map to SQLite txn lifecycle; Commit applies Insert/Update/InsertOrUpdate/Replace/Delete mutations; PartitionQuery/PartitionRead return single partition (one-process sim); ExecuteBatchDml/BatchWrite left Unimplemented (outside the high-level client's paths); composite-PK range reads return loud Unimplemented (not a wrong result). Tests via the real high-level clients reached through `FIRESTORE_EMULATOR_HOST`/`PUBSUB_EMULATOR_HOST`/`SPANNER_EMULATOR_HOST` (the same coordinates real clients use for the real emulators) — real CRUD + query + transaction + publish/pull/ack + ack-deadline redelivery + StreamingPull via Receive + INSERT/Read/Query/UPDATE/DELETE round-trips. (2) **Compute v1 control-plane tranche** (`compute_more2.go`, ~630 lines, isolated from `compute.go`/`compute_more.go`): ~30 metadata-only resource families (), the IAM-policy triplet (getIamPolicy/setIamPolicy/testIamPermissions — stored in a dedicated `compute_iam_policies` store keyed by resource path, no collision) on 10 resources, the `listAvailableFeatures` cipher catalog, and read-only nodeTypes/storagePoolTypes/networkProfiles/region-diskTypes catalogs. Compute v1 floor 440→864/1994. Review caught + fixed **one fidelity bug** in the nodeTypes catalog: hardcoded `guestCpus=96`/`memoryMb=655360` for every node type (real node types differ) — now real per-name specs for node types whose name encodes the spec (`<gen>-node-<vCPU>-<GB>` → vCPU + GB*1024 MB), verifiable against the GCP node-types documentation. (3) The gRPC slices are orthogonal to the REST Discovery coverage gate (`gcpMethodFloor` unchanged for them; only compute-v1 bumped). gcp build/lint(0)/vet clean; all _GRPC SDK test suites green (run twice, no flakiness); existing REST tests for the four services unaffected; TestServiceConformance (floor + route-validity + doc-consumption + spec-validator) green — 0 new spec violations. The REST remainders named in the BUG one-liner (bigquery 9, pubsub 6, sqladmin-v1beta4 2) were investigated and found to be `{+resource}`/`{+name}` template alternates at/near faithful ceiling (the flatPath forms are already covered) — honestly skipped, not counted. |
## Recently fixed (cont.)

| ID | Sev | Area | Pattern | One-liner |
|----|-----|------|---------|-----------|
| ~~2238~~ | P3 | GCP sim SDK test — TestCloudBuild_FaithfulBuildPush hung the whole sdk-test suite when the container runtime was unresponsive | reliability / test-infra | `TestCloudBuild_FaithfulBuildPush` and its setup helpers (`pullImageWithRetry`, `startThrowawayRegistry`) issued `docker pull`/`run`/`push` calls with **no per-operation deadline** (`http.DefaultClient` for the build POST; bare `exec.Command` for the setup calls), and the sim's `runDockerStep` ran `docker push` under the HTTP request's context (also unbounded). So a wedged container runtime (Podman-on-macOS in its `unable to upgrade to tcp, received 500` gvproxy state) or a stalled registry made a single `docker` call hang forever, consuming the entire 8–10m go-test budget and masking every other test's result. Surfaced as a boyscout finding while auditing the unrelated Bigtable gRPC data plane (BUG-2237). Fixed by bounding every `docker` invocation in the test through a new `dockerCLIWithTimeout(deadline, args…)` helper (180s pull / 60s run/rmi/inspect) and the synchronous build POST through a 120s `context.WithTimeout`. On a healthy runtime nothing changes (a real build+push completes in <15s); on a wedged runtime the test now fails in ~3min with a clear `signal: killed … start throwaway registry` / `context deadline exceeded` instead of an 8m hang. Pure robustness improvement; helps CI too (a slow registry push no longer monopolises the suite). The orthogonal localhost-HTTP-registry trust requirement on Podman (Docker auto-trusts 127.0.0.1; Podman needs an insecure-registries drop-in) stays documented in the test's own comment — that's a host-config requirement, not a code bug. |
| ~~2237~~ | P2 | GCP sim — Bigtable gRPC data plane (BUG-2159 / PR #656) only narrow-tested | conformance / surface-table-completeness | The Bigtable Data API v2 gRPC slice (`bigtable_data.go`, 740 lines) implements the full surface — 18 RowFilter types, all 4 mutation kinds (SetCell/DeleteFromColumn/DeleteFromFamily/DeleteFromRow), both ReadModifyWrite rules (IncrementAmount/AppendValue), the `MutateRows` batch path, and `SampleRowKeys` — but `sdk-tests/bigtable_data_test.go` exercised only 2 filters (`ColumnFilter`, `LatestNFilter`), 1.5 mutations (`Set`+`DeleteRow`), and `Increment`. The other 16 filters, `DeleteCellsInColumn`/`DeleteCellsInFamily`/`DeleteTimestampRange`, `AppendValue`, `ApplyBulk` (`MutateRows`), and `SampleRowKeys` had **zero** coverage (verified by grep across sdk-tests/cli-tests/terraform-tests); the CLI half of the contract was admin-only (`gcloud bigtable instances/tables create|list`) — the canonical data-plane CLI `cbt` was never invoked, so the data plane had no second-adaptor coverage at all. Same shape as the half-fixed surface-table issues: implemented-but-unexercised code silently rots. Fixed by enumerating every implemented surface in the SDK test — `TestBigtable_DataPlane_Filters` (18 subtests, one per filter, asserting real survival semantics: which cells a `ValueRangeFilter(["a1","a9")` keeps, that `CellsPerRowOffsetFilter(2)` slices the flattened order correctly, that `StripValueFilter` zeroes values, that `RowSampleFilter(1.0/0.0)` keeps/drops every row); `TestBigtable_DataPlane_Mutations` (DeleteCellsInColumn / DeleteCellsInFamily / DeleteTimestampRange); `TestBigtable_DataPlane_AppendValue` (the second ReadModifyWrite rule); `TestBigtable_DataPlane_ApplyBulk` (the `MutateRows` batch path with per-entry status, including a partial-failure case proving an unknown-family entry surfaces as a per-entry error while siblings stay OK); `TestBigtable_DataPlane_SampleRowKeys` (seeding 150 rows so the sampler's ~100-row cadence produces multiple samples). The CLI gap closed by `TestBigtableCLI_DataPlane_cbt` driving `cbt createtable`/`createfamily`/`set`/`lookup`/`read`/`count` over gRPC through `BIGTABLE_EMULATOR_HOST` (the same coordinate the real client uses for the real emulator) — `cbt` is built in TestMain via `go install cloud.google.com/go/bigtable/cmd/cbt@v1.13.0` so the test runs unconditionally (no skip-if-absent). The new tests surfaced one genuine test-side assumption bug (the SDK's `ValueRangeFilter(start,end)` is `[start,end)` — end-exclusive — so a `["a2","a3"]` range keeps only `a2`; the sim was already faithful, the original draft assertion was wrong) and confirmed everything else matches real Bigtable semantics. |
## Recently fixed (cont.)

| ID | Sev | Area | Pattern | One-liner |
|----|-----|------|---------|-----------|
| ~~2236~~ | P2 | realexec — `CreateNetworkNamespace` failed on a leaked orphan netns (CI-caught) | reliability / host-state leak | `sim (gcp)` failed in `TestGcloudComputeInstances_Lifecycle` with a 503 `ip netns add gn757b5096: File exists` (gcloud surfaces it as "Could not fetch resource"). The netns name is a deterministic fnv32a hash of the default-network selfLink shared by all instances. The GCP ratchet (BUG-2235) added a Compute SDK test that creates a real instance, so the **SDK** test step's sim process now materializes that default-network namespace in the host-global `/run/netns/`; when that process is killed at step-end without running cleanup, the namespace orphans, and the next step's fresh sim process (empty in-memory owner map) hits "File exists" on `ip netns add`. Fixed in the shared realexec `Host.CreateNetworkNamespace`: on an `ip netns add` failure it now deletes the orphan and retries once — safe because callers gate on their in-process owner map first, so a collision here is always a dead-process leftover with no live owner. General robustness improvement for both GCP and Azure realexec against killed-process namespace leaks. |
| ~~2235~~ | P2 | GCP sim — service ratchet round 3 (Compute v1 control plane + Cloud Run v2) | conformance / fidelity | Ratcheted the two GCP surfaces with genuinely-addressable gaps (the other two waves-targets — Bigtable Admin v2 136/162, Cloud Resource Manager v3 105/124, Logging v2 480/508, Cloud Functions v2 31/42 — were independently re-verified already at their faithful ceiling: every uncovered Discovery method is a `{+name}`/`{+resource}` reserved-expansion template alternate the route-validity gate forbids, so no concrete flatPath remains). **Compute Engine v1** 174→**440/1994** (`compute_more.go` + `registerComputeMore`): images/snapshots/machineImages, regionDisks, globalAddresses/routes, the load-balancing control plane (targetPools/forwardingRules/healthChecks/backendServices/urlMaps/targetHttp(s)Proxies/targetTcp+GrpcProxies/sslCertificates), zonal+regional instanceGroupManagers (resize/setInstanceTemplate/listManagedInstances), instance actions (reset/setMachineType/setMetadata/attachDisk/detachDisk/getSerialPortOutput), catalog reads (regions/diskTypes/machineTypes/acceleratorTypes), and aggregatedList/operations across the lot — all metadata-only/host-agnostic (real-exec-requiring network ops stay Linux-gated), each returning a faithful zonal/regional/global `Operation`. **Cloud Run v2** 62→**89/116** (`cloudrunworkerpools.go` + `cloudruninstances.go`): the WorkerPools family (CRUD + revisions + IAM, via `run.WorkerPoolsClient`), the Instances family (CRUD + start/stop + IAM), and v2 UpdateJob/DeleteExecution/Tasks get+list — plus a real fix: v2 `GET .../jobs/{job}` didn't colon-split, so `JobsClient.GetIamPolicy` (`.../jobs/{job}:getIamPolicy`) fell through to the job lookup and 404'd; it now splits the verb to the shared IAM handler like the services handler does. **GCP total coverage 3180→3473/5244 (61%→66%).** Every op spec-validated against its Discovery doc (0 new violations) + real Google Cloud Go SDK; floors reconciled from one combined measured pass. **Known limitation (not separately filed):** Cloud Logging `projects.locations.get` is the one concrete method left unserved — in the collapsed single-port sim its `{location}` segment collides with Cloud Run v2's `:exportProjectMetadata` colon-verb, and exportProjectMetadata has no stable client to implement first, so mounting `locations.get` cannot be done cleanly without cross-service coverage inflation; documented in `logging_admin.go`. |
| ~~2234~~ | P2 | Azure sim — fourth service ratchet (Logic Apps / App Service-Web Apps / Cosmos completion + Log Analytics / APIM-Resources-PostgreSQL completion) | conformance / fidelity | Fourth wave against the `azureMethodFloor` gate (four agents, one service-set each, every op spec-validated against vendored ARM Swagger — 0 new violations, list-by-subscription/RG paths explicitly exercised — + real `azure-sdk-for-go`). **Logic Apps** (Microsoft.Logic) 13→**106/106** (workflows + versions + triggers + runs/actions/repetitions, integration accounts + 8 artifact collections, integration service environments + managed APIs). **App Service / Web Apps** (Microsoft.Web) 37→**161/692** (the core surface a real client uses: app service plans, web apps + slots, config/appsettings/connectionstrings, deployments, host-name bindings, source control, functions, static web apps, subscription-global catalogs) — surfaced + fixed [[BUG-2233]] (FunctionEnvelope `properties.name` leak + status-code corrections). **Cosmos DB** finished — 2024-08-15 103→**124/124**, 2021-10-15 100→**121/121**, private-endpoint-connections 0→**4/4** each (metrics/usages/percentile time-series + region online/offline LROs); **Log Analytics query** data-plane (monitor-dataplane-operationalinsights) 1→5/7 (shared `runKQLQuery` engine, real tables/columns/rows — no faked results). **API Management** apimapis 52→**91/91**, apimproducts 25→**31/31** (GraphQL resolvers, API issues/comments/attachments, tag descriptions, wikis); **PostgreSQL Flexible Server** 37→**66/66** (LTR backups, threat protection, tuning, migrations, PEC/private-link); **ARM Resources** 32→36/40 (the 4 uncovered are generic-resource `{ns}/{parent}/{type}/{name}` routes Go 1.22's ServeMux can't host without shadowing every service — the route-validity-gate-forbidden greedy territory). **Azure total coverage 1409→1758/2597 (54%→68%).** SDK + CLI tests per service; floors reconciled from one combined measured pass (merged-sim coverage exactly equals the sum of the per-agent measurements); new LRO paths reuse the shared `issueAzureAsyncOperation`/`handleAzureAsyncOperationStatus` (Retry-After:1, so the SDK pollers stay fast). |
| ~~2233~~ | P3 | Azure sim — Microsoft.Web FunctionEnvelope emitted an undeclared `properties.name` | fidelity / spec divergence | The `FunctionEnvelopeProperties` struct serialized a `name` member (`json:"name"`), but the Microsoft.Web `FunctionEnvelopeProperties` Swagger schema declares no `name` (the function's name lives on the ProxyResource envelope), so `GET/PUT …/sites/{name}/functions/{functionName}` leaked `$.properties.name`. Surfaced while broadening the App Service / Web Apps ARM slice (the new `WebApps_CreateFunction`/`GetFunction` SDK round-trips exercised the shape under the spec-validator). Fixed by marking the field `json:"-"` (kept for internal tracking, never emitted). |
| ~~2232~~ | P2 | Azure sim — EventGrid regenerateKey leaked an undeclared `keyGeneration` property (CI-caught) | fidelity / spec divergence | Once the LRO + CLI timeout fixes (BUG-2230/2231) let the full `sim (azure)` suite run, the spec-validator flagged `GET …/Microsoft.EventGrid/partnerNamespaces` returning `$.value[].properties.keyGeneration` — a field no EventGrid properties schema declares. `eventGridRegenerateKeyResponse` stashed its per-resource SAS-key rotation counter inside the resource's spec-visible `properties` map, so it echoed on every GET/LIST. (Missed by the agent's own validation because its tests didn't exercise the partner-namespace list-by-subscription path.) Fixed by moving the counter to a dedicated package-level `eventGridKeyGen` map (the `ec2SubnetIPCursor` pattern), out of the serialized properties — `listKeys` never read it anyway. |
| ~~2231~~ | P3 | Azure sim — CLI test suite exceeded its hardcoded 120s go-test timeout (CI-caught) | CI timeout / test infra | With the LRO-poll fix (BUG-2230) letting the SDK suite finish, the azure `cli-test` step then panicked at its hardcoded `-timeout 120s` — every CLI test passed (each ~0.5-2.3s) but the third-ratchet wave's added `az rest` tests (Cosmos/Event Grid/APIM/PostgreSQL/Resources/App Insights) pushed the cumulative suite to ~137s. Each `az` invocation carries ~0.5-2s of Python-interpreter startup, so wall-clock grows with the test count as coverage ratchets up (same class as the AWS appdata CLI-shard split). Fixed by making the azure `cli-test` target honor a `CLI_TEST_TIMEOUT` variable (default 600s, matching the AWS Makefile pattern) and raising the CI "Run CLI tests" step `timeout-minutes` 5→12 so the inner deadline governs. |
| ~~2230~~ | P2 | Azure sim — async-operation status poll lacked Retry-After → 30s azcore polls (CI-caught timeout) | fidelity / CI timeout | `sim (azure)` on PR #695 panicked at the 10m `go test` default — the suite ran >10m. Root cause: the shared `handleAzureAsyncOperationStatus` returned the in-progress operation with **no `Retry-After` header**, and `issueAzureAsyncOperation` marks the op `InProgress` then flips it to `Succeeded` 50ms later, so the SDK's immediate first poll catches `InProgress`. Per azcore `runtime/poller.go@v1.22.0` (verified), a `Retry-After` ≤ 0 is ignored and the poller falls back to its **30s default frequency** — so every long-running operation in a test waited 30s per poll. This was latent until the PostgreSQL ratchet (BUG-2229) added many lifecycle LROs (start/stop/restart/administrators/backups/virtualEndpoints): `TestAzurePGFlexibleServer_LifecycleActions` took 150s and `_NestedResources` 270s (~7min combined), tipping the suite over 10m. Fixed by advertising `Retry-After: 1` on the in-progress async-operation poll (real Azure returns a positive Retry-After on in-progress polls; matches the existing `cache_redis.go` pattern). The two PG tests dropped to 5s and 9s; the suite is back to ~3-4min. |
| ~~2229~~ | P2 | Azure sim — third service ratchet (Cosmos DB / Event Grid / API Management / PostgreSQL-Resources-App Insights) | conformance / fidelity | Third wave against the `azureMethodFloor` gate (four agents, one service-set each, every op spec-validated against vendored ARM Swagger — 0 new violations — + real `azure-sdk-for-go`). **Cosmos DB** (Microsoft.DocumentDB): 2024-08-15 25→103/124, 2021-10-15 25→100/121 (MongoDB/Cassandra/Gremlin/Table resource families, SQL stored-procedures/UDFs/triggers/client-encryption-keys, throughput autoscale↔manual migration, account update/failover/regenerateKey/checkNameExists, operations/locations; one version-agnostic handler set serves both API versions). **Event Grid** (Microsoft.EventGrid): 2021-12-01 31→61/61, 2022-06-15 40→127/127 — both **100%** (topics/domains/system-topics + per-scope event subscriptions with getFullUrl/getDeliveryAttributes, the full partner registrations/namespaces/configurations family, topic-type + system-event-type catalogs). **API Management** (Microsoft.ApiManagement): apimapis 9→52/91, apimproducts 4→25/31, apimbackends/apimdeletedservices/apimdeployment/apimnamedvalues/apimsubscriptions all **100%** (service skus/backup/restore/migrateToStv2/getSsoToken, API schemas/policies/diagnostics/releases/revisions, product/subscription/named-value/backend lifecycle + GetEntityTag HEAD ops) — surfaced + fixed [[BUG-2228]] (named-value secret leak). **PostgreSQL Flexible Server** 15→37/66 (start/stop/restart/failover LROs, administrators/backups/replicas/virtualEndpoints/configurations), **ARM Resources core** 15→32/40 (providers register/unregister/get/list, deployments + exportTemplate, moveResources/validateMoveResources LROs, tagNames), **subscriptions** 1→7/7 (**100%**), **Application Insights components** 3→8/8 (**100%**) — the shared moveResources route also lifted web-arm 35→37 incidentally. **Azure total coverage 1000→1409/2597 (38%→54%).** SDK + CLI tests per service; floors reconciled from one combined measured pass (merged-sim coverage exactly equals the sum of the per-agent measurements). |
| ~~2228~~ | P3 | Azure sim — APIM NamedValue GET/LIST/PUT echoed the secret `value` | fidelity / secret leak | The Microsoft.ApiManagement `namedValues` handlers stored and echoed the request `properties.value` verbatim on create/get/list, but a named value's secret is write-only on the real service — only `POST .../namedValues/{nv}/listValue` returns it; GET/LIST/PUT/PATCH redact it (`NamedValueContract` carries `value` in the schema but the real service never populates it on read). Fixed by stripping `properties.value` from every read response (`apimRedactNamedValue`) and surfacing the secret solely through the new `listValue` operation. Found and fixed while ratcheting the APIM ARM coverage. |
| ~~2227~~ | P3 | AWS sim — `TestOrganizations_DescribeAndList` flaky on account ordering (CI-caught) | flaky test | `sim (aws sdk)` failed on PR #694's branch (zero AWS code) with `TestOrganizations_DescribeAndList` asserting `ListAccounts()[0].Id == MasterAccountId`. The management account id is the fixed `123456789012`, but `orgNewAccountID()` generates **random** 12-digit ids and `handleOrgListAccounts` sorts ascending by id — so once any other test (e.g. `TestOrganizations_OUTreeAndAccounts`'s `CreateAccount`) leaves an account whose random id sorts below `123456789012`, `Accounts[0]` is no longer the management account. The sim is faithful (real AWS account ids are arbitrary and `ListAccounts` is not management-account-first); the test's position assumption was the bug. Fixed by asserting the management account is **present** in the list (membership) rather than first. |
| ~~2226~~ | P2 | Azure sim — second service ratchet (Storage / Networking / Redis-KeyVault-MSI / Container Instances-RBAC) | conformance / fidelity | Second wave of driving Azure services up against the `azureMethodFloor` gate (four agents, one service-set each, every operation spec-validated against its vendored ARM Swagger — 0 new violations — + real `azure-sdk-for-go` round-trips). **Storage** (`storagearm.go` + `registerStorageAccounts`): storage-arm-storage 6→44/49 (account action verbs regenerateKey/ListAccountSas/ListServiceSas/failover/revokeUserDelegationKeys, catalog ops Operations/Skus/Usages/CheckNameAvailability/DeletedAccounts/ListByResourceGroup, and managementPolicies/inventoryPolicies/privateEndpointConnections/privateLinkResources/objectReplicationPolicies/encryptionScopes/localUsers), blob 6→17/17, file 5→12/12, queue 1→8/8, table 6→8/8 — surfaced + fixed a pre-existing fidelity bug (the `FileShareProperties` struct emitted an undeclared `provisioningState` the storage-arm-file Swagger has no member for; removed the field + its `"Succeeded"` assignment) and a wrong key-permission enum (`listKeys`/`regenerateKey` emitted `"FULL"`; the `armstorage` `KeyPermission` enum is `Read`/`Full` — the pre-existing `storage_test.go` assertion that pinned `"FULL"` was corrected to `"Full"`). **Networking** to 100% on six docs: dns 10→14/14, privatedns 12→17/17, loadbalancer 22→27/27, networkinterface 14→15/15, publicipaddress 6→9/9, virtualnetwork 18→21/21 (DNS subscription-wide list path is lowercase `dnszones` per the SDK; LoadBalancingRules_Health does real probe-based up/down). **Redis / Key Vault / Managed Identity** all to 100%: redis 11→41/41 (access policies + assignments, linked servers, patch schedules, PEC/private-link, regenerateKey/forceReboot/import/export/flush, checkNameAvailability, asyncOperationStatus), keyvault-arm 11→17/17 (checkNameAvailability + PEC + privateLinkResources), msi 4→12/12 (user-assigned identity PATCH/list-by-RG/list-by-sub, federated identity credentials CRUD, systemAssigned by-scope) — fixed in-place that Redis CheckNameAvailability returns a 200-no-body (available) / error-envelope (taken), unlike Key Vault's `CheckNameAvailabilityResult` body, and that the patch-schedule `ScheduleEntries` schema carries no `provisioningState`. **Container Instances** 11→18/18 (Operations_List, Location ListUsage/ListCapabilities/ListCachedImages, GetOutboundNetworkDependenciesEndpoints, Containers_Attach, SubnetServiceAssociationLink_Delete) and **RBAC** roleAssignments 5→9, roleDefinitions 3→5 (concrete subscription/RG-scope routes + custom role-definition CRUD; the remaining ops are the caller-effective-permissions / arbitrary-resource-scope ones left honestly unimplemented rather than faked). **Azure total 857→1000/2597 (33%→38%).** SDK + CLI tests per service; floors reconciled from one combined measured pass (the merged sim's coverage exactly equals the sum of the per-agent measurements). |
| ~~2225~~ | P2 | AWS sim — EC2 RunInstances ignored ClientToken idempotency (CI-caught flake) | fidelity / idempotency / flaky test | `sim (aws sdk)` failed on PR #693's branch (which touches zero AWS code) with `TestEC2_DescribeInstancesPagination` seeing **6** instances tagged `page-test=yes` when the test created **3** (page 2 returned count 2 + NextToken `"4"` instead of count 1 + empty). **Root cause:** the EC2 `RunInstances` `ClientToken` member carries the Smithy `idempotencyToken` trait, so `aws-sdk-go-v2`'s `OperationIdempotencyTokenAutoFill` initialize step (before the Retry middleware) populates a UUID once and **re-sends the same token on every retry** (verified in `service/ec2@v1.308.0/api_op_RunInstances.go:546-551`). Under CI load the SDK retried `RunInstances`, and the sim — which ignored `ClientToken` entirely — created a **second batch of 3** instead of replaying the first, doubling the count. Real EC2 dedupes on `ClientToken` within the idempotency window: a retried `RunInstances` returns the originally-created reservation, not new instances. **Fixed** by recording each successful `RunInstances` under its `ClientToken` (new `ec2_run_instances_tokens` store) and replaying the original reservation/instances when the same token arrives again — faithful EC2 idempotency that eliminates the duplication at the root. SDK test asserts a re-issued `RunInstances` with a fixed `ClientToken` returns the same instance IDs and creates no new instances. |
| ~~2224~~ | P2 | Azure sim — first service ratchet (now that the Azure coverage gate exists) | conformance | First wave of driving Azure services up against the `azureMethodFloor` gate built in #689 (the Azure sim had a coverage gate but no services ratcheted yet). Four agents, one handler-file set each, every operation spec-validated against its vendored ARM Swagger (0 new violations) + real `azure-sdk-for-go` round-trips: **Container Apps** (Microsoft.App) — containerApps 5→11/11, jobs 9→12/12, managedEnvironments 3→19/19 (all 100%; start/stop do real replica work, certificates + managedCertificates CRUD); **Container Registry** (Microsoft.ContainerRegistry) — 2025-11-01 12→58/58, 2023-07-01 12→52/52, registrytasks 2→25/25 (all 100%; replications/scopeMaps/tokens/credentialSets/connectedRegistries + webhooks whose write-only secrets surface only via getCallbackConfig); **Networking** (Microsoft.Network) — NSG/NAT-Gateway/PublicIP-Prefix/RouteTable to 100%, virtualNetwork 6→18/21, loadBalancer 9→22/27, NIC 4→14/15, publicIP 4→6/9 (~50 routes, cross-references read back faithfully); **Service Bus + Event Hubs** — all 8 docs to 100% (namespaces CRUD + list-by-sub + PATCH, authorizationRules listKeys/regenerateKeys, disasterRecovery break-pairing/failover, migrationConfigs, networkRuleSets, private-endpoint connections) — plus a latent PascalCase casing bug (`AuthorizationRules`/`ListKeys`) the new SDK tests exposed, and two stub-404 DR/migration handlers converted to store-backed. **Azure total coverage 630→857/2597 (24%→33%); all three simulators now actively ratcheting.** Each service uses the existing `issueAzureAsyncOperation` LRO helper + provisioningState; the path-normalization middleware casing was honored per-route. Integration: floors reconciled from a single measured pass; a literal-path doc block records the 4 subscription-wide list ops the SDK builds internally (contract hook). |
| ~~2223~~ | P2 | AWS sim — ELBv2 NLB DescribeLoadBalancers returned proxy host:port as DNSName (#691) | fidelity / terraform idempotency / Route53 | Regression from #683 (BUG-2218): to make the NLB raw-TCP data plane reachable, `elbv2ReportedDNSName` overrode the load balancer's stable AWS-shaped `DNSName` with the proxy's dialable `host:port` (e.g. `10.89.3.2:44425`) for any NLB with a running stream listener. The proxy bound an **ephemeral** port → the reported DNSName changed every run (perpetual `terraform-provider-aws` `dns_name` drift) and a `host:port` is not a valid Route53 alias target. **Fixed by making reachability faithful instead of hijacking DNSName:** DescribeLoadBalancers now always returns the stable `<name>-<id>.elb.<region>.amazonaws.com` hostname (NLB + ALB alike, `CanonicalHostedZoneId` present so a Route53 alias is valid); the NLB stream proxy binds the **listener's configured port** on a stable host (`127.0.0.1`, or — on Linux, when that port is taken by another NLB — a distinct per-NLB `127.0.0.0/8` address leased via the new `realexec.ReserveNLBLoopbackIPv4`, the real-NLB "each LB has its own IP" shape, collision-free); and the AWS-shaped DNSName resolves to that proxy host through workload-container hosts entries (`elbv2NLBHostEntries`/`elbv2WorkloadExtraHosts` merged onto the ECS workload `ExtraHosts`, the same mechanism metadata/Cloud Map use — the faithful production consumer of `elbv2NLBProxyAddress`, so the deadcode gate stays green). CreateListener is best-effort on the bind (real AWS never fails CreateListener for a sim-local socket issue — privileged port / off-Linux same-port collision — so the control plane + stable DNSName stay faithful; only the local data plane for that listener is unavailable, and it's logged). Tests: #691 contract guard (DNSName AWS-shaped, colon-free, stable across calls, CanonicalHostedZoneId present); the #683 raw-TCP capability test reworked to resolve the DNSName via the production hosts mapping then connect on the listener port; idempotency-fidelity terraform stack gained a `network` `aws_lb` + TCP listener/target-group + a Route53 alias targeting it (real apply + clean plan, 151s). aws+realexec build/lint(0)/deadcode(0) + conformance + spec-validator(0) green. Closes #691. |
| ~~2222~~ | P2 | AWS sim — ELBv2 TCP target group still returns HealthCheckPath (#688) | fidelity / terraform idempotency | Follow-up to #685 (BUG-2217): that fix omitted the HTTP-only `Matcher` for TCP health checks, but `HealthCheckPath` is the **same class** of HTTP/HTTPS-only attribute and was still leaking — `CreateTargetGroup` defaulted it to `/` for every protocol and the target-group XML emitted `<HealthCheckPath>` unconditionally, so a TCP target group returned `HealthCheckPath=/`, which `terraform-provider-aws` reads back and plans as a perpetual `health_check.path` drift. Fixed by generalizing the predicate (`elbv2MatcherApplies` → `elbv2HTTPHealthCheck`, now governing both fields) + a new `elbv2DefaultedHealthCheckPath` helper: Create defaults the path to `/` only for HTTP/HTTPS, the XML emits `<HealthCheckPath>` only for HTTP/HTTPS, and ModifyTargetGroup clears both `Matcher` and `HealthCheckPath` when the health check changes to a non-HTTP protocol (and defaults them when it changes to HTTP). Boyscout-checked the sibling `ProtocolVersion` (already correctly omitted for TCP). SDK + CLI fidelity tests extended to assert the path is omitted for TCP (and `/` for an HTTP health check on a TCP target group); the idempotency-fidelity terraform stack's TCP target group gained a TCP `health_check` block so `plan -detailed-exitcode` now catches this drift class (real apply + clean plan, 127s). Closes #688. |
| ~~2221~~ | P3 | smoke-test Docker builds not resilient to proxy.golang.org blips (CI-caught) | CI infra resilience | The `smoke (ecs)` job failed with `go mod download … proxy.golang.org … stream error … INTERNAL_ERROR; received from peer` while Docker-building the AWS sim image (`Dockerfile.ecs`). **Root cause, not a one-off:** the three smoke Dockerfiles (ecs/cloudrun/aca) fetch every module fresh from the proxy inside the docker build (the `actions/setup-go` cache the other CI jobs use isn't available there) and ran `go build` / `go mod download` with **no retry**, so any single transient proxy error fails the whole image build and the smoke job. Investigated rather than just re-run (per boyscout): added `smoke-tests/retry-go-build.sh` — retries the network-only `go mod download` with backoff (5 attempts), then runs the deterministic `go build` once — and wired it into all three smoke Dockerfiles (replacing the bare `go build`/`go mod download && go build`). Shellcheck-clean; verified one smoke image builds through the wrapper. The flaked `sim (gcp)`/`sim (azure)` jobs that actually exercise this PR's changes passed; this hardens the orthogonal smoke build against the class of failure. |
| ~~2220~~ | P2 | GCP coverage ratchet round 2 + build the Azure operation-coverage gate | conformance | **Azure gate:** built `azureMethodFloor` (in `simulators/azure/azure_coverage_test.go`) — the Swagger-spec analogue of the AWS service-conformance ratchet and the GCP Discovery-doc ratchet, the last sim without a coverage gate. Per vendored Azure ARM Swagger doc it counts how many operations the sim implements (an operation is covered when a registered route matches its method + normalized path under the same `matchAzureSegs` rules the route-validity gate uses), locked by an exact-equality ratchet over 90 swagger files (Azure measured at **630/2597, 24%**). **All three simulators now have an operation-coverage ratchet.** **GCP ratchet:** drove a second wave of services (one service file per agent): Spanner 186→198/198, Cloud SQL v1 136→148/148, VPC Access 15→16/16, ServiceUsage 19→20/20, IAM Credentials 11→14/14 (all 100%); API Gateway 54→57/60, Cloud Functions v2 15→31/42, Cloud SQL v1beta4 45→146/148, Cloud Resource Manager v3 11→105/124 (projects/folders/orgs/liens/tagKeys/tagValues/tagBindings + IAM), Cloud Run v1 61→98/152 + v2 51→62/116 (Knative child resources, domain mappings, revisions, IAM); plus the big-surface wave: Dataflow 8→84/84 (100%, global + regional job/template/snapshot surfaces), Bigtable Admin 65→136/162 (clusters/backups/appProfiles/tables/authorizedViews/schemaBundles/logical+materialized views + IAM), Cloud Logging 170→480/508 (sinks/exclusions/logs/buckets/views/links/settings/savedQueries across the four parent scopes — project/org/billing/folder). **GCP total 2413→3180/5244 (46%→61%); ~22 GCP services now at 100%.** Every service spec-validated against its Discovery schema (0 new violations) + real Google Cloud Go SDK round-trips; the consistently-uncovered remainder is the `{+name}`/`{+resource}` reserved-expansion template alternates the flatPath form already covers. |
| ~~2216~~ | P3 | CI flakiness sweep — AWS + GCP + Azure test suites | flaky test | The `sim (aws sdk)` CI job failed once on PR #686's branch then **passed on a no-change re-run** → flaky, not a deterministic divergence (the GCP-ratchet branch touched zero AWS code; `git diff --stat origin/main` = only `simulators/gcp/**` + `.md`). The exact attempt-1 assertion wasn't recoverable (GitHub retained only the passing attempt; `amazon/dynamodb-local` is amd64-only and won't run on the arm64 dev Mac), so this was a **systematic flaky-pattern hardening pass** across all three sims rather than a single-line fix. Fixed: **AWS** — `resource_mapping_test.go` 800ms-sleep→poll-until for the ENI attachment; `ec2_test.go`/`cli-tests/ec2_test.go` EBS-snapshot poll deadlines **2s→60s** (the most likely culprit — a 2s give-up on a loaded runner); DynamoDB-Local readiness wait **30s→120s** (JVM cold start under load). **GCP** — Cloud Run job-execution 3s-sleep→poll-until-terminal; multi-container job `Eventually` 15s→60s; cross-job DNS waits 10s→30s. **Azure** — AppTraces 100ms-sleep→`Eventually`; `waitAzureAsyncOperation` 2s→30s; `waitForCLIJSON` 5s→30s; Service Bus / Event Hubs AMQP receive ctx 5s→30s (one demonstrably necessary — azeventhubs holds the link the full window); ACA/ACI cross-job DNS + real-replica log waits 10s→30s. Genuine cloud-delay sleeps and deterministic timestamp-separator sleeps were left alone (verified non-flaky). All assertions unchanged; tests rerun multiple times green. |
| ~~2217~~ | P2 | AWS sim — ELBv2 emits HealthCheck Matcher for TCP target groups (#685) | fidelity / terraform idempotency | `DescribeTargetGroups` returned a `Matcher` (HTTP response-code matcher) for TCP target groups, but real AWS omits `Matcher` for any non-HTTP/HTTPS health check — so `terraform-provider-aws` saw a `matcher` it never set and produced a perpetual plan diff. Fixed: `Matcher` is now emitted only when the target group's effective **health-check protocol** is HTTP/HTTPS (or a gRPC code is set); TCP/TLS/UDP/TCP_UDP/GENEVE health checks carry none. A TCP target group with an explicit HTTP health check correctly does carry `200`. SDK + CLI + a TCP target group added to the `idempotency-fidelity` terraform stack (a matcher diff now fails `plan -detailed-exitcode`). Closes #685. |
| ~~2218~~ | P2 | AWS sim — ELBv2 NLB data plane was HTTP-only (#683) | data-plane feature | The ELBv2 Network Load Balancer data plane only carried HTTP (host-header interception over the sim's HTTP mux), so raw-TCP workloads (e.g. SSH-through-NLB) couldn't be forwarded end-to-end at the sim. Added a real raw-TCP forwarding path (`elbv2_nlb_proxy.go`): a `TCP`/`TCP_UDP` listener binds a real `net.Listener`, and each accepted connection resolves a healthy registered target **at connect time** (target health + (de)registration honored per-connection like a real NLB), `net.Dial`s it, and `io.Copy`s both directions. Lifecycle tied to listener/LB deletion. Honest remainder: `TLS` listeners (terminate at the listener — needs a modeled cert/handshake) and `UDP` (not a stream) are excluded; `TCP`/`TCP_UDP` forward faithfully, which covers the motivating SSH case. Tests prove raw bytes round-trip through the proxy and that a no-healthy-target connection closes empty. Closes #683. |
| ~~2219~~ | P3 | AWS sim — CloudTrail silently dropped all ElastiCache management events | event recording | CloudTrail's `awsEventSourceByQueryVersion` table had no entry for the ElastiCache query API version `2015-02-02`, so every ElastiCache management call (surfaced by the runtime warning on `FailoverGlobalReplicationGroup` / `TestFailover`) hit the "no eventSource mapping for service slice; event not recorded" path and was dropped from the trail — breaking `LookupEvents` filtering for ElastiCache. Fixed by adding `"2015-02-02" → elasticache.amazonaws.com` (verified unique to ElastiCache among the sim's query services). Also closed the coverage-test gap: `TestAWSEventSourceCoversAllServiceSlices` now pins ElastiCache (and the previously-untested CloudWatch `2010-08-01`) so the mapping can't silently regress. |
| ~~2215~~ | P2 | GCP sim — DNS DNSSEC dnsKeys non-deterministic (CI-caught) + Firestore streaming | non-determinism / streaming | `TestDNS_DnsKeysReflectDnssecState` failed in CI (passed locally): `dnsKeys.list` returned key tag "2882" but `dnsKeys.get/{tag}` 404'd, because `deriveDNSKey` generated the ECDSA P-256 key via `ecdsa.GenerateKey(P256, deterministicKeystream)` — and Go's `ecdsa.GenerateKey` deliberately calls the crypto-internal `randutil.MaybeReadByte`, consuming a random number of bytes from a *global* source to defeat exactly this determinism. So list and get derived *different* keys (different tags) run-to-run; it matched locally by luck. Fixed by deriving the private scalar directly from the SHA-256 keystream (`d = keystream(320 bits) mod (n-1) + 1`, public point via `ScalarBaseMult`) — no `GenerateKey`, no `MaybeReadByte`, fully deterministic (verified 8/8 fresh processes). **Bundled into the BUG-2214 PR.** Also in the same PR, per user request: (1) removed the Firestore `runQuery`/`batchGet` streaming entries from `spec-violation-allowlist.txt` (now empty) — the validator instead recognizes server-streaming responses and validates each stream *element* against the Discovery schema (`isStreamingResponseOp` + per-element walk), strictly stronger than allowlisting the array mismatch; (2) implemented **true incremental streaming** for `runQuery`/`batchGet` — each result element written + flushed as its own chunk via `http.Flusher` over chunked transfer-encoding (added `Flush()` to the shared `statusWriter` so flush propagates through the middleware chain), honoring client cancellation, matching how real Firestore streams over REST; proven by `TestFirestore_RunQueryStreamsChunked` (asserts chunked encoding + no Content-Length + valid array). |
| ~~2214~~ | P2 | GCP sim — build the operation-coverage gate + ratchet 12 mid-size services | conformance | Built the GCP equivalent of the AWS service-conformance ratchet: a `gcpMethodFloor` (in `gcp_coverage_test.go`) that counts, per vendored Google **Discovery document**, how many REST methods the sim implements (a method is covered when a registered route matches its HTTP-method + normalized path under the same `matchSegs` rules the route-validity gate uses) and locks the count with an exact-equality ratchet. The bulk of Compute (~2000 methods) is honestly far from 100% — the floor records the real count. Then ratcheted **12 mid-size services** (2 rounds, one service file per agent): **Cloud Build 104→130/130, Memorystore Redis 64→90/90, Firestore admin 89→112/112, Cloud Storage JSON 32→84/84 (all 100%)**; Cloud KMS 122→157/166 (real Go-stdlib crypto for mac/raw/asymmetric/generateRandomBytes; honest metadata-only for EKM/HSM/post-quantum decapsulate), IAM admin 204→264/266 (workload/workforce identity pools, OAuth clients, custom roles, SA keys), Artifact Registry 97→144/147 (packages/versions/tags/files/rules/attachments + project settings), Eventarc 97→124/132 (enrollments/messageBuses/pipelines/googleApiSources + IAM), BigQuery 38→86/95 (models/routines/row-access-policies + IAM), Cloud DNS 24→74/80 (policies/response-policies/rules + real DNSSEC DS digests), Pub/Sub 77→86/92 (schemas + IAM), Secret Manager 50→60/64 (regional secrets + IAM). **GCP total coverage 1986→2413/5244 (38%→46%); 6 GCP services now at 100%.** Every service spec-validated against its Discovery schema (0 new violations) + real Google Cloud Go SDK round-trips. Integration fix: 2 staticcheck (dns ECDSA embedded-field) + 1 unused func (iam) cleared; floor bumps reconciled from a single measured-coverage pass. The consistently-uncovered remainder per service is the `{+name}`/`{+resource}` reserved-expansion *template alternates* the Discovery docs list alongside each flatPath — the flatPath form every real client uses is covered; matching the template form would need an over-broad catch-all the route-validity gate forbids. |
| ~~2212~~ | P3 | AWS sim — IAM fail-loud gaps (boyscout) | validation / delete-cascade | A boyscout audit of the older IAM slice found five real fail-loud gaps. **Missing policy-document validation:** PutRolePolicy and UpdateAssumeRolePolicy stored the supplied JSON without `parseIAMPolicy`, unlike CreateRole/CreatePolicy/PutUserPolicy — a malformed policy/trust document was silently persisted instead of raising `MalformedPolicyDocument`. **Missing delete-conflict enforcement:** DeleteUser, DeleteRole, and DeleteInstanceProfile deleted the principal even when it still had attachments (access keys / inline + attached policies / group memberships for a user; inline + attached policies or instance-profile membership for a role; an attached role for an instance profile) — real IAM raises `DeleteConflict` and requires detaching first. All five now fail loud with the real IAM error code; existing tests/terraform that relied on permissive delete were updated to detach first. One dead-var (`iam_slr_oidc.go` `_ = elemName`) and the idiomatic `crypto/rand.Read` error ignore were classified non-bugs. |
| ~~2211~~ | P2 | AWS sim — event-stream ops + drive CloudWatch/Organizations/SQS/Kinesis to 100% | conformance | Implemented the genuinely-streaming ops faithfully (real `application/vnd.amazon.eventstream` framing via the sim's existing `awsEventStreamMessage` encoder, decoded by the real aws-sdk-go-v2 client) and ratcheted the next mid-size floors. **Five more services to 100%:** CloudWatch monitoring (GraniteServiceVersion20100801) 38→49/49 (metrics datasets + CMK, OTel enrichment, managed Contributor-Insight rules, GetInsightRuleReport, DescribeAlarmContributors, GetMetricWidgetImage returning a real `image/png`; both awsJson1.0 + rpc-v2-cbor handlers), Organizations 53→63/63 (CreateGovCloudAccount, LeaveOrganization, responsibility-transfer family, effective-policy validation), SQS 19→23/23 (DLQ-redrive message-move-task family — really moves messages), CloudWatch Logs 111→113/113 (StartLiveTail + GetLogObject event streams), Kinesis 38→39/39 (SubscribeToShard event stream). **S3 103→104** (SelectObjectContent — a real minimal S3 Select over CSV/JSON-lines streaming a `SelectObjectContentEventStream`; WriteGetObjectResponse stays deferred as an Object-Lambda dedicated-endpoint op). **Twenty-six AWS services now at 100%.** Two load-bearing streaming findings: the SDK eventstream reader blocks on a mandatory `initial-response` frame (omitting it deadlocks), and StartLiveTail/GetLogObject carry an `@endpoint(hostPrefix:"stream-")` the test client disables via `DisableEndpointHostPrefix`. The spec-shape validator gained an event-stream exemption: a `vnd.amazon.eventstream` response is a framed multi-message body the SDK decoder validates, so the restXml/query shape-validators skip it (the awsJson path already skipped non-json content-types). All spec-validated (0 divergences) + real SDK + `aws` CLI. |
| ~~2210~~ | P3 | AWS sim — concurrent read-modify-write races | TOCTOU | A boyscout audit found three real request-time concurrency races where a read-modify-write spanned multiple separate store locks. **PutMetricData** (`cloudwatch_metrics.go` + `cloudwatch_metrics_json.go` `cwStoreDatum`): `Update(key, append)` returns false for a not-yet-existing metric, then a separate `Get`+`Put` creates it — two concurrent PutMetricData to the same new metric key could lose a datum (thread A's `Put` skipped because thread B's `Put` already created the key between A's Update and Get). **PurgeQueue** (`sqs.go`): `Get`+`Put` snapshot-and-write-back could clobber a concurrent `ReceiveMessage`'s mutation. Fixed by adding an atomic `Upsert(id, fn)` to the `Store[T]` interface (single-locked create-or-modify in both MemoryStore + SQLiteStore) and using it for the metric append; PurgeQueue now clears via the atomic `Update`. |
| ~~2209~~ | P3 | AWS sim — silent request-body parse swallows | error swallow | Three handlers dropped `sim.ReadJSON` errors with `_ =`, silently losing an optional filter/param on a malformed body instead of failing loud: Organizations CreateOrganization (FeatureSet, `organizations.go`), Organizations ListDelegatedAdministrators (ServicePrincipal filter), CloudWatch ListDashboards (DashboardNamePrefix, `cloudwatch_dashboards.go`). Now each returns the service's InvalidParameter/InvalidInput error on a genuinely-malformed body (`ReadJSON` returns nil for an empty body, so valid no-body requests are unaffected). Two further audit findings were classified non-bugs: a `cbor.Marshal(map[string]any{})` drop (cannot fail on an empty plain map — unreachable) and a `json.NewEncoder(w).Encode` drop (idiomatic ignored response-writer error, unactionable after WriteHeader). |
| ~~2208~~ | P3 | AWS sim — ECS ExecuteCommand container-handle poll | flake / premature-break | `ExecuteCommand` polled `ecsProcessHandles` for the task's Docker container handle but **broke out of the retry loop the first time the task entry existed with the container handle not-yet-populated** (`if handle == nil { break }`), instead of continuing to poll — so under the heavier concurrent SDK-test load this PR added, the handle wasn't ready on the first `Load` and the op spuriously returned `TargetNotConnectedException`, failing `TestECS_ExecDataChannelHandshake` in CI. Fixed: keep polling (up to 40×250ms) until both the task entry AND the named container's handle are ready; a single missing handle is no longer a give-up signal. Verified green 3× locally. |
| ~~2207~~ | P2 | AWS sim — drive EC2 + SSM to 100% + ratchet CloudWatch Logs + S3, gate DynamoDB | conformance | Raised ~291 ops in two rounds (6+5 agents, each owning a distinct service file or pre-wired stub). **EC2 to 100%:** 600→769/769 — image-management (watermarks, usage-reports, export/import-image tasks, fast-launch, deprecation, recycle-bin), instance attributes/IAM-profile/console/credit/CPU/placement/SQL-HA, Local Gateway (Outposts) + Capacity Manager + Declarative Policies + Network-Performance, account-settings/ID-format/address-attrs/ClassicLink/enclave-cert/VPN-config/TGW-connect-peer/peering-options/network-ACL, volumes/snapshots/recycle-bin/CoIP/default-VPC/managed-prefix-lists/security-group-references/launch-template-data/DNS-options/Mac-tasks/IPv6. **SSM to 100%:** 43→146/146 — State Manager associations, Automation, Run Command, OpsCenter (ops-items), Maintenance Window executions, Session Manager, hybrid Activations, Inventory, Compliance, Nodes, managed-instance information, document permissions, Patch groups, OpsMetadata, ResourcePolicy, parameter history/labels. **CloudWatch Logs 104→111/113** (OpenSearch S3-table integrations, lookup tables read fields, log-groups-for-query, bearer-token auth, delivery-config update; the 2 remaining — StartLiveTail, GetLogObject — are HTTP/2 event-stream ops). **S3 91→103/107** (bucket-metadata configuration + table configs, ABAC, RenameObject, object-encryption update; the 4 remaining are not faithfully serviceable on the regional surface: SelectObjectContent event-stream, WriteGetObjectResponse Object-Lambda data-plane, and the two S3 Express dedicated-endpoint ops ListDirectoryBuckets + CreateSession). **DynamoDB gated at 57/57** and **Amplify confirmed 37/37** — both already complete. **Twenty-one AWS services now at 100%** (incl. DynamoDB, Amplify). All spec-validated against the vendored `aws-sdk-go-v2` Smithy models (0 divergences) + real SDK + `aws` CLI. Integration coordination: the EC2 `DescribeMacModificationTasks` op was dropped by both owning agents (each deferred to the other) and added during integration; the S3 measurement matrix (method × subresource) was wired into the `s3ImplementedOps` harness from the S3 agent's report; SSM's final `DeleteOpsItem` was implemented to reach 100%. |
| ~~2206~~ | P2 | AWS sim — drive RDS + CloudFront to 100% + ratchet EC2 + boyscout fixes | conformance | Raised ~181 ops in two rounds (5+3 agents). **Two more services to 100%:** RDS 150→164/164 (custom DB engine versions, recommendations, snapshot tenant databases, ServerlessV2 platform versions, valid-instance-modifications, current-cluster-capacity, ModifyOptionGroup, automated-backups replication, global-cluster + read-replica switchovers), CloudFront 152→167/167 (connection functions with publish/test, TestFunction, tagging, and the dynamically-dispatched CreateDistribution*/CreateStreamingDistributionWithTags op names registered after a *verified* real-SDK round-trip — plus a real fix: CreateStreamingDistributionWithTags previously returned 400 because the handler only decoded the bare config). **Big EC2 ratchet 448→600:** EBS encryption-by-default / KMS key / fast-snapshot-restores / snapshot-tiering / block-public-access / volume-attributes (17); VPC endpoint-service configs+permissions / VPC+subnet CIDR / secondary networks / security-group-VPC associations / encryption control (29); Instance Connect endpoints / serial-console / IMDS defaults / event-notify / monitor/reboot (18); Reserved Instances + Capacity Reservation billing/topology/splitting + Capacity Blocks (29); FPGA images + AllowedImagesSettings + ImageBlockPublicAccess + deregistration-protection + bundle/conversion/store-image tasks (22); address transfers / BYOIP / public-IPv4-pools / NAT-gateway secondary addresses / trunk interfaces / carrier gateways / COIP pools / NIC permissions / VGW route propagation / VPN concentrators / ModifyManagedPrefixList (37). **Nineteen AWS services now at 100%.** **Boyscout fixes (BUG-2205):** a focused audit of the older EC2/RDS/CloudFront core found 3 real silent-fallback / wrong-error bugs (verified against real AWS error codes, not guessed) — CreateRouteTable fabricated a `10.0.0.0/16` CIDR for a non-existent VPC instead of `InvalidVpcID.NotFound`; AssociateAddress silently accepted a non-existent InstanceId instead of `InvalidInstanceID.NotFound`; CreateNatGateway returned the wrong `InsufficientFreeAddressesInSubnet` (and left VpcId empty) for a missing subnet instead of `InvalidSubnetID.NotFound`. All three now reject invalid input with the real error (verified no test regressions across the full EC2 suite). One CloudFront-agent audit finding (CreateStreamingDistributionWithTags 400) was fixed in the same PR. All spec-validated (0 divergences) + real SDK + `aws` CLI. |
| ~~2204~~ | P2 | AWS sim — drive API Gateway v1+v2 to 100% + ratchet EC2/RDS/CloudFront/CloudWatch Logs + boyscout audit | conformance | Raised ~244 ops in two rounds (5+2 agents, each owning a distinct service file). **Both API Gateway services to 100%:** v1 99→124/124 (Update* PATCH ops, tags, import/put-rest-api, flush-stage, test-invoke, domain-name access associations), v2 61→103/103 (developer portals + products + pages, routing rules, UpdateApi/UpdateApiMapping, ResetAuthorizersCache). **Big ratchets:** EC2 389→448 (Transit Gateway multicast/metering/policy-table/route-table-announcements + IPAM policies/BYOASN/prefix-list-resolvers/external-resource-tokens/discovered-resources), RDS 101→150 (restores, reserved instances, blue/green deployments, zero-ETL integrations, tenant databases, shard groups, activity streams, backtrack, export tasks), CloudFront 104→152 (distribution tenants, connection groups, trust stores, resource policy, web-ACL associations, the ListDistributionsBy* family), CloudWatch Logs 73→104 (OpenSearch integrations, lookup tables, scheduled queries, transformers, import tasks, anomalies — deferred StartLiveTail/GetLogObject HTTP/2 streaming). **Seventeen AWS services now at 100%.** **Boyscout audit (BUG-2203):** a read-only sweep of the older core sim surfaced 12 candidates; fixed the 2 genuine ones — `time.Parse` error swallows in `cloudTrailObjectKey` (S3 key) and `cloudwatch_dashboards.go` ListDashboards (a malformed stored timestamp would silently produce a year-0001 value; now falls back to now). The other 10 were classified: 9 are unreachable `json.Marshal`-of-plain-`map[string]any`/struct error-drops (Go's `json.Marshal` cannot fail on those types — not real swallows), and the `restRegisterOp` "data race" is a **false positive** (registration is single-threaded at startup; the map is read-only during request serving, so concurrent reads are safe). All spec-validated (0 divergences) + real SDK + `aws` CLI. |
| ~~2202~~ | P2 | AWS sim — finish Glue + drive ElastiCache/AutoScaling/Route 53 to 100% + ratchet EC2/RDS/API Gateway/CloudFront/CloudWatch Logs | conformance | Raised ~320 ops in two rounds (5+5 agents, each owning a distinct service file — no collisions). **Three more services to 100%:** ElastiCache 41→75/75 (serverless caches/snapshots, global replication groups, security groups, update actions, migration), Auto Scaling 25→66/66 (attach/detach LBs/target-groups/traffic-sources, instance refresh, warm pools, lifecycle actions, real static enumerations), Route 53 33→71/71 (reusable delegation sets, CIDR collections, DNSSEC/key-signing-keys, traffic-policy instances, VPC-association authorizations). **Glue 258→264** — the 3 remaining ops (DescribeEntity/GetEntityRecords/ListEntities) query a real external SaaS connector API with no faithful local source, so 264/267 is Glue's honest max (not faked). **Big ratchets:** EC2 354→389 (Network Insights / Network Access Analyzer, Route Servers, Local Gateway routes — used `SearchLocalGatewayRoutes`, the real op, not the non-existent `DescribeLocalGatewayRoutes`), RDS 64→101 (DB proxies + endpoints + targets, IAM roles, security groups, certificates, automated backups, log files, copy groups, pending maintenance), API Gateway v1 62→99 (base-path mappings, documentation parts/versions, client certs, gateway responses, SDK/export/usage/account) + v2 44→61 (integration/route responses, model templates, import/export, config deletes), CloudFront 67→104 (field-level encryption, key-value stores, realtime log configs, streaming distributions, VPC origins, anycast IP lists), CloudWatch Logs 36→73 (account/query/resource policies, vended-log deliveries, anomaly detectors, index policies). **Fifteen AWS services now at 100%.** All spec-validated against the vendored `aws-sdk-go-v2` Smithy models (0 divergences) + real SDK + `aws` CLI; the go-build cache filled the disk mid-run (`go clean -cache` recovered ~58G). CI flake fixed: the existing ECS Docker test `waitTaskContainersGone` had a tight 30s container-teardown wait that starved under the heavier compute shard — raised to 90s, and split EC2 into its own CI CLI shard (the compute shard carried both ECS Docker integration and the now-huge EC2 metadata suite) with the sim-aws-cli job timeout 20→30m. |
| ~~2201~~ | P2 | AWS sim — ratchet EC2/Glue further + drive Lambda/EFS/STS/Scheduler/Cloud Map to 100% | conformance | Raised ~199 ops in two rounds (5+2 agents over isolated pre-wired files), all spec-validated against the vendored `aws-sdk-go-v2` Smithy models (0 divergences) + real SDK + `aws` CLI round-trips. **EC2 270→354:** Capacity Reservations + EC2 Fleets + Spot (38 — reservations/fleets/spot-fleets settling active, scheduled instances, host reservations), Dedicated Hosts + Image/Snapshot attributes + Instance Event Windows + VPC ClassicLink/endpoint-connections/block-public-access (46). **Glue 194→258:** ML-task runs + crawler/column-stats schedules + materialized-view refresh + workflow-run properties + connection types (33), custom entity types + usage profiles + Identity Center config + schema-version metadata + source-control sync + code-gen (GetMapping/GetPlan/GetDataflowGraph) (32 — deferred 3 SaaS-connector entity-introspection ops that query a real external API, no faithful local source). **Five more services to 100%:** **Lambda** 62→85/85 (capacity providers, durable executions, scaling config, GetFunctionConfiguration/UpdateFunctionCode, **InvokeWithResponseStream** with real `vnd.amazon.eventstream` framing the SDK decoder reassembles — no fake framing), **EFS** 29→31, **STS** 4→11 (GetFederationToken/AssumeRoleWithSAML/AssumeRoot/… minting real temp creds), **EventBridge Scheduler** 9→12 (tag ops, resolving a `/tags/{arn}` mux-overlap with Amplify via a faithful in-band SigV4-service discriminator), **Cloud Map** 16→30. **Twelve AWS services now at 100%.** Same pre-wired-stub pattern (5 sub-registrar stubs) for zero-collision parallel agents; fixed 4 staticcheck QF1012 (WriteString(Sprintf)→Fprintf) at integration. |
| ~~2200~~ | P2 | AWS sim — ratchet up EC2 + Glue (the two biggest floors) | conformance | Raised the two largest service floors by ~248 ops in two rounds (4+3 agents over isolated pre-wired sub-registrar files), all spec-validated against the vendored `aws-sdk-go-v2` Smithy models (0 divergences) + real SDK + `aws` CLI round-trips. **EC2 122→270:** Transit Gateway (42 — gateways, route tables, VPC/peering attachments, routes, associations/propagations, prefix-list refs, multicast/connect), IPAM (31 — IPAMs, scopes, pools with real CIDR carving, allocations, resource discoveries), Site-to-Site + Client VPN (35 — customer/VPN gateways, VPN connections with two IPsec tunnels, Client VPN endpoints/routes/auth/target-networks), Verified Access + Traffic Mirroring (41 — instances, trust providers, groups+endpoints with policies, mirror targets/filters/sessions). **Glue 102→194:** ML Transforms + Data Quality + column-statistics tasks (34 — rulesets, eval/recommendation runs settling synchronously with honest empty results), Interactive Sessions + Statements + Dev Endpoints + Blueprints (26), Catalog + Table Optimizer + BatchGet families + zero-ETL Integrations (32). **Validator fix (FP, BUG-2200a):** the runtime spec-shape validator applied the Smithy `jsonName` trait for awsJson1.1 services, but awsJson codegen ignores `jsonName` — verified against the aws-sdk-go-v2 Glue deserializer, which reads `case "SparkConnect"` for the `GetSessionEndpointResponse.SparkConnect` member despite its `jsonName: "SPARK_CONNECT"`; the validator now accepts a field under either the member name or its jsonName (REST protocols use jsonName, awsJson uses the member name), still flagging genuinely-unknown fields. Used a pre-wired-stub pattern (7 empty sub-registrar files + their registerEC2/registerGlue calls created up front) so the parallel area agents each edited only their own file with zero shared-file collisions. |
| ~~2199~~ | P2 | AWS sim — drive Batch/CloudTrail/CodeBuild/WAFv2/ECR to 100% | conformance | Drove **five services to 100% conformance** (~138 ops) in a 5-agent fan-out, all spec-validated against the vendored `aws-sdk-go-v2` Smithy models (0 divergences) + real SDK + `aws` CLI round-trips: **Batch** 24→45/45 (consumable resources, service environments, service jobs, quota shares, GetJobQueueSnapshot), **CloudTrail** 23→60/60 (CloudTrail Lake event data stores + queries run over the recorded events, dashboards, imports, federation, resource policy→IAM gate, org delegated admin), **CodeBuild** 22→59/59 (build batches, compute fleets, sandboxes with real local-process command execution, webhooks, report test-cases/coverage/trend, resource policy→IAM gate), **WAFv2** 32→55/55 (API keys, real WCU `CheckCapacity` computation, the real AWS Managed Rules catalog, managed rule sets, permission policies, mobile SDK releases), **ECR** 38→58/58 (repository creation templates, registry scanning config, signing config, pull-time update exclusions, pull-through-cache update/validate, account settings, image referrers). All five floors now equal their Smithy model max — **seven AWS services at 100%** (these five + KMS + ELBv2). Honest empties where the sim has no backing infra (ECR signing status / image referrers, WAFv2 traffic stats, CloudTrail digest public keys) — shaped exactly per the SDK models, never faked. **Boyscout fixes (CI failure + flakiness/timeouts):** CloudTrail `LookupEvents` ordered same-second events by a *random EventId*, so under the larger event volume a just-recorded event could fall past the first page and a presence assertion (`TestCloudTrailLookupFilterKeysSDK`) flaked — replaced with a monotonic record-sequence (`CloudTrailEvent.Seq`, internal-only) used as the sort + pagination-cursor tiebreaker, making ordering deterministically chronological. The `sim (aws sdk)` job ran an unbounded `go test` (default 10m) under a tight 5-min step kill that named no test on hang — gave `sdk-test` an explicit parameterized `SDK_TEST_TIMEOUT` (clean named-stack on hang) and raised the step to 12 min for headroom as the suite grows; the edge CLI shard `-timeout` rose 420s→1200s (carried from #674). |
| ~~2198~~ | P2 | AWS sim — ratchet up RDS/Glue/Lambda/API Gateway/CloudFront/ElastiCache; complete KMS + ELBv2 | conformance | Raised eight services by ~165 ops in two 4/3-agent rounds, all spec-validated against the vendored `aws-sdk-go-v2` Smithy models (0 divergences) + real SDK + `aws` CLI round-trips. **Floors bumped:** RDS 40→64 (instance/cluster state, global clusters, event subscriptions, cluster endpoints, parameter detail), Glue 78→102 (table versions, real partition indexes, column statistics, resource policy→IAM gate, schema versions), Lambda 37→62 (event-invoke config, provisioned concurrency, code-signing configs, runtime management, layer permissions), API Gateway v1 28→62 + v2 23→44 (API keys, usage plans, models, authorizers, domain names, VPC links — caught the v1 `item`-singular jsonName + v2 lowerCamelCase wire shapes), CloudFront 52→67 (origin access identities, continuous deployment policies, monitoring subscriptions), ElastiCache 25→41 (snapshots, users/user-groups, parameter detail). **Now conformance-COMPLETE (catalogs emptied):** **KMS** 35→54/54 — the asymmetric-crypto surface implemented with **real Go stdlib crypto** (Sign/Verify via RSA-PSS/PKCS1v15 + ECDSA, GenerateMac/VerifyMac via HMAC, GenerateDataKeyPair, DeriveSharedSecret via ECDH — a real Sign round-trips through Verify and a tampered message fails), plus custom key stores, grants, and multi-region keys; **ELBv2** 36→51/51 — the mutual-TLS trust-store surface + the real predefined SSL-policy set. **Kinesis** stays 38/39 (the only gap is the HTTP/2 event-stream `SubscribeToShard` — not faked). RDS fixed a `GlobalClusterMember` list-element wire-shape bug. The gate now measures ~37 AWS services with steadily-rising floors and two more at 100%. |
| ~~2197~~ | P2 | AWS sim — ratchet up EC2/ECR/AutoScaling/CloudWatch Logs | conformance | Raised four floored services by ~62 ops, all spec-validated against the vendored `aws-sdk-go-v2` Smithy models (0 divergences) + real SDK + `aws` CLI round-trips: **EC2** 102→122 (network ACLs + entries, VPC peering with the pending→active transition, managed prefix lists, flow logs, egress-only internet gateways), **ECR** 26→38 (lifecycle-policy previews, repository/registry policies, image scanning with honest empty findings, replication config, image tag mutability, tags), **Auto Scaling** 13→25 (scaling policies with real capacity adjustment, scheduled actions, lifecycle hooks, instance health/termination), **CloudWatch Logs** 18→36 (metric filters + TestMetricFilter, subscription filters, export tasks, data-protection policy, legacy + ARN tagging, DeleteLogStream/DeleteRetentionPolicy). Built by a 4-agent fan-out over disjoint files (no cutoffs); agents were told not to create `zz_*.go` stubs. The gate now measures ~37 AWS services with higher floors. |
| ~~2196~~ | P2 | AWS sim — raise CloudTrail/CodeBuild/WAFv2 + ratchet up Glue | conformance | Raised four floored services by ~50 ops, all spec-validated against the vendored `aws-sdk-go-v2` Smithy models (0 divergences) + real SDK + `aws` CLI round-trips: **CloudTrail** 16→23 (ListTrails, insight selectors, CloudTrail Lake channels — plus two pre-existing bugs the new tests surfaced: UpdateTrail emitted a non-spec `HomeRegion`, and GetEventSelectors/GetInsightSelectors read `Name` instead of the wire `TrailName`), **CodeBuild** 9→22 (stop/retry build, report groups, reports produced for real from the buildspec `reports:` section, source credentials), **WAFv2** 28→32 (the logging-configuration ops, scope-keyed), **Glue** 52→78 (security configurations, workflows + runs, classifiers, user-defined functions, schema registry). Built by a 4-agent fan-out over disjoint files (smaller batch after the prior weekly-limit halt — no cutoffs this time); a stale `zz_tmp_glue_stubs.go` build-stub from earlier scaffolding was removed (it redeclared the now-real Glue handlers). The gate now measures ~37 AWS services with higher floors. |
| ~~2195~~ | P2 | AWS sim — ratchet up EC2/RDS/Glue/Lambda/Batch/API Gateway + add CloudFront | conformance | Ratcheted up six floored services with more high-value ops (all spec-validated against the vendored `aws-sdk-go-v2` Smithy models, 0 divergences): **EC2** 91→102 (VPC endpoints + AMI/placement-group/DHCP-options round-trips), **RDS** 25→40 (DB cluster snapshots, cluster parameter/option groups, read replicas, events, engine metadata), **Glue** 30→52 (crawlers, jobs + job runs, triggers, connections), **Lambda** 23→37 (versions/aliases, event-source-mappings, layers, concurrency, function URLs), **Batch** 19→24 (job queues, job definitions, jobs), **API Gateway** v1/v2 (deployments/stages, UpdateRoute). Plus **Amazon CloudFront** added to the conformance gate (52/167 via the REST registry). (Five of six implementation agents were cut off mid-run by a weekly usage limit; their compiling work was completed by hand — the missing Lambda tests written, and four cut-off test bugs fixed: Glue `GetTags` now resolves crawler/trigger ARNs, the EC2 AMI test verifies deregistration store-faithfully rather than via the synthesized-AMI `data.aws_ami` path, the EC2 DHCP test's malformed JMESPath corrected, and a Glue CLI test struct field added.) The gate now measures ~37 AWS services. |
| ~~2194~~ | P2 | AWS sim — ratchet-up the floored services (EC2/RDS/ElastiCache/Glue/Route53/EFS) + measure the restJson1 services | conformance | Two-part pass, every op grounded in AWS's own `aws-sdk-go-v2` Smithy models (the vendored `specs/cloud-api/aws/*.smithy.json.gz`) and verified by the runtime spec-shape validator (0 divergences) + real SDK + `aws` CLI round-trips — no guessing. **Part A — ratcheted up 6 floored services (+77 ops):** EC2 88→91 (VPC endpoints; the other ~33 networking ops were already present), RDS 13→25 (DB clusters, subnet/parameter groups, reboot, tags across all resource types), ElastiCache 13→25 (replication groups, subnet/parameter groups, reboot, tag rework), Glue 19→30 (Data Catalog database/table/partition CRUD + batch ops), Route 53 10→33 (health checks, traffic policies, VPC associations, query-logging configs, geo), EFS 13→29 (file-system/backup policies, replication, account preferences, resource + legacy tagging). Each via faithful control-plane CRUD on real stores, SDK + CLI tested, floors bumped. **Part B — measured the restJson1 services** by mapping their CloudTrail event sources in `restConformanceSources` + adding coverage floors: AWS Lambda (23/85), AWS Batch (19/45), Amazon API Gateway v1 (28/124) + v2 (22/103), Amplify (37/37, complete), EventBridge Scheduler (9/12 — `schedulerRecorded` now also records into `restRegisteredOps`). The conformance gate now measures **~36 AWS services** (15 exact-list + S3 + 20 coverage-floor). Also enhanced `scripts/check-simulator-tests.sh` to map a `cloudTrailRecordedREST("Op",…)` REST route to its named op (lowercase subresource paths like `/policy` are exercised by the SDK op, not the literal path) — a genuine hook improvement for all REST services. |
| ~~2193~~ | P2 | AWS sim — conformance coverage for the remaining query/REST services (EC2/STS/RDS/Glue/Route53/EFS/…) | conformance | Extended the service-conformance gate to **every remaining un-ratcheted AWS service** the sim registers, via two new measurement paths + a compact ratchet. **(1) Legacy query bucket:** `serviceRegisteredOps` now also reads the query router's unversioned `""` bucket, so services registered with the plain `Register` (Amazon EC2 — ec2Query, 88/769; STS 4/11; and others) are measured instead of silently reading 0. **(2) REST registry:** `cloudTrailRecordedREST("Op","<source>",…)` now records each static REST op into `restRegisteredOps` (keyed by CloudTrail event source); `restConformanceSources` maps a model shape → source so the gate measures path-based REST services — **Route 53** 10/71 and **EFS** 13/31 — the same way it reads the routers (S3 keeps its bespoke dynamic harness). **(3) Coverage-floor ratchet:** `serviceCoverageFloor` + `TestServiceConformance_CoverageFloor` lock the implemented-op *count* (must equal the floor) for the awsQuery/ec2Query giants and the REST services, where an exact missing-list of hundreds of gaps would bloat the catalog. 14 services added to the floor (EC2, STS, ECR, ElastiCache, RDS, AutoScaling, Glue, WAFv2, CloudTrail, CodeBuild, CloudWatch Logs, Cloud Map/ServiceDiscovery, Route 53, EFS). The conformance system now measures ~30 AWS services (15 exact-list + S3 + 14 floor). No new ops — measurement infrastructure only; `docs/SERVICE_CONFORMANCE.md` documents the three measurement paths + two ratchet shapes. |
| ~~2192~~ | P2 | AWS sim — CloudWatch/Organizations/SSM op completeness + conformance-ratchet expansion to 14 services | conformance | 114-operation completeness pass extending the service-conformance ratchet from 11 AWS services + S3 to **14 + S3**. **CloudWatch** 38/49 — closed 28 monitoring ops (alarm management: enable/disable actions, set-state, history, alarms-for-metric, composite alarms; metric streams; anomaly detectors; insight rules; GetMetricData; alarm mute rules; cross-resource tags) served on all three CloudWatch transports (rpc-v2-cbor for the SDK, awsJson for newer CLI, legacy query for older CLI); the 11 niche gaps (datasets/KMS, OTel enrichment, managed insight rules, metric-widget-image) ratcheted. **Organizations** 53/63 — rewrote the 2-op stub into a full stateful resource tree (a default org/root/management-account materialized at startup so `aws:PrincipalOrgID` keeps resolving): accounts, organizational units, policies (attach/detach, policy types, effective policy), handshakes, delegated administrators, service access, resource policy mirrored into the IAM gate, tags; the 10 niche gaps (GovCloud account, responsibility-transfer, effective-policy-validation) ratcheted. **SSM** 43/146 — closed 33 management ops (versioned documents, maintenance windows + targets/tasks, patch baselines, service settings, resource data sync) on top of the existing Parameter Store; the 103 execution-subsystem ops (associations, ops items, sessions, automation, inventory, compliance, nodes, commands) ratcheted. Each op faithful control-plane CRUD on real stores with SDK + CLI tests; the runtime spec-shape validator reports 0 violations across all new ops (a `MaintenanceWindowIdentity` field divergence was caught + fixed during validation). Also fixed a real build-blocker: `ssm_maintenance_windows.go` had a `_windows.go` GOOS build-constraint suffix that excluded it from the macOS/Linux build → renamed to `ssm_maintenance.go`. |
| ~~2191~~ | P2 | AWS sim — S3 op completeness + conformance-ratchet expansion (KMS/SFN/Kinesis/ACM/SecretsManager/AppAutoScaling) | conformance | ~85-operation completeness pass extending the service-conformance ratchet from 5 AWS services to 11 + S3. **S3:** closed 13 high-value ops (object ACL get/put, object lock-config/retention/legal-hold get/put, GetObjectAttributes, GetObjectTorrent, the V1 ListObjects, RestoreObject, UploadPartCopy) — REST sim with op-name composition + the restXML spec-validator gaining required-header disambiguation (UploadPartCopy vs UploadPart); coverage 78→**91/107**, the 16 remaining gaps (S3 Express directory buckets, bucket Metadata-table feature, ABAC, Object Lambda, S3 Select) ratcheted. **Step Functions** (37/37 — activities, versions/aliases, send-task, map runs, TestState/StartSyncExecution), **ACM** (17/17 — export/get/revoke cert, account-config, search), **Secrets Manager** (23/23 — resource policy mirrored into the IAM gate, rotation, staging labels, replication, batch-get), **Application Auto Scaling** (14/14 — scheduled actions, scaling activities) are now conformance-complete (0 gaps). **Kinesis** 38/39 (stream consumers, resource policy, shard merge/split, tags, stream mode — only the HTTP/2 SubscribeToShard event-stream locked). **KMS** 35/54 (key management: enable/disable/cancel-deletion, alias/description updates, rotation, import key material with a real RSA wrapping key, GenerateRandom — the asymmetric-crypto + CloudHSM custom-key-store surface locked). ELBv2 (36/51) added to the ratchet to lock its trust-store gaps. Each op faithful control-plane CRUD on real stores with SDK + CLI tests; the runtime spec-shape validator reports 0 violations across all new ops. |
| ~~2190~~ | P2 | AWS sim — ECS op completeness + fully-qualified naming rule | conformance / naming | Closed ECS's entire remaining operation surface (47 ops): capacity providers (Create/Update/Delete), task sets (Create/Describe/Update/Delete + UpdateServicePrimaryTaskSet), container instances (Register/Deregister/Describe/List/UpdateState/UpdateAgent + Submit*StateChange + DiscoverPollEndpoint), account settings (Put/PutDefault/Delete/List), attributes (Put/Delete/List), task protection (Get/Update), daemons (Create/Delete/Describe/Update/List + deployments/revisions + daemon task definitions), and service deployments (Describe/List/Stop/Continue + DescribeServiceRevisions + ListServicesByNamespace) — faithful control-plane CRUD on real stores; ECS's conformance ratchet drops to 0 gaps. Each op has an SDK test; ops with a public `aws` CLI surface also have a CLI test (the daemon ops + stop/continue-service-deployment are preview-only with no public CLI command, so SDK-tested only — the contract hook is satisfied by SDK coverage). Plus a naming rule (`AGENTS.md` § "Use proper, fully-qualified service and feature names"): the feature is **Amazon ECS Express Mode**, not "ExpressGateway" — renamed sockerless's feature-label test/helper names to *ECS Express Mode* while keeping AWS's real API operation/SDK-type spelling (`CreateExpressGatewayService`, `ExpressGatewayServiceConfiguration`, `create-express-gateway-service`) as the wire contract. |
| ~~2189~~ | P2 | AWS sim — S3/REST conformance harness + DynamoDB/EventBridge/SNS op completeness + IAM resource-ARN | conformance / authorization | Three-part completeness pass. **(1) S3/REST op-coverage harness** (`s3ImplementedOps`, `TestServiceConformance_S3Ratchet`) — S3 is REST, so its op is composed from method+path+subresource at request time; the harness drives `s3BucketOperationName`/`s3ObjectOperationName` over the method × subresource matrix to enumerate the implemented set vs the vendored Smithy model (78/107, the 29 gaps — S3 Express, Metadata tables, ABAC, Object Lambda, S3 Select, object ACL/lock — ratcheted). **(2) Op completeness:** every remaining **DynamoDB** (31: backups/PITR, global tables, resource policy mirrored into the IAM gate, Kinesis streaming, exports/imports, contributor insights), **EventBridge** (28: api-destinations, connections, endpoints, partner/consumer event sources, archive/replay), and **SNS** (23: platform apps/endpoints, SMS sandbox with a real Pending→Verified state, SMS attributes/opt-out, data-protection policy) operation — faithful control-plane CRUD, each with SDK + CLI tests, ratchets to 0 gaps. **(3) IAM resource-ARN derivation** — the enforcement gate now derives the request's resource ARN for EC2 (volume/snapshot/instance), DynamoDB (TableName), Lambda (FunctionName/path), KMS, SecretsManager, Step Functions, Kinesis, so a policy scoped to one table/key/etc.'s ARN enforces (SDK test: dynamodb:PutItem allowed on the granted table, denied on another). ECS's 47-op surface is tracked in the ratchet for a focused follow-on. | Applied the conformance process (docs/SERVICE_CONFORMANCE.md) to the awsJson/query services: `service_conformance_test.go` loads each service's vendored Smithy model, computes registered-vs-model **operation coverage**, and **ratchets** the gap set per service (SQS/SNS/EventBridge/DynamoDB/ECS) — API completeness is now a measured, enforced number (`TestServiceConformance_Coverage`/`_Ratchet`). Then closed the high-value pub-sub gaps the ratchet flagged, each with SDK + CLI tests: **SQS** ChangeMessageVisibility(+Batch)/DeleteMessageBatch/Add+RemovePermission; **SNS** Confirm/Get/SetSubscriptionAttributes/Add+RemovePermission; **EventBridge** TestEventPattern/ListRuleNamesByTarget/UpdateEventBus. S3/REST op-coverage needs a route-enumeration harness (tracked follow-on); DynamoDB/ECS remaining ops are mostly out-of-scope and tracked in the ratchet. |

_BUG-2188 and earlier (~450 fixed) — full closed-bug ledger in `git log` + the merged PR descriptions. Major arcs: AWS IAM call-time enforcement & 3-cloud identity fidelity (2176–2188); Cosmos / Firestore / Bigtable / DynamoDB NoSQL compliance & emulator differentials (2141–2175); ECS Express Mode; and the runner-cell, sim-fidelity, and fallback/fake/dead-code audit rounds (≤2140)._

## False Positives

| Area | Finding | Why it is not a bug |
|------|---------|---------------------|
| azure sim `pagination.go::acrCatalogPage` (fidelity-pass-6) | An audit agent flagged `start = len(repos)` inside the loop as a logic error overlapping/skipping ACR catalog pages. | Not a bug: `repos` is sorted, so the first `name > last` match is the correct page start and breaks the loop; the `start = len(repos)` is only the final value when *no* repo exceeds `last` (all ≤ last), which correctly returns an empty page. Traced by hand against `[a,b,c]` with several `last` values — pagination is correct. |
| `backends/aca/azure.go::fakeCredential` | Returns literal `"fake-token"` against simulator endpoints. | Simulator auth does not validate bearer tokens. Production clients use `azidentity.NewDefaultAzureCredential`; this credential is only for simulator endpoint clients. |
| `cmd/sockerless-admin/api_observability.go::envOrDefault` | Returns canonical OTel resource-attribute name when unset. | This is a documented default-value helper, not an error-hiding fallback. |
| aws scheduler simulator — GetSchedule `awsvpcConfiguration` (issue #477) | Reported as `Target.EcsParameters.NetworkConfiguration.AwsvpcConfiguration` returning None. | The sim persists + returns all three fields under the **lowercase** `awsvpcConfiguration` wire key — exactly what aws-sdk-go-v2 `scheduler` serializes/deserializes (`object.Key("awsvpcConfiguration")` / `case "awsvpcConfiguration":`) and botocore/GetSchedule use. The consumer's CLI assertion queried capital-A `AwsvpcConfiguration`, which fails against real AWS too; the Terraform read path (Go SDK) is unaffected. Consumer closed it. |
| gcp simulator — Spanner mounted under `/spanner/v1/...` (BUG-1870, an audit-round-3 finding) | Flagged as a "fake-test config masking a wire-path bug" — the tests point the Spanner client at `baseURL+"/spanner/"` rather than `baseURL`. | NOT a bug — the `/spanner/` prefix is a **documented, load-bearing collapsed-port disambiguation**. Cloud SQL registers `registerCloudSQLPrefix(srv, "/v1")` and owns the canonical `/v1/projects/{project}/instances` path (its terraform-provider-google `sqladmin/v1` client uses it — see `sqladmin.go`); Spanner's real path is *identical*, so on the single-port sim it would collide and Go's ServeMux would panic at registration. Real GCP separates them by hostname; the sim disambiguates by prefix (the `gcpMountPrefixes` mechanism, explained in `spec_conformance_test.go`). The test endpoint `baseURL+"/spanner/"` is the per-service **coordinate** for the disambiguated service, and the client still constructs the canonical `/v1/...` path under it. The agent missed the Cloud SQL collision; "fixing" it would crash the sim. (A Host-header-based disambiguation would be marginally more faithful but is a larger change applying equally to Cloud SQL.) |
| bleephub harness — `bleephub/test/run-integration.sh:1164` `docker build -q -t bleephub-spawn-runner:local` lacks `--load` (audit-round-4 finding) | Flagged as the BUG-1824 buildx-cache-only class (image lands in cache, stale image silently spawned). | NOT a bug — that `docker build` runs INSIDE the harness container, whose `bleephub/Dockerfile` installs the **classic `docker.io` CLI** (no `docker-buildx-plugin`, line 69). The classic build path writes straight to the daemon image store regardless of any host buildx default, so there is no cache-only divergence — and a bare `--load` is an *unknown flag* to the classic client, so "fixing" it would break the harness. (The host-side `Makefile` builds DO need `--load` because they run with the host's buildx-aware CLI — fixed under BUG-1874.) |

## Class-of-Bug Rules

- No stubs, fakes, mocks, synthetic data, silent fallbacks, or degraded modes.
- Public simulator APIs must match real cloud APIs; local gateway/admin plumbing must not leak into those APIs.
- Backend host primitive must match the cloud service: ECS in ECS, Lambda in Lambda, Cloud Run in Cloud Run, GCF in Cloud Run Functions, ACA in ACA, AZF in Azure Functions.
- External test fixtures use real clients: official SDKs, vendor CLIs, Terraform providers, and `gh` for bleephub.
- Closed enumeration means full-table audit before claiming fixed.
- Reopens require a postmortem: what test passed but should have failed, what client path was missed, and what new canonical-client test catches it.
- List operations need paged-iterator tests.
- Stateful resources need state-machine assertions.
- Shared stores must snapshot values at their boundaries; maps, slices, and pointers cannot escape as mutable aliases to store-owned state.
- Missing-tool/dependency skips are bugs; required tools are installed by the harness or fail loud.
- Mux pattern overlap is a recurring simulator bug class; run the overlap scanner when adding routes.
| 1928 | backends (FaaS) — `NetworkSettings.IPAddress` empty | **Honest, not synthetic.** Lambda / Cloud Run / Azure Functions expose **no queryable per-container IP** (the platform abstracts it); empty is the faithful value and fabricating an IP would violate the no-synthetic rule. ECS populates a real ENI IP because AWS exposes one. Flagged by a sweep agent; resolved by analysis. |
