# Known Bugs

Status [STATUS.md](STATUS.md) - roadmap [PLAN.md](PLAN.md) - resume [DO_NEXT.md](DO_NEXT.md).

**1852 filed - 1808 fixed - 4 open - 7 false positives.**

Every CI failure, live-cloud failure, simulator fidelity gap, or discovered fake/fallback lands here before any fix attempt. Detailed closed-bug history lives in PR descriptions and `git log`.

## Open

| ID | Sev | Area | Pattern | One-liner |
|----|-----|------|---------|-----------|
| 1345 | P2 | azuread terraform provider | upstream blocker | The `hashicorp/terraform-provider-azuread` provider has no supported way to redirect Microsoft Graph API calls to a custom endpoint (no `microsoft_graph_endpoint` override). Feature request open upstream: https://github.com/hashicorp/terraform-provider-azuread/issues/1837. Entra provisioning via Terraform (`azuread_group`, `azuread_user`, `azuread_group_member`) cannot be tested against the sim until this is resolved upstream. |
| 1075 | P2 | live-cloud validation | unvalidated real cloud | Lambda is the only backend with a green live-cloud cell. Cloud Run Services, ACA Apps, AZF cloud-DNS, Lambda service-mesh, and ACA/AZF Azure AD remain unvalidated against authenticated real clouds. Do not mark these green without real cloud runs. |
| 1840 | P2 | gcp+azure sim — `SimCommand`/`SimImage`/`SimArchitecture` (gcp `cloudfunctions.go`) + `SimCommand` (azure `functions.go`) are sim-only fields on the real cloud resource model (Cloud Functions `ServiceConfig` / App Service `SiteConfig`), accepted off the wire — a CONTRACT VIOLATION (no real client produces them) | sim-only fake field a faithful sim must not have | Set ONLY by the sims' own invoke SDK tests, never a backend. **Deeper finding (2026-06-17 backlog pass):** the `Sim*` path is NOT os/exec — it runs a real `SimImage` container via Docker so the SDK tests can verify invoke semantics (output, exit code) *without* staging an overlay-bootstrap image; the faithful image path (`invokeOverlayContainerHTTP`) requires the bootstrap HTTP server, so a plain image can't be invoked there. The backend always backs a Gen2 function with a Cloud Run service image → the image path; the `Sim*` (image-less) path is backend-dead. **Fix shape:** remove the fields + the image-less `Sim*` branch (backend-safe), and move invoke-semantics coverage to the gcf/azf integration cells (which already exercise the real overlay invoke) — i.e. drop the lightweight `TestCloudFunctions_Invoke*` SDK tests rather than rebuild overlay infra in an SDK unit test. Touches the gcf/azf invoke path, so verify the cells stay green. |
| 1845 | P2 | cloudrun backend — `networkServices sync.Map` (`server.go:51`) is authoritative local state (the sole record of which service containers re-bundle as sidecars into later runner stages) that is never rebuilt from cloud in `recovery.go`/`cloud_state.go` | stateless-invariant violation (extra local authority) | After a backend restart `serviceMembersOfNetwork` returns nil and sidecar wiring silently breaks. The isolation script only greps `Store.Containers` writes, so it misses this. **Fix shape:** tag each tracked service container's Cloud Run Service revision with its network id (a label) at `trackNetworkService`, and reconstruct the map from those labels in recovery — the same pattern as the other stateless reconstructions. **Regression note:** this is on the green cloudrun/gcf network-pod (`services:`) path, so it must be verified against the bleeplab/bleephub cloudrun cell, not just unit tests. Found by the 2026-06-17 backend audit. |
## Recently fixed (cont.)

| ID | Sev | Area | Pattern | One-liner |
|----|-----|------|---------|-----------|
| ~~1852~~ | P2 | admin UI — `HTTPSGatewayCard` fabricated plausible-looking per-cloud gateway endpoint URLs + a CA path when the `/api/v1/https-gateway` query returned nothing/errored, so an operator saw invented infra coordinates as authoritative | fabricated runtime coordinates on a failed/absent query | Fixed: the per-cloud endpoints and CA path are runtime values — they now render `—` when the gateway info is unavailable instead of hardcoded `https://<cloud>.sockerless.localhost:8443` / `.sockerless-state/…/root.crt`. The make-target `commands` fallback stays (fixed documentation, not fabricated state); the StatusBadge already showed `unknown`. Found by the 2026-06-17 UI audit. |
| ~~1851~~ | P2 | docker frontend UI — `App.tsx` had no error state; on a failed health/status/metrics fetch the spinner cleared and the page rendered a healthy-looking zeroed dashboard (`metrics?.x ?? 0`), hiding the failure | query error rendered as success-looking UI | Fixed: the status query's `isError` now renders an `InlineError` (with a Retry) instead of the dashboard, and the metric cards show `—` rather than `0` when metrics are unavailable — so a failure is loud, never a fake "0 requests / 0 goroutines" healthy state. Every other dashboard in the repo already surfaced `InlineError`; this was the outlier. Found by the 2026-06-17 UI audit. |
| ~~1850~~ | P3 | aws sim (issue #590) — `DescribeSnapshots` (and `DescribeVolumes`) ignored `MaxResults` and never returned a `NextToken`, returning the full set in one page | pagination unmodeled | Fixed: the list form (no explicit ids) sorts by id for a stable cursor and pages via the shared `awsPageExplicit` (offset token), emitting `<nextToken>` when results remain — SDK paginators that loop on `NextToken` are now exercised. SDK test `TestEC2_DescribeSnapshotsPagination`. Closes #590. |
| ~~1849~~ | P2 | aws sim (issue #592) — ECS cluster-scoped ops (`DescribeTasks`/`ListTasks`/`StopTask`) didn't validate cluster existence, so an unknown cluster returned empty/`MISSING`/the task-not-found error instead of `ClusterNotFoundException` | missing cluster-existence check | Real ECS raises `ClusterNotFoundException` for every cluster-scoped op against an unknown cluster (so "cluster gone" is distinguishable from "empty cluster"/"task gone"). Fixed: added `ecsRequireCluster(w, ref)` (resolves name/ARN, `"" → default`) and call it first in the three handlers — `RunTask` and the cluster CRUD ops already validated. SDK test `TestECS_UnknownClusterRaisesClusterNotFound`. Closes #592. |
| ~~1848~~ | P3 | aws sim (issue #591) — `CreateVolume` succeeded without the required `AvailabilityZone`, silently defaulting the AZ | missing required-parameter validation | Real EC2 requires `AvailabilityZone` and rejects the request with `MissingParameter`. Fixed: return `MissingParameter` ("The request must contain the parameter availability_zone") when it's absent rather than defaulting. SDK test `TestEC2_CreateVolumeRequiresAZ`. Closes #591. |
| ~~1842~~ | P3 | aws sim — `ensureSimDefaults()` pre-seeded `vpc-sim`/`subnet-0123…` with a "so harnesses that hardcode those IDs work … else Cloud Map setup fails silently" framing, and two paths fell back to the hardcoded subnet ID | mis-framed faithful default-VPC + hardcoded-ID fallbacks | Re-assessed: a default VPC + default subnet IS faithful (real accounts are auto-provisioned with one, and a subnet-less RunInstances lands there) — the deterministic IDs are a sim convention like the fixed account ID, not a sockerless-keyed hack. Kept the seed but reframed the comment; replaced the hardcoded `subnet-0123…` fallbacks in `RunInstances` and `reconcileAutoScalingGroup` with `defaultVPCSubnetID()` (resolves the IsDefault VPC's subnet, faithful "default VPC" behaviour); dropped the "fails silently" framing. Harnesses that reference the default subnet are unaffected. Found by the 2026-06-17 sim audit. |
| ~~1843~~ | P3 | gcp sim — `computeFingerprint()` returned the constant `base64("sockerless")` for every compute resource and no handler validated an incoming fingerprint (optimistic concurrency unimplemented) | constant fake + set-but-never-validated | Fixed: `computeFingerprint()` now returns a fresh opaque per-write token (rotates on every mutation, no `"sockerless"` constant); `fingerprintMatches(current, supplied)` gates instance `setLabels`/`setTags` and disk `setLabels` with `412 conditionNotMet` on a stale `labelFingerprint` (empty is permitted, as the SDK omits it on first write). SDK test `TestCompute_Disks_SetLabelsFingerprintValidation`. Found by the 2026-06-17 sim audit. |
| ~~1841~~ | P2 | aws sim — Cloud Map DNS resolution was keyed on the sockerless-only `sockerless-container-id` ECS task tag (real AWS never produces it), a runner-aware shortcut | sockerless-aware sim special-casing | Fixed: `resolveTaskContainerForInstance` now matches the registered `AWS_INSTANCE_IPV4` attribute (`cmInstanceIPv4`) to the task carrying that private IP (`ecsTaskPrivateIP` from the awsvpc ENI attachment) — the faithful Cloud Map mechanism, no sockerless tag. Verified non-regressing by `TestECS_CrossTaskDNS` + `TestECS_MultiServiceDNS` (the green ECS service-discovery path). Found by the 2026-06-17 sim audit. |
| ~~1846~~ | P3 | backend default-param + dead-code batch | defaulted-on-invalid behaviour + reachable-dead code | Fixed. **Default-param:** added `core.ValidateDurationEnvs(...)` + `core.ValidateJobTimeoutEnv()` (error if a `SOCKERLESS_POLL_INTERVAL`/`SOCKERLESS_LOG_TIMEOUT`/`SOCKERLESS_JOB_TIMEOUT_SECONDS` env is set-but-malformed) and call them at the top of every backend's `Config.Validate` so a typo fails startup loudly instead of silently reverting to a default. **Dead code:** removed cloudrun `maybeAutoRemove` no-op + fixed the stale `pollExecutionExit` comment; removed the ecs `ListTagsForResource(ResourceARN: nil)` guaranteed-fail round-trip; removed the `_ =` pins in cloudrun `runner_pattern.go`/`network_pod.go`; lambda `replay.go` now logs the `ListTags` error loudly instead of folding it into "not managed". |
| ~~1844~~ | P2 | backend error-swallowing batch — cloud failures dropped so a Docker API op reported success while orphaning cloud state or fabricating an exit code | swallowed cloud errors / fabricated success | Fixed. **PodRemove** (cloudrun/gcf/ecs/lambda): removes ALL members then returns `errors.Join(...)` of any failures — a failed member delete no longer reads as a successful pod removal. **aca NetworkRemove**: surfaces a `cloudNetworkDelete` failure (orphaned Private-DNS zone / NSG) after removing the local metadata. **core ContainerWait**: logs the `CloudState.GetContainer` error loudly (no longer conflated with not-found; the local-store fallback stays for the docker passthrough) and the final fully-lost branch reports `-1` (unknown) instead of fabricating exit `0`. **ecs stats**: a CloudWatch `GetMetricData` error now propagates instead of returning all-zero metrics (the "new task, no data yet" case is the successful-empty path, untouched). **lambda cloud_state**: a `GetFunction` error is logged loudly instead of dropping a pod-managed function from `docker ps`. |
| ~~1847~~ | P2 | gcp sim Cloud Build — the build step ran the user's `docker build` verbatim, which on a docker-container buildx-default host leaves the image in the build cache only, so the next push step fails `image not known` (the BUG-1834 azure→gcp twin) | buildx-default host: `docker build` caches, doesn't load to the store | The azure ACR-Tasks sim got the buildx/legacy fix (BUG-1834); the gcp Cloud Build sim's `runDockerStep` had the same latent bug (surfaced by the audit re-running `TestCloudBuild_FaithfulBuildPush` on a buildx-default mac). Fixed the same way: a `build` step routes through `docker buildx build --load` when `dockerBuildxAvailable` (loads to the daemon store for every driver) else plain `docker build` (legacy, store-native); other steps run verbatim. CI (docker, loopback-trusted) and the harness cells (legacy docker.io) were already green; this fixes the buildx-default-host path. (The SDK test's residual local-podman `http: server gave HTTP response to HTTPS client` push error is the same environmental insecure-registry limitation as the azure ACR SDK test — CI-green.) Found by the 2026-06-17 sim audit. |
| ~~1839~~ | P1 | cloudrun backend — `resolveExecutionState` fabricated `ExitCode: 0` when `GetExecution` failed (after the job reference shows the execution completed), so a job that actually exited non-zero was reported as **success** | fabricated success exit code on a cloud-query failure | The other FaaS backends return an error/unknown on this path; cloudrun was the outlier minting exit 0 — the precise hazard `core/handle_containers.go` warns against. Fixed: on a `GetExecution` error with a completion stamp, report `ExitCode: -1` (completed, exit code unknowable) so `docker wait` surfaces it as a failure rather than masking it (a transient pre-completion fetch error stays `created`). The path only triggers on a cloud-query error, so the green cells' happy path is unaffected. Found by the 2026-06-17 backend audit. |
| ~~1838~~ | P3 | azure sim — five dead-variable linter-pins (`_ = x`) that computed/captured a value only to discard it, hiding functionally-dead code | dead-var `_ =` silencers | Removed: `postgres_flexible.go` + `cache_redis.go` unused `prefix` (the `Filter` recomputes the key), `storage_dataplane.go` `q` captured but only `ok` used (→ `_, ok :=`), `files.go` the `file_data` store created-but-never-used (the Files data-plane is host-filesystem-backed via `SIM_AZURE_FILES_DATA_DIR`, not this store), `keyvault.go` redundant `_ = rec` (already read in the guard above). Found by the 2026-06-17 sim audit. |
| ~~1837~~ | P2 | azure sim — VNet DELETE swallowed the real-execution subnet-teardown error (`_ = azureDeleteRealSubnet(...)`) while the adjacent vnet-teardown surfaced its error, so a failed subnet delete leaked host network state silently | swallowed real-exec error, inconsistent with sibling | Fixed: surface the subnet-delete error as a `503 OperationNotAllowed` exactly like the vnet-delete path. Found by the 2026-06-17 sim audit. |
| ~~1836~~ | P2 | gcp sim — `gcsObjectBytes` returned `nil` on an on-disk payload read failure (object metadata exists but the file is unreadable), and the caller wrote an **empty 200 body** instead of a 500 — a read failure masquerading as a zero-byte object (also fed cloudbuild's source fetch) | silent empty-body on read failure | Fixed: `gcsObjectBytes`/`GCSObjectBytes` now return `([]byte, error)`; the GET/compose/copy/download handlers 500 on a read error and cloudbuild surfaces the real fetch error (it previously reported "not found" for a read failure). A legitimately empty object still reads as zero-length with no error. Found by the 2026-06-17 sim audit. |
| ~~1835~~ | P2 | azf backend cloud-dns — `startCloudDNSSite` keyed overlay-vs-raw deploy on `OpenStdin` (a gitlab-runner-only signal), so the GitHub `actions/runner` container-job topology was mis-handled: the exec-driven `container:` job (not OpenStdin) was deployed as a raw image → no reverse-agent → `docker exec` of each step `exit 126`; and a `services:` container was mis-deployed as the overlay → its workload never ran → unreachable | wrong discriminator for "service (run raw image) vs exec/attach-driven (needs the reverse-agent overlay)" under cloud-dns | The GitLab cloud-dns path used `OpenStdin` to pick overlay (the gitlab-runner attach runner) vs raw (a `services:` redis). The bleephub GitHub cell broke both ways: TEST 12's `container:` job is exec-driven but NOT OpenStdin (the runner overrides its entrypoint to a keepalive and `docker exec`s steps) → deployed raw → "no reverse-agent session" → exit 126; TEST 13's `services:` nginx was first mis-classified the other way once the discriminator was widened, because the check read the post-merge base entrypoint/cmd labels — and azf ContainerCreate merges the **image's** default entrypoint/cmd into config when the client gives none, so nginx (image `ENTRYPOINT/CMD`) looked "overridden". Fix: derive `serviceLike` from the ORIGINAL client request (no client entrypoint/cmd override AND not OpenStdin), recorded at ContainerCreate **before** the image-config merge into a dedicated `labelServiceLike` (matches aca's pre-merge `serviceLike`); `startCloudDNSSite` reads that label — a serviceLike container deploys its raw image and runs it (started on the VNet by swift integration, BUG-1831 path), everything else deploys the overlay and is invoked so the in-site reverse-agent registers (`invokeFunctionAsync` blocks for the agent, no fallback). |
| ~~1834~~ | P2 | azure sim ACR Tasks build — the overlay `docker build` hardcoded the buildx-only `--load` flag, which the legacy docker builder (the `docker.io` package CLI, no buildx plugin) rejects with `unknown flag: --load`, so the ACR Task failed and the aca/azf `container:` job's `ContainerCreate` returned 500 | `--load` is buildx-only; hardcoding it broke every host whose `docker build` is the legacy builder | The bleephub GitHub `actions/runner` aca cell failed TEST 12 (`container:` job): the sim's ACR Tasks overlay build ran `docker build --load …`, but the harness container ships `docker.io` (Docker 29.1.3) with **no buildx plugin** (`docker buildx` = unknown command), i.e. the legacy builder, which rejects `--load` → `exit status 125: unknown flag: --load` → `ContainerCreate` 500. The `--load` was added (BUG-1824 class) for a buildx-default host where the docker-container driver leaves the result in the build cache only; it is wrong to hardcode because the legacy builder writes to the daemon store natively and has no `--load`. Fix: probe `docker buildx version` (`dockerBuildxAvailable`); when buildx is present use `docker buildx build --load` (loads to the store for every driver), otherwise plain `docker build` (legacy, store-native, no `--load`). The bleeplab cells (buildx-default host) stay green; the legacy-builder harness now builds. (The bleeplab cells previously passed through the harness container only because the overlay was cached from a pre-`--load` build; the bleephub Makefile wipes its data dir, forcing a real build that exposed it.) |
| ~~1833~~ | P2 | azure sim — the App Service swift-VNet-integration response returned its resource `id`/`type` built from the *operation* path (`.../sites/{name}/networkConfig/virtualNetwork`, type `Microsoft.Web/sites/networkConfig`) instead of the canonical *config* sub-resource id real Azure returns, so terraform-provider-azurerm's `azurerm_app_service_virtual_network_swift_connection` apply failed `ID was missing the 'config' element` | response `id` used the operation-path segment `networkConfig`, but the resource's canonical ARM id (which the provider parses via `*read.Model.Id`) uses `config` | The swift connection's *operation* path is `PUT/GET .../sites/{name}/networkConfig/virtualNetwork`, but the returned resource `id` is the canonical config sub-resource `.../sites/{name}/config/virtualNetwork` (type `Microsoft.Web/sites/config`) — a standard Azure App Service quirk (the `Microsoft.Web/sites/config` child `virtualNetwork` is reached via the `networkConfig/` action path). terraform-provider-azurerm's Create does `parse.VirtualNetworkSwiftConnectionID(*read.Model.Id)` then `d.SetId(...)`, and that parser requires a `config` segment, so the sim's `networkConfig`-shaped `id` was rejected (`ID was missing the 'config' element`) — the apply failed. The #587 SDK test missed it (it asserted only `subnetResourceId`, not the resource `id`). Fix: the sim's swift response `id` is now `…/config/virtualNetwork` and `type` is `Microsoft.Web/sites/config` (the operation path stays `networkConfig/virtualNetwork`). The SDK + CLI tests now also assert the returned `id` carries `/config/virtualNetwork`, so the regression is caught in the (more widely-run) `sim (azure)` job, not only the gated `tf (azure)` job. |
| ~~1832~~ | P2 | azure sim — a subnet delegated to `Microsoft.Web/serverFarms` dropped the delegation's `actions` array on read-back, so `azurerm_subnet` with a `service_delegation` block was non-idempotent (a second `terraform plan` showed drift) | dropped writable field — `SubnetDelegation.Properties` had no `Actions []string`, so the round-trip lost `properties.delegations[].properties.actions` | Adding an App Service swift-integration Terraform test surfaced it: terraform-provider-azurerm sends `delegation { service_delegation { name = "Microsoft.Web/serverFarms"; actions = ["Microsoft.Network/virtualNetworks/subnets/action"] } }`, and `actions` is `Optional+Computed`, so the provider writes the API's returned value to state — the sim returned an empty `actions`, so the next plan wanted to re-add the action (exit 2 / drift). Fix: added `Actions []string` to `SubnetDelegation.Properties` so the subnet PUT→GET round-trips `properties.delegations[].properties.actions` verbatim, matching real Azure (which echoes the delegation's actions). Covered by the new `azurerm_subnet` delegated-subnet + `azurerm_app_service_virtual_network_swift_connection` in the terraform-tests apply/destroy stack. |
| ~~1831~~ | P2 | azure sim — App Service regional VNet integration (`PUT .../sites/{name}/networkConfig/virtualNetwork`) force-started a workload container for any non-HTTP-bootstrap site, so VNet-integrating a site that has no container image (a plain Terraform `azurerm_linux_function_app`) returned `500 site "…" has no container image` | the swift handler used VNet integration as an unconditional run trigger; faithful only when the site actually has a container to attach | The swift PUT branched `if !hasAzureFunctionHTTPBootstrap(site) { ensureStarted(site) }` to start a raw `services:` container (e.g. redis) — but a plain function app with no `linuxFxVersion` image has nothing to start, and `startRawServiceLocked` returns `site "…" has no container image`, failing the PUT. Real Azure VNet integration is a pure networking-config operation; it does not require or start a container. Fix: gate the start on the site actually having a container image (`!hasAzureFunctionHTTPBootstrap(site) && siteContainerImage(site) != ""`) — an imageless site just records the VNet config (faithful), while a raw service site (redis, which has an image) still comes up exactly as before, so the azf cell is unaffected. (The remaining timing nuance — a container site starts at swift-PUT rather than at site-create — is pre-existing and orthogonal; tracked separately, not exercised by the runner cells.) |
| ~~1830~~ | P3 | CI — the `sim (aws sdk)` job's "Run AWS SDK tests" step timed out (`##[error]…timed out after 5 minutes`) on PR #587 even though the suite **passed** (`ok …simulator-aws-sdk-tests 182.505s`) | the single step did build **and** the ~182 s real-exec suite in one 5-min window; a cold recompile of the large AWS-SDK-importing test binary (the `**/go.sum` cache key in ci.yml busts the shared Go cache on any dep change) pushed build+test over 5 min | Not a masked test failure or hang — the suite passes and the slow time is legitimate real-exec work (`TestEC2_InstanceLifecycle` spins a real micro-VM, ECS tasks run real containers/netns). The trigger was this branch's azure `go.sum` change (armnetwork) busting the cache → cold compile. **Fix (not a test-timeout bump):** split the deterministic build into its own step — `Pre-build AWS SDK tests (warm build cache)` compiles the sim binary + the SDK test binary (`go test -c`, `CGO_ENABLED=0` to match `GO_ENV`) under a 6-min build budget — so the timed `Run AWS SDK tests` step reuses the warm cache and the 182 s run keeps its tight 5-min budget. The `docker client not initialized` / `subnet not found` ECS lines are fail-fast, tolerated logs inside passing tests (no retry, no slowness). |
| ~~1828~~ | P1 | azf cell — the full bleeplab GitLab runner pattern (build → artifact → `services:`) brought up on the azf backend; a multi-hurdle bring-up, now **GREEN** (all 4 cell tests) | each hurdle was a place azf's FaaS model diverged from the persistent-container runner lifecycle the cloudrun/gcf/aca cells already solved | **The whole azf docker-executor flow runs on App Service.** Hurdles fixed, in order: (a) the azure sim runs the App Service site container **persistently** (kept for the life of the site, not container-per-invoke); (b) `ContainerStart` BUG-1811 CloudState-fallback + re-invoke (per-stage `start→wait→stop→start` no longer 404s); (c) CloudState reports an OpenStdin runner container **running** across invokes (site tagged `sockerless-open-stdin`; `docker wait` still returns each stage's exit via WaitForExit); (d) gitlab-runner 18.11 uses the **attach** strategy — the `opts.Stdin` stdinPipe/buffered-invoke path takes precedence over the reverse-agent (`rt.mp==nil` → "no main process"; aca BUG-1821 analog), re-invoke no longer races the stdinPipe; (e) the persistent single-container site mounts its attached Azure Files shares (`siteAzureStorageBinds`) so the build container's `/builds` sees the helper's clone (aca BUG-1817 analog); (f) `ContainerCreate` carries image `ExposedPorts` + the network-pod defer path marks the deferred service `running` (services health-check). **Last hurdle — faithful cloud-dns service discovery (the redis `services:` job, `SOCKERLESS_AZF_NETWORK_DISCOVERY=cloud-dns`):** built end-to-end from real Azure primitives, the same backend code against the sim and real Azure (no sim-awareness). `NetworkCreate` provisions a `Microsoft.Network/virtualNetworks` + a subnet delegated to `Microsoft.Web/serverFarms` + a Private DNS zone linked to the VNet (`armnetwork`/`armprivatedns` clients). `ContainerStart` under cloud-dns deploys each container as its **own** site (raw image for a `services:` redis, overlay for the build), does App Service regional VNet integration (`WebApps.CreateOrUpdateSwiftVirtualNetworkConnectionWithCheck`) into the subnet, and registers each `--network-alias` as a Private DNS CNAME → the site's default hostname. The azure sim realizes these faithfully: a `Microsoft.Web/serverFarms`-delegated subnet is the App Service container fabric → a Docker user-defined network (not the IaaS netns); swift integration attaches the site's container to it; a CNAME → a site's default hostname is realized as an embedded-DNS alias on that site's container (`realizeCNAMEAsSiteDockerAlias`, the App Service analog of the ACA `realizeCNAMEAsDockerAlias`). A raw `services:` site runs its image directly on the VNet (no HTTP bootstrap; reached on its own port). **Sub-bug found + fixed:** the overlay injects the image's original entrypoint/cmd as `SOCKERLESS_USER_*` env, and `buildAZFAppSettings` propagated it onto the raw service site's app settings, making the sim mis-classify raw redis as an HTTP function (so it never started) — services now deploy with clean app settings (`buildAZFServiceAppSettings`, no `SOCKERLESS_*`). Result: the build site resolves `redis:6379` via the linked Private DNS / shared VNet and PING/SET/GET pass. SDK test `TestSDK_SiteVNetIntegration_RoundTrip` covers the swift endpoint. azf integration + FaaS-smoke suites stay green (their TestMain overlay builds gained `--load`, BUG-1824 class; the ACR-Tasks sim build also gained `--load`, so it reloads after an image prune on a `docker-container`-default buildx host). |
| ~~1829~~ | P2 | aca backend test — `TestACAGitLabRunnerAttachStdin` asserted the pre-branch attach-stdin semantics (raw stdin piped to the container's own command), but BUG-1822 moved aca to the gcf/cloudrun gitlab-runner pattern (captured attach-stdin runs under `/bin/sh`), and the test's `FROM scratch` overlay has no `/bin/sh` | test overlay had no shell + asserted old stdin→command behavior | After this branch made aca force `argv=[/bin/sh]` for captured attach-stdin (the faithful gitlab-runner pattern — the helper entrypoint ignores a raw script; mirrors gcf BUG-1813/cloudrun `postBootstrap`), CI's `test (azure backends)` failed `TestACAGitLabRunnerAttachStdin` with `attach stdout="" stderr=""`: the test built its App overlay `FROM scratch` (only the static bootstrap + `container-command` binaries, no `/bin/sh`), wrote a bare string `aca-gitlab-stdin-ok` to stdin, and expected it echoed by the image's `stdin-echo` command. With the new behavior the bootstrap execs `/bin/sh` — absent on scratch — so the invoke produced no output (the failure surfaced empty because the exec error isn't an HTTP-invoke error). Fix (mirror `TestGCFGitLabRunnerAttachStdin`): the test overlay is now `FROM busybox:latest` (real `/bin/sh`, like the shell-capable images real overlays build on), the container Cmd is `["sh"]`, and the captured stdin is a shell script `echo aca-gitlab-stdin-ok`; the overlay build also gains `--load` so it lands in the daemon store on a `docker-container`-default buildx host (BUG-1824 class). `TestACAFaaSE2ESmoke` (same overlay) stays green. |
| ~~1827~~ | P2 | AWS ECS sim (#583) — a Fargate task advertised CPU/Memory limits it never enforced on the launched container | container launched with no cgroup limits despite advertising them | ECS task metadata `/task` advertised `Limits:{CPU,Memory}` from the task def, but `sim.ContainerConfig` was launched with no `Memory`/`NanoCPU`, so the cgroup was unbounded (a 512/1024 task saw the host's full 4 vCPU / 9.9 GB — `memory.current` already above the advertised 1024 MiB with no OOM). Fix: added `MemoryBytes`/`NanoCPU` to the shared AWS `ContainerConfig` (applied to `HostConfig.Resources.Memory`/`NanoCPUs`) + `ecsContainerResourceLimits(td, cd)` translating the advertised ECS sizing — container-level cpu/memory refine the task size; CPU units (1024==1 vCPU)→nanoCPU, MiB→bytes. Also hardened `ebsRemoveDockerVolume` to no-op on a nil Docker client (defensive belt for #569, already fixed in 05217316 via host-path-backed process-mode volumes). SDK probes `TestECS_TaskDefinitionFidelitySDK` + `TestECS_ManagedEBSRunTaskProcessMode` green. |
| ~~1826~~ | P2 | azf backend — the bleeplab GitLab cell's ACR-Tasks overlay build failed `FROM simacr.azurecr.io/library/sha256:<digest>: no such host` | overlay base ref was the ACR-hostname rewrite, unpullable in the build env, instead of the original local ref | The azf overlay Dockerfile's `FROM` ran in the ACR-Tasks build environment, which resolves a local/registry-pullable ref but NOT the `<acr>.azurecr.io` hostname (the sim registry is reached via the `SOCKERLESS_AZURE_ACR_ENDPOINT` coordinate, not DNS — `simacr.azurecr.io` doesn't resolve there). `ContainerCreate` set `azfOverlaySpec.BaseImageRef` to `config.Image` *after* `ResolveAzureImageURI` rewrote it to `simacr.azurecr.io/library/<gitlab-runner-helper-digest>`, so the build tried (and failed) to pull from the ACR hostname. aca is green because it keeps the overlay base as the original ref (`ResolveAzureImageURIWithCache` passes a local digest through unchanged when no cache rule matches). Fix: capture the pre-rewrite `config.Image` as `overlayBaseRef` and use it for `BaseImageRef`, so the overlay `FROM` stays the locally-present helper ref (the ACR rewrite still applies to the non-overlay workload pull). |
| ~~1825~~ | P1 | aca backend — the bleeplab GitLab redis `services:` job (`FF_NETWORK_PER_BUILD`) hung at gitlab-runner's `prepare_script` because the runner-stage reverse-agent WebSocket exec never reached the in-container bootstrap | persistent backend→container WS half-opened under per-stage churn; runner stage now uses HTTP buffered-invoke like cloudrun/gcf | aca build/test jobs were green but the redis `services:` job hung at `prepare_script`. Root: the stage ran via the reverse-agent WS exec (`CollectExecWithStdin`) — `SendJSON(TypeExec)` returned on the backend but the frame never reached the bootstrap's serve loop, on the *verified-correct* connection (`rc.ws.RemoteAddr()` == registered remote; ruled out conn-mismatch, re-dial, replica restart, volume, redis reachability, gcs PreExec). A backend WS keepalive (ping + `SetPongHandler`/read-deadline on `agent.ReverseAgentConn`, added this branch) proved the connection was **fully half-open** server→client and deterministic for the services job (gvisor/podman port-reuse under the heavy per-stage container churn — 13/15 sessions dropped — misrouting the persistent backend→container direction). cloudrun + gcf pass the same job because they route runner stages through the bootstrap **HTTP buffered-invoke**, never a persistent backend→container connection. Fix: aca runner stages now POST the exec envelope over HTTP (`runACAStageInvoke` → `azurecommon.PostExecEnvelope`) to the App's ingress at `/`, reached via the `EndpointURL` coordinate with the App's `LatestRevisionFqdn` as the Host header (mirrors azf's `invokeURLForHost`). The azure sim implements **faithful ACA ingress** (`registerContainerAppsIngress`): a `srv.WrapHandler` that reverse-proxies any request whose Host matches an App's `latestRevisionFqdn` to that App's running replica on its configured ingress `targetPort` (8080) — exactly how real ACA routes an App's FQDN to the container, same virtual-host shape as the storage data-plane / Functions invoke; differs from real ACA only in the endpoint/port coordinate. (An earlier iteration used a sim-specific `POST /aca-app-invoke` endpoint — a bad smell, since real ACA has no such endpoint — replaced with the faithful ingress.) The reverse-agent WS stays for interactive `docker exec`. All 4 aca cell tests GREEN (incl. redis PING/SET/GET). The keepalive stays — it fixes a real all-FaaS infinite-hang bug independent of this. |
| ~~1824~~ | P1 | bleeplab runner harness — `make bleeplab-runner-docker-build` built to the buildx cache but never loaded the image into the Docker daemon, so every `docker run bleeplab-runner-int:local` silently ran a STALE image (backend changes appeared to have no effect) | `docker build -t` under the default `docker-container` buildx driver caches but does not `--load` | The default buildx builder on this host is the `docker-container` driver; `docker build -f … -t bleeplab-runner-int:local .` under it stores the result in the build cache only ("Build result will only remain in the build cache" warning) and does NOT update the daemon image the subsequent `docker run` uses. Iterating on a backend therefore re-ran whatever image was last loaded (observed: a 44-min-old image lacking every change made this session — keep-alive, re-invoke, diagnostics — so on-host debugging drew conclusions from dead code). Fix: `docker build --load …` in the `bleeplab-runner-docker-build` target forces the export into the daemon image store. |
| ~~1823~~ | P2 | aca backend — every gitlab-runner per-stage `start` redeployed the App (ACR-Tasks rebuild) instead of re-invoking, so the redis `services:` job churned the predefined helper and looped | no UseApp runner-pattern keep-alive / re-invoke (cloudrun parity gap) | After BUG-1822 made each prepare/stage script run correctly (exit 0), the redis `services:` job STILL looped: gitlab-runner cycles its predefined/build helper `start→wait→stop→start` on the SAME container once per stage, but ACA `ContainerStop` deleted the App on every stop and `ContainerStart` re-deployed it (a slow ACR-Tasks overlay rebuild) on every start — the per-stage redeploy churn made the runner give up and recreate the container in a loop. cloudrun's UseService cell (#585) is green on the identical job because it keeps the Service alive across stop and re-invokes the next stage with a fresh stdin pipe (`invokeRunningRunnerStage`) instead of redeploying. Fix (cloudrun parity): `ContainerStop` keeps the App alive for OpenStdin runner-pattern containers (read from the original PendingCreates container, since cloud-state reconstruction drops OpenStdin) and defers teardown to `ContainerRemove`; `ContainerStart`, for an OpenStdin container whose App is already deployed and which has a fresh stdinPipe, re-invokes `runACAInitialStdinStage` under a fresh wait channel rather than redeploying. |
| ~~1822~~ | P2 | aca backend — a gitlab-runner attach-stdin script ran under the image's own entrypoint instead of `/bin/sh`, so the redis `services:` prepare produced no output and looped | stdin-stage argv used image entrypoint+cmd, not a shell | After BUG-1821 stopped the gitlab-runner prepare-script attach from being mis-routed to the reverse-agent, the redis `services:` job's `Preparing environment` still produced no output and the runner kept recreating the predefined container (loop). Root cause (same class as gcf BUG-1813): `runACAInitialStdinStage` ran the captured stdin script under the container's resolved command (`acaInitialStdinArgv` → the image entrypoint+cmd / `SOCKERLESS_USER_*`), but the gitlab-runner helper's entrypoint reads stdin in its own protocol and silently ignores a raw shell script — so the prepare script never executed, the runner saw no output, timed out, and looped. Fix: when stdin is captured (the attach-stdin pattern), run it under `argv=[/bin/sh]` (mirroring gcf/cloudrun's `postBootstrap`); the no-stdin one-shot command path (BUG-1816 cache-init) still uses the container's own command. |
| ~~1821~~ | P2 | aca backend — `ContainerAttach` routed a gitlab-runner stdin script to the reverse-agent (no main process), so the redis `services:` job's prepare looped forever | reverse-agent attach precedence over the buffered-invoke stdin path | After BUG-1820, the bleeplab GitLab aca cell's redis `services:` job (`FF_NETWORK_PER_BUILD`) still looped in `prepare_executor`: gitlab-runner created the predefined helper on the per-build network, attached the prepare script, started it, got no output, then removed + recreated the container — 15+ times. Root cause (same class as gcf BUG-1812): gitlab-runner writes the prepare/stage script to the container's MAIN process stdin, but ACA `ContainerAttach` checked for a registered reverse-agent FIRST and, when present, routed the stdin attach to `BaseServer.ContainerAttach` — the reverse-agent never registers a main process (`mp==nil`), so it replied "no main process to attach to" and the script never ran. The App bootstrap registers a reverse-agent, and on a per-build network it registers *before* the runner attaches (unlike the build/test jobs, where the first attach beats the agent and took the stdinPipe path), so the services job always hit the broken route. Fix: the `opts.Stdin` stdinPipe/buffered-invoke path (`runACAInitialStdinStage`) now takes precedence over the reverse-agent routing; the reverse-agent is used only for exec and read-only attach. |
| ~~1820~~ | P2 | aca backend — `NetworkInspect`/`NetworkList` reported synthetic local-Store container membership (stale stopped containers), violating the stateless invariant | local `Store.Networks[].Containers` instead of cloud-truth | The bleeplab GitLab aca cell's redis `services:` job (job6, `FF_NETWORK_PER_BUILD`) looped forever in `prepare_executor` with `WARNING: Possibly zombie container … predefined is disconnected from network bridge`, never reaching `step_script`. gitlab-runner inspects the `bridge` network to verify a reused container isn't a leftover; the aca backend wrote each container into `Store.Networks[bridge].Containers` at `ContainerCreate` and **never removed it on stop/remove**, so `docker network inspect bridge` returned stale stopped containers — gitlab-runner repeatedly tried to disconnect these "zombies" but the backend kept reporting them, an infinite retry. This is forbidden synthetic local state (a stateless cloud backend must not own network membership). Fix: removed the synthetic membership write; `NetworkInspect`/`NetworkList` now populate `Containers` from cloud-truth via `CloudState.ListContainers` (only running Apps whose `NetworkSettings` reference the network), so a stopped container vanishes from membership immediately. Build + test jobs were unaffected (they don't use a per-build network); the services job needs accurate membership. |
| ~~1819~~ | P2 | azure sim — workload containers bind-mounted the share dir without an SELinux relabel, so on an SELinux-enforcing host the workload was denied access | bind mount missing the `:z` shared relabel | After BUG-1818 made the bleeplab GitLab aca cell's Azure Files share dir 0777, `get_sources` still failed `mkdir: cannot create directory '/builds/root': Permission denied`. The simulator runs the workload containers via the host engine; on an SELinux-enforcing host (a rootful podman-machine VM) a bind-mounted directory is denied to the container unless relabeled — confirmed: a one-off container reading the same path got `Permission denied`, but `--security-opt label=disable` or a `:z` relabel made it read+write. The simulator's `StartContainer`/`createAndStartContainer` set `HostConfig.Binds` directly with no relabel (gcp/cloudrun never hit this — their workspace syncs over the network via gcs-sync rather than a host bind). Fix: the azure sim now appends the shared SELinux relabel option `:z` to every workload bind (`selinuxRelabelBinds`), so the shares are accessible on SELinux-enforcing hosts; the option is ignored where SELinux is absent (Linux CI), so it's portable. |
| ~~1818~~ | P2 | azure sim — a materialized Azure Files share dir was created 0755 (umask), so a non-root workload couldn't write to the mounted share | `os.MkdirAll(…, 0o777)` masked by the process umask | After BUG-1817 gave the bleeplab GitLab aca cell a shared `/builds` Azure Files volume, `get_sources` failed `mkdir: cannot create directory '/builds/root': Permission denied` — the gitlab-runner helper runs as a non-root user and the share dir was `drwxr-xr-x root` (0755). `FileShareHostDir` created the share with `os.MkdirAll(dir, 0o777)`, but `MkdirAll` honors the process umask (typically 022), so the dir landed at 0755. A real Azure Files SMB mount is writable by the mounting container (CIFS default `dir_mode`/`file_mode` 0777). Fix: `FileShareHostDir` now `os.Chmod(dir, 0o777)` after `MkdirAll` (Chmod is not umask-masked), so the materialized share matches a writable Azure Files mount and the workload can create the workspace. |
| ~~1817~~ | P2 | aca backend — an ad-hoc named volume (gitlab-runner's `/builds` cache volume) was backed by per-container EmptyDir, not a shared Azure Files share, so the workspace wasn't shared across the job's containers | named volume mounted as per-revision tmpfs instead of a shared share | After BUG-1816, the bleeplab GitLab aca cell's `get_sources` cloned the repo into `/builds` (in the helper container) but `step_script` failed `can't cd to /builds/root/demo: No such file or directory` (in the build container). `/builds` is gitlab-runner's named cache volume; `VolumeCreate` already provisions an Azure Files share for it, but `resolveVolumeForName` (App spec volume builder) defaulted ad-hoc named volumes to the storage-backing registry default `BackingMemory` → `StorageTypeEmptyDir` (a per-revision tmpfs). So each container got its own empty `/builds`, and the helper's clone was invisible to the build container (and nothing landed in any Azure Files share on disk). A docker named volume is shared across the containers that mount it; EmptyDir violates that. The GitHub aca cell never hit this because its workspace is the operator-declared `runner-ws` SharedVolume (already azure-files). Fix: `resolveVolumeForName` now backs ad-hoc named volumes with `BackingAzureFilesEphemeral` (a shared share per volume name, matching what `VolumeCreate` provisions), reserving EmptyDir for true scratch; the helper and build container now share `/builds`. |
| ~~1816~~ | P2 | aca backend — a one-shot command container (gitlab-runner's `--rm` cache-volume permission container) deployed a long-lived App but never ran its command, so `docker wait` hung | App serves only the reverse-agent; the container command is never executed | After BUG-1815, the bleeplab GitLab aca cell's build job hung in `prepare_executor`: gitlab-runner creates a cache volume then runs a one-shot `--rm` "permission container" (`create` → `start` → `wait`) that chmods the mount and exits. The aca App path deployed the App (which runs the `sockerless-cloudrun-bootstrap` overlay → connects the reverse-agent → serves :8080) but, for a non-OpenStdin container, only called `waitForReverseAgentAfterStart` — it never ran the container's command, so the App stayed alive serving the agent and `docker wait` never returned. cloudrun runs such a container's command via `invokeServiceDefaultCmd`; aca only handled long-lived exec-driven job containers (GitHub) and OpenStdin per-stage script containers (gitlab build/helper). The permission container is not `--rm` and the App overlay moves the container's command into `SOCKERLESS_USER_*` env (clearing `Config.Cmd`), so neither AutoRemove nor a residual Cmd identify it. Fix: ACA `ContainerStart` runs the user command via the reverse-agent (`runACAOneShotCommand`, sharing `runACACommandViaAgent` with the stdin-stage path) for any started container that carries a user command which is NOT marked to run at App startup (`acaCommandRunsViaAgent`: has `SOCKERLESS_USER_CMD/ENTRYPOINT` but not the serviceLike `SOCKERLESS_RUN_USER_WORKLOAD=1`) and is not OpenStdin, then publishes the result and closes the wait channel. A one-shot command (cache-init) exits and unblocks `docker wait`; a long-lived keepalive stays up until the container is stopped, which closes the wait channel then. |
| ~~1815~~ | P2 | aca + azf backends — docker `/version` reported a hardcoded `amd64` arch | hardcoded host arch, ignores configured workload platform | Bringing up the **bleeplab GitLab cell on aca**, the build job failed at `prepare_executor` (`creating cache volume: set volume permissions: create permission container … Error response from daemon: ensure aca overlay image: … image not known`). Root cause: both `backends/aca/server.go` and `backends/azure-functions/server.go` hardcoded `Architecture: "amd64"` in the BackendDescriptor (the docker `/version` arch a docker-host client reads). On an arm64 deployment, gitlab-runner read `amd64` and selected the `x86_64` gitlab-runner-helper — which the harness never staged (it stages the arch-matched helper) — so the backend resolved it to a registry manifest digest the host engine doesn't have locally, and the ACA-Tasks overlay build's `FROM sha256:<x86_64-digest>` failed `image not known`. Same class as cloudrun BUG-1808 / gcf. Fix: derive the reported arch from `config.BuildPlatform` (`SOCKERLESS_AZURE_BUILD_PLATFORM`) via a shared `azurecommon.ArchFromPlatform` (mirror of `gcpcommon.ArchFromPlatform`), used by both Azure backends. |
| ~~1814~~ | P2 | gcf bootstrap — a reused function instance restored its persist (gcs-snapshot) workspace only once at startup, so a gitlab-runner job's `upload_artifacts` stage couldn't see the build container's output | persist volume restored once-at-startup, not before each invoke | After BUG-1813 the bleeplab GitLab gcf build-job went GREEN (clone + `apk add gcc` + compile + `./calc 6 x 7`), but the **test-job** failed with `exit 127` (`./calc: not found`) because the build artifact never uploaded: build-job's `upload_artifacts` reported `WARNING: calc: no matching files`. The gitlab-runner `/builds` workspace is shared across the job's containers via the gcs-snapshot ("persist") path — each container restores `/builds` from a shared GCS object and saves back after each step. The gcf function instance is **reused** across a job's predefined-helper stages (get_sources, then upload_artifacts on the SAME instance), but the bootstrap restored persist volumes only **once at startup** (it restored *sync* volumes before every invoke, but not *persist*). So the reused helper kept its stale get_sources workspace (cloned repo, no `calc`) and its post-stage save even **clobbered** the build container's fresh snapshot. cloudrun is green on the identical persist classification because `UseService` cold-starts a fresh instance per stage that restores at startup. Fix: the gcf bootstrap now `restoreAll(persistVols)` before every invoke (mirroring its sync-restore and its `saveAll`), so each reused-instance stage observes the latest cross-container `/builds` snapshot. Verified: build → test → integration all GREEN (`calc` artifact downloaded; arithmetic + sum-1..100=5050 pass). |
| ~~1813~~ | P2 | gcf backend — a gitlab-runner attach-stdin script was piped to the image's own entrypoint instead of `/bin/sh`, so `get_sources` ran but never cloned | invoke argv used image entrypoint+cmd, not a shell, for the captured script | After BUG-1811/1812 the bleeplab GitLab gcf cell's `get_sources` ran (exit 0) but produced no clone — `/builds` stayed empty (1024-byte gcs-sync snapshot), bleeplab logged **zero** git requests, and the helper printed only `gitlab-runner-build`'s `Running on … via …` banner. Root cause: the gitlab-runner docker executor attaches to the container and pipes each stage's shell script to the container process's stdin. The gcf invoke goroutine built `argv = Config.Entrypoint + Config.Cmd` (the gitlab-runner-helper image's `[dumb-init /entrypoint gitlab-runner-build]`) and passed the captured script as that command's stdin — but `gitlab-runner-build` reads stdin in its own protocol and silently ignores a raw script (banner, exit 0, no clone). cloudrun's `postBootstrap` already forces `argv=[/bin/sh]` whenever stdin is captured, so the inner shell executes the script; gcf didn't. Fix: in the gcf invoke goroutine, when stdin was captured (the attach-stdin pattern), override `invokeArgv = []string{"/bin/sh"}` so the bootstrap runs `/bin/sh` with the script on stdin — matching cloudrun. The image entrypoint+cmd still runs for the no-stdin (exec-driven / default-invoke) path. |
| ~~1812~~ | P2 | gcf backend — `ContainerAttach` routed a gitlab-runner stdin-script to the reverse-agent (no main process), so `get_sources` failed and the clone never happened | reverse-agent attach precedence over the buffered-invoke stdin path | After BUG-1811, the bleeplab GitLab gcf cell's `get_sources` stage failed with `sockerless: agent exec error: no main process to attach to`, and the subsequent `step_script` died with `can't cd to /builds/root/demo: No such file or directory` (the clone never ran). Root cause: gitlab-runner writes each stage's script to the container's MAIN process stdin via `docker attach`. The gcf `ContainerAttach` checked for a registered reverse-agent FIRST and, when present, routed the attach to `BaseServer.ContainerAttach` (the reverse-agent's attach handler) — but the reverse-agent never registers a main process (`mp==nil`; `ServeReverseAgentWithExecHooks` constructs the router with a nil main process, reverse mode carries only exec sessions), so the agent replied "no main process to attach to". The gcf **network-pod** bootstrap registers a reverse-agent for *every* member, so the second per-stage attach resolved the agent and broke; cloudrun is green only because its single helper never registers a reverse-agent, so its stdinPipe path always won. The stdin script always belongs on the buffered-invoke path (`stdinPipe` → `invokePodServiceMain` → POST to the Service, which runs it as the main process and returns the output envelope). Fix: in gcf `ContainerAttach`, the `opts.Stdin` stdinPipe path now takes precedence over the reverse-agent routing (the reverse-agent is used only for exec and read-only attach). |
| ~~1811~~ | P2 | gcf backend — `ContainerStart` only resolved from `PendingCreates`, so a gitlab-runner per-stage container re-start failed with NOT FOUND | missing cloud-state resolution fallback on restart | Bringing up the **bleeplab GitLab cell on gcf**, the gitlab-runner predefined helper container started successfully twice but the third `ContainerStart` failed with `ContainerStart: NOT FOUND in PendingCreates`, then the container was DELETE'd and gitlab-runner reported `No such container … failure_reason=runner_system_failure`. Root cause: gitlab-runner's docker executor does start→wait→stop→start cycling per stage on the SAME container ID, but the first `ContainerStart` removes the container from `PendingCreates` (the only transient local state a stateless backend keeps), and the gcf `ContainerStart` resolved **solely** from `PendingCreates` — so any subsequent restart of an already-deployed container missed. The cloudrun backend already handles this with a `ResolveContainerAuto` cloud-state fallback that re-adds the resolved container to `PendingCreates`; gcf lacked it. Fix: gcf `ContainerStart` now falls back to `s.ResolveContainerAuto(s.ctx(), ref)` (CloudState) when the container isn't in `PendingCreates`, re-adding it so the existing re-invoke flow runs the next stage — matching cloudrun. |
| ~~1810~~ | P2 | gcp sim — Cloud Run Service default-cmd invoke unreachable when the sim runs inside a harness container | sim dialed the host-published port on its OWN loopback | Bringing up the **bleeplab GitLab cell on cloudrun**, the gitlab-runner cache-volume "permission container" failed with `bootstrap not ready at http://127.0.0.1:<port>/: timeout after 30s` (surfaced by the BUG-1810 diagnostic below). Root cause: the sim's `postCloudRunServiceInstance` reaches the workload's bootstrap at `127.0.0.1:<hostPort>` — the port `StartHTTPContainer` published. That works when the sim runs directly on the host, but in the harness the sim runs **inside a container**, where the host-published port binds the *host's* loopback, not the sim container's — so the dial never connects (the reverse-agent still works because it dials OUTBOUND to `host.docker.internal`). bleephub never hit this: its github-runner job containers are exec-driven (reverse-agent), never one-shot Service invokes; the gitlab-runner one-shot permission container is the first to exercise this path. Fix: the sim now reaches the workload by its **bridge container IP:8080** (routable container-to-container from the harness) and falls back to `127.0.0.1:<hostPort>` (sim-on-host) — `firstReachableBase` races the candidates. Validated: the bleeplab cloudrun cell is GREEN across all 4 tests (build/artifact/`services:`). |
| ~~1809~~ | P2 | gcp sim — AR pull-through cache only hydrated `docker-hub` remote repos, not `gitlab-registry` | partial pull-through coverage | The cloudrun backend rewrites `registry.gitlab.com/<path>` → `<AR>/gitlab-registry/<path>` (gcp-common `image_resolve.go`), but the gcp sim's `hydrateOCIImageFromLocalDocker` only mapped `/docker-hub/` refs back to a local daemon image — a `/gitlab-registry/` manifest miss returned 404. So the **gitlab-runner-helper** image (the git-clone + artifact-transfer container, which cloudrun overlay-builds) failed to pull (`auth: unexpected status 404 from registry`). bleephub never hit it (github-runner doesn't use a gitlab-registry helper). Fix: `hydrateOCIImageFromLocalDocker` now also maps `/gitlab-registry/<path>` → the local `registry.gitlab.com/<path>` ref, mirroring the backend rewrite; the harness stages the arch-matched helper tag locally. |
| ~~1808~~ | P2 | cloudrun backend — docker `/version` reported a hardcoded `amd64` arch | hardcoded host arch, ignores configured workload platform | On an arm64 deployment, gitlab-runner reads the DOCKER_HOST's reported arch to pick its helper image and chose the `x86_64` helper (wrong arch) because `backends/cloudrun/server.go` hardcoded `Architecture: "amd64"` in the BackendDescriptor — unlike ECS, which derives it from the configured workload arch ("sockerless reports the cloud workload's architecture, not its own host"). Fix: derive the reported arch from `config.BuildPlatform` (`SOCKERLESS_GCP_BUILD_PLATFORM`, e.g. `linux/arm64` → `arm64`) via `archFromPlatform`. The cloudrun bootstrap-stdout-on-failure diagnostic (`start_service.go`: surface a failing default-cmd's output tail, not just its length) was added alongside, turning opaque permission-container exits diagnosable. |
| ~~1781~~ | P2 | backends lambda/gcf/azf — multiple containers sharing one pod assembled from cloud primitives (FaaS multi-container pods) | not-yet-assembled abstraction → assembled | Full pod semantics — including localhost / shared-loopback networking between members — on the FaaS backends, so GitHub `services:`/sidecar `container:` jobs and GitLab service containers run there. On investigation **lambda and gcf already deliver this**: lambda runs all pod members as chroot subprocesses of one supervisor in a single execution environment (one shared netns → `localhost` works); gcf co-deploys members into one multi-container Cloud Run revision (shared loopback + `/etc/hosts` alias→127.0.0.1). The remaining gap was **azf, which hard-rejected multi-container pods**. Fixed by assembling the pod as ONE App Service site with **sitecontainers** (the faithful native Azure multi-container primitive — `Microsoft.Web/sites/{}/sitecontainers`, one `isMain` container + N sidecars sharing a network namespace): the **sim** models the `sitecontainers` sub-resource (CreateOrUpdate/Get/List/Delete) and starts the main + sidecars sharing one netns on invoke (sidecar reachable on `localhost:<port>`); the **backend** replaces the two fail-fast rejections (`PodStart`, `ContainerStart`) with a network-pod materializer (mirroring gcf's `shouldDeferOrMaterializeNetworkPod`) that creates the site + sitecontainers — main runs the reverse-agent overlay, sidecars run their RAW service images (un-overlaid, so they don't bind the main's HTTP port) — and reconstructs pod membership from the site's member manifest in cloud-state (stateless). The **azf bootstrap** writes `SOCKERLESS_HOST_ALIASES` to `/etc/hosts` so a sibling resolves by name to the shared loopback. Proven end to end: a GitHub-`services:`-shaped pod (job + sidecar service) on the azf sim — the job reaches the sidecar both on `localhost:9099` and by alias `svc` (`TestAZFMultiContainerPodSharesLocalhost`), with sim SDK+CLI sitecontainers coverage (`TestSDK_AzureFunctions_MultiContainerSharesLocalhost`, `TestSiteContainers_CLI_CRUD`) + backend unit tests. Replaces the interim fail-fast rejections (BUG-1779). |
| ~~1805~~ | P2 | bleeplab GitLab ECS cell — a `services:` job's build container couldn't resolve the service alias (`redis`), so the gitlab-runner `services:` flow never completed | backend resolv.conf command-wrapper froze per-network DNS | After BUG-1804 made the service alias resolvable on the Cloud Map namespace network, a GitLab `services:` job's build container still couldn't reach `redis`. Root cause (verified on Podman, the local runtime): each network's DNS runs at its gateway and a container gets one nameserver per attached network, added as networks connect. The ECS backend wrapped the user's container command in a `/bin/sh` shim that rewrote `/etc/resolv.conf` to a STATIC snapshot at entrypoint time (to inject the namespace as a DNS search domain) — capturing only the VPC nameserver and dropping the namespace network's DNS that the runtime adds when Cloud Map connects the container afterwards. So `redis` never resolved, and the wrapper also mangled the user's argv (violating faithful command execution). Fix, respecting module boundaries: **remove the backend's resolv.conf command-wrapper** (the container's argv now runs verbatim; DNS is owned by the runtime), and have the **sim** realize each Cloud Map service name as BOTH `<service>` and `<service>.<namespace>` network aliases (verified both resolve), matching what the netns/`/etc/hosts` tier already provided — so a search domain is unnecessary. Validated end to end: the bleeplab GitLab ECS harness now runs a 3-stage pipeline whose integration stage `apk add redis` + `redis-cli -h redis` PING/SET/GET all succeed over the per-build pod network (TEST 4). The now-dead `searchDomainsForContainer`/`shellQuoteArgs` helpers + their tests were removed. |
| ~~1804~~ | P2 | Cloud Map (aws sim + ecs backend) — a container could resolve under only ONE DNS name, so a GitLab `services:` alias (e.g. `redis`) never resolved | one-name-per-container vs Cloud Map's one-instance-many-services | gitlab-runner attaches a `services:` container with a network alias (`redis`); siblings must resolve it by that alias. Two layers were incomplete vs real Cloud Map (where one instance may back several services → several DNS names → one IP, verified against the vendored `servicediscovery.smithy.json` model): **(sim)** the Docker-network DNS realization connected the task container to the namespace network with a single alias via a plain `NetworkConnect`, which Docker rejects on the second registration (`network is already connected`, verified by hand) — so only the first name resolved and the second `RegisterInstance` 500'd; `handleCMRegisterInstance` now stores the instance first, then re-attaches the container with the FULL set of service names it backs (disconnect-then-reconnect, the same pattern azure's ACA multi-CNAME path already uses), and `DeregisterInstance` re-realizes the reduced set. The netns/`/etc/hosts` tier already aggregated all names. **(backend)** ECS `ContainerCreate` dropped the request's `NetworkingConfig.EndpointsConfig[net].Aliases`, and the Cloud Map registration registered only the container hostname; it now captures the aliases into `EndpointSettings.Aliases` and registers the container under its hostname AND every alias, and `deregisterInstance` enumerates the namespace's services (a container may back several) instead of the old 1:1 container→service mapping that leaked. Proven by `TestECS_MultiServiceDNS` (a client task resolves both of a server's two service names via real Cloud Map DNS) + `TestDedupeNonEmpty`/`TestCloudMapNamesFor`; end-to-end the redis service container is registered under both `redis` and its hostname and carries both as Docker aliases on the namespace network. (Remaining gitlab-runner `services:` e2e gate tracked as BUG-1805.) |
| ~~1803~~ | P3 | backend-azf — `TestAZFGitLabRunnerAttachStdin` intermittently failed in CI with empty attach output | create→attach→start stdin-capture race | The test attaches to an azf-backed container, writes a line to stdin, starts it, and asserts the echoed stdout. Intermittently in CI the attach stream closed with a clean EOF but **empty** output (`attach stdout = "" stderr = ""`) after ~150s — exactly the attach deadline. Root cause: `ContainerStart`'s async invoke goroutine called `captureAZFStdin`, which did `stdinPipes.LoadAndDelete` *immediately*; when the hijacked attach connection's handler hadn't stored the stdin pipe yet (a contended CI runner reorders the concurrent attach + start requests), it returned `!ok` → the function ran with no stdin → `sh` read EOF and exited with no output → `hasCapturedStdin == false`, and that success path **never called `publishAZFAttachResponse`**, so the attached reader stranded to the full attach deadline and EOF'd empty. The same create→attach→start race as ECS BUG-1798. Two fixes: (1) `captureAZFStdin` now waits up to 5s for the stdin pipe to appear+open when the container is `OpenStdin`, so the capture doesn't race ahead of the attach handler; (2) the no-captured-stdin invoke path now always `publishAZFAttachResponse`s (a no-op when no reader is attached), so an attached reader unblocks promptly instead of stranding to the deadline. Verified locally against the sim target (5× + full azf integration suite green); the buffered attach path validates on Docker Desktop for Mac. |
| ~~1802~~ | P2 | backend-ecs — a restarted container loses its named-volume binds, so gitlab-runner's predefined helper clones `get_sources` into ephemeral storage the build container can't see | create-time config not preserved across stop/start | The bleeplab GitLab ECS cell's build job failed at `step_script` with `sh: cd: can't cd to /builds/root/demo: No such file or directory` — even though `get_sources` reported a successful clone+checkout. gitlab-runner's docker executor runs each build stage by **re-starting the same predefined helper container** (`create` once → `attach→start→wait→stop` per stage: prepare_script, get_sources, cleanup). On ECS each `/start` spawns a fresh deferred task. The **first** start resolves the container from `PendingCreates` (which carries `HostConfig.Binds`), so its task def gets the `/builds` EFS volume + mount; **subsequent** starts resolve the container from cloud state via `taskToContainer`, whose reconstructed `HostConfig` omitted `Binds` entirely — so the re-registered task def had **no volume mounts**. `get_sources` (the 2nd start) therefore cloned into the helper container's ephemeral `/builds`, lost on exit; the build container (which *did* get the shared EFS `/builds`) then found only its own `…/root/demo.tmp` and no clone. Proven with per-container DIAG: all 3 stage containers resolved the *same* access point `fsap-…`, but the sim showed two helper tasks with `mountPoints=0 binds=[]`. Fix (backend-side, stateless): `taskToContainer` reconstructs the named-volume binds from the task definition's mount points (`SourceVolume:ContainerPath[:ro]`) — the same `volName:/path` spec the create path produced — so every restart re-registers a task def with the original volumes. Unit-tested (`TestTaskToContainer_BindsFromMountPoints`). No sim coupling; the sim already shares an access point's host dir across tasks deterministically. |
| ~~1801~~ | P2 | bleeplab — the GitLab control-plane sim didn't serve the project as a git repo, so a real gitlab-runner couldn't clone it to materialize `CI_PROJECT_DIR` | sim didn't host the project repo | With no git server, the harness used `GIT_STRATEGY: none`, which skips both the clone and the project-dir creation, so the build job's `cd $CI_PROJECT_DIR` failed. Fixed by making **bleeplab serve each project as a real git repository over smart-HTTP** — pure-Go go-git (`/info/refs` + `git-upload-pack`/`git-receive-pack`), object-store-backed exactly like bleephub (`s3fs` go-billy filesystem → go-git Storer chosen by env: `BLEEPLAB_S3_BUCKET` > `BLEEPLAB_GIT_DIR` > in-memory). A commit through the GitLab commits API now writes a real go-git commit (additive create/update on the default branch) and records the real SHA; `git_info.repo_url` points at `<BLEEPLAB_EXTERNAL_URL>/<namespace>/<project>.git` (reachable from the job/helper container via `host.docker.internal:8929`) and `git_info.sha`/`refspecs` drive a faithful clone. The harness switched to `GIT_STRATEGY: clone`. Validated in-process by `TestGitCloneSeededProject` (a real go-git client clones the seeded repo; HEAD == commit SHA; second commit is additive) and end-to-end by the bleeplab ECS harness (clone materializes `CI_PROJECT_DIR`). bleeplab accepts the runner's `gitlab-ci-token` exactly as GitLab does — coordinate-only, no sockerless-aware special-casing. |
| ~~1800~~ | P2 | aws sim — EFS access-point host dir wasn't writable by a workload: `CreationInfo` (ownership/permissions) was ignored, and the bind wasn't SELinux-relabeled for confined task containers | access-point CreationInfo + SELinux | After BUG-1798 the bleeplab GitLab ECS build `step_script` failed at `mkdir: can't create directory '/builds/project-1.tmp': Permission denied`. Two sim-side causes, both fixed: **(1)** `EFSAccessPointHostDir`/`EFSFileSystemHostDir` created the host dir with `os.MkdirAll(…, 0o777)`, whose mode is reduced by the umask to `0755 root`, and **never applied the access point's `RootDirectory.CreationInfo`** — real EFS creates the root dir with the requested owner uid/gid + permissions. New `ensureAccessPointRootDir` applies `CreationInfo.{Permissions(chmod, not umask-masked), OwnerUid, OwnerGid(chown, best-effort)}` on creation only (so a workload's later perm changes aren't clobbered), defaulting to `0777` when absent; unit-tested (`efs_creationinfo_test.go`). **(2)** On an SELinux-enforcing host (a local podman machine) the sim-spawned ECS task runs confined as `container_t`, which can't write to the EFS host dir even at `0777`; the sim now mounts task EFS binds with the `z` (shared relabel) option — relabeling them to `container_file_t`, a no-op on hosts without SELinux (Docker on CI). Validated: the `/builds` write now succeeds (the cell advances past the permission error; next gate is BUG-1801, volume-sharing). aws sim ECS/EFS SDK tests stay green. No backend coupling. |
| ~~1798~~ | P2 | backend-ecs — gitlab-runner docker-executor: the helper container's attach-stdin script was never delivered, so the job hung in container setup | attach-stdin deferral dependency inversion | gitlab-runner 18's docker executor does `create(OpenStdin) → /attach(stdin) → /start` and pipes the stage script to the container's stdin (the helper's default `gitlab-runner-build` reads it). On ECS the `/start` deferral that bakes the captured stdin into the task command requires the stdin pipe to already exist + be open — but the attach driver created the pipe only **after** a stage-boundary barrier that itself **waits for `/start` to register a WaitCh**. So `/start` arrived first (DIAG: `pipe_exists=false`), found no pipe, fell through, and launched the helper's image-default command, which then hung forever waiting for stdin (the job timed out in `running`). A dependency inversion: `/start` needs the pipe before it makes the WaitCh; the attach needs the WaitCh before it makes the pipe. Fix (backend-side): create + open the stdin pipe **before** the barrier in `ecsStdinAttachDriver.Attach`, and have `ContainerStart` wait briefly (`waitForOpenStdinPipe`, 5s) for the open pipe before deciding — closing the create→attach→start race from both ends. Validated: the bleeplab GitLab ECS harness now runs the helper stages and delivers the script into the build container (the hang is gone; it advances to the build `step_script`, blocked next only by BUG-1800). No sim coupling. |
| ~~1799~~ | P2 | aws sim sdk-tests — `TestECS_TaskArithmetic*` flaked on CI with container `ExitCode -1` | flaky runtime image pull + swallowed start error | The ECS arithmetic tests run an awsvpc/Fargate task; on CI (Linux netns tier) the task starts a `busybox` **pause container** whose image (`public.ecr.aws/docker/library/busybox:latest`) was pulled **lazily at RunTask time** from the ECR gallery. A transient gallery throttle there failed `startECSTaskContainers`, which the sim recorded only in the task's `StoppedReason` and surfaced as an opaque container `ExitCode -1` (the test asserts 0) — with no diagnosable cause in any log (a swallow, same class as BUG-1796). Re-running the job passed (intermittent), confirming a pull/transient flake. Fix: (1) the aws sdk-tests `TestMain` pre-pulls busybox up front with bounded-backoff retry (`pullImageWithRetry`, the established azure-sdk pattern) so the gallery fetch is no longer a per-task lifecycle dependency — busybox also backs many other ECS tests; (2) the sim now logs the start failure to stderr (`[sim-ecs] task …: container start failed: …`) so any residual netns/veth-tier flake is diagnosable instead of a silent `-1`. Sim-side + test-side only — no backend coupling. |
| ~~1797~~ | P2 | backends/core — image manifest selection hardcoded linux/amd64, so an arch-specific image with no amd64 entry failed to pull on arm64 hosts | hardcoded amd64 manifest pick | `getManifestInfo` (`core/registry.go`) always selected the `linux/amd64` entry from a multi-arch manifest list. The local sims run workloads on the host engine, so on Apple Silicon (and arm64 CI / Graviton ECS) the workload is arm64 — but image metadata was fetched for amd64. For *multi-arch* images (alpine) this was merely inconsistent (the amd64 config's Cmd is arch-agnostic; the host ran arm64 anyway), but for an **arch-specific** image — the gitlab-runner-helper `arm64-v18.11.3` tag, whose manifest list has only `linux/arm64` — there was no amd64 entry and the pull failed: `manifest list has no linux/amd64 entry`. Surfaced bringing up the bleeplab GitLab ECS cell. Fix: select the linux manifest matching the configured workload architecture (`SOCKERLESS_WORKLOAD_ARCH`, normalized; default `amd64` so live behavior is unchanged), falling back to amd64 before erroring. The selection policy is extracted to `selectPlatformManifest` and unit-tested (`registry_arch_test.go`: multi-arch picks per-arch, arm64-only resolves on arm64, amd64-only falls back, neither errors). The bleeplab ECS harness sets `SOCKERLESS_WORKLOAD_ARCH` from `uname -m`; existing backends that don't set it keep amd64. |
| ~~1796~~ | P2 | reverse-agent exec — an agent-reported error (`TypeError`) was swallowed backend-side and exec lifecycle was unlogged, so a failing exec left an opaque exit 255 with the real cause stranded only in the workload's own stderr | silent error swallow + missing observability | Bringing up the GCF cell, a step failed with `exit 255` and *zero* backend-side detail; the real cause (`restore sync runner-ws: metadata token status 404`) was visible only by `docker exec`-ing into the workload and reading the bootstrap's own stderr — a separate log stream with no exec-ID correlation. Root cause: `ReverseAgentConn.bridge`'s `TypeError` case (`agent/reverse.go`) just `return`ed — the agent's error message was neither written to the caller's stream nor mapped to a non-zero exit; and the whole exec lifecycle (dispatch, driver path, exit code, session register/replace/drop) logged nothing on the backend. Fixed across all FaaS backends (shared core + agent): (1) `bridge` now writes the agent error to the caller's stderr frame (so `docker exec`/the runner prints it) and returns `AgentExecErrorExitCode=255`; `BridgeExec` fails fast + surfaces if even the initial exec dispatch send fails; (2) `core/handle_exec.go` logs exec dispatch (with the resolved driver via `Describe()`) and completion (Info on exit 0, Warn on non-zero); (3) `ReverseAgentExecDriver.Exec` logs dispatch + the returned exit code; (4) `HandleReverseAgentWS` logs missing-`session_id`/upgrade failures, session register/replace(takeover)/drop at Info/Warn; (5) the agent bootstrap logs its serve-loop start/teardown + malformed inbound messages, checks the final exit-frame send (`agent/exec.go`), and a new nil-safe `OnDroppedMessage` callback surfaces full-channel message drops. Pure-additive logging except the `TypeError`→stderr+255 behavior fix (previously an opaque stream-close → 255 via the `-1` default; now the same exit with the cause printed). |
| ~~1795~~ | P2 | backend-gcf (Cloud Run Functions) GitHub-topology cell — container-mode jobs never executed: the exec/materialize/gcs-sync chain the cloudrun cell uses was entirely unported | unported cell wiring | The bleephub `gcf` cell (TEST 12 container job → build→push→pull→deploy→materialize→reverse-agent→exec→gcs-sync, GCF Gen2 deploys jobs as Cloud Run Service revisions) failed at the first step. Five gaps, each the GCF twin of a cloudrun fix: **(1)** the GCF `Typed.Exec` was wired to the bare `ReverseAgentExecDriver` (`WrapLegacyExec`), so the HTTP exec path bypassed `s.ExecStart` — the materialize-on-exec and gcs-sync hooks were dead code; rewired through `s.ExecStart` via `WrapLegacyExecStart` like cloudrun. **(2)** A no-`services:` GH job is deferred at `ContainerStart` and never deployed; added `materializeDeferredNetworkPodForExec` to GCF's `ExecStart`. **(3)** `invokePodServiceMain` skipped the default-invoke for the keepalive container, so the scale-to-zero Service never cold-started its bootstrap (BUG-1794 twin); added `warmBootstrap` POSTing `/_sockerless/ready`. **(4)** The gcf bootstrap lacked the `/_sockerless/ready` route + the WS-exec gcs-sync `ExecHooks` (used `ServeReverseAgent`, not `ServeReverseAgentWithExecHooks`); ported both. **(5)** The gcf bootstrap's `persist.go` ignored `STORAGE_EMULATOR_HOST` and tried a GCE metadata token (404 from the workload) — the `#568` cloudrun prereq was never ported; added `gcsBase`/`gcsAuthToken`/`setGCSAuth`, and the backend now injects `STORAGE_EMULATOR_HOST` (`SOCKERLESS_GCS_WORKLOAD_ENDPOINT`) + a `gcsSyncPreExec`/`execPostHook` data plane. Plus harness plumbing (`provision_gcf` + `gcf` case in `run-integration.sh`, `bleephub-runner-docker-test-gcf` Make target, both GCF binaries in `bleephub/Dockerfile`). Validated: bleephub `gcf` harness TEST 1–14 all green; coordinate-only (`SOCKERLESS_GCP_AR_ENDPOINT` / `SOCKERLESS_GCS_WORKLOAD_ENDPOINT`), no `if sim` branch. |
| ~~1794~~ | P2 | cloudrun cell — an exec-driven Service was deployed but never received a request, so its scale-to-zero bootstrap never started and never registered its reverse-agent (TEST 12 blocker) | scale-to-zero never cold-started | The materialize-on-exec path deployed the Cloud Run Service and then *skipped the default-invoke entirely* (it closed `WaitCh` and returned) because the GH job container's keepalive (`tail -f /dev/null`) must not run as a request. But a scale-to-zero Service that never gets a request never creates its first instance — so the overlay bootstrap never ran, never dialed `host.docker.internal:3375`, and `materializeDeferredNetworkPodForExec` timed out with the job hung in `running`. Fix: the overlay bootstrap serves a `/_sockerless/ready` route (HTTP 204, runs no user command); the backend's materialize path POSTs to it (`warmBootstrap` in `start_service.go`) to cold-start the revision so the bootstrap starts and dials back **without** running the keepalive as a request. The gcp sim's `/v2-services-invoke/` handler now forwards the request path + query to the bootstrap (added a `{path...}` route variant + path/query params on `postCloudRunServiceInstance`) so `/_sockerless/ready` reaches the bootstrap instead of collapsing to `/`. SDK regression `TestSDK_CloudRunV2Services_ForwardsRequestPath` (new `echo-request` probe mode) + `TestHandleReady_DoesNotRunDefaultCommand`; validated end-to-end by bleephub cloudrun harness TEST 12/13/14 (all green). |
| ~~1792~~ | P2 | backend-cloudrun — the gcs-sync per-exec workspace data plane (wired in #570) was unvalidated; the official storage client couldn't follow the sim's resumable-upload continuation URL | resumable continuation URL hardcoded HTTPS | Cloud Run shares the runner workspace via GCS snapshot-sync (backend `GCSSyncDriver.PreExec` tars the host workspace → GCS + `SOCKERLESS_SYNC_VOLUMES`; the bootstrap restores into the Service tmpfs, runs, re-uploads; `PostExec` downloads back). With the hooks wired, the last gap was the resumable upload: the sim emitted `Location: https://<host>/upload/...` hardcoded to HTTPS, so the official Go storage client — pointed at the explicit **HTTP** sim coordinate — followed it and failed `server gave HTTP response to HTTPS client`. Fix: derive the continuation-URL scheme from the request (`requestScheme` — `X-Forwarded-Proto` / `r.TLS`), matching real GCS (HTTPS) and a custom HTTP coordinate alike. Also added the JSON-API `alt=media` object download, resumable-chunk-via-`POST` (`upload_id`), and `bytes */<total>` Content-Range parsing the client uses. Proven by `TestGCS_ResumableWriterFollowsCustomEndpoint` + `TestGCS_JSONAPIObjectGetAltMedia` (official Go storage SDK) and the bleephub cloudrun TEST 12 workspace round-trip (`proof.txt` written inside the job container is visible in the runner workspace). |
| ~~1793~~ | P2 | aws sim — process-mode (`SIM_RUNTIME=process`) ECS RunTask with a managed-EBS volume panics on a nil Docker client (issue #569) | docker call on the process-mode in-memory path | The managed-EBS RunTask volume provisioning (`ecs.go`) set `EC2Volume.DockerVolumeName` unconditionally and routed the deleteOnTermination cleanup through `ebsRemoveDockerVolume` → `sim.DockerClient().VolumeRemove`. In process mode (`SIM_RUNTIME=process`) the Docker client is never initialised (nil), so the async task-transition goroutine panicked with a nil-pointer deref and the process exited (every later request → ECONNREFUSED). `ec2:CreateVolume` already works in process mode by using the host-path/in-memory model (`ebsPrepareVolumeHostPath` + `ebsCopyDir`), never a Docker named volume. Fix (no fallbacks — process vs container is the explicit `SIM_RUNTIME` mode): the managed-EBS path detects process mode (`sim.DockerClient() == nil`) and backs the volume with a host-path dir (no `DockerVolumeName`, bind = `HostPath`) exactly as `CreateVolume` does, so termination takes the existing `os.RemoveAll(HostPath)` branch and never calls into Docker. A Docker-volume-backed snapshot referenced under process mode now fails loudly with an explicit `InvalidParameterException` rather than silently skipping the restore or panicking. Container mode unchanged. SDK + CLI regression in `simulators/aws/{sdk,cli}-tests`. (PR #570) |
| ~~1791~~ | P2 | backend-cloudrun — a GH container-job with no services was deferred forever and never deployed | over-eager network-pod defer | `shouldDeferOrMaterializeNetworkPod` deferred any `OpenStdin=false` container alone in a user-defined network, marking it fake-"running" but never deploying the Cloud Run Service — waiting for a sibling (service / gitlab script-runner) that a no-`services:` GH job never gets, so the first `docker exec` failed "reverse-agent WebSocket not registered". Fixed with **materialize-on-exec** (the runner always `docker exec`s its job container → `ExecStart` lazily deploys it, bundling any deferred service siblings, via `startSingleContainerService`/`startMultiContainerServiceTyped` which block on `waitForReverseAgentAfterStart`). Second facet: `startSingleContainerService` hardcoded `skipDefaultInvoke=false` → it ran the job's `tail -f /dev/null` keepalive as a Cloud Run request, blocking until the request-lifetime cap SIGTERMed the pod; parameterized `skipDefaultInvoke` (exec-driven materialize passes `true`, real `docker run` keeps `false`). Cloud Run container-job path was previously unproven (ECS/ACA only). (PR #567) |
| ~~1790~~ | P2 | gcp sim — AR docker-hub pull-through served an invalid mixed-media-type manifest | OCI manifest + Docker v2s2 config | `hydrateOCIImageFromLocalDocker` built an OCI manifest (OCI manifest + OCI layer types) with a **Docker v2s2** config descriptor — tolerated by `docker pull` but rejected by `docker build`'s `FROM` (`invalid mixed OCI image with Docker v2s2 config`), failing the cloudrun cell's overlay build. The `docker save` layers are uncompressed tar (naturally OCI), so the consistent fix is full OCI: relabel the config blob + descriptor to `application/vnd.oci.image.config.v1+json` (byte-compatible with the OCI config schema). (PR #567) |
| ~~1789~~ | P2 | backends gcp-common — overlay/base image registry coordinate not honored (two facets) | hardcoded AR host vs coordinate | The Cloud Run overlay flow ignored `SOCKERLESS_GCP_AR_ENDPOINT` in two places. **(1)** `ResolveGCPImageURI` rewrote a Docker Hub base to the AR docker-hub pull-through path using the hardcoded real `<region>-docker.pkg.dev` → the overlay's `FROM` 403'd; fixed by building the host via `OverlayRegistryHost` (the sim hydrates docker-hub from the local daemon; the harness pre-pulls base images via ECR gallery). **(2)** `ARAuthProvider.{IsCloudRegistry,RegistryEndpoint}` only matched `*-docker.pkg.dev`/`*.gcr.io`, so a relocated coordinate host (`127.0.0.1:5000`) got no endpoint override → `FetchImageMetadata` dialed `https://127.0.0.1:5000` (unreachable from inside the backend, wrong scheme); fixed with `IsOverlayCoordinateRegistry` so registry HTTP routes through the backend's reachable `SOCKERLESS_ENDPOINT_URL`. Real cloud unchanged when the coordinate is unset. (PR #567) |
| ~~1785~~ | P2 | sims (azure ACR Tasks + gcp Cloud Build) — a build landed the image only in the host's local docker daemon; it was never pushed to the registry, and the workload ran the local copy instead of pulling from the registry | local-daemon shortcut vs build→push→pull | Real ACR Tasks / Cloud Build (`IsPushEnabled`) push the built image to the registry; the cloud's compute then pulls it over the public `/v2/` API. The sims instead `docker build -t <ref>` into the local daemon and relied on `StartContainerSync` finding it locally, so the sim's registry diverged from the cloud after a build and the run used a non-faithful shortcut. Fixed faithfully on **both** slices, with the registry reached **only by coordinate** (no `if sim` branch in backend or test code): the build does a real `docker push <ref>` + `docker rmi`, the run does a plain `docker pull <ref>`, and the backend builds the **real** registry ref for cloud and sim alike — only the registry-host *coordinate* differs (`SOCKERLESS_AZURE_ACR_ENDPOINT` / `SOCKERLESS_GCP_AR_ENDPOINT`, default = the real registry; a harness sets it to the sim's published `/v2/` at `127.0.0.1:<port>`, which Docker auto-trusts as insecure). **Azure half** (`backends/azure-common` ACR Tasks): real push/pull + `SOCKERLESS_AZURE_ACR_ENDPOINT`, validated by ACA harness TEST 12 + the ACR Tasks SDK test. **GCP half** (`simulators/gcp/cloudbuild.go` + `gcpcommon.OverlayRegistryHost`): the Cloud Build `push` step does a real `docker push`/`rmi`; cloudrun + gcf overlays resolve the host via the coordinate; the cloudrun/gcf integration harnesses set it per-target like `endpointURL` (no sim branch). `TestCloudBuild_FaithfulBuildPush` asserts the image lands in `/v2/` and is gone from the local daemon; the `test (gcp backends)` + gcp/gcf faas-smoke CI jobs (which always build overlays) exercise the full round-trip against the sim. Pattern documented in [specs/CLOUD_RESOURCE_MAPPING.md](specs/CLOUD_RESOURCE_MAPPING.md) § "Faithful build → push → pull" + [AGENTS.md](AGENTS.md) § "A sim test differs from a cloud test ONLY in coordinates". |
| ~~1784~~ | P2 | bleephub ACA topology harness — `services:` container reachable but not serving (bootstrap never launched a service's workload) | service workload not run | TEST 13 (service container) failed: the job's `wget http://web/` got **Connection refused** (TEST 12+13+14 now all green on ACA). **Discovery already worked** (confirmed by held-run inspection): the backend captures the runner's `--network-alias web` and registers it in Azure Private DNS at ContainerStart (`registerContainerServiceDiscovery`); the azure sim realizes the CNAME→App-FQDN as a Docker embedded-DNS alias on the App's env-network container (`realizeCNAMEAsDockerAlias`). From the job, `web` resolves (via podman aardvark-dns) to the nginx container's IP and ping succeeds. **The real bug:** the nginx App container has the correct `SOCKERLESS_USER_ENTRYPOINT=["/docker-entrypoint.sh"]` / `_CMD=["nginx",…]` and the `web` alias, but **only the cloudrun-bootstrap is running (PID 1, port 8080) — nginx (port 80) was never started.** The bootstrap runs the user workload only in `SOCKERLESS_SIDECAR` mode (`runSidecar`) or on an HTTP invoke / reverse-agent exec; a single-container *service* App runs in "ingress" mode (HTTP server only), so its workload never launches. (The job container is fine without its workload: it's exec-driven and the bootstrap keepalive holds it open.) **Fix:** the ACA backend marks a service container — one where the client didn't override the entrypoint (the GitHub job overrides to `tail -f /dev/null`; a service uses the image default) — with `SOCKERLESS_RUN_USER_WORKLOAD=1`; the bootstrap, in ingress mode, then launches the user workload as a background subprocess alongside the HTTP/reverse-agent server so `docker start` actually runs the service (Docker-faithful). The earlier "restored TEST 12" realize work (only service aliases realized, never the aliasless job's hostname) stays. |
| ~~1788~~ | P2 | azure sim sdk-tests — `TestACRTasks_ScheduleRunDockerBuild` flaked CI with `docker run … registry:2` exit status 125 | flaky image pull | `startThrowawayRegistry` ran `docker run -d … public.ecr.aws/docker/library/registry:2` which inline-pulls the image on first use; a transient public-mirror throttle made the pull (and so the `run`) exit 125, failing `sim (azure)` on PR #564 (it passed on #563 — flaky, not deterministic). Fixed by pulling the registry image (and the build's `alpine:3.20` base) up front via a new `pullImageWithRetry` helper (bounded exponential backoff, mirroring the sim pull paths' strict rate-limit posture) and retrying the `docker run` itself a few times. Clarified that 127.0.0.1:<port> is auto-insecure on Docker (CI) but a local Podman host needs an insecure-registries drop-in. |
| ~~1787~~ | P2 | bleephub — no standard / config-only way to obtain a usable pre-registered GitHub App (fixed id + a *known* private key) for coordinate-only consumers (consumer issue #559) | missing operator seeding | App creation was only available via `POST /internal/apps` (operator surface a coordinate-only consumer must not call) or the manifest-conversion flow (needs an interactive approval code); `store.CreateApp` always generated the key internally, so a harness couldn't hand the consumer a fixed app id + private key + org the way real GitHub does (app registered out of band, consumer holds id + PEM). Fixed by seeding one or more pre-registered Apps at startup from operator config (`BLEEPHUB_SEED_APPS` inline JSON / `BLEEPHUB_SEED_APPS_FILE`): caller-chosen id + slug, caller-supplied RSA private key (PKCS1 or PKCS8, normalized to the PKCS1 the app-JWT verifier expects), permissions/events, and optional pre-created installation(s) on given org(s) — unknown install accounts are created as real orgs, never fabricated ids. Idempotent across restarts; fails loud on a malformed spec. The seed is **operator-only**; the consumer's client stays 100% coordinate-only (base URL + app id + private key + org through the standard `/api/v3/app/...` surface) — no bleephub-specific client config. Regression test `TestSeedPreRegisteredApp` drives the full Octokit-style flow (sign app JWT → GET /app → list installations → mint installation token). |
| ~~1786~~ | P2 | aws sim — ECS ExecuteCommand WebSocket skipped the SSM `OpenDataChannel` token handshake real clients send (consumer issue #562) | missing connection handshake | `handleECSExecWebSocket` upgraded the WebSocket and immediately bridged stdin/stdout, treating the **first** inbound frame as `input_stream_data`; the `tokenValue` ExecuteCommand returned was never read or validated. Real AWS (session-manager-plugin, and any faithful client) sends an `OpenDataChannel` JSON message — carrying `TokenValue` — as the first frame, which the service validates before streaming. So a coordinate-only consumer couldn't drive ECS-Exec identically against the sim and real AWS. Fixed: the sim reads the first frame as `OpenDataChannel`, validates `TokenValue` against the session (10s read deadline; rejects with ClosePolicyViolation on mismatch/absence), then bridges. The sim's own ECS backend already sent this handshake (`exec_cloud.go`) so it keeps working. SDK regression test `TestECS_ExecDataChannel{Handshake,RejectsBadToken}` drives the data channel like a real client; `TestECSContainerExec` (backend integration) stays green. |
| ~~1783~~ | P2 | bleephub harness — the reverse-agent bootstrap (and agent) were built glibc-dynamic, so they failed to exec inside musl/alpine workload images | non-static binary | The bleephub `Dockerfile` built `sockerless-cloudrun-bootstrap` + `sockerless-agent` under `golang:1.25` with CGO on by default → dynamically linked against glibc. Baked into an alpine overlay (the ACA App image), the bootstrap failed to exec with `No such file or directory` (the missing dynamic loader), so it never dialed back and ACA container-job exec timed out. The canonical agent Makefile already builds with `CGO_ENABLED=0`; the harness Dockerfile was the anomaly. Fixed by adding `CGO_ENABLED=0` to both builds. Found by direct-running the overlay image; it unblocked ACA TEST 12. |
| ~~1782~~ | P2 | backends/azure-common `NewACRBuildService` ignored `SOCKERLESS_ENDPOINT_URL` — the ACA/AZF App-overlay bootstrap-build path couldn't reach a custom or simulated Azure endpoint | missing endpoint override | `NewACRBuildService` built both the ARM `RegistriesClient`/`RunsClient` and the `azblob` client with default options targeting real Azure, unlike `newAzureClientsWithEndpoint` which threads a `cloud.Configuration` override + `InsecureAllowCredentialWithHTTP`. So `SOCKERLESS_ACA_USE_APP=1` (the overlay path that injects the reverse-agent bootstrap, needed for ACA container-job exec) only worked against real Azure. Fixed: thread `endpointURL` into `NewACRBuildService`; build the ARM clients with the cloud-config override; build the azblob client lazily against the storage account's advertised `primaryEndpoints.blob` (discovered via `armstorage` GetProperties). Validated end-to-end by ACA TEST 12 going green. |
| ~~1780~~ | P2 | backends lambda/cloudrun/gcf/aca/azf — `docker network create` did real Linux netns (`ip netns add` + veth), 400ing without iproute2 and provisioning a meaningless kernel netns where it succeeded | wrong network driver | Only the ecs backend overrode `Drivers.Network` to the metadata-only `SyntheticNetworkDriver`; the other five stateless cloud backends fell through to `BaseServer.InitDrivers`' Linux platform driver. But their docker networks map to *cloud* primitives (Lambda VPC ENIs, Cloud Run/GCF VPC-connector + Cloud DNS, ACA + AZF NSG / Private DNS) provisioned by each backend's NetworkCreate wrapper — the kernel netns inside the runner-task is irrelevant, exactly as the ecs comment documents. On a host without iproute2 (the bleephub ubuntu:22.04 harness) `docker network create github_network_<hex>` 400'd, blocking every GitHub container-mode job; on a host with iproute2 it silently created and leaked a kernel netns per network. Found by the Arc-2 ACA topology harness (TEST 12). All five now mirror ecs's `SyntheticNetworkDriver`. |
| ~~1779~~ | P3 | backend-azf — multi-container pod rejected late/asymmetrically (only at the 2nd ContainerStart) | late fail | `PodCreate` + both `ContainerCreate`s succeeded; the single-invocation rejection only fired when the 2nd member's `ContainerStart` ran (deferred materialize), surfacing as a buried per-container error. `PodStart` now rejects a multi-container pod up front with one clear `InvalidParameterError` (the ContainerStart guard stays as the per-container backstop); documented in the azf README's out-of-scope list. |
| ~~1778~~ | P2 | backends lambda+gcf — `docker pod stop/kill/rm` (and lambda `pod start`) leaked the cloud resource; isolation lint didn't catch it | stateless violation | Lambda delegated all four Pod lifecycle methods, and GCF delegated Stop/Kill/Remove, to `BaseServer.Pod*` — which read `Store.Containers` + call `Store.ForceStopContainer` (local in-memory only, NO cloud call), so the Lambda function / Cloud Run Service kept running. ECS/Cloud Run/ACA already override these to loop their cloud-aware `Container*` methods; lambda+gcf now mirror that. `scripts/check-cloud-backend-isolation.sh` forbade `BaseServer.Container*` but not `BaseServer.Pod{Start,Stop,Kill,Remove}` — gap closed so the class can't recur. |

## Recently fixed

The current arc (BUG-1745 onward). Older fixed bugs are struck out in `git log` and the PR descriptions that closed them — this section is a rolling window for context, not the full ledger; the header counts above are the cumulative tally.

| ID | Sev | Area | Pattern | One-liner |
|----|-----|------|---------|-----------|
| ~~1807~~ | P2 | aws/gcp/azure sims + bleephub HTTP servers — large uploads/downloads timed out under loaded CI | `http.Server.ReadTimeout`/`WriteTimeout` cap the WHOLE body, not just headers | `TestGCFArithmeticDivision` failed with `cloud build … close GCS writer: googleapi: Error 500: failed to read data: read tcp …: i/o timeout`. Root cause: every sim's HTTP server (and bleephub's) set `ReadTimeout: 30s` + `WriteTimeout: 60s`. Go's `ReadTimeout` is a hard deadline on reading the ENTIRE request including the body — so a large overlay build-context / OCI image-layer upload (and symmetrically a large layer download under `WriteTimeout`) that takes longer than the cap on a loaded runner is severed mid-stream, surfacing as a 500 at `io.ReadAll(dataPart)` (`simulators/gcp/gcs.go`). Fixed per Go best practice: drop `ReadTimeout`/`WriteTimeout` and bound only the header read with `ReadHeaderTimeout: 30s` (slowloris protection) + keep `IdleTimeout: 120s`, so streaming handlers aren't capped. Applied to all three `simulators/{aws,gcp,azure}/shared/server.go` and `bleephub/server.go`. (bleeplab uses the default no-timeout server — no body cap; the backends' Docker-API server likewise has no body-capping timeouts.) |
| ~~1806~~ | P3 | repo-wide proactive flaky-test sweep — CI surfaced one, a sweep fixed the whole class | (a) single-Read of a multiplexed log stream; (b) fixed `time.Sleep` as the sole sync before an async-completion assertion | Triggered by `TestACASharedVolumeWorkspaceSharing` flaking ("workspace not shared" though the volume WAS shared): its `sharedVolRun` helper read logs with ONE `logRC.Read(buf)`, capturing only the sim's first "Container started" multiplexed frame and missing the script's stdout in a later frame (the reader exited 0, proving the volume was shared). **(a)** Fixed the single-Read class with `stdcopy.StdCopy` everywhere it gated an assertion: `backends/aca` + `backends/ecs` (`sharedVolRun` twins) + `backends/ecs` `TestECSContainerLogs`. **(b)** A repo-wide hunt then found the dominant flaky class — a fixed `time.Sleep(2–3s)` as the only wait before asserting an *asynchronously-completing* job/task/execution had finished (the sims complete executions on a background goroutine, so a fixed sleep races a loaded runner). Converted ~20 sites to `require.Eventually`/poll-until-terminal: `simulators/aws/sdk-tests` (ECS exit-code + arithmetic-log tests), `simulators/gcp/{sdk,cli}-tests` (Cloud Run execution-state + log tests), `simulators/azure/{sdk,cli}-tests` (Container Apps execution-state + KQL-log tests), and `backends/lambda` (post-stop exec-must-fail poll). All verified green locally. Lenient `t.Logf`-only reads + sleeps-inside-poll-loops were left alone (not flaky). |
| ~~1777~~ | P3 | backends — cloud-state recovery WaitForExit re-listed every cloud resource per poll; Cloud Run service-URI poll was a flat 2s loop | needless poll volume | A pod-model review flagged the recovery-path `WaitForExit` (cloudrun + aca) re-running a full `ListJobs`+`ListServices` (with per-pod GetService follow-ups) on every tick to check one container — narrowed to resolve the backing Job once then poll just that job's state (the identical resolveExecutionState/resolveJobState derivation, single resource), with a list fallback for Service/App-backed and vanished jobs. Cloud Run's `waitForServiceURL` flat 2s/150-call loop → exponential backoff (1s→15s). GCF's WaitForExit left alone (already event-driven hot path; FaaS has no persistent exit state). Recovery-path only; hot path was already single-resource (pollTaskExit/pollExecutionExit). |
| ~~1776~~ | P2 | backend-aca — buffered-attach Read had no deadline; a stalled/lifetime-capped Job strands an attached reader forever | unbounded wait | ACA's `attachStream.Read` did a bare `<-respReady`; the AZF twin bounds the identical buffered-attach wait with `select { respReady / time.After(deadline) }` (the BUG-1505 fix) but it was never ported to ACA. If the ACA Job stalls or hits its replica-timeout before publishing output, a `docker`/`StdCopy` reader blocks indefinitely. Fixed by porting the AZF deadline pattern (bootstrap window + job-run budget). Found by the pod-model review. |
| ~~1775~~ | P3 | gcp sim — rrsets.list ignores the `name` and `type` query params; returns the whole zone | dropped filter | Real Cloud DNS `rrsets.list` filters on `?name=&type=`, which the Go SDK's `.Name(fqdn).Type("A")` sets — the Cloud Run service-discovery path uses exactly this (`backends/gcp-common/network_discovery_clouddns.go`). The sim returned every record in the zone; the backend re-filters client-side so it isn't broken today, but the sim diverges from real GCP and a filter-trusting consumer would over-read. Found by real-SDK probe. Fix: honor name+type in the list handler. |
| ~~1774~~ | P2 | aws sim — AuthorizeSecurityGroup{Ingress,Egress} never rejects a duplicate rule (no InvalidPermission.Duplicate); silently appends a second identical IpPermission | missing idempotency error | The ECS backend re-applies its self-referencing ingress rule on every network-create and swallows exactly `InvalidPermission.Duplicate`; the sim instead appends the permission again, so DescribeSecurityGroups read-back accumulates duplicate rules and diverges from real AWS (which 400s the duplicate). Found by a real-SDK probe mirroring `backends/ecs/network_cloud.go`. Fix: detect an existing equivalent permission and return InvalidPermission.Duplicate; egress mirror too. |
| ~~1773~~ | P2 | aws sim — CreateSecurityGroup never rejects a duplicate name in the same VPC (no InvalidGroup.Duplicate); always mints a fresh sg-id | missing idempotency error | The ECS backend's idempotent network-create (`backends/ecs/network_cloud.go:43`) relies on `InvalidGroup.Duplicate` to detect an existing SG and reuse it by name+VPC; against the sim the second create silently succeeds with a NEW id, so a retried/partial network-create leaks duplicate security groups instead of reusing. Real AWS rejects same-name groups within one VPC (different VPCs may reuse). Found by real-SDK probe. Fix: reject same name+VPC with InvalidGroup.Duplicate. |
| ~~1772~~ | P2 | sims (all 3) — image pull fails hard on transient registry throttle (toomanyrequests/429/503) | missing rate-limit handling | A moment of public-mirror throttling (public.ecr.aws "Rate exceeded" on PR #552's `sim (aws sdk)` — TestAmplifyHostingSSRComputeE2E) turns into a red suite because the pull path surfaces the first failure immediately (visible thanks to BUG-1744's propagation). The strict rate-limit rule applies: bounded exponential backoff retries on explicitly-transient registry errors in all three shared pull paths; non-transient errors still fail immediately. |
| ~~1771~~ | P3 | dispatcher-aws — spawned runner containers can't resolve host.docker.internal on Linux Docker | engine-conditional flag | Runner images spawned via the docker shape dial back to host-published services (bleephub, sockerless) via host.docker.internal — native on Docker Desktop/Podman 4+, absent on Linux Docker unless `--add-host host.docker.internal:host-gateway` is passed, which Podman 5 REJECTS (the GH-8 pair). Spawn now detects the engine (docker version components ~ "Podman") and adds the flag only where it's both needed and legal — the same runtime detection the sims use. |
| ~~1770~~ | P2 | bleephub — job messages bake the SUBMITTER's Host as the server URL; off-host runners cannot complete jobs | wrong URL provenance | `serverURL = scheme://r.Host` at workflow submit means a run triggered via `127.0.0.1` hands the runner a job whose Worker dials `127.0.0.1/_apis/connectionData` — connection refused for any runner not sharing the server's network namespace (the listener half works because IT uses the configured registration URL). Real GHES embeds its configured external URL. Fix: `BLEEPHUB_EXTERNAL_URL` honored by baseURL/serverURL derivation; request-Host remains the default when unset. Found by TEST 14 — the dispatcher-spawned runner ran the job but could never report completion. |
| ~~1769~~ | P2 | bleephub — agent registration drops `ephemeral`; `config.sh --ephemeral` refuses ("server does not support… 'Ephemeral' flag") | round-trip fidelity | The Agent struct has the field but handleRegisterAgent never copies it from the request, so the response omits it and the official runner aborts ephemeral configuration — the dispatcher's one-runner-per-job model registers all runners `--ephemeral`. Round-trip the flag and deregister ephemeral agents after their job completes (real GitHub auto-removes them). Found by TEST 14. |
| ~~1768~~ | P2 | bleephub — runs with no started job report `in_progress`, invisible to `?status=queued` pollers | run-status fidelity | Internal Workflow.Status goes "running" at submission; the REST mapping translated that to `in_progress` even when every job was still pending/queued. Real GitHub keeps the run `queued` until a job STARTS — the github-runner dispatcher (and any ARC-style autoscaler) polls `runs?status=queued` and so never saw label-stranded runs. runStatus now consults job states. Found by TEST 14 (dispatcher→bleephub). |
| ~~1767~~ | P3 | bleephub — admin token advertises X-OAuth-Scopes without `workflow`/`admin:org` | scope-header fidelity | The all-powerful admin token hardcodes `repo, read:org, gist` in the scope header while honoring every operation — classic-scope clients that gate on the header (the github-runner dispatcher requires `workflow`; gh checks `read:org`) wrongly refuse it. Advertise the scopes the token actually honors. Found by TEST 14 (dispatcher→bleephub). |
| ~~1766~~ | P2 | bleephub — `container:` object form loses env/ports/options/volumes | partial wire shape | `buildJobMessageFromDef` reduces an object container to `ContainerImage()` (bare string), silently dropping the object's env/ports/volumes/options/credentials — the runner provisions the job container without them. Emit the full mapping TemplateToken. Found alongside 1765. |
| ~~1765~~ | P1 | bleephub — jobServiceContainers emitted as plain JSON; the official runner rejects the job ("The template is not valid. Unexpected value ''") | wrong wire encoding | The runner deserializes `jobContainer`/`jobServiceContainers` as TemplateTokens (mapping = `{"type":2,"map":[{"Key":…,"Value":…}]}`, sequence = type 1, literals via `lit`); `buildServiceContainers` returned a raw `map[alias]spec` (with a nonexistent `environment` key, vs the runner's `env`), so every `services:` workflow failed at job start. Found by the first official-runner service-container run in the topology harness — the shape was never exercised before. |
| ~~1764~~ | P2 | dispatcher scopes.Verify — classic-PAT-only: tokens without X-OAuth-Scopes are rejected | impossible requirement | `Verify` requires the `X-OAuth-Scopes` response header to cover repo+workflow, but fine-grained PATs and GitHub App installation tokens never send that header — the error text even suggests "a new fine-grained PAT including: …", which can never pass. Fix: when the header is absent, verify by CAPABILITY instead — mint a registration token (`POST …/actions/runners/registration-token`), the exact permission the dispatcher needs. |
| ~~1763~~ | P2 | dispatchers — api.github.com hardcoded; GHES/bleephub targets impossible | missing endpoint override | `scopes.Verify` hardcodes `https://api.github.com/user` and the dispatchers expose no API-base flag, so a GHES instance (or bleephub) can never be targeted even though `poller.Client.APIBase` exists for tests. Fix: `--api-base` flag threaded through poller + scopes + registration-token mint (GHES shape `https://host/api/v3`). Found wiring the runner-as-cloud-task harness (dispatcher polling bleephub). |
| ~~1762~~ | P3 | dispatchers gcp+azure — runner registration token rides plain control-plane env | secret hygiene | `RUNNER_REG_TOKEN` is set as a plain `EnvVar` on the Cloud Run Job template (`-gcp/internal/spawner/spawner.go:125`) and a plain `EnvironmentVar` on the ACA Job template (`-azure/internal/spawner/spawner.go:115`) — readable by anyone with `run.jobs.get` / ARM reader on the resource for the token's ~1h validity. Both platforms have first-class secret bindings (Secret Manager env binding; ACA `secretRef`); use them. The AWS/docker variant is laptop-local (`docker inspect` exposure only) — same hygiene applies when the daemon is shared. |
| ~~1761~~ | P3 | specs/CLOUD_RESOURCE_MAPPING.md — dispatcher table documents the broken cleanup keying + stale poll cadence | spec drift | The per-cloud dispatcher table documents GCP cleanup keyed off `TerminalCondition.State` `CONDITION_*` — the exact approach the code REMOVED because it deleted runner-tasks right after spawn (see `executionStateForJob`'s comment) — and documents the Azure `ProvisioningState` keying that IS bug 1752. Also says the poll loop runs "every 15 s" while `poller.New` deliberately sets 60 s with a quota rationale (`pkg/poller/poller.go:83-88`). Spec must follow the fixed semantics. |
| ~~1760~~ | P3 | dispatchers (all 3) — no max-concurrent-runners cap | unbounded fan-out | A burst of N queued jobs spawns N runner containers / Cloud Run Jobs / ACA Jobs in one poll cycle with no ceiling — straight into ENI/concurrency/regional-CPU quota walls (predicted hurdles P-1/P-7 for sub-tasks apply equally to runner-tasks; ARC's `maxRunners` is the equivalent knob). Add a per-label `max_concurrent` config gating Spawn on the live count ListManaged already computes. |
| ~~1759~~ | P3 | dispatchers (all 3) — spec'd `runner_job_timeout` TOML knob unimplemented; per-cloud timeouts hardcoded/absent | missing config | CLOUD_RESOURCE_MAPPING.md § timeout knobs says the dispatcher TOML carries `runner_job_timeout` (default 3600) applied as the cloud-API timeout on the runner-task. No config schema has the field: GCP hardcodes `Timeout: 3600s` (kills any CI job >1h; GitHub allows 6h), Azure hardcodes `ReplicaTimeout: 21600`, AWS/docker applies no bound at all. |
| ~~1758~~ | P3 | dispatcher-azure — deployable hardening missing (healthz, env fallback, verify retry, rate-limit loop); zero tests | parity gap | The GCP variant binds `$PORT` with `/healthz`, falls back to `$REPO`, retries scope-verify with backoff, and honors rate-limit windows across poll+cleanup ticks (`rateLimitedUntil`). The Azure main.go is the bare laptop loop: deployed as an ACA App it fails probes and exits on a transient 403/429 at startup — crashlooping into the abuse window the GCP comment explicitly warns about. The module also has no `_test.go` at all (AWS has 7 test files, GCP has spawner tests). |
| ~~1757~~ | P3 | dispatcher-azure — runner container spawned with default ACA resources | undersized runner | The ACA Job template sets no `Resources`, so executions get ACA defaults (0.5 vCPU / 1 Gi). The GCP twin explicitly sets 2 cpu / 4 Gi because the default "is too small for a runner that compiles real workloads" (`-gcp/internal/spawner/spawner.go:120-135`). Real CI on the Azure path will OOM/crawl. Set parity resources using a valid ACA cpu/memory combination. |
| ~~1756~~ | P2 | dispatcher-gcp — Jobs whose RunJob failed (NO_EXECUTION) are never reaped | leak vs. own contract | `Spawn` deliberately leaves the Job orphaned when `RunJob` errors, with the comment "State recovery sweep will catch it" (`spawner.go:217-221`) — but the sweep's `isTerminalJobState` treats only `EXECUTION_SUCCEEDED/FAILED` as reapable, so `NO_EXECUTION` Jobs accumulate forever (and count as live owners' siblings in every ListManaged). Reap NO_EXECUTION Jobs older than a grace window via `Job.CreateTime`. |
| ~~1755~~ | P2 | dispatcher-aws ↔ ECS/Lambda runner images — env contract mismatch: spawn can't compose | name drift | The spawner injects `RUNNER_REG_TOKEN` + `RUNNER_REPO` (owner/repo) (`-aws/internal/spawner/spawner.go:79-83`), but `dockerfile-ecs/entrypoint-ecs.sh` and `dockerfile-lambda/bootstrap.sh` require `RUNNER_TOKEN` + `RUNNER_REPO_URL` (full URL) and fail-loudly on absence — a dispatcher-spawned ECS/Lambda runner dies at `${RUNNER_REPO_URL:?}` before registering. Only the vanilla/cloudrun/gcf images speak the dispatcher's names. The live harnesses spawn images directly with their own env, which is why cells 1+2 never exposed it. One contract, one set of names, every image. |
| ~~1754~~ | P2 | runner images — idle-timeout contract unimplemented or wrong-semantics; duplicate-spawn race not actually benign | claimed-but-absent mechanism | The dispatcher's statelessness story rests on "the runner image's entrypoint enforces a 60-s no-job timeout" (`-aws/internal/spawner/spawner.go:13`, README). No image implements an IDLE timeout: vanilla/ecs/lambda entrypoints run `run.sh --once` unbounded (a runner whose job was taken by a sibling waits forever — on cloud backends burning a 2cpu/4Gi task until the platform timeout), while cloudrun/gcf bootstraps wrap the WHOLE run in `timeout ${RUNNER_IDLE_SECONDS:-3600}` — a total-lifetime kill that would terminate an in-flight job at N s (with the AWS spawner's `RUNNER_IDLE_SECONDS=60`, any job >60 s). Real fix: bound only the pre-pickup window (watch for the Runner.Worker child / job-started marker), then let the job run to the job-level timeout. |
| ~~1753~~ | P2 | dispatchers gcp+azure — offline GitHub runner registrations never reaped | half-ported cleanup | CLOUD_RESOURCE_MAPPING.md requires the cleanup loop to deregister offline `dispatcher-*` runners (ephemeral runners that die without completing leave zombie entries), and both variants' `--cleanup-only` help text claims "(… + GitHub runners)" — but only the AWS loop calls `ListRunners`/`DeleteRunner`; the GCP and Azure `Cleanup` sweeps touch only cloud resources. Crash-killed runner-tasks accumulate offline registrations indefinitely. Fix in both loops; while there, add pagination to the shared `ListRunners` (silently truncates at 100 — acknowledged in its own comment). |
| ~~1752~~ | P1 | dispatcher-azure — cleanup sweep deletes runner Jobs while their execution is RUNNING | wrong state dimension | `isTerminalProvisioningState` keys reap-ability off ACA Job `Properties.ProvisioningState` (`Succeeded/Failed/Canceled`) — but that tracks the ARM RESOURCE provisioning, which goes `Succeeded` as soon as `BeginCreateOrUpdate` completes. Every spawned runner Job is therefore reap-eligible on the next 2-min sweep; `BeginDelete` on the Job kills its in-flight execution → CI jobs die ≤2 min after spawn. Exact Azure mirror of the GCP `TerminalCondition` bug already fixed by `executionStateForJob` (query the latest EXECUTION's status, not the resource state); fix the same way via the Job executions API. Never manifested only because Azure cells (Phase 123) haven't run live yet. |
| ~~1751~~ | P2 | backend-aca harness — nested TestMain deleted the parent suite's binaries | re-exec cleanup stomp | TestACAGitLabRunnerAttachStdin re-execs the test binary (apps-E2E mode), running TestMain AGAIN as a subprocess; the nested run's cleanup os.Remove'd the shared backend/sim binaries from under the parent, so any LATER parent test exec'ing the backend hit fork/exec ENOENT — exactly what the new shared-volumes test exposed in CI (and reproduced locally only in full-suite order). Nested runs now skip repo-path artifact removal. Also: the ACA shared-volumes test used the FROM-scratch eval image whose lack of a shell made `sh -c` scripts exit 1 with empty logs — switched to alpine like its passing ECS twin, and nonzero-exit failures now include container logs. |
| ~~1750~~ | P2 | backends — bind-mount→shared-volume translation parity incomplete (the runner-on-cloud gate) | partial mechanism | ECS/Cloud Run/GCF translated runner host binds to shared cloud volumes (`SOCKERLESS_*_SHARED_VOLUMES`), but lambda had the config UNWIRED into its bind path, and ACA/AZF had no mechanism at all (rejection message didn't even name a config). Parity completed across all six container backends with consistent sub-path-drop semantics; per-backend unit tests + ECS/ACA sim-backed integration tests prove the actual contract (container A writes via named volume, container B reads via translated host bind). What remains for runner-as-cloud-task is topology (runner images mounting the shared volume), not translation. |
| ~~1749~~ | P3 | bleephub — runner's "Canceled" result spelling leaked through unmapped | result normalization | The official runner's TaskResult uses the US spelling; normalizeResult only mapped "Cancelled"/"cancelled", so runner-cancelled jobs carried Result "canceled" — invisible to every ResultCancelled comparison (fail-fast, rollups, conclusions). Found by the harness's new cancellation e2e asserting the strict value. |
| ~~1748~~ | P3 | bleephub — actions hosted on bleephub itself can't resolve (`uses: owner/repo@ref` 502s) | action-source fidelity | The action tarball proxy serves the in-memory cache or fetches from real api.github.com; a repo ON bleephub holding an action (composite or otherwise) is never consulted, so self-hosted actions — GHES's bread and butter — fail with 502. Serve tarballs from bleephub's own git storage first (GitHub tarball layout: top-level `<owner>-<repo>-<sha>/` prefix), falling back to github.com for external actions. |
| ~~1747~~ | P3 | bleephub actions — invalid workflows refuse the run instead of a startup_failure shell | trigger-failure fidelity | Real GitHub creates a run with conclusion `startup_failure` (no jobs) when a triggered workflow has an invalid reusable-workflow ref or unparseable expansion; bleephub logs and creates nothing, so the failure is invisible to the runs API/UI. |
| ~~1746~~ | P3 | bleephub actions — runner groups unmodeled | missing ops | Single hardcoded pool; no `/orgs/{org}/actions/runner-groups` family (CRUD + group runners membership), `runner_group_id` always 1. |
| ~~1745~~ | P2 | bleephub actions — cancellation never reaches running jobs; always()/cancelled() jobs don't run after cancel | cancellation semantics | Cancel marks pending/queued jobs cancelled but a job executing on a runner runs to completion (no `JobCancellation` broker message — the runner's open mid-job poll exists exactly for this), and pending jobs gated on `always()`/`cancelled()` are force-cancelled instead of dispatched (real GitHub runs them with cancelled()==true). |

## False Positives

| Area | Finding | Why it is not a bug |
|------|---------|---------------------|
| `backends/aca/azure.go::fakeCredential` | Returns literal `"fake-token"` against simulator endpoints. | Simulator auth does not validate bearer tokens. Production clients use `azidentity.NewDefaultAzureCredential`; this credential is only for simulator endpoint clients. |
| `cmd/sockerless-admin/api_observability.go::envOrDefault` | Returns canonical OTel resource-attribute name when unset. | This is a documented default-value helper, not an error-hiding fallback. |
| aws scheduler simulator — GetSchedule `awsvpcConfiguration` (issue #477) | Reported as `Target.EcsParameters.NetworkConfiguration.AwsvpcConfiguration` returning None. | The sim persists + returns all three fields under the **lowercase** `awsvpcConfiguration` wire key — exactly what aws-sdk-go-v2 `scheduler` serializes/deserializes (`object.Key("awsvpcConfiguration")` / `case "awsvpcConfiguration":`) and botocore/GetSchedule use. The consumer's CLI assertion queried capital-A `AwsvpcConfiguration`, which fails against real AWS too; the Terraform read path (Go SDK) is unaffected. Consumer closed it. |

## Class-of-Bug Rules

- No stubs, fakes, mocks, synthetic data, silent fallbacks, or degraded modes.
- Public simulator APIs must match real cloud APIs; local gateway/admin plumbing must not leak into those APIs.
- Backend host primitive must match the cloud service: ECS in ECS, Lambda in Lambda, Cloud Run in Cloud Run, GCF in Cloud Run Functions, ACA in ACA, AZF in Azure Functions.
- External test fixtures use real clients: official SDKs, vendor CLIs, Terraform providers, and `gh` for bleephub.
- Closed enumeration means full-table audit before claiming fixed.
- Reopens require a postmortem: what test passed but should have failed, what client path was missed, and what new canonical-client test catches it.
- List operations need paged-iterator tests.
- Stateful resources need state-machine assertions.
- Mux pattern overlap is a recurring simulator bug class; run the overlap scanner when adding routes.
