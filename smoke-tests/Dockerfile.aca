FROM golang:1.25

RUN apt-get update && apt-get install -y --no-install-recommends curl && rm -rf /var/lib/apt/lists/* \
    && ARCH=$(uname -m) \
    && curl -fsSL "https://download.docker.com/linux/static/stable/${ARCH}/docker-27.5.1.tgz" | tar xz -C /usr/local/bin --strip-components=1 docker/docker

# Build simulator (independent module, not in go.work)
WORKDIR /sim
COPY simulators/azure/ /sim/
RUN GOWORK=off go build -tags noui -o /usr/local/bin/simulator-azure .

# Copy source and build backend
WORKDIR /src
COPY api/ api/
COPY agent/ agent/
COPY backends/core/ backends/core/
COPY backends/azure-common/ backends/azure-common/
COPY backends/aca/ backends/aca/

RUN cd backends/aca && GOWORK=off go mod download && GOWORK=off go build -tags noui -o /usr/local/bin/sockerless-backend-aca ./cmd/sockerless-backend-aca

COPY smoke-tests/run.sh /test/run.sh
RUN chmod +x /test/run.sh

ENV BACKEND=aca
WORKDIR /test
ENTRYPOINT ["/test/run.sh"]
