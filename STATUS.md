# Sockerless - Status

Roadmap [PLAN.md](PLAN.md) - resume [DO_NEXT.md](DO_NEXT.md) - bugs [BUGS.md](BUGS.md) - narrative [WHAT_WE_DID.md](WHAT_WE_DID.md).

## Snapshot

| | |
|---|---|
| Active branch | `feat/ratchet-cloud-data-planes` |
| Terraform-in-ECS validation | The HashiCorp Terraform 1.15.8 workload image carries an ahead-of-time filesystem mirror of AWS provider 6.50.0, so its private-subnet task performs one offline initialization and one apply without undeclared internet egress. The task publishes exact output through Amazon CloudWatch Logs, and terminal Step Functions failures report it immediately; the focused execution and exact AWS SDK N-Z shard passed. |
| Filesystem staging validation | Core filesystem-driver tests force the direct extraction path to fail with a child beneath a regular file, proving staging without relying on `/usr/local` permissions that differ between local and hosted runners. |
| Dependency freshness | The repository-owned upgrade fan-out refreshed the coordinated AWS SDK patch wave, Google Cloud Spanner client, and every affected resolved transitive graph after pre-push validation detected their publication. Go 1.26 module reconciliation advanced the Microsoft Azure and Google Cloud common backends to the selected `go-isatty` 0.0.24 transitive release. A later pre-push publication advanced gRPC to 1.83.0 across both Cloud Run backends, the shared Google Cloud backend, the simulator, and its official SDK suite; every affected module and the complete official Google Cloud SDK suite passed. |
| Client-module acquisition | Google Cloud and Microsoft Azure SDK/CLI jobs pre-fetched their separate official-client modules through the bounded module-proxy retry helper before invoking `go test`. |
| Firecracker cache ownership | Simulator SDK, CLI, and Terraform matrix jobs restored the immutable Firecracker seed cache but never saved their test-mutated guest root filesystems. The dedicated Firecracker job remained the sole cache publisher, eliminating post-job permission failures on root-owned guest files. |
| Continuous-integration annotations | The Microsoft Azure workload-dispatch invariant retains its two justified `os/exec` exceptions as reviewable source comments without emitting `t.Logf` file-and-line output that GitHub's Go problem matcher misclassified as failure annotations on a successful job. |
| AWS Key Management Service policy persistence | Customer-managed key policies survived the simulator's SQLite serialization and process restart. The focused persistent-store regression passed, and the production-shaped HashiCorp AWS provider graph declared a custom policy so its create-time `GetKeyPolicy` waiter exercised the same durable read-back contract that ECS Dev Desktop required. |
| AWS DynamoDB auxiliary persistence | Table TTL, point-in-time recovery, and tags lived in a dedicated SQLite-backed settings record rather than non-serialized response fields. Store-reopen coverage proved all three survived, table deletion removed the auxiliary state, IAM resource-tag conditions read the durable tags, and the production-shaped HashiCorp AWS provider graph exercised all three API wait paths. |
| Google Cloud SQL Admin specification freshness | The exact hosted revision 20260722 v1 and v1beta4 Discovery documents were retained. Their method and route sets were unchanged; authenticated public-route coverage round-tripped the newly published `databaseCenterIntegrationEnabled`, `dmsManaged`, and top-level user `serverRoles` members. |
| Simulator persistence and statefulness | The persistent store retained cloud state that public JSON intentionally hid, rebuilt monotonic counters and derived revisions, rebound Network Load Balancing and Amazon RDS data-plane listeners, and adopted or resumed state-directory-owned Amazon ECS, AWS Batch, CodeBuild, Amplify, Lambda, scheduler, autoscaling, and Step Functions work after hard process replacement. Accepted asynchronous Lambda events were durable before `202 Accepted`, and Step Functions resumed the original external task rather than duplicating it. Official AWS SDK, AWS CLI, and HashiCorp AWS provider restart scenarios passed; the production Compose recipes enabled persistent state on named volumes for all three cloud simulators. Elastic Load Balancing fidelity fixtures provisioned issued, exportable AWS Certificate Manager certificates and isolated real listener/DNS coordinates, so the complete compute shard exercised the live data plane without host-port coupling. |
| Conformance measurement | The per-API coverage floors measured served surface instead of pattern collisions (BUG-2651). Google Cloud Spanner advanced from 58 to 82 of 198 documented methods after the REST session data plane joined the existing gRPC surface; SQL, DML, reads, transactions, partitions, and batch writes reached real handlers over both transports. Microsoft Azure remained at 1878 of 2612 documented methods, and every Amazon Web Services floor remained unchanged. |
| Simulator fidelity from measurement | Probing exposed handlers answering resource semantics for methods they do not serve — Firestore had created a document in a collection named after the method — and they now resolve the method before the resource, as Google's API frontend does. The Azure Storage data planes stopped falling through to sibling handlers (Set Blob Tier had answered `201 Created` having created a blob), which uncovered and fixed Files `?comp=range` landing on Create File, Set Queue Metadata falling to Create Queue, and missing ranged reads. The Azure Files data plane wrote where an Azure Container Apps workload bind-mounts it, replacing two implementations of which the disk-backed one was unreachable. Amazon ECS honoured the task definition's `networkMode` across all four modes, and an unresolvable VPC became an error rather than a silent fall back to Docker's default bridge. An Amazon RDS client calling `DescribeAccountAttributes` no longer received Amazon EC2's XML. |
| Deployment recipe | A committed `deploy/` hosted Sockerless Admin, the three simulators, and a Shauth + Ory Hydra + PostgreSQL stack at persistent origins: `compose.yaml` ran the published `ghcr.io/e6qu/*` images (a required `SOCKERLESS_TAG`), `compose.build.yaml` built from source, `provision.sh` registered the consoles as Shauth OpenID Connect clients and provisioned the cloud federation resources through the real APIs, and `smoke.sh` proved health, unauthenticated redirects, each data plane's enforcement contract, and a `sockerless login` authorize URL. A Caddy reverse proxy TLS-terminated every hostname (Admin and the consoles reject non-HTTPS non-`localhost` issuers) with a local CA the federated-JWT and backchannel-logout services trusted via `SSL_CERT_FILE`. The full boot → provision → smoke passed fresh and idempotent; `make deploy-smoke` is the documented manual gate (a cold five-image build exceeds the CI ceiling). |
| Azure portal federation | BUG-2640 closed: the Azure portal's Workload Identity Federation exchange moved into the console's own server-side broker (`/auth/federation/token`) because real Microsoft Entra serves no CORS for the `client_credentials` grant; the simulator gained faithful Azure Resource Manager and Microsoft Graph CORS (the token endpoint deliberately none), the SPA calls the broker on its own origin and reads the cloud cross-origin, and the relying-party harness runs the Azure console and cloud as separate processes — provisioning the console identity and federated identity credential on the cloud, then starting the console pointed at it. This unblocked the Azure browser data plane and the app-registration/client-secret minting flow deferred since the credential-minting phase; the relying-party matrix now mints an Entra client secret through the portal in a real browser. |
| Terminal sign-in | `sockerless login` (stdlib-only) ran the RFC 8252 loopback PKCE flow against Shauth — registered as a public Hydra client with the any-port loopback redirect, traversing the explicit consent screen once — and wired vendor-native credentials the vendor tools refresh themselves: an INI-preserving AWS profile (`role_arn` + `web_identity_token_file` + `endpoint_url`), a real workforce `external_account` ADC file activated through `gcloud auth login --cred-file` (which surfaced and fixed BUG-2641 — the simulator lacked STS `POST /v1/introspect`, implemented against gcloud's captured live wire), and `az login --federated-token` against a TLS-serving simulator instance (az/MSAL reject http authorities). `sockerless logout` removed what login wrote. The relying-party matrix drove the full story — browser sign-in, then `aws`/`az`/`gcloud` each verified vendor-natively, then logout. |
| Console account management | A Shauth-authenticated operator managed accounts, projects, and subscriptions through each cloud's real APIs over the federated session. Google Cloud gained a real Cloud Resource Manager slice — replacing a faked partial v3 surface that synthesized projects and operations — with v1+v3 lifecycle, Cloud Billing reads, ID-or-number resolution, 403-without-disclosure, soft-delete, and the console's real project-picker chip threading the selected project through every page; SDK, gcloud, and `google_project` Terraform coverage landed together. Microsoft Azure gained the Microsoft.Subscription alias API at vendored-Swagger fidelity (billing-scope creation + adoption, provisioning-state polling proven against both poller stacks, rename/cancel/enable) with a portal Subscriptions blade and armsubscription/az/`azurerm_subscription` coverage, running as its own `tf (azure subscription)` CI shard. AWS gained the Organizations console page (async CreateAccount polling, organization-not-in-use state, remove/close) over the existing slice, with real awsjson1.1 error codes surfaced. The relying-party matrix drove both new browser flows and passed with its exit code observed directly. |
| Console credential minting | All three simulator consoles let a Shauth-authenticated operator mint the credentials their vendor CLI needs, through the operator's federated credentials calling the cloud's real APIs — AWS IAM users + Create access key (Query protocol, browser-side SigV4, one-time secret disclosure), Google Cloud service accounts + one-time `privateKeyData` JSON key download, Microsoft Entra app registrations + Certificates & secrets (Graph `applications`/`addPassword`). Proving the loops hardened both weak token endpoints: the Google OAuth 2.0 endpoint now verifies service-account assertion signatures against registered, revocable public keys, and the Entra v2.0 endpoint validates client secrets for directory-registered applications with real AADSTS failures. The invented `/sim/v1/entra/users` routes and `entraActiveOID` global were deleted for Graph provisioning. CLI tests proved each minted credential authenticates the real vendor CLI (and wrong/tampered/revoked material is rejected); the Shauth relying-party browser matrix mints on the AWS and Google Cloud consoles with one-time disclosure asserted (the harness's federation role gained `iam:*`, which the simulator's IAM enforcement correctly required), and the full local matrix passed with its true exit code observed. The Azure portal's browser-side federation exchange is same-origin-only (real Microsoft Entra serves no CORS for `client_credentials`), so its browser minting flow is staged with the deployment-phase broker work (BUG-2640); Entra minting is proven by the Azure SDK and az CLI suites. |
| Skip-if-absent gate + sweep | `scripts/check-no-tool-absent-skips.sh` gained two shapes beyond `t.Skip` — a `fmt.Print*`/`log.Print*` tool-absent phrase and a `LookPath`-anchored `os.Exit(0)` — closing the TestMain-level silent-skip hole, and exempted skips that self-identify as platform/kernel-capability gates so a GOOS gate reading "not available" was not a false positive. Every remaining tool-absent skip resolved to install-or-fail-loud: the Google Cloud CLI suite installed a pinned release when `gcloud` was absent (the Azure CLI suite failed loud, `az` having no clean TestMain install); the six vestigial `docker CLI not available` guards (Docker already guaranteed by each TestMain's image build) were removed while the netns capability gates stayed; and the `session-manager-plugin`, `git`, `gcloud` (Firestore), and `nsenter` skips became `t.Fatal` against CI-provided tools. |
| Required-check drift gate | A version-controlled manifest (`.github/required-status-checks.txt`) mirrored `main`'s required-status-check contexts, and `scripts/check-required-status-checks.sh` (pre-commit + the `build-gates` CI job) enumerated every check name the workflows could emit — rendering each job's `name:` over its matrix — and failed the pull request when a required context was no longer emittable, so a job rename could no longer silently stall the merge queue (BUG-2633). A maintainer-only `--verify-branch-protection` mode reconciled the manifest against live branch protection. |
| Credential enforcement | All three simulators verified caller credentials on their data planes as the real clouds do (BUG-2625): AWS recomputed and verified the SigV4 signature at the awsjson/query control plane and S3 (persisting `ASIA` temporary-credential secrets, seeding a bootstrap account credential); Google Cloud and Azure verified the bearer's signature, issuer, audience, and expiry against the simulator-minted token (GCP consolidated to one RS256 key + published JWKS; Azure reused its RS256 verifier and added the audience check). Token minters, discovery/JWKS, metadata/IMDS, health, OCI registries, and the anonymous flows (`AssumeRoleWithWebIdentity`/SAML, presigned URLs, S3 public reads) stayed exempt. Every SDK/CLI/Terraform suite, the sockerless backends (their `WithoutAuthentication`/`fakeCredential` fakes replaced with a real GCP metadata token source and Azure `DefaultAzureCredential`), and the relying-party provisioning authenticated for real; the console browser e2e reached the enforcing simulator unauthenticated, so its data-render assertions moved to the authenticated relying-party path. The AWS ECS Terraform harness (BUG-2569) made services synchronous control-plane objects and reaped subprocesses deterministically, so apply/destroy terminated with no leaks. |
| Branch purpose | The cloud data-plane ratchet made Google Cloud Spanner transactional over official REST and gRPC clients; made AWS Amplify job, artifact, retry, hosting, compute, and WAF state observable through public contracts; and proved AWS Step Functions could launch an Amazon ECS task whose official HashiCorp Terraform process applied resources back through the standard global AWS endpoint. The AWS simulator retained control-plane resources—including DynamoDB TTL, point-in-time recovery, and tags—live workloads, asynchronous work, data-plane bindings, counters, and cross-service checkpoints across hard replacement, while official SDK, CLI, and Terraform clients verified recovered state externally. The AWS Batch console gained complete job and definition operation paths, all three deployed cloud simulators retained state on named volumes, Google Cloud SQL Admin revision 20260722 was implemented, and current dependency graphs, generated surface catalogs, and CI shard assignments remained checked in. |
| Product ownership | Standalone repositories owned product source, web interfaces, Terraform, official-client tests, and runner consumer harnesses. Sockerless remained a standalone Docker-compatible cloud backend and simulator project. |
| Operator identity | Admin required a Shauth administrator role, exact issuer validation, server-tracked sessions, front- and back-channel logout, and a public no-cache signed-out page. Its logout completion bridge remained reachable after local revocation and redirected only to Shauth's correlated completion endpoint. |
| Simulator identity | All three dashboards used first-party OpenID Connect authorization code with PKCE, exact issuer validation, signed server-tracked sessions, identity, RP-Initiated Logout, signed back-channel revocation, atomic replay rejection, and application-local signed-out return. Native cloud API slices remained independent of browser authentication. |
| Product-interface contract | Admin and all three simulator dashboards marked the visible account control with the exact Shauth username and the real sign-out control with a stable qualification hook. The browser matrix asserted the rendered username against each application's identity endpoint and signed out by clicking the product control, so a broken product shell failed qualification even when its protocol endpoints answered. |
| Exact browser contract | Shauth `0fda680cba964e5768ed75a9c3e5b7230c418ca6`, PostgreSQL, Ory Hydra, four freshly compiled relying parties, and Chromium passed direct and catalog entry, relying-party and provider logout, exact completion bridging, local signed-out reload and reauthentication, global revocation, anonymous fail-closed behavior, release identity, active event-stream readiness, and validator-credential isolation in eight serialized flows. |
| Production builds | The root build created every frontend bundle before compiling all 11 UI-bearing Go binaries. A repository gate rejected build-order regressions that could silently produce `noui` release binaries. |
| Continuous integration budgets | Every ordinary workflow job declared a timeout of at most 15 minutes, enforced by a tested repository gate. The historically over-budget AWS edge and Amazon EC2 command-line interface groups were split into four non-overlapping shards while all 630 AWS CLI tests remained covered exactly once. The AWS SDK runner no longer scanned the whole hosted filesystem after successful tests: normal diagnostics were constant-time, disk-budget failures scanned only three relevant paths for at most five seconds each, and the workflow-budget gate rejected any future recursive `du /`. |
| Fuzzing | The nightly harness ran targets in bounded parallel batches with one Go fuzz worker per target, preserved truthful logs and crasher handling, failed on missing modules, and passed a real one-second run across every target in all nightly modules. |
| Dependency freshness | Google Cloud Spanner used v1.94.0. The AWS client graph used `github.com/aws/aws-sdk-go-v2` v1.43.2 with the current service releases across the AWS common, Amazon ECS, AWS Lambda, simulator SDK-test, and shared-test modules. HashiCorp AWS provider 6.50.0 and HashiCorp Google provider 7.36.0 passed their production-shaped graphs. Every upgraded module and both complete official SDK suites passed, and the authenticated whole-repository freshness audit reported no drift. |
| Release images | Admin and the AWS, Google Cloud, and Microsoft Azure simulators published direct native ARM64 and AMD64 OCI manifests plus an immutable short-SHA multi-architecture OCI index. Every architecture image carried its source revision as an OCI label, preventing byte-identical releases from collapsing onto one GitHub Container Registry package version, and retention preserved any version carrying a retained tag before deleting obsolete versions. |
| Open bugs | [BUGS.md](BUGS.md) retained 8 defects. AWS WAF statement evaluation and transactional Elastic Load Balancing listener binding were complete; AWS Amplify Hosting image optimization and macOS Podman's missing nested KVM for the real Google Compute Engine Terraform graph remained explicit. |

## Verified Gates

- An unmodified ecs-dev-desktop Terraform consumer applied 178 resources through the standard `AWS_ENDPOINT_URL`, passed its external shared-infrastructure assertions, produced a zero-change plan with runtime Smithy validation reporting no violations, destroyed all 178 resources, and retained a clean external working tree.
- The complete official AWS SDK suite passed in 487 seconds after the same-day dependency upgrade; all five official HashiCorp AWS provider packages passed apply, refresh, workload/data-plane assertions, and destroy.
- The Terraform-in-ECS proof passed a focused real-container execution and the exact AWS SDK N-Z shard with its exact provider mirrored into the private-subnet workload image and task output required through Amazon CloudWatch Logs.
- AWS DynamoDB table settings passed a SQLite close-and-reopen regression, the complete AWS simulator unit suite, focused official AWS SDK TTL/PITR/tag coverage, and the production-shaped Terraform fixture declared all three settings.
- Google Cloud SQL Admin revision 20260722 retained the same 75 public methods and paths in both versions while adding three schema fields; the complete simulator unit suite and focused authenticated public-route coverage passed their round trip.
- The complete official Google Cloud SDK suite passed, and the focused HashiCorp Google provider Cloud Spanner graph passed apply, a zero-change plan, and destroy.
- The gRPC 1.83.0 dependency wave passed both Cloud Run backends, the shared Google Cloud backend, the Google Cloud simulator, and the complete official Google Cloud SDK suite; its Cloud Build test built and pushed through a real temporary registry with leak-free container and volume cleanup.
- The shared simulator Linux test image loaded through Buildx and contained Firecracker v1.15.1 plus `unsquashfs` 4.5.1; the macOS Podman host's absent `/dev/kvm` remained explicit under BUG-2764 instead of skipping the test.
- The authenticated whole-repository dependency freshness audit reported no drift.
- The complete repository test and lint fan-outs passed.
- A clean full production build passed and embedded every current web bundle.
- The full pre-commit suite passed before final branch publication.
- The official AWS SDK hard-restart matrix retained control-plane resources, CloudWatch log sequence tokens, Amplify and Lambda callbacks, accepted asynchronous Lambda work, live Amazon ECS, AWS Batch, and CodeBuild workloads, a Step Functions synchronous Amazon ECS task without duplication, Network Load Balancing listeners, and an Amazon RDS native endpoint.
- The exact official AWS SDK compute shard passed after every HTTPS/TLS listener fixture imported issued, exportable AWS Certificate Manager material and selected an isolated live port; the nested process-mode simulator also received an explicit ephemeral Route 53 DNS coordinate.
- The official AWS CLI retained service state and CloudWatch log upload ordering across hard simulator replacement.
- The production-shaped HashiCorp AWS provider graph completed apply, hard simulator replacement on the same persistent data directory, a zero-change refresh plan, and destroy in 225 seconds.
- All 85 AWS Lambda and all 37 AWS Step Functions vendored operations passed the exact service-conformance gates.
- Official AWS SDK, AWS CLI, and Terraform suites passed against the simulator; selected AWS Lambda and AWS Step Functions flows also matched short-lived live AWS resources in an external differential.
- Official AWS SDK and AWS CLI eventing shards passed real cross-service delivery through Lambda, Step Functions, EventBridge, EventBridge Scheduler, Amazon SNS, Amazon SQS, CloudWatch, CloudWatch Logs, and CloudTrail; the official HashiCorp AWS provider production-shaped apply/destroy passed.
- The exact AWS SDK A–M shard passed ordinary ZIP invocation, durable execution, and durable callback replay through the managed Node.js runtime with zero Smithy response-shape violations.
- The AWS console passed 241 Chromium package tests and the authenticated Shauth/Ory Hydra/PostgreSQL browser matrix, including real federated event delivery, Step Functions execution, CloudWatch resources, CloudWatch Logs account policies, S3-backed CloudTrail, Firehose-to-S3 delivery, AWS Private CA lifecycle, a real AWS CodeBuild container lifecycle, connected AWS Amplify branch management, and Amazon RDS authentication modification.
- The complete official AWS SDK drove AWS Step Functions Amazon ECS and AWS CodeBuild integrations and cancellation, a deployed AWS Lambda function reached Amazon SQS through standard endpoint coordinates, a private authenticated Git server produced and cached a Python plus Node.js AWS Amplify artifact, and stock PostgreSQL and MySQL drivers proved PostgreSQL, MySQL, and MariaDB authentication, password rotation, and persistence.
- The focused official AWS SDK Step Functions workload flow passed with real Amazon ECS and AWS CodeBuild containers after its shard provisioned the exact configured public images before `m.Run`, and the private authenticated AWS Amplify build restored and served its second-release cache in five consecutive real-container runs.
- The AWS, Google Cloud, and Microsoft Azure console accessibility checks focused the loaded document before keyboard traversal and passed in real Chromium, proving each skip link remained the first in-document focus target without browser-chrome focus variance.
- The exact AWS CLI appdata shard passed after CloudWatch metric streams provisioned real Amazon S3, IAM, and Amazon Data Firehose dependencies.
- HashiCorp AzureRM 5.0.0 validated the Azure Container Apps and Azure Functions modules and examples, and completed a Microsoft.Subscription apply, zero-drift plan, and destroy against the Azure simulator.
- The Azure Terraform workflow installed Ubuntu's signed Caddy package through bounded APT acquisition instead of depending on unbounded Cloudsmith downloads.
- The production-shaped AzureRM 5 stack created real subnet network fabric from `addressPrefixes` and explicitly selected `log-analytics` for its linked workspace, while the official Azure SDK round-tripped the plural subnet member and the complete Azure portal suite retained failed-delete errors in the open Fluent dialog.
- Every production Terraform module validated with its real provider after the AWS Lambda module's Step Functions conformance role used the declared regional input for all IAM policy ARNs.
- The complete Terraform tree passed recursive canonical formatting.
- The root HashiCorp AWS provider 6.50.0 graph passed its complete concurrent apply, workload assertions, refresh, and destroy through Caddy HTTPS with runtime Smithy response validation armed.
- The complete UI build and test fan-out passed, including all 131 Microsoft Azure console tests and the retained failed-deletion error dialog.
- The hosted Google specification freshness gate passed with the exact five published Discovery documents; authenticated official-SDK transports exercised Bigtable `updateMemoryLayer` and Cloud Resource Manager v3 `fetchResourceSemantics`, and the generated coverage floors measured 164 of 164 and 126 of 126.
- The official Azure SDK round-tripped a Microsoft.Storage File-share stored access policy through ARM create and update, and Azure CLI observed Microsoft.OperationalInsights' `Enabled` ingestion and query defaults on create and read.
- The external AzureRM 5 stack completed its zero-drift plan before asserting canonical Microsoft.Storage ARM IDs for its Blob container, Table, and File share.
- Microsoft Azure NAT gateways retained a subnet association that arrived before public addressing and programmed real outbound fabric when the later public-prefix update completed.
- The complete nightly fuzz target matrix passed with a one-second budget per target.
- The exact real PostgreSQL, Ory Hydra, Shauth, compiled relying-party, and Chromium matrix passed all eight application-and-direction validation runs.

## Invariants

- The user merged pull requests; agents never merged them.
- Sockerless kept at most one open pull request and carried all related work in that branch.
- Pull-request branches were rebased on the freshest `origin/main` before publication.
- No stubs, fakes, mocks, synthetic responses, silent fallbacks, degraded modes, or skipped required tools were accepted.
- Every observed failure was fixed or recorded with evidence in [BUGS.md](BUGS.md).
