# Sockerless - Status

Roadmap [PLAN.md](PLAN.md) - resume [DO_NEXT.md](DO_NEXT.md) - bugs [BUGS.md](BUGS.md) - narrative [WHAT_WE_DID.md](WHAT_WE_DID.md).

## Snapshot

| | |
|---|---|
| Amazon ECS Express managed security group | The Express-managed group admits TCP 443 from the world and the container port from the VPC CIDR, so the real-VPC tier's nftables enforcement passes the ALB health-check and forwarding flow and Express rollouts become healthy. The lifecycle test owns an isolated VPC and authorized caller group, and its diagnostic reports per-task and per-target state. |
| Simulator surface-table generation | `scripts/seed-surface-tables.sh` pins `LC_ALL=C`, so macOS and hosted runners emit identical row order and the build gate compares like with like. |
| AWS simulator unit-test isolation | The delete-time drain test initializes the scheduler-state, deployment-record, and alarm stores its asynchronous reconciliation reads; the complete AWS simulator module suite passed. |
| Google Cloud Artifact Registry specification freshness | The multi-probe vendor fetch retained Artifact Registry v1 Discovery revision 20260727 after the hosted gate observed it; methods, paths, and schema fields were unchanged from the revision 20260724 pin, and the complete freshness audit passed. |
| Amazon ECS Express hosted rollout validation | The rollout assertion window covers the scheduler's full bounded placement-retry chain, matching real Amazon ECS rollout timing, and failure output retains desired/running/pending counts, both task definitions, deployment state, and the latest service events. The focused official AWS SDK Express suite passed. |
| Active branch | `elb-cloudwatch-acm-derivation` |
| Simulator restart, network and data plane | A workload reattaches to its VPC after a restart: a veth name left behind by the dead process is reclaimed rather than colliding with the one the restarted process needs, which had failed the AWS Lambda invocation behind the production-shape stack (BUG-2915). The Amazon RDS restart regression waits for a recovering database to accept connections instead of failing on its own `starting up` reply (BUG-2916). |
| Guest execution | `FirecrackerVM.Exec` runs a command inside a booted guest over the machine's own SSH service, and reports a command that ran and failed as a non-zero exit code rather than an error, so a caller can tell that from not reaching the guest at all. A per-machine ed25519 key is authorized in the root filesystem before it is packed, the way a cloud installs the public keys in a machine's OS profile; the private half never leaves the host. This generalizes the mechanism the Firecracker arithmetic harness has always used into something the simulators can call, which is what the eight remaining Azure virtual machine operations were waiting on (BUG-2914). |
| Azure virtual machines | Complete at 29 of 29. Everything answerable from the machine's own state or by moving its guest process was served before; the eight that describe something happening *inside* the machine now run there. A Custom Script extension executes its command in the guest and reports the output and exit status it produced; patch assessment reads the guest's own package manager, classifying an update by the archive it comes from; installation runs that package manager under the caller's masks and honours the reboot setting; and a capture quiesces the guest, copies the disk it is running on into the named container, and returns the template pointing at it, refused unless the machine was generalized. A handler this simulator does not implement is refused rather than reported succeeded, and protected settings are never returned. |
| Google Compute Engine packet mirroring | A policy forwards real traffic. `realexec.StartMirror` reads the mirrored interface and transmits every frame that passes the filter out of each collector's interface, verbatim and headers intact, so a collector receives the traffic as it does in the cloud. `collectorIlb` resolves through forwarding rule, regional backend service and instance group to the instances behind it; mirrored resources may be named instances, a subnetwork, or a network tag; and insert, patch and delete reconcile the running sessions, so disabling a policy stops it. The seven methods were served before as metadata that mirrored nothing (BUG-2910). |
| Firecracker assets | A kernel is verified and evicted whenever it is present rather than only when the root-filesystem marker is too, so an interrupted download no longer poisons the cache for every later run (BUG-2911), and the kernel magic is checked per architecture — PE on arm64, ELF elsewhere — so an arm64 host boots the image Firecracker publishes instead of rejecting it as corrupt (BUG-2912). |
| Resource-scoped IAM authorization | Nineteen services authorize a request against the resource it names, from the resource types AWS publishes and the ARN format published beside each — one identifier, several nested, or none at all, in whatever service's ARN the format names. A service supplies a lookup that reads a field in its protocol and, where the API renamed an identifier, an alias table held to the vendored model by a test; Amazon ElastiCache and Amazon EventBridge needed no renamings. Where the reference publishes one resource two ways — an Amazon EventBridge rule on the default bus and on a custom one, which end in the same variable — the request decides which applies before the derivation runs. AWS Organizations reads the resource type off the identifier instead, because its members accept several types at once and its ids carry the prefix that says which. 1,553 of the 1,974 served operations that authorize against a resource type now derive it, ratcheted by `TestIAMResourceDerivationCoverage`; the remainder is BUG-2909. |
| Response pattern conformance | All three simulators match a response string against the pattern its specification declares, not only its type. Discovery patterns are anchored because they describe a whole value; Smithy and Swagger patterns anchor themselves and are matched as written. AWS reports three divergences, all patterns stricter than the service they describe (BUG-2932) and allowlisted against it; Google Cloud reports none across its SDK and CLI suites. Each check carries its own regression, so removing it fails a test rather than going unnoticed while a simulator happens to be clean. |
| Amazon CloudWatch Logs paging | `GetLogEvents` returns a bounded page — 10,000 events or 1 MB — anchored at the end of the stream `startFromHead` selects, so a default read shows the newest lines rather than every event from the oldest. |
| Real traffic capture | An `AF_PACKET` socket opened inside a workload's network namespace records the frames crossing its interface into a real libpcap file, validated by `tcpdump` and `file(1)`. Five-tuple filters parse real headers; per-packet, total-byte and duration bounds are honoured, and truncation preserves the frame's length on the wire. A host that cannot capture fails loudly rather than returning an empty file. |
| Azure Network Watcher | Complete at 35 of 35 operations. The six packet-capture operations run real sessions against the target machine's live interface and write the recording into the storage account the capture named, through the same Blob data plane a client reads it back from (BUG-2888 closed). |
| Shauth integration is gated per simulator | All three simulators mount the Shauth OpenID Connect relying party as an addition to their own cloud authentication, and each now has a regression that builds the simulator the way `main()` does with Shauth configured and asserts it: every endpoint of the contract is routed, the session and federation-subject reads answer 401 anonymously rather than 404, and the cloud data plane still enforces its own credential — which is what proves Shauth adds to cloud authentication instead of replacing it. The opt-in half is pinned too: with no identity-provider coordinate no relying party is served. The `ui-auth` package's own tests proved the package in isolation; nothing had asserted a simulator actually mounts it, so the wiring could have regressed in any of the three unnoticed. |
| Vendored specification freshness | All three clouds are gated again, each as its own CI step capturing the newer document it sampled; the check had been passing only `gcp`, so seven AWS Smithy models and two Azure swaggers drifted unnoticed. The refresh added three real EC2 transit-gateway policy-table entry operations, and the entries read that had answered with a fixed empty list now reports what the table holds. |
| Amazon S3 scope boundary | S3 serves 109 of its 112 modelled operations, and the three it does not serve are scoped out by decision rather than outstanding: `WriteGetObjectResponse` (S3 Object Lambda callback), `CreateSession` and `ListDirectoryBuckets` (S3 Express One Zone). None is addressed to the regional surface the simulator hosts, none has a consumer in this repository, and each would require hosting another endpoint family plus the feature behind it. The reasoning and the condition to revisit live beside the ratchet list and in the S3 surface table. |
| Google Cloud Resource Manager | All three API versions complete: v1 at 76/76 method spellings, v2 at 24/24 over a newly vendored Discovery document, v3 at 126/126. Organization Policy stores policies per hierarchy node and resolves the effective one by walking organization → folder → project, with real etag concurrency; the constraint catalog and the accepted set are one set, and two IAM constraints are enforced by the service they govern with Google's exact FAILED_PRECONDITION contract. `getAncestry`, a real organizations store (403, never 404, for an organization the caller cannot see) and one lien collection under both spellings complete v1; v2 serves the folders collection `gcloud resource-manager folders` speaks. |
| Google simulator request ordering | A URI no Google method publishes answers 404 anonymously and a real API path answers 401, matching Google's own API frontend — routing precedes the credential gate, so an absent endpoint no longer reads as an authentication failure (GitHub issue #875). The Compute Engine load-balancer front end sits outside the gate, as no real client presents a Google access token to a load balancer. |
| Azure storage data planes | Blob served all 69 documented operations, Azure Files all 51, and Queues all 16. Leases, snapshots, tags, immutability, page/append semantics, container ACLs, multipart batch, and Blob Query became real behavior over real state; Files directories exist at any depth on the backing store so a mounting workload walks the same tree, with real hard/symbolic links and range listing read from the filesystem's own extent map. |
| Azure Microsoft.Network | Nine vendored swaggers that served nothing now serve 116 of 123 operations. Private endpoints write into the same connection object the target resource's own surface serves, application security groups affect NSG evaluation, the Application Gateway is a real forwarding L7 listener with probe-driven backend health, and Network Watcher diagnostics are computed from the real rules, routes, and address space. |
| Azure Microsoft.Subscription | All 15 operations served, including the ownership-acceptance long-running operation whose polling target follows the published examples rather than assumption, with tenant policies enforced where the simulator models the mechanism and projected where it does not. |
| AWS Amplify Hosting image optimization | ImageOptimization manifest targets serve the real primitive: imageSettings validation with the Next.js optimizer's exact error contract, artifact/allowed-remote fetch, no-upscale resize, format negotiation whose Content-Type always matches the served bytes, ETag/304, Cache-Control flooring, and a durable transform cache. Malformed deploy manifests fail the job with Amplify's real CustomerError line, and route fallback works on all three target kinds. |
| Azure Container Apps configuration | The full armappcontainers Configuration surface round-trips (dapr, identitySettings, maxInactiveRevisions, runtime, service), and dapr.enabled assembles the real pinned daprd sidecar into each replica's network namespace with lifecycle tied to the replica set — proven by a live replica reading daprd's own metadata endpoint. |
| Azure secret rotation and messaging auth | All seven key-bearing surfaces (Storage, Redis, Event Hubs, Service Bus, API Management, Container Registry, Logic Apps incl. callback-URL signatures) rotate through one durable generation store; Event Hubs' constant-key defect and Service Bus' orphaned-rule cascade closed with it; and the Event Hubs/Service Bus AMQP + HTTP data planes verify real SAS signatures against current rotation-aware keys with the services' real error shapes — no permissive mode, negative controls included. |
| Workload log streaming | All three simulator container runtimes followed the Docker log stream live into the CloudWatch, Cloud Logging, and Log Analytics sinks while workloads ran, with a post-exit drain that skips exactly the lines already delivered — long-running service tasks became observable without stopping them (GitHub issue #872). An official-SDK regression drove a real Amazon ECS service and required its container's stdout in CloudWatch while the task was still RUNNING; the previous guard drove a one-shot `RunTask`, whose output also arrives through the post-exit drain, so it could not tell the live half from the drain. |
| AWS container log ingestion | Amazon ECS and AWS Lambda container output reached CloudWatch through the same ingestion path `PutLogEvents` uses: the stream's first/last event timestamps, ingestion time and upload sequence token advance, embedded-metric documents are extracted, and the log group's metric filters fire. `DescribeLogStreams` ordered by `LastEventTime` therefore distinguished a service task that is still writing from one that has said nothing since it started, and an alarm on a running service's error lines could fire (BUG-2905 closed). |
| Simulator persistence completeness | A full three-sim audit closed every gap where cloud state died with the process despite `SIM_PERSIST=true`: bulk-data roots (EFS, EBS, Amplify cache, GCS bytes, Azure Files) moved under `SIM_DATA_DIR`; Spanner engines became real files and Bigtable rows persist; the Entra directory, Service Bus messages, Cosmos reachability + consistency counters, Event Grid key generations, ACI logs, Pub/Sub snapshot backlogs, Compute operations, resumable uploads, SNS/GCP/Azure signing identities, and refresh tokens all persist; EC2/Cloud Run/Compute workloads left RUNNING by a dead process settle truthfully; stale ARM operations fail instead of hanging. Each sim gained an end-to-end SIGTERM-and-relaunch restart suite. |
| DynamoDB transactional IAM resources | The call-time IAM gate derived per-item table ARNs for `TransactWriteItems`, `TransactGetItems`, `BatchWriteItem`, and `BatchGetItem` and required every referenced table to be authorized, so resource-scoped least-privilege policies worked for transactions exactly as for single-item operations. Official AWS SDK and AWS CLI regressions covered granted and denied tables. |
| Azure Container Apps PATCH and DELETE fidelity | Container apps, jobs, and managed environments applied real RFC 7396 JSON Merge Patch (recursive member merge, null deletes) through one shared helper, and their DELETEs became true ARM long-running operations: 202 with absolute `Azure-AsyncOperation`/`Location` URLs and an empty body, a `Deleting` provisioning state observable until the shared operation completes, envelope `id`, and 409 on writes during deletion. The duplicated ACA operation store and header builder were deleted in favor of the shared ARM LRO helper. |
| Google Cloud Run v2 updateMask validation | Service, worker pool, and instance PATCH handlers honored every mutable field the Discovery schema declares — the Service and RevisionTemplate models were completed to that schema — merged `template.*` sub-paths member-wise, and rejected unknown or output-only mask paths with 400 INVALID_ARGUMENT instead of silently dropping them. |
| Azure simulator cold start and federation latency | Deployment sims exposed compose healthchecks on `/health`; Caddy waits for healthy sims and answers residual proxy failures with an explicit 503 + Retry-After on every origin. OpenID Connect discovery/JWKS fetches in the federation and console-auth paths were bounded by a 10-second client on a background context, the broker reused one pooled HTTP client, and the three console SPAs deduplicated concurrent token exchanges. |
| Amazon EC2 virtual private cloud completeness | Amazon EC2 implemented all 772 operations in the vendored service model. Regional account VPC encryption modes and exclusions reconciled existing and new VPCs, endpoint payer responsibility remained durable, and PrivateLink acceptance controlled a real provider connection. The AWS console exposed account encryption controls, and official AWS SDK, AWS CLI, Terraform, and hard-restart tests passed. |
| AWS Glue completeness | AWS Glue implemented all 297 operations in the vendored service model. Business glossaries, terms, forms, asset types, assets, attachments, associations, and idempotency state were durable; entity metadata and records came from native Data Catalog/Amazon S3 or DynamoDB connection state; data-quality batch reads used real evaluation runs. `ListGlossaryTerms` emitted only its modeled `Items` and optional `NextToken` members, and entity coverage located its own DynamoDB table without assuming an otherwise empty account. The AWS console exposed business glossaries and asset types, and official AWS SDK, AWS CLI, runtime Smithy response-shape, hard-restart, package, and browser validation passed. |
| AWS SDK dependency freshness | The Lambda backend and official AWS SDK suite used Lambda `v1.101.0`; the SDK suite used IAM `v1.57.0`. The backend built and passed its tests, the complete official-client suite passed in 546.212 seconds, and the repository-wide dependency, Terraform-provider, and GitHub Actions audit was current. |
| Official AWS SDK local-suite budget | The shared Go library test target accepted module-specific flags, and the exhaustive AWS SDK suite declared a 30-minute package timeout. The complete local suite no longer inherited Go's ten-minute default while hosted CI retained its independently bounded four-shard execution. |
| Microsoft Azure delete-error dialogs | Azure Resource Manager deletion failures remained visible through concurrent Fluent UI backdrop events. Operators could still dismiss the actionable error explicitly with Cancel or Escape, and the Azure console typecheck, all 131 package tests, and production build passed. |
| AWS CLI nested-simulator isolation | Secondary process-mode AWS simulators requested their own operating-system-selected Route 53 DNS listener coordinate. Local full-shard runs no longer depended on port 5353 being unused by another simulator, and the focused official AWS CLI test plus the complete compute shard passed. |
| Amazon ECS task-role credentials | Task definitions with `taskRoleArn` received expiring task-metadata credentials registered with AWS Signature Version 4 enforcement. Real-VPC workloads used the standard ECS relative credential URI, and an official AWS CLI task called `sts:GetCallerIdentity` as the exact assumed-role principal. |
| Amazon ECS deployment fidelity | Service tasks registered their real elastic-network-interface address and port in AWS Cloud Map and reconciled discovery state on replacement, scale-in, deletion, and restart. Load-balanced deployments retained one bounded reconciliation timer while in progress, so a target that became healthy after the first probe completed without another API or task event. Launch failures persisted bounded exponential retry state; deployment circuit breakers and CloudWatch alarms drove AWS-shaped failure and rollback, including across hard simulator replacement. |
| Amazon ECS console operations | The AWS console listed ECS services and exposed desired/running convergence, deployments, events, service discovery, circuit-breaker and alarm configuration, desired-count updates, and deletion through public ECS APIs. |
| Amazon DynamoDB item limits | Item validation measured DynamoDB stored bytes—including attribute names and nested values—rather than a JSON encoding and enforced the exact 400 KiB limit across write paths. |
| AWS Secrets Manager regional replication | Secrets were Region-scoped durable records. Primary updates synchronized replicas; removal and stop-replication lifecycle transitions were enforced and persisted; the AWS console exposed primary/replica state and public replication operations. |
| AWS Step Functions generic SDK integrations | Generated Smithy bindings let generic AWS SDK Task states call AWS JSON, AWS Query, REST JSON, and REST XML services with modeled URI, query, header, payload, and response handling. |
| Amazon ECS legacy restart compatibility | A persisted `RUNNING` task with no state-scoped workload containers transitions to `STOPPED` with `EssentialContainerExited`, an explicit control-plane restart reason, and unknown exit code instead of aborting the complete AWS simulator startup. Recovery continues across every such task, container-instance counts and managed resources reconcile, and actual runtime discovery failures remain fatal. |
| Amazon ECS service execution | Amazon Elastic Container Service (ECS) services launched their declared task-definition workloads through the real task runtime, derived counts from durable tasks, replaced stopped tasks, rolled task-definition revisions within deployment bounds, honored task scale-in protection, registered and deregistered real load-balancer targets, waited for target health, and recovered the same service task after hard simulator replacement. Subnet allocation derives occupancy from live cloud resources and circularly reuses addresses released by stopped tasks. Serialized startup and stop transitions plus dependency-ordered container removal keep terminal task state, workload lifecycle, and virtual networking aligned. Restart harnesses reserve one Route 53 coordinate that is simultaneously available on TCP and UDP before launching a child simulator. Desired-count changes, Application Auto Scaling, deletion, and ECS Express Mode all drove that scheduler. Official AWS SDK and AWS CLI clients plus production-shaped HashiCorp AWS provider apply/restart/zero-plan/destroy scenarios passed. |
| AWS CLI shard budget | The hosted appdata2 job passed every test and its Smithy ratchet but was finalized as cancelled at the exact 15-minute job limit. Measured top-level time split RDS, Route 53, S3, and restart persistence from Scheduler, Secrets Manager, Step Functions, SNS, SQS, SSM, STS, and WAFv2 into appdata2/appdata3; deterministic coverage still assigns every CLI test exactly once. |
| Google Cloud specification freshness | The exact hosted Dataflow v1b3 revision 20260719 artifact and newest multi-probe API Gateway v1 revision 20260724 document replaced their older pins. Dataflow retained all 42 methods/paths and 1,174 schema field/type entries; API Gateway retained all 30 methods/paths and 143 schema field/type entries. Only descriptions and provenance advanced. |
| Terraform-in-ECS validation | The HashiCorp Terraform 1.15.8 workload image carries an ahead-of-time filesystem mirror of AWS provider 6.50.0, so its private-subnet task performs one offline initialization and one apply without undeclared internet egress. The task publishes exact output through Amazon CloudWatch Logs, and terminal Step Functions failures report it immediately; the focused execution and exact AWS SDK N-Z shard passed. |
| Filesystem staging validation | Core filesystem-driver tests force the direct extraction path to fail with a child beneath a regular file, proving staging without relying on `/usr/local` permissions that differ between local and hosted runners. |
| Dependency freshness | The repository-owned upgrade fan-out refreshed the coordinated AWS SDK patch wave, Google Cloud Spanner client, and every affected resolved transitive graph after pre-push validation detected their publication. Go 1.26 module reconciliation advanced the Microsoft Azure and Google Cloud common backends to the selected `go-isatty` 0.0.24 transitive release. A later pre-push publication advanced gRPC to 1.83.0 across both Cloud Run backends, the shared Google Cloud backend, the simulator, and its official SDK suite; every affected module and the complete official Google Cloud SDK suite passed. |
| Client-module acquisition | Google Cloud and Microsoft Azure SDK/CLI jobs pre-fetched their separate official-client modules through the bounded module-proxy retry helper before invoking `go test`. |
| Firecracker cache ownership | Simulator SDK, CLI, and Terraform matrix jobs restored the immutable Firecracker seed cache but never saved their test-mutated guest root filesystems. The dedicated Firecracker job remained the sole cache publisher, eliminating post-job permission failures on root-owned guest files. |
| Continuous-integration annotations | The Microsoft Azure workload-dispatch invariant retains its two justified `os/exec` exceptions as reviewable source comments without emitting `t.Logf` file-and-line output that GitHub's Go problem matcher misclassified as failure annotations on a successful job. |
| AWS Key Management Service policy persistence | Customer-managed key policies survived the simulator's SQLite serialization and process restart. The focused persistent-store regression passed, and the production-shaped HashiCorp AWS provider graph declared a custom policy so its create-time `GetKeyPolicy` waiter exercised the same durable read-back contract that ECS Dev Desktop required. |
| AWS DynamoDB auxiliary persistence | Table TTL, point-in-time recovery, and tags lived in a dedicated SQLite-backed settings record rather than non-serialized response fields. Store-reopen coverage proved all three survived, table deletion removed the auxiliary state, IAM resource-tag conditions read the durable tags, and the production-shaped HashiCorp AWS provider graph exercised all three API wait paths. |
| Google Cloud SQL Admin specification freshness | The exact hosted revision 20260722 v1 and v1beta4 Discovery documents were retained. Their method and route sets were unchanged; authenticated public-route coverage round-tripped the newly published `databaseCenterIntegrationEnabled`, `dmsManaged`, and top-level user `serverRoles` members. |
| Simulator persistence and statefulness | The persistent store retained cloud state that public JSON intentionally hid, rebuilt monotonic counters and derived revisions, rebound Network Load Balancing and Amazon RDS data-plane listeners, and adopted or resumed state-directory-owned Amazon ECS, AWS Batch, CodeBuild, Amplify, Lambda, scheduler, autoscaling, and Step Functions work after hard process replacement. Accepted asynchronous Lambda events were durable before `202 Accepted`, Step Functions resumed the original external task rather than duplicating it, and AWS Glue database tags survived while remaining absent from `GetDatabase`. All three cloud simulators used SQLite `synchronous=FULL`, checkpointed and truncated their WAL during orderly shutdown, closed the database, and surfaced checkpoint or close failures. The AWS server cancelled and drained Lambda event-source, CloudWatch alarm, Application Auto Scaling, and Scheduler background workers before that checkpoint/close boundary. Official AWS SDK, AWS CLI, and HashiCorp AWS provider restart scenarios passed; the production Compose recipes enabled persistent state on named volumes for all three cloud simulators. Elastic Load Balancing fixtures provisioned issued, exportable AWS Certificate Manager certificates through both official clients and used isolated listener/DNS coordinates. Durable Lambda callback recovery was observed solely through Lambda and Amazon CloudWatch Logs APIs. |
| Conformance measurement | The per-API coverage floors measured served surface instead of pattern collisions (BUG-2651). Google Cloud Spanner advanced from 58 to 82 of 198 documented methods after the REST session data plane joined the existing gRPC surface; SQL, DML, reads, transactions, partitions, and batch writes reached real handlers over both transports. Microsoft Azure remained at 1878 of 2612 documented methods, and every Amazon Web Services floor remained unchanged. |
| Simulator fidelity from measurement | Probing exposed handlers answering resource semantics for methods they do not serve — Firestore had created a document in a collection named after the method — and they now resolve the method before the resource, as Google's API frontend does. The Azure Storage data planes stopped falling through to sibling handlers (Set Blob Tier had answered `201 Created` having created a blob), which uncovered and fixed Files `?comp=range` landing on Create File, Set Queue Metadata falling to Create Queue, and missing ranged reads. The Azure Files data plane wrote where an Azure Container Apps workload bind-mounts it, replacing two implementations of which the disk-backed one was unreachable. Amazon ECS honoured the task definition's `networkMode` across all four modes, and an unresolvable VPC became an error rather than a silent fall back to Docker's default bridge. An Amazon RDS client calling `DescribeAccountAttributes` no longer received Amazon EC2's XML. |
| Deployment recipe | A committed `deploy/` hosted Sockerless Admin, the three simulators, and a Shauth + Ory Hydra + PostgreSQL stack at persistent origins: `compose.yaml` ran the published `ghcr.io/e6qu/*` images (a required `SOCKERLESS_TAG`), `compose.build.yaml` built from source, `provision.sh` registered the consoles as Shauth OpenID Connect clients and provisioned the cloud federation resources through the real APIs, and `smoke.sh` proved health, unauthenticated redirects, each data plane's enforcement contract, and a `sockerless login` authorize URL. A Caddy reverse proxy TLS-terminated every hostname (Admin and the consoles reject non-HTTPS non-`localhost` issuers) with a local CA the federated-JWT and backchannel-logout services trusted via `SSL_CERT_FILE`. The full boot → provision → smoke passed fresh and idempotent; `make deploy-smoke` is the documented manual gate (a cold five-image build exceeds the CI ceiling). |
| Azure portal federation | BUG-2640 closed: the Azure portal's Workload Identity Federation exchange moved into the console's own server-side broker (`/auth/federation/token`) because real Microsoft Entra serves no CORS for the `client_credentials` grant; the simulator gained faithful Azure Resource Manager and Microsoft Graph CORS (the token endpoint deliberately none), the SPA calls the broker on its own origin and reads the cloud cross-origin, and the relying-party harness runs the Azure console and cloud as separate processes — provisioning the console identity and federated identity credential on the cloud, then starting the console pointed at it. This unblocked the Azure browser data plane and the app-registration/client-secret minting flow deferred since the credential-minting phase; the relying-party matrix now mints an Entra client secret through the portal in a real browser. |
| Terminal sign-in | `sockerless login` (stdlib-only) ran the RFC 8252 loopback PKCE flow against Shauth — registered as a public Hydra client with the any-port loopback redirect, traversing the explicit consent screen once — and wired vendor-native credentials the vendor tools refresh themselves: an INI-preserving AWS profile (`role_arn` + `web_identity_token_file` + `endpoint_url`), a real workforce `external_account` ADC file activated through `gcloud auth login --cred-file` (which surfaced and fixed BUG-2641 — the simulator lacked STS `POST /v1/introspect`, implemented against gcloud's captured live wire), and `az login --federated-token` against a TLS-serving simulator instance (az/MSAL reject http authorities). `sockerless logout` removed what login wrote. The relying-party matrix drove the full story — browser sign-in, then `aws`/`az`/`gcloud` each verified vendor-natively, then logout. |
| Console account management | A Shauth-authenticated operator managed accounts, projects, and subscriptions through each cloud's real APIs over the federated session. Google Cloud gained a real Cloud Resource Manager slice — replacing a faked partial v3 surface that synthesized projects and operations — with v1+v3 lifecycle, Cloud Billing reads, ID-or-number resolution, 403-without-disclosure, soft-delete, and the console's real project-picker chip threading the selected project through every page; SDK, gcloud, and `google_project` Terraform coverage landed together. Microsoft Azure gained the Microsoft.Subscription alias API at vendored-Swagger fidelity (billing-scope creation + adoption, provisioning-state polling proven against both poller stacks, rename/cancel/enable) with a portal Subscriptions blade and armsubscription/az/`azurerm_subscription` coverage, running as its own `tf (azure subscription)` CI shard. AWS gained the Organizations console page (async CreateAccount polling, organization-not-in-use state, remove/close) over the existing slice, with real awsjson1.1 error codes surfaced. The relying-party matrix drove both new browser flows and passed with its exit code observed directly. |
| Console credential minting | All three simulator consoles let a Shauth-authenticated operator mint the credentials their vendor CLI needs, through the operator's federated credentials calling the cloud's real APIs — AWS IAM users + Create access key (Query protocol, browser-side SigV4, one-time secret disclosure), Google Cloud service accounts + one-time `privateKeyData` JSON key download, Microsoft Entra app registrations + Certificates & secrets (Graph `applications`/`addPassword`). Proving the loops hardened both weak token endpoints: the Google OAuth 2.0 endpoint now verifies service-account assertion signatures against registered, revocable public keys, and the Entra v2.0 endpoint validates client secrets for directory-registered applications with real AADSTS failures. The invented `/sim/v1/entra/users` routes and `entraActiveOID` global were deleted for Graph provisioning. CLI tests proved each minted credential authenticates the real vendor CLI (and wrong/tampered/revoked material is rejected); the Shauth relying-party browser matrix mints on the AWS and Google Cloud consoles with one-time disclosure asserted (the harness's federation role gained `iam:*`, which the simulator's IAM enforcement correctly required), and the full local matrix passed with its true exit code observed. The Azure portal's browser-side federation exchange is same-origin-only (real Microsoft Entra serves no CORS for `client_credentials`), so its browser minting flow is staged with the deployment-phase broker work (BUG-2640); Entra minting is proven by the Azure SDK and az CLI suites. |
| Skip-if-absent gate + sweep | `scripts/check-no-tool-absent-skips.sh` gained two shapes beyond `t.Skip` — a `fmt.Print*`/`log.Print*` tool-absent phrase and a `LookPath`-anchored `os.Exit(0)` — closing the TestMain-level silent-skip hole, and exempted skips that self-identify as platform/kernel-capability gates so a GOOS gate reading "not available" was not a false positive. Every remaining tool-absent skip resolved to install-or-fail-loud: the Google Cloud CLI suite installed a pinned release when `gcloud` was absent (the Azure CLI suite failed loud, `az` having no clean TestMain install); the six vestigial `docker CLI not available` guards (Docker already guaranteed by each TestMain's image build) were removed while the netns capability gates stayed; and the `session-manager-plugin`, `git`, `gcloud` (Firestore), and `nsenter` skips became `t.Fatal` against CI-provided tools. |
| Required-check drift gate | A version-controlled manifest (`.github/required-status-checks.txt`) mirrored `main`'s required-status-check contexts, and `scripts/check-required-status-checks.sh` (pre-commit + the `build-gates` CI job) enumerated every check name the workflows could emit — rendering each job's `name:` over its matrix — and failed the pull request when a required context was no longer emittable, so a job rename could no longer silently stall the merge queue (BUG-2633). A maintainer-only `--verify-branch-protection` mode reconciled the manifest against live branch protection. |
| Credential enforcement | All three simulators verified caller credentials on their data planes as the real clouds do (BUG-2625): AWS recomputed and verified the SigV4 signature at the awsjson/query control plane and S3 (persisting `ASIA` temporary-credential secrets, seeding a bootstrap account credential); Google Cloud and Azure verified the bearer's signature, issuer, audience, and expiry against the simulator-minted token (GCP consolidated to one RS256 key + published JWKS; Azure reused its RS256 verifier and added the audience check). Token minters, discovery/JWKS, metadata/IMDS, health, OCI registries, and the anonymous flows (`AssumeRoleWithWebIdentity`/SAML, presigned URLs, S3 public reads) stayed exempt. Every SDK/CLI/Terraform suite, the sockerless backends (their `WithoutAuthentication`/`fakeCredential` fakes replaced with a real GCP metadata token source and Azure `DefaultAzureCredential`), and the relying-party provisioning authenticated for real; the console browser e2e reached the enforcing simulator unauthenticated, so its data-render assertions moved to the authenticated relying-party path. The AWS ECS Terraform harness (BUG-2569) reaped subprocesses deterministically, so apply/destroy terminated with no leaks while services ran their real workloads. |
| Branch purpose | The cloud data-plane ratchet made Google Cloud Spanner transactional over official REST and gRPC clients; made AWS Amplify job, artifact, retry, hosting, compute, and WAF state observable through public contracts; and proved AWS Step Functions could launch an Amazon ECS task whose official HashiCorp Terraform process applied resources back through the standard global AWS endpoint. The AWS simulator retained control-plane resources—including DynamoDB TTL, point-in-time recovery, and tags—live workloads, asynchronous work, data-plane bindings, counters, and cross-service checkpoints across hard replacement, while official SDK, CLI, and Terraform clients verified recovered state externally. All three cloud simulators strengthened orderly SQLite durability, and Amazon ECS reclaimed stopped-task subnet addresses while coordinating task startup, stop, container teardown, and networking lifecycle. The AWS Batch console gained complete job and definition operation paths, all three deployed cloud simulators retained state on named volumes, Google Cloud SQL Admin revision 20260722 was implemented, and current dependency graphs, generated surface catalogs, and CI shard assignments remained checked in. |
| Product ownership | Standalone repositories owned product source, web interfaces, Terraform, official-client tests, and runner consumer harnesses. Sockerless remained a standalone Docker-compatible cloud backend and simulator project. |
| Operator identity | Admin required a Shauth administrator role, exact issuer validation, server-tracked sessions, front- and back-channel logout, and a public no-cache signed-out page. Its logout completion bridge remained reachable after local revocation and redirected only to Shauth's correlated completion endpoint. |
| Simulator identity | All three dashboards used first-party OpenID Connect authorization code with PKCE, exact issuer validation, signed server-tracked sessions, identity, RP-Initiated Logout, signed back-channel revocation, atomic replay rejection, and application-local signed-out return. Native cloud API slices remained independent of browser authentication. |
| Product-interface contract | Admin and all three simulator dashboards marked the visible account control with the exact Shauth username and the real sign-out control with a stable qualification hook. The browser matrix asserted the rendered username against each application's identity endpoint and signed out by clicking the product control, so a broken product shell failed qualification even when its protocol endpoints answered. |
| Exact browser contract | Shauth `0fda680cba964e5768ed75a9c3e5b7230c418ca6`, PostgreSQL, Ory Hydra, four freshly compiled relying parties, and Chromium passed direct and catalog entry, relying-party and provider logout, exact completion bridging, local signed-out reload and reauthentication, global revocation, anonymous fail-closed behavior, release identity, active event-stream readiness, and validator-credential isolation in eight serialized flows. |
| Production builds | The root build created every frontend bundle before compiling all 11 UI-bearing Go binaries. A repository gate rejected build-order regressions that could silently produce `noui` release binaries. |
| Continuous integration budgets | Every ordinary workflow job declared a timeout of at most 15 minutes, enforced by a tested repository gate. Lighter Go lint shards retained five-minute limits while the required simulator lint context received a measured ten-minute job and step budget after the combined simulator modules reached the former five-minute ceiling. The historically over-budget AWS edge and Amazon EC2 command-line interface groups were split into four non-overlapping shards while all 630 AWS CLI tests remained covered exactly once. The AWS SDK runner no longer scanned the whole hosted filesystem after successful tests: normal diagnostics were constant-time, disk-budget failures scanned only three relevant paths for at most five seconds each, and the workflow-budget gate rejected any future recursive `du /`. |
| Shauth relying-party browser orchestration | The real PostgreSQL/Ory Hydra/Shauth/relying-party matrix retries convergence of the same Compose stack up to four times with bounded backoff when an external registry pull fails transiently, and still fails loudly after exhausting the bound. Its harness-owned AWS relying party requests an operating-system-selected Route 53 UDP/TCP coordinate rather than contending for the host's fixed port 5353. |
| Fuzzing | The nightly harness ran targets in bounded parallel batches with one Go fuzz worker per target, preserved truthful logs and crasher handling, failed on missing modules, and passed a real one-second run across every target in all nightly modules. |
| Dependency freshness | Google Cloud Spanner used v1.94.0. Eventarc v1 Discovery revision 20260723 was current across all freshness probes and changed only its revision marker. The AWS client graph used `github.com/aws/aws-sdk-go-v2` v1.43.2 with Lambda 1.101.0 and IAM 1.57.0 across the affected AWS Lambda backend and official simulator SDK suite. HashiCorp AWS provider 6.50.0 and HashiCorp Google provider 7.36.0 passed their production-shaped graphs. Every upgraded module and both complete official SDK suites passed, and the authenticated whole-repository freshness audit reported no drift. |
| Release images | Admin and the AWS, Google Cloud, and Microsoft Azure simulators published direct native ARM64 and AMD64 OCI manifests plus an immutable short-SHA multi-architecture OCI index. Every architecture image carried its source revision as an OCI label, preventing byte-identical releases from collapsing onto one GitHub Container Registry package version, and retention preserved any version carrying a retained tag before deleting obsolete versions. |
| Open bugs | [BUGS.md](BUGS.md) retained 8 defects. AWS Amplify Hosting image optimization remained the next independent AWS fidelity slice; macOS Podman's missing nested KVM for the real Google Compute Engine Terraform graph and external-provider limitations remained explicit. |

## Verified Gates

- The complete AWS simulator module suite passed after the drain test's store isolation fix, and the focused official AWS SDK ECS Express suite passed against the ingress-complete managed security group.
- The surface-table seeder passed ShellCheck plus bash and zsh syntax validation with its pinned collation, and repeated local runs are byte-identical.

- The complete Google Cloud specification freshness audit passed all probes with Artifact Registry v1 pinned at revision 20260727; methods, paths, and schema fields were unchanged from the previous pin.
- The focused official AWS SDK ECS Express suite passed with the rollout assertion covering the scheduler's full bounded retry budget and retaining the complete last-observed service state in its failure diagnostic.

- The exact AWS Glue SDK and CLI business-glossary lifecycles passed with runtime Smithy response validation armed; a signed raw `ListGlossaryTerms` request proved its response omitted the undeclared `GlossaryId` member.
- The focused AWS Glue entity lifecycle passed while allowing other valid DynamoDB tables in its shared simulator account and still requiring its own created entity.
- A deterministic server lifecycle regression persisted a worker's final state before SQLite close and recovered it after reopen; the real AWS persistent-state scenario required its child simulator to exit cleanly before reopening the same database.
- The exact Lambda managed Python 3.12 downstream-SDK flow passed after the disk-full-corrupted runtime image was replaced, and invocation failures now retain their complete Lambda payload in assertion output.
- The exact official AWS SDK A-M shard passed the final source state in 300.747 seconds with all child simulators exiting cleanly.
- The shared simulator module passed exact golangci-lint and its complete test suite after server context creation moved beyond every fallible persistence initialization path.
- The badge-refresh PR 869 was squash-merged into PR 868's branch through GitHub, leaving PR 868 as the sole open pull request without abandoning the stacked change.
- The dual-protocol Route 53 coordinate regression and the real-container Amazon ECS hard-restart adoption scenario passed after the SDK harness selected a port available on both TCP and UDP.
- The complete Google Cloud specification freshness audit passed all probes with Eventarc v1 pinned at revision 20260723; method, resource, and schema surfaces were unchanged.
- The complete AWS simulator module, shared container-runtime suite, focused subnet-reuse regression, multi-container localhost lifecycle case, and exact hosted `TestE` compute shard passed. The shard exercised real container workloads and completed in 98.497 seconds without subnet exhaustion or orphaned task networking.
- Focused Amazon ECS task-role coverage passed an official AWS CLI workload from metadata credential acquisition through an IAM-authorized, Signature Version 4-signed `sts:GetCallerIdentity` response carrying the expected assumed-role ARN; focused metadata and Signature Version 4 unit coverage passed.
- Focused official AWS SDK and AWS CLI coverage passed Amazon ECS Cloud Map registration, task replacement, circuit-breaker rollback, CloudWatch-alarm rollback, and service reconciliation. Hard-restart regressions retained failure thresholds and retry timing and released the adopted Fargate service's network after deletion.
- The focused official AWS SDK load-balancer scenario delayed the real workload listener until after the initial target-health probe and proved the Amazon ECS deployment reached `COMPLETED` through its own recurring reconciliation.
- Amazon DynamoDB exact stored-byte boundary coverage, Secrets Manager regional replication lifecycle and persistence coverage, and generic AWS Step Functions integrations across all four supported Smithy protocol families passed focused unit, official AWS SDK, and AWS CLI suites.
- The AWS simulator console package passed TypeScript validation, all 69 unit tests, its production build, and all 243 Chromium tests with ECS service and Secrets Manager replication operations included.
- The production-shaped HashiCorp AWS provider graph passed apply, hard simulator replacement, refresh and external assertions, and destroy locally in 227 seconds after a non-destructive Podman virtual-machine restart cleared the volatile overlay fault.
- Focused Amazon ECS recovery coverage passed for multiple legacy `RUNNING` tasks without workload containers and for the fail-loud runtime-discovery path.
- An unmodified ecs-dev-desktop Terraform consumer applied 178 resources through the standard `AWS_ENDPOINT_URL`, passed its external shared-infrastructure assertions, produced a zero-change plan with runtime Smithy validation reporting no violations, destroyed all 178 resources, and retained a clean external working tree.
- The complete official AWS SDK suite passed in 487 seconds after the same-day dependency upgrade; all five official HashiCorp AWS provider packages passed apply, refresh, workload/data-plane assertions, and destroy.
- The Terraform-in-ECS proof passed a focused real-container execution and the exact AWS SDK N-Z shard with its exact provider mirrored into the private-subnet workload image and task output required through Amazon CloudWatch Logs.
- AWS DynamoDB table settings passed a SQLite close-and-reopen regression, the complete AWS simulator unit suite, focused official AWS SDK TTL/PITR/tag coverage, and the production-shaped Terraform fixture declared all three settings.
- Google Cloud SQL Admin revision 20260722 retained the same 75 public methods and paths in both versions while adding three schema fields; the complete simulator unit suite and focused authenticated public-route coverage passed their round trip.
- The complete official Google Cloud SDK suite passed, and the focused HashiCorp Google provider Cloud Spanner graph passed apply, a zero-change plan, and destroy.
- The gRPC 1.83.0 dependency wave passed both Cloud Run backends, the shared Google Cloud backend, the Google Cloud simulator, and the complete official Google Cloud SDK suite; its Cloud Build test built and pushed through a real temporary registry with leak-free container and volume cleanup.
- The shared simulator Linux test image loaded through Buildx and contained Firecracker v1.15.1 plus `unsquashfs` 4.5.1; the macOS Podman host's absent `/dev/kvm` remained explicit under BUG-2764 instead of skipping the test.
- The authenticated whole-repository dependency freshness audit reported no drift.
- The complete repository test and lint fan-outs passed.
- The complete AWS, Google Cloud, and Microsoft Azure shared simulator suites
  passed with real container-runtime access; each durability regression proved
  SQLite `synchronous=FULL`, an orderly truncate checkpoint, and preserved
  state after reopening the database.
- A clean full production build passed and embedded every current web bundle.
- The full pre-commit suite passed before final branch publication.
- The official AWS SDK hard-restart matrix retained control-plane resources, CloudWatch log sequence tokens, Amplify and Lambda callbacks, accepted asynchronous Lambda work, live Amazon ECS, AWS Batch, and CodeBuild workloads, a Step Functions synchronous Amazon ECS task without duplication, Network Load Balancing listeners, and an Amazon RDS native endpoint.
- The exact official AWS SDK compute shard passed after every HTTPS/TLS listener fixture imported issued, exportable AWS Certificate Manager material and selected an isolated live port; the nested process-mode simulator also received an explicit ephemeral Route 53 DNS coordinate.
- The official AWS CLI retained service state and CloudWatch log upload ordering across hard simulator replacement.
- The production-shaped HashiCorp AWS provider graph completed apply, hard simulator replacement on the same persistent data directory, a zero-change refresh plan, and destroy in 225 seconds.
- All 85 AWS Lambda and all 37 AWS Step Functions vendored operations passed the exact service-conformance gates.
- Official AWS SDK, AWS CLI, and Terraform suites passed against the simulator; selected AWS Lambda and AWS Step Functions flows also matched short-lived live AWS resources in an external differential.
- Official AWS SDK and AWS CLI eventing shards passed real cross-service delivery through Lambda, Step Functions, EventBridge, EventBridge Scheduler, Amazon SNS, Amazon SQS, CloudWatch, CloudWatch Logs, and CloudTrail; the official HashiCorp AWS provider production-shaped apply/destroy passed.
- The exact AWS SDK A–M shard passed ordinary ZIP invocation, durable execution, and durable callback replay through the managed Node.js runtime with zero Smithy response-shape violations.
- The complete official AWS SDK services A–M shard passed Cloud Map health-check errors, AWS Glue tag persistence, and Lambda hard-restart callback replay in 230.8 seconds with zero runtime Smithy violations; the complete AWS CLI edge-delivery shard passed real TLS listeners and its other delivery services in 284.9 seconds.
- The AWS console passed 241 Chromium package tests and the authenticated Shauth/Ory Hydra/PostgreSQL browser matrix, including real federated event delivery, Step Functions execution, CloudWatch resources, CloudWatch Logs account policies, S3-backed CloudTrail, Firehose-to-S3 delivery, AWS Private CA lifecycle, a real AWS CodeBuild container lifecycle, connected AWS Amplify branch management, and Amazon RDS authentication modification.
- The complete official AWS SDK drove AWS Step Functions Amazon ECS and AWS CodeBuild integrations and cancellation, a deployed AWS Lambda function reached Amazon SQS through standard endpoint coordinates, a private authenticated Git server produced and cached a Python plus Node.js AWS Amplify artifact, and stock PostgreSQL and MySQL drivers proved PostgreSQL, MySQL, and MariaDB authentication, password rotation, and persistence.
- The focused official AWS SDK Step Functions workload flow passed with real Amazon ECS and AWS CodeBuild containers after its shard provisioned the exact configured public images before `m.Run`, and the private authenticated AWS Amplify build restored and served its second-release cache in five consecutive real-container runs.
- The AWS, Google Cloud, and Microsoft Azure console accessibility checks focused the loaded document before keyboard traversal and passed in real Chromium, proving each skip link remained the first in-document focus target without browser-chrome focus variance.
- The exact AWS CLI appdata shard passed after CloudWatch metric streams provisioned real Amazon S3, IAM, and Amazon Data Firehose dependencies.
- HashiCorp AzureRM 5.0.0 validated the Azure Container Apps and Azure Functions modules and examples, and completed a Microsoft.Subscription apply, zero-drift plan, and destroy against the Azure simulator.
- The Azure Terraform workflow installed Ubuntu's signed Caddy package through bounded APT acquisition instead of depending on unbounded Cloudsmith downloads.
- The production-shaped AzureRM 5 stack created real subnet network fabric from `addressPrefixes` and explicitly selected `log-analytics` for its linked workspace, while the official Azure SDK round-tripped the plural subnet member and the complete Azure portal suite retained failed-delete errors in the open Fluent dialog.
- Every production Terraform module validated with its real provider after the AWS Lambda module's Step Functions conformance role used the declared regional input for all IAM policy ARNs.
- The complete Terraform tree passed recursive canonical formatting.
- The root HashiCorp AWS provider 6.50.0 graph passed its complete concurrent apply, workload assertions, refresh, and destroy through Caddy HTTPS with runtime Smithy response validation armed.
- The complete UI build and test fan-out passed, including all 131 Microsoft Azure console tests and the retained failed-deletion error dialog.
- The hosted Google specification freshness gate passed with the exact five published Discovery documents; authenticated official-SDK transports exercised Bigtable `updateMemoryLayer` and Cloud Resource Manager v3 `fetchResourceSemantics`, and the generated coverage floors measured 164 of 164 and 126 of 126.
- The official Azure SDK round-tripped a Microsoft.Storage File-share stored access policy through ARM create and update, and Azure CLI observed Microsoft.OperationalInsights' `Enabled` ingestion and query defaults on create and read.
- The external AzureRM 5 stack completed its zero-drift plan before asserting canonical Microsoft.Storage ARM IDs for its Blob container, Table, and File share.
- Microsoft Azure NAT gateways retained a subnet association that arrived before public addressing and programmed real outbound fabric when the later public-prefix update completed.
- The complete nightly fuzz target matrix passed with a one-second budget per target.
- The exact real PostgreSQL, Ory Hydra, Shauth, compiled relying-party, and Chromium matrix passed all eight application-and-direction validation runs.
- The refreshed Shauth matrix passed again after pulling the real PostgreSQL image, with bounded Compose retry and an isolated Route 53 UDP/TCP coordinate; ShellCheck plus bash and zsh syntax validation passed.

## Invariants

- The user merged pull requests; agents never merged them.
- Sockerless kept at most one open pull request and carried all related work in that branch.
- Pull-request branches were rebased on the freshest `origin/main` before publication.
- No stubs, fakes, mocks, synthetic responses, silent fallbacks, degraded modes, or skipped required tools were accepted.
- Every observed failure was fixed or recorded with evidence in [BUGS.md](BUGS.md).
