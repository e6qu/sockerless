# Sockerless - Status

Roadmap [PLAN.md](PLAN.md) - resume [DO_NEXT.md](DO_NEXT.md) - bugs [BUGS.md](BUGS.md) - narrative [WHAT_WE_DID.md](WHAT_WE_DID.md) - coverage [specs/SIM_TEST_COVERAGE_MATRIX.md](specs/SIM_TEST_COVERAGE_MATRIX.md).

## Snapshot

| | |
|---|---|
| Active branch | `feat/ratchet-up-2` — **ratchet up EC2/RDS/Glue/Lambda/Batch/API Gateway + add CloudFront (BUG-2195).** All ops spec-validated against the vendored `aws-sdk-go-v2` Smithy models (0 divergences). EC2 91→102 (VPC endpoints, AMI/placement-group/DHCP), RDS 25→40 (cluster snapshots, cluster param/option groups, read replicas, events, engine metadata), Glue 30→52 (crawlers, jobs+runs, triggers, connections), Lambda 23→37 (versions/aliases, event-source-mappings, layers, concurrency, function URLs), Batch 19→24 (job queues, job definitions, jobs), API Gateway v1/v2 (deployments/stages, UpdateRoute). CloudFront added (52/167 via REST registry). Gate now measures **~37 AWS services**. (5/6 agents were cut off mid-run by a weekly usage limit; their compiling work was completed by hand — missing Lambda tests written + 4 cut-off test bugs fixed: Glue GetTags resolves crawler/trigger ARNs, EC2 AMI deregister verified store-faithfully, EC2 DHCP JMESPath corrected, Glue CLI struct field added.) aws sim/sdk/cli build/lint(0)/unit green; contract + cli-shard + all conformance tests pass. |
| Last merged (#670) | `feat/ratchet-up-services` — **ratchet-up the floored services + measure the restJson1 services (BUG-2194)** — EC2/RDS/ElastiCache/Glue/Route53/EFS +77 ops; Lambda/Batch/API Gateway/Amplify/Scheduler measured. |
| Last merged (#669) | `feat/conformance-rest-harness` — **conformance coverage for the remaining query/REST services (BUG-2193)** — query legacy bucket (EC2/STS), REST registry (Route53/EFS), coverage-floor ratchet; gate measured ~30 services. |
| Last merged (#668) | `feat/cw-org-ssm-completeness` — **CloudWatch/Organizations/SSM op completeness + conformance-ratchet expansion to 14 services (BUG-2192)** — CloudWatch 38/49, Organizations 53/63 (full stateful tree), SSM 43/146; 114 ops. |
| Last merged (#667) | `feat/s3-ops-and-conformance-expansion` — **S3 op completeness + conformance expansion to 11 services (BUG-2191).** |
| Last merged (#666) | `feat/ecs-op-completeness` — **ECS op completeness (47 ops) + fully-qualified-naming rule + doc sweep (BUG-2190).** |
| Last merged (#665) | `feat/service-op-completeness` — **S3/REST op-coverage harness + DynamoDB/EventBridge/SNS op completeness + IAM resource-ARN derivation (BUG-2189).** |
| Last merged (#663) | `feat/iam-conformance-eventing` — **IAM policy-engine conformance system + completeness + service-initiated event delivery (BUG-2186/2187)** — the conformance gate (catalog/ratchet/golden-corpus/oracle differential) drove the IAM engine to 26/26 operators + 24/24 condition keys, and SNS/EventBridge/S3 now actually deliver events to SQS/Lambda authorized via the target resource policy. |
| Last merged (#662) | `feat/iam-resource-condition-keys` — **IAM enforcement: resource-scoped condition keys (consumer #661, BUG-2185)** — `aws:ResourceTag/<k>`, `ec2:ResourceTag/<k>`, `ecs:cluster`, `aws:RequestTag`/`TagKeys` fed into the authz context. |
| Last merged (#660) | `feat/iam-fidelity-audit` — **a 3-cloud IAM/identity fidelity sweep (BUG-2177..2184).** A per-cloud audit (AWS IAM/STS, GCP IAM, Azure RBAC/Entra/MI) drove a broad set of faithful fixes, each with SDK/CLI tests. **AWS:** the policy evaluator gained the full condition-operator set (Numeric/Date/IpAddress/Null/…), `ForAllValues`/`ForAnyValue`, policy variables (`${aws:username}`), and `Principal`/`NotPrincipal` (2178); STS `AssumeRole`/`AssumeRoleWithWebIdentity`/`GetSessionToken` + faithful `GetCallerIdentity`, with assumed-role (`ASIA…`) temp credentials resolving to the role's policies under enforcement (2179); IAM groups + group-policy inheritance, permission boundaries, `ListUsers` (2180); resource-based policies (S3 bucket / Lambda / SNS / SQS) evaluated by the gate, and **S3 REST data-plane call-time enforcement** (2181); plus resource-ARN derivation closing #657 phase 2 (2177). **GCP:** bucket-IAM etag validation+persistence, member validation, numeric SA uniqueId, predefined + **custom roles** CRUD, SA-as-resource IAM, SA enable/disable/patch (2182). **Azure:** role-assignment + built-in-role-permission + list fidelity (2183); service principals/applications Graph endpoints, MI↔SP linkage, user PATCH, and a **real RS256 MSI JWT** (2184). All three sims + sdk/cli build/lint(0)/unit green; contract + cli-shard guards pass. |
| Last merged (#659) | `feat/iam-enforcement-cosmos-partitions` — **AWS call-time IAM enforcement (#657 / BUG-2176): the gate on `POST /` resolves the SigV4 key → IAM principal → policies → deny with the per-service shape; + Cosmos partition isolation & query pagination (BUG-2175).** |
| Last merged (#658) | `feat/cosmos-compliance` — **Cosmos DB compliance: the real `azcosmos` SDK works against the sim (account-discovery `GET /` + Cosmos-vs-storage routing + ETag/`_attachments`) + a differential vs Microsoft's vNext emulator** (BUG-2174). |
| Last merged (#656) | `feat/nosql-differential-bigtable-audit` — **GCP Firestore transactions (2158) + Bigtable data plane (2159) + Firestore differential + 3-cloud audit (2171/2172/2173)**. Firestore tx (beginTransaction/commit/rollback), the `google.bigtable.v2.Bigtable` gRPC data service + RowFilter, the Firestore differential vs the gcloud emulator, and silent-swallow/invalid-input audit fixes incl. the #652 class in Azure OData `$filter`. |
| Last merged (#655) | `fix/652-close-differential` — **closed #652**: lever 5b differential testing vs Amazon's DynamoDB Local (`TestDynamoDB_DifferentialVsLocal`, with the sim-superiority `diffKnownDivergences` registry) + CloudWatch fail-loud (BUG-2170, FilterLogEvents/StartQuery reject malformed patterns). |
| Last merged (#654) | `fix/silent-incompleteness-652` — **#652 levers 1 + 2 for DynamoDB**: fail-loud condition/filter/key-condition/PartiQL evaluators (`ddbCompileExpr` → `ValidationException` for a malformed expr or undefined `#name`/`:value`, never a silent non-match; `==`/`>>` rejected) + spec-derived `@required` input-member validation (`ddbRequire` decorator + registry enforced against the vendored Smithy model). Lever 3 (lexer→AST) was already in place. |
| Last merged (#653) | `fix/cloudtrail-management-events` — **CloudTrail data-plane-into-control-plane leak: a systemic data-vs-management classification** (consumer #650/#651, BUG-2167/2168). *Registration-time* classification — each service's register fn declares its data events via `cloudTrailDeclareDataEvents`, consulted centrally; data events excluded on both client- and service-initiated paths; unauthenticated requests not recorded. Cross-service conformance guard added (#652 lever 5a). |
| Last merged (#649) | `fix/dynamodb-set-rhs-parens` — **consumer #648 + a cross-cloud NoSQL "silent-wrong evaluator" sweep** (BUG-2149..2166). Started from #648 (SET RHS doesn't strip enclosing parens — `(if_not_exists(c,:0)-:v)` and `(:z)` stored null; ElectroDB always parenthesizes `.subtract()`, so every ORM counter decrement corrupted to null; fixed with `ddbStripParens` in `ddbEvalSetRHS`/`ddbEvalSetOperand`, **2149**), then audited every cloud sim's DynamoDB-like service for the same class. **AWS DynamoDB** (2150–2153): `if_not_exists()`/bare-path copy resolve **nested** paths via `ddbResolvePath` (were top-level only); number `=`/`<>`/`IN` canonicalize (`{N:5}=={N:5.0}`); SET arithmetic + relational comparison use exact `big.Rat` (counters past 2^53 / 38-digit numbers no longer corrupt); `+`/`-` on a non-numeric operand → ValidationException. **GCP Firestore** (2154–2157): field **transforms** (`increment`/`arrayUnion`/`arrayRemove`/`serverTimestamp`) were silently dropped — now decoded + applied with `transformResults`; `currentDocument` preconditions enforced (Create/Update error semantics); typed value-equality (int 1 == double 1.0); query cursors + `select`; plus 2 fidelity bugs the high-level `firestore.NewRESTClient` surfaced (enum-as-number decode, transform-only updateMask collapse). **Azure Cosmos+Table** (2160–2166): replaced the equality-only Cosmos SQL matcher with a real recursive-descent parser (`cosmos_sql.go` — WHERE ops/AND/OR/NOT/IN/CONTAINS/STARTSWITH, nested paths, `@param` numeric unification, ORDER BY/OFFSET/LIMIT/TOP, `VALUE COUNT(1)`; unparseable → BadRequest, never all docs); Cosmos upsert+409, Cosmos PATCH; Table MERGE-vs-PUT (merge was wholesale-replace), `$filter` typed literals (`datetime'…'`/`guid'…'`/`L`), `$batch`, `$select`. SDK tests for every fix across all three clouds (+ Cosmos-SQL & update-expr fuzz); each module build/lint(0)/test green. **Staged (filed, not in this PR):** GCP Firestore transactions (2158 — needs an isolation model), GCP Bigtable data plane (2159 — entirely absent, nothing in the repo issues those RPCs yet). |
| Last merged (#647) | `fix/dynamodb-partiql-and-tail` — the remaining actionable DynamoDB items (BUG-2141..2148): **PartiQL** (`ExecuteStatement`/`BatchExecuteStatement`/`ExecuteTransaction` in new `dynamodb_partiql.go`, SDK+CLI tested + `FuzzDDBPartiQLParse`); **ConsumedCapacity**; CreateTable KeySchema validation; legacy `AttributeUpdates` ADD; nested `ProjectionExpression`; legacy `Expected`/`ConditionalOperator`; `ConsistentRead`-on-GSI rejection; `DescribeLimits`. |
| Last merged (#646) | `fix/dynamodb-completeness-safety` — 4 consumer DynamoDB bugs (#641–#644) + a completeness/safety sweep (BUG-2126..2140): TransactWrite Update action, CancellationReasons, if_not_exists arithmetic, DeleteTable purge, ScanIndexForward, Select=COUNT, big.Rat numeric-key canonicalization, nested UpdateExpression paths, 32-level nesting limit, parallel-Scan partition, contains()-on-sets, list_append, Batch/Transact limits, ReturnValuesOnConditionCheckFailure+Item, error-code consistency, +2 safety fuzz targets. |
| Last merged (#645) | `harden/systematic-fuzz-classes` — **systematic prevention of the bug CLASSES fuzzing keeps finding** (analyzed the last several rounds; the cases cluster into 6 classes — case-fold-then-slice, unchecked index/slice, integer-overflow length math, unbounded alloc, unbounded recursion, unchecked type assertions). **Delivered:** (1) shared safe primitives in the `sim` lib (3 copies) — `ASCIIFold`/`ASCIIFoldUpper`/`CaseInsensitiveIndex` (byte-length-preserving; kills case-fold-slice) + bounded `FrameReader` (Take/UintN with built-in bounds; kills slice/overflow/OOM in framed parsers); migrated CloudWatch/DynamoDB/Cosmos folds + the SSM frame decoder onto them. (2) **`forcetypeassert` linter enabled** + the whole backlog (~all type assertions across sims/core/agent/bleephub) fixed with fail-loud comma-ok. (3) **errcheck tightened** — removed the `json.Unmarshal`/`io.ReadAll`/`Decoder.Decode`/`Sscanf` exclusions + fixed every resulting site (surfaced, never silenced). (4) **`scripts/check-casefold-slice.sh`** pre-commit guard (bans the inline `Index(...ToLower...)` form → the helper). (5) **nightly exploratory-fuzz CI** (`scripts/run-fuzz.sh` + `.github/workflows/fuzz-nightly.yml`) to discover NEW crashers (the committed seed corpus already regresses known ones under `go test`). All modules lint-clean (0 forcetypeassert/errcheck), all touched tests green. (6) **Shared parser-safety core** `sim.Scanner` (bounds-safe string cursor) + `sim.ParseGuard` (depth/node budget) — the 5 recursive expression parsers (DynamoDB exprs, CloudWatch Insights filter, CloudWatch filter pattern, GCP AIP-160 filter, Azure OData $filter) migrated onto it: hand-rolled `s[i]`/`s[i:j]` cursors → Scanner, ad-hoc depth limits → ParseGuard, grammar/behaviour byte-identical (validated by each parser's full unit suite + 40s fuzz, zero crashers). (7) **CI golangci-lint bumped v2.10.1 → v2.12.2** (latest; local already there) so the new linters run in CI. Nothing deferred. |
| Last merged (#640) | `audit-round20` — 14 fixes (BUG-2112..2125): gcp-family label single-source refactor, EventBridge input engine, default-on-invalid, 3 fuzz crashes (SSM/AMQP/CW), 8 fidelity (Key Vault PATCH, EFS, RDS, ElastiCache, Kinesis, GCP Logging, Cloud DNS, SSM). |
| Last merged (#639) | `audit-fuzz-round19` — 8 fixes (BUG-2104..2111): ECR filter, SQS subset-MD5, GCP IAM/AR pagination, azf/gcf own-data fail-loud, core dead-label clean break, AMQP + iamSLRName fuzz-found DoS. |
| Last merged (#638) | `audit-fuzz-round18` — 3 fixes (BUG-2101 Cloud Build triggers.patch updateMask, 2102 Azure storage PATCH nested props, 2103 fuzz-found CloudWatch Insights multibyte panic) + the ~192 "wrong HTTP status" AWS findings rejected as FP-13 (awsJson returns 400 regardless of `@httpError`). |
| Last merged (#637) | error-path + fidelity audit (BUG-2095..2100). |
| Last merged (#637) | `fix/error-path-and-fidelity-audit` — 6 fidelity fixes (BUG-2095..2100): cbor error shapes, Cloud Run updateMask, DynamoDB Query filter / Scan limit, EC2 pagination, Pub/Sub error details. |
| Last merged (#636) | `fix/sim-validate-invalid-numeric-input` — fidelity: reject invalid/missing required numeric request params (BUG-2093) + rpc-v2-cbor error-shape fix (BUG-2094). |
| Last merged (#635) | `audit-fuzz-round17` — audit + extended-fuzz: audited surface held (every candidate a verified false positive); **+11 fuzz targets** for previously-uncovered untrusted-input parsers; zero crashers. |
| Last merged (#634) | `feat/ecs-express-mode` — **full AWS ECS Express Mode support in the AWS sim** (the service AWS launched 2025-11-21). Researched from the AWS API reference, ECS developer guide, the vendored `aws-sdk-go-v2/service/ecs@v1.85.0` (exact enums/shapes), and terraform-provider-aws (`aws_ecs_express_gateway_service`, v6.23.0+) — no guessing. Implements the 4 ops `Create/Describe/Update/DeleteExpressGatewayService` (awsJson1.1) with the exact request/response shapes, enums (accessType PUBLIC/PRIVATE, autoScalingMetric AVERAGE_CPU/AVERAGE_MEMORY/REQUEST_COUNT_PER_TARGET, statusCode ACTIVE/DRAINING/INACTIVE), defaults (cpu 256/memory 512/healthCheckPath /ping/port 80/scaling target 60), and errors. **Full faithful cloud-slice assembly:** each Express service composes the REAL underlying sim resources — an ECS Fargate service + an ELBv2 ALB (internet-facing/internal) + target group + HTTPS:443 listener + ACM cert + EC2 security group + an Application Auto Scaling scalable-target + target-tracking policy — all describable via their own sim APIs; the AWS-provided domain = the ALB DNSName; the 25-services-per-ALB consolidation and the DRAINING→INACTIVE teardown cascade are implemented. SDK + CLI + Terraform tests (contract hook green, all 3 dims); the underlying Describe handlers were audited and confirmed faithful (no gaps). **BUG-2088** captures 3 issues testing surfaced (cluster returned as ARN not bare name; auto-scaling always provisioned even at defaults; Delete persists INACTIVE so the TF destroy-waiter converges). New doc `docs/ECS_EXPRESS_MODE.md` (13-row Express-vs-vanilla-ECS comparison) cross-linked from README/docs-index/CLOUD_RESOURCE_MAPPING/SIM_SURFACE_TABLES/BACKENDS/POD_MATERIALIZATION. **Real upgrades, no workarounds (PR #634, CI green):** CI installs the latest aws CLI v2 so the ECS Express Mode command-line interface (CLI) tests run for real — whole aws cli-test suite drift-verified against v2.35.9, clean (**BUG-2091**); the pinned `ecs.smithy` spec snapshot re-vendored to the latest aws-models so Express `taskDefinitionArn` validates, no allowlist (**BUG-2090**); and a **skip sweep** rewrote + CI-enabled bleephub's broken-and-never-run postgres persistence test (a `postgres:16-alpine` service in `test-core`) — the other ~34 skips are legitimate platform/capability gates that run in CI (**BUG-2092**). aws sim build/lint/test clean; hooks pass. |
| Last merged (#633) | `audit-fuzz-weaktypes-round16` — weak-types + deep-fuzz + fidelity sweep (25 fixes). |
| Last merged (#632) | `audit-fuzz-round15` — service-by-service audit + fuzz (security DoS, auth leaks, ~36 fixes). |
| Last merged (#632?) | `audit-fuzz-round15` — 5-agent sim/bleephub/shared audit (~36 bugs incl. OCI zip-bomb, WS read-limit, sandbox traversal, bleephub auth leaks, SFN recursion, ~8 races, ~15 list-filter fidelity gaps). |
| Last merged (#631) | `fix/sim-filters-629-630` — ListSecrets Filters + ListTaskDefinitions sort/status (consumer #629/#630). |
| Last merged (#628) | `fuzz-soak-audit-round14` — tokenizer hang, SQLite-busy panic, log-follow panic + fuzz/soak regression suites. |
| Last merged (#627) | `audit-round13` — docker inspect State-drop + converters, REST fidelity, ACR coordinate, CI guard false-negatives. |
| Last merged (#626) | `audit-round12` — core crash + leaks, sim aliasing/realexec, UI fail-loud, harness/api. |
| Last merged (#625) | `fix/audit-round11-everything` — the prior fresh audit (agent, bleephub, GCP/Azure backends, dispatcher, CLI). |
| Last merged (#624) | `fix/backlog-sweep-round10` — cleared the entire prior fixable backlog (1950/1952/1953/1954). |
| Last merged (#623) | `fix/poller-hang-retention-round9` — WaitForExit hang on a vanished cloud resource + gcp log cap. |
| Last merged (#622) | `fix/concurrency-round8-sweep` — cleared the 1937-1941 follow-ons + a fresh sweep (SQS/SNS/IAM/SFN/Lambda validation + ecs/lambda attach leak). |
| Last merged (#621) | `fix/sim-validation-618-619` — ECS request validation (#618) + Scheduler expression validation (#619). |
| Last merged (#620) | `fix/flake-sweep-round7` — CI flake (realexec nft table races, comprehensive) + parser stack-overflow guard (P1) + OCI chunked-upload atomicity. |
| Last merged (#617) | `fix/docker-inspect-faithfulness` — Mounts (Cloud Run/ACA/gcf) + azf Cmd/Env + attach stdin-pump leak. |
| Last merged (#616) | `fix/concurrency-engine` — per-container ContainerStart lock (1932) + follow-stream goroutine-leak fix (1931) + 1928 reclassified FP. |
| Last merged (#615) | `fix/sweep-concurrency-faithfulness-ui` — fresh sweep: P1 cloudrun livelock, 2 data races, CPU/Memory faithfulness across all backends, ACA ExposedPorts, gcf Cmd/Env, 5 UI fail-loud gaps. |
| Last merged (#614) | `fix/behavioral-audit-round1` — 20 behavioral-audit fixes (error-semantics, async state machines incl. the full SFN ASL interpreter, optimistic concurrency, GCP compute-op registry, backend cleanup logging). |
| Last merged (#613) | `docs/landscape-comparison` — `docs/LANDSCAPE.md` (sockerless vs LocalStack/moto/official emulators/2026 wave, full service-by-service matrix) + spec-fidelity audit baseline (AWS 0 / GCP 2 allowlisted / Azure 0 divergences). |
| Last merged (#611) | `feat/cloudwatch-dashboards-percentile-insights` — two consumer CloudWatch issues: **BUG-1900 (#608)** dashboard API (Put/Get/List/Delete) on all 3 protocols; **BUG-1899 (#609)** alarm `ExtendedStatistic` percentile round-trip on all 3 protocols + percentile state eval. SDK + CLI + terraform. #608 auto-closed; #609 still open (manual-close pending). |
| Last merged (#610) | `feat/sim-fidelity-pass-6` — sixth fidelity pass (BUG-1887–1894: EC2 SG/ENI/NAT filters, EventBridge KMS, Cloud Map real health, Lambda RepositoryType, ECS launchType, CloudWatch startFromHead, SQS attrs+MD5, DynamoDB projection, GCP/Azure list filter/orderBy/$top) **plus a full query-language program** (BUG-1895–1898: GCP AIP-160 `filter`, DynamoDB Condition/Filter/KeyCondition expressions, CloudWatch Logs filter pattern, Azure OData `$filter`) — each a real recursive-descent parser+evaluator, unit-tested. |
| Last merged (#607) | `feat/aws-sim-cloudwatch-snapshot-602-604` — **five** AWS-sim fidelity issues (#602–#606, all observability): CopySnapshot, CloudWatch alarms (3 protocols + live state eval + tagging), EMF extraction, FilterLogEvents `logStreamNamePrefix`, CloudTrail `LookupEvents` stable cursor. SDK + CLI (+ TF where applicable). All five GitHub issues closed. **BUG-1885 (#605):** CloudWatch Logs `FilterLogEvents` ignored `logStreamNamePrefix` → leaked every stream's events; added prefix selection + the mutual-exclusion error. **BUG-1886 (#606):** CloudTrail `LookupEvents` paged via an absolute offset over a head-growing list → duplicate/overlapping pages; the `NextToken` is now an opaque `(EventTime, EventId)` cursor (snapshot-consistent), and `LookupEvents` reads are no longer recorded into the trail (real CloudTrail doesn't log them). **BUG-1882 (#602):** EC2 `CopySnapshot` → was `InvalidAction`; new `handleCopySnapshot` duplicates the source's backing data into a new snapshot id (the cross-region EBS DR primitive). **BUG-1883 (#603):** CloudWatch metric alarms were entirely unimplemented; new `cloudwatch_alarms.go` adds `PutMetricAlarm`/`DescribeAlarms`/`DeleteAlarms` on all three CW wire protocols (query/awsJson/cbor) sharing one store, with live `StateValue` evaluation (OK/ALARM/INSUFFICIENT_DATA) from the metric data + `TreatMissingData`, plus cbor alarm tagging for the `aws_cloudwatch_metric_alarm` provider's read path. **BUG-1884 (#604):** EMF (embedded metric format) log events weren't extracted into metrics; `extractEMFMetrics` parses `_aws.CloudWatchMetrics` blocks in `PutLogEvents` and feeds the existing `cwMetrics` store (the ECS/Fargate EMF-over-stdout path). Each has SDK + CLI + terraform coverage. |
| Last merged (#601) | `fix/aca-exposedports-cloudstate` — **BUG-1879**: aca `appToContainer` (cloud-state reconstruction) dropped `Config.ExposedPorts` (stateless cloud-is-source-of-truth divergence); persisted as a `sockerless-exposed-ports` tag + reconstructed. Also **BUG-1880** (CI: kill-safe `apt-get update` retry helper — update runs no dpkg txn so it's safe to `timeout`+retry, unlike install) and **BUG-1881** (CI: `--retry` on the Firecracker S3 asset downloads, transient 503). Verified the already-merged aca GitLab cell green by running the real harness; streamlined stale DO_NEXT framing. |
| Last merged (#600) | `fix/audit-round5-concurrency` — a FIFTH audit pass (concurrency/race correctness, resource leaks, shared-sim-copy divergence) + CI hardening. **Fixed:** BUG-1875 (concurrency batch: `core` reverse-agent `OnDroppedMessage` set-after-readLoop race + `execForOutput` timeout buf race + `DropSession` closing the new live session on re-dial; `cloudrun` `exitCh` double-close panic; `agent` per-conn `mp.Done()` watcher goroutine leak), 1876 (azure `eventgrid` leaked an `http.Post` body per subscriber/event), 1877 (shared-copy divergence: AWS's `RegisterUI` panic-guard + 5xx error-body logging ported to GCP/Azure `shared/`), 1878 (CI: Playwright apt/download split + job `timeout-minutes` on all 25 jobs + apt `Acquire` retry/timeout so a degraded mirror fails-fast instead of hanging 6h). All `-race`-clean. |
| Prev (PR #599, MERGED) | `fix/audit-round4-ui` — deep UI pass + sim emitted-URL + harness scripts (BUG-1872/1873/1874 + a spawn-runner FP). |
| Prev (PR #598, MERGED) | `fix/audit-round3-deferred` — the 3 items deferred from #597. **BUG-1869 (sqlite integrity):** the shared sim `SQLiteStore` `Put`/`Update`/`Get`/`List`/`Len`/`Delete` swallowed every DB error → now panic on an unexpected DB error via `fatalDBErr` (net/http recovers → 500; loud, doesn't nuke the sim), `sql.ErrNoRows` stays the legit miss; all 3 shared copies + regression test `TestSQLiteStoreFailsLoudOnDBError`. **BUG-1868 (fabricated zero stats):** `buildStatsEntry` now returns `(map,error)` — `NotImplementedError` when no `StatsProvider`, propagates the provider error; the 4 FaaS/container backends stop reporting a live container at 0% CPU, ECS keeps real metrics. **BUG-1870: FALSE POSITIVE** — the Spanner `/spanner/` prefix is a *documented, load-bearing* collapsed-port disambiguation (Cloud SQL owns the canonical `/v1/projects/{project}/instances` via `registerCloudSQLPrefix(srv,"/v1")`; serving Spanner there would ServeMux-panic). Verifying before "fixing" caught it; reclassified as FP. |
| Prev (PR #597, MERGED) | `fix/audit-round3` — a THIRD audit pass over the shared `sim` library, per-cloud `*-common`, cloud backend impls, CLI + harnesses (BUG-1860–1867 fixed + BUG-1871 flaky-CI fix). **Fixed (each verified at file:line, tested per module):** BUG-1860 (ecs `deriveMACFromIP` fabricated a MAC → read the real ENI `macAddress`), 1861 (ecs Kill/Remove dropped the `StopTask` error → orphaned billable task), 1862 (ecs `fargateResources` oversized request → smallest tier; now clamps to max), 1863 (ecs+lambda `ListImages` truncation swallow), **1864** (azure-common hardcoded `<acr>.azurecr.io` + blob host, ignoring `SOCKERLESS_AZURE_ACR_ENDPOINT` — BUG-1789 class 3rd instance; routed through a new `AzureRegistryHost` coordinate helper + blob `sourceURL` from the discovered endpoint, regression test), 1865 (shared-sim `process.go` `StdoutPipe`/`StderrPipe` swallow → nil-reader scan panic; ×3 copies), 1866 (cloudrun+aca Stop/Kill orphan swallow — `*Strict` delete), 1867 (CLI fabricated success-on-parse: status `UP`, context `Reloaded`, admin project-drop). **Filed open** (deferred, fix-shapes in BUGS.md — each needs its own careful change): 1868 (4 backends fabricate zero stats — no `StatsProvider`), 1869 (shared-sim `state_sqlite` `Put`/`Update`/`Get`/`List`/`Len` swallows, all 3 sims), 1870 (Spanner non-canonical `/spanner/` path). **Also BUG-1871 (boy-scout):** the `sim (bleephub e2e)` job's flaky "cancelled" was a Playwright-install hang (9.1 min) on a 10-min job timeout — fixed with a browser cache + bounded-retry install + timeout headroom, not just a re-run. |
| Prev (PR #596, MERGED) | `fix/audit-round2-swallows` — a fail-loud / no-swallow / no-fake / no-dead-code audit (5 parallel agents over `agent/`, the github-runner dispatchers, `bleephub/`, `bleeplab/`, `core/`+`realexec/`). **Fixed (each verified at file:line, tested per module):** BUG-1853 (P1 — gcp github-runner dispatcher `it.Next()` loops conflated a real API error with `iterator.Done` → a transient blip reaped a RUNNING runner Job / its pod-Services; now `UNKNOWN`/propagate like the Azure twin), 1854 (agent bootstraps — PostExec sync-volume data-loss + parseUserArgv decode + azf body-read swallows now loud), 1855 (core build COPY staging swallowed fs errors → fake `Successfully built`; + docker-import ImageTag swallow + a dead pin), 1856 (bleeplab runner-verify fabricated `id:0` + job-trace read swallow advancing a durability ack), 1857 (bleephub two dead GraphQL enum silencers wired faithfully + a dead token lookup), **1858** (bleephub `viewerPermission` now computed from the viewer's real access via the rbac helpers, not a hardcoded ADMIN — regression test added), **1859** (the minor batch: bleeplab `readJSON` ×3 fail-loud + git EOF `errors.Is`, agent azf `jobTimeout` malformed→default, core `image_manager` loud-log on skipped local populate). `realexec` audited clean. One fat PR — all 7 findings fixed, none deferred. |
| Prev (PR #595, MERGED) | `fix/audit-backlog-deferred` — the 3 items deferred from #594. **BUG-1840 DONE + tested:** removed the sim-only `Sim*` fields — gcp `cloudfunctions.go` (`SimCommand`/`SimImage`/`SimArchitecture` + the backend-dead image-less Sim branch + its `Sim*`-only SDK tests; the gcf backend invokes `svc.Uri`→`/v2-services-invoke`, never `/v2-functions-invoke`, so the cell path is untouched), azure `functions.go` (`SimCommand` fallback + the now-identity `Site.wire()`; kept the real `invokeAzureFunctionProcess` the azf backend drives via `LinuxFxVersion`+`SOCKERLESS_CMD`). Invoke SDK tests re-grounded on the real `SOCKERLESS_CMD` contract (azure) / removed in favour of Cloud Run **Jobs** arithmetic + the gcf cell (gcp). Sub-fix: the three sim sdk-tests' `buildGoScratchImage` now `buildx build --load` (BUG-1824 class — was 500ing eval workloads locally on a buildx-default host). **BUG-1845 DONE + cell-verified:** cloudrun `networkServices` map now rebuilt from cloud after a restart — `buildServiceSpec` persists the network's service-style members (`sockerless_network_id` + `sockerless_network_service_members` annotations) on each revision; `serviceMembersOfNetwork` reconstructs the map on a cache miss + re-seeds PendingCreates (guarded so a service-less network doesn't re-list every stage). Unit test `TestNetworkServiceMembers_PersistAndRebuild`; green `services:` path verified by the cloudrun cell. **FaaS pod polish — already delivered in #584, not a remaining item:** the azf shared-workspace volume (named volume → site-level Azure Files share mounted by every sitecontainer) + per-sidecar exec routing (each overlaid sidecar registers its own reverse-agent keyed by container ID) are implemented and proven by `TestAZFMultiContainerPodSharesLocalhost` (re-run green on current code); the PLAN § FaaS "follow-on polish" line + #594's deferral of it were stale references — corrected. PR #595 opened. |
| Last merged (#594) | **Audit backlog: 5/7 fixed** — BUG-1846 (default-param fail-loud + dead code), 1844 (backend error-swallow batch — PodRemove ×4 / aca NetworkRemove / core ContainerWait / ecs stats / lambda pod-row), 1843 (gcp fingerprint per-write + 412 optimistic concurrency), 1841 (aws Cloud Map DNS via `AWS_INSTANCE_IPV4`), 1842 (aws default-VPC reframed faithful + `defaultVPCSubnetID()`). Deferred 1840/1845/FaaS-pod-polish with refined fix-shapes. |
| Prev merged (#593) | **Codebase audit for fallbacks / error-swallowing / fakes / sim-contract violations / dead code** (sims → backends → UIs) + fixable open GitHub issues. **Contained fixes** (BUG-1836/1837/1838 azure-sim swallows+dead-vars, 1839 cloudrun fabricated exit-0, 1847 gcp Cloud Build buildx-load, 1848/1849/1850 = issues #591/#592/#590 with SDK tests, 1851/1852 UI fail-loud). Closed already-fixed issues #583/#569; #590/#591/#592 auto-closed on merge. **GitHub issue board: only #394 (azuread upstream-blocked) open.** |
| Last merged (#593) | **Codebase audit for fallbacks / error-swallowing / fakes / sim-contract violations / dead code** (sims → backends → UIs) + fixable open GitHub issues. **Contained fixes** (BUG-1836/1837/1838 azure-sim swallows+dead-vars, 1839 cloudrun fabricated exit-0, 1847 gcp Cloud Build buildx-load, 1848/1849/1850 = issues #591/#592/#590 with SDK tests, 1851/1852 UI fail-loud). **Larger findings staged OPEN with fix-shapes** (BUG-1840 gcp+azure sim `Sim*` fields contract violation; 1841 aws cloudmap sockerless-tag DNS; 1842 aws ec2 sockerless seeding; 1843 gcp fingerprint; 1844 backend swallow batch incl. PodRemove ×4; 1845 cloudrun `networkServices` stateless violation; 1846 backend default-param + dead-code batch). Closed already-fixed issues #583/#569; #590/#591/#592 auto-closed on merge. **GitHub issue board: only #394 (azuread upstream-blocked) open.** |
| Last merged (#589) | **GitHub `actions/runner` cells on aca + azf — both GREEN** (all 14 bleephub harness tests, incl. container-mode job, `services:` container, dispatcher-spawned runner) → GitHub+GitLab runner parity on every container-capable backend. aca was already wired and stayed green after #587; azf was newly wired into the bleephub harness (Dockerfile builds `sockerless-backend-azf` + `sockerless-azf-bootstrap`; `provision_azf` + case arm in `run-integration.sh`; `bleephub-runner-docker-test-azf` Makefile target). Two real bugs surfaced + fixed: **BUG-1834** the azure-sim ACR-Tasks overlay build hardcoded the buildx-only `--load`, which the harness's legacy `docker.io` builder rejects → `container:` job `ContainerCreate` 500 (now probes `docker buildx` and only `buildx build --load` when present, else plain `docker build`; the chosen path is logged); **BUG-1835** azf cloud-dns `startCloudDNSSite` keyed overlay-vs-raw on `OpenStdin` (a gitlab-runner-only signal), so the exec-driven GitHub `container:` job was deployed raw (no agent → exec exit 126) and a `services:` container was deployed as the overlay (workload never ran) — now keyed on a `serviceLike` marker derived from the ORIGINAL client request (recorded pre-image-merge). |
| Prev merged (#588) | **azf cloud-dns hardening.** (1) azf `NetworkConnect` registers `--network-alias` on connect-after-create — a PendingCreate gets the network+alias stamped (so `ContainerStart`'s cloud-dns site deploy VNet-integrates + registers it), and a live site gets VNet-integrated + its aliases written as Private DNS CNAMEs now (the synthetic network driver writes only `Store.Containers`, which the stateless backend ignores, so without this a post-create connect was lost). (2) Completed the App Service swift-VNet-integration testing contract — CLI (`az rest` round-trip) + Terraform (`azurerm_app_service_virtual_network_swift_connection` + a `Microsoft.Web/serverFarms`-delegated subnet, apply/idempotency/destroy) joining the SDK test from #587. Three sim fidelity bugs surfaced + fixed: **BUG-1833** the swift response returned its `id`/`type` from the operation path (`networkConfig`) instead of the canonical config sub-resource (`config/virtualNetwork`), so the azurerm provider's apply failed `ID was missing the 'config' element` (the #587 SDK test missed it — asserted only `subnetResourceId`; SDK+CLI now assert the `id` too); **BUG-1832** subnet delegation dropped the `actions` array (azurerm idempotency drift); **BUG-1831** swift PUT force-started a container for any non-HTTP site, 500ing an imageless function app's VNet integration (now gated on the site actually having a container image). |
| Prev merged (#587) | **bleeplab GitLab cells GREEN on aca + azf** (BUG-1828 closed). The full GitLab docker-executor flow (build → artifact → redis `services:` PING/SET/GET) runs on **both** Azure backends. **aca:** runner stages over HTTP buffered-invoke to a **faithful ACA ingress** (`registerContainerAppsIngress`, Host=App-FQDN → replica `targetPort`) + a backend WS keepalive fixing a real all-FaaS infinite-hang. **azf:** persistent App Service site container, `ContainerStart` CloudState-fallback + re-invoke, OpenStdin-running CloudState, stdin-attach precedence (attach strategy), Azure Files share mounts, ExposedPorts — capped by **faithful cloud-dns service discovery** (VNet + `Microsoft.Web/serverFarms`-delegated subnet + App Service swift VNet integration + linked Private DNS zone; the sim realizes a Web-delegated subnet as a Docker network, attaches the site on swift integration, realizes a CNAME→site-FQDN as an embedded-DNS alias). Same backend code vs sim and real Azure, no sim-awareness. Also: AWS sim ECS CPU/Mem enforcement (#583) + process-mode EBS hardening (#569); CI flake fix (BUG-1830, AWS SDK build split into its own step). Bugs closed: 1815–1830. |
| Prev merged (#586) | bleeplab GitLab cell on the Cloud Run Functions (gcf) backend — GREEN (BUG-1811 ContainerStart CloudState fallback, BUG-1812 stdin-attach precedence, BUG-1813 `/bin/sh` stage, BUG-1814 persist-restore-before-invoke; arch via `gcpcommon.ArchFromPlatform`). |
| Prev merged (#585) | bleeplab GitLab cell on the Cloud Run backend — GREEN (BUG-1808 hardcoded arch, BUG-1809 AR `gitlab-registry` pull-through, BUG-1810 sim-in-container reaches workload by bridge IP). |
| Prev merged (#584) | AZF pod polish (shared volume + per-sidecar exec) + bleeplab artifact UI + flaky-test sweep (BUG-1806) + server ReadHeaderTimeout (BUG-1807). |
| Prev merged (#582) | FaaS multi-container pod assembly (BUG-1781): lambda+gcf already delivered; azf assembled via App Service sitecontainers. |
| Prev merged (#581) | Full gitlab-runner `services:` on the bleeplab GitLab ECS cell (BUG-1804 Cloud Map one-instance-many-DNS-names + BUG-1805 dropped the ECS resolv.conf wrapper). |
| Prev merged (#580) | bleeplab dashboard UI (GitLab-themed) — completed bleephub parity (git + artifacts + UI). |
| Prev merged (#579) | bleeplab object-store-backed CI artifacts (cross-stage passing) + BUG-1803 (azf attach stdin-capture race). |
| Prev merged (#578) | The single-job bleeplab GitLab ECS cell GREEN: **BUG-1801** bleeplab serves each project as a real git repo over smart-HTTP (object-store-backed go-git), `GIT_STRATEGY: clone` materializes `CI_PROJECT_DIR`; **BUG-1802** the ECS backend reconstructs a container's `HostConfig.Binds` from the task def's mount points on restart so gitlab-runner's per-stage helper restarts keep the EFS `/builds` mount. |
| Last merged | #577 BUG-1800 EFS access-point writability. #576 BUG-1798 ECS gitlab attach-stdin. #575 bleeplab ECS harness (BUG-1797, BUG-1799). #574 bleeplab GitLab sim. |
| Open GitHub issues | #394 azuread Terraform Graph override — upstream-blocked (BUG-1345). |
| Bugs | See [BUGS.md](BUGS.md) header (2092 filed · 2048 fixed · 2 open · 12 FP). 2 open: BUG-1075 (live-cloud), BUG-1345 (azuread upstream) — both externally gated. |
| Live infra | None up. |

## What's next

Ordered continuation plan (full detail in [PLAN.md](PLAN.md) § Next; resume steps in [DO_NEXT.md](DO_NEXT.md)):

- **GitLab docker-executor arc — COMPLETE.** The full build → artifact → `services:` flow is GREEN on every cloud backend: ECS (#578/#581), Cloud Run (#585), GCF (#586), ACA + AZF (#587). FaaS multi-container pod assembly (BUG-1781) done.
- **A. Harden the merged cloud-dns work — DONE (merged #588).** Swift-VNet-integration testing contract completed (CLI + Terraform, joining the SDK test from #587); azf `NetworkConnect` registers `--network-alias` on connect-after-create (PendingCreate stamp + live VNet-integrate/CNAME). Surfaced + fixed three sim fidelity bugs (BUG-1831/1832/1833).
- **B. GitHub `actions/runner` cells on aca + azf — DONE (merged #589).** Both backends green on the full bleephub container-job topology (container job + `services:` + dispatcher). GitHub+GitLab parity reached on all six container-capable backends.
- **C. Standing (NEXT)** — live-cloud pass (BUG-1075, biggest open gap), releases (#363), fresh sim fidelity audits. Also pending: a dedicated codebase sweep for fallback / error-swallowing / functionally-dead-code patterns (user guidance 2026-06-17 — fail loudly, no contract-breaking sim fallbacks, avoid defaulted behaviour).

All four container backends (ECS, ACA, Cloud Run, GCF) are sim-proven for the full GitHub container-job topology; GitLab control plane now exists (bleeplab) and runs jobs with a real gitlab-runner — the full GitLab docker-executor flow (build → artifact → `services:`) is GREEN on **ECS, Cloud Run, and GCF** (aca next).

## Invariants

- Never auto-merge PRs; the user handles merges.
- **At most one PR open at a time** — put all work in the single in-progress PR; never open a new one while one exists. If two ever exist, **consolidate** their work into one (merge the branches together) — do not evade the rule. Closing a PR *without merging it* abandons and deletes that work for good; it is never a way to park work or dodge the rule. Enforced by `scripts/check-single-open-pr.sh` (pre-commit + the `single-open-pr` CI job).
- Rebase PR branches on `origin/main` before pushing; sync local `main` after.
- File a concrete `BUGS.md` entry before fixing a discovered defect.
- No stubs, fakes, mocks, synthetic responses, silent fallbacks, or degraded modes (see [AGENTS.md](AGENTS.md)).
- Simulators implement real cloud-API slices, one binary per cloud; every public endpoint ships with official SDK + vendor CLI + Terraform coverage where those surfaces exist.
- External identity stays GitHub/GHES-shaped (public paths, fields, `GITHUB_*` vars, runner contract, client-facing UI text); bleephub-specific names only for internal code or operator-only surfaces.
- Coverage authorities: [specs/SIM_TEST_COVERAGE_MATRIX.md](specs/SIM_TEST_COVERAGE_MATRIX.md) and [specs/SIM_SURFACE_TABLES](specs/SIM_SURFACE_TABLES).

## Environment notes

- Simulator ports: AWS 4566, GCP 4567, Azure 4568.
- AWS and GCP Terraform providers accept localhost custom endpoints directly; AzureRM needs HTTPS through the local Caddy gateway (`make stack-https-{up,status,ca,down}`). Azure Terraform tests are Docker-only.
- Linux network-fabric tests require `CAP_NET_ADMIN` + iproute2 + nftables; off-Linux they skip through the realexec capability gate.
- Local bleephub runner topology harness: `make bleephub-runner-docker-test` (ECS) / `make bleephub-runner-docker-test-aca` (ACA); self-contained, mounts docker.sock + a sim-storage host dir. `BLEEPHUB_BACKEND` selects the backend; `BLEEPHUB_TEST_FROM` skips to a test; `BLEEPHUB_HOLD=1` freezes the stack on failure. The one harness image bundles the aws + azure sims, backend-ecs + backend-aca, and the cloudrun-bootstrap.
