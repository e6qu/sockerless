# Standardized Makefile for the sockerless CLI — see docs/MAKEFILE_STANDARD.md.

APP_NAME      := sockerless
GO_PACKAGE    := .
GO_ENV        := CGO_ENABLED=0

REPO_ROOT_REL := ../..

include $(REPO_ROOT_REL)/make/go-app.mk
