# Standardized Makefile for sockerless-admin — see docs/MAKEFILE_STANDARD.md.

APP_NAME      := sockerless-admin
GO_PACKAGE    := .
UI_PACKAGE    := admin
DEFAULT_PORT  := 9090
RUN_FLAGS     := --addr :9090

REPO_ROOT_REL := ../..

include $(REPO_ROOT_REL)/make/go-app.mk
