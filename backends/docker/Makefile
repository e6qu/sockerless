# Standardized Makefile for sockerless-backend-docker — see docs/MAKEFILE_STANDARD.md.

APP_NAME      := sockerless-backend-docker
GO_PACKAGE    := ./cmd
UI_PACKAGE    := backend-docker
DEFAULT_PORT  := 3375
RUN_FLAGS     := --addr :3375

REPO_ROOT_REL := ../..

include $(REPO_ROOT_REL)/make/go-app.mk
