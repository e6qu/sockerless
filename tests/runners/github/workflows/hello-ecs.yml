name: hello-ecs
on:
  workflow_dispatch:
jobs:
  hello:
    runs-on: [self-hosted, sockerless-ecs]
    container:
      image: alpine:latest
    steps:
      - run: echo "hello from sockerless"
      - run: env | sort
