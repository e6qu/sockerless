test-with-service:
  image: alpine:latest
  services:
    - name: web
      alias: web
      entrypoint: ["sh", "-c", "mkdir -p /www && echo ok > /www/index.html && httpd -f -p 8080 -h /www"]
      command: [""]
  script:
    - sleep 2
    - wget -qO- http://web:8080/ | grep -q ok
